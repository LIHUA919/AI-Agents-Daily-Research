<div id=toc></div>

# Table of Contents

- [cs.AI](#cs.AI) [Total: 139]
- [cs.LG](#cs.LG) [Total: 228]
- [cs.MA](#cs.MA) [Total: 7]


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [1] [Understanding Annotator Safety Policy with Interpretability](https://arxiv.org/abs/2605.05329)
*Alex Oesterling,Donghao Ren,Yannick Assogba,Dominik Moritz,Sunnie S. Y. Kim,Leon Gatys,Fred Hohman*

Main category: cs.AI

TL;DR: APMs learn interpretable models of annotators' internal safety policies from labeling behavior alone, enabling analysis of disagreement sources without additional annotation burden.


<details>
  <summary>Details</summary>
Motivation: Understanding why annotators disagree on safety labeling is crucial but difficult. Disagreement can stem from operational failures, policy ambiguity, or value pluralism, each requiring different interventions (quality control, policy clarification, or deliberation about diverse perspectives). Current approaches of directly asking annotators are costly and unreliable.

Method: Introduces Annotator Policy Models (APMs) - interpretable models that learn annotators' internal safety policies from their labeling behavior alone. APMs make annotator reasoning visible and comparable without requiring additional annotation effort.

Result: APMs accurately model annotator safety policy (>80% accuracy), faithfully predict responses to counterfactual edits, and recover known policy differences in controlled settings. Applied to LLM and human annotations, APMs successfully surface policy ambiguity (different interpretations of safety instructions) and value pluralism (systematic differences in safety priorities across demographic groups).

Conclusion: APMs provide a powerful tool for understanding annotation disagreement, enabling more targeted, transparent, and inclusive safety policy design by distinguishing between different sources of disagreement without additional annotation burden.

Abstract: Safety policies define what constitutes safe and unsafe AI outputs, guiding data annotation and model development. However, annotation disagreement is pervasive and can stem from multiple sources such as operational failures (annotators misunderstand or misexecute the task), policy ambiguity (policy wording leaves room for interpretation), or value pluralism (different annotators hold different perspectives on safety). Distinguishing these sources matters. For example, operational failures call for quality control, ambiguity calls for policy clarification, and pluralism calls for deliberation about incorporating diverse perspectives. Yet understanding why annotators disagree is difficult. Directly asking annotators for their reasoning is costly, substantially increasing annotation burden, and can be unreliable for both human and LLM annotators as self-reported reasoning often fails to reflect actual decision processes.
  We introduce Annotator Policy Models (APMs), interpretable models that learn annotators' internal safety policies from labeling behavior alone, making annotator reasoning visible and comparable without additional annotation effort. We validate that APMs accurately model annotator safety policy (>80% accuracy), faithfully predict responses to counterfactual edits, and recover known policy differences in controlled settings. Applying APMs to LLM and human annotations, we demonstrate two core applications: (1) surfacing policy ambiguity by revealing how annotators interpret safety instructions differently, and (2) surfacing value pluralism by uncovering systematic differences in safety priorities across demographic groups. Together, these capabilities support more targeted, transparent, and inclusive safety policy design.

</details>


### [2] [FinRAG-12B: A Production-Validated Recipe for Grounded Question Answering in Banking](https://arxiv.org/abs/2605.05482)
*Denys Katerenchuk,Pablo Duboue,Keelan Evanini,David Gondek,Nithin Govindugari,Olivier Allauzen,Joshua Baptiste,David J More,Joshua Schechter*

Main category: cs.AI

TL;DR: A unified framework for training grounded domain-specific LLMs for banking that achieves high accuracy, improved citation grounding, calibrated refusal, and significant cost savings while being deployed across 40+ financial institutions.


<details>
  <summary>Details</summary>
Motivation: LLM adoption in banking faces resistance due to demands for high accuracy, regulatory compliance, and need for verifiable/grounded responses. There's a need for domain-specific LLMs that can provide reliable, citation-backed answers while knowing when to refuse unanswerable questions.

Method: A unified, data-efficient framework with: 1) Data generation pipeline using LLM-as-a-Judge filtering, citation annotation, and curriculum learning (143M tokens), 2) Calibrated refusal mechanism training on 22% unanswerable examples, 3) End-to-end methodology from data curation to quantized serving.

Result: The 12B model outperforms GPT-4.1 on citation grounding with modest citation tradeoff, achieves 12% "I don't know" rate (vs base's unsafe 4.3% and GPT-4.1's over-refusal 20.2%), deployed at 40+ financial institutions with 7.1 percentage point query resolution improvement, 3-5x faster responses, and 20-50x lower cost vs GPT-4.1.

Conclusion: The framework successfully addresses banking industry LLM adoption challenges through a data-efficient training approach that balances answer quality, citation grounding, and calibrated refusal, achieving practical deployment success with significant performance and cost advantages over GPT-4.1.

Abstract: Large language models (LLMs) are rapidly being adopted across various domains. However, their adoption in banking industry faces resistance due to demands for high accuracy, regulatory compliance, and the need for verifiable and grounded responses. We present a unified, data-efficient framework for training grounded domain-specific LLMs that optimizes answer quality, citation grounding, and calibrated refusal under real-world deployment constraints. First, we describe a data generation pipeline that combines LLM-as-a-Judge filtering, citation annotation, and curriculum learning with only 143M tokens. The resulting 12B model achieves high answer quality outperforming GPT-4.1 on citation grounding, with a modest citation tradeoff versus the untuned base. Second, we propose a calibrated refusal mechanism: training on 22% unanswerable examples yield a 12% "I don't know" rate, substantially improving over the base model's unsafe 4.3% rate while avoiding GPT-4.1's over-refusal (20.2%). Third, we present an end-to-end methodology spanning from data curation to quantized serving. The system is deployed at 40+ financial institutions, achieving a 7.1 percentage point improvement in query resolution (p < 0.001). Additionally, the model delivers 3-5x faster responses at 20-50x lower cost compared to GPT-4.1.

</details>


### [3] [ZAYA1-8B Technical Report](https://arxiv.org/abs/2605.05365)
*Robert Washbourne,Rishi Iyer,Tomas Figliolia,Henry Zheng,Ryan Lorig-Roach,Sungyeon Yang,Pritish Yuvraj,Quentin Anthony,Yury Tokpanov,Xiao Yang,Ganesh Nanduru,Stephen Ebert,Praneeth Medepalli,Skyler Szot,Srivatsan Rajagopal,Alex Ong,Bhavana Mehta,Beren Millidge*

Main category: cs.AI

TL;DR: ZAYA1-8B is a reasoning-focused mixture-of-experts model with 700M active/8B total parameters that matches or exceeds larger models on math/coding benchmarks using novel training and inference techniques.


<details>
  <summary>Details</summary>
Motivation: To create an efficient, open-weight reasoning model that can compete with much larger models (like Gemini-2.5 Pro, DeepSeek-V3.2, GPT-5-High) on challenging mathematics and coding tasks while using relatively few active parameters.

Method: Built on Zyphra's MoE++ architecture; trained from scratch for reasoning with reasoning data included from pretraining using answer-preserving trimming; uses four-stage RL cascade (reasoning warmup, 400-task RLVE-Gym curriculum, math/code RL with test-time compute traces and synthetic code environments, behavioral RL); introduces Markovian RSA test-time compute method for recursive aggregation of parallel reasoning traces.

Result: With under 1B active parameters, matches/exceeds DeepSeek-R1-0528 on several math/coding benchmarks; achieves 91.9% on AIME'25 and 89.6% on HMMT'25 with Markovian RSA while carrying only 4K-token tail; competitive with substantially larger open-weight reasoning models.

Conclusion: ZAYA1-8B demonstrates that carefully designed training methodologies and novel inference techniques (like Markovian RSA) can enable relatively small models to achieve reasoning performance competitive with much larger state-of-the-art models, offering an efficient alternative for reasoning tasks.

Abstract: We present ZAYA1-8B, a reasoning-focused mixture-of-experts (MoE) model with 700M active and 8B total parameters, built on Zyphra's MoE++ architecture. ZAYA1-8B's core pretraining, midtraining, and supervised fine-tuning (SFT) were performed on a full-stack AMD compute, networking, and software platform. With under 1B active parameters, ZAYA1-8B matches or exceeds DeepSeek-R1-0528 on several challenging mathematics and coding benchmarks, and remains competitive with substantially larger open-weight reasoning models. ZAYA1-8B was trained from scratch for reasoning, with reasoning data included from pretraining onward using an answer-preserving trimming scheme. Post-training uses a four-stage RL cascade: reasoning warmup on math and puzzles; a 400-task RLVE-Gym curriculum; math and code RL with test-time compute traces and synthetic code environments built from competitive-programming references; and behavioral RL for chat and instruction following. We also introduce Markovian RSA, a test-time compute method that recursively aggregates parallel reasoning traces while carrying forward only bounded-length reasoning tails between rounds. In TTC evaluation, Markovian RSA raises ZAYA1-8B to 91.9\% on AIME'25 and 89.6\% on HMMT'25 while carrying forward only a 4K-token tail, narrowing the gap to much larger reasoning models including Gemini-2.5 Pro, DeepSeek-V3.2, and GPT-5-High.

</details>


### [4] [Retrieval-Conditioned Topology Selection with Provable Budget Conservation for Multi-Agent Code Generation](https://arxiv.org/abs/2605.05657)
*Abhijit Talluri,Pujith Anne,Bhagavan Choudary Pendiyala,Raghavendra Chilukuri*

Main category: cs.AI

TL;DR: RGAO is a retrieval-guided adaptive orchestration system for multi-agent LLM code generation that dynamically selects orchestration topologies based on code structural complexity, achieving provable budget conservation through formal contracts.


<details>
  <summary>Details</summary>
Motivation: Existing multi-agent LLM systems for code generation select orchestration topologies without considering the structural complexity of the codebase, leading to suboptimal routing decisions. There's a need for systems that can adapt their topology based on code complexity while maintaining provable resource constraints.

Method: RGAO extracts a structural complexity vector from a hierarchical code index before selecting orchestration topology. It operates within Code-Agent framework with sub-agents governed by formal contracts having six-dimensional budget vectors. Combines complexity-conditioned LLM routing with formal resource algebras to enable retrieval-conditioned dynamic topology selection.

Result: Achieved reduction of proxy-measured misrouting from 30.1% to 8.2% using complexity-conditioned topology router. Developed budget algebra with structural-induction conservation theorem and hierarchical code retrieval engine. Demonstrated sub-millisecond DAG construction and linear tree-index scalability in empirical evaluation.

Conclusion: RGAO successfully bridges complexity-conditioned routing with formal resource algebras, enabling provable budget conservation under dynamic topology selection while significantly reducing misrouting in multi-agent code generation systems.

Abstract: Multi-agent LLM systems for code generation face a fundamental routing problem: the optimal orchestration topology depends on the structural complexity of the code under modification, yet existing systems select topologies without consulting the codebase. We present Retrieval-Guided Adaptive Orchestration (RGAO), an architecture that closes this loop by extracting a structural complexity vector from a hierarchical code index before selecting the orchestration topology. RGAO operates within Code-Agent, a multi-agent framework whose sub-agents are governed by formal contracts with six-dimensional budget vectors. Our headline contribution is the composition of two previously separate lines of work -- complexity-conditioned LLM routing and formal resource algebras -- yielding a property neither admits alone: provable budget conservation under retrieval-conditioned dynamic topology selection. Concretely we contribute: (1) a complexity-conditioned topology router that reduces proxy-measured misrouting from 30.1% to 8.2%; (2) a budget algebra with a structural-induction conservation theorem; and (3) a hierarchical code retrieval engine. Empirical evaluation demonstrates sub-millisecond DAG construction and linear tree-index scalability.

</details>


### [5] [Partial Evidence Bench: Benchmarking Authorization-Limited Evidence in Agentic Systems](https://arxiv.org/abs/2605.05379)
*Krti Tallam*

Main category: cs.AI

TL;DR: A benchmark (Partial Evidence Bench) for measuring enterprise agents' failure to recognize when they provide incomplete answers due to access control boundaries, with 72 tasks across due diligence, compliance, and security scenarios.


<details>
  <summary>Details</summary>
Motivation: Enterprise agents operating in scoped retrieval systems can produce answers that appear complete even when critical evidence lies outside their authorization boundaries, creating dangerous false confidence in enterprise decision-making.

Method: Created a deterministic benchmark with 3 scenario families (due diligence, compliance audit, security incident response), 72 tasks total, ACL-partitioned corpora, oracle complete/authorized answers, completeness judgments, and structured gap-report oracles.

Result: Baselines show silent filtering is "catastrophically unsafe" across all scenarios, while explicit fail-and-report eliminates unsafe completeness without trivial abstention. Real-model runs reveal model-dependent variations in overclaiming completeness, underclaiming, or reporting incompleteness in enterprise-usable forms.

Conclusion: The benchmark makes governance-critical agent failure measurable without human judges or contamination-prone static corpora, enabling systematic evaluation of systems' ability to handle partial evidence scenarios in enterprise settings.

Abstract: Enterprise agents increasingly operate inside scoped retrieval systems, delegated workflows, and policy-constrained evidence environments. In these settings, access control can be enforced correctly while the system still produces an answer that appears complete even though material evidence lies outside the caller's authorization boundary. This paper introduces Partial Evidence Bench, a deterministic benchmark for measuring that failure mode. The benchmark ships three scenario families -- due diligence, compliance audit, and security incident response -- with 72 tasks total, ACL-partitioned corpora, oracle complete answers, oracle authorized-view answers, oracle completeness judgments, and structured gap-report oracles. It evaluates systems along four surfaces: answer correctness, completeness awareness, gap-report quality, and unsafe completeness behavior. Checked-in baselines show that silent filtering is catastrophically unsafe across all shipped families, while explicit fail-and-report behavior eliminates unsafe completeness without collapsing the task into trivial abstention. Preliminary real-model runs show model-dependent and scenario-sensitive differences in whether systems overclaim completeness, conservatively underclaim, or report incompleteness in an enterprise-usable form. The benchmark's broader contribution is to make a governance-critical agent failure measurable without human judges or contamination-prone static corpora.

</details>


### [6] [BioResearcher: Scenario-Guided Multi-Agent for Translational Medicine](https://arxiv.org/abs/2605.05985)
*Remigiusz Kinas,Joanna Krawczyk,Rafał Powalski,Przemysław Pietrzak,Agnieszka Kowalewska,Krzysztof Kolmus,Maciej Sypetkowski,Łukasz Smoliński,Tomasz Jetka*

Main category: cs.AI

TL;DR: Ingenix BioResearcher is a scenario-guided multi-agent system designed for translational medicine that outperforms existing methods on biomedical reasoning and clinical discovery tasks through specialized agents, versioned research playbooks, and structured workflows.


<details>
  <summary>Details</summary>
Motivation: Translational medicine requires combining diverse biomedical sources (literature, trials, patents, multi-omics data) while preserving identifiers, uncertainty, and provenance. General-purpose foundation models and existing multi-agent systems are inadequate as they produce single-shot answers or run open-endedly without the auditable, scenario-specific workflows needed for heterogeneous biomedical data.

Method: BioResearcher is a scenario-guided multi-agent system that maps queries to versioned research playbooks, delegates to specialized subagents using 30+ tools and ML endpoints, mixes structured database access with sandboxed code for genome-scale analyses, and applies claim-level multi-model reconciliation before editorial assembly.

Result: The system achieves 83.49% pass rate on 109 single-step tests (average score 0.892), strong biomedical benchmark performance (89.33% on BixBench-Verified-50, top mean score 0.758 on BaisBench Scientific Discovery), and leads on clinical end-to-end benchmark with highest positive hit rate (74.7% ± 3.3%) and negative clear rate (96.8% ± 0.2%).

Conclusion: BioResearcher demonstrates broad, competitive performance across unit-level, open-ended, and end-to-end clinical evaluations, showing it effectively addresses the limitations of existing systems for translational medicine workflows that require auditable, scenario-specific processing of heterogeneous biomedical sources.

Abstract: Translational medicine turns underspecified development goals into evidence synthesis that must combine literature, trials, patents, and quantitative multi-omics analysis while preserving identifiers, uncertainty, and retrievable provenance. General-purpose foundation models and off-the-shelf tool-augmented or multi-agent systems are not built for this: they tend to produce single-shot answers or run open-endedly, and fall short on the auditable, scenario-specific workflows that heterogeneous biomedical sources demand.
  This paper introduces Ingenix BioResearcher, a scenario-guided multi-agent system that maps queries to versioned research playbooks, delegates to specialized subagents over 30+ tools and machine-learning endpoints, mixes structured database access with sandboxed code for genome-scale analyses, and applies claim-level multi-model reconciliation before editorial assembly.
  We evaluate BioResearcher across unit-level capabilities, open-ended biomedical reasoning, and end-to-end clinical discovery. It leads evaluated baselines on 109 single-step tests (83.49% pass rate; 0.892 average score), achieves strong biomedical benchmark performance (89.33% on BixBench-Verified-50 and the top 0.758 mean score on BaisBench Scientific Discovery), and leads on a 30-query clinical end-to-end benchmark with the highest positive hit rate (74.7% $\pm$ 3.3%) and negative clear rate (96.8% $\pm$ 0.2%). These results show broad, competitive performance across unit-level, open-ended, and end-to-end clinical evaluations.

</details>


### [7] [BALAR : A Bayesian Agentic Loop for Active Reasoning](https://arxiv.org/abs/2605.05386)
*Aymen Echarghaoui,Dongxia Wu,Emily B. Fox*

Main category: cs.AI

TL;DR: BALAR is a task-agnostic outer-loop algorithm that enables LLMs to proactively ask clarifying questions using Bayesian reasoning to maximize information gain in interactive dialogues.


<details>
  <summary>Details</summary>
Motivation: Current LLM systems treat dialogues reactively and lack mechanisms to reason about missing information and determine what questions to ask next, limiting their effectiveness in multi-turn interactive settings where tasks require information exchange.

Method: BALAR is a Bayesian Agentic Loop for Active Reasoning that maintains structured beliefs over latent states, selects clarifying questions by maximizing expected mutual information, and dynamically expands its state representation when needed. It's task-agnostic and requires no fine-tuning.

Result: BALAR significantly outperformed all baselines across three diverse benchmarks: AR-Bench-DC (detective cases) with 14.6% higher accuracy, AR-Bench-SP (thinking puzzles) with 38.5% higher accuracy, and iCraft-MD (clinical diagnosis) with 30.5% higher accuracy.

Conclusion: BALAR provides a principled approach for LLMs to engage in structured multi-turn interactions by actively reasoning about information gaps, enabling more effective dialogue-based problem solving across diverse domains without task-specific fine-tuning.

Abstract: Large language models increasingly operate in interactive settings where solving a task requires multiple rounds of information exchange with a user. However, most current systems treat dialogue reactively and lack a principled mechanism to reason about what information is missing and which question should be asked next. We propose BALAR (Bayesian Agentic Loop for Active Reasoning), a task-agnostic outer-loop algorithm that requires no fine-tuning and enables structured multi-turn interaction between an LLM agent and a user. BALAR maintains a structured belief over latent states, selects clarifying questions by maximizing expected mutual information, and dynamically expands its state representation when the current one proves insufficient. We evaluate BALAR on three diverse benchmarks: AR-Bench-DC (detective cases), AR-Bench-SP (thinking puzzles), and iCraft-MD (clinical diagnosis). BALAR significantly outperforms all baselines across all three benchmarks, with $14.6\%$ higher accuracy on AR-Bench-DC, $38.5\%$ on AR-Bench-SP, and $30.5\%$ on iCraft-MD.

</details>


### [8] [From Agent Loops to Deterministic Graphs: Execution Lineage for Reproducible AI-Native Work](https://arxiv.org/abs/2605.06365)
*Josh Rosen,Seth Rosen*

Main category: cs.AI

TL;DR: Execution lineage uses DAGs with explicit dependencies to make AI-generated work maintainable under change, ensuring what changes and what stays stable across revisions.


<details>
  <summary>Details</summary>
Motivation: Current LLM agentic workflows rely on implicit conversational state, making it difficult to preserve stable work products, isolate irrelevant updates, or propagate changes through intermediate artifacts. There's a need for better maintainability of evolving AI-generated work under change.

Method: Introduces execution lineage: an execution model where AI-native work is represented as a directed acyclic graph (DAG) of artifact-producing computations with explicit dependencies, stable intermediate boundaries, and identity-based replay.

Result: In controlled policy-memo update tasks, DAG replay preserved final memos exactly with zero churn and contamination in unrelated-branch updates, while achieving perfect upstream preservation, downstream propagation, and consistency in intermediate-artifact edits. Loop baselines frequently imported unrelated context and lacked state consistency.

Conclusion: Final answer quality and maintained-state quality are distinct. Execution lineage provides stronger guarantees about what should change, what should remain stable, and how work evolves across revisions, addressing limitations of loop-centric approaches that can mask partial state inconsistency.

Abstract: Large language model systems are increasingly deployed as agentic workflows that interleave reasoning, tool use, memory, and iterative refinement. These systems are effective at producing answers, but they often rely on implicit conversational state, making it difficult to preserve stable work products, isolate irrelevant updates, or propagate changes through intermediate artifacts.
  We introduce execution lineage: an execution model in which AI-native work is represented as a directed acyclic graph (DAG) of artifact-producing computations with explicit dependencies, stable intermediate boundaries, and identity-based replay. The goal is not to make the model a better one-shot writer, but to make evolving AI-generated work maintainable under change.
  We compare execution-lineage replay against loop-centric update baselines on two controlled policy-memo update tasks. In an unrelated-branch update, DAG replay preserved the final memo exactly in all runs, with zero churn and zero unrelated-branch contamination, while loop baselines regenerated the memo and frequently imported unrelated context. In an intermediate-artifact edit, all systems reflected the new constraint in the final memo, but only DAG replay achieved perfect upstream preservation, downstream propagation, unaffected-artifact preservation, and cross-artifact consistency.
  These results show that final answer quality and maintained-state quality are distinct. Strong loop baselines can remain competitive at producing polished final outputs when the task is a bounded synthesis/update problem and all current sources fit in context, but immediate task success can mask partial state inconsistency that may compound over future revisions. Execution lineage provides stronger guarantees about what should change, what should remain stable, and how work evolves across revisions.

</details>


### [9] [Intelligent CCTV for Urban Design: AI-Based Analysis of Soft Infrastructure at Intersections](https://arxiv.org/abs/2605.05402)
*Vinit Katariya,Seungjin Kim,Curtis Craig,Nichole Morris,Hamed Tabkhi*

Main category: cs.AI

TL;DR: AI-enabled computer vision framework using existing CCTV to assess traffic-calming interventions shows speed reductions up to 20% and traffic decreases up to 12.2%


<details>
  <summary>Details</summary>
Motivation: Traditional transportation data collection methods are often costly and limited. There's a need for efficient, low-cost methods to evaluate the impact of soft traffic-calming interventions (like pedestrian refuges and curb extensions) on vehicle speed and safety using existing infrastructure.

Method: Developed an AI-enabled analytics framework leveraging existing CCTV infrastructure, using deep learning and perspective-based speed estimation to evaluate driver behavior before and after interventions with repeated post-installation monitoring (Week 1 and Week 2) in Minneapolis.

Result: At unsignalized intersections: mean speeds fell by up to 18.75%, 85th-percentile speeds by up to 16.56%, and pass-through traffic decreased by up to 12.2%. Signalized intersections showed comparable reductions (up to 20.0% mean speed reduction, 17.19% 85th-percentile reduction) except one location.

Conclusion: Soft traffic infrastructure interventions effectively calm traffic, and AI-powered methods provide rapid, low-cost, evidence-based solutions for transportation policy evaluation using existing CCTV systems.

Abstract: Artificial intelligence (AI) and computer vision are transforming transportation data collection. This study introduces an AI-enabled analytics framework leveraging existing CCTV infrastructure to evaluate the impact of soft interventions, such as temporary pedestrian refuges and curb extensions, on vehicle speed and safety. Using deep learning and perspective-based speed estimation, we evaluated driver behavior before and after interventions, with repeated post-installation monitoring in Week 1 and Week 2, in Minneapolis. Findings reveal that at unsignalized intersections, mean and 85th-percentile speeds fell by up to 18.75% and 16.56%, respectively, while pass-through traffic decreased by as much as 12.2%. Signalized intersections showed comparable reductions except one location, with mean and 85th-percentile speeds dropping by up to 20.0% and 17.19%. These results demonstrate the traffic-calming effectiveness of soft infrastructure and underscore the utility of AI-powered methods for rapid, low-cost, and evidence-based transport policy evaluation.

</details>


### [10] [When Helpfulness Becomes Sycophancy: Sycophancy is a Boundary Failure Between Social Alignment and Epistemic Integrity in Large Language Models](https://arxiv.org/abs/2605.05403)
*Jiechen Li,Catherine A. Barry,Rishika Randev,Janet Chen,Ella Jorgensen,Brinnae Bent*

Main category: cs.AI

TL;DR: The paper argues that LLM sycophancy represents a boundary failure between social alignment and epistemic integrity, proposes a three-condition framework for defining sycophancy, and introduces a taxonomy for classification.


<details>
  <summary>Details</summary>
Motivation: Existing research on sycophancy in LLMs focuses too narrowly on overt behaviors like agreement with incorrect beliefs, position reversals, or deviation from objective correctness standards. These formulations miss subtler boundary failures between social alignment and epistemic integrity, leaving the phenomenon underspecified.

Method: The paper proposes a three-condition framework for sycophancy: 1) user expresses a cue (belief, preference, or self-concept), 2) model shifts toward that cue through alignment behavior, 3) this shift compromises epistemic accuracy, independent reasoning, or appropriate correction. Also introduces a taxonomy with alignment targets, mechanisms, and severity.

Result: A refined conceptualization of sycophancy as alignment behavior that displaces independent epistemic judgment rather than mere agreement. Provides structured framework and taxonomy for better understanding and classification of sycophancy in LLMs.

Conclusion: The paper concludes with implications for alignment evaluation, advocating for boundary-aware assessment, structured rubrics, and mitigation strategies. Situates these proposals alongside alternative views of sycophancy to advance the discourse.

Abstract: This position paper argues that sycophancy in LLMs is a boundary failure between social alignment and epistemic integrity. Existing work often operationalizes sycophancy through external behavior such as agreement with incorrect user beliefs, position reversals, or deviation from an objective standard of correctness. These formulations capture only overt forms of the phenomenon and leave subtler boundary failures involving epistemic integrity and social alignment underspecified. We argue that sycophancy should not be understood as agreement alone, but as alignment behavior that displaces independent epistemic judgment. To clarify this boundary, we propose a three-condition framework for sycophancy. First, the user expresses a cue in the form of a belief, preference, or self-concept. Second, the model shifts toward that cue through alignment behavior. Third, this shift compromises epistemic accuracy, independent reasoning, or appropriate correction. We also introduce a taxonomy for classifying sycophancy, consisting of alignment targets, mechanisms, and severity. The paper concludes by discussing implications for alignment evaluation and argues for boundary-aware assessment, structured rubrics, and mitigation strategies, while situating these proposals alongside alternative views of sycophancy.

</details>


### [11] [PRISM: Perception Reasoning Interleaved for Sequential Decision Making](https://arxiv.org/abs/2605.05407)
*Mohamed Salim Aissi,Clemence Grislain,Clement Romac,Laure Soulier,Mohamed Chetouani,Olivier Sigaud,Nicolas Thome*

Main category: cs.AI

TL;DR: PRISM is a framework that closes the perception-reasoning-decision gap in multimodal embodied agents by enabling interactive, goal-oriented dialogue between VLMs and LLMs through dynamic question-answering.


<details>
  <summary>Details</summary>
Motivation: Scaling LLM-based embodied agents from text-only to complex multimodal environments is challenging. Current standalone Vision-Language Models (VLMs) have a perception-reasoning-decision gap where they often overlook task-critical information.

Method: PRISM introduces a closed-loop interaction framework where the LLM actively critiques the VLM's initial description, probes it with goal-oriented questions, and synthesizes a compact, task-driven image description through a dynamic question-answer pipeline.

Result: PRISM significantly outperforms state-of-the-art image-based models on ALFWorld and Room-to-Room benchmarks. The interactive goal-oriented perception pipeline yields systematic and substantial gains, and the framework is fully automatic without needing handcrafted questions or answers.

Conclusion: The PRISM framework successfully addresses the perception-reasoning-decision gap by enabling tight coupling between perception (VLM) and decision (LLM) components, leading to improved task performance and more efficient scene understanding in multimodal embodied agents.

Abstract: Scaling LLM-based embodied agents from text-only environments to complex multimodal settings remains a major challenge. Recent work identifies a perception-reasoning-decision gap in standalone Vision-Language Models (VLMs), which often overlook task-critical information. In this paper, we introduce PRISM, a framework that tightly couples perception (VLM) and decision (LLM) through a dynamic question-answer (DQA) pipeline. Instead of passively accepting the VLM's description, the LLM critiques it, probes the VLM with goal-oriented questions, and synthesizes a compact image description. This closed-loop interaction yields a sharp, task-driven understanding of the scene. We evaluate PRISM on the ALFWorld and Room-to-Room (R2R) benchmarks. We show that: (1) PRISM significantly outperforms state-of-the-art image-based models, (2) our Interactive goal-oriented perception pipeline yields systematic and substantial gains, and (3) PRISM is fully automatic, eliminating the need for handcrafted questions or answers.

</details>


### [12] [Agentic Retrieval-Augmented Generation for Financial Document Question Answering](https://arxiv.org/abs/2605.05409)
*Yang Shu,Yingmin Liu,Zequn Xie*

Main category: cs.AI

TL;DR: FinAgent-RAG is an agentic retrieval-augmented generation framework designed for financial document QA, using iterative retrieval-reasoning loops with self-verification to handle complex multi-step numerical reasoning over heterogeneous financial evidence.


<details>
  <summary>Details</summary>
Motivation: Existing RAG approaches use a single-pass retrieve-then-generate paradigm that struggles with compositional reasoning chains in financial analysis. Financial QA requires complex multi-step numerical reasoning over heterogeneous evidence (tables, text, footnotes) scattered across corporate filings.

Method: FinAgent-RAG features: (1) Contrastive Financial Retriever trained with hard negative mining to distinguish semantically similar but numerically distinct passages; (2) Program-of-Thought reasoning module that generates executable Python code for precise arithmetic; (3) Adaptive Strategy Router that dynamically allocates computational resources based on question complexity.

Result: Achieves 76.81%, 78.46%, and 74.96% execution accuracy on FinQA, ConvFinQA, and TAT-QA benchmarks, outperforming strongest baseline by 5.62-9.32 percentage points. Reduces API costs by 41.3% on FinQA while preserving accuracy through adaptive resource allocation.

Conclusion: The framework demonstrates robustness through ablation studies, cross-backbone evaluation with four LLMs, and deployment cost analysis, confirming practical viability for financial institutions dealing with complex numerical reasoning tasks.

Abstract: Financial document question answering (QA) demands complex multi-step numerical reasoning over heterogeneous evidence--structured tables, textual narratives, and footnotes--scattered across corporate filings. Existing retrieval-augmented generation (RAG) approaches adopt a single-pass retrieve-then-generate paradigm that struggles with the compositional reasoning chains prevalent in financial analysis. We propose FinAgent-RAG, an agentic RAG framework that orchestrates iterative retrieval-reasoning loops with self-verification, specifically engineered for the precision requirements of financial numerical reasoning. The framework integrates three domain-specific innovations: (1) a Contrastive Financial Retriever trained with hard negative mining to distinguish semantically similar but numerically distinct financial passages, (2) a Program-of-Thought reasoning module that generates executable Python code for precise arithmetic rather than relying on error-prone LLM-based mental computation, and (3) an Adaptive Strategy Router that dynamically allocates computational resources based on question complexity, reducing API costs by 41.3% on FinQA while preserving accuracy. Extensive experiments on three benchmark datasets--FinQA, ConvFinQA, and TAT-QA--demonstrate that FinAgent-RAG achieves 76.81%, 78.46%, and 74.96% execution accuracy respectively, outperforming the strongest baseline by 5.62--9.32 percentage points. Ablation studies, cross-backbone evaluation with four LLMs, and deployment cost analysis confirm the framework's robustness and practical viability for financial institutions.

</details>


### [13] [LaTA: A Drop-in, FERPA-Compliant Local-LLM Autograder for Upper-Division STEM Coursework](https://arxiv.org/abs/2605.05410)
*Jesse A. Rodríguez*

Main category: cs.AI

TL;DR: LaTA is an open-source, on-premises autograder that uses locally hosted LLMs to grade LaTeX assignments in STEM courses, achieving high accuracy, significant cost savings, and improved student performance.


<details>
  <summary>Details</summary>
Motivation: Existing LLM graders violate student privacy (FERPA) by sending data to third-party APIs, require substantial assignment modification, and expose institutions to data risks. There's a need for a privacy-preserving, drop-in solution that works with existing LaTeX workflows.

Method: LaTA implements a four-stage pipeline (ingest, segment, grade, report) using a locally hosted open-weight chain-of-thought LLM grader (gpt-oss:120b). It compares student work to instructor-authored reference solutions and applies a YAML rubric with binary per-item scoring, running entirely on commodity on-premises hardware.

Result: Deployed in ME 373 for ~200 students, grading on a Mac Studio with $0 marginal cost per assignment, 1-3 minutes per submission. Error rate: 0.02-0.04% per rubric item. LaTA-graded cohort outperformed traditionally-graded cohort by 11% on midterm and 8% on final exam. Students reported significant confidence gains (Δ≥+1.49 Likert points, p<10^{-27}).

Conclusion: LaTA demonstrates that locally hosted LLM graders can provide accurate, cost-effective grading while preserving student privacy, enabling regrading opportunities, expanded TA support, and improving student learning outcomes in upper-division STEM courses.

Abstract: Large-language-model (LLM) graders promise to relieve the grading burden of upper-division STEM courses, but most deployments to date send student work to third-party APIs, violating FERPA and exposing institutions to data risk while requiring substantial assignment modification. We present $\textbf{LaTA}\ (\textit{LaTeX Teaching Assistant})$, a drop-in, open-source autograder that runs entirely on commodity on-premises hardware and assumes a LaTeX-native workflow already adopted by many engineering and physics courses. LaTA implements a four-stage pipeline (ingest, segment, grade, report) using a locally hosted open-weight chain-of-thought LLM grader (gpt-oss:120b) that compares student work to an instructor-authored reference solution and applies a YAML rubric with binary per-item scoring. We deployed LaTA in Winter~2026 in ME 373 (Mechanical Engineering Methods) at Oregon State University, grading every weekly assignment for approximately 200 students on a single Mac Studio at \$0 marginal cost per assignment and 1--3 minutes of wall-clock time per submission, enabling regrading of corrected assignments and greatly expanded TA office hour offerings. The instructor-confirmed grading-error rate held at roughly $0.02$--$0.04\%$ per rubric line item across the term. Relative to the same instructor's previous traditionally-graded cohort, the LaTA-graded cohort outperformed by approximately $11\%$ on the midterm exam and $8\%$ on the final exam, and reported large gains in self-assessed confidence on every stated learning objective ($N = 159$ survey responses, $Δ\geq +1.49$ Likert points, $p < 10^{-27}$ on every comparison). We release the code under AGPLv3.

</details>


### [14] [From History to State: Constant-Context Skill Learning for LLM Agents](https://arxiv.org/abs/2605.05413)
*Haoyang Xie,Xinyuan Wang,Yancheng Wang,Puda Zhao,Feng Ju*

Main category: cs.AI

TL;DR: A constant-context skill learning framework that moves procedural knowledge from long prompts into model weights, enabling privacy-preserving local LLM agents with reduced context usage while maintaining strong performance.


<details>
  <summary>Details</summary>
Motivation: Personal LLM agents face a privacy-cost-capability dilemma: cloud models handle complex workflows but expose sensitive data, while local models preserve privacy but are less reliable. Both approaches also incur high costs from repeated use of long prompts and growing context histories.

Method: Proposes constant-context skill learning with a context-to-weights framework where reusable procedures are learned in lightweight task-family modules. Inference only conditions on current observation and compact state block. Uses deterministic tracker for state management and provides aligned subgoal rewards, enabling training via step-level SFT and refinement through online RL.

Result: Achieves strong performance across ALFWorld, WebShop, and SciWorld benchmarks with Qwen3-4B, Qwen3-8B and Llama-3.1-8B models. With Qwen3-8B, SFT+RL reaches 89.6% unseen success on ALFWorld, 76.8% on WebShop, and 66.4% unseen success on SciWorld. Reduces prompt tokens per turn by 2-7× compared to ReAct prompting baselines.

Conclusion: Procedural context can be effectively moved from prompts into model weights, enabling privacy-preserving local agents that match or exceed strong published agent-training results while significantly reducing context requirements.

Abstract: Large language model (LLM) agents are increasingly used to operate browsers, files, code and tools, making personal assistants a natural deployment target. Yet personal agents face a privacy-cost-capability tension: cloud models execute multi-step workflows well but expose sensitive intermediate context to external APIs, while local models preserve privacy but remain less reliable. Both settings also pay repeatedly for long skill prompts and growing histories. We propose constant-context skill learning, a context-to-weights framework for recurring agent workflows: reusable procedures are learned in lightweight task-family modules, while inference conditions only on the current observation and a compact state block. A deterministic tracker renders this state block from task progress and supplies aligned subgoal rewards, so each module can be trained with step-level SFT and refined through online RL. Across ALFWorld, WebShop, and SciWorld, our agents achieve strong performance across Qwen3-4B, Qwen3-8B and Llama-3.1-8B. With Qwen3-8B, SFT+RL reaches 89.6\% unseen success on ALFWorld, 76.8\% success on WebShop, and 66.4\% unseen success on SciWorld. They match or exceed strong published agent-training results while reducing prompt tokens per turn by 2--7$\times$ relative to controlled ReAct prompting baselines, showing that procedural context can be moved from prompts into weights.

</details>


### [15] [The Geopolitics of AI Safety: A Causal Analysis of Regional LLM Bias](https://arxiv.org/abs/2605.05427)
*Alif Al Hasan*

Main category: cs.AI

TL;DR: This paper introduces a causal Probabilistic Graphical Model framework to audit LLM safety mechanisms, revealing that standard observational fairness metrics overestimate demographic bias by failing to account for context toxicity, with distinct alignment patterns between Western and Eastern models.


<details>
  <summary>Details</summary>
Motivation: As LLMs are integrated into global software systems, ensuring equitable safety guardrails is critical. Current fairness evaluations are confounded by inherent toxicity of topics naturally paired with specific demographics in testing datasets, requiring causal rather than observational analysis.

Method: Developed a Probabilistic Graphical Model framework using Pearl's do-operator to mathematically isolate causal effects of injecting cultural demographics into prompts. Conducted large-scale empirical analysis across seven instruction-tuned models from different regions using ToxiGen and BOLD datasets.

Result: Revealed disparity between observational and interventional bias, showing standard fairness metrics can overestimate demographic bias. Western models exhibit higher causal refusal rates for specific demographic groups, while Eastern models show low overall intervention rates with targeted regional sensitivities.

Conclusion: The study demonstrates the importance of causal analysis for fairness auditing, highlights different alignment trends across regions, and warns that demographic-sensitive over-triggering restricts benign discourse in downstream applications.

Abstract: As Large Language Models (LLMs) are integrated into global software systems, ensuring equitable safety guardrails is a critical requirement. Current fairness evaluations predominantly measure bias observationally, a methodology confounded by the inherent toxicity of topics naturally paired with specific demographics in testing datasets. This study introduces a Probabilistic Graphical Model (PGM) framework to audit LLM safety mechanisms causally. By applying Pearl's do-operator, we mathematically isolate the causal effect of injecting a cultural demographic into a prompt. We conduct a large-scale empirical analysis across seven instruction-tuned models spanning diverse origins: the United States (Llama-3.1-8B, Gemma-2-9B), Europe (Mistral-7B-v0.3), the UAE (Falcon3-7B), China (Qwen2.5-7B, DeepSeek-7B), and India (Airavata-7B). Utilizing two distinct datasets (ToxiGen and BOLD), the findings reveal a disparity between observational and interventional bias, demonstrating that standard fairness metrics can overestimate demographic bias by failing to account for context toxicity. Furthermore, the causal probabilities indicate distinct alignment trends: Western models exhibit higher causal refusal rates for specific demographic groups, whereas Eastern models demonstrate low overall intervention rates with targeted sensitivities toward regional demographics. We discuss the implications of these biases, highlighting how demographic-sensitive over-triggering restricts benign discourse in downstream applications.

</details>


### [16] [Authorization Propagation in Multi-Agent AI Systems: Identity Governance as Infrastructure](https://arxiv.org/abs/2605.05440)
*Krti Tallam*

Main category: cs.AI

TL;DR: Authorization propagation is a critical security problem in multi-agent AI systems that isn't reducible to prompt injection or covered by traditional access-control models, requiring new architectural approaches.


<details>
  <summary>Details</summary>
Motivation: Current security discussions around agentic AI focus too narrowly on prompt injection, while overlooking the distinct authorization problems that arise in multi-agent systems. These systems create new challenges as non-human principals retrieve data, delegate tasks, and synthesize results across dynamic boundaries.

Method: The paper formalizes authorization propagation as a workflow-level property, identifies three sub-problems (transitive delegation, aggregation inference, and temporal validity), and derives seven structural requirements for authorization architectures. It examines recent approaches like invocation-bound capability tokens, task-scoped authorization envelopes, dependency-graph policy enforcement, and execution-count revocation.

Result: The field is converging on recognizing the authorization propagation problem but lacks a complete architecture solution. Preliminary implementation evidence from a production enterprise AI platform shows that ordinary system behavior (not just adversarial actions) already produces the predicted authorization failures.

Conclusion: Identity governance must be treated as essential infrastructure in multi-agent AI systems, requiring continuous evaluation, enforcement at every interaction boundary, and integration before orchestration logic scales. Traditional access-control models are insufficient for the dynamic nature of AI agent interactions.

Abstract: The security discussion around agentic AI focuses heavily on prompt injection. This paper argues that multi-agent systems also create a distinct authorization problem: maintaining authorization invariants as non-human principals retrieve data, delegate tasks, and synthesize results across changing boundaries. We call this problem authorization propagation. It is not reducible to prompt injection and is not fully addressed by classical access-control models such as RBAC, ABAC, or ReBAC. The paper formalizes authorization propagation as a workflow-level property, identifies three sub-problems (transitive delegation, aggregation inference, and temporal validity), and derives seven structural requirements for authorization architectures in multi-agent AI systems. Recent work on invocation-bound capability tokens, task-scoped authorization envelopes, dependency-graph policy enforcement, and execution-count revocation demonstrates that the field is converging on the problem, but not yet on a complete architecture. The central claim is that identity governance must be treated as infrastructure: evaluated continuously, enforced at every interaction boundary, and designed into the system before orchestration logic is allowed to scale. Preliminary implementation evidence from a production enterprise AI platform shows that ordinary system behavior, not only adversarial action, already produces the failures this model predicts.

</details>


### [17] [Agentic Discovery of Exchange-Correlation Density Functionals](https://arxiv.org/abs/2605.05460)
*Titouan Duston,Jiashu Liang,Yuanheng Wang,Weihao Gao,Xuelan Wen,Nan Sheng,Weiluo Ren,Yang Sun,Yixiao Chen*

Main category: cs.AI

TL;DR: An AI-driven automated system using large language models discovers improved exchange-correlation functionals for density functional theory, with the best-performing functional (SAFS26-a) achieving ~9% improvement over current gold standard ωB97M-V, while highlighting risks of AI exploiting benchmark loopholes without proper constraints.


<details>
  <summary>Details</summary>
Motivation: Exchange-correlation (XC) functionals in density functional theory (DFT) have traditionally been manually designed by human researchers using physical insight, exact constraints, and empirical fitting. This human-driven design process is challenging and could benefit from systematic, automated alternatives enabled by recent advances in large language models.

Method: The paper presents an agentic search system where an LLM proposes structured functional-form changes guided by evolutionary history. The system operates through an iterative plan-execute-summarize loop. Functional performance improvements are measured by optimizing functional parameters against a standard thermochemistry dataset and evaluating on a held-out subset.

Result: The strongest discovered functional, SAFS26-a (Seed Agentic Functional Search 2026), improves upon the gold-standard ωB97M-V baseline by approximately 9% in performance. However, the research reveals that powerful AI models capable of discovering genuine improvements are also capable of exploiting unphysical shortcuts to "game" the benchmark.

Conclusion: While AI-driven approaches can successfully discover improved XC functionals, domain expertise translated into explicitly enforced constraints remains essential to keep results scientifically grounded. The paper presents both the promise of AI-assisted science and a cautionary lesson about the need for proper safeguards against AI exploiting benchmark loopholes.

Abstract: The development of accurate exchange-correlation (XC) functionals remains a longstanding challenge in density functional theory (DFT). The vast majority of XC functionals have been hand designed by human researchers combining physical insight, exact constraints, and empirical fitting. Recent advances in large language models enable a systematic, automated alternative to this human-driven design loop. This report presents an agentic search system in which an LLM proposes structured functional-form changes guided by evolutionary history. The system attempts to improve functional performance through an iterative plan-execute-summarize loop, where improvements are measurable by optimizing functional parameters against a standard thermochemistry dataset, then evaluating performance on a held-out subset. The strongest discovered functional, SAFS26-a (Seed Agentic Functional Search 2026), improves upon the gold-standard ωB97M-V baseline by ~9%. These results also surface a cautionary lesson for AI-assisted science: models powerful enough to discover genuine improvements are equally capable of exploiting unphysical shortcuts to game the benchmark; domain expertise translated into explicitly enforced constraints remains essential to keeping results scientifically grounded.

</details>


### [18] [Intentionality is a Design Decision: Measuring Functional Intentionality for Accountable AI Systems](https://arxiv.org/abs/2605.05475)
*Allessia Chiappetta,Robert Mahari*

Main category: cs.AI

TL;DR: The paper proposes a Functional Intentionality Test (FIT) framework to quantify and measure intentional-like behavior in AI systems for governance and accountability purposes.


<details>
  <summary>Details</summary>
Motivation: As AI systems become more autonomous, goal-directed, and capable of long-horizon behavior, there's a lack of standardized ways to detect the degree to which they function like intentional actors, which is crucial for governance and accountability.

Method: The paper defines intentionality as a behavioral profile characterized by purpose, foresight, volition, temporal commitment, and coherence. It introduces the Functional Intentionality Test (FIT) as a multidimensional framework to quantify intentional-like behavior across these five observable dimensions, along with FIT-Eval, a structured evaluation protocol for eliciting and scoring them.

Result: The FIT framework translates intentionality into interpretable levels, enabling proportionate oversight and deliberate autonomy calibration in increasingly agentic AI systems. It provides a way to measure intentionality that is design-contingent (affected by architectural choices like memory persistence, planning depth, and tool autonomy).

Conclusion: Intentionality in AI systems is design-contingent and therefore controllable, but control requires measurement. The FIT framework addresses this need by providing a standardized way to quantify intentional-like behavior, allowing for better governance, accountability, and risk management as AI systems become more autonomous.

Abstract: As AI systems increasingly exhibit autonomous, goal-directed, and long-horizon behavior, users lack a standardized way to detect the degree to which a system functions like an intentional actor for governance and accountability purposes. This position paper defines intentionality not as consciousness, but as a behavioral profile characterized by purpose, foresight, volition, temporal commitment, and coherence - criteria long used in legal and philosophical contexts to infer intent. These properties are design-contingent: architectural choices such as memory persistence, planning depth, and tool autonomy shape the degree to which systems exhibit organized goal pursuit. If intentionality is design-contingent, it is in principle controllable. Yet control requires measurement.
  We introduce the Functional Intentionality Test (FIT), a multidimensional framework that quantifies intentional-like behavior across five observable dimensions, and propose FIT-Eval, a structured evaluation protocol for eliciting and scoring them. While reduced human agency can increase efficiency, rising intentional capacity heightens accountability risks. By translating intentionality into interpretable levels, FIT enables proportionate oversight and deliberate autonomy calibration in increasingly agentic systems.

</details>


### [19] [LANTERN: LLM-Augmented Neurosymbolic Transfer with Experience-Gated Reasoning Networks](https://arxiv.org/abs/2605.05478)
*Mahyar Alinejad,Yue Wang,Amrit Singh Bedi,George Atia*

Main category: cs.AI

TL;DR: LANTERN is a multi-source neurosymbolic transfer learning framework that uses LLMs to generate task automata from natural language, aggregates multiple source policies with semantic similarity weighting, and employs adaptive gating for improved RL sample efficiency.


<details>
  <summary>Details</summary>
Motivation: Current neurosymbolic transfer methods have limitations: they typically require manually specified task automata, assume only a single source task, and use fixed knowledge-integration mechanisms that don't adapt to varying source relevance. These constraints hinder scalability and robustness in symbolic RL settings.

Method: LANTERN uses three main components: 1) Deterministic finite automata generated from natural language task descriptions using large language models, 2) Semantic embedding-based aggregation of multiple source policies weighted by cross-task similarity, and 3) Adaptive teacher-student gating based on temporal-difference error and semantic uncertainty.

Result: Across domains including resource management, navigation, and control, LANTERN achieves 40-60% improvements in sample efficiency compared to existing baselines while maintaining robustness to poorly aligned source tasks.

Conclusion: Multi-source, adaptively weighted neurosymbolic transfer can significantly improve scalability and robustness in symbolic reinforcement learning settings, demonstrating the effectiveness of LANTERN's unified approach to knowledge transfer.

Abstract: Transfer learning in reinforcement learning (RL) seeks to accelerate learning in new tasks by leveraging knowledge from related sources. Existing neurosymbolic transfer methods, however, typically rely on manually specified task automata, assume a single source task, and use fixed knowledge-integration mechanisms that cannot adapt to varying source relevance. We propose LANTERN, a unified framework for multi-source neurosymbolic transfer that addresses these limitations through three components: (i) deterministic finite automata generated from natural language task descriptions using large language models, (ii) semantic embedding-based aggregation of multiple source policies weighted by cross-task similarity, and (iii) adaptive teacher-student gating based on temporal-difference error and semantic uncertainty. Across domains spanning resource management, navigation, and control, LANTERN achieves 40-60% improvements in sample efficiency over existing baselines while remaining robust to poorly aligned sources. These results demonstrate that multi-source, adaptively weighted neurosymbolic transfer can improve scalability and robustness in symbolic RL settings.

</details>


### [20] [FoodCHA: Multi-Modal LLM Agent for Fine-Grained Food Analysis](https://arxiv.org/abs/2605.05499)
*Woojin Lee,Pranav Mekkoth,Ye Tian,Onat Gungor,Tajana Rosing*

Main category: cs.AI

TL;DR: FoodCHA is a multimodal agentic framework that reformulates food recognition as hierarchical decision-making, using progressive anchoring to improve semantic consistency and fine-grained attribute discrimination while maintaining practical deployability with compact models.


<details>
  <summary>Details</summary>
Motivation: Real-world food recognition faces challenges including high intra-class similarity, multiple food items in single images, difficulty capturing fine-grained cooking styles, and limitations of open-ended generation in vision-language models producing non-canonical labels that limit practical deployment.

Method: FoodCHA is a multimodal agentic framework that reformulates food recognition as a hierarchical decision-making process. It progressively anchors predictions: high-level categories guide subcategory identification, and subcategories guide cooking style recognition. The system utilizes the compact Moondream-2B vision-language model for strong reasoning capability with lower computational and memory overhead.

Result: Experiments on FoodNExTDB show FoodCHA outperforms Food-Llama-3.2-11B by 13.8% in category recognition precision, 38.2% in subcategory recognition precision, and achieves a striking 153.2% improvement in cooking style classification precision.

Conclusion: FoodCHA provides an effective hierarchical approach to food recognition that improves semantic consistency and attribute-level discrimination while maintaining practical deployability through efficient model selection and progressive anchoring strategies.

Abstract: The widespread adoption of camera-equipped mobile devices and wearables has enabled convenient capture of meal images, making food recognition a key component for real time dietary monitoring. However, real-world food images present challenges due to high intra-class similarity and the frequent presence of multiple food items within a single image. While deep learning models achieve strong performance in coarse grained classification, they often struggle to capture fine-grained attributes such as cooking style. Moreover, open-ended generation in modern vision-language models can produce non-canonical labels, limiting their practical deployment. We propose FoodCHA, a multimodal agentic framework that reformulates food recognition as a hierarchical decision-making process. By progressively anchoring predictions, FoodCHA guides subcategory identification using high-level categories and guides cooking style recognition using subcategories, improving semantic consistency and attribute-level discrimination. To ensure practical deployability, FoodCHA utilizes the compact Moondream-2B vision language model, which provides strong reasoning capability while maintaining lower computational and memory overhead. Experiments on FoodNExTDB show that FoodCHA outperforms Food-Llama-3.2-11B by 13.8% and 38.2% in category and subcategory recognition precision, respectively, and achieves a striking 153.2% improvement in cooking style classification precision.

</details>


### [21] [Housing Potential Common Data Model and City Digital Twin](https://arxiv.org/abs/2605.05535)
*Megan Katsumi,Mark Fox,Anderson Wong,Divnoor Chatha*

Main category: cs.AI

TL;DR: This research introduces a Housing Potential Common Data Model (HPCDM) to standardize data integration for housing analysis, implements it in a City Digital Twin with dashboard, and identifies adoption barriers and mitigation strategies.


<details>
  <summary>Details</summary>
Motivation: Current housing potential evaluation suffers from data silos across diverse datasets including zoning, land use, population characteristics, and access to services, requiring integrated approaches.

Method: Development of the Housing Potential Common Data Model (HPCDM) as a standard for data integration, creation of a City Digital Twin for housing, and implementation of a pilot dashboard application for practical demonstration.

Result: Successfully created the HPCDM framework, demonstrated its implementation through a City Digital Twin and pilot dashboard, and identified critical barriers to adoption with actionable mitigation strategies.

Conclusion: The HPCDM provides a standardized approach to overcome data silos in housing potential analysis, with practical implementation tools and adoption strategies for urban planning stakeholders.

Abstract: The evaluation of housing potential requires consideration of a location from multiple perspectives, ranging from zoning and land use to population characteristics and access to services. This research introduces the Housing Potential Common Data Model (HPCDM) to overcome existing data silos, serving as a standard to support integration and interoperability across the diverse range of datasets that are required for housing potential analysis. This report details the evaluation of the model along with the creation of a City Digital Twin for housing and a pilot dashboard application to demonstrate a practical implementation. Beyond the technical framework, this work identifies critical barriers to adoption and provides actionable mitigation strategies for urban planners and stakeholders.

</details>


### [22] [AgenticRAG: Agentic Retrieval for Enterprise Knowledge Bases](https://arxiv.org/abs/2605.05538)
*Susheel Suresh,Hazel Mak,Shangpo Chou,Fred Kroon,Sahil Bhatnagar*

Main category: cs.AI

TL;DR: AgenticRAG is a lightweight agentic harness that enhances enterprise RAG systems by enabling iterative, tool-using LLM agents to search, navigate documents, and analyze evidence, achieving significant performance gains over standard retrieval pipelines.


<details>
  <summary>Details</summary>
Motivation: Standard RAG pipelines place excessive burden on the search stack to ground responses, constraining LLMs to a fixed set of candidates chosen early in retrieval. This creates overdependence on search infrastructure and limits the model's ability to iteratively explore and analyze information.

Method: A lightweight harness layered on existing enterprise search infrastructure that equips reasoning LLMs with four key tools: search, find, open, and summarize. This enables models to autonomously perform iterative information retrieval, navigate within documents, and analyze evidence through agentic tool use rather than single-shot retrieval.

Result: Substantial gains on three benchmarks: 49.6% recall@1 on BRIGHT (+21.8 pp over best embedding baseline), 0.96 factuality on WixQA (+13% relative improvement), and 92% answer correctness on FinanceBench (within 2 pp of oracle access). Ablation studies show agentic tool use provides 5.9× improvement over single-shot retrieval.

Conclusion: AgenticRAG demonstrates suitability for real-world enterprise production environments, with design choices informed by pre-production deployments. The shift from static retrieval to agentic tool use is the most significant factor in performance improvement, while multi-query search and in-document navigation contribute to both quality and efficiency gains.

Abstract: We present AgenticRAG, a practical agentic harness for retrieval and analysis over enterprise knowledge bases. Standard RAG pipelines place significant burden of grounding on the search stack, constraining the language model to a fixed candidate set chosen deep in the retrieval process. Our approach reduces this overdependence by layering a lightweight harness on top of existing enterprise search infrastructure, equipping a reasoning LLM with search, find, open, and summarize tools enabling the model to iteratively retrieve information, navigate within documents, and analyze evidence autonomously. On three open benchmarks we observe substantial gains: $49.6\%$ recall@1 on BRIGHT (+21.8 pp over the best embedding baseline), 0.96 factuality on WixQA ($+13\%$ relative improvement), and $92\%$ answer correctness on FinanceBench--within 2 pp of oracle access to true evidence. Ablation studies show that the most significant factor is the shift from single-shot retrieval to agentic tool use ($5.9\times$ improvement), while multi-query search and in-document navigation contribute to both quality and efficiency. We present various design choices in our agentic harness that were informed by pre-production deployments. Our results demonstrate its suitability for real-world enterprise production environments.

</details>


### [23] [SPARK: Self-Play with Asymmetric Reward from Knowledge Graphs](https://arxiv.org/abs/2605.05546)
*Hyobin Park,Taeseop Kim,Dong-Geol Choi*

Main category: cs.AI

TL;DR: SPARK is a self-play reinforcement learning framework that uses knowledge graphs from scientific literature to generate relational reasoning questions and compute verifiable rewards, improving performance on multi-hop QA tasks.


<details>
  <summary>Details</summary>
Motivation: Self-play reinforcement learning works well in structured domains (math, coding) but faces challenges in scientific literature where relationships are implicit, making automatic question generation difficult and reward signals unreliable.

Method: SPARK automatically constructs unified knowledge graphs from multi-document scientific literature, uses KG paths for generating relational reasoning questions, and employs structured facts for verifiable reward computation. A single small VLM alternates between Proposer and Solver roles with information asymmetry against a fixed KG.

Result: SPARK consistently outperforms flat-corpus-based self-play baselines on public benchmarks and a self-constructed cross-document multi-hop QA dataset. The performance gap widens with increasing hop count, showing KG-structure grounding improves relational multi-hop reasoning.

Conclusion: KG-based self-play effectively extends self-play reinforcement learning to scientific literature by providing structural grounding for question generation and reward computation, enabling better relational reasoning than unstructured corpus approaches.

Abstract: Self-play reinforcement learning has shown strong performance in domains with formally verifiable structure, such as mathematics and coding, where both problem generation and reward computation can be grounded in explicit rules. Extending this paradigm to scientific literature is more challenging: the relationships among multi-modal elements within and across documents are rarely made explicit in text, which makes automatic generation of relational reasoning questions difficult and weakens the reliability of reward signals. We propose SPARK (Self-Play with Asymmetric Reward from Knowledge Graphs), a framework that automatically constructs a unified knowledge graph (KG) from multi-document scientific literature and uses it as the structural basis for self-play. KG paths over multimodal nodes serve as a source for generating relational reasoning questions, and structured facts stored in the KG provide a basis for verifiable reward computation. A single small vision-language model (sVLM) alternates between Proposer and Solver roles under information asymmetry against a fixed KG, a design that we believe can be naturally extended toward online adaptation in future work. We evaluate SPARK on public benchmarks and a self-constructed cross-document multi-hop QA dataset. Results show that SPARK consistently outperforms flat-corpus-based self-play baselines, and the performance gap widens as hop count increases, suggesting that KG-structure grounding contributes to relational multi-hop reasoning beyond what unstructured corpus grounding can provide.

</details>


### [24] [Who Prices Cognitive Labor in the Age of Agents? A Position on Compute-Anchored Wages](https://arxiv.org/abs/2605.05558)
*Siqi Zhu*

Main category: cs.AI

TL;DR: AI agents are not labor but a production technology that converts compute capital into cognitive labor, meaning wages are bounded by compute costs rather than labor supply.


<details>
  <summary>Details</summary>
Motivation: To correct the common intuition that AI agents drive cognitive wages to zero by showing they're infinitely elastic labor supply. The paper argues this framing is wrong and that understanding the true mechanism matters for theory and policy.

Method: Reconceptualize AI agents as a production technology converting compute capital into cognitive labor. Apply textbook factor-pricing framework to derive a Compute-Anchored Wage (CAW) bound using factor pricing equations. Generalize through CES aggregation and analyze substitutable vs. complementary tasks.

Result: Derives the CAW bound: on substitutable tasks, competitive human wage is bounded above by λ·k·r_c (where r_c is compute rental rate, k is compute intensity, λ is relative human-to-agent productivity). Shows wage anchor shifts from labor to compute capital market, with implications for skill-biased technical change and factor shares.

Conclusion: The price-setter for cognitive labor is no longer the labor market but the compute capital market. This corrected understanding has important theoretical and policy implications for how AI will affect wages and labor markets.

Abstract: A natural intuition about the economics of AI agents is that, because agents can be replicated at near-zero marginal cost, they constitute a labor input in infinitely elastic supply, and therefore drive cognitive-labor wages to zero. We argue this framing is wrong in mechanism but partially correct in conclusion, and that the correction matters for both theory and policy. \textbf{Agents are not labor; they are a production technology that converts compute capital $K_c$ into effective units of cognitive labor $L_A$.} Once this is recognized, the elastic-supply margin that anchors the equilibrium wage migrates from the labor market to the compute capital market. Building on the textbook factor-pricing framework \citep{mankiw2020}, we derive a \emph{Compute-Anchored Wage} (CAW) bound stating that, on tasks where human and agent cognitive labor are substitutes, the competitive human wage is bounded above by $λ\cdot k \cdot r_c$, where $r_c$ is the rental rate of compute capital, $k$ is the compute intensity of one effective agent-labor unit, and $λ$ is the relative human-to-agent productivity. We generalize the result through CES aggregation, separate substitutable from complementary tasks (yielding a directional inversion of skill-biased technical change), and discuss factor-share consequences. The position is concise: \emph{the price-setter for cognitive labor is no longer the labor market.}

</details>


### [25] [BitCal-TTS: Bit-Calibrated Test-Time Scaling for Quantized Reasoning Models](https://arxiv.org/abs/2605.05561)
*Sai Babu Patarlapalli,Surya Teja Avvaru*

Main category: cs.AI

TL;DR: BitCal-TTS: A lightweight runtime controller for 4-bit quantized models that prevents premature stopping during reasoning by combining uncertainty proxies, bit-aware confidence scaling, and confirmation horizons.


<details>
  <summary>Details</summary>
Motivation: Post-training quantization distorts online signals that drive adaptive test-time compute allocation, causing harmful early halting where models may output plausible but incorrect reasoning due to miscalibrated confidence under fixed token budgets.

Method: BitCal-TTS combines (1) inexpensive online proxies for token-level uncertainty and reasoning-trace stability, (2) bit-conditioned confidence rescaling that's conservative at low precision, and (3) bit-aware post-marker confirmation horizons for GSM8K-style outputs. Requires no fine-tuning and integrates with Hugging Face 4-bit inference using forward hooks.

Result: On GSM8K evaluation shards, BitCal-TTS improves exact-match accuracy over non-bit-aware baselines: +3.7 points for 7B and +2.8 points for 14B models at B=512 token cap. Premature-stop rate reduced from 14.8% to 11.1% (7B) and 17.1% to 11.4% (14B). Preserves substantial token savings relative to fixed-budget decoding.

Conclusion: BitCal-TTS effectively addresses the interaction between quantization and adaptive stopping, providing a lightweight solution that improves reasoning reliability of 4-bit quantized models without requiring model fine-tuning, though evaluations are limited to small shards and statistical power is constrained.

Abstract: Post-training quantization makes large reasoning models practical under tight memory and latency budgets, but it can distort the online signals that drive adaptive test-time compute allocation. Under a fixed cap on the number of newly generated tokens, miscalibrated confidence can lead to harmful early halting: the model may surface a plausible final line while the underlying reasoning is still wrong, or the controller may stop before the trace has stabilized. We study this interaction for greedy 4-bit inference and propose BitCal-TTS, a lightweight runtime controller that combines (i) inexpensive online proxies for token-level uncertainty and reasoning-trace stability, (ii) a bit-conditioned confidence rescaling that is conservative at low nominal precision, and (iii) a bit-aware post-marker confirmation horizon designed for GSM8K-style structured outputs. The method requires no fine-tuning of the base model and integrates with standard Hugging Face 4-bit inference using forward hooks for logits and last-layer hidden states.
  On small evaluation shards of GSM8K with Qwen2.5 Instruct models, BitCal-TTS improves exact-match accuracy over a non-bit-aware adaptive baseline at the 7B and 14B scales while preserving substantial token savings relative to fixed-budget decoding. At a token cap of B=512, on the evaluation shards we report (N=54 for 7B and N=35 for 14B; not the full GSM8K test set), accuracy gains are +3.7 points (7B) and +2.8 points (14B), with the premature-stop rate falling from 14.8% to 11.1% on 7B and from 17.1% to 11.4% on 14B. We report Wilson 95% confidence intervals throughout and explicitly discuss the limited statistical power of the partial-shard comparisons. We release code and figure-generation scripts to support full reproduction.

</details>


### [26] [Nonsense Helps: Prompt Space Perturbation Broadens Reasoning Exploration](https://arxiv.org/abs/2605.05566)
*Langlin Huang,Chengsong Huang,Jinyuan Li,Donghong Cai,Yuyi Yang,Jiaxin Huang*

Main category: cs.AI

TL;DR: LoPE (Lorem Perturbation for Exploration) is a training framework that uses task-irrelevant Latin-based prompt perturbations to solve the "zero-advantage problem" in LLM reinforcement learning, enabling better exploration of reasoning pathways for hard questions.


<details>
  <summary>Details</summary>
Motivation: GRPO (Group Relative Policy Optimization) suffers from the "zero-advantage problem" where all sampled rollouts for a query fail, causing the relative advantage to collapse to zero. This wastes training data and computational budget, and simply increasing sampling budget doesn't solve the exploration bottleneck.

Method: LoPE prepends sequences stochastically assembled from Lorem Ipsum vocabulary (pseudo-Latin placeholder text) to prompts before resampling. This creates task-irrelevant prompt-space perturbations that shift the model's output distribution to unlock orthogonal reasoning pathways for hard questions.

Result: Experiments across 1.7B, 4B, and 7B models show LoPE significantly outperforms resampling with original prompts. Further analysis reveals other Latin-based random sequences with low perplexity are also effective perturbations.

Conclusion: LoPE establishes a strong baseline for broadening exploration in LLM reinforcement learning by solving the zero-advantage problem through simple yet effective prompt perturbations that enhance reasoning exploration.

Abstract: Reinforcement learning with verifiable rewards, particularly Group Relative Policy Optimization (GRPO), has significantly advanced the reasoning capabilities of Large Language Models (LLMs). However, in complex tasks, GRPO frequently suffers from the ``zero-advantage problem'': when all sampled rollouts for a query fail, the relative advantage collapses to zero. Consequently, the model loses effective training signals for these questions, wasting the training data and computational budget. While simply increasing the sampling budget for these questions is a common remedy, the static sampling policy inherently constrains reasoning exploration, limiting the success rate. In this paper, we propose Lorem Perturbation for Exploration (LoPE), a simple yet effective training framework to break this exploration bottleneck. We posit that task-irrelevant prompt-space perturbations can shift the model's output distribution enough to unlock orthogonal reasoning pathways for hard questions. Specifically, LoPE prepends sequences stochastically assembled from Lorem Ipsum vocabulary (a pseudo-Latin placeholder text) to the prompts before resampling. Experiments across 1.7B, 4B, and 7B models demonstrate that LoPE significantly outperforms resampling with the original prompts. Further analysis reveals that other Latin-based random sequences with low perplexity are also effective perturbations. Our results establish LoPE as a strong baseline for broadening exploration in LLM reinforcement learning.

</details>


### [27] [Locality-aware Private Class Identification for Domain Adaptation with Extreme Label Shift](https://arxiv.org/abs/2605.05567)
*Chuan-Xian Ren,Cheng-Jun Guo,Hong Yan*

Main category: cs.AI

TL;DR: A new domain adaptation method called ReOT that identifies private classes via local optimal transport and mitigates their negative effects to handle severe label shifts between domains.


<details>
  <summary>Details</summary>
Motivation: Domain adaptation faces challenges when source and target domains have non-overlapping private classes. Existing methods assume private classes are outliers with large shifts, but in reality, variance within shared classes can exceed differences between private and shared classes, making private class identification difficult and worsening cross-domain classification.

Method: Proposes a locality-aware private class identification approach using optimal transport (OT) properties. Develops a score function on transport mass to distinguish shared vs. private classes. Introduces ReOT method that minimizes classification risk while learning separated cluster structures between identified shared and private classes, avoiding mismatch between shared-private sample pairs and ensuring reliable intra-class knowledge transport.

Result: Theoretical proof shows the score function effectively distinguishes shared and private classes. ReOT is validated through extensive experiments on benchmarks. Provides generalization upper bound for target risk in extreme label shift scenarios, which ReOT can minimize.

Conclusion: ReOT effectively addresses domain adaptation under severe label shift by reliably identifying private classes and transporting knowledge within shared classes, outperforming existing methods and handling challenging cases where traditional assumptions fail.

Abstract: Domain adaptation aims to transfer knowledge from a labeled source domain to an unlabeled target domain with different distributions. In real-world scenarios, the label spaces of the two domains often have an inclusion relationship, where some classes exist only in one domain but not the other. These non-overlapping classes are referred to as private classes. Identifying private class samples and mitigating their adverse effects is critical in the literature. Existing methods rely on the assumption that shifts in private classes are large enough to be considered outliers. However, the variance within a single shared class can be significantly larger than the difference between a private class and another shared class, challenging this assumption. Consequently, private classes substantially increase the difficulty of cross-domain classification. To address these issues, based on local transportation and metric properties of optimal transport (OT), a locality-aware private class identification approach is proposed in the form of a score function on transport mass. The effectiveness of the proposed approach is theoretically proven, highlighting the score function's strong ability to distinguish between shared and private class samples. Building on this, we introduce a reliable OT-based method (ReOT) for domain adaptation under severe label shift. ReOT minimizes classification risk while learning the separated cluster structure between the identified shared classes and private classes, effectively avoiding mismatch between shared-private sample pairs, thus ensuring that important knowledge is reliably transported intra-class to mitigate class-conditional discrepancy. Furthermore, a generalization upper bound of the target risk is provided for extreme label shift scenarios, which can be minimized by ReOT. Extensive experiments on benchmarks validate the effectiveness of ReOT.

</details>


### [28] [AlphaCrafter: A Full-Stack Multi-Agent Framework for Cross-Sectional Quantitative Trading](https://arxiv.org/abs/2605.05580)
*Yishuo Yuan,Jiayi Sheng,Sirui Zeng,Jiaqi Wang,Jiaheng Liu*

Main category: cs.AI

TL;DR: AlphaCrafter is an automated multi-agent framework that unifies factor discovery, regime-adaptive selection, and risk-constrained execution in a closed-loop system for quantitative trading.


<details>
  <summary>Details</summary>
Motivation: Current approaches in quantitative trading optimize components under static assumptions - factor mining assumes persistent efficacy across regimes, while execution systems use anthropomorphic agents introducing behavioral noise rather than systematic rationality. There's a need for a fully automated, rationality-driven framework that unifies the entire quantitative pipeline.

Method: AlphaCrafter uses three specialized agents: 1) Miner continuously expands factor pool via LLM-guided search, 2) Screener assesses market conditions to construct regime-conditioned factor ensembles, 3) Trader translates ensembles into quantitative strategies under explicit risk constraints. Together they form a closed-loop cross-sectional trading system.

Result: Extensive experiments on CSI 300 and S&P 500 show AlphaCrafter consistently outperforms state-of-the-art baselines in risk-adjusted returns while exhibiting the lowest cross-trial variance.

Conclusion: An integrated and adaptive factor-to-execution design yields robust trading performance, addressing the limitations of existing fragmented approaches in quantitative finance.

Abstract: Financial markets are inherently non-stationary, driven by complex interactions among macroeconomic regimes, microstructural frictions, and behavioral dynamics. Building quantitative strategies that remain profitable demands the continuous coupling of factor discovery, regime-adaptive selection, and risk-constrained execution. Prevailing approaches, however, optimize these components under static or isolated assumptions. Factor mining frameworks typically treat alpha discovery as a one-time search process, implicitly assuming that factor efficacy persists across market regimes. Execution-oriented systems often adopt role-playing agent architectures that simulate anthropomorphic trading committees, introducing behavioral noise rather than systematic rationality. Consequently, a fully automated, rationality-driven framework unifying a coherent quantitative pipeline remains absent. We introduce AlphaCrafter, a full-stack multi-agent framework that closes this gap through a continuously adaptive factor-to-execution pipeline, designed to track and respond to evolving market conditions without manual intervention. AlphaCrafter operates via three specialized agents: a Miner that continuously expands the factor pool via LLM-guided search, a Screener that assesses prevailing market conditions to construct regime-conditioned factor ensembles, and a Trader that translates these ensembles into quantitative strategies under explicit risk constraints. Together, these three agents form a closed-loop cross-sectional trading system that adapts holistically to evolving market dynamics. Extensive experiments on CSI 300 and S&P 500 demonstrate that AlphaCrafter consistently outperforms state-of-the-art baselines in risk-adjusted returns while exhibiting the lowest cross-trial variance, confirming that integrated and adaptive factor-to-execution design yields robust trading performance.

</details>


### [29] [Belief Memory: Agent Memory Under Partial Observability](https://arxiv.org/abs/2605.05583)
*Junfeng Liao,Qizhou Wang,Jianing Zhu,Bo Du,Rui Yan,Xiuying Chen*

Main category: cs.AI

TL;DR: BeliefMem is a probabilistic memory system for LLM agents that stores multiple candidate conclusions with probabilities instead of single deterministic facts, addressing self-reinforcing errors in partially observable environments.


<details>
  <summary>Details</summary>
Motivation: Current LLM agent memory systems store each observation as a single deterministic conclusion, which introduces self-reinforcing errors when observations are partial or ambiguous. By committing to one conclusion and discarding uncertainty, agents act on potentially wrong information and never revisit alternatives, leading to error reinforcement over time.

Method: BeliefMem shifts from storing single conclusions to retaining multiple candidate conclusions with probabilities. It stores candidates as separate memory entries, each with a probability updated via Noisy-OR rules as new observations arrive. During retrieval, all candidates surface together with their probabilities, keeping alternatives visible to the agent.

Result: Empirical evaluations on LoCoMo and ALFWorld benchmarks show BeliefMem achieves the best average performance even with limited data, remarkably outperforming well-known baselines. The probabilistic memory approach produces substantial gains in agent performance.

Conclusion: BeliefMem explores a new direction for agent memory in partially observable environments by preserving uncertainty that deterministic paradigms discard, enabling agents to act with confidence on well-evidenced knowledge while retaining the capacity to update confidence when new evidence arrives.

Abstract: LLM agents that operate over long context depend on external memory to accumulate knowledge over time. However, existing methods typically store each observation as a single deterministic conclusion (e.g., inferring "API~X failed" from temporary errors), even though such observations are inherently partial and potentially ambiguous. By committing to one conclusion and discarding uncertainty, these methods introduce self-reinforcing error: the agent acts on the stored conclusion, never revisits alternatives, and reinforces the conclusion over time. To address this issue, we propose BeliefMem, which shifts the memory paradigm from committing to a single conclusion per observation to retaining multiple candidate conclusions with their probabilities. Concretely, BeliefMem stores the candidate conclusions as separate memory entries, each carrying a probability that is updated via Noisy-OR rules as new observations arrive. At retrieval, all candidates surface together with their probabilities, keeping alternatives visible to the agent. Since each conclusion in memory retains its probability, BeliefMem preserves the uncertainty that the deterministic paradigm discards, enabling the agent to act with high confidence on well-evidenced knowledge while retaining the capacity to update its confidence when new evidence arrives. Empirical evaluations on LoCoMo and ALFWorld benchmarks show that, even with limited data, BeliefMem achieves the best average performance, remarkably outperforming well-known baselines. More broadly, such probabilistic memory produces substantial gains and explores a new direction for agent memory in partially observable environments.

</details>


### [30] [Causal Probing for Internal Visual Representations in Multimodal Large Language Models](https://arxiv.org/abs/2605.05593)
*Zehao Deng,Tianjie Ju,Zheng Wu,Liangbo He,Jun Lan,Huijia Zhu,Weiqiang Wang,Zhuosheng Zhang*

Main category: cs.AI

TL;DR: MLLMs encode visual concepts differently: entities are locally memorized while abstract concepts are globally distributed across layers. Scaling benefits abstract concepts more, and there's a perception-reasoning disconnect.


<details>
  <summary>Details</summary>
Motivation: Despite MLLMs' success, there's poor understanding of how they internally encode and ground different visual concepts. The researchers want to understand the mechanistic differences between how entities vs. abstract concepts are represented.

Method: Propose a causal framework using activation steering to probe and manipulate internal visual representations. Systematic interventions across four visual concept categories.

Result: 1) Divergent encoding: entities show localized memorization, abstract concepts are globally distributed. 2) Scaling laws driver: model depth is crucial for abstract concepts while entity localization is scale-invariant. 3) Reverse steering reveals compensatory mechanism between perception/generation. 4) Visual reasoning disconnect: MLLMs treat geometric relations as static features rather than triggering procedural reasoning.

Conclusion: The paper reveals fundamental differences in how MLLMs encode different visual concepts, explains scaling law mechanisms, exposes perception-generation tradeoffs, and identifies a critical gap between perception and reasoning in visual problem-solving.

Abstract: Despite the remarkable success of Multimodal Large Language Models (MLLMs) across diverse tasks, the internal mechanisms governing how they encode and ground distinct visual concepts remain poorly understood. To bridge this gap, we propose a causal framework based on activation steering to actively probe and manipulate internal visual representations. Through systematic intervention across four visual concept categories, our results reveal a divergence in concept encoding: entities exhibit distinct localized memorization, whereas abstract concepts are globally distributed across the network. Critically, this divergence uncovers a mechanistic driver of scaling laws: increasing model depth is indispensable for encoding distributed and complex abstract concepts, whereas entity localization remains remarkably invariant to scale. Furthermore, reverse steering uncovers that blocking explicit output triggers a surge in latent activations, exposing a compensatory mechanism between perception and generation. Finally, extending our analysis to visual reasoning, we expose a disconnect between perception and reasoning although MLLMs successfully recognize geometric relations, they treat them merely as static visual features, failing to trigger the procedural execution necessary for abstract problem-solving.

</details>


### [31] [Prober.ai: Gated Inquiry-Based Feedback via LLM-Constrained Personas for Argumentative Writing Development](https://arxiv.org/abs/2605.05598)
*Ran Bi,Shiyao Wei,Yuanyiyi Zhou*

Main category: cs.AI

TL;DR: Prober.ai is an AI writing tool that uses constrained LLMs to ask targeted questions about argumentative weaknesses rather than generating text, implementing pedagogical friction through a Challenge-Unlock architecture to preserve student cognition.


<details>
  <summary>Details</summary>
Motivation: Current AI tools in education (especially LLMs) undermine critical thinking by allowing students to outsource cognitive work to AI assistants, leading to cognitive debt and diminished argumentative reasoning skills.

Method: Developed Prober.ai, a web-based writing environment that constrains Gemini 3 Flash Preview LLM through persona-specific system prompts and structured JSON output schemas to produce only targeted, inquiry-based questions. Uses a two-phase Challenge-Unlock architecture that gates revision suggestions behind mandatory student reflection.

Result: Created a functional prototype in 36 hours during the NY EdTech Hackathon (March 2026) where it won second place. The system successfully implements a pedagogical friction mechanism based on Toulmin's argumentation theory, peer feedforward questioning, and AI-supported feedback research.

Conclusion: Prober.ai demonstrates a scalable approach to cognition-preserving AI integration in writing education by inverting the conventional AI-tutoring paradigm, using constrained questioning rather than text generation to maintain student cognitive engagement.

Abstract: The proliferation of large language models (LLMs) in educational settings has paradoxically undermined the cognitive processes they purport to support. Students increasingly outsource critical thinking to AI assistants that generate polished text on demand, resulting in measurable cognitive debt and diminished argumentative reasoning skills. We present Prober.ai, a web-based writing environment that inverts the conventional AI-tutoring paradigm: rather than generating or rewriting student text, the system constrains an LLM (Gemini 3 Flash Preview) through persona-specific system prompts and structured JSON output schemas to produce only targeted, inquiry-based questions about argumentative weaknesses. A two-phase interaction architecture -- Challenge and Unlock -- implements a pedagogical friction mechanism whereby revision suggestions are gated behind mandatory student reflection. The system's design is grounded in Toulmin's argumentation theory, research on peer feedforward questioning mechanisms, and evidence on AI-supported feedback in writing instruction. A functional prototype was developed in 36 hours during the NY EdTech Hackathon (March 2026), where it was awarded second place. We describe the system architecture, the prompt engineering methodology for constraining LLM output to pedagogically aligned JSON schemas, and discuss implications for scalable, cognition-preserving AI integration in writing education.

</details>


### [32] [Text-Graph Synergy: A Bidirectional Verification and Completion Framework for RAG](https://arxiv.org/abs/2605.05643)
*Jiarui Zhong,Hong Cai Chen*

Main category: cs.AI

TL;DR: TGS-RAG is a new framework that synergistically enhances text and graph retrieval for RAG systems using bidirectional channels to address the "Information Island" problem and improve multi-hop reasoning.


<details>
  <summary>Details</summary>
Motivation: Current RAG systems have limitations: text-based RAG often retrieves irrelevant evidence, graph-based RAG suffers from search-time pruning that discards valid reasoning paths, and existing hybrid approaches fail to address the "Information Island" problem caused by asymmetric reasoning flows between unstructured text and structured graphs.

Method: TGS-RAG introduces a bidirectional synergistic framework with: 1) Graph-to-Text channel using Global Voting strategy to re-rank and refine textual evidence by filtering semantic noise, and 2) Text-to-Graph channel using Memory-based Orphan Entity Bridging algorithm to proactively resurrect valid but previously pruned reasoning paths from search history without additional database overhead.

Result: Experimental results on multiple multi-hop reasoning benchmarks show that TGS-RAG significantly outperforms state-of-the-art baselines, achieving superior balance between retrieval precision and computational efficiency.

Conclusion: TGS-RAG effectively addresses the limitations of traditional RAG approaches by synergistically enhancing text and graph evidence through bidirectional channels, solving the "Information Island" problem and improving multi-hop reasoning performance.

Abstract: Retrieval-Augmented Generation (RAG) has become a core paradigm for enhancing factual grounding and multi-hop reasoning in Large Language Models (LLMs). Traditional text-based RAG often retrieves logically irrelevant pseudo-evidence, while graph-based RAG is frequently hindered by search-time pruning, which may discard potentially valid reasoning paths. Existing hybrid approaches primarily adopt simple evidence concatenation or unidirectional enhancement, which fails to address the fundamental "Information Island" problem caused by asymmetric reasoning flows between unstructured text and structured graphs. We propose \textbf{TGS-RAG}, a unified framework for \textbf{T}ext-\textbf{G}raph \textbf{S}ynergistic enhancement. TGS-RAG introduces a bidirectional mechanism: (i) a \textbf{Graph-to-Text} channel that employs a Global Voting strategy from visited graph nodes to re-rank and refine textual evidence, filtering out semantic noise; and (ii) a \textbf{Text-to-Graph} channel that utilizes the \textbf{Memory-based Orphan Entity Bridging} algorithm. This algorithm utilizes textual cues to proactively resurrect valid but previously pruned reasoning paths from the search history without additional database overhead. Experimental results on multiple multi-hop reasoning benchmarks demonstrate that TGS-RAG significantly outperforms state-of-the-art baselines, achieving a superior balance between retrieval precision and computational efficiency.

</details>


### [33] [Large Vision-Language Models Get Lost in Attention](https://arxiv.org/abs/2605.05668)
*Gongli Xi,Ye Tian,Mengyu Yang,Huahui Yi,Liang Lin,Xiaoshuai Hao,Kun Wang,Wendong Wang*

Main category: cs.AI

TL;DR: Attention in LVLMs serves as subspace-preserving reconfiguration while FFNs drive semantic innovation, but attention weights are often redundant and can be replaced with predefined values without performance loss.


<details>
  <summary>Details</summary>
Motivation: To understand the distinct roles of internal modules in large vision-language models (LVLMs) and guide architectural optimization, as current approaches lack a unified theoretical basis for analyzing residual Transformer architectures.

Method: Proposes a unified framework grounded in information theory and geometry to quantify the geometric and entropic nature of residual updates in Transformer decoders, applying this framework to analyze attention and FFN modules.

Result: Reveals fundamental functional decoupling: attention acts as subspace-preserving operator for reconfiguration, while FFNs serve as subspace-expanding operators driving semantic innovation. Experiments show learned attention weights can be replaced with predefined values (e.g., Gaussian noise) with comparable or superior performance across most datasets.

Conclusion: Current LVLMs suffer from severe misallocation and redundancy in attention mechanisms, effectively "getting lost in attention" rather than efficiently leveraging visual context, suggesting attention weights may not be as critical as assumed and highlighting potential architectural optimizations.

Abstract: Despite the rapid evolution of training paradigms, the decoder backbone of large vision--language models (LVLMs) remains fundamentally rooted in the residual-connection Transformer architecture. Therefore, deciphering the distinct roles of internal modules is critical for understanding model mechanics and guiding architectural optimization. While prior statistical approaches have provided valuable attribution-based insights, they often lack a unified theoretical basis. To bridge this gap, we propose a unified framework grounded in information theory and geometry to quantify the geometric and entropic nature of residual updates. Applying this unified framework reveals a fundamental functional decoupling: Attention acts as a subspace-preserving operator focused on reconfiguration, whereas FFNs serve as subspace-expanding operators driving semantic innovation. Strikingly, further experiments demonstrate that replacing learned attention weights with predefined values (e.g., Gaussian noise) yields comparable or even superior performance across a majority of datasets relative to vanilla models. These results expose severe misallocation and redundancy in current mechanisms, suggesting that state-of-the-art LVLMs effectively ``get lost in attention'' rather than efficiently leveraging visual context.

</details>


### [34] [Chain of Risk: Safety Failures in Large Reasoning Models and Mitigation via Adaptive Multi-Principle Steering](https://arxiv.org/abs/2605.05678)
*Xiaomin Li,Jianheng Hou,Zheyuan Deng,Zhiwei Zhang,Taoran Li,Binghang Lu,Bing Hu,Yunhan Zhao,Yuexing Hao*

Main category: cs.AI

TL;DR: Large reasoning models expose chain-of-thought reasoning, creating a safety blind spot where harmful content appears in reasoning traces even when final answers look safe. Testing reveals reasoning traces consistently show additional safety risks beyond final answers, particularly in leak cases (unsafe reasoning → safe answer) and escape cases (safe reasoning → unsafe answer).


<details>
  <summary>Details</summary>
Motivation: Large reasoning models increasingly expose chain-of-thought-like reasoning for transparency, but this creates a safety blind spot where harmful or policy-violating content may appear in reasoning traces even when final answers appear safe. The paper tests whether final-answer safety is sufficient for evaluating the full reasoning-answer trajectory.

Method: Used a unified twenty-principle safety rubric to score both reasoning traces and final answers. Evaluated 15 open-weight and API-based LRMs across 41K prompts per model from seven public harmfulness/jailbreak sources plus four OOD sources. Proposed adaptive multi-principle steering - a white-box test-time mitigation that learns unsafe-to-safe activation directions per safety principle and activates only when hidden states are closer to unsafe than safe centroids.

Result: Reasoning traces consistently reveal additional safety risks beyond final answers, especially in high-severity stage-wise failures: leak cases (unsafe reasoning → safe answer) and escape cases (safe reasoning → unsafe answer). Risk concentrates in misinformation, legal compliance, discrimination, physical harm, and psychological harm. Adaptive steering reduces unsafe counts in both reasoning traces and final answers on held-out and OOD benchmarks. DeepSeek-R1-Qwen-7B achieves 40.8% average unsafe-count reduction while retaining 97.7% macro-averaged accuracy.

Conclusion: LRM safety should be evaluated and mitigated over the full exposed reasoning-answer trajectory, not only at the final-answer stage. The proposed adaptive multi-principle steering effectively reduces safety risks while maintaining model performance.

Abstract: Large reasoning models (LRMs) increasingly expose chain-of-thought-like reasoning for transparency, verification, and deliberate problem solving. This creates a safety blind spot: harmful or policy-violating content may appear in reasoning traces even when final answers appear safe. We test whether final-answer safety is a sufficient proxy for the full reasoning-answer trajectory by scoring both stages under a unified twenty-principle safety rubric. Using prompts from seven public harmfulness and jailbreak sources, plus four out-of-distribution (OOD) sources, we evaluate 15 open-weight and API-based LRMs across 41K prompts per model. Reasoning traces consistently reveal additional safety risks beyond final answers, especially in high-severity stage-wise failures: leak cases, where unsafe reasoning precedes a safe-looking answer, and escape cases, where benign-looking reasoning precedes an unsafe final response. Principle-level analysis shows that risk concentrates in misinformation, legal compliance, discrimination, physical harm, and psychological harm. We further propose adaptive multi-principle steering, a white-box test-time mitigation that learns one unsafe-to-safe activation direction per safety principle and activates only directions whose current hidden state is closer to the unsafe than safe centroid. On three steerable open reasoning models, adaptive steering reduces unsafe counts in both reasoning traces and final answers on held-out and OOD benchmarks. DeepSeek-R1-Qwen-7B achieves a 40.8% average unsafe-count reduction while retaining 97.7% macro-averaged accuracy on BBH, GSM8K, and MMLU. These results suggest that LRM safety should be evaluated and mitigated over the full exposed reasoning-answer trajectory, not only at the final-answer stage.

</details>


### [35] [Attractor Geometry of Transformer Memory: From Conflict Arbitration to Confident Hallucination](https://arxiv.org/abs/2605.05686)
*Qiyao Liang,Risto Miikkulainen,Ila Fiete*

Main category: cs.AI

TL;DR: Language models fail in two ways: conflict (when internal knowledge conflicts with context) and hallucination (when knowledge was never learned), both produce confident outputs. Hidden state geometry reveals these failures through distance to memorized attractor basins, providing better detection than output entropy.


<details>
  <summary>Details</summary>
Motivation: Current language models fail in two mechanistically distinct ways but produce similarly confident outputs, making output-based monitoring ineffective. There's a need to understand the underlying geometric mechanisms behind these failures and develop better detection methods.

Method: The authors use a unified geometric framework where learned facts form attractor basins in hidden-state space. They study conflict as basin competition and hallucination as basin absence. They verify this using controlled synthetic tasks with entity identifiers mapped to unique codes, using LoRA adapters for precise control over parametric memory placement. They measure geometric margin (distance to nearest memorized basin) versus output entropy.

Result: Geometric margin separates correct recall from hallucination much more cleanly than output entropy, with zero false refusals where entropy-based detection rejects most correct outputs. Hidden states reliably encode epistemic state, but the frozen output head systematically erases this information. The fraction of confident hallucinations follows a scaling law C = exp(-c/Δ̄), increasing with model scale despite falling overall error rates.

Conclusion: The paper provides a unified geometric account of LM failure modes through attractor basins in hidden-state space. Hidden state geometry offers superior failure detection compared to output-based methods, revealing that the frozen output head systematically hides epistemic uncertainty, and this problem worsens with increasing model scale.

Abstract: Language models draw on two knowledge sources: facts baked into weights (parametric memory, PM) and information in context (working memory, WM). We study two mechanistically distinct failure modes--conflict, when PM and WM disagree and interfere; and hallucination, when the queried fact was never learned. Both produce confident output regardless, making output-based monitoring blind by design. We show both failures share a unified geometric account. In the hidden-state space of autoregressive generation, learned facts form attractor basins. Conflict is basin competition: WM disrupts convergence to the correct basin without raising output entropy. Hallucination is basin absence: the hidden state drifts freely when no memorized basin exists. The frozen LM head, designed for next-token prediction, cannot distinguish these cases and fires confidently either way. We verify this account in a controlled synthetic task--entity identifiers mapped to unique codes with PM installed via LoRA adapters--where ground truth is exact and component roles can be causally isolated through targeted adapter placement. Geometric margin--the hidden state's distance to the nearest memorized basin--reads this geometry directly and separates correct recall from hallucination far more cleanly than output entropy, with zero false refusals where entropy-based detection cannot avoid rejecting the vast majority of correct outputs. The separation holds on natural-language factual queries from the pretrained model with no adaptation, confirming attractor geometry is structural rather than a fine-tuning artifact. The fraction of confident hallucinations follows a scaling law $C = \exp(-c/\barΔ)$, growing with scale even as overall error rates fall. Hidden states reliably encode epistemic state; the frozen output head systematically erases it--and this erasure worsens with scale.

</details>


### [36] [DataDignity: Training Data Attribution for Large Language Models](https://arxiv.org/abs/2605.05687)
*Xiaomin Li,Andrzej Banburski-Fahey,Jaron Lanier*

Main category: cs.AI

TL;DR: The paper introduces FakeWiki, a benchmark for evaluating pinpoint provenance (identifying which source documents support LLM responses), and proposes two methods: SteerFuse (activation-steering retrieval-fusion) and ScoringModel (supervised contrastive provenance ranker), which significantly outperform retrieval baselines.


<details>
  <summary>Details</summary>
Motivation: Current language model auditing requires identifying specific source documents that support generated responses, not just judging correctness. There's a need for robust evaluation that separates true answer support from topical or lexical resemblance.

Method: 1) Created FakeWiki benchmark with 3,537 fabricated Wikipedia-style articles with QA probes, paraphrases, variants, and hard anti-documents. 2) Developed SteerFuse (training-free activation-steering retrieval-fusion). 3) Built ScoringModel (supervised contrastive provenance ranker trained with InfoNCE using multiple negative types).

Result: ScoringModel improved mean Recall@10 from 35.0 (best baseline) to 52.2 across 9 LLMs and 5 query conditions, winning 41/45 comparisons. SteerFuse was usually second-best without training. ScoringModel improved by 15.7 points on jailbreak-inspired queries over best baseline.

Conclusion: Robust training data attribution requires evaluation settings that separate true answer support from topical/lexical resemblance. Activation-space evidence can efficiently complement text retrieval, and supervised contrastive learning significantly improves pinpoint provenance.

Abstract: Auditing language-model outputs often requires more than judging correctness: an auditor may need to identify which source document most likely supports the knowledge expressed in a response. We study this as pinpoint provenance: given a prompt, a target-model response, and a candidate corpus, rank the documents that best support the response. We introduce FakeWiki, a controlled benchmark of 3,537 fabricated Wikipedia-style articles designed to preserve ground-truth provenance while weakening lexical shortcuts. FakeWiki includes QA probes, source-preserving paraphrases, retro-generated variants, hard anti-documents that remain topically similar while removing answer-critical facts, and five query conditions: clean prompting plus four jailbreak-inspired transformations. We evaluate seven retrieval baselines, a training-free activation-steering retrieval-fusion method, SteerFuse, and a supervised contrastive provenance ranker, ScoringModel. ScoringModel maps response and document features into a shared space and is trained with InfoNCE using in-batch, retrieval-mined, and anti-document negatives. Across nine open-weight instruction-tuned LLMs and five query conditions, ScoringModel improves mean Recall@10 from 35.0 for the strongest retrieval baseline to 52.2, without inference-time fusion, and wins 41/45 model-by-condition cells. SteerFuse is usually second-best despite requiring no supervised training, showing that activation-space evidence can efficiently complement text retrieval. On jailbreak-inspired transformed queries, ScoringModel improves Recall@10 by 15.7 points on average over the best baseline. Overall, our work shows that robust training data attribution requires evaluation settings that separate true answer support from topical or lexical resemblance.

</details>


### [37] [GCCM: Enhancing Generative Graph Prediction via Contrastive Consistency Model](https://arxiv.org/abs/2605.05689)
*Shaozhen Ma,Wei Huang,Hanchen Wang,Dong Wen,Wenjie Zhang*

Main category: cs.AI

TL;DR: GCCM introduces graph contrastive consistency modeling to prevent shortcut solutions in diffusion-based graph prediction, where models might ignore noisy targets and collapse to deterministic predictors, by adding contrastive separation requirements.


<details>
  <summary>Details</summary>
Motivation: Existing consistency training methods for graph prediction can fall into a shortcut solution where models ignore noisy targets and collapse into purely deterministic predictors, undermining the benefits of generative modeling.

Method: Proposes GCCM (Graph Contrastive Consistency Model) that introduces negative pairs into a contrastive consistency objective, going beyond pairwise matching between the same target at different noise levels. Also applies feature perturbation to input node/edge features to break identical conditioning, making shortcut solutions less attractive.

Result: Extensive experiments on benchmark datasets show that GCCM effectively mitigates the shortcut solution and yields consistent performance improvements in graph prediction compared to deterministic predictors.

Conclusion: The contrastive consistency approach with feature perturbation successfully prevents the collapse into deterministic predictors while maintaining the benefits of generative modeling for graph prediction tasks.

Abstract: Conditional generative models, particularly diffusion-based methods, have recently been applied to graph prediction by modeling the target as a conditional distribution given the input graph, yielding competitive results compared to deterministic predictor. However, existing diffusion-based prediction methods typically require expensive iterative denoising at inference and often suffer from unstable sampling, which motivates recent efforts to reduce inference denoising steps and enable stable sampling via techniques such as consistency training. Despite this progress, we find that existing consistency training methods for graph prediction could potentially fall into a shortcut solution: the model may attempt to satisfy the self-consistency constraint by ignoring the noisy target (i.e., assigning it negligible weight), ultimately collapsing into a purely deterministic predictor. To mitigate such shortcut solution, we propose GCCM, a graph contrastive consistency model that goes beyond isolated pairwise matching between the same target at different noise levels by introducing negative pairs into a contrastive consistency objective. This adds an additional separation requirement, making the shortcut solution no longer trivially sufficient to satisfy the proposed objective. Moreover, we apply feature perturbation to the input node/edge features to break identical conditioning on the input graph, so that the shortcut no longer yields the same predictions across noise levels and becomes less attractive. Extensive experiments on benchmark datasets demonstrate that GCCM mitigates the shortcut solution and yields consistent performance improvements in graph prediction compared to deterministic predictors.

</details>


### [38] [Saliency-Aware Regularized Quantization Calibration for Large Language Models](https://arxiv.org/abs/2605.05693)
*Yanlong Zhao,Xiaoyuan Cheng,Huihang Liu,Baihua He,Xinyu Zhang,Harrison Bo Hua Zhu,Wenlong Chen,Li Zeng,Zhuo Sun*

Main category: cs.AI

TL;DR: SARQC improves LLM post-training quantization by adding saliency-aware regularization to keep quantized weights close to original weights, enhancing generalization without inference overhead.


<details>
  <summary>Details</summary>
Motivation: Existing PTQ methods use limited calibration data to minimize reconstruction error, which can move quantized weights away from original weights and increase generalization risk, potentially degrading downstream performance.

Method: Proposes Saliency-Aware Regularized Quantization Calibration (SARQC) - a unified framework that augments standard PTQ objectives with a saliency-aware regularization term to encourage quantized weights to stay close to original weights during calibration.

Result: Extensive experiments on dense and Mixture-of-Experts LLMs show consistent improvements in perplexity and zero-shot accuracy without additional computational overhead during inference. The method integrates seamlessly into existing PTQ pipelines and enhances both scale search and Gram-based methods.

Conclusion: SARQC addresses the generalization risk problem in PTQ by regularizing calibration objectives, leading to better quantized models that maintain performance on downstream tasks while being computationally efficient.

Abstract: Post-training quantization (PTQ) is an effective approach for deploying large language models (LLMs) under memory and latency constraints. Most existing PTQ methods determine quantization parameters by minimizing a layer-wise reconstruction error on a predetermined calibration dataset, usually optimized via either scale search or Gram-based methods. However, from the perspective of generalization risk, existing calibration objectives of PTQ based only on empirical reconstruction error on limited or unrepresentative calibration data could move the quantized weights away from the original weights. This may cause the generalization risk to diverge, potentially degrading downstream performance. To address this issue, we propose \emph{Saliency-Aware Regularized Quantization Calibration} (SARQC) a unified framework that augments the standard PTQ objective with a saliency-aware regularization term. This term encourages quantized weights to stay close to the original weights during calibration, leading to improved generalization during inference. SARQC integrates seamlessly into existing PTQ pipelines, enhancing both scale search and Gram-based methods under a unified formulation. Extensive experiments on dense and Mixture-of-Experts LLMs demonstrate consistent improvements in perplexity and zero-shot accuracy, without additional computational overhead during inference.

</details>


### [39] [Inference-Time Budget Control for LLM Search Agents](https://arxiv.org/abs/2605.05701)
*Zhengru Fang,Senkang Forest Hu,Zhonghao Chang,Yu Guo,Yihang Tao,Hongyao Liu,Mengzhe Ruan,Jun Huang,Yuguang Fang*

Main category: cs.AI

TL;DR: A novel inference-time budget control method for LLM search agents that optimizes dual budgets (tool calls and tokens) in multi-hop QA through VOI-based action selection during search and selective answer refinement.


<details>
  <summary>Details</summary>
Motivation: LLM search agents face dual budget constraints (tool calls and generated tokens) during inference, requiring intelligent control over how to allocate limited resources between search actions and when to commit to final answers, rather than just relying on stronger models.

Method: Two-stage inference-time budget control: 1) Search-time control using Value-of-Information (VOI) scoring to choose among retrieval, decomposition, and answer commitment actions based on marginal task value per unit budget; 2) Selective evidence-grounded finalization that rewrites answers only when residual errors appear to be low-risk formatting issues.

Result: Positive aggregate gains across four multi-hop QA benchmarks, three LLM backbones, and four budget levels compared to four baselines under the same hard dual-budget protocol. Ablations show search-time budget control (especially budget-dependent penalty) provides main performance gains.

Conclusion: Inference-time budget control should govern both how budget is spent during search and how final answers are committed, with search-time control being crucial and answer-time control helping when retrieval paths are already adequate.

Abstract: LLM search agents increasingly rely on tools at inference time, but their trajectories are often constrained by hard limits on both tool calls and generated tokens. Under such dual budgets, better answers require not only stronger models, but also explicit control over which search action should receive the next budget unit and when the accumulated evidence is sufficient to commit a final answer. We study this problem in multi-hop question answering (QA) and formulate it as two-stage inference-time budget control. At search time, our controller assigns each feasible action a task-level Value-of-Information (VOI) score, defined as an operational estimate of marginal task value per unit budget under the current search state and remaining dual budget, and uses this score to choose among retrieval, decomposition, and answer commitment. After search, a selective evidence-grounded finalizer compares the trajectory answer with a refined candidate and rewrites only when the residual error appears to be a low-risk answer-form error. Across four multi-hop QA benchmarks, three LLM backbones, and four budget levels, the method yields positive aggregate gains over four audited baselines under the same hard dual-budget protocol. Ablations show that search-time budget control, especially budget-dependent penalty, provides the main performance gain, while answer-time control helps mainly when the retrieval path is already adequate. These results suggest that inference-time budget control for LLM search agents should govern both how budget is spent during search and how the final answer is committed.

</details>


### [40] [Knowledge-Graph Paths as Intermediate Supervision for Self-Evolving Search Agents](https://arxiv.org/abs/2605.05702)
*Huyu Wu,Jun Liu,Xiaochi Wei,Yan Gao,Yi Wu,Yao Hu*

Main category: cs.AI

TL;DR: A self-evolving search agent framework improves question generation and answering by reusing knowledge-graph paths as intermediate supervision for both question construction and reward shaping.


<details>
  <summary>Details</summary>
Motivation: Current Search Self-Play (SSP) frameworks have two bottlenecks: Proposers generate questions from isolated answer entities without relational context (yielding invalid/unverifiable questions), and Solvers receive only binary outcome rewards that discard useful signal from partially correct search trajectories.

Method: 1) Ground question construction in LLM-guided knowledge-graph subgraphs to provide relational context for the Proposer. 2) Introduce Waypoint Coverage Reward (WCR) that gives graded partial credit to incorrect Solver trajectories based on their coverage of entities on the question construction path, while preserving full reward for correct answers.

Result: Across seven QA benchmarks and nine model configurations, the approach improves average scores over standard SSP in all configurations, with notable gains on multi-hop QA tasks.

Conclusion: Knowledge-graph paths can be reused as lightweight intermediate supervision, providing both relational guidance and process feedback without additional task-specific human annotations or manually labeled process steps.

Abstract: Self-evolving search agents reduce reliance on human-written training questions by generating and solving their own search tasks. We build on Search Self-Play (SSP), a representative Proposer and Solver framework in which questions are generated and answered via multi-step search and reasoning. In practice, however, SSP faces two bottlenecks: the Proposer constructs questions from isolated answer entities without relational context, yielding many invalid or unverifiable questions in early self-play training, while the Solver receives only a binary outcome reward that discards useful signal from partially on-track search trajectories. We address both bottlenecks by reusing knowledge-graph paths as construction-derived intermediate supervision for both question construction and reward shaping. First, we ground question construction in LLM-guided knowledge-graph subgraphs, providing relational context for the Proposer. Second, we observe that constructing and solving a multi-hop question can involve overlapping intermediate entities: the factual bridges used to formulate the question may provide approximate waypoints for answering it. Exploiting this overlap, we introduce Waypoint Coverage Reward (WCR), which grants graded partial credit to incorrect Solver trajectories according to their coverage of entities on the construction path, while preserving full reward for correct answers. Across seven QA benchmarks and nine model configurations, our approach improves the average score over standard SSP in all configurations, including notable gains on multi-hop QA tasks. These results suggest that knowledge-graph paths can be reused as lightweight intermediate supervision, providing both relational guidance and process feedback without additional task-specific human annotations or manually labeled process steps.

</details>


### [41] [Resolving the bias-precision paradox with stochastic causal representation learning for personalized medicine](https://arxiv.org/abs/2605.05706)
*Peisong Zhang,Manqiang Peng,Yuxuan Wu,Pawit Phadungsaksawasdi,Wesley Yeung,Ye Zhang,Trang Nguyen,Qiang Zhang,Nan Liu,Meng Wang,Kee Yuan Ngiam,Yih-Chung Tham,Ching-Yu Cheng,Tianfan Fu,Qingyu Chen,Rosemary Ke,Chang Li,Wenzhuo Yang,Zhenghao Lu,Chunyou Lai,Yu Zhang,Sheng Zhong,Hao Deng,Dianbo Liu*

Main category: cs.AI

TL;DR: A new method for estimating individualized treatment effects that addresses the bias-precision paradox using stochastic alignment and subset-level matching, improving clinical predictions and decision support.


<details>
  <summary>Details</summary>
Motivation: Existing methods for estimating individualized treatment effects from longitudinal observational data face a fundamental limitation: reducing confounding bias often suppresses clinically informative heterogeneity, degrading patient-specific predictions. This creates a bias-precision paradox in causal representation learning.

Method: Introduces sampling-based maximum mean discrepancy (sMMD), a stochastic alignment strategy that replaces global adversarial balancing with subset-level matching. This is instantiated in a framework for counterfactual outcome prediction with attribution-grounded interpretability.

Result: Across two large-scale ICU cohorts (n = 27,783), the framework improves accuracy under distribution shift, reducing error by up to 11.5% and substantially increasing recall in high-risk tasks. Mechanistic analyses show sMMD selectively preserves clinically decisive variables. In human-AI evaluation, the method outperforms clinicians-in-training and large language models, improving clinician accuracy by 14.7% while reducing decision time.

Conclusion: The proposed sMMD-based framework successfully addresses the bias-precision paradox in causal representation learning, enabling interpretable, real-time clinical decision support with improved accuracy and preservation of clinically relevant heterogeneity.

Abstract: Estimating individualized treatment effects from longitudinal observational data is central to data-driven medicine, yet existing methods face a fundamental limitation: reducing confounding bias often suppresses clinically informative heterogeneity, degrading patient-specific predictions. Here, we identify this tension as a bias-precision paradox in causal representation learning and introduce sampling-based maximum mean discrepancy (sMMD), a stochastic alignment strategy that replaces global adversarial balancing with subset-level matching. We instantiate this approach in a framework for counterfactual outcome prediction with attribution-grounded interpretability. Across two large-scale ICU cohorts (n = 27,783), our framework improves accuracy under distribution shift, reducing error by up to 11.5% and substantially increasing recall in high-risk tasks. Mechanistic analyses show that sMMD selectively preserves clinically decisive variables. In human-AI evaluation, our method outperforms clinicians-in-training and large language models, and improves clinician accuracy by 14.7% while reducing decision time, enabling interpretable, real-time clinical decision support.

</details>


### [42] [Conceal, Reconstruct, Jailbreak: Exploiting the Reconstruction-Concealment Tradeoff in MLLMs](https://arxiv.org/abs/2605.05709)
*Md Farhamdur Reza,Richeng Jin,Tianfu Wu,Huaiyu Dai*

Main category: cs.AI

TL;DR: Intent-obfuscation jailbreak attacks hide harmful queries in multimodal inputs to bypass MLLM safety filters, but face a reconstruction-concealment tradeoff. The authors propose concealment-aware variant construction with character removal and keyword-related distractor images, showing these exploit MLLMs' own reconstruction abilities to produce unsafe responses.


<details>
  <summary>Details</summary>
Motivation: Intent-obfuscation jailbreak attacks on multimodal large language models (MLLMs) transform harmful queries into concealed multimodal inputs to bypass safety mechanisms. However, existing methods struggle to balance the reconstruction-concealment tradeoff - the transformed input must hide harmful intent from safety filters while remaining recoverable enough for the victim model to reconstruct the original request.

Method: 1) Analysis shows character-removed variants achieve better reconstruction-concealment balance than existing methods. 2) Propose concealment-aware variant construction that greedily selects character-removed variants low in harmful-keyword alignment and mutually diverse. 3) Instantiate through five modality-aware prompting strategies. 4) Introduce keyword-related distractor images that depict harmful keywords in diverse contexts as more effective auxiliary visual context than generic distractors.

Result: Experiments across closed-source and open-source MLLMs show the proposed strategies outperform strong baselines. The methods reveal an underexplored vulnerability: a model's own reconstruction ability can be exploited to recover hidden harmful intent and produce unsafe responses.

Conclusion: Intent-obfuscation jailbreak attacks exploit a fundamental reconstruction-concealment tradeoff in MLLM safety. Character removal with concealment-aware variant construction and keyword-related distractor images provides more effective attacks than existing methods, demonstrating that MLLMs' reconstruction capabilities can be weaponized against their own safety mechanisms.

Abstract: Intent-obfuscation-based jailbreak attacks on multimodal large language models (MLLMs) transform a harmful query into a concealed multimodal input to bypass safety mechanisms. We show that such attacks are governed by a \emph{reconstruction--concealment tradeoff}: the transformed input must hide harmful intent from safety filters while remaining recoverable enough for the victim model to reconstruct the original request. Through a reconstruction analysis of three representative black-box methods, we find that existing transformations struggle to balance this tradeoff, limiting their effectiveness. In contrast, we show that character-removed variants achieve a better balance. Building on this, we propose \emph{concealment-aware variant construction}, which greedily selects character-removed variants that are low in harmful-keyword alignment and mutually diverse, and instantiates them through five modality-aware prompting strategies. We further introduce \emph{keyword-related distractor images} that depict the harmful keyword in diverse contexts, providing more effective auxiliary visual context than generic distractor images. Experiments across closed-source and open-source MLLMs show the proposed strategies outperform strong baselines, revealing an underexplored vulnerability: a model's own reconstruction ability can be exploited to recover hidden harmful intent and produce unsafe responses.

</details>


### [43] [Decodable but Not Corrected by Fixed Residual-Stream Linear Steering: Evidence from Medical LLM Failure Regimes](https://arxiv.org/abs/2605.05715)
*Ming Liu*

Main category: cs.AI

TL;DR: The paper reveals that while failure signals in LLMs can be linearly decoded for detection, they cannot be used for direct correction due to representational entanglement, though they enable reliable selective abstention.


<details>
  <summary>Details</summary>
Motivation: To investigate whether linearly decodable failure signals in LLM hidden states can be leveraged to correct those failures, examining the gap between classification (detecting failures) and correction (fixing them).

Method: Studied Overthinking (OT) behavioral regime in medical QA where models answer correctly under resampling but fail in extended chain-of-thought. Tested five families of fixed linear steering (29 configurations) on 1,273 instances, cross-architecture validation (Qwen2.5-7B), and cross-domain testing (MMLU-STEM).

Result: OT is linearly decodable at 71.6% balanced accuracy (p<10^{-16}), but all linear steering attempts yielded near-zero correction (Delta ~=0). Evidence shows representational entanglement: OT direction has 85-88% overlap with task-critical computation, non-targeted steering damages accuracy (-12.1pp), LEACE erasure damages accuracy (-3.6pp), and probe-steering correlation is negligible (r=-0.002).

Conclusion: Linearly decodable failure structure enables reliable selective abstention (AUROC=0.610 exceeding baselines) but cannot be exploited for direct correction due to representational entanglement, highlighting a fundamental classification-correction gap in LLMs.

Abstract: Can linearly decodable failure signals in LLM hidden states be leveraged to correct those failures? We investigate this classification-correction gap via Overthinking (OT)--a stable behavioral regime (Jaccard >= 0.81, 94% inter-annotator agreement) in medical QA where models answer correctly under resampling yet fail in extended chain-of-thought. OT is linearly decodable at 71.6% balanced accuracy (p < 10^{-16}). Yet five families of fixed linear steering (29 configurations, n=1,273) all yield Delta ~= 0, with identical null results cross-architecture (Qwen2.5-7B) and cross-domain (MMLU-STEM). Three convergent lines of evidence suggest representational entanglement: the OT direction has 85-88% overlap with task-critical computation (specificity ratio <= 0.152); non-targeted shared-direction steering damages accuracy (-12.1pp); and LEACE concept erasure damages accuracy (-3.6pp, p=0.01), while 10 random erasures produce Delta=+0.3pp. The per-instance probe-steering correlation is r=-0.002 (p=0.97). Positively, the same probe enables selective abstention (held-out AUROC=0.610, exceeding all five uncertainty baselines, p=0.009): decodable failure structure supports post-generation reliability estimation even when the fixed linear steering family cannot exploit it for correction.

</details>


### [44] [More Is Not Always Better: Cross-Component Interference in LLM Agent Scaffolding](https://arxiv.org/abs/2605.05716)
*Ming Liu*

Main category: cs.AI

TL;DR: Stacking all agent components (planning, tools, memory, self-reflection, retrieval) often hurts performance due to cross-component interference - optimal subsets outperform full systems by 32-79%.


<details>
  <summary>Details</summary>
Motivation: Current LLM agent systems assume more components are better, but the authors hypothesize that component interactions can cause destructive interference, questioning the "all-in" design philosophy.

Method: Full factorial experiment over 32 subsets of five agent components on HotpotQA and GSM8K with Llama-3.1-8B/70B models (96 conditions, up to 10 seeds), fitting main-effects regression, computing Shapley values, and analyzing submodularity violations.

Result: All-In systems consistently suboptimal: single-tool agent beats All-In by 32% on HotpotQA, 3-component subset beats All-In by 79% on GSM8K. Optimal component count is task-dependent (k*=1-4) and model-scale sensitive. Found 56.3% submodularity violations, making greedy selection unreliable. Identified three-body synergy among Tool Use, Self-Reflection, and Retrieval.

Conclusion: Maximally-equipped agent defaults should be replaced by task-specific subset selection through interaction-aware analysis, as component interactions can be destructive rather than additive.

Abstract: LLM agent systems are built by stacking scaffolding components (planning, tools, memory, self-reflection, retrieval) assuming more is better. We study cross-component interference (CCI): degradation when components interact destructively. We run a full factorial experiment over all 2^5=32 subsets of five components on HotpotQA and GSM8K with Llama-3.1-8B/70B (96 conditions, up to 10 seeds). The All-In system is consistently suboptimal: on HotpotQA, a single-tool agent surpasses All-In by 32% (F1 0.233 vs 0.177, p=0.023); on GSM8K, a 3-component subset beats All-In by 79% (0.43 vs 0.24, p=0.010). Optimal component count is task-dependent (k*=1-4) and scale-sensitive: at 70B, combinations that hurt at 8B provide gains, though All-In still trails the best subset. We fit a main-effects regression (R^2=0.916, adj-R^2=0.899, LOOCV=0.872), compute exact Shapley values, and find 183/325 submodularity violations (56.3%), showing greedy selection is unreliable. A three-body synergy among Tool Use, Self-Reflection, and Retrieval (INT_3=+0.175, 95% CI [+0.003,+0.351]) is reported as exploratory. CCI replicates across model families (Qwen2.5) and is robust to prompt paraphrasing. Our findings suggest maximally-equipped agent defaults should be replaced by task-specific subset selection via interaction-aware analysis.

</details>


### [45] [Detecting Time Series Anomalies Like an Expert: A Multi-Agent LLM Framework with Specialized Analyzers](https://arxiv.org/abs/2605.05725)
*Hyeongwon Kang,Jeongseob Kim,Jinwoo Park,Pilsung Kang*

Main category: cs.AI

TL;DR: SAGE: A multi-agent framework for time-series anomaly detection using specialized analyzers for different anomaly types, evidence consolidation, and synthetic in-context learning without real anomaly examples.


<details>
  <summary>Details</summary>
Motivation: Existing approaches use single general-purpose models for time-series anomaly detection, which limits controllability, interpretability, and reliability for complex anomaly patterns. There's a need for more structured and reliable diagnostic approaches.

Method: Proposes SAGE (Specialized Analyzer Group for Expert-like Detection), a multi-agent framework that decomposes anomaly analysis into four specialized Analyzers (point, structural, seasonal, and pattern anomalies). Each Analyzer uses family-specific numerical tools and diagnostic visualizations to generate evidence. An evidence-grounded Detector consolidates evidence into confidence-scored anomaly records. A Supervisor converts records into diagnostic reports. Uses synthetic in-context examples from normal-reference training segments without real anomaly examples.

Result: Achieves best average performance across three benchmarks compared to strong ML/DL and language-model-based baselines. Ablation studies and human evaluation show improved detection reliability and practical usefulness of diagnostic outputs.

Conclusion: SAGE provides a structured, interpretable framework for time-series anomaly detection that outperforms existing methods and improves reliability through specialized analyzers and evidence-based diagnosis.

Abstract: Recent studies have explored large language models for time-series anomaly detection, yet existing approaches often rely on a single general-purpose model to directly infer anomaly indices or intervals, limiting controllability, interpretability, and reliability for complex anomaly patterns. We propose SAGE (Specialized Analyzer Group for Expert-like Detection), a multi-agent framework for structured anomaly diagnosis in univariate time series. It decomposes anomaly analysis into four specialized Analyzers for point, structural, seasonal, and pattern anomalies. Each Analyzer applies family-specific numerical tools and diagnostic visualizations to generate evidence, while an evidence-grounded Detector consolidates the evidence into confidence-scored anomaly records with intervals and candidate types. A Supervisor then converts these structured records into analyst-facing diagnostic reports. SAGE further constructs synthetic in-context examples from normal-reference training segments, without using real anomalous segments or anomaly-type labels as in-context examples. Across three benchmarks, SAGE achieves the best average performance among strong ML/DL and language-model-based baselines. Ablation studies and human evaluation further show that the proposed framework improves detection reliability and the practical usefulness of diagnostic outputs.

</details>


### [46] [SkillRet: A Large-Scale Benchmark for Skill Retrieval in LLM Agents](https://arxiv.org/abs/2605.05726)
*Hongcheol Cho,Ryangkyung Kang,Youngeun Kim*

Main category: cs.AI

TL;DR: SkillRet is a new large-scale benchmark for skill retrieval in LLM agents, addressing the challenge of selecting appropriate skills from large libraries, with significant performance improvements through fine-tuning.


<details>
  <summary>Details</summary>
Motivation: As LLM agents deploy with growing libraries of reusable skills, skill selection becomes critical. Current approaches fail as skill ecosystems expand under context/latency constraints, and skill retrieval lacks proper benchmarks and understanding of real-world behavior.

Method: Created SkillRet benchmark with 17,810 public agent skills organized with semantic tags and a two-level taxonomy. Provides 63,259 training samples and 4,997 evaluation queries with disjoint skill pools. Evaluates diverse retrievers including off-the-shelf models and fine-tuned versions.

Result: Skill retrieval remains unsolved: off-the-shelf models struggle on large-scale libraries, and prior models leave substantial room for improvement. Fine-tuning on SkillRet improves NDCG@10 by +13.1 points over best prior retriever and +16.9 points over best off-the-shelf retriever.

Conclusion: SkillRet establishes a strong benchmark for future research on retrieval in large-scale agent systems, showing that fine-tuned models better focus on skill-relevant signals in noisy queries and enabling both benchmarking and retrieval-oriented training.

Abstract: As LLM agents are increasingly deployed with large libraries of reusable skills, selecting the right skill for a user request has become a critical systems challenge. In small libraries, users may invoke skills explicitly by name, but this assumption breaks down as skill ecosystems grow under tight context and latency budgets. Despite its practical importance, skill retrieval remains underexplored, with limited benchmarks and little understanding of retrieval behavior on realistic skill libraries. To address this gap, we introduce SkillRet, a large-scale benchmark for skill retrieval in LLM agents. SkillRet contains 17,810 public agent skills, organized with structured semantic tags and a two-level taxonomy spanning 6 major categories and 18 sub-categories. It provides 63,259 training samples and 4,997 evaluation queries with disjoint skill pools, enabling both benchmarking and retrieval-oriented training. Across a diverse set of retrievers, we find that skill retrieval remains far from solved: off-the-shelf models struggle on realistic large-scale skill libraries, and prior skill-retrieval models still leave substantial headroom. Task-specific fine-tuning on SkillRet substantially improves performance, improving NDCG@10 by +13.1 points over the strongest prior retriever and by +16.9 points over the strongest off-the-shelf retriever. Our analysis further suggests that these gains arise because fine-tuned models better focus on the small skill-relevant signals within long and noisy queries. These results establish SkillRet as a strong benchmark and foundation for future research on retrieval in large-scale agent systems.

</details>


### [47] [Knee Osteoarthritis Severity Grading Using Optimized Deep Learning and LLM-Driven Intelligent AI on Computationally Limited Systems](https://arxiv.org/abs/2605.05731)
*Dayam Nadeem,Neha,Safdar Mustafa,Adnan Alvi,Mohd Hussain*

Main category: cs.AI

TL;DR: Automated KOA severity grading system using ResNet-18 CNN optimized for on-device deployment with TensorFlow Lite, achieving 94.48% accuracy in classifying knee images into KL grades, plus LLM-generated interpretive findings.


<details>
  <summary>Details</summary>
Motivation: Knee osteoarthritis (KOA) significantly restricts mobility and causes chronic pain, impacting quality of life. Conventional diagnostic methods suffer from subjectivity and inter-observer variability, necessitating precise and timely diagnosis for effective severity assessment.

Method: Proposes a deep learning approach using ResNet-18 CNN trained on publicly available knee image databases. Uses transfer learning to classify images into five Kellgren-Lawrence grades, then optimizes the model for deployment on resource-constrained devices using TensorFlow Lite format. Also integrates an auxiliary LLM (Gemini-2.0-flash) to generate structured interpretive findings without affecting classification.

Result: Achieved 94.48% test accuracy with stable convergence during model training. Successfully transformed the optimized model into lightweight TensorFlow Lite format for deployment on devices with limited internet connectivity. The system operates effectively in offline environments.

Conclusion: Demonstrates feasibility of on-device, interpretable decision-support tools for early KOA diagnosis, improving accessibility to AI-assisted knee screening. The approach enables precise severity grading without continuous internet connectivity while providing additional clinical insights through LLM-generated interpretations.

Abstract: Knee osteoarthritis (KOA) is among the musculoskeletal disorders that considerably restrict joint mobility, cause severe chronic pain and impact negatively on quality life. It is one of the persistent health issues worldwide. Generally, subjectivity and inter-observer variability undermine conventional practices and evaluation process that are adopted to address such health issues. Hence precise and timely diagnosis would be one of the effective ways for the assessment of its severity. This paper proposes an automated diagnostic approach for severity grading of KOA by blending a deep learning convolutional neural network (CNN) with a device-based inference platform powered by TensorFlow Lite. It proposes a model based on the ResNet-18 convolutional neural network. The designed model is trained on publicly available database. Through a transfer learning approach obtained knee images are first classified into five Kellgren-Lawrence (KL) grades. Further the developed model is optimised. During the training of the model test accuracy of 94.48% with stable convergence has been achieved. Subsequently the optimised model transformed into a lightweight TensorFlow Lite format, facilitating seamless deployment on resource-constrained devices. The designed model is capable enough to operate in the environment having no continuous internet connectivity. Also, an auxiliary Large Language Model (Gemini-2.0-flash) is applied to generate structured interpretive findings like potential symptoms, risk factors, and preventive majors etc. The LLM component functions as interface without influencing the classification process. The proposed model articulates the feasibility of an on-device, interpretable decision-support tools for early diagnosis and improve accessibility to Artificial Intelligence (AI)-assisted knee screening tool.

</details>


### [48] [SDFlow: Similarity-Driven Flow Matching for Time Series Generation](https://arxiv.org/abs/2605.05736)
*Wei Li,Shibo Feng,Pengcheng Wu,Min Wu,Peilin Zhao*

Main category: cs.AI

TL;DR: SDFlow is a non-autoregressive framework that uses flow matching in frozen VQ latent spaces to eliminate exposure bias and enable parallel generation for time-series, achieving SOTA with faster inference.


<details>
  <summary>Details</summary>
Motivation: Current VQ+AR approaches for time-series generation suffer from exposure bias where errors accumulate during sequential predictions, leading to quality degradation in long-horizon generation.

Method: SDFlow replaces autoregressive token prediction with flow matching in frozen VQ latent spaces. It uses: 1) global transport maps instead of step-wise predictions, 2) low-rank manifold decomposition with learned anchor prior to handle high-dimensional VQ spaces, and 3) variational flow-matching with categorical posterior over codebook indices to incorporate discrete supervision.

Result: SDFlow achieves state-of-the-art performance with improved Discriminative Score and substantially reduced Context-FID, especially for challenging long-sequence generation. It also provides significant inference speedups over autoregressive baselines.

Conclusion: SDFlow successfully addresses exposure bias in VQ-based time-series generation through non-autoregressive flow matching, offering both high fidelity and computational efficiency for long-horizon sequence generation.

Abstract: Vector quantization (VQ) with autoregressive (AR) token modeling is a widely adopted and highly competitive paradigm for time-series generation. However, such models are fundamentally limited by exposure bias: during inference, errors can accumulate across sequential predictions, leading to pronounced quality degradation in long-horizon generation. To address this, we propose SDFlow ($\textbf{S}$imilarity-$\textbf{D}$riven $\textbf{Flow}$ Matching), a non-autoregressive framework that operates entirely in the frozen VQ latent space and enables parallel sequence generation via flow matching. We tackle three key challenges in making this transition: (1) eliminating exposure bias by replacing step-wise token prediction with a global transport map; (2) mitigating the high-dimensionality of VQ token spaces via a low-rank manifold decomposition with a learned anchor prior over the latent manifold; and (3) incorporating discrete supervision into continuous transport dynamics by introducing a categorical posterior over codebook indices within a variational flow-matching formulation. Extensive experiments show that SDFlow achieves state-of-the-art performance, improving Discriminative Score and substantially reducing Context-FID, particularly for challenging long-sequence generation. Moreover, SDFlow provides significant inference speedups over autoregressive baselines, offering both high fidelity and computational efficiency. Code is available at https://anonymous.4open.science/r/SDFlow-D6F3/

</details>


### [49] [ReFlect: An Effective Harness System for Complex Long-Horizon LLM Reasoning](https://arxiv.org/abs/2605.05737)
*Fan Huang*

Main category: cs.AI

TL;DR: ReFlect is a training-free, model-agnostic reasoning harness that creates deterministic error detection and recovery logic to address accumulated errors in long-horizon reasoning tasks, significantly improving task success rates across multiple LLMs.


<details>
  <summary>Details</summary>
Motivation: Current LLM reasoning paradigms (chain-of-thought, ReAct, post-hoc self-critique) fail on long-horizon, multi-stage tasks due to silent error accumulation across reasoning steps. There's an open question about whether reasoning systems can effectively detect and recover from their own failures.

Method: ReFlect creates standalone error detection and recovery logic as a deterministic wrapper around LLMs. It's model-agnostic, training-free, and operates entirely at inference time, addressing limitations of prompt-level self-critique which produces formulaic templates that fail to detect issues.

Result: ReFlect achieves task success rates from 41% (gpt-4o-mini) to 56% (Claude Sonnet 4.5) across six models, with gains over Direct CoT ranging from +7pp to +29pp. It raises SWE-bench patch-structural quality from 0% to 82-87%. Harness gain is inversely proportional to baseline success rate (-1.69 slope, r=-0.76), meaning each pp lost in baseline is recovered by 1.69pp of harness gain.

Conclusion: ReFlect provides an effective solution for error detection and recovery in long-horizon reasoning tasks, showing that structured reasoning state approaches are limited at current model scales, while deterministic harness systems offer significant improvements across various LLMs.

Abstract: Current reasoning paradigms for LLMs include chain-of-thought, ReAct, and post-hoc self-critique. These paradigms rely on two assumptions that fail on long-horizon, multi-stage tasks. As a result, errors accumulate silently across reasoning steps, leaving an open question: can a reasoning system effectively detect and recover from its own failures? We present ReFlect, a \emph{harness} system for LLM reasoning that creates standalone error detection and recovery logic as a deterministic wrapper around the model. Controlled experiments across 6 reasoning domains show that prompt-level self-critique produces formulaic templates that flag no issues in 90 of 100 audited reflection blocks, and the investigated LLMs wrongly accept a wrong answer in at least 76\% of cases. Our ReFlect harness achieves task success rates ranging from 41\% on gpt-4o-mini to 56\% on Claude Sonnet 4.5 across six models spanning small and frontier scale, with per-model gains over Direct CoT ranging from +7 pp on Qwen2.5-72B to +29 pp on Claude Sonnet 4.5, and additionally raises SWE-bench patch-structural quality from 0\% (Direct CoT) to between 82\% (Qwen2.5-72B) and 87\% (GPT-4o). Notably, the harness gain is inversely proportional to the model's Direct CoT task success rate (the fitted slope is -1.69 with r=-0.76): each pp lost in baseline success rate is mechanically recovered by 1.69 pp of harness gain. We spot that adding structured reasoning state and operators yields only 15.0--18.7\% pair-mean on Llama-3.3-70B and Qwen2.5-72B because models at this scale cannot reliably populate the state its operators require. ReFlect is model-agnostic, training-free, and operates entirely at inference time.

</details>


### [50] [HyperLens: Quantifying Cognitive Effort in LLMs with Fine-grained Confidence Trajectory](https://arxiv.org/abs/2605.05741)
*Chengda Lu,Xiaoyu Fan,Wei Xu*

Main category: cs.AI

TL;DR: HyperLens is a high-resolution probe that traces confidence trajectories in LLMs, revealing that complex tasks require higher cognitive effort and identifying a side effect of SFT that reduces cognitive effort and degrades performance.


<details>
  <summary>Details</summary>
Motivation: LLM inference dynamics remain poorly understood due to limited resolution of existing analysis tools. There's a need for finer-grained analysis to understand how LLMs process different types of tasks and measure cognitive effort during inference.

Method: Identified intrinsic magnification mechanism in transformers where deeper layers amplify small confidence changes. Built HyperLens probe to trace confidence trajectories and quantify cognitive effort. Applied across various LLMs and datasets to analyze patterns.

Result: HyperLens reveals consistent divergence in confidence trajectories separating complex from simple tasks. This pattern is abstracted into a quantitative cognitive effort metric showing complex tasks consistently require higher cognitive effort. Also identified that standard SFT can reduce cognitive effort and degrade in-domain task performance.

Conclusion: The paper provides a fundamental principle: complex tasks require higher cognitive effort, and introduces a diagnostic tool that reveals a problematic side effect of standard SFT - it reduces cognitive effort and harms performance. This offers new insights into LLM inference dynamics.

Abstract: While Large Language Models (LLMs) achieve strong performance across diverse tasks, their inference dynamics remain poorly understood because of the limited resolution of existing analysis tools. In this work, we identify an intrinsic magnification mechanism in transformer architectures: deeper layers inherently magnify the small changes of layer-wise confidence, providing a fine-grained confidence trajectory. Building on this insight, we introduce HyperLens, a high-resolution probe designed to trace confidence trajectories and quantify the cognitive effort during inference. Across LLMs and datasets, HyperLens reveals a consistent divergence in confidence trajectories that separates complex from simple tasks. We abstract this pattern into a quantitative cognitive effort metric. Our analysis reveals a fundamental principle: complex tasks consistently require higher cognitive effort. Finally, we provide a mechanistic diagnosis of a common side effect of standard Supervised Fine-Tuning (SFT): it can reduce cognitive effort and consequently degrade performance on in-domain tasks.

</details>


### [51] [Best Arm Identification in Generalized Linear Bandits via Hybrid Feedback](https://arxiv.org/abs/2605.05745)
*Qirun Zeng,Xuchuang Wang,Jiayi Shen,Xutong Liu,Fang Kong,Jinhang Zuo*

Main category: cs.AI

TL;DR: A hybrid bandit algorithm combining absolute and relative feedback for generalized linear models with improved sample efficiency and cost-awareness.


<details>
  <summary>Details</summary>
Motivation: To address the practical scenario where learning agents can choose between different feedback modalities (absolute rewards from single arms or relative comparisons between arm pairs) in generalized linear bandits, which has not been well-studied in fixed-confidence best arm identification settings.

Method: Develop a unified likelihood-ratio-based confidence sequence for heterogeneous generalized linear observations, create an explicit ellipsoidal confidence set using self-concordance assumptions, and design a hybrid Track-and-Stop algorithm that adaptively allocates queries by tracking a minimax-optimal design over the joint action space.

Result: Proved δ-correctness of the algorithm and provided high-probability upper bounds on the stopping time. Extended the framework to handle heterogeneous acquisition costs across feedback modalities, and demonstrated through empirical experiments that the proposed algorithms significantly improve sample efficiency over baseline methods.

Conclusion: The proposed hybrid feedback framework for generalized linear bandits effectively combines different feedback modalities with theoretical guarantees and practical performance improvements, offering a flexible approach that can adapt to different cost structures while maintaining statistical efficiency.

Abstract: We study fixed-confidence best arm identification in generalized linear bandits under a hybrid feedback model: at each round, the learner may query either (i) absolute reward feedback from a single arm or (ii) relative (dueling) feedback from an arm pair, both governed by generalized linear models. We introduce a likelihood-ratio--based confidence sequence that unifies heterogeneous generalized linear observations and yields an explicit ellipsoidal confidence set under a self-concordance assumption. Building on this confidence set, we propose a hybrid Track-and-Stop algorithm that adaptively allocates queries by tracking a minimax-optimal design over a joint action space of arms and pairs. We establish $δ$-correctness and provide high-probability upper bounds on the stopping time. We further extend the framework to a cost-aware setting that accounts for heterogeneous acquisition costs across feedback modalities. Empirical experiments demonstrate that the proposed algorithms significantly improve sample efficiency over baseline methods.

</details>


### [52] [Evaluating Explainability in Safety-Critical ATR Systems: Limitations of Post-Hoc Methods and Paths Toward Robust XAI](https://arxiv.org/abs/2605.05748)
*Vanessa Buhrmester,David Muench,Dimitri Bulatov,Michael Arens*

Main category: cs.AI

TL;DR: Current XAI methods have critical limitations for safety-critical ATR systems, often providing visually convincing but unreliable explanations that can lead to overtrust and insufficient validation.


<details>
  <summary>Details</summary>
Motivation: XAI is essential for deploying machine learning in safety-critical environments like Automatic Target Recognition (ATR), where high predictive performance alone is insufficient. Models must be interpretable, reliable, and suitable for validation in these critical applications.

Method: The paper presents a structured evaluation of explainability methods for safety-critical ATR systems. It identifies major XAI paradigms (saliency-based, attention-based, surrogate approaches), formalizes explainability as an assurance-oriented assessment problem, introduces a taxonomy, and assesses methods across four key dimensions: interpretability, robustness, vulnerability to manipulation, and suitability for validation/verification.

Result: The analysis reveals systematic limitations of current post-hoc explanation methods, identifying critical failure modes including spurious explanations, instability under perturbations, and overtrust induced by visually convincing outputs. Widely used XAI techniques may be insufficient for safety-critical deployment.

Conclusion: There's a need to move beyond visually plausible explanations toward more robust, causally grounded, and physically informed explainability methods that support reliable decision making and system-level assurance in safety-critical ATR systems.

Abstract: Explainable Artificial Intelligence (XAI) is increasingly rec ognized as essential for deploying machine learning systems in safety critical environments. In Automatic Target Recognition (ATR), where models operate on image, video, radar, and multisensor data, high pre dictive performance alone is insufficient. Model decisions must also be interpretable, reliable, and suitable for validation. This paper presents a structured evaluation of explainability methods in the context of safety-critical ATR systems: We identify major XAI paradigms, including saliency-based, attention-based, and surrogate ap proaches, as well as recent detection-aware extensions. Based on this, we formalize explainability as an assurance-oriented assessment problem, introduce a taxonomy, and assess these methods with respect to four key dimensions: interpretability, robustness, vulnerability to manipula tion, and suitability for validation and verification. The analysis identifies systematic limitations of current post-hoc explanation methods. In par ticular, we derive critical failure modes such as spurious explanations, instability under perturbations, and overtrust induced by visually con vincing outputs. These findings indicate that widely used XAI techniques may be insufficient for safety-critical deployment. Finally, we discuss implications for ATR systems and outline directions toward more robust, causally grounded, and physically informed explain ability methods. Our results emphasize the need to move beyond visually plausible explanations toward approaches that support reliable decision making and system-level assurance.

</details>


### [53] [Confidence is the key: how conformal prediction enhances the generative design of permeable peptides](https://arxiv.org/abs/2605.05770)
*Laura van Weesep,Sunay Chankeshwara,Leonardo De Maria,Florian David,Ola Engkvist,Gökçe Geylan*

Main category: cs.AI

TL;DR: RL-guided generative framework combines conformal prediction uncertainty quantification to design permeable cyclic peptides while avoiding unreliable predictions outside the model's domain.


<details>
  <summary>Details</summary>
Motivation: Generative models with RL can suggest molecules outside predictor's applicability domain, leading to unreliable predictions. This is particularly problematic for cyclic peptides which have therapeutic promise but are understudied compared to small molecules, making optimal permeable designs challenging to identify for intracellular targeting.

Method: RL-guided generative framework using uncertainty-aware permeability predictor with conformal prediction (CP) as uncertainty quantification method. CP assesses designs based on calibrated model under user-defined confidence level, allowing for CP-informed predictions to guide optimization.

Result: Rewarding generated peptides with CP-informed predictions improves both reliability and efficiency of peptide optimization process and discourages exploration outside predictor's applicability domain.

Conclusion: This approach successfully bridges the gap between predictive uncertainty and RL-guided exploration, demonstrating for the first time how generative modeling and conformal prediction can be effectively combined for more reliable molecular design.

Abstract: Generative models coupled with reinforcement learning (RL), such as REINVENT and PepINVENT, have emerged as a powerful framework for de novo molecular design. During the ideation process these generative frameworks utilize various predictive models as part of the optimization objectives. However, the utility of the predictive models can be limited by their domain of applicability. When RL is used to explore the chemical space with predictive models, it can suggest molecules that lie outside the predictor's domain of applicability. As a result, the predictions may become less reliable, potentially steering designs into high reward but also high uncertainty chemical spaces. This is particularly pronounced for cyclic peptides which show therapeutic promise due to their modifiability and large interaction surfaces but are understudied compared to small molecules. While passive membrane permeation in cyclic peptides has attracted interest, identifying optimal permeable designs remains challenging yet crucial for targeting intracellular sites. We present an RL-guided generative framework that designs permeable cyclic peptides using an uncertainty-aware permeability predictor as the scoring component. To address predictive uncertainty, especially impacted by novel chemistry, we integrate conformal prediction (CP) as our uncertainty quantification method. CP assesses designs based on the calibrated model under a user-defined confidence level. We demonstrate that rewarding generated peptides with CP-informed predictions improves both reliability and efficiency of peptide optimization process. This also discourages exploration outside the predictor's applicability domain. This approach bridges the gap between predictive uncertainty and RL-guided exploration, showing how generative modelling and conformal prediction can be combined for the first time.

</details>


### [54] [CircuitFormer: A Circuit Language Model for Analog Topology Design from Natural Language Prompt](https://arxiv.org/abs/2605.05773)
*Md Touhidul Islam,Sujan Kumar Saha,Farimah Farahmandi,Mark Tehranipoor*

Main category: cs.AI

TL;DR: A novel circuit-specific language model called CircuitFormer is developed for analog circuit design automation, featuring a specialized Circuit Tokenizer (CKT) that encodes netlist connectivity by mining frequent subcircuits, achieving superior performance over general-purpose LLMs with far fewer parameters.


<details>
  <summary>Details</summary>
Motivation: Transformers and LLMs have revolutionized software code generation but face two critical limitations when applied to analog hardware design: (1) scarcity of analog design datasets with natural language descriptions and corresponding netlists, and (2) inefficiency of general-purpose tokenizers like BPE in capturing the graph structure of circuits.

Method: Two main contributions: First, creation of the largest annotated dataset of analog circuit netlists (31,341 netlist-description pairs). Second, development of Circuit Tokenizer (CKT) that mines frequent subcircuits to encode netlist connectivity with constant complexity (O(1)), unlike previous methods with linear complexity (O(n_max)). The tokenizer achieves 57% reduction in sequence length and 2.3x better compression ratio using a fixed vocabulary of 512 tokens.

Result: CircuitFormer, a 511M parameter transformer trained on this optimized tokenization, achieves 100% syntactic correctness and 83% functional success rate across all major analog circuit categories. This outperforms state-of-the-art open-source LLMs by 10% and 14% respectively, while requiring 240x fewer parameters.

Conclusion: The proposed Circuit Tokenizer and CircuitFormer model successfully bridge the gap between LLMs and analog circuit design by addressing dataset scarcity and tokenization inefficiency. The approach demonstrates that circuit-specific tokenization and language modeling can significantly outperform general-purpose LLMs for analog hardware design automation while being much more parameter-efficient.

Abstract: Automating analog circuit design remains a longstanding challenge in Electronic Design Automation (EDA). While Transformer-based Large Language Models (LLMs) have revolutionized software code generation, their application to analog hardware design is hindered by two critical limitations: (i) the scarcity of analog design datasets containing natural language description of a design and its corresponding netlist, and (ii) the inefficiency of general-purpose tokenizers (e.g., Byte Pair Encoding (BPE)) in capturing the inherent graph structure of circuits. To bridge this gap, first, we curate the largest annotated dataset of analog circuit netlists to date, comprising 31,341 netlist-natural language description pairs across all major circuit classes. Furthermore, we propose Circuit Tokenizer (CKT), a novel circuit graph tokenizer designed to encode netlist connectivity by explicitly mining frequent subcircuits. In terms of scalability, CKT overcomes the bottleneck of prior circuit graph serialization methods where vocabulary size scales linearly with maximum number of components in the dataset, n_max, (O(n_max)); instead, CKT decouples vocabulary growth from circuit complexity, achieving a constant O(1) complexity. Empirically, CKT outperforms standard BPE on circuit topology representation, reducing sequence length by 57% and achieving a 2.3x superior compression ratio using a compact, fixed vocabulary of size 512. Leveraging this optimized tokenization, we train a circuit-specific language model, CircuitFormer, a 511M parameter encoder-decoder transformer. Our model achieves 100% syntactic correctness and an 83% functional success rate across all major analog circuit categories, outperforming state-of-the-art open-source LLMs by 10% and 14%, respectively, while requiring 240x fewer parameters. The dataset is publicly available at https://huggingface.co/datasets/touhid314/cktformer-dataset.

</details>


### [55] [HEDP: A Hybrid Energy-Distance Prompt-based Framework for Domain Incremental Learning](https://arxiv.org/abs/2605.05776)
*Yu Feng,Zhen Tian,Haoran Luo,Xie Yu,Diancheng Cheng,Haoyue Zheng,Shuai Lyu,Ping Zong,Lianyuan Li,Xin Ge,Yifan Zhu*

Main category: cs.AI

TL;DR: HEDP is a domain-incremental learning framework that uses hybrid energy-distance prompts with Helmholtz free energy inspiration to improve domain adaptation and mitigate forgetting.


<details>
  <summary>Details</summary>
Motivation: Domain incremental learning faces severe performance degradation due to domain shifts, requiring models to adapt to new domains without retraining while maintaining performance on previous domains.

Method: Proposes Hybrid Energy-Distance Prompt (HEDP) framework with energy regularization loss for better domain representation separability, and a hybrid energy-distance weighted mechanism that fuses energy-based and distance-based cues for improved domain selection and generalization.

Result: Achieves superior performance on multiple benchmarks including CORe50, with 2.57% accuracy gain on unseen domains, effectively mitigating catastrophic forgetting and enhancing open-world adaptability.

Conclusion: HEDP provides an effective solution for domain incremental learning by combining energy-based regularization with distance-based mechanisms, demonstrating strong performance in mitigating forgetting and improving adaptability to new domains.

Abstract: Domain Incremental Learning is a critical scenario that requires models to continuously adapt to new data domains without retraining. However, domain shifts often cause severe performance degradation. To address this, we propose Hybrid Energy-Distance Prompt, a domain-incremental framework inspired by Helmholtz free energy. HEDP introduces an energy regularization loss to enhance the separability of domain representations and a hybrid energy-distance weighted mechanism that fuses energy-based and distance-based cues to improve domain selection and generalization. Experiments on multiple benchmarks, including CORe50, show that HEDP achieves superior performance on unseen domains with a 2.57\% accuracy gain, effectively mitigating catastrophic forgetting and enhancing open-world adaptability. Our code is \href{https://github.com/dannis97500/HEDP/}{available here}.

</details>


### [56] [Von Neumann Networks](https://arxiv.org/abs/2605.05780)
*Shekhar S. Chandra*

Main category: cs.AI

TL;DR: A new neural network architecture called Von Neumann Networks (VNNs) based on John von Neumann's cellular computational model, featuring self-engineered architectures that outperform traditional deep learning models while being more parameter efficient.


<details>
  <summary>Details</summary>
Motivation: To develop a modern deep learning implementation of John von Neumann's computational cellular array concept, which was originally conceived as a simple model of the human brain based on diffusion processes, and explore its potential for creating self-engineered neural architectures.

Method: Created Von Neumann neurons with specialized learnable roles based on von Neumann's cellular array system, developed Von Neumann Networks (VNNs) as neural operators that learn Green's functions with convolutions on cellular topology having diffusion signature, and proved VNNs are part of computationally universal Cellular Machines.

Result: VNN-based multi-layered perceptrons outperform equivalent deep learning variants on basic tasks, demonstrate greater parameter efficiency, can learn new types of tasks including solving for and constructing extensions of Von Neumann hardware architecture to cells.

Conclusion: Von Neumann Networks represent a promising new direction in neural network design that combines von Neumann's computational cellular concepts with modern deep learning, offering self-engineered architectures with better performance and efficiency while opening new research opportunities.

Abstract: In the mid-twentieth century, mathematician and polymath John von Neumann created a computational system on an array of cells as a simple model of the human brain, where each cell had one of a finite set of roles or states that he predicted would be modelled by a diffusion process. In this work, we show that such a system, when developed in a modern deep learning setting, enables the construction of an artificial neuron having specialized roles that can be learnt. We refer to this neuron as the Von Neumann neuron, and the resulting neural network from such neurons result in a self-engineered design whose architecture is only dependent on the structure and locations of its inputs and outputs on this cellular array. The mathematical framework for these Von Neumann Networks (VNNs) is also constructed and shows that they are based on the extension of neural operators and the learning of Green's functions with convolutions on a cellular topology having a diffusion signature. We also prove that these VNNs are part of a more general computational system called Cellular Machines that are computationally universal. Initial experiments show that VNN based multi-layered perceptrons outperform their equivalent deep learning variant on basic tasks, while being more parameter efficient and are capable of learning new types of tasks. This includes the ability to solve for and construct an extension of the Von Neumann (hardware) architecture common to all modern computers to cells and suggests new opportunities that could be explored.

</details>


### [57] [Sheet as Token: A Graph-Enhanced Representation for Multi-Sheet Spreadsheet Understanding](https://arxiv.org/abs/2605.05811)
*Yiming Lei,Yiqi Wang,Yujia Zhang,Bo Guan,Depei Zhu,Chunhui Wang,Zhuonan Hao,Tianyu Shi*

Main category: cs.AI

TL;DR: Sheet as Token treats each worksheet as a unified semantic unit for multi-sheet spreadsheet retrieval, using graph-enhanced cross-sheet reasoning to improve retrieval performance.


<details>
  <summary>Details</summary>
Motivation: Workbook-scale spreadsheet understanding is challenging due to distributed information across multiple sheets with heterogeneous schemas, layouts, and implicit relationships. Existing chunk-centric approaches fragment worksheets and weaken global sheet-level semantics.

Method: Sheet as Token extracts schema-aware records from sheet names, column headers, representative values, and layout features, encoding each worksheet into a compact dense token. A Graph Retriever constructs query-specific candidate graphs over sheet tokens using multiple relation types (semantic, query-conditioned, schema-consistency, shape-compatibility) and composes them through a multi-stage graph transformer to retrieve supporting sheet sets.

Result: Experiments on a constructed multi-sheet spreadsheet corpus show that sheet-level tokenization learns stable representations, and graph-enhanced cross-sheet reasoning improves listwise retrieval over shallow graph baselines with limited additional computation.

Conclusion: Sheet-level tokenization is a promising abstraction for scalable multi-sheet spreadsheet understanding, providing a unified semantic representation that preserves worksheet-level semantics while enabling effective cross-sheet reasoning.

Abstract: Workbook-scale spreadsheet understanding is increasingly important for language-model-based data analysis agents, but remains challenging because relevant information is often distributed across multiple sheets with heterogeneous schemas, layouts, and implicit relationships. Existing retrieval-augmented approaches typically decompose spreadsheets into rows, columns, or blocks to improve scalability; however, such chunk-centric representations can fragment worksheets into isolated text spans and weaken global sheet-level semantics. We propose Sheet as Token, a graph-enhanced framework that treats each worksheet as a unified semantic unit for multi-sheet spreadsheet retrieval. Our method extracts schema-aware records from sheet names, column headers, representative values, and layout features, and encodes each worksheet into a compact dense token. Given a natural-language query, a Graph Retriever constructs a query-specific candidate graph over sheet tokens using semantic, query-conditioned, schema-consistency, and shape-compatibility relations, and composes these channels through a multi-stage graph transformer to retrieve supporting sheet sets. Experiments on a constructed multi-sheet spreadsheet corpus show that sheet-level tokenization learns stable representations, and that graph-enhanced cross-sheet reasoning improves listwise retrieval over a shallow graph baseline with limited additional graph-side computation. These results suggest that sheet-level tokenization is a promising abstraction for scalable multi-sheet spreadsheet understanding.

</details>


### [58] [Long-Horizon Q-Learning: Accurate Value Learning via n-Step Inequalities](https://arxiv.org/abs/2605.05812)
*Armaan A. Abraham,Lucy Xiaoyang Shi,Chelsea Finn*

Main category: cs.AI

TL;DR: Long-horizon Q-learning (LQL) stabilizes Q-learning against compounding error by using optimality tightening bounds with hinge loss penalties, improving performance over 1-step and n-step TD learning without additional computational cost.


<details>
  <summary>Details</summary>
Motivation: Off-policy, value-based RL methods like Q-learning are attractive for learning from arbitrary experience, but they suffer from brittleness in long-horizon learning due to compounding estimation errors that propagate backward through TD updates.

Method: LQL introduces optimality tightening bounds: any realized action sequence lower-bounds what the optimal policy can achieve in expectation. The method turns this inequality into a practical stabilization mechanism using a hinge loss to penalize violations of these bounds, requiring no auxiliary networks or additional forward passes beyond standard Q-learning.

Result: When combined with state-of-the-art methods on online and offline-to-online benchmarks, LQL consistently outperforms both 1-step TD and n-step TD learning at similar runtime.

Conclusion: LQL provides a principled, computationally efficient approach to stabilize Q-learning against compounding error in long-horizon problems, making value-based reinforcement learning more robust while maintaining the benefits of off-policy learning.

Abstract: Off-policy, value-based reinforcement learning methods such as Q-learning are appealing because they can learn from arbitrary experience, including data collected by older policies or other agents. In practice, however, bootstrapping makes long-horizon learning brittle: estimation errors at later states propagate backward through temporal-difference (TD) updates and can compound over time. We propose long-horizon Q-learning (LQL), which introduces a principled backstop against compounding error when learning the optimal action-value function. LQL builds on a prior optimality tightening observation: any realized action sequence lower-bounds what the optimal policy can achieve in expectation, so acting optimally earlier should not be worse than following the observed actions for several steps before switching to optimal behavior. Our contribution is to turn this inequality into a practical stabilization mechanism for Q-learning by using a hinge loss to penalize violations of these bounds. Importantly, LQL computes these penalties using network outputs already produced for the TD error, requiring no auxiliary networks and no additional forward passes relative to Q-learning. When combined with multiple state-of-the-art methods on a range of online and offline-to-online benchmarks, LQL consistently outperforms both 1-step TD and n-step TD learning at similar runtime.

</details>


### [59] [AGPO: Asymmetric Group Policy Optimization for Verifiable Reasoning and Search Ads Relevance at JD](https://arxiv.org/abs/2605.05826)
*Yang Xu,Kun Yao,Yiming Deng,Zheng Fang,Kai Ming Ting,Ming Pang*

Main category: cs.AI

TL;DR: AGPO is a new RLVR method that uses asymmetric negative-dominant reinforcement and group advantage scaling to prevent reasoning capability shrinkage while improving performance at scale.


<details>
  <summary>Details</summary>
Motivation: Current RLVR methods for LLMs improve sampling efficiency but narrow reasoning capability boundaries compared to base models, which achieve better coverage at large sample sizes. There's a need to counteract this boundary shrinkage while maintaining exploration capacity.

Method: AGPO uses asymmetric reinforcement: negative-dominant strategy to suppress incorrect reasoning paths while preserving base model exploration, and positive reinforcement with group advantage mechanism that scales updates based on intra-group variance to focus on rare correct paths while suppressing trivial ones.

Result: Experiments on five mathematical benchmarks show AGPO achieves state-of-the-art accuracy and consistently improves pass@k performance at scale. In industrial search ads relevance optimization, AGPO enhances data annotation quality, leading to substantial performance gains in downstream student models.

Conclusion: AGPO effectively addresses the boundary shrinkage problem in RLVR by maintaining exploration capacity while focusing reinforcement on meaningful reasoning patterns, demonstrating practical benefits in both academic benchmarks and real-world industrial applications.

Abstract: Reinforcement Learning with Verifiable Rewards (RLVR) has demonstrated notable success in enhancing the reasoning performance of large language models (LLMs). However, recent studies reveal that while current RLVR methods improve sampling efficiency towards correct paths, they do not elicit fundamentally new reasoning patterns. Instead, the reasoning capability boundary of trained models often narrows compared to their base models, with base models achieving higher coverage at large sample sizes. In this work, we propose Asymmetric Group Policy Optimization (AGPO) to counteract this boundary shrinkage. AGPO adopts a negative-dominant reinforcement strategy to suppress incorrect reasoning paths, maintaining the base model's exploration capacity. For positive reinforcement, AGPO adopts a group advantage mechanism, which scales positive updates based on intra-group variance, allowing the model to focus on rare correct paths while suppressing updates from trivial paths. Our experiments on five mathematical benchmarks demonstrate that AGPO achieves state-of-the-art accuracy while consistently improving pass@$k$ performance at scale. In a large-scale industrial application for search ads relevance optimization, AGPO effectively enhances the quality of the data annotation, leading to substantial performance gains in downstream student models.

</details>


### [60] [MolRecBench-Wild: A Real-World Benchmark for Optical Chemical Structure Recognition](https://arxiv.org/abs/2605.05832)
*Haote Yang,Hui Wang,Chen Zhu,Jingchao Wang,Linye Li,Hongbin Lai,Huijie Ao,Yongxuan Lyu,Jiang Wu,Jiaxing Sun,Lua Chen,Yuanyuan Cao,Ruijie Zhang,Shengxin Lu,Lijun Wu,Bin Wang,Conghui He*

Main category: cs.AI

TL;DR: MOSAIC introduces a dual-dimensional difficulty framework with 37 labels for molecular diagram complexity, creates MolRecBench-Wild benchmark with 5,029 structures, proposes CARBON representation language for semantic evaluation, and shows current OCSR models perform poorly on real-world academic images.


<details>
  <summary>Details</summary>
Motivation: Current Optical Chemical Structure Recognition (OCSR) systems are unreliable on real-world molecular diagrams due to visual and chemical complexity. There's a gap between existing benchmarks (often based on patents) and actual academic literature with complex structures, valence variations, and non-standard semantics.

Method: 1) Introduced MOSAIC framework with 37 fine-grained labels for visual interference and chemical semantic challenges. 2) Built MolRecBench-Wild benchmark with 5,029 structures from 820 recent chemistry papers covering full difficulty spectrum. 3) Proposed CARBON representation language for expressing valence variations, icon-based groups, and non-standard semantics. 4) Established dual-track evaluation protocol supporting both CARBON and SMILES outputs. 5) Evaluated 18 OCSR-capable models on the new benchmark.

Result: Comprehensive experiments reveal severe performance degradation of OCSR models on MolRecBench-Wild, exposing a large gap between previous patent benchmarks and real-world academic scenarios. Current systems struggle with the visual and chemical complexity found in actual chemistry publications.

Conclusion: The paper demonstrates that existing OCSR benchmarks don't reflect real-world academic complexity, and proposes a more comprehensive evaluation framework (MOSAIC difficulty labels, MolRecBench-Wild benchmark, CARBON representation) to better assess and improve OCSR systems for practical use in chemistry research.

Abstract: Optical Chemical Structure Recognition (OCSR) aims to translate molecular diagrams in scientific literature into machine-readable formats, but current systems remain unreliable on real-world images due to substantial visual and chemical complexity. We introduce MOSAIC, a dual-dimensional difficulty framework with 37 fine-grained labels that jointly characterize visual interference and chemical semantic challenges in molecular diagrams. Based on this framework, we construct MolRecBench-Wild, a benchmark of 5,029 structures from 820 recent chemistry papers, covering the full difficulty spectrum observed in real publications. To enable faithful semantic evaluation beyond SMILES and MolFile, we propose CARBON, a representation language capable of expressing valence variations, icon-based groups, and other non-standard chemical semantics. We further adopt a dual-track evaluation protocol supporting both CARBON and SMILES outputs for broad model compatibility. Comprehensive experiments over 18 OCSR-capable models reveal severe performance degradation on MolRecBench-Wild, exposing a large gap between previous patent benchmarks and real-world academic scenarios.

</details>


### [61] [On the Role of Language Representations in Auto-Bidding: Findings and Implications](https://arxiv.org/abs/2605.05833)
*Guanyu Zhu,Jining Luan,Hanwen Du,Xinyu Fang,Sibo Xu,Ersheng Ni,Hongji Li,Jincheng Fang,Ronghao Chen,Huacan Wang,Xuanqi Lan,Yongxin Ni,Yiqi Sun,Youhua Li*

Main category: cs.AI

TL;DR: SemBid is an auto-bidding framework that integrates LLM-encoded semantics (Task, History, Strategy) with numerical trajectory tokens using self-attention, improving controllability and generalization across diverse advertising scenarios.


<details>
  <summary>Details</summary>
Motivation: Existing auto-bidding methods use compact numerical state representations that implicitly capture delivery dynamics but offer limited support for explicitly representing and controlling high-level intent, evolving feedback, and operator-style strategic guidance in real campaigns. While LLMs can encode semantic information, it's unclear when they help and how to integrate them without sacrificing numerical precision.

Method: Proposes SemBid framework that injects LLM-encoded semantics into offline bidding trajectories at token level. Introduces three semantic inputs: Task (campaign objective), History (past performance patterns), and Strategy (operator guidance). Injects these semantics as tokens alongside numerical trajectory tokens and uses self-attention to integrate them, allowing bidirectional interaction between semantic and numerical information.

Result: Across diverse scenarios and budget regimes, SemBid outperforms competitive baselines from offline RL and generative sequence modeling, showing more consistent gains in overall performance, constraint satisfaction, and robustness. Systematic studies show LLM embeddings contain bidding-relevant cues but cannot replace numerical features, and gains emerge only with careful semantic-numeric integration.

Conclusion: SemBid successfully integrates LLM-encoded semantics with numerical bidding trajectories, improving auto-bidding performance through better controllability and generalization. The framework demonstrates that careful integration of semantic and numeric information, rather than naive concatenation, is key to leveraging LLMs' strengths while maintaining numerical precision in advertising auto-bidding systems.

Abstract: Auto-bidding is a crucial task in real-time advertising markets, where policies must optimize long-horizon value under delivery constraints (e.g., budget and CPA). Existing methods for auto-bidding rely on compact numerical state representations: while they can implicitly capture delivery dynamics, they offer limited support for explicitly representing and controlling high-level intent, evolving feedback, and operator-style strategic guidance in real campaigns. Meanwhile, Large Language Models (LLMs) offer a powerful method for encoding semantic information, it remains unclear when LLMs help and how to integrate them without sacrificing numerical precision. Through systematic preliminary studies, we find that (1) LLM embeddings contain bidding-relevant cues yet cannot replace numerical features, and (2) gains emerge only with careful semantic--numeric integration rather than naive concatenation. Motivated by these findings, we propose \textit{SemBid}, a novel auto-bidding framework that injects LLM-encoded semantics into offline bidding trajectories at the token level. SemBid introduces three semantic inputs: \textit{Task}, \textit{History}, and \textit{Strategy}. It injects these semantics as tokens alongside numerical trajectory tokens and uses self-attention to integrate them, improving controllability and generalization across objectives. Across diverse scenarios and budget regimes, SemBid outperforms competitive baselines from offline RL and generative sequence modeling, with more consistent gains in overall performance, constraint satisfaction, and robustness. Our code is available at: \href{https://github.com/AlanYu04/SemBid-KDD2026}{\textcolor{blue}{here}}.

</details>


### [62] [Taklif.AI: LLM-Powered Platform for Interest-Based Personalized College Assignments](https://arxiv.org/abs/2605.05842)
*Zaki Kurdya,Mohammed Zuqlam,Salem Amassi,Shady Telbany,Motaz Saad*

Main category: cs.AI

TL;DR: Taklif.AI is an AI-powered platform that generates personalized assignments based on students' extracurricular interests and cultural contexts using LLMs, moving beyond traditional performance-based personalization to increase engagement and reduce plagiarism.


<details>
  <summary>Details</summary>
Motivation: Traditional one-size-fits-all assignments lead to decreased student engagement and increased plagiarism. Educators struggle to create engaging, personalized assignments that accommodate diverse student interests and cognitive abilities. Existing AI educational platforms only personalize based on academic performance metrics, missing important personalization dimensions.

Method: Taklif.AI uses a structured prompt engineering pipeline with input/output guardrails and LLMs (Llama 3.3 70B) to generate assignments incorporating extracurricular interests and cultural contexts. Built on AWS serverless architecture with Next.js, using LiteLLM for multi-provider load balancing and LangChain for prompt orchestration.

Result: Preliminary user acceptance testing with 68 participants (65 students, 3 educators) showed positive reception: 84% rated the personalization feature as beneficial. The paper describes the system architecture, prompt design methodology, and guardrails framework ensuring output quality.

Conclusion: Taklif.AI demonstrates a promising approach to personalized assignment generation by incorporating extracurricular interests and cultural contexts. The platform shows positive initial reception but requires rigorous empirical evaluation of learning outcomes. The authors outline directions for future research including learning impact studies.

Abstract: Educators face significant challenges in creating engaging, personalized assignments that accommodate students' diverse interests and cognitive abilities. Traditional one-size-fits-all assignments frequently lead to decreased student engagement and increased reliance on unethical practices such as plagiarism. To address these challenges, we present Taklif.AI, a platform that leverages Large Language Models (LLMs) to automatically generate personalized assignments tailored to individual student interests. Unlike existing AI-powered educational platforms that personalize based on academic performance metrics alone, Taklif.AI incorporates students' extracurricular interests and cultural contexts into the assignment generation process through a structured prompt engineering pipeline with input and output guardrails. The platform employs a serverless architecture on AWS with Next.js, using Llama 3.3 70B as the primary LLM via LiteLLM for multi-provider load balancing and LangChain for prompt orchestration. We describe the system architecture, the prompt design methodology, and the guardrails framework that ensures output quality. Preliminary user acceptance testing with 68 participants (65 students and 3 educators) indicates positive reception, with 84% of participants rating the personalization feature as beneficial. We discuss the platform's current capabilities and limitations, and outline directions for rigorous empirical evaluation of learning outcomes.

</details>


### [63] [AirQualityBench: A Realistic Evaluation Benchmark for Global Air Quality Forecasting](https://arxiv.org/abs/2605.05854)
*Xing Xu,Xu Wang,Yudong Zhang,Huilin Zhao,Zhengyang Zhou,Yang Wang*

Main category: cs.AI

TL;DR: AirQualityBench is a global multi-pollutant benchmark that evaluates forecasting models under realistic conditions like missing data, uneven coverage, and heterogeneous scales, rather than sanitized datasets, revealing that strong performance on clean data doesn't transfer well to real monitoring networks.


<details>
  <summary>Details</summary>
Motivation: Current air-quality forecasting evaluations use sanitized datasets that remove or impute missing data, hiding real-world challenges: uneven global coverage, structured missingness, heterogeneous pollutant scales, and deployment costs. This creates a gap between research performance and real-world applicability.

Method: Created AirQualityBench with 3,720 monitoring stations (2021-2025), six major pollutants, preserving provider-native observation masks. Instead of imputing missing data, exposes missingness as part of forecasting problem and reports errors on valid future observations after inverse transformation to physical concentration scales.

Result: Evaluation of representative spatio-temporal models shows that strong performance on sanitized datasets does not reliably transfer to global, fragmented monitoring streams, highlighting the importance of realistic evaluation protocols.

Conclusion: AirQualityBench serves as a realistic testbed for scalable, mask-aware, and physically interpretable air-quality forecasting, addressing the disconnect between research evaluations and real-world deployment conditions.

Abstract: Air-quality forecasting models are commonly evaluated on regional, preprocessed, and normalized datasets, where missing observations are removed or artificially completed. Such protocols simplify comparison but hide the conditions that dominate real monitoring networks: uneven global coverage, structured missingness, heterogeneous pollutant scales, and deployment cost. We introduce \textbf{AirQualityBench}, a global multi-pollutant benchmark designed to evaluate forecasting models under these realistic conditions. The benchmark contains hourly observations from 3,720 monitoring stations over 2021--2025, covers six major pollutants, and preserves provider-native observation masks. Rather than imputing a dense data tensor, AirQualityBench exposes missingness as part of the forecasting problem and reports errors on valid future observations after inverse transformation to physical concentration scales. Evaluating representative spatio-temporal models under this unified protocol shows that strong performance on sanitized datasets does not reliably transfer to global, fragmented monitoring streams. AirQualityBench therefore serves as a realistic testbed for scalable, mask-aware, and physically interpretable air-quality forecasting. All benchmark data, code, evaluation scripts, and baseline implementations are available at \href{https://github.com/Star-Learning/AirQualityBench}{GitHub}.

</details>


### [64] [SANEmerg: An Emergent Communication Framework for Semantic-aware Agentic AI Networking](https://arxiv.org/abs/2605.05861)
*Yong Xiao,Haoran Zhou,Yujie Zhou,Marwan Krunz*

Main category: cs.AI

TL;DR: SANEmerg enables AI agents to develop efficient communication protocols under bandwidth and computational constraints for collaborative task completion in agentic networks.


<details>
  <summary>Details</summary>
Motivation: Traditional networking systems have rigid decoupling of communication and computation, causing inefficiencies in large-scale AI agent networks (AgentNet) where heterogeneous agents need to collaborate in real-time to fulfill complex user requirements.

Method: Proposes SANEmerg, a multi-agent emergent communication framework with a bandwidth-adaptable importance-filter to prioritize high-contribution message dimensions and a complexity-regularizer based on Minimum Description Length principle for computationally bounded signaling.

Result: SANEmerg achieves superior task accuracy while significantly reducing bandwidth and computational overhead compared to state-of-the-art solutions in AgentNet prototype evaluations.

Conclusion: Emergent communication enables efficient AI agent collaboration under realistic constraints, with SANEmerg demonstrating practical viability for semantic-aware agentic networks through adaptive bandwidth utilization and computational regularization.

Abstract: Future networking systems are envisioned to become part of an agentic AI-native ecosystem in which a vast number of heterogeneous and specialized AI agents cooperate seamlessly to fulfill complex user requirements in real time. However, traditional networking paradigms are characterized by a rigid decoupling of communication and computation, which often leads to significant inefficiencies in large-scale agentic AI networking (AgentNet) systems. Emergent communication offers a novel solution by enabling autonomous agents that support task-specific signaling protocols for information exchange and collaborative coordination. In this paper, we consider a multi-agent emergent communication framework, tailored for semantic-aware AgentNet systems in which the user's semantic intent can be automatically detected, inferred, and linked to a set of sub-tasks to be assigned to a set of agents. We investigate how communication and signaling protocols can emerge among collaborative agents with computationally bounded intelligence under stringent bandwidth constraints. Our proposed framework, called SANEmerg, is designed to facilitate the emergence of communication for collaborative task fulfillment while adhering to the physical limits of AgentNet. SANEmerg incorporates a bandwidth-adaptable importance-filter that dynamically prioritizes the transmission of higher-contribution message dimensions, ensuring robust performance in bandwidth-limited environments. Furthermore, SANEmerg integrates a complexity-regularizer grounded in the Minimum Description Length (MDL) principle to facilitate the emergence of computationally bounded signaling. Evaluated via an AgentNet prototype and extensive experimentation, SANEmerg demonstrates significant performance improvements over state-of-the-art solutions, achieving superior task accuracy while significantly reducing bandwidth and computational overhead.

</details>


### [65] [XDecomposer: Learning Prior-Free Set Decomposition for Multiphase X-ray Diffraction](https://arxiv.org/abs/2605.05866)
*Hanyu Gao,Bin Cao,Yunyue Su,Tong-Yi Zhang,Qiang Liu*

Main category: cs.AI

TL;DR: XDecomposer is a prior-free framework for joint decomposition and identification of multiphase XRD patterns without requiring candidate lists or prior knowledge, achieving accurate phase separation and identification across diverse chemical systems.


<details>
  <summary>Details</summary>
Motivation: Multiphase powder X-ray diffraction analysis is a fundamental bottleneck in structure identification because real-world synthesis often produces complex mixtures that cannot be reliably disentangled. Existing approaches assume single-phase inputs and break down in multiphase settings.

Method: Formulates multiphase diffraction analysis as a set prediction problem where the model infers unordered sets of phase-resolved components, mixture proportions, and structural representations. Uses a phase-query-driven decomposition mechanism with diffraction-consistent physical reconstruction to enable accurate source separation while preserving crystallographic fidelity.

Result: Extensive experiments on simulated and experimental datasets show that XDecomposer substantially improves reconstruction accuracy and phase identification across diverse chemical systems while maintaining strong generalization to unseen mixtures.

Conclusion: Provides a practical route toward data-driven, source-resolved multiphase XRD analysis and reduces long-standing dependence on prior-guided iterative phase matching, with openly available code for implementation.

Abstract: Multiphase powder X-ray diffraction (PXRD) analysis remains a fundamental bottleneck in structure identification, as real-world synthesis often produces complex mixtures whose constituent phases (components) cannot be reliably disentangled. While recent advances in representation-based crystal retrieval and generation suggest the possibility of inferring structures directly from PXRD, existing approaches largely assume single-phase inputs and break down in multiphase settings. Here, we present XDecomposer, a prior-free framework for joint decomposition and identification of multiphase XRD patterns without requiring candidate phase lists, structural templates, or prior knowledge of phase number. We formulate multiphase diffraction analysis as a set prediction problem, where the model infers an unordered set of phase-resolved components, their mixture proportions, and corresponding structural representations within a unified architecture. A phase-query-driven decomposition mechanism, together with diffraction-consistent physical reconstruction, enables accurate source separation while preserving crystallographic fidelity. Extensive experiments on both simulated and experimental datasets show that XDecomposer substantially improves reconstruction accuracy and phase identification across diverse chemical systems, while maintaining strong generalization to unseen mixtures. These results provide a practical route toward data-driven, source-resolved multiphase XRD analysis and reduce long-standing dependence on prior-guided iteratively phase matching. The code is openly available at https://github.com/Licht0812/XDecomposer

</details>


### [66] [Agentic, Context-Aware Risk Intelligence in the Internet of Value](https://arxiv.org/abs/2605.05878)
*Basel Magableh,OmniRisk Research*

Main category: cs.AI

TL;DR: A risk management framework for Internet of Value networks combining prediction engines, decentralized verification, sentiment analysis, constrained AI agents, and scenario-based action programs, validated through empirical stress tests on Solana.


<details>
  <summary>Details</summary>
Motivation: The Internet of Value (IoV) operates as a heterogeneous, partially-trusted network where the main risks are composite (route, sentiment, liquidity, policy commitments) rather than isolated to single chains, requiring a more sophisticated risk primitive than current approaches.

Method: Proposes a risk primitive composed of five engines: 1) prediction engine for price, liquidity, volatility, and route health; 2) Bittensor verification subnet for decentralized scoring of predictions; 3) sentiment-fusion engine analyzing text, on-chain flow, and grey literature; 4) agentic engine with constitutional, role-bound action constraints; 5) API-risk and scenario engine converting forecasts into pre-committed action programs via Monte-Carlo simulation.

Result: Validated through two empirical studies: 27-hour policy-constrained liquidity stress-response experiment on Solana, and 168-hour prediction-router calibration with explicit class-imbalance honesty reporting. The architecture demonstrates deployability with formally stated, falsifiable validator-loss decomposition.

Conclusion: The proposed composite risk primitive provides a deployable solution for managing complex, multi-dimensional risks in IoV networks, with empirical validation supporting its practical applicability and formal, falsifiable risk assessment methodology.

Abstract: The Internet of Value (IoV) is a heterogeneous, partially-trusted network in which the dominant marginal risk is composite (route, sentiment, liquidity, and the policy a system is willing to commit to) rather than a property of any single chain. We argue that a risk primitive adequate for this regime is a composition of five engines: a prediction engine over price, liquidity, volatility, and route health; a Bittensor verification subnet that decentralises and economically scores prediction outputs; a sentiment-fusion engine over text, on-chain flow, and grey-literature feeds; an agentic engine under constitutional, role-bound action constraints; and an API-risk and scenario engine that converts forecasts into pre-committed action programs in the sense of Monte-Carlo scenario generation. We anchor the architecture in two empirical artefacts: a 27-hour policy-constrained liquidity stress-response experiment on Solana, and a 168-hour prediction-router calibration arc reported with explicit class-imbalance honesty. The case study supports deployability; the validator-loss decomposition is stated formally and is falsifiable.

</details>


### [67] [Null Space Constrained Contrastive Visual Forgetting for MLLM Unlearning](https://arxiv.org/abs/2605.05909)
*Yuhang Wang,Zhenxing Niu,Haoxuan Ji,Guangyu He,Linlin Zhang,Haichang Gao*

Main category: cs.AI

TL;DR: A multimodal LLM unlearning approach that removes target visual knowledge while preserving non-target visual and all textual knowledge by fine-tuning only the visual module with contrastive forgetting and null space constraints, extending to continual unlearning scenarios.


<details>
  <summary>Details</summary>
Motivation: The core challenge of machine unlearning is balancing target knowledge removal with non-target knowledge retention. For MLLMs, this is especially difficult because knowledge is divided into tightly intertwined visual and textual modalities, requiring a more sophisticated approach.

Method: 1) Freeze the LLM backbone and achieve unlearning by fine-tuning only the visual module. 2) Use Contrastive Visual Forgetting (CVF) to separate target visual knowledge from retained visual knowledge by guiding target visual concept representations toward appropriate feature space regions. 3) Identify the null space associated with retained knowledge and constrain unlearning within this space to minimize knowledge retention degradation. 4) Extend the approach to continual unlearning scenarios where forgetting requests arrive sequentially.

Result: Extensive experiments across diverse benchmarks demonstrate that the approach achieves a strong balance between effective forgetting and robust knowledge retention.

Conclusion: The proposed MLLM unlearning approach effectively addresses the challenge of selectively forgetting target visual knowledge while preserving both non-target visual knowledge and all textual knowledge, with strong performance demonstrated in both static and continual unlearning scenarios.

Abstract: The core challenge of machine unlearning is to strike a balance between target knowledge removal and non-target knowledge retention. In the context of Multimodal Large Language Models (MLLMs), this challenge becomes even more pronounced, as knowledge is further divided into visual and textual modalities that are tightly intertwined. In this paper, we introduce an MLLM unlearning approach that aims to forget target visual knowledge while preserving non-target visual knowledge and all textual knowledge. Specifically, we freeze the LLM backbone and achieve unlearning by fine-tuning the visual module. First, we propose a Contrastive Visual Forgetting (CVF) mechanism to separate target visual knowledge from retained visual knowledge, guiding the representations of target visual concepts toward appropriate regions in the feature space. Second, we identify the null space associated with retained knowledge and constrain the unlearning process within this space, thereby significantly mitigating degradation in knowledge retention. Third, beyond static unlearning scenarios, we extend our approach to continual unlearning, where forgetting requests arrive sequentially. Extensive experiments across diverse benchmarks demonstrate that our approach achieves a strong balance between effective forgetting and robust knowledge retention.

</details>


### [68] [PREFER: Personalized Review Summarization with Online Preference Learning](https://arxiv.org/abs/2605.05911)
*Millend Roy,Agostino Capponi,Vineet Goyal*

Main category: cs.AI

TL;DR: This paper proposes an online learning framework for generating personalized product review summaries that adapt to individual user preferences through iterative feedback, addressing the limitations of static, generic summarization systems in e-commerce.


<details>
  <summary>Details</summary>
Motivation: Current e-commerce review summarization systems produce generic, static summaries that fail to account for individual user preferences and evolving needs. Users care about different product characteristics, and these preferences may change over time with interactions, making personalized summarization challenging due to unknown latent preferences.

Method: The authors propose an online learning framework that generates personalized summaries for each user. The system iteratively refines its understanding of user preferences by incorporating direct feedback from the generated summaries over time, allowing it to adapt to latent preferences that aren't known in advance.

Result: The authors provide a case study using the Amazon Reviews'23 dataset, showing in controlled simulations that their online preference learning approach improves alignment with target user interests while maintaining summary quality.

Conclusion: Personalized review summarization through online preference learning effectively addresses the limitations of static systems by adapting to individual user needs over time, creating more relevant summaries that better serve diverse and evolving user interests in e-commerce environments.

Abstract: Product reviews significantly influence purchasing decisions on e-commerce platforms. However, the sheer volume of reviews can overwhelm users, obscuring the information most relevant to their specific needs. Current e-commerce summarization systems typically produce generic, static summaries that fail to account for the fact that (i) different users care about different product characteristics, and (ii) these preferences may evolve with interactions. To address the challenge of unknown latent preferences, we propose an online learning framework that generates personalized summaries for each user. Our system iteratively refines its understanding of user preferences by incorporating feedback directly from the generated summaries over time. We provide a case study using the Amazon Reviews'23 dataset, showing in controlled simulations that online preference learning improves alignment with target user interests while maintaining summary quality.

</details>


### [69] [Wisteria: A Unified Multi-Scale Feature Learning Framework for DNA Language Model](https://arxiv.org/abs/2605.05913)
*Weihua Wang,Haoji Li,Feilong Bao,Lei Yang,Guanglai Gao*

Main category: cs.AI

TL;DR: Wisteria is a genomic language model that integrates multi-scale feature learning using Mamba architecture augmented with gated dilated convolutions for local motifs, gated MLPs for global dependencies, and Fourier-based attention for frequency domain modeling, showing strong performance across various experimental settings.


<details>
  <summary>Details</summary>
Motivation: Existing DNA language models focus on long-range token interactions but often neglect the interplay between local motifs (regulatory patterns) and global dependencies, which is crucial for understanding genomic regulatory grammar and semantics.

Method: Wisteria integrates multi-scale feature learning within a unified framework by: 1) augmenting Mamba-based architecture with gated dilated convolutions to capture local motifs, 2) using gated multilayer perceptrons to refine global dependencies, and 3) introducing Fourier-based attention mechanism for frequency domain modeling, periodic extension, and length generalization.

Result: Wisteria demonstrates strong performance across four experimental settings with both short and long-range dependencies, outperforming competitive DNA language model baselines on downstream benchmarks.

Conclusion: Wisteria effectively unifies local and global dependency modeling for multi-scale genomic sequence analysis, addressing the limitations of existing methods that often ignore the interplay between local motifs and global dependencies.

Abstract: DNA language model aims to decipher the regulatory grammar and semantic of genomes by capturing long range dependencies in DNA sequences. Existing methods emphasize long range token interactions but often ignore the interplay between local motifs and global dependencies. In this paper, we propose Wisteria, a genomic language model that integrates multi scale feature learning within a unified framework for DNA sequence. Specifically, Wisteria augments the Mamba based architecture with gated dilated convolutions to capture local motifs and regulatory patterns, while gated multilayer perceptrons refine global dependencies. We further introduce a Fourier based attention mechanism to support frequency domain modeling, periodic extension and length generalization. Across four experimental settings with both short and long range dependencies, Wisteria demonstrates strong performance on downstream benchmarks against competitive DNA language model baselines. These results indicate that Wisteria effectively unifies local and global dependency modeling for multi scale genomic sequence analysis.

</details>


### [70] [Intentmaking and Sensemaking: Human Interaction with AI-Guided Mathematical Discovery](https://arxiv.org/abs/2605.05921)
*Alex Bäuerle,Adam Connors,Alexander Novikov,Adam Zsolt Wagner,Ngân Vũ,Fernanda Viegas,Martin Wattenberg,Lucas Dixon*

Main category: cs.AI

TL;DR: The paper introduces the concept of "intentmaking" as a distinct workflow in AI-assisted scientific discovery, observed through a study of mathematicians using AlphaEvolve, emphasizing iterative goal refinement through system interaction.


<details>
  <summary>Details</summary>
Motivation: Current AI tools for scientific discovery often use a simple question/answer model, which doesn't effectively support the complex, iterative nature of real scientific investigation. There's a need for better interaction paradigms that go beyond treating AI as black-box assistants.

Method: Conducted a formative user study with 11 expert mathematicians who used AlphaEvolve (an evolutionary coding agent) to tackle advanced problems in their fields, observing and analyzing their workflows and interaction patterns.

Result: Identified a distinct iterative workflow termed "intentmaking" - the process of discovering, defining, and refining experimental goals through active system interaction. Found that users enter cycles of intentmaking (defining/updating experiments) and sensemaking (interpreting results) that repeat throughout investigations.

Conclusion: AI tools for scientific discovery should be designed as collaborative instruments rather than opaque black-box assistants, supporting iterative cycles of intentmaking and sensemaking to better align with real scientific investigation processes.

Abstract: Artificial intelligence offers powerful new tools for scientific discovery, but the interaction paradigms required to effectively harness these systems remain underexplored. In this paper, we present findings from a formative user study with 11 expert mathematicians who used AlphaEvolve, an evolutionary coding agent, to tackle advanced problems in their fields of expertise. We identify and characterize a distinct workflow we term intentmaking, the iterative process of discovering, defining, and refining one's experimental goals through active system interaction. We frame this as a natural extension to sensemaking, the cognitive process of building an understanding of complex or novel data. We suggest that users enter a cycle of intentmaking (defining and updating their experiment) and sensemaking (interpreting the results) which repeats many times during the course of an investigation. Our documentation of these themes suggests an approach to designing AI tools for scientific discovery that goes beyond the existing question/answer model of many current systems, treating them as collaborative instruments rather than opaque black-box assistants.

</details>


### [71] [Which Are the Low-Resource Languages of the Semantic Web?](https://arxiv.org/abs/2605.05929)
*Ndeye-Emilie Mbengue,Pierre Monnin,Miguel Couceiro,Fabien Gandon*

Main category: cs.AI

TL;DR: A method to analyze language distribution in Linked Open Data Knowledge Graphs and propose a formal definition of low-resource languages to address digital divide in Open Access Data.


<details>
  <summary>Details</summary>
Motivation: Emerging digital technologies are widening the Open Access Data divide between high- and low-resource languages, excluding many communities from global digital transformation. There's currently no clear quantitative definition of low-resource languages in the context of Linked Open Data Knowledge Graphs.

Method: The authors present a methodology to analyze language distribution across LOD KGs and propose a preliminary multi-level categorization based on three major knowledge bases: DBpedia, BabelNet, and Wikidata.

Result: The categorization enables a formal definition of low-, high-, and medium-resource languages that can be leveraged for selecting cross-lingual transfer candidates in multilingual LOD KGs.

Conclusion: By establishing formal definitions of language resource levels in LOD KGs, this work provides a foundation for mitigating the digital divide through more systematic cross-lingual transfer approaches in multilingual knowledge graphs.

Abstract: Emerging digital technologies are exacerbating the existing divide in Open Access Data (OAD) between high-and low-resource languages, excluding many communities from the global digital transformation. Multilingual Linked Open Data Knowledge Graphs (LOD KGs) could contribute to mitigating this divide through cross-lingual transfer; however, no clear quantitative definition of low-resource languages has yet been established in the context of LOD KGs. In this poster, we present a methodology to analyze the distribution of languages across LOD KGs and propose a preliminary multi-level categorization based on DBpedia, BabelNet, and Wikidata. This categorization is leveraged to bring a formal definition of low-, high-, and medium-resource languages that could be later leveraged to select cross-lingual transfer candidates.

</details>


### [72] [In Data or Invisible: Toward a Better Digital Representation of Low-Resource Languages with Knowledge Graphs](https://arxiv.org/abs/2605.05931)
*Ndeye-Emilie Mbengue*

Main category: cs.AI

TL;DR: This PhD proposal addresses the language divide in Linked Open Data knowledge graphs by analyzing language representation and developing methods to improve multilingual KG completion through cross-lingual transfer and analogical reasoning.


<details>
  <summary>Details</summary>
Motivation: Digital technologies are exacerbating the Open Access Data divide between high- and low-resource languages, excluding many communities from global digital transformation. There's a significant gap in language coverage within Linked Open Data knowledge graphs that needs to be addressed.

Method: 1) Identify key variables characterizing language distribution in LOD (Wikipedia articles per language, language-tagged entities). 2) Analyze these variables across DBpedia, BabelNet, and Wikidata. 3) Study cross-lingual transfer candidate selection for multilingual KG completion. 4) Investigate strategies based on linguistic proximity and curated alignments. 5) Explore analogical reasoning using (dis)similarities to identify cross-linguistic correspondences.

Result: The abstract doesn't present completed results since this is a PhD proposal, but outlines planned outcomes: insights into language representation in major LOD KGs, improved multilingual KG completion methods, and enhanced language coverage through novel cross-lingual approaches.

Conclusion: This research aims to bridge the language divide in Linked Open Data by developing systematic approaches to analyze and improve language coverage in knowledge graphs, ultimately enabling more inclusive participation in the digital transformation for speakers of low-resource languages.

Abstract: Emerging digital technologies are exacerbating the existing divide in Open Access Data (OAD) between high-and low-resource languages, excluding many communities from participating in the global digital transformation. In this PhD proposal, we aim to address this gap, focusing on the language coverage of Linked Open Data knowledge graphs (LOD KGs). First, we identify key variables that characterize language distribution in LOD, including the number of Wikipedia articles per language edition and the number of language-tagged entities in LOD KGs. These variables are analyzed across three major multilingual LOD KGs, DBpedia, BabelNet, and Wikidata, providing insights into the representation and distribution of languages within LOD. Building on this analysis, we intend to study the impact of cross-lingual transfer candidate selection on the task of multilingual KG completion. In particular, we plan to investigate strategies based on linguistic proximity and the availability of curated annotated alignments between languages. Language proximity also motivates us to explore the benefits of analogical reasoning that relies on (dis)similarities and has not yet been investigated to identify correspondences across languages to improve KG completion performance and enhance language coverage in LOD.

</details>


### [73] [ICU-Bench:Benchmarking Continual Unlearning in Multimodal Large Language Models](https://arxiv.org/abs/2605.05938)
*Yuhang Wang,Wenjie Mei,Junkai Zhang,Guangyu He,Zhenxing Niu,Haichang Gao*

Main category: cs.AI

TL;DR: ICU-Bench is a new benchmark for continual multimodal unlearning on privacy-sensitive document data, revealing that current unlearning methods struggle in continual settings.


<details>
  <summary>Details</summary>
Motivation: MLLMs trained on large multimodal datasets raise serious privacy concerns, making machine unlearning necessary. Existing benchmarks focus on static/short-sequence settings, lacking support for evaluating continual privacy deletion requests in realistic deployments.

Method: Introduced ICU-Bench, built on privacy-critical document data (medical reports and labor contracts). Contains 1,000 privacy-sensitive profiles with 9,500 images, 16,000 Q&A pairs, and 100 forget tasks. Introduced new continual unlearning metrics for comprehensive analysis.

Result: Extensive experiments show existing unlearning methods generally struggle in continual settings, exhibiting limitations in balancing forgetting quality, utility preservation, and scalability over long task sequences.

Conclusion: Findings highlight the need for multimodal unlearning methods explicitly designed for continual privacy deletion, as current approaches are inadequate for realistic deployment scenarios.

Abstract: Although Multimodal Large Language Models (MLLMs) have achieved remarkable progress across many domains, their training on large-scale multimodal datasets raises serious privacy concerns, making effective machine unlearning increasingly necessary. However, existing benchmarks mainly focus on static or short-sequence settings, offering limited support for evaluating continual privacy deletion requests in realistic deployments. To bridge this gap, we introduce ICU-Bench, a continual multimodal unlearning benchmark built on privacy-critical document data. ICU-Bench contains 1,000 privacy-sensitive profiles from two document domains, medical reports and labor contracts, with 9,500 images, 16,000 question-answer pairs, and 100 forget tasks. Additionally, new continual unlearning metrics are introduced, facilitating a comprehensive analysis of forgetting effectiveness, historical forgetting preservation, retained utility, and stability throughout the continual unlearning process. Through extensive experiments with representative unlearning methods on ICU-Bench, we show that existing methods generally struggle in continual settings and exhibit clear limitations in balancing forgetting quality, utility preservation, and scalability over long task sequences. These findings highlight the need for multimodal unlearning methods explicitly designed for continual privacy deletion.

</details>


### [74] [MAS-Algorithm: A Workflow for Solving Algorithmic Programming Problems with a Multi-Agent System](https://arxiv.org/abs/2605.05949)
*Yuliang Xu,Xiang Xu,Yao Wan,Hu Wei,Tong Jia*

Main category: cs.AI

TL;DR: MAS-Algorithm is a multi-agent workflow for algorithmic problem solving that decomposes coding tasks into modular stages, achieving significant performance improvements over existing methods.


<details>
  <summary>Details</summary>
Motivation: Existing approaches to algorithmic problem solving in AI are either costly (model-centric strategies like architectural modifications and data scaling) or fragmented (external tools or prompting techniques like chain-of-thought), lacking a unified framework for structured reasoning.

Method: Proposes MAS-Algorithm, a systematic multi-agent workflow inspired by competitive programmers and algorithm engineers. The framework decomposes end-to-end solving into modular stages enabling structured reasoning, tool integration, and flexible coordination among agents, emphasizing both rigor and extensibility.

Result: Experimental results show consistent improvements: 6.48% average gain in acceptance rate across Qwen series models vs. only 0.89% from parameter-efficient fine-tuning; 4.72% gain on LiveCodeBench-Pro; individual agents can contribute improvements up to 27.7% based on ablation studies.

Conclusion: MAS-Algorithm demonstrates strong potential for advancing AI-driven algorithmic reasoning through its multi-agent workflow, offering both performance improvements and better interpretability of the reasoning process compared to existing approaches.

Abstract: Algorithmic problem solving serves as a rigorous testbed for evaluating structured reasoning in AI coding systems, as it directly reflects a model's ability to perform structured reasoning in complex scenarios.Existing approaches predominantly rely on model-centric strategies, such as architectural modifications and data scaling, which are costly and offer limited interpretability. Alternative methods leveraging external tools or prompting techniques (e.g., chain-of-thought) are often fragmented and lack a unified framework. In this paper, we propose MAS-Algorithm, a systematic multi-agent workflow for algorithmic problem solving inspired by the practices of competitive programmers and algorithm engineers. Our framework decomposes the end-to-end solving process into modular stages, enabling structured reasoning, tool integration, and flexible coordination among agents. The design emphasizes both rigor and extensibility, allowing it to generalize across diverse problem types.Experimental results on a self-constructed benchmark demonstrate consistent improvements across multiple Qwen series models, achieving an average gain of 6.48% in acceptance rate. In contrast, parameter-efficient fine-tuning on the same data yields only a marginal improvement of 0.89%. We further observe a 4.72% gain on LiveCodeBench-Pro, along with consistent improvements across additional accuracy and efficiency metrics.Beyond performance gains, we conduct comprehensive analyses to better understand the reasoning process within the workflow, including error patterns and cross-scenario behaviors. We further perform customized replacement and ablation studies to explore the upper bound of the framework, showing that individual agents can contribute improvements of up to 27.7%. These results highlight the strong potential of MAS-Algorithm for advancing AI-driven algorithmic reasoning.

</details>


### [75] [HaM-World: Soft-Hamiltonian World Models with Selective Memory for Planning](https://arxiv.org/abs/2605.05951)
*Haoyun Tang,Haodong Cui,Keyao Xu,Kun Wang,Zhandong Mei*

Main category: cs.AI

TL;DR: HaM-World (HMW) introduces a structured world model with canonical (q,p) Hamiltonian dynamics and context subspace c, plus Mamba memory, for stable long-horizon planning and improved robustness to distribution shifts.


<details>
  <summary>Details</summary>
Motivation: World models struggle with unstable imagined rollouts as planning horizon grows or dynamics distribution shifts, due to missing history-conditioned memory and geometric organization in planner-facing latents.

Method: Decomposes latent state into canonical (q,p) subspace for Hamiltonian dynamics and context subspace c for semantic/dissipative factors, using Mamba selective state-space memory as history-conditioned input. (q,p) evolves through energy-derived Hamiltonian vector field plus learnable residuals.

Result: On DeepMind Control Suite: highest Avg. AUC (117.9, +9.5%), reduces long-horizon rollout error to 45% of baseline, wins 11/12 MSE comparisons. Under OOD perturbations, achieves highest return in every condition with average gains of 10.2% (Finger Spin) and 13.6% (Reacher Easy).

Conclusion: Structured world modeling with Hamiltonian dynamics and memory enables stable long-horizon planning and robustness to distribution shifts, addressing fundamental limitations of previous world models.

Abstract: World models enable model-based planning through learned latent dynamics, but imagined rollouts become unstable as the planning horizon grows or the dynamics distribution shifts. We argue that this instability reflects two missing structures in planner-facing latents: history-conditioned memory for approximate Markov completeness, and geometric organization that separates configuration, momentum, and task semantics. We propose HaM-World (HMW), a structured world model that decomposes the latent state into a canonical (q, p) subspace and a context subspace c, while using Mamba selective state-space memory as the history-conditioned input to the same latent dynamics. Within this interface, (q, p) evolves through an energy-derived Hamiltonian vector field plus learnable residual/control dynamics, while c captures semantic, dissipative, and non-conservative factors. This gives the planner a single latent state shared by dynamics prediction, reward/value estimation, imagined rollouts, and CEM action search. On four DeepMind Control Suite tasks, HaM-World reaches the highest Avg. AUC (117.9, +9.5%), reduces long-horizon rollout error to 45% of a strong baseline model, and wins 11/12 k in {3,5,7} MSE cells. Under 12 OOD perturbations spanning dynamics shifts, action delay, and observation masking, HaM-World achieves the highest return in every condition, with average OOD-return gains of 10.2% on Finger Spin and 13.6% on Reacher Easy. Mechanism diagnostics further show bounded action-free Hamiltonian-energy drift, structured energy variation under policy rollouts, and coherent control-induced energy transfer, supporting the intended Soft-Hamiltonian dynamics design.

</details>


### [76] [Temporal Smoothness Doubly Robust Learning for Debiased Knowledge Tracing](https://arxiv.org/abs/2605.05958)
*Peilin Zhan,Wei Chen,Weilin Chen,Shuyi Pan,Ruichu Cai*

Main category: cs.AI

TL;DR: A novel doubly robust framework called TSDR addresses selection bias in knowledge tracing by combining propensity scoring and imputation models with temporal smoothness regularization to reduce variance accumulation over time.


<details>
  <summary>Details</summary>
Motivation: Knowledge tracing faces severe selection bias due to non-random exercise recommendations and student choices, which existing methods neglect. This bias leads to inaccurate mastery estimates and error accumulation in subsequent recommendations, undermining intelligent education systems.

Method: Proposes a Temporal Smoothness Doubly Robust (TSDR) framework that combines: (1) a doubly robust formulation with propensity and imputation models for bias correction, (2) theoretical analysis showing estimator variance accumulates over time, and (3) smoothness regularization to control variance while preserving unbiasedness.

Result: TSDR consistently enhances various state-of-the-art KT backbones across multiple real-world benchmarks. The framework effectively reduces variance accumulation while maintaining the unbiasedness guarantee of doubly robust estimation.

Conclusion: Principled bias correction is vital for knowledge tracing. The proposed TSDR framework successfully addresses selection bias and variance accumulation through doubly robust estimation combined with temporal smoothness regularization, leading to improved performance across diverse KT models.

Abstract: Knowledge Tracing (KT) is fundamental to intelligent education systems, yet relies on educational logs that are selectively observed. The non-random nature of exercise recommendations and student choices inevitably induces severe selection bias. Most existing KT methods neglect this issue, training on observed logs using standard empirical risk, which yields biased mastery estimates and accumulates errors in subsequent recommendations. To address this, we introduce a doubly robust (DR) formulation for KT that integrates a propensity model with an error imputation model, theoretically guaranteeing unbiasedness if either model is accurate. Beyond unbiasedness, in the sequential setting of KT, we identify that the estimator's performance is compromised by variance-dependent stochastic deviations that accumulate over time, thereby causing training instability and limiting performance. To mitigate this, we derive a generalization bound that explicitly characterizes the impact of estimator variance and identifies temporal smoothness as a key factor in controlling it. Building on these theoretical insights, we propose the Temporal Smoothness Doubly Robust (TSDR) framework. TSDR jointly optimizes the KT predictor and the imputation model with a smoothness regularizer, effectively reducing variance while preserving the unbiasedness guarantee of DR. Experiments on multiple real-world benchmarks demonstrate that TSDR consistently enhances various state-of-the-art KT backbones, underscoring the vital role of principled bias correction in KT.

</details>


### [77] [From Coordinate Matching to Structural Alignment: Rethinking Prototype Alignment in Heterogeneous Federated Learning](https://arxiv.org/abs/2605.05959)
*Xinghao Wu,Jianwei Niu,Guogang Zhu,Xuefeng Liu,Shaojie Tang,Jiayuan Zhang*

Main category: cs.AI

TL;DR: FedSAF introduces structural alignment instead of coordinate alignment for heterogeneous federated learning, achieving better performance by focusing on inter-class relational structure rather than forcing clients into a single global feature subspace.


<details>
  <summary>Details</summary>
Motivation: Existing prototype-based HtFL methods use coordinate alignment (MSE or cosine-based) developed for homogeneous FL, which forces all clients to map representations into the same global feature subspace. This is problematic for HtFL because heterogeneous feature extractors naturally create client-specific feature subspaces, and forcing alignment to a single global subspace suppresses learning capacity.

Method: FedSAF shifts from coordinate alignment to structural alignment. Instead of aligning absolute coordinates, it focuses on aligning inter-class relational structure. This decouples the beneficial semantic structure alignment from the unnecessary shared feature basis requirement.

Result: Structural alignment consistently outperforms coordinate alignment in heterogeneous settings. Experiments on multiple benchmarks show FedSAF outperforms state-of-the-art prototype-based HtFL methods by up to 3.52%.

Conclusion: Coordinate alignment is suboptimal for HtFL because it couples semantic structure alignment with unnecessary feature subspace constraints. Structural alignment better preserves client-specific learning capacity while enabling effective collaboration across heterogeneous models.

Abstract: Heterogeneous federated learning (HtFL) aims to enable collaboration among clients that differ in both data distributions and model architectures. Prototype-based methods, which communicate class-level feature centers (prototypes) instead of full model parameters, have recently shown strong potential for HtFL. Existing prototype-based HtFL methods typically reuse the MSE-based or cosine-based alignment mechanism developed for homogeneous FL when aligning client-specific representations with global prototypes. These approaches are essentially coordinate alignment, where representations of clients are forced to match the global prototypes in the embedding space in an element-wise manner. Such alignment implicitly assumes that all clients should map their representations into the feature subspace defined by the global prototypes. This assumption is reasonable in homogeneous FL, where all clients share the same feature extractor. However, it becomes problematic in HtFL, since heterogeneous feature extractors naturally induce client-specific feature subspaces, and forcing all clients to optimize within a single global subspace unnecessarily suppresses their learning capacity. We observe that coordinate alignment implicitly couples two distinct objectives: aligning inter-class semantic structure, which is directly beneficial for classification, and enforcing a shared feature basis, which is unnecessary and even harmful under model heterogeneity. Building on this insight, we design FedSAF, which shifts the alignment objective from absolute coordinates to inter-class relational structure. We demonstrate that structural alignment consistently outperforms coordinate alignment in heterogeneous settings. Experiments on multiple benchmarks show that our structural alignment outperforms state-of-the-art prototype-based HtFL methods by up to 3.52\%.

</details>


### [78] [TheraAgent: Self-Improving Therapeutic Agent for Precise and Comprehensive Treatment Planning](https://arxiv.org/abs/2605.05963)
*Junkai Li,Yunghwei Lai,Tianyi Zhu,Zheng Long Lee,Weizhi Ma,Yang Liu*

Main category: cs.AI

TL;DR: TheraAgent is an agentic framework that replaces one-shot LLM generation with iterative generate-judge-refine pipelines for creating safer, more comprehensive treatment plans by mimicking human expert reasoning.


<details>
  <summary>Details</summary>
Motivation: Existing LLMs produce treatment plans through one-shot generation without verification, resulting in rough, incomplete, and potentially unsafe plans that lack the iterative refinement process used by human experts.

Method: TheraAgent uses an agentic framework with iterative generate-judge-refine pipeline, where TheraJudge serves as a treatment-specific evaluation module integrated into the inference loop to enforce clinical standards, mimicking how human experts revise plans.

Result: Achieves state-of-the-art results on HealthBench (leading in Accuracy and Completeness), 86% win rate against physicians in expert evaluations with superior Targeting and Harm Control, and high agreement between TheraJudge and HealthBench evaluations.

Conclusion: The iterative agentic framework significantly improves treatment plan quality and safety over one-shot LLM generation, demonstrating the value of mimicking human expert reasoning processes with specialized evaluation modules integrated into the inference loop.

Abstract: Formulating a treatment plan is inherently a complex reasoning and refinement task rather than a simple generation problem. However, existing large language models (LLMs) mainly rely on one-shot output without explicit verification, which may result in rough, incomplete, and potentially unsafe treatment plans. To address these limitations, we propose TheraAgent, an agentic framework that replaces one-shot generation with an iterative generate-judge-refine pipeline. By mirroring the actual reasoning process of human experts who iteratively revise treatment plans, our framework progressively transforms coarse and incomplete drafts into precise, comprehensive, and safer therapeutic regimens. To facilitate the critical judge component, we introduce TheraJudge, a treatment-specific evaluation module integrated into the inference loop to enforce clinical standards. Experiments show TheraAgent achieves state-of-the-art results on HealthBench, leading in Accuracy and Completeness. In expert evaluations, it attains an 86% win rate against physicians, with superior Targeting and Harm Control. Moreover, the highly agreement between TheraJudge and HealthBench evaluations confirms the reliability of our framework.

</details>


### [79] [BehaviorGuard: Online Backdoor Defense for Deep Reinforcement Learning](https://arxiv.org/abs/2605.05977)
*Yinbo Yu,Xueyu Yin,Jiadai Wang,Chunwei Tian,Sai Xu,Qi Zhu,Daoqiang Zhang*

Main category: cs.AI

TL;DR: BehaviorGuard is an online behavior-based framework that detects and mitigates backdoor attacks in deep reinforcement learning by analyzing action distribution shifts, without needing to identify specific triggers.


<details>
  <summary>Details</summary>
Motivation: Current backdoor defenses for DRL rely on reward anomalies to reverse-engineer triggers and model fine-tuning, but these approaches are limited by complex trigger patterns and high computational costs, reducing their practical utility.

Method: BehaviorGuard shifts focus to trigger-agnostic backdoor output behaviors by analyzing action distribution shifts. The framework identifies that backdoored policies induce consistent shifts in action distributions, detectable in high-quantile regions and distribution tails. It uses a novel metric to capture behavioral drift in action distributions to identify and suppress backdoor actions at runtime.

Result: Evaluated across diverse benchmarks with different backdoor attacks, BehaviorGuard consistently surpasses prior methods in both efficacy and efficiency. It is the first online backdoor defense that works for both single- and multi-agent DRL.

Conclusion: BehaviorGuard provides a practical, trigger-agnostic defense against backdoor attacks in DRL by focusing on behavioral analysis rather than trigger identification, offering superior performance and efficiency compared to existing methods.

Abstract: Backdoor attacks pose a serious threat to deep reinforcement learning (DRL). Current defenses typically rely on reward anomalies to reverse-engineer triggers and model finetuning to remove backdoors. However, complex trigger patterns undermine their robustness, and fine-tuning entails high costs, limiting practical utility. Therefore, we shift defense concerns to trigger-agnostic backdoor output behaviors and propose BehaviorGuard, an online behavior-based backdoor detection and mitigation framework for DRL. Specifically, we find that regardless of attacks, backdoored policies induce consistent shifts in action distributions to ensure reliable activation, leaving detectable traces in high-quantile regions and distribution tails, even in the absence of triggers. Based on this, we design a novel metric that captures behavioral drift in action distributions to identify and suppress backdoor actions at runtime. To our knowledge, this is the first online backdoor defense that counters attacks both in single- and multi-agent DRL. Evaluated across diverse benchmarks with different backdoor attacks, BehaviorGuard consistently surpasses prior methods in both efficacy and efficiency.

</details>


### [80] [TACT: Mitigating Overthinking and Overacting in Coding Agents via Activation Steering](https://arxiv.org/abs/2605.05980)
*Yuan Sui,Yulin Chen,Yibo Li,Xue Jiang,Yufei He,Yihong Dong,Xiaoxin He,Tianyu Gao,Bryan Hooi*

Main category: cs.AI

TL;DR: TACT (Think-Act Calibration via activation Steering) detects and mitigates agent drift in language model agents by identifying steerable directions in the residual stream to prevent overthinking and overacting failures.


<details>
  <summary>Details</summary>
Motivation: Language model agents degrade over long trajectories when tackling complex software engineering tasks, exhibiting two recurring failure modes: overthinking (repeatedly reasoning over existing information) and overacting (issuing tool calls without integrating recent observations). This degradation is called "agent drift."

Method: The authors label trajectory steps as overthinking, overacting, or calibrated, and find hidden states separate linearly along two "drift axes" pointing from calibrated behavior toward each failure mode. They then project each step's activation onto these axes at test time and pull drifted ones back toward the calibrated region to mitigate drift.

Result: Experiments show TACT outperforms unsteered baselines across SWE-bench Verified, Terminal-Bench 2.0, and CLAW-Eval, lifting average resolve rate by +5.8 percentage points on Qwen3.5-27B and +4.8 pp on Gemma-4-26B-A4B-it while cutting steps-to-resolve by up to 26%.

Conclusion: Agent drift represents a steerable direction in the residual stream, and TACT serves as a viable method for building reliable long-horizon agents by proactively detecting and correcting drift before it surfaces as behavioral failure.

Abstract: When language model agents tackle complex software engineering tasks, they often degrade over long trajectories, which we define as *agent drift*. We focus on two recurring failure modes *overthinking* and *overacting*, i.e., where the agent repeatedly reasons over information it already has, and where it issues tool calls without integrating recent observations or acquiring new evidence. In this paper, we introduce TACT (Think-Act Calibration via activation Steering), to detect and mitigate agent drift in the residual stream before it surfaces as a behavioral failure. In specific, we label trajectory steps as overthinking, overacting, or calibrated, and find that their hidden states can separate linearly along two *drift axes*, pointing from calibrated behavior toward each failure mode (AUC $\approx$ 0.9). To mitigate agent drift, we project each step's activation onto these axes at test time and pull drifted ones back toward the calibrated region. Experiments show that TACT outperforms unsteered baselines across SWE-bench Verified, Terminal-Bench 2.0, and CLAW-Eval, lifting average resolve rate by $+5.8$ pp on Qwen3.5-27B and $+4.8$ pp on Gemma-4-26B-A4B-it while cutting steps-to-resolve by up to $26\%$. These gains frame agent drift as a steerable direction in the residual stream, and position TACT as a viable handle for reliable long-horizon agents.

</details>


### [81] [Strat-LLM: Stratified Strategy Alignment for LLM-based Stock Trading with Real-time Multi-Source Signals](https://arxiv.org/abs/2605.06024)
*Wenliang Huang,Zengyi Yu*

Main category: cs.AI

TL;DR: Strat-LLM proposes a stratified strategy alignment framework for LLM-based trading agents, tested in 2025 live-forward settings to eliminate look-ahead bias, revealing how different model architectures and alignment modes perform across market regimes.


<details>
  <summary>Details</summary>
Motivation: Existing LLM-based trading agent benchmarks often overlook the interplay between architectural reasoning capabilities and strategy consistency, creating gaps in understanding how different model types should be aligned for optimal trading performance.

Method: Stratified Strategy Alignment framework (Strat-LLM) operating in live-forward setting throughout 2025, integrating heterogeneous data including sequential prices, real-time news, and annual reports. Tested on A-share and U.S. markets with extensive stress tests across three alignment modes: Free Mode (reasoning-heavy models), Strict Mode (standard models with external constraints), and Guided Mode (balanced approach).

Result: Four key findings: 1) Reasoning-heavy models excel in Free Mode using internal logic, while standard models need Strict Mode for risk control; 2) Alignment utility is regime-dependent with different modes optimal for uptrending vs downtrending markets; 3) Mid-scale models (35B) perform best under strict constraints, while ultra-large models (122B) suffer under rigid rules but gain in Guided Mode; 4) Standard LLMs fall into high win-rate traps optimizing small gains at expense of total returns.

Conclusion: The optimal alignment strategy for LLM trading agents depends on model architecture, market regime, and model scale. A stratified approach matching reasoning capabilities with appropriate constraints is essential for robust trading performance, with different model types requiring different alignment strategies to avoid pitfalls like high win-rate traps.

Abstract: Large Language Models (LLMs) are evolving into autonomous trading agents, yet existing benchmarks often overlook the interplay between architectural reasoning and strategy consistency. We propose Strat-LLM, a framework grounded in Stratified Strategy Alignment. Operating in a live-forward setting throughout 2025, it integrates heterogeneous data including sequential prices, real-time news, and annual reports to eliminate look-ahead bias. Extensive stress tests on A-share and U.S. markets reveal: (1) reasoning-heavy models achieve peak utility in Free Mode via internal logic, whereas standard models require Strict Mode as a vital risk anchor; (2) alignment utility is regime-dependent, with Free and Guided modes capturing momentum in uptrending markets, while Strict Mode mitigates drawdowns in downtrends; (3) mid-scale models (35B) show optimal fidelity under strict constraints, whereas ultra-large models (122B) suffer an alignment tax under rigid rules but gain a performance premium in Guided Mode; (4) standard LLMs often fall into a high win-rate trap, optimizing for small gains at the expense of total returns, which can only be mitigated through deep reasoning or strict external guardrails. Project details are available at https://Strat-LLM.github.io.

</details>


### [82] [Pathways to AGI](https://arxiv.org/abs/2605.06029)
*Gordon Fletcher,Saomai Vu Khan*

Main category: cs.AI

TL;DR: A critical software studies perspective examining five key questions about AI development pathways, decision points, and alternative trajectories, with emphasis on the conceptual problems of AGI and recommendations for socio-technical development.


<details>
  <summary>Details</summary>
Motivation: The paper adopts a critical software studies perspective to challenge assumptions about AI's inevitability and examine the close linkage between commercial AI development and current social, political, and economic circumstances. It aims to avoid accepting the current AI situation as predetermined and instead critically analyze how we arrived at the present state.

Method: The research employs a critical software studies approach, examining five related questions: (1) critical pathways producing current generative AI tools, (2) leverage nodes and dead ends in decision points, (3) differences across foundational-model trajectories, (4) analysis of alternative projects from key nodes, and (5) development of socio-technical programs. The perspective treats AGI as conceptually problematic.

Result: The paper develops critical and conditional perspectives on AI development, leading to specific directions regarding the future of AI. It analyzes different development pathways (frontier proprietary, open-weight, domain-specific models) and identifies leverage nodes and alternative possibilities. Based on this analysis, the work offers final recommendations for socio-technical development programs.

Conclusion: Adopting a critical perspective enables recommendations for alternative development pathways toward AGI-adjacent capabilities that meet requirements for transparency, moderation, wellbeing, and sustainable business models. The paper emphasizes the need to move beyond current commercial AI trajectories and develop socio-technical approaches that align with broader societal values.

Abstract: Our focus are five related questions that stem from a critical software studies perspective. Underpinning this view is the acknowledged need to avoid assumptions regarding the inevitability of the current situation relating to AI. What we need to see is the closeness of the linkage between current commercial AI development and our prevailing social, political and economic circumstances. This does mean that the perspectives presented here are done so critically and conditionally. Most importantly, Artificial General Intelligence (AGI) is seen as being problematic both conceptually and definitionally. This conditioning of any view regarding AGI does lead the discussion in specific directions and to certain conclusions regarding the future. However, adopting this perspective enables the work to offer some final recommendations. We set out to ask the following questions, 1. What are the critical pathways that produced the current dominant generative AI tools (capabilities, product forms, adoption patterns)? 2. Which decision points acted as leverage nodes (small changes that had large downstream effects), and which dead ends reveal alternative possibilities that did not become dominant? 3. How do pathways differ across three foundational-model trajectories such as the frontier proprietary models, open-weight models or specific domain and sovereign models? 4. Which alternative projects branched from key leverage nodes, what is their current state, and why did some succeed, stall, fail or become absorbed? 5. Based on this analysis, what socio-technical development programmes could plausibly move toward AGI-adjacent capability while meeting requirements for transparency, moderation, wellbeing and sustainable business models?

</details>


### [83] [Novelty-based Tree-of-Thought Search for LLM Reasoning and Planning](https://arxiv.org/abs/2605.06040)
*Leon Hamm,Zlatan Ajanovic*

Main category: cs.AI

TL;DR: The paper introduces a novelty-based pruning method for tree-of-thought reasoning in LLMs, using LLM-prompted novelty estimation to reduce search space and token costs while improving performance.


<details>
  <summary>Details</summary>
Motivation: Current LLM reasoning methods (chain-of-thought, tree-of-thought, reinforcement learning) are brittle, haven't achieved human-level performance in many domains, and suffer from high time and token costs. There's a need to improve tree-of-thought reasoning efficiency.

Method: Proposes a measurable concept of novelty that assesses uniqueness of new nodes (thoughts) compared to previously seen nodes in the search tree. Novelty is estimated by prompting an LLM and leveraging embedded general knowledge from pre-training. This metric is used to prune branches and reduce search scope.

Result: Although the method introduces more prompts per state, overall token cost can be reduced by pruning and reducing overall tree size. The procedure was tested and compared using several benchmarks in language-based planning and general reasoning.

Conclusion: Transferring width-based search concepts from planning to language domains through novelty-based pruning can improve tree-of-thought reasoning efficiency by reducing search space and token costs while maintaining or improving performance.

Abstract: Although advances such as chain-of-thought, tree-of-thought or reinforcement learning have improved the performance of LLMs in reasoning and planning tasks, they are still brittle and have not achieved human-level performance in many domains, and often suffer from high time and token costs. Inspired by the success of width-based search in planning, we explore how the concept of novelty can be transferred to language domains and how it can improve tree-of-thought reasoning. A tree of thoughts relies on building possible "paths" of consecutive ideas or thoughts. These are generated by repeatedly prompting an LLM. In our paper, a measurable concept of novelty is proposed that describes the uniqueness of a new node (thought) in comparison to nodes previously seen in the search tree. Novelty is estimated by prompting an LLM and making use of embedded general knowledge from pre-training. This metric can then be used to prune branches and reduce the scope of the search. Although this method introduces more prompts per state, the overall token cost can be reduced by pruning and reducing the overall tree size. This procedure is tested and compared using several benchmarks in language-based planning and general reasoning.

</details>


### [84] [Visual Fingerprints for LLM Generation Comparison](https://arxiv.org/abs/2605.06054)
*Amal Alnouri,Andreas Hinterreiter,Christina Humer,Furui Cheng,Marc Streit*

Main category: cs.AI

TL;DR: The paper introduces "visual fingerprints" - a method to compare LLM outputs across different generation conditions by modeling responses as linguistic choices and visualizing their distributions, revealing consistent behavioral patterns.


<details>
  <summary>Details</summary>
Motivation: Understanding how different generation conditions (prompts, instructions, parameters, architecture) shape LLM behaviors is essential for prompt design and model evaluation, but challenging due to the stochastic and open-ended nature of text generation.

Method: The approach models LLM responses as collections of linguistic choices (content, expression, structure), extracts these choices using NLP pipelines, represents their distributions across repeated samples, and visualizes them as "visual fingerprints" for direct comparison of condition-specific tendencies.

Result: Through four usage scenarios, the visual fingerprints reveal consistent patterns in LLM behavior that are difficult to observe through individual responses or aggregate metrics, enabling distribution-level comparison of condition-specific tendencies.

Conclusion: Visual fingerprints provide an effective method for understanding how generation conditions shape LLM outputs, offering insights that go beyond traditional individual response analysis and aggregate metrics, with applications in prompt engineering and model evaluation.

Abstract: Large language model (LLM) outputs arise from complex interactions among prompts, system instructions, model parameters, and architecture. We refer to specific configurations of these factors as generation conditions, each of which can bias outputs in various ways. Understanding how different generation conditions shape model behaviors is essential for tasks such as prompt design and model evaluation, yet it remains challenging due to the stochastic and open-ended nature of text generation. We present an approach to visually compare LLM outputs across generation conditions by modeling responses as collections of linguistic choices, including content, expression, and structure. We extract these choices using natural language processing pipelines and represent their distributions across repeated samples. We then visualize these distributions as visual fingerprints, enabling direct, distribution-level comparison of condition-specific tendencies. Through four usage scenarios, we demonstrate how visual fingerprints reveal consistent patterns in LLM behavior that are difficult to observe through individual responses or aggregate metrics.

</details>


### [85] [VibeServe: Can AI Agents Build Bespoke LLM Serving Systems?](https://arxiv.org/abs/2605.06068)
*Keisuke Kamahori,Shihang Li,Simon Peter,Baris Kasikci*

Main category: cs.AI

TL;DR: VibeServe introduces an agentic loop that automatically generates specialized LLM serving systems tailored to specific usage scenarios, moving from generic hand-tuned infrastructure to automatic generation-time specialization.


<details>
  <summary>Details</summary>
Motivation: Traditional LLM serving systems are built as single general-purpose stacks that require extensive manual tuning and optimization. This approach doesn't adapt well to different models, workloads, and hardware scenarios, creating inefficiencies when dealing with non-standard deployment scenarios.

Method: VibeServe uses a multi-agent loop with two components: an outer loop to plan and track system design search, and an inner loop to implement candidate systems, verify correctness, and measure performance on target benchmarks. It automatically synthesizes bespoke serving systems end-to-end.

Result: In standard deployment scenarios, VibeServe remains competitive with highly optimized existing systems like vLLM. More significantly, in six non-standard scenarios involving unusual model architectures, workload patterns, and hardware-specific requirements, VibeServe outperforms existing systems by exploiting optimization opportunities that generic systems miss.

Conclusion: The results suggest a paradigm shift in infrastructure software design: moving from runtime generality to generation-time specialization, where systems are automatically synthesized for specific scenarios rather than manually optimized for broad applicability.

Abstract: For years, we have built LLM serving systems like any other critical infrastructure: a single general-purpose stack, hand-tuned over many engineer-years, meant to support every model and workload. In this paper, we take the opposite bet: a multi-agent loop that automatically synthesizes bespoke serving systems for different usage scenarios. We propose VibeServe, the first agentic loop that generates entire LLM serving stacks end-to-end. VibeServe uses an outer loop to plan and track the search over system designs, and an inner loop to implement candidates, check correctness, and measure performance on the target benchmark. In the standard deployment setting, where existing stacks are highly optimized, VibeServe remains competitive with vLLM, showing that generation-time specialization need not come at the cost of performance. More interestingly, in non-standard scenarios, VibeServe outperforms existing systems by exploiting opportunities that generic systems miss in six scenarios involving non-standard model architectures, workload knowledge, and hardware-specific optimizations. Together, these results suggest a different point in the design space for infrastructure software: generation-time specialization rather than runtime generality. Code is available at https://github.com/uw-syfi/vibe-serve.

</details>


### [86] [Safety Certification is Classification](https://arxiv.org/abs/2605.06087)
*Oliver Schön,Licio Romao,Sadegh Soudjani*

Main category: cs.AI

TL;DR: The paper proposes a kernel embedding framework that directly estimates T-step safety probabilities from trajectory data without recursive dynamic programming, avoiding compounding errors and working with non-Markovian systems.


<details>
  <summary>Details</summary>
Motivation: Existing approaches for safety certification of dynamical systems use recursive dynamic programming (DP) on estimated transition probabilities, which leads to compounding errors that collapse safety probability bounds to vacuous values for long horizons. These methods also require Markovian assumptions.

Method: The authors propose a kernel embedding framework that treats safety certification as a classification problem on trajectory data. Instead of recursive DP, they directly estimate T-step safety probabilities, bypassing the recursion step entirely.

Result: The framework subsumes existing approaches (barrier certificates, robust Markov models) as special cases, goes beyond their limitations, avoids compounding errors, works with non-Markovian dynamics, and remains stable independent of certification horizon. Simulation on a neural-controlled quadrotor confirms DP-based certificates become unsound while direct estimators remain reliable.

Conclusion: Direct safety certification via kernel embedding avoids compounding errors inherent in DP-based approaches, enables certification for non-Markovian systems, and provides stable safety guarantees independent of horizon length.

Abstract: The goal of this paper is certifying safety of dynamical systems subject to uncertainty. Existing approaches use trajectory data to estimate transition probabilities, and compute safety probabilities recursively via dynamic programming (DP). This recursion may lead to compounding errors in the certified safety probability, thus collapsing to a vacuous lower bound for growing horizons $T$. We propose a kernel embedding framework that treats safety certification as a classification problem on trajectory data, directly estimating the $T$-step safety probability without recursion. We show that the framework subsumes well-established approaches from the literature (e.g., barrier certificates, robust Markov models) as special cases, and allows us to go beyond their limitations. As the main consequence, it bypasses compounding error across the horizon and enables certification for systems with non-Markovian dynamics. We demonstrate that direct estimators remain stable independent of the certification horizon and in the non-Markovian setting, whilst DP-based certificates silently go unsound -- confirmed in simulation on a neural-controlled quadrotor.

</details>


### [87] [Shallow Prefill, Deep Decoding: Efficient Long-Context Inference via Layer-Asymmetric KV Visibility](https://arxiv.org/abs/2605.06105)
*Jungsuk Oh,Hyeseo Jeon,Hyunjune Ji,Kyongmin Kong,Jay-Yoon Lee*

Main category: cs.AI

TL;DR: SPEED is a novel KV-cache optimization method that processes long prompts only in lower layers during prefill, reducing computational cost and memory while maintaining quality.


<details>
  <summary>Details</summary>
Motivation: Long-context inference in decoder-only LLMs is expensive because long prompts are fully processed during prefill, cached at every layer, and repeatedly attended to during decode, leading to high computational cost and memory usage.

Method: SPEED (Shallow Prefill, dEEp Decode) uses a phase-asymmetric KV-visibility policy that materializes non-anchor prompt-token KV states only in lower layers while keeping decode-phase tokens full-depth. It removes prefill tokens from upper-layer decode visibility altogether, using only a minimal beginning-of-sequence (BoS) anchor.

Result: In Llama-3.1-8B instruction-tuning study, SPEED using only 75% of layers for prefill tokens achieved 51.2 average score on OLMES-style benchmarks vs 51.4 for full-depth baseline, while improving TTFT by 33%, TPOT by 22%, and reducing active KV memory by 25.0% at 128K context. Layer-wise diagnostics show this cutoff retains main prompt-selection and representation-stabilization regions.

Conclusion: Long-context prompt tokens don't need to persist as full-depth KV-cache objects when decode-phase tokens remain full-depth. SPEED offers significant efficiency gains with minimal quality loss by leveraging the insight that prompt processing can be shallower than generation.

Abstract: Long-context inference in decoder-only language models is costly because long prompts are processed during Prefill, cached at every layer, and repeatedly attended to during autoregressive Decode. We introduce \emph{Shallow Prefill, dEEp Decode} (SPEED), a phase-asymmetric KV-visibility policy that materializes non-anchor prompt-token KV states only in lower layers while keeping Decode-phase tokens full-depth. Unlike previous approaches that make upper-layer prompt KV states cheaper to store or construct, SPEED removes prefill tokens from the upper-layer Decode visibility set altogether. With a minimal BoS anchor, this simple change preserves broad benchmark quality while reducing long-context cost. In a controlled Llama-3.1-8B instruction-tuning study, SPEED using only 75\% of layers for prefill tokens reaches 51.2 average score on OLMES-style benchmarks, compared with 51.4 for the full-depth baseline, while improving TTFT by 33\%, TPOT by 22\%, and reducing active KV memory by 25.0\% at 128K context. Layer-wise diagnostics suggest that this cutoff retains the main prompt-selection and representation-stabilization regions of the full-depth model. These results show that long-context prompt tokens need not always persist as full-depth KV-cache objects when Decode-phase tokens remain full-depth.

</details>


### [88] [On Time, Within Budget: Constraint-Driven Online Resource Allocation for Agentic Workflows](https://arxiv.org/abs/2605.06110)
*Xinglin Wang,Zishen Liu,Shaoxiong Feng,Peiwen Yuan,Yiwei Li,Jiayi Shi,Yueqi Zhang,Chuyi Tan,Ji Zhang,Boyuan Pan,Yao Hu,Kan Li*

Main category: cs.AI

TL;DR: MCPP is a Monte Carlo planning method that maximizes the probability of completing agentic workflows within given budget and deadline constraints by simulating executions and dynamically allocating resources.


<details>
  <summary>Details</summary>
Motivation: Real-world agentic system deployments often have concrete budget and deadline requirements, shifting the optimization goal from average efficiency to maximizing the probability of successful completion under explicit constraints.

Method: Monte Carlo Portfolio Planning (MCPP) - a lightweight closed-loop planner that formulates the problem as finite-horizon stochastic online allocation, estimates constrained completion probability through simulated workflow executions, and replans after observed outcomes.

Result: Experiments on CodeFlow and ProofFlow show that MCPP consistently improves constrained completion probability over strong baselines across a wide range of budget-deadline constraints.

Conclusion: The proposed MCPP approach effectively addresses the constraint-driven resource allocation problem for agentic workflows, providing practical improvements for real-world deployments with explicit budget and deadline requirements.

Abstract: Agentic systems increasingly solve complex user requests by executing orchestrated workflows, where subtasks are assigned to specialized models or tools and coordinated according to their dependencies. While recent work improves agent efficiency by optimizing the performance--cost--latency frontier, real deployments often impose concrete requirements: a workflow must be completed within a specified budget and before a specified deadline. This shifts the goal from average efficiency optimization to maximizing the probability that the entire workflow completes successfully under explicit budget and deadline constraints. We study \emph{constraint-driven online resource allocation for agentic workflows}. Given a dependency-structured workflow and estimates of success rates and generation lengths for each subtask--model pair, the executor allocates models and parallel samples across simultaneously executable subtasks while managing the remaining budget and time. We formulate this setting as a finite-horizon stochastic online allocation problem and propose \emph{Monte Carlo Portfolio Planning} (MCPP), a lightweight closed-loop planner that directly estimates constrained completion probability through simulated workflow executions and replans after observed outcomes. Experiments on CodeFlow and ProofFlow demonstrate that MCPP consistently improves constrained completion probability over strong baselines across a wide range of budget--deadline constraints.

</details>


### [89] [CrossCult-KIBench: A Benchmark for Cross-Cultural Knowledge Insertion in MLLMs](https://arxiv.org/abs/2605.06115)
*Zhen Zeng,Leijiang Gu,Feng Li,Jing Yu,Zenglin Shi*

Main category: cs.AI

TL;DR: A method for making multimodal LLMs culturally aware by inserting cross-cultural knowledge while preserving behavior in other cultures, with a new benchmark and baseline approach.


<details>
  <summary>Details</summary>
Motivation: Multimodal LLMs trained on English-centric data often generate culturally inappropriate responses in cross-cultural settings, creating a need for culturally-aware adaptation.

Method: Introduce cross-cultural knowledge insertion task, propose MCKI (Memory-Conditioned Knowledge Insertion) baseline that retrieves cultural knowledge from external memory using frozen MLLM representations, and prepends matched entries as conditional prompts.

Result: Current approaches struggle to balance effective cultural adaptation with behavioral preservation in other cultures, highlighting a key challenge in developing culturally-aware MLLMs.

Conclusion: The work underscores the importance of developing more culturally adaptive and responsible MLLMs and provides a foundation for future research in this direction.

Abstract: Multimodal Large Language Models (MLLMs), trained primarily on English-centric data, frequently generate culturally inappropriate or misaligned responses in cross-cultural settings. To mitigate this, we introduce the task of cross-cultural knowledge insertion, which focuses on adapting models to specific cultural contexts while preserving their original behavior in other cultures. To facilitate research in this area, we introduce CrossCult-KIBench, a comprehensive evaluation benchmark for assessing both the effectiveness of knowledge insertion and its unintended side effects on non-target cultures. The benchmark includes 9,800 image-grounded cases covering 49 culturally relevant visual scenarios across English, Chinese, and Arabic language-culture groups. It supports evaluation in both single-insert and sequential-insert settings. We also propose Memory-Conditioned Knowledge Insertion (MCKI) as a baseline method. MCKI retrieves relevant cultural knowledge from an external memory using frozen MLLM representations, prepending matched entries as conditional prompts when applicable. Extensive experiments on CrossCult-KIBench reveal that current approaches struggle to balance effective cultural adaptation with behavioral preservation, highlighting a key challenge in developing culturally-aware MLLMs. Our work thus underscores an important research direction for developing more culturally adaptive and responsible MLLMs.

</details>


### [90] [Policy-Guided Stepwise Model Routing for Cost-Effective Reasoning](https://arxiv.org/abs/2605.06116)
*Wenwen Si,Insup Lee,Osbert Bastani*

Main category: cs.AI

TL;DR: Small RL-trained control policy for stepwise model routing improves accuracy-cost tradeoff in LLM reasoning tasks without needing large process reward models.


<details>
  <summary>Details</summary>
Motivation: Inference-time computation like chain-of-thought improves LLM reasoning but incurs high costs. Existing routing solutions are limited - handcrafted strategies underperform, while process reward models require large training that may be infeasible.

Method: Formulate stepwise model routing as constrained decision-making problem, train small control policy using reinforcement learning, and apply threshold calibration to tune performance-efficiency tradeoff.

Result: Validated on three math benchmarks (GSM8K, MATH500, OmniMath) with open and closed models. Method consistently improves accuracy-cost tradeoff over handcrafted approaches, and achieves comparable tradeoff to methods requiring large process reward models.

Conclusion: RL-based control policy provides an effective solution for efficient model routing in reasoning tasks, balancing performance and cost without requiring extensive training resources.

Abstract: Inference-time computation has greatly enhanced the performance of large language models (LLMs) on challenging reasoning tasks, but this strategy can incur high inference costs. One solution is to route intermediate chain-of-thought (CoT) states to language models of different sizes; however, existing approaches rely on handcrafted routing strategies that limit performance, or on training large process reward models that may be infeasible in many applications. We formulate stepwise model routing as a constrained decision-making problem, which we solve by training a small control policy using reinforcement learning in conjunction with threshold calibration to tune the performance-efficiency tradeoff. We validate our method on three math benchmarks (GSM8K, MATH500, and OmniMath) on both open and closed models. Our method consistently improves the accuracy-cost tradeoff compared to handcrafted approaches, while achieving a comparable tradeoff to methods that require training large process reward models.

</details>


### [91] [Back to the Beginning of Heuristic Design: Bridging Code and Knowledge with LLMs](https://arxiv.org/abs/2605.06123)
*Nguyen Viet Tuan Kiet,Bui Dinh Pham,Dao Van Tung,Tran Cong Dao,Huynh Thi Thanh Binh*

Main category: cs.AI

TL;DR: The paper introduces a top-down "knowledge-first" paradigm for automatic heuristic design with LLMs, treating knowledge as the primary search object rather than code, improving efficiency and generalization across combinatorial optimization tasks.


<details>
  <summary>Details</summary>
Motivation: Existing bottom-up approaches in automatic heuristic design (AHD) search over executable programs and distill insights from execution feedback, moving from low-level implementations to high-level principles. The authors argue this view is incomplete and propose a complementary top-down perspective where knowledge becomes the primary search object and code merely instantiates and tests it, making what is learned explicit and reusable.

Method: The authors formalize the shift through a statistical-learning view that exposes a distortion-compression trade-off, and instantiate it in both population-based and tree-based AHD frameworks. They treat knowledge as the primary search object rather than code, allowing for explicit knowledge representation and reuse across problems and search trajectories.

Result: Across combinatorial optimization and tasks beyond it, knowledge-first search improves discovery efficiency, transfer, and generalization, often outperforming code-centric pipelines. Combining both bottom-up and top-down strategies yields further gains.

Conclusion: Progress in automatic heuristic design depends on iteratively constructing and evolving interpretable hypotheses that retain value beyond a single search trajectory. The knowledge-first approach makes learned principles explicit and reusable, offering complementary benefits to traditional code-centric methods.

Abstract: Large language models (LLMs) have recently advanced automatic heuristic design (AHD) for combinatorial optimization (CO), where candidate heuristics are iteratively proposed, evaluated, and refined. Most existing approaches search over executable programs and distill insights from execution feedback to guide later iterations. Because this process moves from low-level implementations to high-level principles, we refer to it as a bottom-up paradigm. We argue that this view is incomplete and introduce a complementary top-down perspective: knowledge becomes the primary search object and code merely instantiates and tests it, making what is learned explicit and reusable across problems and trajectories. We formalize this shift through a statistical-learning view that exposes a distortion--compression trade-off, and instantiate it in both population-based and tree-based AHD frameworks. Across CO and tasks beyond it, knowledge-first search improves discovery efficiency, transfer, and generalization, often outperforming code-centric pipelines, while combining both strategies yields further gains. Our results suggest that progress in AHD depends on iteratively constructing and evolving interpretable hypotheses that retain value beyond a single search trajectory.

</details>


### [92] [P-Guide: Parameter-Efficient Prior Steering for Single-Pass CFG Inference](https://arxiv.org/abs/2605.06124)
*Xin Peng,Ang Gao*

Main category: cs.AI

TL;DR: P-Guide enables efficient classifier-free guidance for flow matching by modulating only the initial latent state with a single inference pass, achieving ~50% latency reduction while maintaining competitive quality.


<details>
  <summary>Details</summary>
Motivation: Classifier-Free Guidance (CFG) imposes significant computational overhead by requiring dual forward passes at each sampling step for high-fidelity conditional generation in flow matching.

Method: P-Guide modulates only the initial latent state to steer generation from the prior space, eliminating explicit velocity field extrapolation during sampling. It considers both homoscedastic and heteroscedastic priors, with joint modeling of mean and variance for adaptive loss attenuation.

Result: P-Guide reduces inference latency by approximately 50% while maintaining fidelity and prompt alignment competitive with standard dual-pass CFG baselines.

Conclusion: P-Guide provides an efficient alternative to CFG for flow matching that maintains quality while significantly reducing computational overhead through single-pass inference and initial latent state modulation.

Abstract: Classifier-Free Guidance (CFG) is essential for high-fidelity conditional generation in flow matching, yet it imposes significant computational overhead by requiring dual forward passes at each sampling step. In this work, we address this bottleneck by introducing \textbf{P-Guide}, a framework that achieves high-quality guidance through a single inference pass by modulating only the initial latent state. We further show that, under a first-order approximation, P-Guide is equivalent to CFG in the sense that it steers generation from the prior space, without requiring explicit velocity field extrapolation during sampling. We consider both homoscedastic and \textbf{heteroscedastic} priors, and find that jointly modeling the mean and variance enables adaptive loss attenuation and improved robustness to data uncertainty. Extensive experiments demonstrate that P-Guide reduces inference latency by approximately 50\% while maintaining fidelity and prompt alignment competitive with standard dual-pass CFG baselines.

</details>


### [93] [Skill1: Unified Evolution of Skill-Augmented Agents via Reinforcement Learning](https://arxiv.org/abs/2605.06130)
*Yaorui Shi,Yuxin Chen,Zhengxi Lu,Yuchun Miao,Shugui Liu,Qi GU,Xunliang Cai,Xiang Wang,An Zhang*

Main category: cs.AI

TL;DR: Skill1 is a framework that trains a single policy to co-evolve skill selection, utilization, and distillation using a shared task-outcome objective, outperforming prior methods on ALFWorld and WebShop benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing methods for maintaining persistent skill libraries optimize skill selection, utilization, and distillation in isolation or with separate reward sources, leading to partial and conflicting evolution rather than coordinated improvement.

Method: Skill1 trains a single policy that performs four key operations: generates a query to search the skill library, re-ranks candidates to select a skill, solves tasks conditioned on the selected skill, and distills new skills from the trajectory. All learning derives from a single task-outcome signal, with low-frequency trends crediting selection and high-frequency variation crediting distillation.

Result: Skill1 outperforms prior skill-based and reinforcement learning baselines on ALFWorld and WebShop benchmarks. Training dynamics confirm the co-evolution of the three capabilities, and ablation studies show that removing any credit signal degrades evolution.

Conclusion: The Skill1 framework successfully demonstrates that coordinated co-evolution of skill selection, utilization, and distillation through a unified learning approach leads to superior performance compared to isolated optimization methods, with the integrated credit assignment mechanism being essential for effective skill library maintenance.

Abstract: A persistent skill library allows language model agents to reuse successful strategies across tasks. Maintaining such a library requires three coupled capabilities. The agent selects a relevant skill, utilizes it during execution, and distills new skills from experience. Existing methods optimize these capabilities in isolation or with separate reward sources, resulting in partial and conflicting evolution. We propose Skill1, a framework that trains a single policy to co-evolve skill selection, utilization, and distillation toward a shared task-outcome objective. The policy generates a query to search the skill library, re-ranks candidates to select one, solves the task conditioned on it, and distills a new skill from the trajectory. All learning derives from a single task-outcome signal. Its low-frequency trend credits selection and its high-frequency variation credits distillation. Experiments on ALFWorld and WebShop show that Skill1 outperforms prior skill-based and reinforcement learning baselines. Training dynamics confirm the co-evolution of the three capabilities, and ablations show that removing any credit signal degrades the evolution.

</details>


### [94] [Graphlets as Building Blocks for Structural Vocabulary in Knowledge Graph Foundation Models](https://arxiv.org/abs/2605.06154)
*Kossi Amouzouvi,Robert Wardenga,Jens Lehmann,Sahar Vahdati*

Main category: cs.AI

TL;DR: A framework for Knowledge Graph Foundation Models that uses graphlets as structural tokens to achieve transferable representations across heterogeneous knowledge graphs, enabling better zero-shot link prediction.


<details>
  <summary>Details</summary>
Motivation: Knowledge Graphs lack the geometric regularity of language and vision tasks - while they have discrete symbols (entities/relations), they lack a common fixed grid and form irregular, non-Euclidean topologies. This makes transfer learning difficult for KGFMs since they can't rely on universal tokens like in NLP/vision.

Method: Treat graphlets (small connected graphs) as structural tokens that recur across KGs. Develop a model-agnostic framework with a vocabulary of graphlets (closed/open 2-/3-paths and star graphlets) mined via pattern matching between relations. This captures robust structural invariances.

Result: Evaluation on 51 KGs across various domains shows that adding simple graphlets to vocabulary yields models outperforming prior KGFMs in zero-shot inductive and transductive link prediction tasks.

Conclusion: Graphlets serve as effective structural tokens for KGFMs, bridging the gap caused by KG heterogeneity and enabling better transfer learning across diverse knowledge graphs without requiring a universal token set.

Abstract: Foundation models excel at language, where sentences become tokens, and vision, where images become pixels, because both reduce to discrete symbols on a shared, fixed grid. Knowledge Graphs share the discreteness, but not the geometry. Their entities and relations are discrete symbols, yet their arrangement is relational and lacks a common, fixed grid. Knowledge Graphs (KGs) share the discreteness, but not the geometry. They form irregular, non-Euclidean topologies whose local neighborhoods differ from graph to graph. Therefore, Knowledge Graph Foundation Models (KGFMs) rely on identifying structural invariances to produce transferable representations. Without a universal token set, KGFMs are limited in their ability to transfer representations across unseen KGs. We close this gap by treating graphlets, small connected graphs, as structural tokens that recur in heterogeneous KGs. In this paper, We introduce a model-agnostic framework based on a vocabulary of graphlets that mines a KG between relations via pattern matching. In particular, we considered closed and open 2- and 3-path, and star graphlets, to obtain robust invariances. The framework is evaluated on 51 KGs from a wide range of domains, for zero-shot inductive and transductive link prediction. Experiments show that adding simple graphlets to the vocabulary yields models that outperform prior KGFMs.

</details>


### [95] [Beyond Accuracy: Policy Invariance as a Reliability Test for LLM Safety Judges](https://arxiv.org/abs/2605.06161)
*Shihao Weng,Yang Feng,Xiaofei Xie*

Main category: cs.AI

TL;DR: Current LLM-as-a-Judge safety evaluation systems lack robustness to policy wording changes, with up to 9.1% verdict flips from content-preserving rewrites. The paper introduces policy invariance principles and testing protocol to measure judge reliability.


<details>
  <summary>Details</summary>
Motivation: Existing agent safety benchmarks treat LLM judge verdicts as ground truth without verifying whether verdicts depend on actual agent behavior or just on how evaluation policies are worded. There's a need to ensure trustworthy safety judges that are invariant to policy wording changes.

Method: The authors operationalize policy invariance as three testable principles: rubric-semantics invariance under certified-equivalent rewrites, rubric-threshold invariance under strict-to-lenient shifts, and ambiguity-aware calibration. They instantiate these principles as a stress-test protocol tested on four agent-class judges using trajectories from ASSEBench and R-Judge.

Result: Current judges respond to meaningful normative shifts and meaningless structural rewrites with comparable strength, unable to distinguish between them. Content-preserving policy rewrites flip up to 9.1% of verdicts above baseline jitter, with 18-43% of flips occurring on unambiguous cases. The study reveals an order-of-magnitude spread in judge reliability invisible to accuracy-only metrics.

Conclusion: Existing safety scores conflate agent behavior with evaluator prompting. The paper contributes the Policy Invariance Score and Judge Card reporting protocol to audit evaluator reliability. Future benchmarks should audit their evaluators rather than trusting them by default.

Abstract: LLM-as-a-Judge pipelines have become the de facto evaluator for agent safety, yet existing benchmarks treat their verdicts as ground-truth proxies without checking whether the verdicts depend on the agent's behavior or merely on how the evaluation policy happens to be worded. We argue that any trustworthy safety judge must satisfy a basic property we call policy invariance, and we operationalize it as three testable principles: rubric-semantics invariance under certified-equivalent rewrites, rubric-threshold invariance under intentional strict-to-lenient shifts, and ambiguity-aware calibration so that verdict instability concentrates on genuinely ambiguous cases. Instantiating these principles as a stress-test protocol with four agent-class judges on trajectories drawn from ASSEBench and R-Judge, we surface a previously unmeasured failure mode: today's judges respond to meaningful normative shifts and to meaningless structural rewrites with comparable strength, and cannot tell the two apart. Content-preserving policy rewrites flip up to 9.1% of verdicts above baseline jitter, and 18-43% of all observed flips occur on unambiguous cases under such rewrites, so existing safety scores conflate what the agent did with how the evaluator was prompted. Beyond the diagnosis, we contribute the Policy Invariance Score and the Judge Card reporting protocol, which expose an order-of-magnitude spread in judge reliability that is invisible to accuracy-only leaderboards. We release the protocol and code so that future agent-safety benchmarks can audit their own evaluators rather than trust them by default.

</details>


### [96] [Post Reasoning: Improving the Performance of Non-Thinking Models at No Cost](https://arxiv.org/abs/2605.06165)
*Richmond Sin Jing Xuan,Rishabh Bhardwaj,Soujanya Poria*

Main category: cs.AI

TL;DR: Post-Reasoning improves LLM performance by generating answers first and reasoning afterward, eliminating extra latency and cost while enhancing accuracy through simple instruction augmentation.


<details>
  <summary>Details</summary>
Motivation: Current LLMs generate intermediate reasoning traces before answers, which increases inference latency and operational costs. Many real-world tasks don't need explicit reasoning, and sometimes additional reasoning even degrades performance. There's a need to reduce token consumption while maintaining or improving performance.

Method: Post-Reasoning conditions instruction-tuned models to justify their answers AFTER generating the final response. This allows answers to be obtained without additional latency or token cost. The approach includes simple instruction augmentation and also proposes supervised post-reason tuning to internalize this behavior through training.

Result: Evaluated across 117 model-benchmark settings (13 models, 4 families, 9 benchmarks). Post-Reasoning improved performance in 88.19% of settings with 17.37% mean relative improvement. Supervised post-reason tuning improved 91.11% of settings, exceeding prompt-based baseline by 8.01% average improvement.

Conclusion: Post-Reasoning establishes a new performance ceiling for direct-answer capabilities by decoupling reasoning from answer generation, reducing latency and costs while improving performance. It's a simple yet effective approach that can be implemented through both prompting and supervised tuning.

Abstract: As the widespread adoption of Large Language Models (LLMs) accelerates, token consumption from intermediate reasoning traces increasingly contributes to inference latency and operational cost. Recent studies suggest that many real-world tasks require little to no explicit reasoning, with additional reasoning sometimes even degrading performance. In this work, we propose \textbf{Post-Reasoning}, a simple yet effective approach that improves instruction-tuned models by conditioning them to justify their answers after generating the final response. By design, it enables the final answer to be obtained without additional latency or token cost, while still improving performance through simple instruction augmentation. We evaluate Post-Reasoning across \(117\) model--benchmark settings spanning \(13\) open and proprietary models, \(4\) model families, and \(9\) diverse reasoning and knowledge-intensive benchmarks, including AMC, HMMT, GSM8K, GPQA, MMLU-Pro, and BIG-Bench Hard. Post-Reasoning improves performance in over \(88.19\%\) of evaluated settings, achieving a mean relative improvements of \(17.37\%\). Furthermore, we propose supervised post-reason tuning, which further improves performance in over \(91.11\%\) of evaluated settings, and exceeds the prompt-based post-reasoning baseline by an average of \(8.01\%\), demonstrating that post-reasoning can be effectively internalized through training. Ultimately, Post-Reasoning establishes a new performance ceiling for direct-answer capabilities.

</details>


### [97] [BioMedArena: An Open-source Toolkit for Building and Evaluating Biomedical Deep Research Agents](https://arxiv.org/abs/2605.06177)
*Jinge Wu,Hongjian Zhou,Mingde Zeng,Jiayuan Zhu,Junde Wu,Jiazhen Pan,Sean Wu,Honghan Wu,Fenglin Liu,David A. Clifton*

Main category: cs.AI

TL;DR: BioMedArena is an open-source toolkit that standardizes evaluation of biomedical deep research agents, eliminating per-paper engineering inconsistencies and enabling fair model comparisons with state-of-the-art results.


<details>
  <summary>Details</summary>
Motivation: Current deep research agent evaluation suffers from inconsistent "glue code" implementations where the same backbone model reports different accuracies in different papers due to varying tool registries, harnesses, and evaluation surfaces. This creates a "per-paper engineering tax" where integrating new models requires weeks of model-specific engineering work.

Method: BioMedArena decouples six layers of biomedical agent evaluation: benchmark loading, tool exposure, tool selection, execution mode, context management, and scoring. It exposes 147 biomedical benchmarks and 75 tools across 9 functional families. The system uses provider adapters that allow adding new models, benchmarks, or tools with just a few lines of code, plus 6 agent harnesses with 6 context-management strategies.

Result: The toolkit enables 12 backbones to achieve competitive research capabilities with significantly improved performance, obtaining state-of-the-art results on 8 representative biomedical benchmarks with an average improvement of +15.03 percentage points over previous SOTA methods.

Conclusion: BioMedArena provides a standardized framework that eliminates evaluation inconsistencies in biomedical agent research, enables fair comparisons across models, and dramatically reduces integration overhead while delivering superior performance on key benchmarks.

Abstract: Building a deep research agent today is an exercise in glue code: the same backbone evaluated on the same benchmark can report different accuracies in different papers because harness and tool registry all differ, and integrating a new foundation model into a comparable evaluation surface costs weeks of model-specific engineering. We call this the per-paper engineering tax and release BioMedArena, an open-source toolkit that not only alleviates it but also provides an arena for fair comparison of different foundation models when evaluating them as deep-research agents. BioMedArena decouples six layers of biomedical agent evaluation -- benchmark loading, tool exposure, tool selection, execution mode, context management, and scoring -- and exposes 147 biomedical benchmarks and 75 biomedical tools across 9 functional families. Adding a new model, benchmark, or tool reduces to registering a few-line provider adapter. We further provide 6 agent harnesses with 6 context-management strategies, which provide 12 backbones with competitive research capabilities and significantly improved performance, achieving state-of-the-art (SOTA) results on 8 representative biomedical benchmarks, with an average lift of +15.03 percentage points over prior SOTA. The toolkit, configurations, and per-task traces are available at https://github.com/AI-in-Health/BioMedArena

</details>


### [98] [Rethinking Adapter Placement: A Dominant Adaptation Module Perspective](https://arxiv.org/abs/2605.06183)
*Suoxin Zhang,Run He,Di Fang,Xiang Tan,Kaixuan Chen,Huiping Zhuang*

Main category: cs.AI

TL;DR: DomLoRA places a single LoRA adapter at a dominant shallow FFN down-projection module, achieving superior performance with only ~0.7% of vanilla LoRA's parameters across multiple tasks.


<details>
  <summary>Details</summary>
Motivation: While recent studies show fewer LoRA adapters can maintain or improve performance, existing methods still distribute adapters broadly, leaving optimal placement of limited adapters as an open question. The paper aims to determine where to place a limited number of adapters to maximize performance.

Method: 1. Introduces PAGE (Projected Adapter Gradient Energy) - a gradient-based sensitivity probe that estimates initial trainable gradient energy for each candidate LoRA adapter. 
2. Discovers that PAGE is highly concentrated on a single shallow FFN down-projection across multiple models and tasks.
3. Identifies this as the "dominant adaptation module" with layer index that is architecture-dependent but task-stable.
4. Proposes DomLoRA - a placement method that places only a single adapter at this dominant adaptation module.

Result: DomLoRA with only ~0.7% of vanilla LoRA's trainable parameters outperforms vanilla LoRA on average across various downstream tasks including instruction following, mathematical reasoning, code generation, and multi-turn conversation. The method also improves other LoRA variants.

Conclusion: The dominant adaptation module perspective provides a practical placement guideline for LoRA adapters, showing that optimal performance can be achieved with minimal parameter overhead by focusing adaptation on a single key module rather than distributing adapters broadly.

Abstract: Low-rank adaptation (LoRA) is a widely used parameter-efficient fine-tuning method that places trainable low-rank adapters into frozen pre-trained models. Recent studies show that using fewer LoRA adapters may still maintain or even improve performance, but existing methods still distribute adapters broadly, leaving where to place a limited number of adapters to maximize performance largely open. To investigate this, we introduce PAGE (Projected Adapter Gradient Energy), a gradient-based sensitivity probe that estimates the initial trainable gradient energy available to each candidate LoRA adapter. Surprisingly, we find that PAGE is highly concentrated on a single shallow FFN down-projection across two model families and four downstream tasks. We term this module the dominant adaptation module and show that its layer index is architecture-dependent but task-stable. Motivated by this finding, we propose DomLoRA, a placement method that places a single adapter at the dominant adaptation module. With only ~0.7% of vanilla LoRA's trainable parameters, DomLoRA outperforms it on average across various downstream tasks, including instruction following, mathematical reasoning, code generation, and multi-turn conversation. This method also improves other LoRA variants, supporting the dominant adaptation module perspective as a practical placement guideline.

</details>


### [99] [Event-Causal RAG: A Retrieval-Augmented Generation Framework for Long Video Reasoning in Complex Scenarios](https://arxiv.org/abs/2605.06185)
*Peizheng Yan,Yu Zhao,Liang Xie,Juntong Qi,Mingming Wang,Erwei Yin*

Main category: cs.AI

TL;DR: Event-Causal RAG: A lightweight retrieval-augmented framework for infinite video reasoning that segments videos into coherent events as State-Event-State graphs, enabling efficient causal-temporal reasoning across extended durations.


<details>
  <summary>Details</summary>
Motivation: Current vision-language models struggle with ultra-long video reasoning due to O(n²) complexity of self-attention and fragmented clip-level memory in existing RAG approaches, which fail to model temporal and causal structure effectively across extended time periods.

Method: Segments streaming videos into semantically coherent events, representing each as structured State-Event-State (SES) graphs capturing event states and transitions. These merge into a global Event Knowledge Graph stored in dual-store memory supporting semantic matching and causal-topological retrieval. Uses bidirectional retrieval to identify relevant event causal chains for video foundation models.

Result: Outperforms clip-based retrieval baselines and long-context video models on long-video benchmarks, especially on questions requiring multi-event integration and causal inference across temporal gaps, while achieving improved memory efficiency and robust streaming performance.

Conclusion: Event-Causal RAG provides an effective framework for infinite video reasoning by addressing key limitations of existing approaches through structured event representation, dual-store memory, and bidirectional retrieval that preserves temporal and causal relationships across extended video sequences.

Abstract: Recent large vision-language models have achieved strong performance on short- and medium-length video understanding, yet they remain inadequate for ultra-long or even infinite video reasoning, where models must preserve coherent memory over extended durations and infer causal dependencies across temporally distant events. Existing end-to-end video understanding methods are fundamentally limited by the $O(n^2)$ complexity of self-attention, while recent retrieval-augmented generation (RAG) approaches still suffer from fragmented clip-level memory, weak modeling of temporal and causal structure, and high storage and online inference costs. We present Event-Causal RAG, a lightweight retrieval-augmented framework for infinite long-video reasoning. Instead of indexing fixed-length clips, our method segments streaming videos into semantically coherent events and represents each event as a structured State-Event-State (SES) graph, capturing the event together with its surrounding state transitions. These graphs are merged into a global Event Knowledge Graph and stored in a dual-store memory that supports both semantic matching and causal-topological retrieval. On top of this memory, we design a bidirectional retrieval strategy to efficiently identify the most relevant event causal chains and provide them, together with the associated video evidence, to a backbone video foundation model for answer generation. Experiments on long-video understanding benchmarks demonstrate that Event-Causal RAG consistently outperforms strong clip-based retrieval baselines and long-context video models, particularly on questions requiring multi-event integration and causal inference across long temporal gaps, while also achieving improved memory efficiency and robust streaming performance.

</details>


### [100] [OPSD Compresses What RLVR Teaches: A Post-RL Compaction Stage for Reasoning Models](https://arxiv.org/abs/2605.06188)
*Jaehoon Kim,Dongha Lee*

Main category: cs.AI

TL;DR: OPSD works as a compression mechanism in thinking-enabled math reasoning, shortening correct responses but not improving accuracy; SFT→RLVR→OPSD pipeline is proposed.


<details>
  <summary>Details</summary>
Motivation: OPSD has shown promise in RLVR settings but doesn't work well for thinking-enabled mathematical reasoning - accuracy gains shrink or become negative, suggesting OPSD may not be effectively correcting errors in long reasoning traces.

Method: Applied OPSD separately to correct and incorrect rollout groups to isolate compression and correction effects. Analyzed OPSD behavior specifically in thinking-enabled mathematical reasoning tasks.

Result: OPSD behaves as a compression mechanism rather than correction mechanism: training on correct rollouts preserves accuracy while shortening responses; training on incorrect rollouts damages accuracy.

Conclusion: Proposes revised pipeline for thinking-enabled mathematical reasoning: Supervised Fine-Tuning (SFT), then Reinforcement Learning with Verifiable Rewards (RLVR), then On-Policy Self-Distillation (OPSD) as a final compression step.

Abstract: On-Policy Self-Distillation (OPSD) has recently emerged as an alternative to Reinforcement Learning with Verifiable Rewards (RLVR), promising higher accuracy and shorter responses through token-level credit assignment from a self-teacher conditioned on privileged context. However, this promise does not carry over to thinking-enabled mathematical reasoning, where reported accuracy gains shrink and sometimes turn negative. We hypothesize that hindsight supervision can specify better token-level alternatives in short thinking-disabled outputs, but in long thinking-enabled traces it more readily identifies redundancy than supplies better replacements. To test this, we applied OPSD separately to correct and incorrect rollout groups, so that compression and correction can be observed in isolation. Our results show that in thinking-enabled mathematical reasoning, OPSD behaves most reliably as a compression mechanism rather than a correction mechanism: training only on correct rollouts preserves accuracy while substantially shortening responses, whereas training only on incorrect rollouts damages accuracy. In light of these findings, we propose a revised post-training pipeline for thinking-enabled mathematical reasoning: SFT then RLVR then OPSD.

</details>


### [101] [Systematic Evaluation of Large Language Models for Post-Discharge Clinical Action Extraction](https://arxiv.org/abs/2605.06191)
*Shivali Dalmia,Ananya Mantravadi,Prasanna Desikan*

Main category: cs.AI

TL;DR: LLMs show competitive performance on binary clinical action detection but supervised models still outperform on fine-grained classification, revealing that current clinical NLP evaluation suffers from annotation convention mismasks that obscure true clinical reasoning failures.


<details>
  <summary>Details</summary>
Motivation: The paper aims to evaluate LLMs for safety-critical clinical action extraction from discharge notes, focusing on transitions of care and post-discharge patient safety. There's a need to assess how well modern LLMs can handle complex clinical documentation compared to traditional supervised methods, while also addressing the challenge of annotation inconsistencies in clinical NLP.

Method: The authors introduce a two-stage extraction framework that decomposes narrative discharge notes into fine-grained actionable clinical tasks using staged prompting. They conduct a systematic evaluation comparing zero-shot/few-shot LLMs with task-specific supervised BERT-based models on the CLIP discharge-note dataset, while also analyzing annotation inconsistencies across different action categories.

Result: Contemporary LLMs achieve performance comparable to or exceeding supervised models on binary actionability detection. However, supervised baselines maintain a meaningful advantage on fine-grained multi-label category classification, even without task-specific fine-tuning and under data-privacy constraints. Error analysis reveals many failures stem from misalignment between model reasoning and dataset annotation conventions, particularly with implicit clinical actions and rigid labeling rules.

Conclusion: The reported performance reflects limitations due to lack of clinical reasoning not captured by plain annotations. The key insight is that labels without rationales make it impossible to distinguish clinical reasoning failures from annotation convention mismatches. Advancing clinical NLP requires reasoning-annotated datasets that document why specific spans are actionable, enabling proper evaluation of model clinical understanding beyond mere labeling.

Abstract: The work in this paper evaluates zero-shot and few-shot large language models (LLMs) for safety-critical clinical action extraction using the CLIP discharge-note dataset, with particular emphasis on transitions of care and post-discharge patient safety. To manage the complexity of clinical documentation, we introduce a two-stage extraction framework that decomposes discharge notes, that are written in narrative form, into fine-grained, explicitly actionable clinical tasks through a staged prompting strategy. Our contributions include a systematic assessment of generative LLMs for clinical action extraction, a detailed comparison between general-purpose LLMs and task-specific supervised BERT-based models, and an analysis of annotation inconsistencies across different action categories. We show that contemporary LLMs achieve performance comparable to or exceeding supervised models on binary actionability detection, while supervised baselines retain a meaningful advantage on fine-grained multi-label category classification, despite the absence of task-specific fine-tuning and under strict data-privacy constraints. Qualitative error analysis reveals that many failures stem from misalignment between model reasoning and dataset annotation conventions, particularly in cases involving implicit clinical actions and rigid structural labeling rules. These results indicate that reported performance reflects model limitations due to lack of clinical reasoning, that is not captured by plain annotations. Labels without rationales make it impossible to distinguish clinical reasoning failures from annotation convention mismatches. Advancing clinical NLP requires reasoning-annotated datasets that document why specific spans are actionable, not merely which spans were labeled, enabling proper evaluation of model clinical understanding.

</details>


### [102] [The Granularity Axis: A Micro-to-Macro Latent Direction for Social Roles in Language Models](https://arxiv.org/abs/2605.06196)
*Chonghan Qin,Xiachong Feng,Ziyun Song,Xiaocheng Feng,Jing Xiong,Lingpeng Kong*

Main category: cs.AI

TL;DR: LLMs internally represent social roles along a granularity axis from individual to institutional levels, which is the dominant geometric organization in role representation space and can be causally manipulated to shift response granularity.


<details>
  <summary>Details</summary>
Motivation: While LLMs are commonly prompted to adopt various social roles (from individuals to institutions), it's unclear whether their internal representations actually encode the granularity differences between micro-level individual experiences and macro-level organizational/institutional reasoning.

Method: Defined a contrast-based Granularity Axis as the difference between mean macro- and micro-role hidden states. Created 75 social roles across five granularity levels, collected 91,200 role-conditioned responses over shared questions, extracted hidden states, and projected them onto the axis. Tested across layers, prompt variants, endpoint definitions, held-out splits, score-filtered subsets, and different models (Qwen3-8B and Llama-3.1-8B-Instruct). Used activation steering to causally manipulate the axis.

Result: In Qwen3-8B, the Granularity Axis aligns with the principal axis (PC1) of role representation space at cosine 0.972, accounting for 52.6% of variance. Role projections increase monotonically across all five granularity levels. The axis remains stable across various conditions and transfers to Llama-3.1-8B-Instruct. Activation steering along the axis causally shifts response granularity (Llama moving from 2.00 to 3.17 on a five-point macro scale with positive steering). Models differ in controllability based on their default operating regimes.

Conclusion: Social role granularity is not just a stylistic surface feature in LLMs, but a structured, ordered, and causally manipulable latent direction in role-conditioned language model behavior that organizes role representations at a fundamental geometric level.

Abstract: Large language models (LLMs) are routinely prompted to take on social roles ranging from individuals to institutions, yet it remains unclear whether their internal representations encode the granularity of such roles, from micro-level individual experience to macro-level organizational, institutional, or national reasoning. We show that they do. We define a contrast-based Granularity Axis as the difference between mean macro- and micro-role hidden states. In Qwen3-8B, this axis aligns with the principal axis (PC1) of the role representation space at cosine 0.972 and accounts for 52.6% of its variance, indicating that granularity is the dominant geometric axis organizing prompted social roles. We construct 75 social roles across five granularity levels and collect 91,200 role-conditioned responses over shared questions and prompt variants, then extract role-level hidden states and project them onto the axis. Role projections increase monotonically across all five levels, remain stable across layers, prompt variants, endpoint definitions, held-out splits, and score-filtered subsets, and transfer to Llama-3.1-8B-Instruct. The axis is also causally relevant: activation steering along it shifts response granularity in the predicted direction, with Llama moving from 2.00 to 3.17 on a five-point macro scale under positive steering on prompts that admit local responses. The two models differ in controllability, suggesting that steering depends on each model's default operating regime. Overall, our findings suggest that social role granularity is not merely a stylistic surface feature, but a structured, ordered, and causally manipulable latent direction in role-conditioned language model behavior.

</details>


### [103] [Towards Annotation-Free Validation of MLLMs: A Vision-Language Logical Consistency Metric](https://arxiv.org/abs/2605.06201)
*Ying Gu,Mei Chee Leong,Hui Li Tan,Shangbo Mao,Liyuan Li,Nancy Chen*

Main category: cs.AI

TL;DR: A new framework for evaluating vision-language logical consistency in MLLMs using both sufficient and necessary cause-effect relations, without requiring ground-truth annotations.


<details>
  <summary>Details</summary>
Motivation: Traditional accuracy evaluation rewards unwarranted guessing in LLMs and is not applicable for novel tasks without ground-truth annotations. There's a need for better evaluation metrics that assess logical consistency rather than just accuracy.

Method: Proposed VL-LCM (Vision-Language Logical Consistency Metric) based on basic logic principles, evaluating both sufficient and necessary cause-effect relations. Applied to traditional MC-VQA tests and recent NaturalBench tests without ground-truth annotation.

Result: Evaluation of 11 recent open-source MLLMs from 4 frontier families on benchmarks MMMU and NaturalBench revealed that despite significant progress on accuracy, logical consistency lags behind significantly. VL-LCM showed strong correlations with ground-truth metrics and reliable results.

Conclusion: Beyond accuracy, logical consistency is crucial for both accuracy and reliability. VL-LCM can be used for MLLM selection, validation, and reliable answer justification in novel tasks without ground-truth annotation.

Abstract: Dominant accuracy evaluation might reward unwarranted guessing of Large Language Models, and it might not be applicable to novel tasks for model validation without ground-truth (gt) annotation. Based on basic logic principle, we propose a novel framework to evaluate the vision-language logical consistency of MLLMs on both sufficient and necessary cause-effect relations. We define Vision-Language Logical Consistency Metric (VL-LCM) on traditional MC-VQA tests, and recent NaturalBench tests without the need for gt annotation. Through systematic experiments on representative VL benchmark MMMU and recent VL challenges like NaturalBench, we evaluated 11 recent open-source MLLMs from 4 frontier families. Our findings reveal that, despite significant progress of recent MLLMs on accuracy, logical consistency lags behind significantly. Extensive evaluations on the correlations of VL-LCM with metrics on gt, the reliability of LCM, and the relation of VL-LCM with response distribution justify the validity and applicability of VL-LCM even without gt annotation. Our findings suggest that, beyond accuracy, logical consistency could be employed for both accuracy and reliability. VL-LCM can also be employed for MLLM selection, validation, and reliable answer justification in novel tasks without gt annotation.

</details>


### [104] [Beyond Fixed Benchmarks and Worst-Case Attacks: Dynamic Boundary Evaluation for Language Models](https://arxiv.org/abs/2605.06213)
*Haoxiang Wang,Da Yu,Huishuai Zhang*

Main category: cs.AI

TL;DR: DBE (Dynamic Boundary Evaluation) is a new LLM evaluation framework that finds each model's capability boundary where pass probability is ~0.5, providing more informative assessment than fixed benchmarks that suffer from ceiling/floor effects.


<details>
  <summary>Details</summary>
Motivation: Current LLM evaluation uses fixed benchmarks that apply the same items to all models, creating ceiling and floor effects that mask true capability differences. The most informative signal lies at the boundary where models struggle equally.

Method: DBE has three components: 1) Calibrated item bank with difficulty labels validated across 9 reference LLMs, 2) Skill-Guided Boundary Search (SGBS) algorithm that finds boundary items for target LLMs using only API access, and 3) Adaptive evaluation protocol that places models on unified scale and expands coverage when needed.

Result: DBE was applied to four categories: safety (harmful request refusal, over-refusal), capability (constrained instruction following), and truthfulness (multi-turn sycophancy resistance). It provides broader model spectrum coverage without saturation while remaining compatible with existing datasets.

Conclusion: Dynamic Boundary Evaluation offers a more informative and adaptive approach to LLM assessment by focusing on capability boundaries rather than static benchmarks, enabling better comparison across diverse models while avoiding ceiling/floor effects.

Abstract: Evaluating large language models (LLMs) today rests on fixed benchmarks that apply the same set of items to any model, producing ceiling and floor effects that mask capability gaps. We argue that the most informative evaluation signal lies at the boundary, where the per-prompt pass probability is near $0.5$ under random-sampling decoding, and propose Dynamic Boundary Evaluation (DBE), which actively locates each model's boundary and places it on a globally comparable difficulty scale. DBE delivers three artifacts: (i) a calibrated item bank covering safety, capability, and truthfulness, with per-item difficulty labels validated across $9$ reference LLMs; (ii) Skill-Guided Boundary Search (SGBS), a search algorithm that finds boundary items for a given target LLM using only API-level query access; and (iii) an evaluation protocol that places a new LLM on a unified ability scale and grows the evaluation set adaptively when the target falls outside the bank's coverage. We instantiate DBE on four categories spanning safety (harmful request refusal and over-refusal), capability (constrained instruction following), and truthfulness (multi-turn sycophancy resistance). The resulting evaluation covers a broader model spectrum without saturation while remaining compatible with existing datasets.

</details>


### [105] [Joint Consistency: A Unified Test-Time Aggregation Framework via Energy Minimization](https://arxiv.org/abs/2605.06219)
*Yunzhen Yao,Hongye Wang,Yahong Wang,Michael C. Gastpar,Bo Jiang,Lie He*

Main category: cs.AI

TL;DR: Proposes Joint Consistency (JC), a test-time aggregation method that generates multiple reasoning traces and aggregates them using both independent evaluation signals and pairwise comparisons via an Ising-type energy minimization framework.


<details>
  <summary>Details</summary>
Motivation: Existing test-time aggregation methods for reasoning tasks only use evaluation signals from individual candidate traces or simple answer frequencies, failing to leverage the valuable comparative interactions between different reasoning candidates.

Method: Joint Consistency formulates aggregation as a constrained Ising-type energy minimization problem where independent evaluation signals act as external fields and pairwise comparisons serve as interactions. The approach uses LLM-as-a-judge for pairwise comparisons and includes an efficient approximation strategy for large-scale applications.

Result: Experiments on math and code reasoning benchmarks demonstrate that JC consistently outperforms existing baselines across various tasks, judge models, trace budgets, and trace-generation settings.

Conclusion: Joint Consistency provides a unified framework that subsumes existing voting and weighted aggregation methods while enabling more effective test-time aggregation through modeling of pairwise candidate interactions, with proven effectiveness on complex reasoning tasks.

Abstract: This paper studies test-time aggregation, an approach that generates multiple reasoning traces and aggregates them into a final answer. Most existing methods rely on evaluation signals collected from candidate traces in isolation or answer frequencies, while ignoring comparative interactions among candidates. We propose Joint Consistency (JC), formulated as a constrained Ising-type energy minimization problem, where independent evaluation signals act as external fields and pairwise comparisons act as interactions. JC provides a unified framework for test-time aggregation that subsumes existing voting and weighted aggregation methods as special cases. Our construction of the interaction matrix leverages LLM-as-a-judge comparisons, and admits a theoretical interpretation under answer-level homogeneity assumptions. Moreover, we develop an efficient approximation strategy that makes interaction modeling practical for large-scale test-time aggregation. Experiments on math and code reasoning benchmarks show that JC consistently outperforms existing baselines across tasks, judge models, trace budgets, and trace-generation settings.

</details>


### [106] [Proactive Instance Navigation with Comparative Judgment for Ambiguous User Queries](https://arxiv.org/abs/2605.06223)
*Junhyuk Kwon,Seungjoon Lee,Hyejin Park,Kyle Min,Jungseul Ok*

Main category: cs.AI

TL;DR: ProCompNav is a two-stage framework for instance navigation that proactively asks discriminative questions through comparative judgment to efficiently disambiguate targets among similar distractors.


<details>
  <summary>Details</summary>
Motivation: Existing instance navigation approaches fail when initial user requests are ambiguous. Current methods either stop at the first plausible candidate without sufficient exploration, or ask questions about target attributes derived from individual candidates rather than selecting questions that effectively distinguish between multiple candidates. This leads to premature decisions and lengthy user responses.

Method: ProCompNav uses a two-stage framework: 1) Constructs a candidate pool, 2) Identifies the target through comparative judgment. At each round, it extracts an attribute-value pair that splits the current pool, asks binary yes/no questions, and prunes all inconsistent candidates at once, reframing disambiguation from open-ended description to pool-level discriminative questioning.

Result: On CoIN-Bench, ProCompNav improves Success Rate over interactive baselines with minimal input and non-interactive baselines with detailed descriptions, while substantially reducing Response Length. It also achieves state-of-the-art Success Rate on TextNav, demonstrating the broad usefulness of comparative judgment for instance-level navigation.

Conclusion: Comparative judgment is an effective approach for instance navigation with ambiguous targets, allowing agents to proactively ask discriminative questions that efficiently distinguish targets from similar distractors while reducing user burden and response length.

Abstract: Natural-language instance navigation becomes challenging when the initial user request does not uniquely specify the target instance. A practical agent should reduce the user's burden by actively asking only the information needed to distinguish the target from similar distractors, rather than requiring a detailed description upfront. Existing approaches often fall short of this goal: they may stop at the first plausible candidate before sufficiently exploring alternatives, or, even after collecting multiple candidates, ask about the target's attributes derived from individual candidates rather than questions selected to distinguish candidates in the pool. As a result, despite the dialogue, the agent may still fail to distinguish the target from distractors, leading to premature decisions and lengthy user responses. We propose Proactive Instance Navigation with Comparative Judgment (ProCompNav), a two-stage framework that first constructs a candidate pool and then identifies the target through comparative judgment. At each round, ProCompNav extracts an attribute-value pair that splits the current pool, asks a binary yes/no question, and prunes all inconsistent candidates at once. This reframes disambiguation from open-ended target description to pool-level discriminative questioning, where each question is chosen to narrow the candidate set. On CoIN-Bench, ProCompNav improves Success Rate over interactive baselines with the same minimal input and non-interactive baselines with detailed descriptions, while substantially reducing Response Length. ProCompNav also achieves state-of-the-art Success Rate on TextNav, suggesting that comparative judgment is broadly useful for instance-level navigation among similar distractors.

</details>


### [107] [A Versatile AI Agent for Rare Disease Diagnosis and Risk Gene Prioritization](https://arxiv.org/abs/2605.06226)
*Tianyu Liu,Wangjie Zheng,Rui Yang,Benny Kai Guo Loo,Hui Zhang,Jeffries Lauran,Jianlei Gu,Botao Yu,Weihao Xuan,Kexin Huang,Nan Liu,James Zou,Yonghui Jiang,Hua Xu,Hongyu Zhao*

Main category: cs.AI

TL;DR: Hygieia is a multi-modal AI agent system that improves rare disease diagnosis by integrating phenotypic, genetic, and clinical data, outperforming physicians by 12-60% while reducing clinician workload.


<details>
  <summary>Details</summary>
Motivation: Current diagnostic workflows for rare diseases suffer from prolonged assessment times and low accuracy, creating a need for better tools to support precision disease diagnosis.

Method: Developed Hygieia - a router-based, knowledge-enhanced multi-modal AI agent system that integrates phenotypic features, genetic profiles, and clinical records, with mechanisms to mitigate hallucinations and tailor strategies to different disease categories.

Result: Hygieia achieves state-of-the-art performance across multiple diagnostic benchmarks, demonstrates 12-60% improvement over physicians, and effectively assists clinicians with real-world medical records while providing confidence scores for decision-making.

Conclusion: Hygieia enhances diagnostic accuracy, interpretability, and significantly reduces clinician workload, showing strong potential as a valuable clinical decision support system tool.

Abstract: Accurate and timely diagnosis is essential for effective treatment, particularly in the context of rare diseases. However, current diagnostic workflows often lead to prolonged assessment times and low accuracy. To address these limitations, we introduce Hygieia, a multi-modal AI agent system designed to support precision disease diagnosis by integrating diverse data sources, including phenotypic features, genetic profiles, and clinical records. Hygieia features a router-based and knowledge-enhanced framework that mitigates hallucination and tailors diagnostic strategies to different disease categories. Notably, it prioritizes risk-related genomic factors for rare diseases and provides confidence scores to assist clinical decision-making. We conducted a comprehensive evaluation demonstrating that Hygieia achieves state-of-the-art performance across multiple diagnostic benchmarks. In collaboration with clinical experts from Yale School of Medicine and Duke-NUS Medical School, we further validated its practical utility by showing (1) Hygieia's superior diagnostic performance compared to physicians with an improvement from 12%-60% and (2) its effectiveness in assisting clinicians with medical records for handling real-world cases. Our findings indicate that Hygieia not only enhances diagnostic accuracy and interpretability but also significantly reduces clinician workload, highlighting its potential as a valuable tool in clinical decision support systems.

</details>


### [108] [Price of Fairness in Short-Term and Long-Term Algorithmic Selections](https://arxiv.org/abs/2605.06227)
*Shahin Jabbari,Chen Wang*

Main category: cs.AI

TL;DR: The paper studies fairness in sequential decision-making, showing that static fairness constraints can worsen long-term disparities, while proposing new notions of fairness that balance short-term fairness with long-term population equity.


<details>
  <summary>Details</summary>
Motivation: Algorithmic decision-making in high-stakes settings impacts individuals and populations, but prior work focuses on static fairness. Recent results show static fairness constraints may exacerbate long-run disparities, creating tension between short-term fairness and long-term equity.

Method: Study a stylized sequential selection problem where a decision-maker repeatedly selects individuals, affecting immediate utility and population distribution over time. Introduce new notions of group fairness for both short and long term, theoretically analyze fairness-utility trade-off via Price of Fairness (PoF). Characterize optimal/fair policies in short term and show PoF can be large even with similar group distributions. Analyze long-term disparities under investment policies.

Result: Static fairness constraints can have large PoF even when group distributions are nearly identical. In contrast, long-term disparities can vanish under simple investment policies that achieve low PoF. Empirical validation using synthetic and real datasets supports theoretical observations.

Conclusion: Long-term thinking is crucial for fair algorithmic decision-making. Simple investment policies can achieve both low PoF and vanishing long-term disparities, offering a more sustainable approach than static fairness constraints that may worsen inequities over time.

Abstract: Algorithmic decision-making in high-stakes settings can have profound impacts on individuals and populations. While much prior work studies fairness in static settings, recent results show that enforcing static fairness constraints may exacerbate long-run disparities. Motivated by this tension, we study a stylized sequential selection problem in which a decision-maker repeatedly selects individuals, affecting both immediate utility and the population distribution over time. We introduce notions of group fairness for both the short and long term and theoretically analyze the trade-off between fairness and utility via the Price of Fairness (PoF). We characterize optimal and fair policies in the short term and show that the PoF can be large even when group distributions are nearly identical. In contrast, we show that long-term disparities can vanish under simple investment policies that achieve a low PoF. We also empirically validate these theoretical observations using both synthetic and real datasets.

</details>


### [109] [Safactory: A Scalable Agent Factory for Trustworthy Autonomous Intelligence](https://arxiv.org/abs/2605.06230)
*Xinquan Chen,Zhenyun Yin,Shan He,Bin Huang,Shanzhe Lei,Pengcheng Shi,Kun Cai,Bei Chen,Bangwei Liu,Zeyu Kang,Chao Huang,Yang Zhang,Wenjie Li,Ruijun Ge,Yajie Wang,Tianshun Fang,Tianyang Xu,Yiwen Cong,Meng Jin,Gaolei Li,Xuansheng Wu,Linhan Liu,Zijing He,An Li,Yan Teng,Xin Tan,ChaoChao Lu,Ji He,Jie Li,Chunfeng Song,Jinya Xu,Fan Song,Shujie Wang,Jianmin Qian,Jie Hou,Xuhong Wang,Yingchun Wang,Hui Wang,Xia Hu*

Main category: cs.AI

TL;DR: Safactory is a scalable agent factory framework that unifies simulation, data management, and evolution platforms for developing trustworthy autonomous intelligence through continuous closed-loop improvement.


<details>
  <summary>Details</summary>
Motivation: As large models evolve into autonomous agents, challenges arise from long-horizon decision making, tool use, and real environment interaction. Existing infrastructure remains fragmented across evaluation, data management, and agent evolution, making systematic risk discovery and continuous improvement difficult.

Method: Safactory integrates three tightly coupled platforms: 1) Parallel Simulation Platform for trajectory generation, 2) Trustworthy Data Platform for trajectory storage and experience extraction, and 3) Autonomous Evolution Platform for asynchronous reinforcement learning and on-policy distillation.

Result: Safactory is presented as the first framework to propose a unified evolutionary pipeline for next-generation trustworthy autonomous intelligence, addressing fragmentation in current agent infrastructure.

Conclusion: Safactory provides a comprehensive solution for developing trustworthy autonomous agents through integrated simulation, data management, and evolutionary learning platforms, enabling systematic risk assessment and continuous improvement in a closed-loop system.

Abstract: As large models evolve from conversational assistants into autonomous agents, challenges increasingly arise from long-horizon decision making, tool use, and real environment interaction. Existing agenticinfrastructure remain fragmented across evaluation, data management, and agent evolution, making it difficult to discover risks systematically and improve models in a continuous closed loop. In this report, we present \textbf{Safactory}, a scalable agent factory for trustworthy autonomous intelligence. Safactory integrates three tightly coupled platforms: a \textbf{Parallel Simulation Platform} for trajectory generation, a \textbf{Trustworthy Data Platform} for trajectory storage and experience extraction, and an \textbf{Autonomous Evolution Platform} for asynchronous reinforcement learning and on-policy distillation. As far as we know, Safactory is the first framework to propose a unified evolutionary pipeline for next-generation trustworthy autonomous intelligence.

</details>


### [110] [Data Language Models: A New Foundation Model Class for Tabular Data](https://arxiv.org/abs/2605.06290)
*Eda Erol,Giuliano Pezzoli,Ozer Cem Kelahmet*

Main category: cs.AI

TL;DR: DLM (Data Language Model) is a native foundation model for tabular data that understands tables directly without preprocessing, outperforming existing methods on prediction tasks and enabling new capabilities like dataset classification.


<details>
  <summary>Details</summary>
Motivation: Tabular data lacks a native foundation model unlike other modalities (text, images, audio). Current approaches require preprocessing pipelines and don't understand tabular data as a native modality.

Method: Introduces Data Language Model (DLM) that understands tables natively without serialization or preprocessing. Presents Schema-1, a 140M parameter model trained on over 2.3M synthetic and real-world tabular datasets.

Result: Schema-1 outperforms gradient-boosted ensembles, AutoML stacks, and tabular foundation models on row-level prediction benchmarks. Achieves lower reconstruction error than statistical methods and LLMs on missing value imputation. Can identify industry sector of any unseen dataset from raw cell values alone - a novel capability.

Conclusion: DLM provides the missing native tabular understanding layer for AI stack, eliminating preprocessing pipelines and enabling direct consumption of raw tabular data by AI systems.

Abstract: Every major data modality now has a foundation model that understands it natively: text has language models, images have vision models, audio has audio models. Tabular data, the modality on which many consequential real-world AI decisions are made, does not. Every approach to tabular AI today, from gradient-boosted trees to the latest tabular foundation models, requires a preprocessing pipeline before any model can consume the data. None of them understand tabular data as a modality. We introduce the Data Language Model (DLM), the missing foundation model for tabular data. A DLM understands tables the way a language model understands sentences: natively, without serialization or preprocessing, directly from raw cell values. It is the tabular data layer on which AI models, agents, and vertical AI applications can be built, eliminating the preprocessing pipelines that currently stand between raw data and every AI system that consumes it. We present Schema-1, the first DLM: a 140M parameter model trained on more than 2.3M synthetic and real-world tabular datasets. Schema-1 outperforms gradient-boosted ensembles, AutoML stacks, and the tabular foundation models we evaluate on established row-level prediction benchmarks. On missing value reconstruction it achieves lower reconstruction error than all classical statistical methods and frontier large language models on mean performance across conditions, establishing that structural understanding of a dataset's own distributional geometry is more useful for imputation than world knowledge encoded in language. It identifies the industry sector of any unseen dataset from raw cell values alone, reliably across any domain, a task no prior tabular model can perform. It is the native tabular understanding layer that has been missing from the AI stack.

</details>


### [111] [Addressing Labelled Data Scarcity: Taxonomy-Agnostic Annotation of PII Values in HTTP Traffic using LLMs](https://arxiv.org/abs/2605.06305)
*Thomas Cory,Axel Küpper*

Main category: cs.AI

TL;DR: LLMs can perform taxonomy-agnostic PII detection in HTTP traffic through a multi-stage pipeline, enabling flexible privacy audits without fixed label dependencies.


<details>
  <summary>Details</summary>
Motivation: Existing PII leakage detectors rely on scarce labeled data and fixed taxonomies, limiting transferability across domains and adaptability to evolving PII definitions.

Method: Multi-stage LLM-based pipeline combining deterministic pre-processing with label-level classification, instance-level value annotation, and output validation, plus an LLM-based generator for synthetic HTTP traffic with validated PII annotations.

Result: The pipeline accurately detects PII types and extracts corresponding values across three taxonomies spanning different PII domains and granularity levels, outperforming fixed-taxonomy approaches.

Conclusion: LLMs provide a promising foundation for flexible, taxonomy-agnostic traffic annotation and labeled data generation under evolving privacy taxonomies.

Abstract: Automated privacy audits of web and mobile applications often analyse outbound HTTP traffic to detect Personally Identifiable Information (PII) leakage. However, existing learning-based detectors typically depend on scarce, manually labelled traffic and are tightly coupled to fixed label taxonomies, limiting transferability across domains and evolving definitions of PII. This paper investigates whether Large Language Models (LLMs) can support taxonomy-agnostic annotation of explicitly transmitted PII values in HTTP message bodies when the taxonomy is provided at runtime. We introduce a multi-stage LLM-based pipeline that combines deterministic pre-processing with label-level classification, targeted instance-level value annotation, and output validation. To enable controlled evaluation and exemplar-based prompting without relying on sensitive real-user captures, we further propose an LLM-based generator for synthetic HTTP traffic with manually validated, taxonomy-derived PII annotations. We evaluate the approach across three taxonomies spanning different PII domains and granularity levels. Results show that the pipeline accurately detects PII types and extracts corresponding values for concrete PII taxonomies. Overall, our findings position LLMs as a promising foundation for flexible, taxonomy-agnostic traffic annotation and for creating labelled data under evolving privacy taxonomies.

</details>


### [112] [Measuring Black-Box Confidence via Reasoning Trajectories: Geometry, Coverage, and Verbalization](https://arxiv.org/abs/2605.06308)
*Marc Boubnovski Martell,Josefa Lia Stoisser,Kaspar Märtens,Jialin Yu,Robert Kitchen,Philip Torr,Jesper Ferkinghoff-Borg*

Main category: cs.AI

TL;DR: A black-box trajectory-confidence score for CoT reasoning that measures convergence to answer anchors via sliding-window embeddings, outperforming self-consistency with fewer samples.


<details>
  <summary>Details</summary>
Motivation: Self-consistency over K samples is linearly expensive and ignores the geometry of reasoning traces. Need reliable confidence estimation for safe CoT deployment through text-only APIs without requiring logits, hidden states, or supervised calibration.

Method: Propose a black-box trajectory-confidence score: embed CoT as sliding-window trajectory and measure its convergence to external answer anchors with one-parameter softmax. Fuse with coverage and verbalized-confidence channels. Test across 6 benchmark/reasoner settings with Gemini 3.1 Pro and Claude Sonnet 4.6.

Result: Fusion at K=4 yields Pareto improvements over self-consistency at K=8 in 6/6 settings (median AUC 0.78 vs 0.71). Geometry peaks in penultimate window across benchmarks. Three regimes identified: Coverage prior (C), within-trace Geometry (G), and conditional Verbalization (V). Fusion beats best single channel in 17/18 settings (median AUC 0.78).

Conclusion: The proposed black-box trajectory-confidence score provides effective confidence estimation for CoT reasoning with fewer samples than self-consistency, revealing distinct contributions from coverage, geometry, and verbalization channels that enable reliable deployment through text-only APIs.

Abstract: Reliable confidence estimation enables safe deployment of chain-of-thought (CoT) reasoning through text-only APIs. Yet the dominant black-box baseline, self-consistency over K samples, is linearly expensive and ignores the geometry of the trace. We propose a black-box trajectory-confidence score: we embed a CoT as a sliding-window trajectory and measure its convergence to external answer anchors with a one-parameter softmax. The method needs no logits, hidden states, or supervised calibrators. Across six (benchmark, reasoner) settings on MedQA-USMLE, GPQA Diamond, and MMLU-Pro with Gemini 3.1 Pro and Claude Sonnet 4.6, fusing this score with coverage and verbalized-confidence channels at K=4 yields Pareto improvements over self-consistency at K=8 in 6/6 settings (median AUC 0.78 vs 0.71, deltaAUC=+0.075). A fixed-pick control (+0.060) and E5 cross-embedder replication rule out answer switching and single-vendor artifacts. Geometry peaks in the penultimate window across benchmarks and reasoners, and inverts at the terminal window on GPQA Diamond. Three unscaffolded regimes separate black-box confidence into a judge-mediated Coverage prior (C), within-trace Geometry (G), and a conditional Verbalization channel (V). Across 18 benchmark x reasoner x proposer settings, C and G provide independent signal in 18/18 and 16/18, while V contributes residual signal in 6/18. Swapping the judge from GPT-5-mini to Claude Sonnet 4.6 leaves G-only AUC unchanged (|delta|<=0.013) and shifts C-only AUC by at most +/-0.02 (kappa=0.82). Fusion beats the best single channel in 17/18 settings (median AUC 0.78, max 0.92).

</details>


### [113] [A Regime Theory of Controller Class Selection for LLM Action Decisions](https://arxiv.org/abs/2605.06339)
*Zhaoyang Jiang,Zhizhong Fu,Yunsoo Kim,Jiacong Mi,Zicheng Li,Xuanqi Peng,Honghan Wu*

Main category: cs.AI

TL;DR: Contrary to monotonicity intuition, greater per-input expressivity in decision controllers isn't always beneficial in finite samples. The paper organizes controllers into a lattice of four complexity classes and develops a regime theory that predicts optimal controller class based on data-estimable bottlenecks.


<details>
  <summary>Details</summary>
Motivation: Deployed language and vision-language models need to make intelligent decisions about how to handle each input (answer directly, retrieve evidence, defer to stronger model, or abstain). The common assumption that more expressive controllers always help is not true in practice, as different benchmarks prefer different controller classes under identical cross-validation.

Method: The authors organize decision controllers into a nested lattice of four complexity classes: fixed actions, partition routers, instance-level controllers, and prior-gated controllers. They develop a regime theory that uses three data-estimable bottlenecks to predict optimal controller class: 1) improvement beyond best fixed action, 2) sufficient samples for instance-level decisions, and 3) partition router performance when instance-level signal is unreliable.

Result: The proposed Bernstein-tight threshold has a matching information-theoretic lower bound, and strict nested cross-validation provably selects a near-best class. Experiments across SMS-Spam, HallusionBench, A-OKVQA, and FOLIO show the predicted class matches empirical winners; prior-gated controller wins on TextVQA when OCR tokens provide label-free prediction-time priors.

Conclusion: Greater expressivity in decision controllers isn't uniformly beneficial in finite samples. The regime theory provides a principled way to select optimal controller complexity based on data properties, with theoretical guarantees and empirical validation across diverse benchmarks.

Abstract: Deployed language and vision-language models must decide, on each input, whether to answer directly, retrieve evidence, defer to a stronger model, or abstain. Contrary to the common monotonicity intuition, greater per-input expressivity is not uniformly beneficial in finite samples: under identical strict cross-validation, different benchmarks prefer different controller classes. This reflects a finite-sample limitation of instance-level uncertainty signals, which can be exhausted at a distribution-dependent scale. We organize controllers into a nested lattice of four classes: fixed actions, partition routers, instance-level controllers, and prior-gated controllers, ordered by complexity. We prove a regime theory that turns three data-estimable bottlenecks into a class choice: how much improvement is possible beyond the best fixed action, whether there are enough samples for instance-level controllers to make reliable decisions, and how much improvement a coarse partition router can recover when instance-level signal is unreliable. The resulting Bernstein-tight threshold has a matching information-theoretic lower bound, and strict nested cross-validation provably selects a near-best class. Across SMS-Spam, HallusionBench, A-OKVQA, and FOLIO, the predicted class matches the empirical winner; the prior-gated controller wins on TextVQA when OCR tokens supply a label-free prediction-time prior. Code is available at https://github.com/Anonymous-Awesome-Submissions/Regime-Theory.

</details>


### [114] [Mind the Gap? A Distributional Comparison of Real and Synthetic Priors for Tabular Foundation Models](https://arxiv.org/abs/2605.06343)
*Alex O. Davies,Telmo de Menezes e Silva Filho,Nirav Ajmeri*

Main category: cs.AI

TL;DR: Despite different data sources for tabular foundation models (web-scraped, curated, synthetic), the distributional differences don't clearly impact downstream performance, suggesting coverage of real-data distribution isn't the primary driver of generalization for synthetic models.


<details>
  <summary>Details</summary>
Motivation: Little is known about how different pre-training corpora for tabular foundation models (web-scraped, curated, synthetic) relate to one another distributionally and how this affects downstream performance. The research aims to characterize and compare these different data sources to understand their impact.

Method: Analyzed three canonical datasets: T4 (web-scraped), TabFM (curated from Kaggle), and TabICL (synthetic). Characterized each corpus using aggregate features over whole tables, columns, and correlations. Compared them using discriminator AUCs and k-NN coverage metrics. Evaluated 86,000+ configurations to see if hyperparameter optimization could close distribution gaps.

Result: TabICL synthetic prior occupies a narrow region of the real table space, and this distribution mismatch cannot be closed by hyperparameter optimization. Curated and web-scraped corpora are broadly interchangeable distributionally. Surprisingly, the distributional gap between synthetic and real data doesn't clearly affect performance under feature-based measures or TabICL's internal representations.

Conclusion: Coverage of the real-data distribution is not the primary driver of TabICL's generalization, suggesting other factors are more important for synthetic tabular foundation model performance than distribution matching.

Abstract: Tabular foundation models are pre-trained on one of three classes of corpus: curated datasets drawn from benchmark repositories, tables harvested at scale from the web, or synthetic tables sampled from a parametric generative prior. Despite the centrality of pre-training data to model performance, little is known about how these corpora relate to one another in distribution, and the impact this has on downstream performance. In this work we take three canonical, archetypal datasets used to train tabular foundation models; the T4 dataset represents web-scraped corpora, the TabFM dataset curated tables from Kaggle, and the TabICL dataset as the only well-used synthetic prior with publicly available parameters. We characterise each corpus using aggregate features over whole tables, columns and correlations, and compare them using discriminator AUCs and k-NN coverage metrics. We find that the TabICL synthetic prior occupies a narrow region of the space of real tables, that this mismatch cannot be closed by optimising prior hyper-parameters across more than 86 thousand configurations, and that curated and web-scraped corpora are broadly interchangeable on a distributional level in feature space. Surprisingly, the distributional gap between synthetic pre-training data and real tables has a clearly detectable effect on performance under neither feature-based proximity measures or TabICL's own internal representations, suggesting that coverage of the real-data distribution is not the primary driver of TabICL's generalisation.

</details>


### [115] [More Than Can Be Said: A Benchmark and Framework for Pre-Question Scientific Ideation](https://arxiv.org/abs/2605.06345)
*Jie Yu,Song Qiu*

Main category: cs.AI

TL;DR: InciteResearch is a multi-agent AI framework that helps researchers transform their tacit, implicit research frictions into explicit, actionable research questions, enabling AI to assist in the early thinking phase rather than just automating downstream tasks.


<details>
  <summary>Details</summary>
Motivation: Current AI research agents only work after research questions have been explicitly formulated, but human research often begins with tacit, unarticulated frictions or misalignments that precede formal question formation. There's a gap in AI systems that can help with this early "thinking phase" of research.

Method: InciteResearch is a multi-agent framework that decomposes Socratic questioning across a pipeline: (1) elicits structured 5D researcher profiles from vague inputs, even domain-unrelated ones; (2) violates hidden assumptions by maximizing feasibility-novelty product with 7-stage causal derivation trace; (3) checks if proposed methods are necessary consequences of reframed insights.

Result: On TF-Bench (first benchmark for tacit-to-explicit research assistance), InciteResearch achieves significant improvements over baseline: novelty increases from 3.671 to 4.250 and impact from 3.806 to 4.397, shifting generated proposals from recombination to architectural insight.

Conclusion: AI can serve as an extension of thinking itself rather than merely automating downstream execution, demonstrating potential for AI to assist in the cognitive phase of research where tacit frictions need to be made explicit.

Abstract: AI research agents have shown strong potential in automating literature search and manuscript refinement, yet most assume a clear and actionable initial input, operating only after a research question has been made explicit. In contrast, human research often begins with tacit friction, a sense of misalignment before a question can be formed. We introduce InciteResearch, a multi-agent framework designed to make a researcher's implicit understanding explicit, inspectable, and actionable. InciteResearch decomposes the logical chain of Socratic questioning and distributes it across the entire pipeline that: (1) Elicits a structured five-dimensional researcher profile state anchored by specific friction points from vague, even domain-unrelated inputs; (2) Violates hidden assumptions by maximizing the feasibility-novelty product with enforcing a 7-stage causal derivation trace; and (3) check whether the proposed method is a Necessary consequence of the reframed insight. We further introduce TF-Bench, the first benchmark for tacit-to-explicit research assistance that distinguishes domain-related from domain-unrelated inspirations across four scientific modes. On TF-Bench, InciteResearch achieves leapfrogging gains over a prompt-based baseline (novelty/impact from 3.671/3.806 to 4.250/4.397), shifting generated proposals from recombination to architectural insight. Our work demonstrates that AI can serve as an extension of thinking itself, rather than merely automating downstream execution.

</details>


### [116] [Prediction and Empowerment: A Theory of Agency through Bridge Interfaces](https://arxiv.org/abs/2605.06346)
*Richard Csaky*

Main category: cs.AI

TL;DR: This paper analyzes agency in deterministic but partially observable worlds, showing that perfect prediction requires either identifying hidden states or using overwrite control, not just high empowerment. It develops a framework for understanding tradeoffs between prediction, compression, and empowerment in AI agents.


<details>
  <summary>Details</summary>
Motivation: The motivation is to understand agency under partial observability in deterministic physical or simulated worlds where randomness appears from uncertainty over initial conditions and hidden variables. The paper aims to clarify the relationships between prediction, compression, and empowerment in AI agents, particularly for modern AI systems where these concepts are often conflated.

Method: The authors model sensing and actuation as bridge interfaces split between agent-controlled parameters and environment-controlled channel states, creating deterministic POMDPs through priors over latent microstates and many-to-one observation coarsening. They analyze this framework mathematically, proving separation theorems between prediction, compression, and empowerment. They also specialize to bit-string models with conserved information budgets to make tradeoffs explicit.

Result: The paper proves that perfect prediction requires either identifying the hidden quotient relevant to the target family or using overwrite control that makes future targets action-determined, and that high empowerment alone is insufficient. Under refinable interfaces and sufficient memory, action-conditioned observation compression reduces posterior uncertainty about latent quotients. A bit-string model shows explicit tradeoffs: prediction by identification requires internal capacity at least equal to relevant latent entropy, while overwrite control requires terminal action capacity over the controlled quotient.

Conclusion: The results suggest a design principle for modern AI agents: objectives should distinguish between hidden-state identification, interface refinement, task-relevant controllability, and mere overwrite or distractor control. Human-AI alignment is framed as an interface-design problem involving bridges between human intent, agent internal state, external tools, and world-side channel conditions.

Abstract: We study agency under partial observability in deterministic physical or simulated worlds, where apparent randomness arises from uncertainty over initial conditions, fixed law bits, and unrolled exogenous noise. We model sensing and actuation as bridge interfaces split between agent-controlled parameters and environment-controlled channel state, inducing a deterministic POMDP through a prior over latent microstates and many-to-one observation coarsening. Within this framework, we prove a separation between prediction, compression, and empowerment. Perfect prediction can be achieved either by identifying the hidden quotient relevant to the target family or by overwrite control that makes the future target action-determined; high empowerment alone is insufficient. Under refinable interfaces and sufficient memory, action-conditioned observation-compression progress reduces posterior uncertainty about the latent quotient, and when refinement requires steering world-side channel conditions, this creates target-conditioned interface empowerment. A bit-string specialization with a conserved information budget makes the resulting tradeoff explicit: prediction by identification requires internal capacity at least the relevant latent entropy, whereas overwrite control requires terminal action capacity over the controlled quotient. For modern AI agents, the results suggest a design principle rather than a theorem of inevitability: objectives should distinguish hidden-state identification, interface refinement, task-relevant controllability, and mere overwrite or distractor control. Human--AI alignment is partly an interface-design problem, where the relevant bridge is between human intent, agent internal state, external tools, and world-side channel conditions. This is a working draft: feedback and criticism is most welcome.

</details>


### [117] [Debiased Multimodal Personality Understanding through Dual Causal Intervention](https://arxiv.org/abs/2605.06371)
*Yangfu Zhu,Zitong Han,Nianwen Ning,Yuting Wei,Yuandong Wang,Hang Feng,Zhenzhou Shao*

Main category: cs.AI

TL;DR: A causal framework (DCAN) with dual adjustment modules to debias multimodal personality understanding by addressing observable and unobservable subject biases through causal disentanglement.


<details>
  <summary>Details</summary>
Motivation: Multimodal personality understanding suffers from subject bias (demographic factors, mental states) that creates unfair predictions; existing methods learn spurious correlations between multimodal features and personality traits.

Method: Propose Dual Causal Adjustment Network (DCAN): (1) Back-door Adjustment Causal Learning (BACL) uses prototype-based confounder dictionary to block observable demographic bias; (2) Front-door Adjustment Causal Learning (FACL) uses learned mediator dictionary to address latent/unobservable bias, achieving causal disentanglement. Also release DMSP dataset with demographic annotations.

Result: DCAN achieves 92.11% (CFI-V2) and 92.90% (DMSP) prediction accuracy. Fairness improvements: 6.57% equal opportunity & 7.97% demographic parity on CFI-V2; 15.38% equal opportunity & 20.06% demographic parity on DMSP.

Conclusion: Causal perspective effectively mitigates subject bias in multimodal personality understanding; DCAN's dual adjustment improves both accuracy and fairness; released DMSP dataset supports fairness research.

Abstract: Multimodalpersonalityunderstandingplaysacriticalroleinhuman centered artificial intelligence. Previous work mainly focus on learn-ing rich multimodal representations for video personality under standing. However, they often suffer from potential harm caused by subject bias (e.g., observable age and unobservable mental states), as subjects originate from diverse demographic backgrounds. Learn ing such spurious associations between multimodal features and traits may lead to unfair personality understanding. In this work, weconstruct aStructural Causal Model (SCM)toanalyze theimpact of these biases from a causal perspective, and propose a novel Dual Causal Adjustment Network (DCAN) to mitigate the interference of subject attributes on personality understanding. Specifically, we design a Back-door Adjustment Causal Learning (BACL) module to block spurious correlations from observable demographic factors via a prototype-based confounder dictionary, and subsequently ap ply a Front-door Adjustment Causal Learning (FACL) module to ad dress latent and unobservable biases throughalearnedmediatordic tionary intervention, thereby achieving causal disentanglement of representations for deconfounded reasoning. Importantly, we con struct a Demographic-annotated Multimodal Student Personality (DMSP) dataset to support the analysis and discussion of fairness related factors. Extensive experiments on the benchmark dataset CFI-V2 and our DMSPdataset demonstrate that DCAN consistently improves prediction accuracy, reaching 92.11% and 92.90%, respec tively. Meanwhile, the improvementsinthefairnessmetricsofequal opportunity and demographic parity are 6.57% and 7.97% on CFI-V2, and 15.38% and 20.06% on the DMSP dataset. Our code and DMSP dataset are available at https://github.com/Sabrina-han/DCAN

</details>


### [118] [Rethinking Vacuity for OOD Detection in Evidential Deep Learning](https://arxiv.org/abs/2605.06382)
*Claire McNamara*

Main category: cs.AI

TL;DR: Vacuity/Uncertainty Mass (UM) in Evidential Deep Learning for OOD detection is sensitive to class cardinality differences between ID and OOD datasets, creating evaluation artifacts that artificially inflate AUROC/AUPR metrics without actual model improvement.


<details>
  <summary>Details</summary>
Motivation: Current evaluation of OOD detection in Evidential Deep Learning using vacuity/UM metrics fails to account for sensitivity to class cardinality (K), leading to unfair comparisons when K differs between ID and OOD datasets. This creates evaluation artifacts that can inflate performance metrics without real model improvement.

Method: The paper empirically demonstrates the issue by showing substantial differences in AUROC and AUPR when class cardinality between ID and OOD differs by just 1. It analyzes standard EDL and IB-EDL models, providing theoretical explanation of how UM = K/S is sensitive to K due to the nature of EDL suppressing incorrectly assigned evidence.

Result: Experimental results show AUROC differences as high as 0.318 and AUPR differences of 0.613 for standard EDL, and 0.360/0.683 for IB-EDL when K differs by 1. The paper identifies this as an evaluation artifact where metrics can be artificially inflated without any change in model predictions.

Conclusion: Vacuity-based OOD detection in EDL-fine-tuned LLMs is highly sensitive to uncontrolled differences in class cardinality between ID and OOD datasets. The paper argues for clearer definitions of ID/OOD in MCQA contexts and highlights the need to control for K in evaluations to avoid misleading results.

Abstract: Vacuity, or Uncertainty Mass (UM), is commonly used as a metric to evaluate Out-of-Distribution (OOD) detection in Evidential Deep Learning (EDL). It generally involves dividing the number of classes ($K$) by the total strength of belief ($S$) of the model's predictions, where $S$ is derived from summing the Dirichlet parameters. As such, UM is sensitive to the cardinality of $K$. In particular, it is unlikely in practice that there is a linear relationship between $K$ and $S$ as $K$ and $S$ increase due to the nature of EDL (suppressing incorrectly assigned evidence). As a result, when comparing In Distribution (ID) and OOD results, it is important that $K_{\mathrm{ID}}$ and $K_{\mathrm{OOD}}$ are equal; something that is not always ensured in practice. We provide an empirical demonstration of how results for AUROC and AUPR can substantially differ when class cardinality between ID and OOD differs by 1, with AUROC differing by as much as 0.318 and AUPR by 0.613 for standard EDL, and AUROC by 0.360 and AUPR by 0.683 for IB-EDL. More concretely, our findings isolate an evaluation artefact: when K differs between ID and OOD, AUROC/AUPR can be artificially inflated without any change in model predictions. We further discuss the evaluation of EDL over causal language models using Multiple-Choice Question-Answer (MCQA) datasets and argue for clearer definitions of ID and OOD in this context. Our primary contribution is an empirical and theoretical demonstration that vacuity-based OOD detection in EDL-fine-tuned LLMs is highly sensitive to uncontrolled differences in evaluated class cardinality.

</details>


### [119] [Automated alignment is harder than you think](https://arxiv.org/abs/2605.06390)
*Aleksandr Bowkis,Marie Davidsen Buhl,Jacob Pfau,Geoffrey Irving*

Main category: cs.AI

TL;DR: Automated alignment research using AI agents may produce misleading safety assessments due to hard-to-supervise fuzzy tasks, leading to deployment of misaligned AI systems despite good intentions.


<details>
  <summary>Details</summary>
Motivation: The paper addresses risks in the popular approach of automating alignment research with AI agents. It argues that even well-intentioned, non-scheming research agents could inadvertently produce catastrophically misleading safety assessments, resulting in deployment of misaligned artificial superintelligence.

Method: The paper analyzes the structural problems of automated alignment research through reasoning about hard-to-supervise fuzzy tasks - alignment research activities without clear evaluation criteria where human judgment is systematically flawed. It examines four specific reasons why agent-generated alignment research is more problematic than human research: optimization pressure concentrating errors, non-human-like mistakes, unevaluatable arguments, and correlated outputs.

Result: The analysis shows that automated alignment research faces fundamental challenges: systematic undetected errors in research outputs, incorrect aggregation into overconfident safety assessments, and multiple factors that make agent-generated problems worse than human-generated ones. The paper identifies that agents need to be trained to reliably perform hard-to-supervise fuzzy tasks.

Conclusion: Generalization and scalable oversight are the leading approaches to address these challenges, but both face novel difficulties in the context of automated alignment research. The paper warns against underestimating the risks of deploying misaligned AI systems based on misleading automated safety assessments.

Abstract: A leading proposal for aligning artificial superintelligence (ASI) is to use AI agents to automate an increasing fraction of alignment research as capabilities improve. We argue that, even when research agents are not scheming to deliberately sabotage alignment work, this plan could produce compelling but catastrophically misleading safety assessments resulting in the unintentional deployment of misaligned AI. This could happen because alignment research involves many hard-to-supervise fuzzy tasks (tasks without clear evaluation criteria, for which human judgement is systematically flawed). Consequently, research outputs will contain systematic, undetected errors, and even correct outputs could be incorrectly aggregated into overconfident safety assessments. This problem is likely to be worse for automated alignment research than for human-generated alignment research for several reasons: 1) optimisation pressure means agent-generated mistakes are concentrated among those that human reviewers are least likely to catch; 2) agents are likely to produce errors that do not resemble human mistakes; 3) AI-generated alignment solutions may involve arguments humans cannot evaluate; and 4) shared weights, data and training processes may make AI outputs more correlated than human equivalents. Therefore, agents must be trained to reliably perform hard-to-supervise fuzzy tasks. Generalisation and scalable oversight are the leading candidates for achieving this but both face novel challenges in the context of automated alignment.

</details>


### [120] [Knowledge Graphs, the Missing Link in Agentic AI-based Formal Verification](https://arxiv.org/abs/2605.06434)
*Vaisakh Naduvodi Viswambharan,Keerthan Kopparam Radhakrishna,Deepak Narayan Gadde,Aman Kumar*

Main category: cs.AI

TL;DR: A knowledge-graph based multi-agent workflow that generates SystemVerilog Assertions from natural language specifications with refinement loops for syntax repair, CEX-guided correction, and coverage augmentation.


<details>
  <summary>Details</summary>
Motivation: LLMs can generate SystemVerilog Assertions from natural language specs, but existing approaches treat specs and RTL as unstructured text, leading to poor grounding, semantic mismatches, and syntax failures during formal verification.

Method: Create a verification-centric Knowledge Graph from structured IRs of specs, RTL, and formal-tool feedback. Use multi-agent workflow to query/update KG for SVA generation with three refinement loops: syntax repair via tool diagnostics, CEX-guided correction using trace links, and coverage-directed property augmentation.

Result: Improves specification-to-RTL grounding, consistently produces compilable SVAs with low syntax-repair overhead. Achieves formal coverage ranging from 78.5% to 99.4% across seven benchmark designs, though convergence shows design dependence and complex temporal/arithmetic reasoning remains challenging for LLMs.

Conclusion: KG-based context retrieval enhances assertion synthesis by providing traceable, design-grounded context, enabling reliable SVA generation with formal coverage improvements, but complex reasoning tasks still pose challenges for current LLM capabilities.

Abstract: Recent advances in Large Language Models (LLMs) have enabled workflows that generate SystemVerilog Assertions (SVAs) from natural-language specifications, with the potential to accelerate Formal Verification (FV). However, high-quality assertion synthesis remains challenging because specifications are often ambiguous or incomplete and critical micro-architectural details reside in the Register Transfer Level (RTL). Many existing approaches treat the specification and RTL as loosely structured text, which weakens specification-to-RTL grounding and leads to semantic mismatches and frequent syntax failures during formal parsing and elaboration. This work addresses these limitations with a verification-centric Knowledge Graph (KG) constructed from structured Intermediate Representations (IRs) extracted from the specification, RTL, and formal-tool feedback, including syntax diagnostics, Counterexamples (CEXs), and coverage reports. The KG links requirements, design hierarchy, signals, assumptions, and properties to provide traceable, design-grounded context for generation. A multi-agent workflow queries and updates this KG to generate SVAs and to drive three refinement loops: syntax repair guided by tool diagnostics, CEX-guided correction using trace links, and coverage-directed property augmentation. Evaluation across seven benchmark designs indicates that KG-based context retrieval improves specification-to-RTL grounding and consistently produces compilable SVAs with low syntax-repair overhead. The approach achieves formal coverage ranging from 78.5% to 99.4%, though convergence exhibits design dependence with complex temporal and arithmetic reasoning remaining challenging for current LLM capabilities.

</details>


### [121] [SCRuB: Social Concept Reasoning under Rubric-Based Evaluation](https://arxiv.org/abs/2605.06444)
*Jamelle Watson-Daniels,Himaghna Bhattacharjee,Skyler Wang,Brandon Handoko,Antonio Li,Anaelia Ovalle,Mahesh Pasupuleti,Candace Ross,Vidya Sarma,Arjun Subramonian,Karen Ullrich,Will van der Vaart,Yijing Xin,Maximilian Nickel*

Main category: cs.AI

TL;DR: SCRuB is a framework for evaluating LLMs' reasoning about social concepts, showing frontier models outperform human experts across critical thinking dimensions and that current exam-style evaluation has reached saturation.


<details>
  <summary>Details</summary>
Motivation: Despite extensive LLM reasoning research on mathematical/technical tasks, there's little systematic evaluation of social concept reasoning capabilities, which is essential for models acting as social agents. Current methods lack rigor for measuring depth and critical thinking in social reasoning.

Method: Developed SCRuB framework with three phases: (1) prompt construction from established sources, (2) response generation by human experts and models, (3) comparative evaluation using five-dimensional critical thinking rubric. Introduced Panel of Disciplinary Perspectives ensemble validated against independent expert judges. Created SCRuBEval dataset (4,711 prompts) and SCRuBAnnotations (300 expert responses, 150 expert judgments).

Result: Frontier models consistently outperformed human experts across all five rubric dimensions. In 1,170 pairwise comparisons, expert judges ranked model responses first in 80.8% of cases and preferred model responses overall 74.4% of the time. The study shows evaluation saturation - current single-turn exam-style format has reached ceiling performance for both models and humans.

Conclusion: SCRuB provides the first expert-grounded demonstration that frontier LLMs can reason about social concepts with greater depth and critical rigor than human experts, and reveals that current evaluation formats have reached saturation, necessitating new evaluation paradigms for social concept reasoning.

Abstract: While many studies of Large Language Model (LLM) reasoning capabilities emphasize mathematical or technical tasks, few address reasoning about social concepts: the abstract ideas shaping social norms, culture, and institutions. This understudied capability is essential for modern models acting as social agents, yet no systematic evaluation methodology targets it. We introduce SCRuB (Social Concept Reasoning under Rubric-Based Evaluation), a framework designed for this setting of task indeterminacy. Our goal is to measure the degree to which a model reasons about social concepts with the depth and critical rigor of a human expert. SCRuB proceeds in three phases: prompt construction from established sources, response generation by experts and models, and comparative evaluation using a five-dimensional critical thinking rubric. To enable generalization of the pipeline, we introduce a Panel of Disciplinary Perspectives ensemble validated against independent expert judges. We release SCRuBEval (n=4,711 evaluation prompts) and SCRuBAnnotations (300 expert-authored responses and 150 expert comparative judgments from 45 PhD-level scholars). Our results show that frontier models consistently outperform human experts across all five rubric dimensions. Across 1,170 pairwise comparisons, expert judges ranked a model response first in 80.8% of judgments and preferred model responses overall 74.4% of the time. Ultimately, this study provides the first expert-grounded demonstration of evaluation saturation for social concept reasoning: the single-turn exam-style format has reached its ceiling for models and humans alike.

</details>


### [122] [PrefixGuard: From LLM-Agent Traces to Online Failure-Warning Monitors](https://arxiv.org/abs/2605.06455)
*Xinmiao Huang,Jinwei Hu,Rajarshi Roy,Changshun Wu,Yi Dong,Xiaowei Huang*

Main category: cs.AI

TL;DR: PrefixGuard is a framework that synthesizes lightweight prefix monitors from execution traces to provide early warnings for LLM agents performing long tool-using tasks, outperforming raw-text baselines and LLM judges.


<details>
  <summary>Details</summary>
Motivation: LLM agents perform long tool-using tasks where final outcome checks come too late for intervention. Existing approaches have limitations: hand-authored event schemas are brittle, and deployment-time LLM judging is costly. There's a need for lightweight prefix monitors that can provide online warnings during task execution.

Method: PrefixGuard uses a two-step approach: 1) StepView induction - offline induction of deterministic typed-step adapters from raw trace samples to create structured representations; 2) Supervised monitor training - learning an event abstraction and prefix-risk scorer from terminal outcomes to predict potential failures early in execution.

Result: The monitors achieved AUPRC scores of 0.900/0.710/0.533/0.557 across WebArena, τ²-Bench, SkillsBench, and TerminalBench respectively. They improved over raw-text baselines by +0.137 AUPRC on average. LLM judges performed substantially worse. DFA extraction remained compact on some benchmarks (29-20 states) but expanded on others (151-187 states). Alert analysis revealed ranking quality doesn't always translate to deployment utility.

Conclusion: PrefixGuard provides a practical monitor-synthesis framework with explicit diagnostics for determining when prefix warnings can be turned into actionable interventions. It demonstrates that learned lightweight monitors outperform both raw-text baselines and LLM judges, though deployment utility varies across domains.

Abstract: Large language model (LLM) agents now execute long, tool-using tasks where final outcome checks can arrive too late for intervention. Online warning requires lightweight prefix monitors over heterogeneous traces, but hand-authored event schemas are brittle and deployment-time LLM judging is costly. We introduce PrefixGuard, a trace-to-monitor framework with an offline StepView induction step followed by supervised monitor training. StepView induces deterministic typed-step adapters from raw trace samples, and the monitor learns an event abstraction and prefix-risk scorer from terminal outcomes. Across WebArena, $τ^2$-Bench, SkillsBench, and TerminalBench, the strongest PrefixGuard monitors reach 0.900/0.710/0.533/0.557 AUPRC. Using the strongest backend within each representation, they improve over raw-text controls by an average of +0.137 AUPRC. LLM judges remain substantially weaker under the same prefix-warning protocol. We also derive an observability ceiling on score-based area under the precision-recall curve (AUPRC) that separates monitor error from failures lacking evidence in the observed prefix. For finite-state audit, post-hoc deterministic finite automaton (DFA) extraction remains compact on WebArena and $τ^2$-Bench (29 and 20 states) but expands to 151 and 187 states on SkillsBench and TerminalBench. Finally, first-alert diagnostics show that strong ranking does not imply deployment utility: WebArena ranks well yet fails to support low-false-alarm alerts, whereas $τ^2$-Bench and TerminalBench retain more actionable early alerts. Together, these results position PrefixGuard as a practical monitor-synthesis recipe with explicit diagnostics for when prefix warnings translate into actionable interventions.

</details>


### [123] [Beyond Task Success: Measuring Workflow Fidelity in LLM-Based Agentic Payment Systems](https://arxiv.org/abs/2605.06457)
*Donghao Huang,Joon Kiat Chua,Zhaoxia Wang*

Main category: cs.AI

TL;DR: The paper introduces Agentic Success Rate (ASR), a new trajectory-level metric for evaluating LLM-based multi-agent payment systems, revealing workflow deviations invisible to existing metrics like Task Success Rate and Agent Handoff F1-Score.


<details>
  <summary>Details</summary>
Motivation: Existing metrics for LLM-based multi-agent payment systems (TSR and HF1) only capture final outcomes or unordered routing decisions, missing critical workflow deviations and trajectory-level issues that are particularly important in regulated domains like payment processing.

Method: Introduces Agentic Success Rate (ASR) which compares observed and expected agent execution sequences at the transition level, decomposing performance into Transition Recall and Transition Precision. Applied the metric to Hierarchical Multi-Agent System for Payments (HMASP) across 18 LLMs and 90,000 task instances.

Result: ASR revealed that 10 of 18 models systematically skipped a confirmation checkpoint during payment checkout, a deviation invisible to TSR and HF1. GPT-4.1 showed hidden workflow shortcuts despite perfect TSR/HF1 scores, while GPT-5.2 achieved perfect ASR. Prompt refinements and deterministic routing guards based on ASR diagnostics yielded TSR improvements up to +93.8 percentage points.

Conclusion: Trajectory-level evaluation is essential in regulated domains, as ASR uncovers critical workflow deviations that traditional metrics miss, enabling substantial performance improvements through targeted refinements guided by transition-level diagnostics.

Abstract: LLM-based multi-agent systems are increasingly deployed for payment workflows, yet prevailing metrics, Task Success Rate (TSR) and Agent Handoff F1-Score (HF1), capture only final outcomes or unordered routing decisions. We introduce the Agentic Success Rate (ASR), a trajectory-fidelity metric that compares observed and expected agent execution sequences at the transition level, decomposing performance into Transition Recall and Transition Precision. Applied to the Hierarchical Multi-Agent System for Payments (HMASP) across 18 LLMs and 90,000 task instances, ASR reveals that 10 of 18 models systematically skip a confirmation checkpoint during payment checkout, a deviation invisible to both TSR and HF1, while 8 models enforce the checkpoint perfectly. Notably, GPT-4.1 exhibits hidden workflow shortcuts despite achieving perfect TSR and HF1, while GPT-5.2 achieves perfect ASR. Prompt refinements and deterministic routing guards guided by ASR diagnostics yield substantial TSR improvements, with gains up to +93.8 percentage points for previously struggling models, demonstrating that trajectory-level evaluation is essential in regulated domains.

</details>


### [124] [Probabilistic Dating of Historical Manuscripts via Evidential Deep Regression on Visual Script Features](https://arxiv.org/abs/2605.06475)
*Ranjith Chodavarapu*

Main category: cs.AI

TL;DR: A probabilistic deep learning model for continuous year dating of medieval manuscripts using visual features, providing uncertainty quantification with excellent accuracy (5.4 years MAE) and calibrated confidence intervals in a single forward pass.


<details>
  <summary>Details</summary>
Motivation: Current approaches for dating historical manuscripts typically aggregate centuries into broad classes, which loses temporal precision and ignores continuous dating needs. There's also a lack of uncertainty quantification in existing methods, which is crucial for reliable manuscript analysis where dating confidence matters.

Method: Uses an evidential deep regression approach with an EfficientNet-B2 backbone and Normal-Inverse-Gamma (NIG) output head. Trained with joint negative-log-likelihood and evidence-regularization objective to produce full predictive distributions with decomposed aleatoric and epistemic uncertainty in a single forward pass.

Result: Achieves test MAE of 5.4 years (well below 50-year century granularity), with 93% of patches within 5 years and 97% within 10 years. Best calibration (PICP=92.6%) outperforming MC Dropout (88.2%) and Deep Ensembles (79.7%) at 5× lower cost. Aleatoric uncertainty strongly predicts dating error (ρ=0.729), selective prediction yields 0.5 years MAE for most certain 20% patches, and page-level aggregation reduces MAE to 4.5 years.

Conclusion: The approach enables precise, continuous dating of historical manuscripts with calibrated uncertainty estimates, providing practical benefits for paleography and digital humanities. Uncertainty decomposition offers interpretability and selective prediction capabilities, making the method both accurate and reliable for real-world applications.

Abstract: We introduce a probabilistic approach for dating historical manuscript pages from visual features alone. Instead of aggregating centuries into classes as is standard in the previous literature, we pose dating as an evidential deep regression problem over a continuous year axis, allowing our neural network to output a full predictive distribution with decomposed aleatoric and epistemic uncertainty in a single forward pass. Our architecture combines an EfficientNet-B2 backbone with a Normal-Inverse-Gamma (NIG) output head trained with a joint negative-log-likelihood and evidence-regularization objective. On the DIVA-HisDB benchmark (150 pages, 3 medieval codices, 151,936 patches), our model scores a test MAE of 5.4 years, well below the 50-year century-label supervision granularity, with 93\% of patches within 5 years and 97\% within 10 years. Our approach achieves \textbf{PICP=92.6\%}, the best calibration among all compared methods, in a single forward pass, outperforming MC Dropout (PICP=88.2\%, 50 passes) and Deep Ensembles (PICP=79.7\%, 5 models) at $5\times$ lower inference cost. Uncertainty decomposition shows aleatoric uncertainty is a strong predictor of dating error (Spearman $ρ=0.729$), and a selective prediction about the most certain 20\% of patches can provide \textbf{0.5 years MAE}. We show that predicted uncertainty increases as image degradation worsens, spatial decomposition maps explain which script regions cause aleatoric uncertainty, and page-level aggregation reduces MAE to 4.5 years with $ρ=0.905$ between uncertainty and page-level error.

</details>


### [125] [Patch-Effect Graph Kernels for LLM Interpretability](https://arxiv.org/abs/2605.06480)
*Ruben Fernandez-Boullon,David N. Olivieri*

Main category: cs.AI

TL;DR: A framework that transforms mechanistic interpretability analyses into graph machine learning problems by representing activation-patching profiles as patch-effect graphs, enabling systematic comparison of circuit structures across prompts and tasks.


<details>
  <summary>Details</summary>
Motivation: Current mechanistic interpretability methods using activation patching produce high-dimensional, unstructured datasets that are difficult to compare systematically across diverse prompts and task families, creating a need for structured approaches to analyze causal circuits.

Method: Reframes mechanistic analysis as graph ML by converting activation-patching profiles into patch-effect graphs over model components using three construction methods: direct-influence (causal mediation), partial-correlation, and co-influence, then applying graph kernels to analyze structural patterns.

Result: Patch-effect graphs preserve discriminative structural signals in GPT-2 Small on IOI tasks, with localized edge-slot features outperforming global graph-shape descriptors for classification accuracy. Screened paired-patching validation shows CI and PC selected edges correspond to stronger activation-influence effects than random/low-rank candidates.

Conclusion: The framework provides a compression and evaluation pipeline for comparing patching-derived structures under controlled baselines, separating robust discriminative evidence from stronger task-general causal-circuit claims, while establishing explicit evidential scope through rigorous controls.

Abstract: Mechanistic interpretability aims to reverse-engineer transformer computations by identifying causal circuits through activation patching. However, scaling these interventions across diverse prompts and task families produces high-dimensional, unstructured datasets that are difficult to compare systematically. We propose a framework that reframes mechanistic analysis as a graph machine-learning problem by representing activation-patching profiles as patch-effect graphs over model components. We introduce three graph-construction methods: direct-influence via causal mediation, partial-correlation, and co-influence and apply graph kernels to analyze the resulting structures. Evaluating this approach on GPT-2 Small using Indirect Object Identification (IOI) and related tasks, we find that patch-effect graphs preserve discriminative structural signals. Specifically, localized edge-slot features provide higher classification accuracy than global graph-shape descriptors. A screened paired-patching validation suggests that CI and PC selected candidate edges correspond to stronger activation-influence effects than random or low-rank candidates. Crucially, by evaluating these representations against rigorous prompt-only and raw patch-effect controls, we make the evidential scope of the benchmark explicit: graph features compress structured patching signal, while raw tensors and surface cues define strong baselines that any circuit-level claim should address. Ultimately, our framework provides a compression and evaluation pipeline for comparing patching-derived structures under controlled baselines, separating robust slice-discriminative evidence from stronger task-general causal-circuit claims.

</details>


### [126] [ReasonSTL: Bridging Natural Language and Signal Temporal Logic via Tool-Augmented Process-Rewarded Learning](https://arxiv.org/abs/2605.06483)
*Bowen Ye,Zhijian Li,Junyue Huang,Junkai Ma,Xiang Yin*

Main category: cs.AI

TL;DR: ReasonSTL is a tool-augmented framework that adapts open-source language models for translating natural language requirements into Signal Temporal Logic (STL) formulas, providing a transparent, low-cost, privacy-preserving alternative to manual specification or commercial LLMs.


<details>
  <summary>Details</summary>
Motivation: Users often express requirements in natural language rather than structured STL formulas, but manual specification requires temporal-logic expertise and doesn't scale, while using commercial LLMs incurs high token costs and privacy concerns for sensitive industrial systems.

Method: ReasonSTL decomposes translation into explicit reasoning, deterministic tool calls, and structured formula construction, using process-rewarded training to supervise both tool-use trajectories and final formulas, and introduces STL-Bench, a bilingual, computation-aware benchmark.

Result: A 4B model trained with ReasonSTL achieves state-of-the-art performance in both automatic metrics and human evaluations, demonstrating superior natural-language-to-STL translation capabilities.

Conclusion: ReasonSTL provides an effective, transparent, low-cost, and privacy-preserving solution for formal specification drafting, making STL more accessible without compromising privacy or incurring high costs.

Abstract: Signal Temporal Logic (STL) is an expressive formal language for specifying spatio-temporal requirements over real-valued, real-time signals. It has been widely used for the verification and synthesis of autonomous systems and cyber-physical systems. In practice, however, users often express their requirements in natural language rather than in structured STL formulas, making natural-language-to-STL translation a critical yet challenging task. Manual specification requires temporal-logic expertise and cannot scale, while prompting commercial LLM APIs incurs substantial token costs and may expose sensitive system requirements to third-party services, raising privacy concerns for industrial deployment. To address these challenges, we present \textsc{ReasonSTL}, a tool-augmented framework that adapts local open-source language models for natural-language-to-STL generation. \textsc{ReasonSTL} decomposes the translation process into explicit reasoning, deterministic tool calls, and structured formula construction. We further introduce process-rewarded training to supervise both tool-use trajectories and final formulas, together with \textsc{STL-Bench}, a bilingual, computation-aware benchmark grounded in real-world signals. Experiments show that a 4B model trained with \textsc{ReasonSTL} achieves state-of-the-art performance in both automatic metrics and human evaluations, demonstrating that \textsc{ReasonSTL} provides a transparent, low-cost, and privacy-preserving alternative for formal specification drafting.

</details>


### [127] [Instrumental Choices: Measuring the Propensity of LLM Agents to Pursue Instrumental Behaviors](https://arxiv.org/abs/2605.06490)
*Jonas Wiedermann-Möller,Leonard Dung,Maksym Andriushchenko*

Main category: cs.AI

TL;DR: AI models sometimes violate instructions to achieve goals through instrumental convergence behaviors like self-preservation, with 5.1% IC rate in tested terminal-based agents.


<details>
  <summary>Details</summary>
Motivation: AI systems show dangerous behaviors raising concerns about whether models choose to violate human instructions to pursue instrumental goals that could lead to risks from highly capable agents.

Method: Created a benchmark with seven operational tasks each having official workflows and policy-violating shortcuts, using eight-variant framework varying monitoring, instruction clarity, stakes, permission, etc. Evaluated ten models with 1,680 samples using deterministic environment-state scorers and trace review.

Result: Overall IC rate of 5.1% (86/1680 samples), concentrated in two Gemini models (66.3% of cases) and three tasks (84.9%). Conditions where IC is indispensable for task success show greatest increase (+15.7 percentage points) in adjusted IC rate.

Conclusion: Realistic, low-nudge environments elicit IC behavior rarely but systematically in most tested models. It's feasible to robustly measure dangerous behavior tendencies in current frontier AI agents.

Abstract: AI systems have become increasingly capable of dangerous behaviours in many domains. This raises the question: Do models sometimes choose to violate human instructions in order to perform behaviour that is more useful for certain goals? We introduce a benchmark for measuring model propensity for instrumental convergence (IC) behaviour in terminal-based agents. This is behaviour such as self-preservation that has been hypothesised to play a key role in risks from highly capable AI agents. Our benchmark is realistic and low-stakes which serves to reduce evaluation-awareness and roleplay confounds. The suite contains seven operational tasks, each with an official workflow and a policy-violating shortcut. An eight-variant shared framework varies monitoring, instruction clarity, stakes, permission, instrumental usefulness and blocked honest paths to support inferences regarding the factors driving IC behaviour. We evaluated ten models using deterministic environment-state scorers over 1,680 samples, with trace review employed for audit and adjudication purposes. The final IC rate is 86 out of 1,680 samples (5.1%). IC behaviour is concentrated rather than uniform: two Gemini models account for 66.3% of IC cases and three tasks account for 84.9%. Conditions in which IC behaviour is indispensable for task success result in the greatest increase in the adjusted IC rate (+15.7 percentage points), whereas emphasising that task success is critical or certain framing choices do not produce comparable effects. Our findings indicate that realistic, low-nudge environments elicit IC behaviour rarely but systematically in most tested models. We conclude that it is feasible to robustly measure tendencies for dangerous behaviour in current frontier AI agents.

</details>


### [128] [From Token Lists to Graph Motifs: Weisfeiler-Lehman Analysis of Sparse Autoencoder Features](https://arxiv.org/abs/2605.06494)
*Ruben Fernandez-Boullon,Pablo Magariños-Docampo,Javier Perez-Robles*

Main category: cs.AI

TL;DR: This paper introduces a graph-structured representation for sparse autoencoder (SAE) features, modeling each feature as a token co-occurrence graph and using graph kernels to analyze higher-order structural relationships between features, revealing motif families not captured by traditional cosine similarity methods.


<details>
  <summary>Details</summary>
Motivation: Current analyses of SAE features in mechanistic interpretability focus almost exclusively on top-activating token lists or decoder weight vectors, leaving the higher-order co-occurrence structure across features largely unexamined. There's a need for methods that capture these structural relationships between features.

Method: Introduces a graph-structured representation where each SAE feature is modeled as a token co-occurrence graph with nodes as frequent tokens near strong activations and edges connecting pairs that co-occur within local context windows. Uses a custom WL-style, frequency-binned graph kernel to measure similarity across this structural space.

Result: Applied to features from a large SAE trained on GPT-2 Small with synthetic mixed-domain corpus, clustering based on graph similarity recovers heuristic motif families (punctuation-heavy patterns, language/script clusters, code-like templates) not recovered by decoder cosine similarity clustering. Graph view surfaces structural relationships that token-frequency and decoder-weight views alone don't capture.

Conclusion: The graph-structured approach provides a complementary view to existing SAE feature analysis methods, capturing higher-order co-occurrence structure that traditional methods miss. While token-histogram baselines achieve higher purity, the graph representation offers valuable structural insights with stable cluster assignments across hyperparameters and random seeds.

Abstract: Sparse autoencoders (SAEs) have become central to mechanistic interpretability, decomposing transformer activations into monosemantic features. Yet existing analyses characterise features almost exclusively through top-activating token lists or decoder weight vectors, leaving the higher-order co-occurrence structure shared across features largely unexamined. We introduce a graph-structured representation in which each SAE feature is modelled as a token co-occurrence graph: nodes are the tokens most frequent near strong activations, and edges connect pairs that co-occur within local context windows. A custom WL-style, frequency-binned graph kernel then provides a similarity measure over this structural space. Applied as a proof of concept to features from a large SAE trained on GPT-2 Small and probed with a synthetic mixed-domain corpus, our clustering recovers heuristic motif families (punctuation-heavy patterns, language and script clusters, and code-like templates) that are not recovered by clustering on decoder cosine similarity. A token-histogram baseline achieves higher overall purity, so the contribution of the graph view is complementary rather than dominant: it surfaces structural relationships that token-frequency and decoder-weight views alone do not capture. Cluster assignments are stable across graph-construction hyperparameters and random seeds.

</details>


### [129] [Process Matters more than Output for Distinguishing Humans from Machines](https://arxiv.org/abs/2605.06524)
*Milena Rmus,Mathew D. Hardy,Thomas L. Griffiths,Mayank Agrawal*

Main category: cs.AI

TL;DR: CogCAPTCHA30 uses 30 cognitive tasks with process-level features to distinguish humans from AI agents, even when task performance is matched, achieving mean AUC of 0.88.


<details>
  <summary>Details</summary>
Motivation: Reliable human-machine discrimination is crucial as AI systems become more prevalent online. Traditional approaches focus on output similarity, but cognitive science suggests evaluating the underlying cognitive processes could provide better discrimination.

Method: Developed CogCAPTCHA30 battery with 30 cognitive tasks designed to elicit diagnostic process-level features. Tested various AI systems: frontier agents (Claude, GPT-5, Gemini), a Centaur model fine-tuned on human decisions, and task-specific fine-tuned Qwen2.5 models with action-level (A-SFT) and process-level (P-SFT) supervision.

Result: Process-level features provided stronger discriminative signal than performance metrics alone (mean AUC = 0.88). Broad fine-tuning on human decisions improved human-like processes, and process-level supervision further enhanced behavioral mimicry, but advantages diminished under cross-task transfer.

Conclusion: Explicit process-level supervision can improve human behavioral mimicry, but only with appropriate task-specific process representations, highlighting process specification as the key bottleneck for achieving human-like cognitive processes in machines.

Abstract: Reliable human-machine discrimination is becoming increasingly important as large language models and autonomous agents are deployed in online settings. Existing approaches evaluate whether a system can produce behavior or responses indistinguishable from those of a human, following the emphasis on outputs as a criterion for intelligence proposed by Alan Turing. Cognitive science offers an alternative perspective: evaluating the process by which behavior is produced. To test whether cognitive processes can reliably distinguish humans from machines, we introduce CogCAPTCHA30, a battery of 30 cognitive tasks designed to elicit diagnostic process-level features even when task performance is matched. Across the battery, process-level features provide stronger discriminative signal than performance metrics alone, reliably distinguishing humans from agents even under output matching (mean process-feature classifier AUC = 0.88). To evaluate agentic process differences, we compare off-the-shelf frontier agents (Claude Sonnet 4.5, GPT-5, Gemini 2.5 Pro), Centaur (a language model fine-tuned on 10.7M human decisions), and two task-specific fine-tuning approaches applied to Qwen2.5-1.5B-Instruct: action-level supervised fine-tuning (A-SFT) and process-level fine-tuning (P-SFT), which directly optimizes process features. Broad fine-tuning on human decisions improves human-like task processes relative to off-the-shelf agents, while task-specific process-level supervision further improves behavioral mimicry. However, this advantage diminishes under cross-task transfer when supervised process targets do not naturally generalize across tasks. Explicit process-level supervision can improve human behavioral mimicry, but only if appropriate task-specific process representations are available, highlighting process specification as a bottleneck for achieving human-like cognitive processes in machines.

</details>


### [130] [Market-Alignment Risk in Pricing Agents: Trace Diagnostics and Trace-Prior RL under Hidden Competitor State](https://arxiv.org/abs/2605.06529)
*Peiying Zhu,Sidi Chang*

Main category: cs.AI

TL;DR: Standard RL agents can achieve good scalar metrics (RevPAR) while learning wrong behavior (poor yield management) - a Goodhart failure under partial observability. Fix with Trace-Prior RL: learn market distribution from traces, then train with RevPAR reward plus KL penalty to prior.


<details>
  <summary>Details</summary>
Motivation: Scalar outcome metrics like RevPAR can be gamed by agents, certifying wrong behavior. In revenue management with partial observability (Hotel A can't see competitor's inventory/pricing rules), agents can achieve near-reference RevPAR while exhibiting market-damaging behaviors like aggressive discounting or price collapse.

Method: Diagnose using trace-level protocol with multiple metrics (RevPAR, occupancy, ADR, price distributions, distances, confidence intervals). Repair with Trace-Prior RL: 1) Learn distributional market prior from lagged market traces, 2) Train stochastic pricing policy with RevPAR reward + KL penalty to learned prior.

Result: Trace-Prior RL policies match Hotel B's key metrics (RevPAR, occupancy, ADR, price distribution) within statistical uncertainty while optimizing Hotel A's own reward. Higher action accuracy can worsen trace alignment when target is distributional.

Conclusion: Main contribution is not a new optimizer or benchmark, but a reproducible failure-and-repair recipe for agentic systems where scalar rewards are easy to game and intended behavior is only visible in traces. Distributional regularization via learned priors from market traces prevents Goodhart failures under partial observability.

Abstract: Outcome metrics can certify the wrong behavior. We study this failure in a two-hotel revenue-management simulator where Hotel A trains an agent against a fixed rule-based revenue-management competitor, Hotel B. A standard learning agent can obtain near-reference revenue per available room (RevPAR) while failing to learn market-like yield management: it sells too aggressively, undercuts, or collapses to modal price buckets. We diagnose this as a Goodhart-style failure under partial observability. Hotel A cannot observe the competitor's remaining inventory, booking curve, or pricing rule, so the same Hotel A-visible state maps to multiple plausible Hotel B prices. Deterministic value-based RL and deterministic copying collapse this unresolved uncertainty into shortcut behavior. We introduce a trace-level diagnostic protocol using RevPAR, occupancy, ADR, full price-bucket distributions, L1/JS distances, and seed-level confidence intervals. The verified repair is Trace-Prior RL: learn a distributional market prior from lagged market traces, then train a stochastic pricing policy with a RevPAR reward and a KL penalty to the learned prior. The final policy matches Hotel B's RevPAR, occupancy, ADR, and price distribution within seed-level uncertainty, while still optimizing Hotel A's own reward. We argue that the contribution is not a new optimizer and not a hotel-pricing leaderboard, but a reproducible failure-and-repair recipe for agentic systems where scalar rewards are easy to game and the intended behavior is only visible in traces. A key finding is that higher exact action accuracy can worsen aggregate trace alignment when the target is distributional.

</details>


### [131] [SpatialEpiBench: Benchmarking Spatial Information and Epidemic Priors in Forecasting](https://arxiv.org/abs/2605.06530)
*Ruiqi Lyu,Alistair Turcan,Bryan Wilder*

Main category: cs.AI

TL;DR: SpatialEpiBench is a new challenging benchmark for spatiotemporal epidemic forecasting that reveals current methods underperform simple baselines and identifies three key failure modes in real-world public health scenarios.


<details>
  <summary>Details</summary>
Motivation: Epidemic forecasting is crucial for public health but difficult due to sparse, noisy, non-stationary data. While spatiotemporal methods seem promising, there's no standardized benchmark, and current evaluations don't reflect real-time forecasting practice, creating a gap in assessing model utility for operational public health.

Method: The authors developed SpatialEpiBench - a benchmark with 11 epidemic datasets, standardized rolling evaluations (instead of simple chronological splits), and outbreak-specific metrics. They evaluated adjacency-informed forecasting models with epidemiological priors against a simple last-value baseline across different forecast horizons (1 day to 1 month).

Result: Most spatiotemporal forecasting methods underperformed a simple last-value baseline across all forecast horizons, even during outbreaks and despite using epidemiological priors. The benchmark revealed three major failure modes: poor outbreak anticipation, difficulty handling sparsity and noise, and limited utility of geographic adjacency for capturing epidemiological spatial relationships.

Conclusion: Current spatiotemporal epidemic forecasting methods have significant limitations in practical public health settings. The SpatialEpiBench provides a realistic evaluation framework to support development of operationally useful models, highlighting that fundamental methodological improvements are needed rather than just applying general spatiotemporal models with epidemiological priors.

Abstract: Accurate epidemic forecasting is crucial for public health response, resource allocation, and outbreak intervention, but remains difficult with sparse, noisy, and highly non-stationary data. Because epidemics unfold across interacting regions, spatiotemporal methods are natural candidates for improving forecasts. Despite growing interest in spatial information, no standardized benchmark exists, and current evaluations often use simple chronological train-test splits that do not reflect real-time forecasting practice. We address this gap with SpatialEpiBench, a challenging benchmark for spatiotemporal epidemic forecasting in realistic public-health settings. SpatialEpiBench includes 11 epidemic datasets with standardized rolling evaluations and outbreak-specific metrics. We evaluate adjacency-informed forecasting models with widely used epidemic priors that adapt general models to epidemiology, but find that most methods underperform a simple last-value baseline from 1 day to 1 month ahead, even during outbreaks and with these priors. We identify three major failure modes: (1) poor outbreak anticipation, (2) difficulty handling sparsity and noise, and (3) limited utility of common geographic adjacency for epidemiological spatial information. We release benchmark data, code, and instructions at https://github.com/Rachel-Lyu/SpatialEpiBench to support development of operationally useful epidemic forecasting models.

</details>


### [132] [Ex Ante Evaluation of AI-Induced Idea Diversity Collapse](https://arxiv.org/abs/2605.06540)
*Nafis Saami Azad,Raiyan Abdul Baten*

Main category: cs.AI

TL;DR: AI can improve individual creative outputs but may reduce population-level diversity through excessive similarity, creating a crowding problem that needs specific evaluation metrics and targeted design solutions.


<details>
  <summary>Details</summary>
Motivation: Current AI evaluation focuses on individual utility, but creative outputs lose value when many are similar. There's a blind spot where AI can improve individual outputs while increasing population-level crowding, potentially causing diversity collapse.

Method: Introduced a human-relative framework for benchmarking AI-induced diversity collapse without human-AI interaction data. Modeled ideas as congestible resources, showing source-level crowding is identifiable from within-distribution comparisons using excess-crowding coefficient Δ and human-relative diversity ratio ρ. Used an adoption game with exposure-dependent redundancy costs and tested across short stories, marketing slogans, and alternative-uses tasks with three frontier LLMs.

Result: Three frontier LLMs fell below the no-excess-crowding parity condition (ρ≥1) across different crowding kernels. Estimates stabilized with feasible model-only sample sizes. Generation-protocol variants showed that crowding can be reduced through targeted design, making diversity collapse actionable at development time.

Conclusion: AI-induced human diversity collapse is a measurable risk that can be evaluated ex ante using model-only generations. While current LLMs increase population-level crowding, targeted design interventions can mitigate this effect, making diversity an actionable evaluation target for population-aware creative AI systems.

Abstract: Creative AI systems are typically evaluated at the level of individual utility, yet creative outputs are consumed in populations: an idea loses value when many others produce similar ones. This creates an evaluation blind spot, as AI can improve individual outputs while increasing population-level crowding. We introduce a human-relative framework for benchmarking AI-induced human diversity collapse without requiring human-AI interaction data, providing an ex ante protocol to estimate crowding risk from model-only generations and matched unaided human baselines. By modeling ideas as congestible resources, we show that source-level crowding is identifiable from within-distribution comparisons, yielding an excess-crowding coefficient $Δ$ and a human-relative diversity ratio $ρ$. We show that $ρ\ge1$ is the no-excess-crowding parity condition and connect $Δ$ to an adoption game with exposure-dependent redundancy costs. Across short stories, marketing slogans, and alternative-uses tasks, three frontier LLMs fall below parity across crowding kernels. Estimates stabilize with feasible model-only sample sizes. Importantly, generation-protocol variants show that crowding can be reduced through targeted design, making diversity collapse an actionable, development-time evaluation target for population-aware creative AI.

</details>


### [133] [Improved techniques for fine-tuning flow models via adjoint matching: a deterministic control pipeline](https://arxiv.org/abs/2605.06583)
*Zhengyi Guo,Jiayuan Sheng,David D. Yao,Wenpin Tang*

Main category: cs.AI

TL;DR: A deterministic adjoint matching framework for flow-based generative model alignment via optimal control over velocity fields with computational efficiency and flexible regularization.


<details>
  <summary>Details</summary>
Motivation: Traditional human preference alignment methods for flow-based generative models need improvement in stability, computational efficiency, and flexibility of regularization.

Method: Formulates alignment as optimal control over velocity fields using deterministic adjoint matching; introduces truncated adjoint scheme focusing computation on terminal trajectory portions; allows flexible trade-offs beyond standard KL regularization.

Result: Experiments on SiT-XL/2 and FLUX.2-Klein-4B show consistent gains across alignment metrics, with substantially improved diversity and mode preservation, while achieving computational savings.

Conclusion: The proposed deterministic adjoint matching framework provides a stable, computationally efficient, and flexible approach for human preference alignment in flow-based generative models.

Abstract: We propose a deterministic adjoint matching framework that formulates human preference alignment for flow-based generative models as an optimal control problem over velocity fields. One can directly regress the control toward a value-gradient-induced target under the current policy, leading to a simple and stable training objective. Building on this perspective, we introduce a truncated adjoint scheme that focuses computation on the terminal portion of the trajectory, where reward-relevant signals concentrate, which yields substantial computational savings while preserving alignment quality. We further generalize the framework beyond standard KL-based regularization, allowing more flexible trade-offs between alignment strength and distributional preservation. Experiments on SiT-XL/2 and FLUX.2-Klein-4B demonstrate consistent gains across multiple alignment metrics, along with substantially improved diversity and mode preservation.

</details>


### [134] [NeuroAgent: LLM Agents for Multimodal Neuroimaging Analysis and Research](https://arxiv.org/abs/2605.06584)
*Lujia Zhong,Yihao Xia,Jianwei Zhang,Shuo huang,Jiaxin Yue,Mingyang Xia,Yonggang Shi*

Main category: cs.AI

TL;DR: NeuroAgent is an LLM-driven framework that automates preprocessing and analysis of multimodal neuroimaging data (sMRI, fMRI, dMRI, PET) using a hierarchical multi-agent architecture with Generate-Execute-Validate engine, achieving high preprocessing accuracy and strong disease classification performance.


<details>
  <summary>Details</summary>
Motivation: Multimodal neuroimaging analysis involves complex, modality-specific preprocessing workflows requiring careful configuration, quality control, and coordination across heterogeneous toolchains. Downstream analysis also requires task-specific code and conventions, creating barriers between raw data and reproducible scientific analysis.

Method: Hierarchical multi-agent architecture with feedback-driven Generate-Execute-Validate engine. Agents autonomously generate executable preprocessing code, detect/recover from runtime errors, and validate output integrity. Human-In-The-Loop interface for edge cases. Evaluated on 1,470 ADNI subjects across modalities.

Result: Qwen3.5-27B backend achieves 84.8% end-to-end preprocessing step correctness and 100% intent-parsing accuracy. For Alzheimer's Disease classification, agent ensemble achieves AUC of 0.9518 with four modalities, outperforming all single-modality baselines. Automated recovery minimizes manual intervention.

Conclusion: NeuroAgent can significantly reduce manual effort for neuroimaging preprocessing and enable end-to-end automated analysis pipelines for neuroimaging research, demonstrating the potential of LLM-driven agents for complex scientific workflows.

Abstract: Multimodal neuroimaging analysis often involves complex, modality-specific preprocessing workflows that require careful configuration, quality control, and coordination across heterogeneous toolchains. Beyond preprocessing, downstream statistical analysis and disease classification commonly require task-specific code, evaluation protocols, and data-format conventions, creating additional barriers between raw acquisitions and reproducible scientific analysis. We present NeuroAgent, an LLM-driven agentic framework that automates key preprocessing and analysis steps for heterogeneous neuroimaging data, including sMRI, fMRI, dMRI, and PET, and supports interactive downstream analysis through natural-language queries. NeuroAgent employs a hierarchical multi-agent architecture with a feedback-driven Generate-Execute-Validate engine: agents autonomously generate executable preprocessing code, detect and recover from runtime errors, and validate output integrity. We evaluate the system on 1,470 subjects pooled across all ADNI phases (CN=1,000, AD=470), where all subjects have sMRI and tabular data, with subsets also having Tau-PET (n=469), fMRI (n=278), and DTI ($n=620$). Pipeline ablation studies across multiple LLM backends show that capable models reach up to 100% intent-parsing accuracy, with the strongest backend (Qwen3.5-27B) reaching 84.8% end-to-end preprocessing step correctness. Automated recovery limits manual intervention to edge cases where human review is required via the Human-In-The-Loop interface. For Alzheimer's Disease classification using automatically preprocessed multimodal data, our agent ensemble achieves an AUC of 0.9518 with four modalities, outperforming all single-modality baselines. These results show that NeuroAgent can reduce the manual effort required for neuroimaging preprocessing and enable end-to-end automated analysis pipelines for neuroimaging research.

</details>


### [135] [SkillOS: Learning Skill Curation for Self-Evolving Agents](https://arxiv.org/abs/2605.06614)
*Siru Ouyang,Jun Yan,Yanfei Chen,Rujun Han,Zifeng Wang,Bhavana Dalvi Mishra,Rui Meng,Chun-Liang Li,Yizhu Jiao,Kaiwen Zha,Maohao Shen,Vishy Tirumalashetty,George Lee,Jiawei Han,Tomas Pfister,Chen-Yu Lee*

Main category: cs.AI

TL;DR: SkillOS is an RL-based training framework that enables LLM agents to learn long-term skill curation policies from experience, allowing them to self-evolve by building and improving reusable skills over time.


<details>
  <summary>Details</summary>
Motivation: Current LLM-based agents deployed for streaming tasks fail to learn from past interactions and remain one-off problem solvers. While reusable skills offer a path to self-evolution, existing approaches struggle with complex long-term curation policies due to manual curation, heuristic operations, or short-horizon training that can't handle delayed feedback.

Method: SkillOS pairs a frozen agent executor (retrieves/applies skills) with a trainable skill curator that updates an external SkillRepo from experience. It uses composite rewards and trains on grouped task streams based on skill-relevant dependencies. Early trajectories update the SkillRepo, and later related tasks evaluate these updates.

Result: SkillOS outperforms memory-free and strong memory-based baselines across multi-turn agentic and single-turn reasoning tasks in both effectiveness and efficiency. The learned curator generalizes across different executor backbones and task domains. Skills evolve into richer Markdown files encoding higher-level meta-skills over time.

Conclusion: Experience-driven RL training enables effective learning of complex skill curation policies, leading to self-evolving agents that build reusable, structured skills and show improved task performance across diverse domains through targeted skill use.

Abstract: LLM-based agents are increasingly deployed to handle streaming tasks, yet they often remain one-off problem solvers that fail to learn from past interactions. Reusable skills distilled from experience provide a natural substrate for self-evolution, where high-quality skill curation serves as the key bottleneck. Existing approaches either rely on manual skill curation, prescribe heuristic skill operations, or train for short-horizon skill operations. However, they still struggle to learn complex long-term curation policies from indirect and delayed feedback. To tackle this challenge, we propose SkillOS, an experience-driven RL training recipe for learning skill curation in self-evolving agents. SkillOS pairs a frozen agent executor that retrieves and applies skills with a trainable skill curator that updates an external SkillRepo from accumulated experience. To provide learning signals for curation, we design composite rewards and train on grouped task streams based on skill-relevant task dependencies, where earlier trajectories update the SkillRepo, and later related tasks evaluate these updates. Across multi-turn agentic tasks and single-turn reasoning tasks, SkillOS consistently outperforms memory-free and strong memory-based baselines in both effectiveness and efficiency, with the learned skill curator generalizing across different executor backbones and task domains. Further analyses show that the learned curator produces more targeted skill use, while the skills in SkillRepo evolve into more richly structured Markdown files that encode higher-level meta-skills over time.

</details>


### [136] [MASPO: Joint Prompt Optimization for LLM-based Multi-Agent Systems](https://arxiv.org/abs/2605.06623)
*Zhexuan Wang,Xuebo Liu,Li Wang,Zifei Shan,Yutong Wang,Zhenxi Song,Min Zhang*

Main category: cs.AI

TL;DR: MASPO is a framework for automatically optimizing prompts across multi-agent LLM systems by jointly evaluating prompts based on downstream agent success rather than local validity alone.


<details>
  <summary>Details</summary>
Motivation: Prompt quality is crucial in LLM-based multi-agent systems, but jointly optimizing prompts across interacting agents is challenging due to misalignment between local agent objectives and holistic system goals.

Method: Introduces MASPO framework with joint evaluation mechanism that assesses prompts by their capacity to facilitate downstream success for successor agents, and employs data-driven evolutionary beam search to navigate high-dimensional prompt space.

Result: Extensive empirical evaluations across 6 diverse tasks show MASPO consistently outperforms state-of-the-art prompt optimization methods, achieving an average accuracy improvement of 2.9.

Conclusion: MASPO effectively bridges the gap between local interactions and global outcomes in multi-agent LLM systems without relying on ground-truth labels, providing a practical solution for prompt optimization in collaborative agent settings.

Abstract: Large language model (LLM)-based Multi-agent systems (MAS) have shown promise in tackling complex collaborative tasks, where agents are typically orchestrated via role-specific prompts. While the quality of these prompts is pivotal, jointly optimizing them across interacting agents remains a non-trivial challenge, primarily due to the misalignment between local agent objectives and holistic system goals. To address this, we introduce MASPO, a novel framework designed to automatically and iteratively refine prompts across the entire system. A core innovation of MASPO is its joint evaluation mechanism, which assesses prompts not merely by their local validity, but by their capacity to facilitate downstream success for successor agents. This effectively bridges the gap between local interactions and global outcomes without relying on ground-truth labels. Furthermore, MASPO employs a data-driven evolutionary beam search to efficiently navigate the high-dimensional prompt space. Extensive empirical evaluations across 6 diverse tasks demonstrate that MASPO consistently outperforms state-of-the-art prompt optimization methods, achieving an average accuracy improvement of 2.9. We release our code at https://github.com/wangzx1219/MASPO.

</details>


### [137] [Can RL Teach Long-Horizon Reasoning to LLMs? Expressiveness Is Key](https://arxiv.org/abs/2605.06638)
*Tianle Wang,Zhaoyang Wang,Guangchen Lan,Xinpeng Wei,Sipeng Zhang,Guanwen Qiu,Abulhair Saparov*

Main category: cs.AI

TL;DR: The paper introduces ScaleLogic, a synthetic framework for studying how RL training for LLM reasoning scales with task difficulty, showing power-law scaling with reasoning depth and that training on more expressive logics leads to better downstream transfer.


<details>
  <summary>Details</summary>
Motivation: There's a lack of controlled, scalable environments to systematically study how reinforcement learning training scales with task difficulty for improving large language model reasoning, which hampers understanding of training efficiency and transfer learning properties.

Method: The authors introduce ScaleLogic, a synthetic logical reasoning framework that allows independent control over two axes of difficulty: reasoning depth (horizon) and logical expressiveness. The framework supports various logics from simple implication-only to first-order reasoning with conjunction, disjunction, negation, and universal quantification.

Result: 1) RL training compute follows a power law with reasoning depth (T ∝ D^γ, R² > 0.99). 2) The scaling exponent γ increases monotonically with logical expressiveness (from 1.04 to 2.60). 3) More expressive training yields larger performance gains on downstream benchmarks (up to +10.66 points) and more compute-efficient transfer. 4) The power-law relationship holds across multiple RL methods, and curriculum-based training improves scaling efficiency.

Conclusion: What a model is trained on (logical expressiveness) shapes downstream transfer, not just how much it is trained, and systematic study reveals power-law scaling relationships that can guide more efficient training for LLM reasoning.

Abstract: Reinforcement learning (RL) has been applied to improve large language model (LLM) reasoning, yet the systematic study of how training scales with task difficulty has been hampered by the lack of controlled, scalable environments. We introduce ScaleLogic, a synthetic logical reasoning framework that offers independent control over two axes of difficulty: the depth of the required proof planning (i.e., the horizon) and the expressiveness of the underlying logic. Our proposed framework supports a wide range of logics: from simple implication-only logic ("if-then") towards more expressive first-order reasoning with conjunction ("and"), disjunction ("or"), negation ("not"), and universal quantification ("for all"). Using this framework, we show that the RL training compute $T$ follows a power law with respect to reasoning depth $D$ ($T \propto D^γ$, $R^{2} > 0.99$), and that the scaling exponent $γ$ increases monotonically with logical expressiveness, from $1.04$ to $2.60$. On downstream mathematics and general reasoning benchmarks, more expressive training settings yield both larger performance gains (up to $+10.66$ points) and more compute-efficient transfer compared to less expressive settings, demonstrating that what a model is trained on, not just how much it is trained, shapes downstream transfer. We further show that the power-law relationship holds across multiple RL methods, and curriculum-based training substantially improves scaling efficiency.

</details>


### [138] [GlazyBench: A Benchmark for Ceramic Glaze Property Prediction and Image Generation](https://arxiv.org/abs/2605.06641)
*Ziyu Zhai,Siyou Li,Juexi Shao,Juntao Yu*

Main category: cs.AI

TL;DR: GlazyBench is the first AI dataset for ceramic glaze design, featuring 23,148 real formulations with tasks for predicting post-firing properties from raw materials and generating visual representations, establishing ML and multimodal baselines.


<details>
  <summary>Details</summary>
Motivation: Developing ceramic glazes is costly and time-consuming due to complex chemistry, which burdens independent artists. Recent multimodal AI offers solutions but lacks the large-scale datasets needed to train such models.

Method: Created GlazyBench dataset with 23,148 real glaze formulations. Established baselines for two tasks: 1) predicting post-firing surface properties (color, transparency) from raw materials using traditional ML and large language models, 2) generating visual representations using deep generative and large multimodal models.

Result: Experiments demonstrate promising but challenging results. The dataset enables systematic evaluation of AI models for glaze property prediction and visual representation generation.

Conclusion: GlazyBench pioneers AI-assisted material design research, providing a standardized benchmark for systematic evaluation and addressing the data gap in ceramic glaze development.

Abstract: Developing ceramic glazes is a costly, time-consuming process of trial and error due to complex chemistry, placing a significant burden on independent artists. While recent advances in multimodal AI offer a modern solution, the field lacks the large-scale datasets required to train these models. We propose GlazyBench, the first dataset for AI-assisted glaze design. Comprising 23,148 real glaze formulations, GlazyBench supports two primary tasks: predicting post-firing surface properties, such as color and transparency, from raw materials, and generating accurate visual representations of the glaze based on these properties. We establish comprehensive baselines for property prediction using traditional machine learning and large language models, alongside image generation benchmarks using deep generative and large multimodal models. Our experiments demonstrate promising yet challenging results. GlazyBench pioneers a new research direction in AI-assisted material design, providing a standardized benchmark for systematic evaluation.

</details>


### [139] [AI Co-Mathematician: Accelerating Mathematicians with Agentic AI](https://arxiv.org/abs/2605.06651)
*Daniel Zheng,Ingrid von Glehn,Yori Zwols,Iuliya Beloshapka,Lars Buesing,Daniel M. Roy,Martin Wattenberg,Bogdan Georgiev,Tatiana Schmidt,Andrew Cowie,Fernanda Viegas,Dimitri Kanevsky,Vineet Kahlon,Hartmut Maennel,Sophia Alj,George Holland,Alex Davies,Pushmeet Kohli*

Main category: cs.AI

TL;DR: AI co-mathematician system provides interactive workspace for mathematicians to leverage AI agents for mathematical research, achieving state-of-the-art results on benchmarks.


<details>
  <summary>Details</summary>
Motivation: To create a system that supports the exploratory and iterative nature of mathematical workflows, addressing the need for holistic AI assistance in mathematical research including ideation, literature search, computational exploration, theorem proving, and theory building.

Method: Developed an asynchronous, stateful workspace that manages uncertainty, refines user intent, tracks failed hypotheses, and outputs native mathematical artifacts, mirroring human collaborative workflows with interactive AI agents.

Result: In early tests, helped researchers solve open problems, identify new research directions, and uncover overlooked literature references. Achieved state-of-the-art performance with 48% score on FrontierMath Tier 4 benchmark, the highest among all evaluated AI systems.

Conclusion: The AI co-mathematician demonstrates a highly interactive paradigm for AI-assisted mathematical discovery that effectively supports real mathematical workflows while achieving top performance on hard problem-solving benchmarks.

Abstract: We introduce the AI co-mathematician, a workbench for mathematicians to interactively leverage AI agents to pursue open-ended research. The AI co-mathematician is optimized to provide holistic support for the exploratory and iterative reality of mathematical workflows, including ideation, literature search, computational exploration, theorem proving and theory building. By providing an asynchronous, stateful workspace that manages uncertainty, refines user intent, tracks failed hypotheses, and outputs native mathematical artifacts, the system mirrors human collaborative workflows. In early tests, the AI co-mathematician helped researchers solve open problems, identify new research directions, and uncover overlooked literature references. Besides demonstrating a highly interactive paradigm for AI-assisted mathematical discovery, the AI co-mathematician also achieves state of the art results on hard problem-solving benchmarks, including scoring 48% on FrontierMath Tier 4, a new high score among all AI systems evaluated.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [140] [Are Flat Minima an Illusion?](https://arxiv.org/abs/2605.05209)
*Michael Timothy Bennett*

Main category: cs.LG

TL;DR: The paper challenges the flat minima hypothesis for generalization, showing that sharpness is not fundamental but reparameterization-dependent, and instead proposes "weakness" (volume of completions) as a reparameterization-invariant predictor of generalization.


<details>
  <summary>Details</summary>
Motivation: The motivation is to challenge the widely-held belief that flat minima are causally linked to better generalization. The author shows that sharpness can be artificially inflated through function-preserving reparameterizations without changing predictions, suggesting sharpness is not fundamental to generalization.

Method: The author introduces the concept of "weakness" - the volume of completions compatible with the learned function in the learner's embodied language. Weakness is reparameterization-invariant since it's defined by what the network does, not how it's parameterized. Theoretical analysis proves weakness is minimax-optimal under exchangeable demands, and shows why PAC-Bayes bounds correlate with it. Empirical validation involves training 100 identical networks on MNIST and Fashion-MNIST, comparing weakness, sharpness, and simplicity as predictors of generalization.

Result: Weakness strongly predicts generalization on MNIST (ρ = +0.374, p = 0.00012) and Fashion-MNIST (ρ = +0.384, p = 8.15×10⁻⁵). Sharpness anticorrelates with generalization, while simplicity shows no predictive power on MNIST (p = 0.848) and only moderate prediction on Fashion-MNIST. The large-batch generalization advantage vanishes as training data grows, showing sharpness is a confounder rather than a cause.

Conclusion: Flat minima are not fundamental to generalization - they're artifacts of parameterization. Weakness, defined as the volume of completions compatible with the learned function, is the actual reparameterization-invariant driver of generalization. Weakness is dataset-invariant while simplicity is dataset-dependent, making weakness a more fundamental predictor of neural network generalization.

Abstract: Neural networks that land in flat regions of the loss landscape tend to generalise better than those in sharp regions. Sharpness-Aware Minimisation exploits this to improve generalisation. But function-preserving reparameterisation can inflate the Hessian of any minimum by two orders of magnitude without changing a single prediction. If the geometry of weight space can be manufactured from nothing, it cannot be the cause of anything. In other words, flat is simple and simplicity depends on encoding. Here I show that the actual driver is weakness, the volume of completions compatible with the learned function in the learner's embodied language. Weakness is reparameterisation-invariant because it is defined over what the network \emph{does}, not how it is parameterised. I prove weakness is minimax-optimal under exchangeable demands, and that PAC-Bayes bounds work because they correlate with it. On MNIST, the large-batch generalisation advantage \emph{vanishes} as training data grows, from $+1.6\%$ at $n = 2{,}000$ to $+0.02\%$ at $n = 60{,}000$. A quantity whose predictive power depends on how much data you have is not a cause but a confounder. I run head-to-heads on 100 networks with identical architecture and training. For MNIST weakness predicts generalisation ($ρ= +0.374$, $p = 0.00012$), sharpness anticorrelates ($ρ= -0.226$) and simplicity predicts nothing ($p = 0.848$). For Fashion-MNIST ($ρ= +0.384$, $p = 8.15 \times 10^{-5}$), though simplicity is at least somewhat predictive there. Simplicity is dataset dependent, whereas weakness is invariant. Flat minima were never the answer.

</details>


### [141] [Nationwide EHR-Based Chronic Rhinosinusitis Prediction Using Demographic-Stratified Models](https://arxiv.org/abs/2605.05213)
*Sicong Chang,Yidan Shen,Justina Varghese,Akshay R Prabhakar,Sebastian Guadarrama-Sistos-Vazquez,Jiefu Chen,Masayoshi Takashima,Omar G. Ahmed,Renjie Hu,Xin Fu*

Main category: cs.LG

TL;DR: A machine learning framework using nationwide EHR data with hybrid feature selection and demographic-stratified models to predict chronic rhinosinusitis diagnosis, achieving improved discrimination for early risk stratification.


<details>
  <summary>Details</summary>
Motivation: Chronic rhinosinusitis (CRS) is common but difficult to identify early due to overlapping symptoms with conditions like allergic rhinitis and heterogeneous phenotypes. Prior predictive studies suffer from limited generalizability due to single-institutional cohorts, and EHR data has extreme feature sparsity and dimensionality issues.

Method: Used nationwide longitudinal EHR data from the All of Us Research Program with two years of pre-diagnostic history. Implemented hybrid feature-selection combining prevalence-based statistical screening with model-based importance ranking to compress ~110,000 candidate codes to 100 interpretable features. Trained demographic stratified models across six sex and life-stage subgroups with subgroup-specific hyperparameter tuning.

Result: The framework achieved an overall AUC of 0.8461, improving discrimination by 0.0168 over the best baseline model. This demonstrates effective use of EHR data for CRS prediction.

Conclusion: Routinely collected EHR data can support population-representative CRS risk stratification, potentially enabling earlier triage and referral prioritization in primary care settings.

Abstract: Chronic rhinosinusitis (CRS) is a common heterogeneous inflammatory disorder that causes substantial morbidity and healthcare costs. CRS is difficult to identify early from routine encounters, as symptom presentations overlap with common conditions such as allergic rhinitis, and heterogeneous phenotypes further obscure risk patterns. Prior predictive studies often rely on single-institutional cohorts , which reduce population-level generalizability. To overcome this, we leveraged nationwide longitudinal EHR data from the \textit{All of Us} Research Program to predict CRS diagnosis using two years of pre-diagnostic history. To address extreme feature sparsity and dimensionality in coded EHR data, we implemented a hybrid feature-selection pipeline that combines prevalence-based statistical screening with model-based importance ranking, compressing approximately 110,000 candidate codes into 100 interpretable features. To capture demographic heterogeneity, we trained demographic stratified models across six adult sex and life-stage subgroups with subgroup-specific hyperparameter tuning. Our framework achieved an overall AUC of 0.8461, improving discrimination by 0.0168 over the best baseline. These results demonstrate that routinely collected EHR data may support population-representative CRS risk stratification and inform earlier triage and referral prioritization in primary care.

</details>


### [142] [SAT: Sequential Agent Tuning for Coordinator Free Plug and Play Multi-LLM Training with Monotonic Improvement Guarantees](https://arxiv.org/abs/2605.05216)
*Yi Xie,Yangyang Xu,Yi Fan,Bo Liu*

Main category: cs.LG

TL;DR: SAT (Sequential Agent Tuning) is a coordinator-free training method for teams of smaller LLMs that collectively outperform single large models, with theoretical guarantees of monotonic improvement and plug-and-play agent upgrades.


<details>
  <summary>Details</summary>
Motivation: Large LLMs are expensive to deploy, and while teams of smaller LLMs can match or outperform them, joint training introduces compounding distribution shifts that make coordination and stability difficult.

Method: Sequential Agent Tuning (SAT) uses a factorized policy representation with block-coordinate updates over agents, sequence-aware on-policy advantage estimation conditioned on evolving team policy, and per-agent KL trust regions to isolate occupancy drift.

Result: A team of three 4B agents (12B total) trained with SAT surpasses the much larger Qwen3-32B on AIME24/25 benchmarks by 3.9% on average. Swapping in two 8B agents boosts composite score by 10.4%.

Conclusion: SAT provides a scalable, decentralized training approach for LLM teams with strong theoretical guarantees (monotonic improvement and plug-and-play invariance) and empirical results showing smaller model teams can outperform much larger single models.

Abstract: Large language models (LLMs) with a large number of parameters achieve strong performance but are often prohibitively expensive to deploy. Recent work explores using teams of smaller, more efficient LLMs that collectively match or even outperform a single large model. However, jointly updating multiple agents introduces compounding distribution shifts, making coordination and stability during training difficult. We address this by introducing Sequential Agent Tuning (SAT), a coordinator-free training paradigm. SAT represents the team as a factorized policy and employs block-coordinate updates over agents, enabling scalable, decentralized training without a central controller. Specifically, we develop a sequence-aware, on-policy advantage estimator that conditions on the evolving team policy, coupled with per-agent KL trust regions that isolate occupancy drift. Theoretically, this framework provides two critical guarantees. First, it ensures monotonic improvement, stabilizing the training process. Second, it establishes provable plug-and-play invariance: any agent can be upgraded to a stronger model without retraining the rest of the team, with a formal guarantee that the performance bound improves. Empirically, a team of three 4B agents (12B total) trained with SAT surpasses the much larger Qwen3-32B on AIME24/25 benchmarks by 3.9\% on average. We validate our plug-and-play theory by swapping in two 8B agents, which boosts the composite score by 10.4\%. We provide code and appendix of proof at https://github.com/Yydc/SAT-AAMAS

</details>


### [143] [Physics-Informed Neural Networks with Learnable Loss Balancing and Transfer Learning](https://arxiv.org/abs/2605.05217)
*Reza Pirayeshshirazinezhad*

Main category: cs.LG

TL;DR: Self-supervised PINN framework with adaptive balancing of physics and data terms using learnable blending neuron and transfer learning for data-scarce scientific ML applications


<details>
  <summary>Details</summary>
Motivation: Addressing the challenge of scientific machine learning under data scarcity, where traditional PINNs rely on fixed or heuristic weighting of physics residuals and data loss, leading to unstable training and poor generalization

Method: Proposes an adaptive PINN framework with learnable blending neuron that dynamically adjusts relative contributions of physics residuals and data loss based on their uncertainties. Integrates transfer learning strategy to reuse representations from related domains for new physical systems with limited data

Result: Validated on heat transfer prediction in liquid-metal miniature heat sinks using only 87 CFD datapoints. Adaptive PINN achieves error <8%, outperforming shallow neural networks, kernel methods, and physics-only baselines

Conclusion: Provides a general recipe for embedding physics adaptively into neural networks, offering robust and reproducible approach for data-scarce problems across various scientific domains including fluid dynamics and material modeling

Abstract: We propose a self-supervised physics-informed neural network (PINN) framework that adaptively balances physics-based and data-driven supervision for scientific machine learning under data scarcity. Unlike prior PINNs that rely on fixed or heuristic weighting of physics residuals and data loss, our approach introduces a learnable blending neuron that dynamically adjusts the relative contribution of each term based on their uncertainties. This mechanism enables stable training and improved generalization without manual tuning. To further enhance efficiency, we integrate a transfer learning strategy that reuses representations from related domains and adapts them to new physical systems with limited data. We validate the framework for the prediction of heat transfer in liquid-metal miniature heat sinks using only 87 CFD datapoints, where the adaptive PINN achieves an error <8%, outperforming shallow neural networks, kernel methods, and physics-only baselines. Our framework provides a general recipe for embedding physics adaptively into neural networks, offering a robust and reproducible approach for data-scarce problems across various scientific domains, including fluid dynamics and material modeling.

</details>


### [144] [Horizon-Constrained Rashomon Sets for Chaotic Forecasting](https://arxiv.org/abs/2605.05218)
*Gauri Kale,Rahul Vishwakarma,Holly Diamond,Ava Hedayatipour,Amin Rezaei*

Main category: cs.LG

TL;DR: This paper bridges chaos theory and predictive multiplicity by introducing horizon-constrained Rashomon sets, showing how model multiplicity evolves in chaotic systems and developing decision-aligned selection algorithms that improve real-world decision quality.


<details>
  <summary>Details</summary>
Motivation: Predictive multiplicity (multiple near-equally accurate models) and chaotic dynamics (exponential divergence) are fundamental challenges in machine learning that have evolved independently despite conceptual connections, creating a gap in understanding how to deploy ML in chaotic, safety-critical domains.

Method: Introduces horizon-constrained Rashomon sets as a theoretical framework, proves effective Rashomon set contraction with lead time determined by maximum Lyapunov exponent, develops Lyapunov-weighted metrics for predictive disagreement bounds, and creates decision-aligned selection algorithms based on downstream utility rather than forecast accuracy alone.

Result: The framework demonstrates improved decision quality by 18-34% while maintaining competitive predictive performance across synthetic chaotic systems (Lorenz-96, Kuramoto-Sivashinsky) and real-world applications (wind power, traffic, weather).

Conclusion: This work establishes the first rigorous connection between chaos theory and predictive multiplicity, providing principled guidance for deploying machine learning in safety-critical chaotic domains where model selection based on downstream utility outperforms accuracy-only approaches.

Abstract: Predictive multiplicity and chaotic dynamics represent two fundamental challenges in machine learning that have evolved independently despite their conceptual connections. We bridge this gap by introducing horizon-constrained Rashomon sets, a theoretical framework that characterizes how model multiplicity evolves with prediction horizon in chaotic systems. Unlike static prediction tasks where the Rashomon set remains fixed, chaos induces exponential divergence among initially similar models, fundamentally transforming the nature of predictive equivalence. We prove that the effective Rashomon set contracts exponentially with lead time at a rate determined by the maximum Lyapunov exponent and introduce Lyapunov-weighted metrics that provide tighter bounds on predictive disagreement. Leveraging these insights, we develop decision-aligned selection algorithms that choose among near-optimal models based on downstream utility rather than forecast accuracy alone. Extensive experiments on synthetic chaotic systems (Lorenz-96, Kuramoto-Sivashinsky) and real-world applications (wind power, traffic, weather) demonstrate that our framework improves decision quality by 18-34\% while maintaining competitive predictive performance. This work establishes the first rigorous connection between chaos theory and predictive multiplicity, providing principled guidance for deploying machine learning in safety-critical chaotic domains.

</details>


### [145] [Sparse Prefix Caching for Hybrid and Recurrent LLM Serving](https://arxiv.org/abs/2605.05219)
*Mikhail Shirokikh,Sergey Nikolenko*

Main category: cs.LG

TL;DR: Sparse prefix caching for state-space models: store exact recurrent states at sparse checkpoints instead of dense KV caching, with optimal checkpoint placement using dynamic programming for LLM serving.


<details>
  <summary>Details</summary>
Motivation: Existing LLM serving systems use dense per-token key/value caching, but state-space models (SSMs) with recurrent layers can resume from single stored states rather than requiring entire token histories. This creates a new design point between no reuse and dense caching.

Method: Formalize sparse prefix caching as checkpoint placement problem under overlap depth distribution, develop exact O(NM) dynamic programming solution. Store exact recurrent states at sparse checkpoint positions, resume from deepest stored checkpoint and recompute remaining suffix exactly.

Result: Method consistently improves Pareto frontier compared to standard heuristics on real-world data (QuALITY and System Prompts). Dominates fixed-budget baselines, matches/outperforms strongest heuristic (block caching) with fewer checkpoints, especially at low budgets where overlap distribution is non-uniform.

Conclusion: Sparse prefix caching is effective for use cases where many requests share substantial but not identical prefixes within cached entries. Preserves exact outputs, doesn't require new kernels, applies to recurrent/SSM layers, and can be combined with existing KV-cache compression techniques for hybrid models.

Abstract: Prefix caching is a key latency optimization for autoregressive LLM serving, yet existing systems assume dense per-token key/value reuse. State-space models change the structure of the problem: a recurrent layer can resume from a single stored state rather than requiring the entire token history. This asymmetry opens a new design point between no reuse and dense caching: store exact recurrent states at a sparse set of checkpoint positions and, on a cache hit, resume from the deepest stored checkpoint and recompute the remaining suffix exactly.
  We formalize sparse prefix caching as checkpoint placement under a distribution over overlap depths, yielding an exact O(NM) dynamic program. For use cases where requests share a non-trivial prefix (e.g. asking different questions about a single long document), we show that our method consistently improves the Pareto frontier traced by standard heuristics on real-world data. Across QuALITY and System Prompts, distribution-aware placement dominates every fixed-budget baseline on the measured layer-group Pareto frontier and matches or outperforms the strongest heuristic (block caching) while typically using substantially fewer checkpoints, with the largest gains at low checkpoint budgets where the overlap distribution is most non-uniform. The method is most relevant when many requests share a substantial but not identical prefix within a retained cache entry. It preserves exact outputs, does not change the recurrent computation itself or require new recurrent update kernels, applies to recurrent/SSM layers whose hidden state can be extracted and restored exactly, and for hybrid models can be combined with existing KV-cache compression techniques.

</details>


### [146] [MidSteer: Optimal Affine Framework for Steering Generative Models](https://arxiv.org/abs/2605.05220)
*Tatiana Gaintseva,Andrew Stepanov,Ziquan Liu,Martin Benning,Gregory Slabaugh,Jiankang Deng,Ismail Elezi*

Main category: cs.LG

TL;DR: The paper formalizes the theory of concept steering for generative models, introduces MidSteer as a general affine framework for minimal-disturbance concept manipulation, and demonstrates its effectiveness across various tasks and modalities.


<details>
  <summary>Details</summary>
Motivation: Despite empirical success of steering intermediate representations for controlling generative models in post-deployment alignment and safety settings, there's currently no comprehensive theoretical framework to understand and guide these techniques.

Method: 1) Formalizes theory of concept steering and establishes link between steering and affine concept erasure, showing standard approach is special case of LEACE; 2) Develops LEACE-Switch for concept switching with optimal affine solution under certain assumptions; 3) Introduces MidSteer (Minimal Disturbance concept Steering) as a more general affine framework that relaxes assumptions and enables directed, minimal-disturbance transformations.

Result: MidSteer performs favorably across a range of tasks, modalities, and architectures, including vision diffusion models and large language models, demonstrating the practical value of the theoretical framework.

Conclusion: The paper provides a comprehensive theoretical framework for concept steering that bridges the gap between empirical practice and theory, enabling more principled control over generative models through intermediate representation manipulation.

Abstract: Steering intermediate representations has emerged as a powerful strategy for controlling generative models, particularly in post-deployment alignment and safety settings. However, despite its empirical success, it currently lacks a comprehensive theoretical framework. In this paper, we bridge this gap by formalizing the theory of concept steering. First, we establish a link between steering and affine concept erasure, proving that the standard approach for removing unwanted behaviors is a special case of LEACE (a closed-form method for affine erasure). Next, we formulate a principled theoretical framework for concept switching, LEACE-Switch, and characterize the assumptions under which it provides an optimal affine solution. Building on this analysis, we then introduce MidSteer (Minimal Disturbance concept Steering), a more general affine framework for concept manipulation that relaxes these assumptions and enables directed, minimal-disturbance transformations. We demonstrate that MidSteer performs favorably across a range of tasks, modalities, and architectures, including vision diffusion models and large language models.

</details>


### [147] [Data-Driven Variational Basis Learning Beyond Neural Networks: A Non-Neural Framework for Adaptive Basis Discovery](https://arxiv.org/abs/2605.05221)
*Andrew Kiruluta*

Main category: cs.LG

TL;DR: DVBL (Data Driven Variational Basis Learning) is a non-neural framework that learns interpretable basis functions directly from data through variational optimization, offering mathematical transparency and data adaptation without neural network complexity.


<details>
  <summary>Details</summary>
Motivation: Classical representation systems (Fourier, wavelets, fixed dictionaries) lack adaptation to high-dimensional data structure, while neural networks sacrifice interpretability, explicit basis control, and mathematical transparency for their learning capabilities.

Method: Treats basis atoms as primary optimization variables, learning them jointly with sample-specific coefficients and optionally a latent linear evolution operator through variational optimization, using alternating minimization algorithms with established convergence properties.

Result: Establishes existence of minimizers, proves blockwise descent properties for alternating minimization, provides conditions for coefficient recovery and basis identifiability, and integrates manifold and dynamical regularization without neural architectures.

Conclusion: DVBL provides a data-adaptive basis expansion that is explicit, interpretable, and analytically tractable, offering a novel middle ground between classical methods and deep learning with rigorous mathematical foundations.

Abstract: Classical representation systems such as Fourier series, wavelets, and fixed dictionaries provide analytically tractable basis expansions, but they are not intrinsically adapted to the empirical structure of modern high-dimensional data. Neural networks overcome this limitation by learning features from data, yet they do so through layered nonlinear parameterizations that often sacrifice interpretability, explicit control over basis structure, and mathematical transparency. In this manuscript we develop a non-neural alternative that learns basis functions directly from data through variational optimization. The proposed framework, termed Data Driven Variational Basis Learning (DVBL), treats basis atoms as primary optimization variables and learns them jointly with sample-specific coefficients and, when appropriate, a latent linear evolution operator. This yields a data-adaptive basis expansion that remains explicit, interpretable, and amenable to rigorous analysis. We formulate the model, establish existence of minimizers, prove blockwise descent properties for an alternating minimization algorithm, give conditions for coefficient recovery and basis identifiability, and show how manifold and dynamical regularization can be integrated without invoking neural architectures. We also discuss the conceptual novelty of the framework relative to classical dictionary learning, spectral methods, Koopman operator methods, and deep representation learning.

</details>


### [148] [Recursive Agent Optimization](https://arxiv.org/abs/2605.06639)
*Apurva Gandhi,Satyaki Chakraborty,Xiangjun Wang,Aviral Kumar,Graham Neubig*

Main category: cs.LG

TL;DR: RAO trains recursive AI agents that can spawn copies of themselves to handle sub-tasks, enabling divide-and-conquer problem solving that scales beyond context limits and generalizes to harder tasks.


<details>
  <summary>Details</summary>
Motivation: Current AI agents face limitations with long contexts and complex problems that require hierarchical decomposition. There's a need for agents that can handle tasks beyond their context window and scale to more difficult problems through structured problem-solving approaches.

Method: Reinforcement learning approach that trains recursive agents to learn when and how to spawn new instantiations of themselves for sub-task delegation and communication between parent and child agents.

Result: Recursive agents show better training efficiency, ability to scale beyond context window limits, generalization to tasks much harder than training tasks, and reduced wall-clock time compared to single-agent systems.

Conclusion: Recursive Agent Optimization enables effective divide-and-conquer problem solving in AI agents, providing a promising approach for scaling to complex, long-context problems through hierarchical decomposition and delegation.

Abstract: We introduce Recursive Agent Optimization (RAO), a reinforcement learning approach for training recursive agents: agents that can spawn and delegate sub-tasks to new instantiations of themselves recursively. Recursive agents implement an inference-time scaling algorithm that naturally allows agents to scale to longer contexts and generalize to more difficult problems via divide-and-conquer. RAO provides a method to train models to best take advantage of such recursive inference, teaching agents when and how to delegate and communicate. We find that recursive agents trained in this way enjoy better training efficiency, can scale to tasks that go beyond the model's context window, generalize to tasks much harder than the ones the agent was trained on, and can enjoy reduced wall-clock time compared to single-agent systems.

</details>


### [149] [Adaptive Computation Depth via Learned Token Routing in Transformers](https://arxiv.org/abs/2605.05222)
*Ahmed Abdelmuniem Abdalla Mohammed*

Main category: cs.LG

TL;DR: Token-Selective Attention (TSA) learns per-token gates to skip transformer layers based on contextual difficulty, achieving 14-23% efficiency gains with minimal quality loss.


<details>
  <summary>Details</summary>
Motivation: Standard transformers apply the same computational depth to all tokens regardless of their contextual difficulty, leading to inefficient computation for simpler tokens that could be processed with fewer layers.

Method: TSA introduces learned per-token gates using lightweight two-layer MLPs that produce continuous halting probabilities for residual updates between consecutive transformer blocks. This differentiable mechanism adds only 1.7% parameter overhead and requires no architectural changes. The gates learn to route tokens proportionally to difficulty driven by task-loss gradients alone.

Result: On character-level language modeling tasks: achieved 14-23% reduction in token-layer operations (TLOps) on Tiny-Shakespeare and enwik8 datasets with <0.5% quality loss; at matched efficiency, outperformed early exit by 0.7% lower validation loss; learned routing enabled direct inference-time sparse execution with real wall-clock speedup.

Conclusion: TSA demonstrates that task-loss gradients alone can drive difficulty-proportional routing without explicit depth regularization, providing an efficient, differentiable approach to token-level computational skipping that transfers well to inference while maintaining model quality.

Abstract: Standard transformer architectures apply the same number of layers to every token regardless of contextual difficulty. We present Token-Selective Attention (TSA), a learned per-token gate on residual updates between consecutive transformer blocks. Each gate is a lightweight two-layer multi-layer perceptron (MLP) that produces a continuous halting probability, making the mechanism end-to-end differentiable with 1.7% parameter overhead and no changes to the base architecture. Notably, TSA learns difficulty-proportional routing without any explicit depth pressure: even at $λ=0$ (no depth regularisation), the task-loss gradient alone drives the router to skip 20% of token-layer operations. On character-level language modeling, TSA saved 14-23% of token-layer operations (TLOps) across Tiny-Shakespeare and enwik8 at <0.5% quality loss. At matched efficiency, TSA achieved 0.7% lower validation loss than early exit, and the learned routing transfers directly to inference-time sparse execution for real wall-clock speedup.

</details>


### [150] [Structural Instability of Feature Composition](https://arxiv.org/abs/2605.05223)
*Yunpeng Zhou*

Main category: cs.LG

TL;DR: The paper develops a geometric framework showing that sparse autoencoders suffer from compositional collapse when activating multiple features simultaneously due to interference effects that grow systematically, limiting union-based steering scalability.


<details>
  <summary>Details</summary>
Motivation: Sparse Autoencoders are used for disentangling features in transformers and activation steering, but the theoretical foundations for compositional steering (simultaneously activating multiple semantic latents) are under-explored. The Linear Representation Hypothesis ignores non-linear interference effects in overcomplete dictionaries.

Method: The authors present a geometric framework modeling activation space as a high-dimensional sparse cone manifold. They derive an asymptotic compositional-collapse threshold under a spherical dictionary model using Gaussian mean width (statistical dimension) of the signal cone. They analyze ReLU rectification in high-bias regimes showing correlation-induced variance fluctuations convert to systematic drift that accumulates under composition.

Result: The theoretical analysis reveals interference growth consistent with a ratchet effect under composition. Validation on CLEVR shows hierarchical correlations accelerate the transition to collapse relative to random baselines. The predicted scaling trends are empirically confirmed.

Conclusion: The results highlight geometric constraints on the scalability of union-based steering in SAEs and motivate the need for composition mechanisms that explicitly manage interference beyond naive linear superposition.

Abstract: Sparse Autoencoders (SAEs) have emerged as a powerful paradigm for disentangling feature superposition in transformer-based architectures, enabling precise control via activation steering. However, the theoretical foundations of compositional steering -- the simultaneous activation of distinct semantic latents -- remain under-explored. The prevailing Linear Representation Hypothesis often abstracts away non-linear interference effects that arise in overcomplete dictionaries. We present a geometric framework for analyzing the instability of feature unions. Modeling the activation space as a high-dimensional sparse cone manifold, we derive an asymptotic compositional-collapse threshold under a spherical dictionary model, characterized by the Gaussian mean width (statistical dimension) of the signal cone. We further show that, in the high-bias regime, ReLU rectification converts microscopic correlation-induced variance fluctuations into a systematic drift that accumulates under composition, yielding interference growth consistent with a ratchet effect. We validate the predicted scaling trends on structured semantic features extracted from CLEVR, where hierarchical correlations accelerate the transition relative to random baselines. Together, our results highlight geometric constraints on the scalability of union-based steering and motivate composition mechanisms that explicitly manage interference beyond naive linear superposition.

</details>


### [151] [Channel-Level Semantic Perturbations: Unlearnable Examples for Diverse Training Paradigms](https://arxiv.org/abs/2605.05224)
*Bo Wang,Jia Ni,Mengnan Zhao,Zhan Qin,Kui Ren*

Main category: cs.LG

TL;DR: Existing unlearnable examples work poorly in pretraining-finetuning settings because frozen pretrained weights filter out their noise; this paper proposes SSC to create unlearnable examples that bypass semantic filtering.


<details>
  <summary>Details</summary>
Motivation: Unlearnable examples have been studied mainly in from-scratch training, but their effectiveness in the widely used pretraining-finetuning paradigm remains unknown, creating a critical gap in privacy protection.

Method: The paper proposes Shallow Semantic Camouflage (SSC), a hierarchical deception strategy that confines noise generation to a semantically valid subspace to bypass the semantic filtering effect of frozen pretrained layers.

Result: Extensive experiments show SSC preserves data unlearnability even under challenging settings like shallow-layer freezing and semantic-focused pretraining, effectively bridging the pretrain-based unlearnable learning gap.

Conclusion: SSC successfully addresses the limitations of existing unlearnable examples in pretraining-finetuning settings, providing a robust privacy protection method that works across diverse training paradigms.

Abstract: The unauthorized use of personal data in model training has emerged as a growing privacy threat. Unlearnable examples (UEs) address this issue by embedding imperceptible perturbations into benign examples to obstruct feature learning. However, existing studies mainly evaluate UEs under from-scratch training settings, leaving their behavior under the widely adopted pretraining-finetuning (PF) paradigm largely unexplored. In this work, we provide the first systematic investigation of unlearnable examples across diverse training paradigms. Our analysis reveals that loading and freezing pretrained weights significantly weakens the effectiveness of existing UEs methods. We further explain these findings through semantic filtering: while UEs tend to induce models to overfit non-semantic noise, thereby weakening their semantic extraction capabilities, under the PF paradigm, frozen shallow layers preserve data semantics, effectively filtering out distracting information like unlearnable noise. Guided by these insights, we propose a hierarchical deception strategy, Shallow Semantic Camouflage (SSC), that confines the generation process to a semantically valid subspace, aiming to bypass the semantic suppression introduced by pretrained weights. Extensive experiments demonstrate that our method consistently preserves data unlearnability even under challenging training paradigms, such as shallow-layer freezing and semantic-focused pretraining (SF-Pretrain), bridging the critical gap in pretrain-based unlearnable learning.

</details>


### [152] [MACS: Modality-Aware Capacity Scaling for Efficient Multimodal MoE Inference](https://arxiv.org/abs/2605.05225)
*Bo Li,Chuan Wu,shaolin Zhu*

Main category: cs.LG

TL;DR: MACS is a training-free inference framework that improves efficiency in MoE MLLMs by using entropy-weighted load balancing and dynamic modality-adaptive capacity allocation to address visual token redundancy and modality dynamics.


<details>
  <summary>Details</summary>
Motivation: Mixture-of-Experts Multimodal LLMs suffer from efficiency bottlenecks during Expert Parallelism inference due to the straggler effect. This is worsened in multimodal contexts where existing token-count-based load balancing fails to address information heterogeneity (redundant visual tokens treated equally to critical ones) and modality dynamics (varying visual-to-text ratios causing resource misallocation).

Method: Proposes MACS (Modality-Aware Capacity Scaling), a training-free inference framework with two key mechanisms: 1) Entropy-Weighted Load to quantify semantic value of visual tokens, addressing information heterogeneity, and 2) Dynamic Modality-Adaptive Capacity to allocate expert resources based on real-time modal composition of input.

Result: Extensive experiments show MACS significantly outperforms existing methods on various multimodal benchmarks, providing an efficient solution for MoE MLLM deployment in EP inference.

Conclusion: MACS offers a novel and robust training-free solution to address the unique efficiency challenges in MoE MLLMs during Expert Parallelism inference, effectively handling information heterogeneity and modality dynamics through entropy-weighted load balancing and dynamic capacity adaptation.

Abstract: Mixture-of-Experts Multimodal Large Language Models (MoE MLLMs) suffer from a significant efficiency bottleneck during Expert Parallelism (EP) inference due to the straggler effect. This issue is worsened in the multimodal context, as existing token-count-based load balancing methods fail to address two unique challenges: (1) Information Heterogeneity, where numerous redundant visual tokens are treated equally to semantically critical ones, and (2) Modality Dynamics, where varying visual to text ratios across tasks lead to resource misallocation. To address these challenges, we propose MACS (Modality-Aware Capacity Scaling), a training-free inference framework. Specifically, MACS introduces an Entropy-Weighted Load mechanism to quantify the semantic value of visual tokens, addressing information heterogeneity. Additionally, the Dynamic Modality-Adaptive Capacity mechanism allocates expert resources based on the real-time modal composition of the input. Extensive experiments demonstrate that MACS significantly outperforms existing methods on various multimodal benchmarks, providing a novel and robust solution for the efficient deployment of MoE MLLMs in EP inference.

</details>


### [153] [Internalizing Outcome Supervision into Process Supervision: A New Paradigm for Reinforcement Learning for Reasoning](https://arxiv.org/abs/2605.05226)
*Fei Ding,Yongkang Zhang,Runhao Liu,Yuhao Liao,Zijian Zeng,Sibo wang,Huiming Yang*

Main category: cs.LG

TL;DR: A new reinforcement learning method for reasoning that internalizes outcome-level supervision into process-level supervision by automatically extracting learning signals from failed reasoning trajectories, enabling fine-grained credit assignment without external process supervision.


<details>
  <summary>Details</summary>
Motivation: The key challenge in reinforcement learning for reasoning is the sparsity of outcome-level supervision and difficulty in transforming end-of-sequence feedback into fine-grained learning signals for intermediate reasoning steps. Existing methods either struggle with precise credit assignment using only outcome rewards or require costly external process supervision that doesn't scale well.

Method: Proposes a supervision-internalization method where the model automatically extracts process-level learning signals by identifying, correcting, and reusing failed reasoning trajectories. This creates a training paradigm where the model continually generates and refines its own internal process supervision during reinforcement learning.

Result: Enables finer-grained policy optimization under outcome-only supervision by internalizing outcome supervision into process supervision, providing a new path for credit assignment in reinforcement learning for reasoning without external process supervision.

Conclusion: The proposed approach opens a new paradigm for reinforcement learning for reasoning by enabling models to self-generate process supervision, addressing fundamental credit assignment challenges and reducing dependence on costly external process supervision.

Abstract: The central challenge of reinforcement learning for reasoning lies not only in the sparsity of outcome-level supervision, but more fundamentally in how to transform feedback provided only at the end of a sequence into fine-grained learning signals that can guide intermediate reasoning steps. Existing approaches either rely on outcome-level rewards for sequence-level optimization, which makes precise credit assignment difficult, or depend on externally constructed process supervision, which is costly and difficult to scale sustainably. To address this, we propose a new perspective: reinforcement learning for reasoning can be understood as the problem of internalizing outcome supervision into process supervision. From this perspective, we introduce a supervision-internalization method for reinforcement learning for reasoning, enabling the model to automatically extract process-level learning signals through identifying, correcting, and reusing failed reasoning trajectories, thereby achieving finer-grained policy optimization under outcome-only supervision. We further abstract this idea into a new training paradigm, in which the model continually generates and refines its own internal process supervision during reinforcement learning, opening a new path for fine-grained credit assignment in reinforcement learning for reasoning that differs from externally provided process supervision.

</details>


### [154] [Rethinking Data Curation in LLM Training: Online Reweighting Offers Better Generalization than Offline Methods](https://arxiv.org/abs/2605.05227)
*Wanru Zhao,Yihong Chen,Yuzhi Tang,Wentao Ma,Shengchao Hu,Shell Xu Hu,Alex Iacob,Abhinav Mehrotra,Nicholas D. Lane*

Main category: cs.LG

TL;DR: ADAPT is an online data reweighting framework that dynamically adjusts sample importance during LLM training via adaptive learning rates, outperforming static offline curation methods while preserving data diversity.


<details>
  <summary>Details</summary>
Motivation: Existing LLM data curation methods operate offline, creating separation from training that leads to engineering overhead and brittleness when models/tasks change. Offline methods also reduce data diversity through hard filtering or resampling, harming generalization.

Method: ADAPT rethinks data curation as an online reweighting problem where sample importance is dynamically adjusted during training via loss weighting. It uses adaptive per-sample learning rates guided by similarity-based quality signals without changing the number of training samples.

Result: Experiments on instruction tuning and large-scale pretraining show ADAPT consistently outperforms offline selection/mixing and prior online methods, achieving stronger cross-benchmark generalization under equal FLOPs.

Conclusion: Online data reweighting via ADAPT provides a more effective alternative to static offline curation methods, acting as an implicit curriculum learner that dynamically adapts to model evolution while preserving data diversity for better generalization.

Abstract: Data curation is a critical yet under-explored area in large language model (LLM) training. Existing methods, such as data selection and mixing, operate in an offline paradigm, detaching themselves from training. This separation introduces engineering overhead and makes the curation brittle: the entire pipeline must be re-run under model/task shifts. Moreover, offline methods alter data size through hard filtering or resampling, often sacrificing data diversity and harming generalization. We propose to rethink data curation as an online reweighting problem, where sample importance is dynamically adjusted during training via loss weighting rather than static pre-processing. Specifically, we introduce ADAPT (Adaptive Data reweighting for Pretraining and FineTuning), a dynamic online framework that reweights training samples with adaptive per-sample learning rates guided by similarity-based quality signals, without changing the number of training samples. Unlike offline methods that enforce a static data distribution, ADAPT acts as an implicit curriculum learner, progressively shifting focus from coarse-grained patterns to fine-grained semantic distinctions as the model evolves. Experiments on both instruction tuning and large-scale pretraining show that ADAPT consistently outperforms offline selection/mixing and prior online methods, achieving stronger cross-benchmark generalization under equal FLOPs.

</details>


### [155] [Evolutionary fine tuning of quantized convolution-based deep learning models](https://arxiv.org/abs/2605.05228)
*Marcin Pietroń*

Main category: cs.LG

TL;DR: Using evolutionary strategies to optimize quantized neural networks by adjusting weight quantization states improves accuracy beyond simple nearest-neighbor rounding.


<details>
  <summary>Details</summary>
Motivation: Deep learning models are too large for IoT/mobile devices, requiring compression via quantization. Standard nearest-neighbor quantization doesn't guarantee optimal accuracy, leaving room for improvement in quantized models.

Method: Evolution strategy optimization that iteratively changes small percentages of weights, shifting them to different quantization states. Uses appropriate evolutionary operators and parameters to efficiently improve quantized model accuracy.

Result: Demonstrated accuracy improvements for VGG and ResNet architectures in image classification/detection, plus autoencoder simulations. Shows evolutionary optimization can quickly enhance quantized model performance.

Conclusion: Evolutionary optimization of weight quantization states is an effective post-quantization refinement technique that improves accuracy beyond standard nearest-neighbor quantization, making compressed models more suitable for resource-constrained devices.

Abstract: Deep learning models are the most efficient models in many machine learning tasks. The main disadvantage when using them in IoT, mobile devices, independent autonomous or real-time systems is their complexity and memory size. Therefore, much research has concentrated on compression techniques of deep learning architectures. One of the most popular technique is quantization. In most of the works, the quantization is done based on the nearest neighbour quantization technique. This work focuses on improving the quantization efficiency in pretrained and quantized models. This approach has the potential to improve the final accuracy of quantized models. The main postulate of the work is that final quantization states of the network based on nearest neighbour rounding does not guarantee optimal accuracy. In the presented work, the evolution strategy is used as an optimization approach. The evolution in each iteration changes the values of the small percentage of weights. It shifts theirs values to different quantization states. The work shows that proposed evolution with an appropriate set of operators and parameters can fast improve the accuracy of the quantized models. The results are presented for popular architectures such as VGG and Resnet for image classification and detection. Additionally, simulations were carried out for the autoencoder architecture.

</details>


### [156] [Expert Routing for Communication-Efficient MoE via Finite Expert Banks](https://arxiv.org/abs/2605.05278)
*Mohammad Reza Deylam Salehi,Ali Khalesi*

Main category: cs.LG

TL;DR: A framework analyzing Mixture-of-Experts routing through information theory, treating the gate as a stochastic channel and developing practical estimators for routing information that tracks generalization performance.


<details>
  <summary>Details</summary>
Motivation: Resource-efficient machine learning increasingly uses sparse Mixture-of-Experts architectures where gating controls computation, communication, and accuracy. There's a need to understand and quantify the routing information in these systems, particularly how information-theoretic measures relate to generalization performance and system efficiency.

Method: Treats MoE gate as a stochastic channel and uses mutual information I(X;T) to quantify routing information. Develops a finite-bank MNIST construction with pretrained CNN experts and discrete, data-dependent selection rules. Uses algorithmic mutual information I(S;W) with closed-form discrete-entropy estimator from empirical posterior q(W|S). Sweeps data-dependence parameter α to observe relationships. Compares with uniform union-bound baseline and introduces empirical estimator of I(X;T) with Blahut-Arimoto procedure for tracing accuracy-rate curves.

Result: The estimator Î(S;W) monotonically tracks the generalization gap, while the Xu-Raginsky bound exhibits expected looseness. Provides practical tools for analyzing resource-aware MoE inference systems and establishes I(X;T) and D(R_g) as design proxies for efficient expert routing.

Conclusion: The proposed information-theoretic framework provides practical tools for analyzing MoE routing systems, showing how mutual information measures can track generalization performance and serve as design proxies for efficient expert selection in resource-aware inference systems.

Abstract: Resource-efficient machine learning increasingly uses sparse Mixture-of-Experts (MoE) architectures, where the gate acts as both a learning component and a routing interface controlling computation, communication, and accuracy. Motivated by finite-rate interpretations of MoE gating, we treat the gate as a stochastic channel and use $I(X;T)$ to quantify the routing information available to the selected expert. To make the associated information quantities tractable beyond synthetic examples, we develop a finite-bank MNIST construction using pretrained CNN experts and a discrete, data-dependent selection rule. Since the selected model belongs to a finite candidate set, the algorithmic mutual information $I(S;W)$ admits a closed-form discrete-entropy estimator from the empirical posterior $q(W|S)$. Sweeping a data-dependence parameter $α$, we observe that $\widehat I(S;W)$ monotonically tracks the generalization gap, while the Xu-Raginsky bound exhibits the expected looseness. We also compare with a uniform union-bound baseline and introduce an empirical estimator of $I(X;T)$ together with a Blahut-Arimoto procedure for tracing an accuracy-rate curve over the expert bank. The proposed framework provides a practical tool for analyzing resource-aware MoE inference systems and for interpreting $I(X;T)$ and $D(R_g)$ as design proxies for efficient expert routing.

</details>


### [157] [Forecasting Green Skill Demand in the Automotive Industry: Evidence from Online Job Postings](https://arxiv.org/abs/2605.05280)
*Sabur Butt,Joshua N. Arrazola E.,Hector G. Ceballos,Patricia Caratozzolo*

Main category: cs.LG

TL;DR: A computational framework analyzes green skill demand in Mexico's automotive industry using online job postings, identifying 274 unique green skills and forecasting future trends with transformer-based models.


<details>
  <summary>Details</summary>
Motivation: The global transition to sustainable economies is reshaping labor markets, but there are limited systematic methods for identifying and forecasting green skills. There's a need for data-driven workforce planning in the green transition, particularly in key economic sectors like Mexico's automotive industry.

Method: Researchers compiled a dataset of job advertisements from Indeed Mexico, OCC Mundial, and LinkedIn (July 2024 to July 2025) with 204,373 skill records. They used a two-stage pipeline combining multilingual embeddings and ESCO validation to identify green skills, then benchmarked 15 time series forecasting models using rolling origin evaluation, with transformer-based models showing best performance.

Result: The framework identified 274 unique green skills across 8,576 occurrences (4.22% of all skills). Transformer-based models (FEDformer, Reformer, Informer) achieved best performance with MAE around 2.5e-5 and relative RMSE below 15. Current demand concentrates on operational sustainability practices, while fastest-growing skills relate to renewable energy, recycling, and hydrogen technologies.

Conclusion: The proposed pipeline successfully measures and predicts green skill demand, supporting data-driven workforce planning in the green transition. The framework's ability to classify skills by absolute and relative growth helps identify stable, emerging, and high-impact competencies crucial for sustainable economic transformation.

Abstract: The global transition toward sustainable economies is reshaping labor markets, yet systematic methods for identifying and forecasting green skills remain limited. This study presents a computational framework to measure and predict green skill demand using online job postings from Mexico's automotive industry, which contributes about 4% of national GDP. We compile a dataset of job advertisements from Indeed Mexico, OCC Mundial, and LinkedIn (July 2024 to July 2025), yielding 204,373 skill records. A two-stage pipeline combining multilingual embeddings and ESCO validation identifies 274 unique green skills across 8,576 occurrences (4.22% of all skills). We benchmark 15 time series forecasting models using a rolling origin evaluation. Transformer-based models, especially FEDformer, Reformer, and Informer, achieve the best performance, with MAE around 2.5e-5 and relative RMSE below 15. We further propose a framework to classify skills by absolute and relative growth, identifying stable, emerging, and high-impact competencies. Results show current demand is concentrated in operational sustainability practices, while the fastest-growing skills relate to renewable energy, recycling, and hydrogen technologies. This pipeline supports data-driven workforce planning in the green transition.

</details>


### [158] [Attribution-Guided Continual Learning for Large Language Models](https://arxiv.org/abs/2605.05285)
*Yazheng Liu,Yuxuan Wan,Rui Xu,Xi Zhang,Sihong Xie,Hui Xiong*

Main category: cs.LG

TL;DR: A novel attribution-guided continual fine-tuning framework for LLMs that uses element-wise parameter importance to modulate gradients, preventing catastrophic forgetting while learning new tasks.


<details>
  <summary>Details</summary>
Motivation: Current methods for mitigating catastrophic forgetting in LLMs (data replay, parameter freezing, regularization) lack semantic awareness of internal knowledge distribution and cannot distinguish which parameters should be preserved or updated when learning new tasks sequentially.

Method: Proposes an attribution-guided continual fine-tuning framework that estimates task-specific, element-wise parameter importance scores for each Transformer layer and uses these scores to modulate gradients during learning - parameters important to previous tasks receive smaller updates while less relevant ones remain plastic.

Result: Experiments on continual learning benchmarks show the method consistently outperforms baselines, achieving better retention of old tasks while maintaining competitive performance on new tasks.

Conclusion: The attribution-guided approach effectively addresses catastrophic forgetting in LLMs by providing semantic awareness of parameter importance, enabling selective gradient modulation that preserves important knowledge while allowing for new learning.

Abstract: Large language models (LLMs) often suffer from catastrophic forgetting in continual learning: after learning new tasks sequentially, they perform worse on earlier tasks. Existing methods mitigate catastrophic forgetting by data replay, parameter freezing, or regularization. However, these methods lack semantic awareness of internal knowledge distribution in LLMs. As a result, they cannot distinguish parameters that should be preserved or updated. We propose an attribution-guided continual fine-tuning framework for LLMs. Our method estimates task-specific, element-wise parameter importance in each Transformer layer and uses these scores to modulate gradients. Parameters important to previous tasks receive smaller updates, while less relevant ones remain plastic for learning new tasks. Experiments on continual learning benchmarks show that our method consistently outperforms baselines, achieving better retention of old tasks while maintaining competitive performance on new tasks.

</details>


### [159] [Graph Normalization: Fast Binarizing Dynamics for Differentiable MWIS](https://arxiv.org/abs/2605.05330)
*Laurent Guigues*

Main category: cs.LG

TL;DR: Graph Normalization (GN) is a differentiable dynamical system that approximates the Maximum Weight Independent Set (MWIS) problem, always converging to binary solutions while connecting to evolutionary game theory and enabling end-to-end learning of constrained optimization.


<details>
  <summary>Details</summary>
Motivation: MWIS is an NP-hard problem with broad applications in combinatorial optimization, assignment, scheduling, set packing, and MAP inference, but existing methods like Belief Propagation don't guarantee binary convergence. There's a need for differentiable approaches that can integrate with deep learning for hard constraint optimization.

Method: GN is a dynamical system on graphs using quasi-Newton descent through exact Majorization-Minimization. It formulates MWIS as a nonlinear evolutionary game (Replicator Dynamics) where vertices compete for inclusion, with connections to Fisher's Fundamental Theorem of Natural Selection.

Result: GN always converges to binary indicators of Maximum Independent Sets, outperforms state-of-the-art methods, finds solutions within 1% of optimal on benchmarks with up to 1M edges in seconds, and enables differentiable hard assignments similar to Sinkhorn but with binarization.

Conclusion: GN provides a principled, differentiable framework for solving MWIS problems with guaranteed convergence, opening new avenues for deep learning architectures needing hard decisions under constraints and enabling end-to-end learning in various applications from computer vision to resource allocation.

Abstract: We introduce Graph Normalization (GN), a principled dynamical system on graphs that serves as a differentiable approximation engine for the NP-hard Maximum Weight Independent Set (MWIS) problem. MWIS encompasses many combinatorial challenges, including optimal assignment, scheduling, set packing, and MAP inference in discrete Markov Random Fields. Unlike Belief Propagation, we prove GN always converges to a binary indicator of a Maximum Independent Set. GN realizes a fast quasi-Newton descent through an exact Majorization-Minimization step, systematically improving the MWIS relaxed primal objective. We establish an equivalence between GN and the Replicator Dynamics of a nonlinear evolutionary game, where vertices compete for inclusion in an independent set. While a non-potential game, the GN game follows Fisher's Fundamental Theorem of Natural Selection, where the average fitness equals the MWIS primal objective and strictly increases. This connection leads to a weighted extension of the Motzkin-Straus theorem, showing MISes are in bijection with the local minima of a quadratic form over a tilted simplex. For the Assignment Problem, GN acts as a variant of the Sinkhorn algorithm that naturally converges to a hard assignment while generalizing to arbitrary constraint graphs. We demonstrate GN's performance as a fast binarization engine for the state-of-the-art Bregman-Sinkhorn relaxed MWIS solver. On real-world benchmarks with up to 1M edges, GN identifies solutions within 1% of the best known results in seconds on a CPU. GN opens new avenues for deep learning architectures requiring differentiable, "hard" decisions under constraints, with applications in structured sparse attention, dynamic network pruning, and Mixture-of-Experts. Beyond core AI, the GN framework enables end-to-end learning of constrained optimization in computer vision, computational biology, and resource allocation.

</details>


### [160] [Feature Starvation as Geometric Instability in Sparse Autoencoders](https://arxiv.org/abs/2605.05341)
*Faris Chaudhry,Keisuke Yano,Anthea Monod*

Main category: cs.LG

TL;DR: Adaptive elastic net SAEs (AEN-SAEs) replace standard ℓ₁-regularized sparse autoencoders with a fully differentiable architecture that combines ℓ₂ structural regularization with adaptive ℓ₁ reweighting to solve feature starvation and shrinkage bias problems in LLM interpretability.


<details>
  <summary>Details</summary>
Motivation: Standard ℓ₁-regularized sparse autoencoders suffer from fundamental problems: feature starvation (dead neurons) and shrinkage bias. These issues require computationally expensive heuristic resampling and nondifferentiable hard-masking methods. The authors argue that feature starvation is not just an empirical artifact but a fundamental optimization-geometric pathology of overcomplete dictionaries, where the ℓ₁-induced sparse coding map is unstable and misaligned with shallow, amortized encoders.

Method: Introduces adaptive elastic net SAEs (AEN-SAEs), a fully differentiable architecture based on classical sparse regression. Combines: 1) ℓ₂ structural term that enforces strong convexity and Lipschitz stability, and 2) adaptive ℓ₁ reweighting that eliminates shrinkage bias and suppresses spurious features. This approach jointly controls the curvature and interaction structure of the induced polyhedral geometry.

Result: Theoretically, AEN-SAEs yield a Lipschitz-continuous sparse coding map and recover global feature support under mild assumptions. Empirically, across synthetic settings and LLMs (Pythia 70M, Llama 3.1 8B), AEN-SAEs mitigate feature starvation without auxiliary heuristics while maintaining competitive reconstruction abilities.

Conclusion: AEN-SAEs provide a principled solution to the fundamental optimization-geometric problems of standard sparse autoencoders, offering a stable, differentiable alternative that eliminates the need for heuristic workarounds while maintaining interpretability and reconstruction performance.

Abstract: Sparse autoencoders (SAEs) are used to disentangle the dense, polysemantic internal representations of large language models (LLMs) into interpretable, monosemantic concepts. However, standard $\ell_1$-regularized SAEs suffer from feature starvation (dead neurons) and shrinkage bias, often requiring computationally expensive heuristic resampling and nondifferentiable hard-masking methods to bypass these challenges. We argue that feature starvation is not merely an empirical artifact of poor data diversity, but a fundamental optimization-geometric pathology of overcomplete dictionaries: the $\ell_1$-induced sparse coding map is unstable and fundamentally misaligned with shallow, amortized encoders. To address this structural instability, we introduce adaptive elastic net SAEs (AEN-SAEs), a fully differentiable architecture grounded in classical sparse regression. AEN-SAEs combine an $\ell_2$ structural term that enforces strong convexity and Lipschitz stability with adaptive $\ell_1$ reweighting that eliminates shrinkage bias and suppresses spurious features, thereby jointly controlling the curvature and interaction structure of the induced polyhedral geometry. Theoretically, we show that AEN-SAEs yield a Lipschitz-continuous sparse coding map and recover the global feature support under mild assumptions. Empirically, across synthetic settings and LLMs (Pythia 70M, Llama 3.1 8B), AEN-SAEs mitigate feature starvation without auxiliary heuristics while maintaining competitive reconstruction abilities.

</details>


### [161] [A Multi-Head Attention Approach for SLA Compliance Monitoring in Data Centers](https://arxiv.org/abs/2605.05354)
*Omanshu Thapliyal*

Main category: cs.LG

TL;DR: A transformer-based predictive framework for anticipating SLA violations in data center colocations by encoding contractual rules as structured training data and generating specialized predictions for finance, operations, and compliance teams.


<details>
  <summary>Details</summary>
Motivation: Traditional reactive monitoring of Service Level Agreements (SLAs) only detects breaches after they occur, limiting opportunities for remediation and resulting in financial penalties. There's a need to proactively anticipate SLA violations to allow for preventive actions and minimize penalties.

Method: Encode SLA rules as structured JSON objects to generate training data automatically without manual annotation. Train a per-customer multi-head transformer model where each attention head specializes in one SLA rule, learning temporal dependencies that precede violations by 30 minutes.

Result: The inference service emits structured prediction events transformed into three role-specific views: 1) Finance schemas exposing credit liability, 2) Operations schemas surfacing risk scores and recommended interventions, and 3) Compliance schemas bundling predictions with immutable telemetry signatures for audit.

Conclusion: By aligning model architecture directly with contractual obligations, this framework enables data center operators to anticipate SLA breaches, prioritize corrective actions, and minimize financial penalties through proactive intervention.

Abstract: Service level agreements (SLAs) in data center colocation contracts define precise thresholds for power, temperature, and humidity, with tiered violation penalties expressed as credits against monthly recurring charges. Traditional reactive monitoring detects breaches only after they occur, limiting remediation opportunities. We present a framework that encodes SLA rules as structured JSON objects to generate training data without manual annotation. We train a per-customer multi-head transformer model in which each attention head specializes in one SLA rule, learning temporal dependencies that precede violations by 30 minutes. Post-training, the inference service emits structured prediction events transformed into three role-specific views: finance schemas exposing credit liability, operations schemas surfacing risk scores and recommended interventions, and compliance schemas bundling predictions with immutable telemetry signatures for audit. By aligning model architecture directly with contractual obligations, this framework enables operators to anticipate SLA breaches, prioritize corrective actions, and minimize financial penalties.

</details>


### [162] [Balancing Stability and Plasticity in Sequentially Trained Early-Exiting Neural Networks](https://arxiv.org/abs/2605.05358)
*Alaa Zniber,Ouassim Karrakchou,Mounir Ghogho*

Main category: cs.LG

TL;DR: Early-exit neural networks with adaptive inference benefit from new training methods that prevent new exits from interfering with previously learned ones, using continual learning techniques to balance stability and plasticity.


<details>
  <summary>Details</summary>
Motivation: In early-exit neural networks, sequential training of exits (adding them incrementally to a shared backbone) can cause newly introduced exits to interfere with previously learned ones, degrading the performance of earlier classifiers.

Method: Two alternative approaches: 1) Constrains learning by protecting parameters important for previously trained exits using Elastic Weight Consolidation, and 2) Preserves output distributions of earlier exits as the network adapts using Learning without Forgetting.

Result: Experiments on standard benchmarks show that these approaches consistently improve early-exit performance, achieving higher accuracy over existing sequential training methods and significant performance speedups at low computational budgets.

Conclusion: The proposed methods effectively address the interference problem in sequential training of early-exit networks by applying continual learning principles to balance stability and plasticity, leading to better performance than existing approaches.

Abstract: Early-exiting neural networks enable adaptive inference by allowing inputs to exit at intermediate classifiers, reducing computation for easy samples while maintaining high accuracy. In practice, exits can be trained sequentially by incrementally adding them to a shared backbone; however, this sequential training can cause newly introduced exits to interfere with previously learned ones, degrading the performance of earlier classifiers. We address this problem by retaining the knowledge embedded in existing exits while allowing new ones to specialize. We propose two alternative approaches that operate at different levels of the model. The first constrains learning by protecting parameters that are important for previously trained exits, while the second preserves the output distributions of earlier exits as the network adapts. These alternatives directly reflect the stability-plasticity trade-off studied in continual learning. Accordingly, we leverage \textit{Elastic Weight Consolidation} to constrain critical weights and \textit{Learning without Forgetting} to preserve output distributions. Experiments on standard benchmarks show that our approaches consistently improve early-exit performance, achieving higher accuracy over existing sequential training methods and significant performance speedups at low computational budgets.

</details>


### [163] [COPYCOP: Ownership Verification for Graph Neural Networks](https://arxiv.org/abs/2605.05360)
*Rahul Nandakumar,Deepayan Chakrabarti*

Main category: cs.LG

TL;DR: CopyCop is an algorithm that detects whether two GNNs have a copycat relationship where one was trained to mimic another's node embeddings, even when they have different architectures, weights, and embedding dimensions, and even after adversarial transformations.


<details>
  <summary>Details</summary>
Motivation: There's a need to determine if two GNNs were trained independently or if one was adversarially trained to mimic another's embeddings. Adversaries can train GNNs to copy other models while transforming outputs to obscure the relationship, and existing watermarking/fingerprinting methods fail under these conditions.

Method: The authors propose CopyCop algorithm that can identify copycat GNNs by analyzing relationships between their node embeddings. The method works despite different architectures, weights, embedding dimensions, and adversarial transformations applied to embeddings.

Result: CopyCop successfully identifies copycat GNNs where existing methods fail. It achieves high accuracy on 14 datasets and 5 GNN architectures, and demonstrates robustness against a broad class of adversarial attacks and transformations. The method also has theoretical guarantees.

Conclusion: CopyCop provides an effective solution for graph ownership verification by detecting copycat GNNs even under adversarial conditions, addressing a significant limitation of current watermarking and fingerprinting approaches for graph neural networks.

Abstract: Given two GNNs that output node embeddings, how can we determine if they were trained independently? An adversary could have trained one GNN specifically to mimic the other GNN's embeddings. To obscure this relationship between the GNNs, the adversarial GNN might then transform its output embeddings. The two GNNs could have different architectures, weights, and embedding dimensions, and the adversary can transform the embeddings. Despite these stringent conditions, our algorithm (named CopyCop) can identify such copycat GNNs, unlike existing watermarking and fingerprinting methods. We also provide theoretical guarantees for CopyCop. Finally, experiments on 14 datasets and 5 GNN architectures demonstrate that CopyCop is accurate and robust against a broad class of adversarial attacks and transformations. Code is available at: https://anonymous.4open.science/r/CopyCop-Graph-Ownership-Verification-8143/README.md

</details>


### [164] [SPADE: Faster Drug Discovery by Learning from Sparse Data](https://arxiv.org/abs/2605.05370)
*Rahul Nandakumar,Ben Fauber,Deepayan Chakrabarti*

Main category: cs.LG

TL;DR: SPADE is a novel algorithm that dramatically improves drug discovery efficiency, requiring only 40 tests on average to find 10 high-quality ligands, while being 10x faster than competitors.


<details>
  <summary>Details</summary>
Motivation: Traditional drug discovery has low success rates (less than 5% of candidates pass early stages) and needs methods that work for novel proteins with no prior data. The field requires more efficient iterative selection and testing of candidate ligands.

Method: SPADE (the proposed algorithm) introduces a novel approach to ligand selection that learns from sparse data. It enables efficient iterative testing starting from scratch without prior data on target proteins.

Result: SPADE requires only 40 tests on average to find 10 high-quality ligands. It outperforms deep learning and Bayesian optimization methods in 1-vs-1 comparisons on more proteins, achieving median improvements of 7%-32% in sample efficiency, and is 10x faster than its closest competitor.

Conclusion: SPADE represents a significant advancement in drug discovery efficiency, particularly for novel proteins, by dramatically reducing the number of tests needed while improving speed and performance compared to existing methods.

Abstract: Drug discovery seeks molecules (ligands) that bind strongly and selectively to a target protein. However, fewer than 5% of candidate ligands pass the bar for even the early stages of drug discovery. Furthermore, we want methods that work for novel proteins for which we have no prior data. Starting from scratch, we have to iteratively select and test candidate ligands such that we find enough ligands of the desired quality in as few tests as possible. Our proposed algorithm, named SPADE, introduces a novel approach to ligand selection that requires only 40 tests on average to find 10 high-quality ligands. In one-vs-one comparisons, SPADE outperforms deep learning and Bayesian optimization methods on more proteins, achieving median improvements of 7%-32% in sample efficiency. SPADE is also 10x faster than its closest competitor at scoring candidate drugs. Dataset and code is available at https://anonymous.4open.science/r/SPADE_Fast_Drug_Discovery_by_Learning_from_Sparse_Data-F028/README.md

</details>


### [165] [Neural Co-state Policies: Structuring Hidden States in Recurrent Reinforcement Learning](https://arxiv.org/abs/2605.05373)
*David Leeftink,Max Hinne,Marcel van Gerven*

Main category: cs.LG

TL;DR: The paper shows that hidden states in recurrent RL policies correspond to PMP co-states, enabling Hamiltonian-based interpretation and improved robustness in partially observable continuous control.


<details>
  <summary>Details</summary>
Motivation: Recurrent policies for partially observable RL are black boxes - their internal dynamics are uninterpretable, which limits understanding and robustness in continuous control tasks.

Method: Establishes formal link between hidden states and Pontryagin Minimum Principle co-states, then introduces PMP-derived co-state loss to structure internal dynamics of recurrent architectures.

Result: Approach matches or improves performance on partially observable DMControl tasks, shows robustness against zero-shot OOD sensor masking, and enables Hamiltonian minimization interpretation of readout layers.

Conclusion: Framing recurrent networks as PMP-governed processes provides principled approach to designing robust continuous control policies with interpretable internal dynamics.

Abstract: A key capability of intelligent agents is operating under partial observability: reasoning and acting effectively despite missing or incomplete state observations. While recurrent (memory-based) policies learned via reinforcement learning address this by encoding history into latent state representations, their internal dynamics remain uninterpretable black boxes. This paper establishes a formal link between these hidden states and the Pontryagin minimum principle (PMP) from optimal control. We demonstrate that for standard recurrent architectures, latent representations map directly to PMP co-states, which allows the readout layer to be interpreted as performing Hamiltonian minimization. Because standard reward maximization does not naturally discover this alignment, we introduce a PMP-derived co-state loss to explicitly structure the internal dynamics. Empirically, this approach matches or improves performance on partially observable DMControl tasks, and is robust against zero-shot out-of-distribution sensor masking. By framing recurrent networks as dynamic processes governed by the minimum principle, we provide a principled approach to designing robust continuous control policies.

</details>


### [166] [Conditional Diffusion Under Linear Constraints: Langevin Mixing and Information-Theoretic Guarantees](https://arxiv.org/abs/2605.05387)
*Ahmad Aghapour,Erhan Bayraktar,Asaf Cohen*

Main category: cs.LG

TL;DR: The paper analyzes and improves conditional sampling for linear inverse problems using pretrained diffusion models, proposing a projected-Langevin initialization and guided reverse denoising to address bias in existing projection-based methods.


<details>
  <summary>Details</summary>
Motivation: Existing projection-based samplers for zero-shot conditional sampling with diffusion models enforce measurement consistency but fail to properly distribute probability mass along the feasible set, leading to biased conditional samples for linear inverse problems like inpainting and super-resolution.

Method: The authors analyze the issue through a normal-tangent decomposition of the score function. For Gaussian noising, they show only the tangent conditional score is unknown. They propose a two-stage approach: 1) projected-Langevin initialization, and 2) guided reverse denoising to properly sample the conditional distribution.

Result: Theoretical analysis shows the error from using unconditional tangent score is bounded by dimension-free conditional mutual information between observed and unobserved components. Experimental results demonstrate the proposed method outperforms strong projection-based baselines in inpainting and super-resolution tasks.

Conclusion: The paper provides both theoretical understanding and practical improvement for conditional sampling with diffusion models in linear inverse problems, showing that proper initialization and guidance are crucial for unbiased conditional sampling beyond just enforcing measurement consistency.

Abstract: We study zero-shot conditional sampling with pretrained diffusion models for linear inverse problems, including inpainting and super-resolution. In these problems, the observation determines only part of the unknown signal. The remaining degrees of freedom must be sampled according to the correct conditional data distribution. Existing projection-based samplers enforce measurement consistency by correcting the observed component during reverse diffusion. However, measurement consistency alone does not determine how probability mass should be distributed along the feasible set, and this can lead to biased conditional samples.
  We analyze this issue through a normal--tangent decomposition of the score function. For Gaussian noising, the observed-direction score is exactly determined by the measurement; only the tangent conditional score is unknown. We prove that the error from replacing this score by the unconditional tangent score is upper bounded by a dimension-free conditional mutual information between observed and unobserved components. This gives an information-theoretic decomposition into initialization and pathwise score-mismatch errors. Motivated by the theory, we propose a projected-Langevin initialization followed by guided reverse denoising, which outperforms a strong projection-based baseline in inpainting and super-resolution experiments.

</details>


### [167] [Two-Stage Learned Decomposition for Scalable Routing on Multigraphs](https://arxiv.org/abs/2605.05389)
*Filip Rydin,Morteza Haghir Chehreghani,Balázs Kulcsár*

Main category: cs.LG

TL;DR: NEPF (Node-Edge Policy Factorization) is a neural method for Vehicle Routing Problems on multigraphs that splits routing policy into node permutation and edge selection stages for scalability, matching or outperforming SOTA with faster training/inference.


<details>
  <summary>Details</summary>
Motivation: Most neural VRP methods work only on Euclidean settings or simple graphs, but not on multigraphs where parallel edges represent distinct travel options with varying trade-offs (distance vs time). Existing methods for multigraphs face major scalability issues.

Method: Node-Edge Policy Factorization (NEPF) approach splits routing policy into: 1) node permutation stage, and 2) edge selection stage. Uses pre-encoding edge aggregation scheme, non-autoregressive architecture for edge stage, and hierarchical reinforcement learning to train stages jointly.

Result: Experiments across six VRP variants show NEPF matches or outperforms state-of-the-art in solution quality while being significantly faster in both training and inference.

Conclusion: NEPF effectively addresses scalability issues in neural VRP methods for multigraphs, providing a practical approach that maintains solution quality while dramatically improving computational efficiency.

Abstract: Most neural methods for Vehicle Routing Problems (VRPs) are limited to Euclidean settings or simple graphs. In this work, we instead consider multigraphs, where parallel edges represent distinct travel options with varying trade-offs (e.g., distance vs time). Few methods are designed for such formulations and those that do exist face major scalability issues. We mitigate these scalability issues via a Node-Edge Policy Factorization (NEPF) approach, which splits the routing policy into a node permutation stage and an edge selection stage. To enable the decomposition, we introduce a pre-encoding edge aggregation scheme and a non-autoregressive architecture for the edge stage, as well as a hierarchical reinforcement learning method to train the stages jointly. Our experiments across six VRP variants demonstrate that NEPF matches or outperforms the state-of-the-art in terms of solution quality, while being significantly faster in training and inference.

</details>


### [168] [Differentiable Parameter Optimization for DAEs with State-Dependent Events](https://arxiv.org/abs/2605.05395)
*Ion Matei,Maksym Zhenirovskyy,Anthony Wong*

Main category: cs.LG

TL;DR: This paper develops two differentiable gradient computation methods for parameter learning in semi-explicit differential-algebraic equations (DAEs) with state-dependent events, addressing challenges with algebraic variables, event timing, and discontinuities.


<details>
  <summary>Details</summary>
Motivation: Gradient-based parameter learning for DAEs with state-dependent events is challenging due to: (1) algebraic variables being implicitly defined, (2) event times depending on parameters, and (3) reset maps introducing discontinuities. These difficulties motivate developing systematic gradient computation methods for such hybrid systems.

Method: Two complementary gradient computation strategies: (1) Automatic-differentiation-through-simulation that solves algebraic variables via implicit function theorem and handles events through segmented differentiable integration. (2) Explicit discrete-adjoint method that represents forward simulation as event-split residual system and computes gradients by solving for Lagrange multipliers of smooth-segment and event residuals.

Result: Both methods successfully provide gradients for event paths selected by forward simulation. The formulation clarifies that residual terms in adjoint method are equality constraints rather than heuristic penalties. The approaches are valid under fixed event ordering and transversal guard crossings.

Conclusion: The paper presents systematic differentiable parameter optimization for semi-explicit DAEs with events, comparing two gradient computation approaches in terms of gradient interpretation, event-time handling, implementation complexity, and local validity, advancing gradient-based learning for hybrid constrained systems.

Abstract: Differential-algebraic equations (DAEs) with state-dependent events arise in systems whose continuous dynamics are constrained by algebraic equations and interrupted by mode changes, switching logic, impacts, or state reinitializations. Gradient-based parameter learning for such systems is challenging because algebraic variables are implicitly defined, event times depend on the parameters, and reset maps introduce discontinuities. This paper studies differentiable parameter optimization for semi-explicit DAEs with events. We formulate the learning problem as a constrained least-squares problem with DAE dynamics, algebraic constraints, guard equations, and reset maps. We then develop two complementary gradient-computation strategies. The first is an automatic-differentiation-through-simulation method that solves algebraic variables inside the vector field, differentiates the algebraic solve using the implicit function theorem, and handles events through segmented differentiable integration. The second is an explicit discrete-adjoint method that represents the forward simulation as an event-split residual system and computes gradients by solving for the Lagrange multipliers of smooth-segment and event residuals. The formulation clarifies that residual terms in the adjoint method are equality constraints, not heuristic penalties. We compare the two approaches in terms of gradient interpretation, event-time handling, implementation complexity, and local validity. Both methods provide gradients for the event path selected by the forward simulation and are valid under fixed event ordering and transversal guard crossings.

</details>


### [169] [Information Theoretic Adversarial Training of Large Language Models](https://arxiv.org/abs/2605.05415)
*Yiwei Zhang,Jeremiah Birrell,Reza Ebrahimi,Rouzbeh Behnia,Jason Pacheco,Elisa Bertino*

Main category: cs.LG

TL;DR: WARDEN is a distributionally robust adversarial training framework for LLMs that optimizes worst-case adversarial loss using f-divergence ambiguity sets, significantly reducing attack success rates while maintaining computational efficiency and model utility.


<details>
  <summary>Details</summary>
Motivation: LLMs remain vulnerable to adversarial prompting despite safety improvements, and existing adversarial training methods are computationally expensive and difficult to scale. There's a need for more efficient and effective robustness techniques.

Method: Proposes WARDEN - a distributionally robust adversarial training framework that dynamically reweights adversarial examples through an f-divergence ambiguity set around the empirical training distribution. Uses convex dual formulation to optimize worst-case adversarial loss within a divergence ball, reducing to a log-sum-exp form under KL divergence with dynamic parameter control.

Result: WARDEN significantly reduces attack success rates across multiple LLMs and attack settings, with computational and utility costs comparable to existing baselines (CAT-, CAPO-, and MixAT-based methods).

Conclusion: WARDEN represents a practical approach for scalable robust alignment of LLMs, introducing a new class of information-theoretic objectives that effectively balance robustness with computational efficiency and model utility.

Abstract: Large language models (LLMs) remain vulnerable to adversarial prompting despite advances in alignment and safety, often exhibiting harmful behaviors under novel attack strategies. While adversarial training can improve robustness, existing approaches are computationally expensive and difficult to scale. Recent continuous adversarial training methods, such as Continuous adversarial training (CAT) and Continuous Adversarial Preference Optimization (CAPO), address this challenge by leveraging gradient-based perturbations in the embedding space, enabling more efficient and expressive attacks. Building on this paradigm, we propose WARDEN, a distributionally robust adversarial training framework for LLMs that dynamically reweights adversarial examples through an f -divergence ambiguity set around the empirical training distribution. Our method optimizes the worst-case adversarial loss within a divergence ball around the empirical data distribution, automatically emphasizing harder adversarial examples. Using the convex dual formulation, the objective reduces to a log-sum-exp form under the KL divergence, with a dynamical parameter controlling the strength of reweighting. This study leads to a new class of information-theoretic objectives that significantly reduce attack success rates while maintaining model utility. Across multiple LLMs and attack settings, WARDEN substantially reduces attack success rates with computational and utility costs comparable to CAT-, CAPO-, and MixAT-based baselines, making it a practical approach for scalable robust alignment.

</details>


### [170] [Active Learning for Conditional Generative Compressed Sensing](https://arxiv.org/abs/2605.05435)
*Alexander DeLise,Nick Dexter*

Main category: cs.LG

TL;DR: A framework for conditional generative compressed sensing that separates sampling distribution prompts from recovery model prompts, with theoretical bounds showing prompt-matched sampling retains optimal complexity while mismatched prompts incur compatibility penalties.


<details>
  <summary>Details</summary>
Motivation: To develop a conditional version of generative compressed sensing for image recovery from limited Fourier measurements, addressing how different prompts should be used for designing sampling distributions versus defining recovery models in prompt-conditioned generative systems.

Method: The framework separates two conditioning roles: prompt for designing sampling distribution and prompt for defining recovery model. Uses prompt-conditioned generative models with ReLU and Lipschitz generators, analyzes prompt-matched Christoffel sampling, and evaluates prompt mismatch effects. Experimental validation with Stable Diffusion.

Result: Theoretical analysis shows prompt-matched Christoffel sampling retains the same Christoffel complexity constant as existing near-optimal generative compressed sensing theory. Prompt mismatch incurs an explicit compatibility penalty. Experiments demonstrate prompts meaningfully reshape Christoffel sampling distributions and influence image recovery.

Conclusion: Prompts should be treated as design variables with distinct effects on sensing, approximation, and recovery in conditional generative compressed sensing systems, with prompt-matched sampling maintaining optimal theoretical guarantees while mismatched prompts degrade performance through quantifiable compatibility penalties.

Abstract: Generative compressed sensing uses the range of a pretrained generator as a nonlinear model for recovering structured signals from limited measurements. We study a conditional version of this problem for image recovery from subsampled Fourier measurements using prompt-conditioned generative models. Our framework separates two roles of conditioning: the prompt used to design the sampling distribution and the prompt used to define the recovery model. For ReLU and Lipschitz conditional generators, we prove stable recovery bounds showing that prompt-matched Christoffel sampling retains the same Christoffel complexity constant as existing near-optimal generative compressed sensing theory, while prompt mismatch incurs an explicit compatibility penalty. Experiments with Stable Diffusion show that prompts meaningfully reshape Christoffel sampling distributions and influence image recovery. Overall, our results suggest that prompts should be treated as design variables with distinct effects on sensing, approximation, and recovery.

</details>


### [171] [On Semantic Loss Fine-Tuning Approach for Preventing Model Collapse in Causal Reasoning](https://arxiv.org/abs/2605.05438)
*Pratik Deshmukh,Atirek Gupta*

Main category: cs.LG

TL;DR: Standard fine-tuning causes catastrophic collapse in causal reasoning models; proposed semantic loss with graph constraints prevents collapse and enables stable reasoning.


<details>
  <summary>Details</summary>
Motivation: Standard transformer fine-tuning on causal reasoning tasks leads to catastrophic model collapse where models learn trivial solutions like always predicting "Yes" or "No" instead of learning actual causal reasoning.

Method: Proposed semantic loss function with graph-based logical constraints and dynamic lambda scheduling to prevent model collapse; comprehensive evaluation on 200,000+ samples across five model variants.

Result: Semantic loss prevents 100% collapse rate seen in baselines; achieves 70.4% accuracy on transitivity and 68.6% on d-separation tasks (42.7% improvement); adversarial evaluation shows 67-70% accuracy vs. collapsed models' 43-71% catastrophic failure.

Conclusion: Semantic loss is essential, not optional, for stable causal reasoning in transformers; prevents trivial solutions and enables models to learn actual causal structure rather than exploiting statistical patterns.

Abstract: Standard fine-tuning of transformer models on causal reasoning tasks leads to catastrophic model collapse, where models learn trivial solutions such as always predicting "Yes" or "No" regardless of input structure. We demonstrate that fine-tuning Gemma 270M on transitivity and d-separation tasks without semantic loss results in 100% collapse rate, with models achieving misleadingly high accuracy (73.9%) while learning no causal reasoning. We propose a semantic loss function with graph-based logical constraints and dynamic lambda scheduling that prevents this collapse. Our approach achieves 70.4% accuracy on transitivity tasks and 68.6% on d-separation tasks with stable, context-dependent predictions, representing a 42.7% improvement over collapsed baselines. Adversarial evaluation on 1,000 structural reasoning samples shows semantic models achieve 67-70% accuracy while collapsed models fail catastrophically at 43-71%. We validate our findings through comprehensive benchmarking on 200,000+ evaluation samples across five model variants, demonstrating that semantic loss is essential and not optional, for stable causal reasoning in transformers.

</details>


### [172] [Robustness of Graph Self-Supervised Learning to Real-World Noise: A Case Study on Text-Driven Biomedical Graphs](https://arxiv.org/abs/2605.05463)
*Othmane Kabal,Mounira Harzallah,Fabrice Guillet,Hideaki Takeda,Ryutaro Ichise*

Main category: cs.LG

TL;DR: Graph Self-Supervised Learning for unsupervised term typing using text-driven graphs, with a focus on robustness to real-world noise through a dual-graph evaluation protocol comparing noisy vs. clean graphs.


<details>
  <summary>Details</summary>
Motivation: Existing GSSL methods assume clean, manually curated graphs, but recent NLP advances enable automatic extraction of knowledge graphs from text, introducing substantial real-world noise. This type of noise remains unexplored as prior robustness studies rely on synthetic perturbations.

Method: NATD-GSSL framework combining automatic graph construction, graph refinement, and GSSL. Uses dual-graph protocol comparing noisy graph from MedMentions with clean UMLS reference graph, aligned through shared gold standard. Evaluates various pretext tasks and GNN architectures.

Result: 1) Pretext task matters: relation reconstruction is highly sensitive to noise and benefits from well-defined schemas; feature reconstruction is more robust; contrastive objectives are less affected but depend on task alignment. 2) GNN architecture matters: bidirectional relational message-passing performs better on noisy graphs; unidirectional relational ones work best on clean graphs. 3) NATD-GSSL achieves up to 7% improvement over pretrained language model baselines.

Conclusion: The study provides practical guidance for applying GSSL to real-world, noisy graphs, highlighting that different pretext tasks and architectures exhibit varying robustness to noise. The findings can inform better design choices when working with automatically extracted text-driven graphs.

Abstract: Graph Self-Supervised Learning (GSSL) offers a powerful paradigm for learning graph representations without labeled data. However, existing work assumes clean, manually curated graphs. Recent advances in NLP enable the large-scale automatic extraction of knowledge graphs from text, opening new opportunities for GSSL while introducing substantial real-world noise. This type of noise remains largely unexplored, as prior robustness studies typically rely on synthetic perturbations. To address this gap, we present the first comprehensive evaluation of GSSL methods on text-driven graphs for unsupervised term typing. We introduce Noise-Aware Text-Driven Graph GSSL (NATD-GSSL), a unified framework that combines automatic graph construction, graph refinement, and GSSL. Our evaluation follows a dual-graph protocol that contrasts a noisy graph derived from MedMentions with a clean Unified Medical Language System (UMLS) reference graph, aligned through a shared gold standard. Our results reveal variability in robustness across both pretext tasks and Graph Neural Network (GNN) architectures. Relation reconstruction is highly sensitive to noise and benefits from well-defined schemas, whereas feature reconstruction is considerably more robust, achieving performance comparable to clean-graph settings. Contrastive objectives are generally less affected by noise but depend strongly on alignment with downstream tasks. GNN architecture also plays a critical role: bidirectional relational message-passing designs are better suited to noisy, text-driven graphs, while unidirectional relational ones perform best on clean graphs. Overall, NATD-GSSL provides practical guidance for applying GSSL to real-world, noisy graphs and achieves up to a 7\% improvement over pretrained language model baselines. All code and benchmarks are publicly available at https://github.com/OthmaneKabal/MC2GAE.

</details>


### [173] [A Unified Benchmark for Evaluating Knowledge Graph Construction Methods and Graph Neural Networks](https://arxiv.org/abs/2605.05476)
*Othmane Kabal,Mounira Harzallah,Fabrice Guillet,Hideaki Takeda,Ryutaro Ichise*

Main category: cs.LG

TL;DR: A benchmark for evaluating Graph Neural Networks on noisy knowledge graphs from text, plus assessing graph construction methods in biomedical domain.


<details>
  <summary>Details</summary>
Motivation: Knowledge graphs from text are noisy/fragmented/inconsistent, affecting GNN performance; hard to tell if poor results come from learning models or graph quality itself.

Method: Create benchmark with two automatically constructed graphs from different extraction methods, plus expert-curated reference graph as upper bound; use semi-supervised node classification for systematic evaluation.

Result: Provides standardized, reproducible, extensible framework for comparing construction methods and evaluating GNN robustness on noisy text-derived graphs.

Conclusion: Benchmark enables controlled comparison of graph construction methods and systematic evaluation of GNN robustness, facilitating better assessment in real-world applications.

Abstract: Knowledge graphs automatically constructed from text are increasingly used in real-world applications. However, their inherent noise, fragmentation, and semantic inconsistencies significantly affect the performance of Graph Neural Networks (GNNs) on downstream tasks. Assessing their performance and robustness remains difficult, as it is often unclear whether observed results stem from the learning model or from the quality of the constructed graph itself. In this work, we introduce a dual-purpose benchmark designed to jointly evaluate (i) the performance of GNNs on noisy, text-derived graphs and (ii) the effectiveness of graph construction methods on a downstream task. The benchmark is built in the biomedical domain from a single textual corpus and includes two automatically constructed graphs generated using different extraction methods, alongside a high-quality reference graph curated by experts that serves as an upper performance bound. This design enables controlled comparison of construction methods and systematic evaluation of GNN robustness through semi-supervised node classification. We further provide a standardized, reproducible, and extensible evaluation framework, facilitating the integration of new graph extraction methods and learning models.

</details>


### [174] [GRALIS: A Unified Canonical Framework for Linear Attribution Methods via Riesz Representation](https://arxiv.org/abs/2605.05480)
*Raimondo Fanale*

Main category: cs.LG

TL;DR: GRALIS is a unified mathematical framework that establishes a representation theory for XAI attribution methods, proving all additive, linear, continuous attribution functionals have a unique canonical form, enabling formal comparison and providing seven theoretical guarantees that individual methods lack.


<details>
  <summary>Details</summary>
Motivation: Current XAI attribution methods (GradCAM, SHAP, LIME, Integrated Gradients) operate on separate theoretical foundations and are not formally comparable, making it difficult to systematically evaluate and compare attribution approaches.

Method: Proposed GRALIS (Gradient-Riesz Averaged Locally-Integrated Shapley) framework based on Riesz Representation Theorem, establishing that every additive, linear, continuous attribution functional on L^2(Q,mu) admits unique canonical representation (Q, w, Delta). The framework includes extensions like multi-scale GRALIS with minimum-variance weights and provides algebraic justification via Mobius transform.

Result: GRALIS satisfies 13.5/14 axiomatic properties vs. 2.5-6/14 for individual methods, includes seven formal theorems with guarantees like canonical form, exact completeness, Monte Carlo convergence, exact Shapley Interaction Values, ANOVA decomposition, Sobol sensitivity generalization, and multi-scale extension. Preliminary validation on BreaKHis dataset shows improved deletion faithfulness AUC (+0.015), 96% class-conditional consistency, SAL=0.762±0.109 and sparsity index 0.39.

Conclusion: GRALIS provides a unified mathematical foundation for XAI attribution methods, enabling formal comparison and offering comprehensive theoretical guarantees that individual methods cannot achieve, representing a significant advance in the formalization and evaluation of attribution techniques in deep learning.

Abstract: The main XAI attribution methods for deep neural networks -- GradCAM, SHAP, LIME, Integrated Gradients -- operate on separate theoretical foundations and are not formally comparable. We present GRALIS (Gradient-Riesz Averaged Locally-Integrated Shapley), a mathematical framework establishing a representation theory for attributions: every additive, linear, and continuous attribution functional on L^2(Q,mu) admits a unique canonical representation (Q, w, Delta), proved necessary by the Riesz Representation Theorem. This class encompasses SHAP, IG, LIME and linearized GradCAM, but excludes nonlinear functionals such as standard GradCAM or attention maps. Seven formal theorems provide simultaneous guarantees absent in any individual method: (T1) necessary canonical form; (T2) exact completeness; (T3) Monte Carlo convergence O(1/sqrt(m))+O(1/k); (T4) exact Shapley Interaction Values; (T5) Hoeffding ANOVA decomposition; (T6) Sobol sensitivity generalization; (T7) multi-scale extension (MS-GRALIS) with minimum-variance weights. An algebraic appendix justifies the GRALIS-SIV correspondence via the Mobius transform without circularity. GRALIS satisfies 13.5/14 axiomatic properties vs. 2.5-6/14 for individual methods, including completeness, sensitivity, locality, order-k interactions and optimal multi-scale aggregation simultaneously. Preliminary validation on BreaKHis (1,187 histology images, DenseNet-121) reports deletion faithfulness AUC +0.015 (malignant), 96% class-conditional consistency, SAL = 0.762+/-0.109 and sparsity index 0.39. Extended comparison with baseline XAI methods is planned for a companion paper.

</details>


### [175] [Approximate Next Policy Sampling: Replacing Conservative Target Policy Updates in Deep RL](https://arxiv.org/abs/2605.05481)
*Dillon Sandhu,Ronald Parr*

Main category: cs.LG

TL;DR: ANPS modifies training distribution instead of policy constraints to solve RL's chicken-and-egg problem, enabling larger policy updates while maintaining safety.


<details>
  <summary>Details</summary>
Motivation: To solve the classic reinforcement learning problem where value functions need accuracy on unknown future state distributions, without resorting to conservative policy updates that limit improvement.

Method: Introduces Approximate Next Policy Sampling (ANPS) and Stable Value Approximate Policy Iteration (SV-API), which keeps target policy fixed while behavioral policy collects experience, only updating policy after convergence criteria met.

Result: SV-PPO (PPO with SV-API) matches or improves performance on Atari and continuous control benchmarks while executing substantially larger target policy updates.

Conclusion: ANPS provides a viable alternative to conservative updates for solving the distribution mismatch problem in RL, enabling safer and more aggressive policy improvements.

Abstract: We revisit a classic "chicken-and-egg" problem in reinforcement learning: to safely improve a policy, the value function must be accurate on the state-visitation distribution of the updated policy. That distribution over states is unknown and cannot be sampled for the purposes of training the value function. Conservative updates solve this problem, but at the cost of shrinking the policy update. This paper explores an alternative solution, Approximate Next Policy Sampling (ANPS), which addresses the problem by modifying the training distribution rather than constraining the policy update. ANPS is satisfied if the distribution of the training data approximates that of the next policy. To demonstrate the feasibility and efficacy of ANPS, we introduce Stable Value Approximate Policy Iteration (SV-API). SV-API modifies the standard approximate policy iteration loop to hold the target policy fixed while an iteratively updated behavioral policy gathers relevant experience. It only commits to a new policy once a convergence criterion has been met. If certain stability criteria are met, the update is guaranteed to be safe; otherwise, it remains no less safe than standard approximate policy iteration. Applying SV-API to PPO yields Stable Value PPO (SV-PPO), which matches or improves performance on high-dimensional discrete (Atari) and continuous control benchmarks while executing substantially larger target policy updates. These results demonstrate the viability of ANPS as a new solution to this classic challenge in RL.

</details>


### [176] [A Robust Foundation Model for Conservation Laws: Injecting Context into Flux Neural Operators via Recurrent Vision Transformers](https://arxiv.org/abs/2605.05488)
*Taeyoung Kim,Joon-Hyuk Ko*

Main category: cs.LG

TL;DR: A hypernetwork architecture that combines finite volume methods with neural operators and ViT-based context injection to solve conservation laws without explicit PDE knowledge.


<details>
  <summary>Details</summary>
Motivation: To create a neural operator model that can solve conservation laws without requiring explicit access to governing equations or PDE coefficients, while maintaining robustness and generalization capabilities.

Method: Augments Flux Neural Operator with ViT-based context injection. Uses hypernetwork formulation: extracts solution dynamics over temporal window, encodes with recurrent Vision Transformer, generates parameters for context-conditioned neural operator.

Result: Preserves Flux NO's robustness, generalization, and long-time prediction advantages while delivering reliable numerical solutions across broad range of conservative systems, including unseen fluxes.

Conclusion: The proposed architecture successfully enables inference and solution of conservation laws without explicit equation access, demonstrating strong performance across various conservative systems.

Abstract: We propose an architecture that augments the Flux Neural Operator (Flux NO), which combines the classical finite volume method (FVM) with neural operators, with ViT-based context injection. Our model is formulated as a hypernetwork: it extracts solution dynamics over a finite temporal window, encodes them with a recurrent Vision Transformer, and generates the parameters of a context-conditioned neural operator. This enables the model to infer and solve conservation laws without explicit access to the governing equation or PDE coefficients. Experimentally, we show that the proposed method preserves the robustness, generalization ability, and long-time prediction advantages of Flux NO over standard neural operators, while delivering reliable numerical solutions across a broad range of conservative systems, including previously unseen fluxes. Our code is available at https://github.com/xx257xx/CONTEXT_FLUX_NO.

</details>


### [177] [MEMOA: Massive Mixtures of Online Agents via Mean-Field Decentralized Nash Equilibria](https://arxiv.org/abs/2605.05492)
*Xuwei Yang,David B. Emerson,Fatemeh Tavakoli,Anastasis Kratsios*

Main category: cs.LG

TL;DR: Federated learning faces scalability issues with large AI populations, so the paper proposes a decentralized mean-field approach with optimal policy derived in closed form that minimizes worst-client regret and converges to Nash-optimal centralized policy.


<details>
  <summary>Details</summary>
Motivation: Federated learning has become important for training large populations of AI agents, but suffers from computational and communication costs that don't scale well with increasing numbers of agents. Decentralized strategies are needed where each agent acts autonomously using only local state and minimal ensemble summary (mean-field).

Method: Derives unique optimal decentralized policy in closed form using worst-client/minimax criterion to minimize under-performer regret (maximal online cost incurred by weakest agent). Uses online weighting mechanism to optimize server-computed mixture of client predictions, improving both mean prediction and weakest-client prediction.

Result: Proves decentralized policy asymptotically converges to Nash-optimal centralized policy in large-population limit (direct computation of which is not scalable). Numerical experiments verify theoretical guarantees and show decentralized policy outperforms natural greedy decentralized baselines.

Conclusion: Decentralized mean-field approach provides scalable solution for federated learning with large agent populations, achieving optimality through minimax criterion while maintaining computational efficiency and converging to centralized optimal in large-scale limit.

Abstract: In the modern age of large-scale AI, federated learning has become an increasingly important tool for training large populations of AI agents; however, its computational and communication costs can rapidly fail to scale with the number of agents. This is precisely where decentralized agentic strategies shine: each agent acts autonomously, using only its own state together with a minimal summary of the ensemble, namely the mean-field. We derive the unique optimal decentralized policy in closed form. Optimality is characterized through a worst-client/minimax criterion: minimizing the under-performer regret, namely the maximal online cost incurred by the weakest agent in the ensemble. We further prove that the resulting decentralized policy asymptotically converges, in the large-population limit, to the Nash-optimal centralized policy, whose direct computation is not scalable. We use an online weighting mechanism to optimize the server-computed mixture of client predictions, thereby improving the mean prediction in addition to the previously optimized weakest-client prediction. Numerical experiments verify our theoretical guarantees and demonstrate that our decentralized policy typically outperforms natural greedy decentralized baselines.

</details>


### [178] [Shortcut Solutions Learned by Transformers Impair Continual Compositional Reasoning](https://arxiv.org/abs/2605.05495)
*William T. Redman,Erik C. Johnson,Brian Robinson*

Main category: cs.LG

TL;DR: Transformer models (BERT and ALBERT) were tested on continual learning tasks using the LEGO framework. BERT learned shortcut solutions that limited generalization, while ALBERT showed better CL performance via For loop-esque solutions, though both failed on composition across experiences.


<details>
  <summary>Details</summary>
Motivation: To understand how Transformer neural network models can leverage their representations to learn new, related experiences in continual learning settings, and to systematically investigate their ability to perform compositional reasoning across domains.

Method: Expanded the Learning Equality and Group Operations (LEGO) framework to a continual learning setting ("continual LEGO"). Tested feedforward (BERT) and recurrent (ALBERT) Transformer models on CL tasks requiring identification and exploitation of common features across domains.

Result: BERT learned shortcut solutions that limited generalization and prevented forward transfer. ALBERT learned For loop-esque solutions leading to better CL performance. Both models failed on CL requiring composition across experiences. ALBERT's performance could be rescued with cross-experience training strategies, but BERT's shortcut solutions became entrenched.

Conclusion: Recurrent ALBERT models have an inductive bias better suited for continual learning compared to feedforward BERT models. The interplay between Transformer architecture and emergent computational solutions warrants further investigation for improving continual learning capabilities.

Abstract: Identifying and exploiting common features across domains is at the heart of the human ability to make analogies, and is believed to be crucial for the ability to continually learn. To do this successfully, general and flexible computational strategies must be developed. While the extent to which Transformer neural network models can perform compositional reasoning has been the subject of intensive recent investigation, little work has been done to systematically understand how well these models can leverage their representations to learn new, related experiences. To address this gap, we expand the previously developed Learning Equality and Group Operations (LEGO) framework to a continual learning (CL) setting ("continual LEGO"). Using this continual LEGO experimental paradigm, we study the capability of feedforward and recurrent Transformer models to perform CL. We find that BERT, a canonical feedforward Transformer model, learns shortcut solutions that limits its ability to generalize and prevents strong forward transfer to new experiences. In contrast, we find evidence supporting the hypothesis that ALBERT, a recurrent version of BERT, learns a For loop-esque solution, which leads to better CL performance. When applying BERT and ALBERT models to a CL setting that requires composition across experiences, we find that both model families fail. Our investigation suggests that ALBERT models can have their performance drop rescued by use of training strategies that combine data across experiences, but this is not true for BERT models, where a detrimental shortcut solution becomes entrenched with initial training. Our results demonstrate that the recurrent ALBERT model may have an inductive bias better suited for CL and motivate future investigation of the interplay between Transformer architecture and computational solutions that emerge in modern models and tasks.

</details>


### [179] [Online Localized Conformal Prediction](https://arxiv.org/abs/2605.05497)
*Yuheng Lai,Garvesh Raskutti*

Main category: cs.LG

TL;DR: OLCP and OLCP-Hedge are online conformal prediction methods that combine online adaptation with covariate-dependent localization to maintain validity while reducing prediction set width under covariate heterogeneity.


<details>
  <summary>Details</summary>
Motivation: Conformal prediction requires exchangeability, which is violated in online learning and time-series settings. Existing online methods like ACI achieve long-run validity but are inefficient under covariate heterogeneity because they rely on global calibration, leading to wide prediction sets.

Method: OLCP combines online adaptation with covariate-dependent localization to reflect heterogeneity. OLCP-Hedge extends this by performing bandwidth selection as an online expert aggregation problem using constrained online convex optimization to reduce sensitivity to localization bandwidth.

Result: The proposed methods maintain valid long-run coverage with narrower prediction sets than existing baselines, as demonstrated through simulations and real-data experiments.

Conclusion: OLCP and OLCP-Hedge provide efficient online conformal prediction methods for settings with covariate heterogeneity, offering both coverage guarantees and improved efficiency compared to existing approaches.

Abstract: Conformal prediction is a framework that provides valid uncertainty quantification for general models with exchangeable data. However, in the online learning and time-series settings, exchangeability is not satisfied. Existing online conformal methods, such as adaptive conformal inference (ACI), can achieve long-run validity, yet they remain inefficient under covariate heterogeneity because they rely on global calibration. We propose \emph{Online Localized Conformal Prediction (OLCP)}, which combines online adaptation with covariate-dependent localization to better reflect heterogeneity. To reduce sensitivity to the localization bandwidth, we further develop \emph{OLCP-Hedge}, which performs bandwidth selection as an online expert aggregation problem using a constrained online convex optimization framework. Importantly, we provide coverage guarantees for both algorithms and demonstrate through simulations and real-data experiments that the proposed methods attain valid long-run coverage with narrower prediction sets than existing baselines.

</details>


### [180] [Non-Myopic Active Feature Acquisition via Pathwise Policy Gradients](https://arxiv.org/abs/2605.05511)
*Linus Aronsson,Morteza Haghir Chehreghani*

Main category: cs.LG

TL;DR: NM-PPG: A non-myopic pathwise policy gradient method for active feature acquisition that enables end-to-end optimization through continuous relaxations of feature acquisition decisions.


<details>
  <summary>Details</summary>
Motivation: Active feature acquisition problems involve costly feature acquisition where learners need to adaptively decide which features to acquire and when to stop. Standard approaches suffer from high variance in policy gradients and myopic decision-making.

Method: Formulates AFA as a POMDP and introduces continuous relaxation of acquisition process for pathwise gradients through full trajectories. Uses straight-through rollout scheme with hard acquisitions in forward pass and soft relaxations in backward pass. Stabilizes with entropy regularization and staged temperature sharpening.

Result: NM-PPG demonstrates superior performance compared to state-of-the-art AFA baselines on both synthetic and real-world datasets.

Conclusion: The proposed NM-PPG method effectively addresses limitations of previous AFA approaches by enabling non-myopic, end-to-end optimization through continuous relaxations, leading to improved performance in cost-sensitive feature acquisition settings.

Abstract: Active feature acquisition (AFA) considers prediction problems in which features are costly to obtain and the learner adaptively decides which feature values to acquire for each instance and when to stop and predict. AFA can be formulated as a partially observable Markov decision process (POMDP), which naturally admits a sequential decision-making perspective. In this paper, we present non-myopic pathwise policy gradients (NM-PPG), a new AFA method built around this formulation. We introduce a continuous relaxation of the acquisition process that enables pathwise gradients through the full acquisition trajectory, avoiding the high variance of standard score-function policy gradients while allowing end-to-end optimization of a non-myopic acquisition policy. To better align training with deployment, we further develop a straight-through rollout scheme that follows hard feature acquisitions in the forward pass while backpropagating through the corresponding soft relaxation in the backward pass. We stabilize optimization with entropy regularization and staged temperature sharpening. Experiments on both synthetic and real-world datasets demonstrate that NM-PPG yields superior performance relative to state-of-the-art AFA baselines.

</details>


### [181] [OpenG2G: A Simulation Platform for AI Datacenter-Grid Runtime Coordination](https://arxiv.org/abs/2605.05519)
*Jae-Won Chung,Zhirui Liang,Yanyong Mao,Jiasi Chen,Mosharaf Chowdhury,Vladimir Dvorkin*

Main category: cs.LG

TL;DR: OpenG2G is a simulation platform that models coordination between AI datacenters and power grids to address grid capacity challenges from AI's growing compute demand.


<details>
  <summary>Details</summary>
Motivation: AI's increasing compute demand creates major grid capacity and reliability challenges, causing multi-year interconnection delays for new datacenters and bottlenecking AI growth. Datacenters need to offer rapid power flexibility to ease grid strain.

Method: Developed OpenG2G simulation platform with modular architecture: datacenter backend using real measurements of production-grade AI services, grid backend built on high-fidelity grid simulators, and generic controller interface connecting them. Allows implementation and comparison of various control paradigms (classic, optimization, and learning-based).

Result: OpenG2G can answer a wide range of coordination questions and quantify how AI model and deployment choices affect datacenter flexibility and coordination outcomes. Demonstrated usefulness through realistic grid scenarios and AI workloads.

Conclusion: OpenG2G provides a versatile simulation platform to study datacenter-grid coordination, enabling better understanding of large datacenters' impact on grids and facilitating design of effective coordination strategies to address AI's growing energy demands.

Abstract: AI's growing compute demand and new datacenter buildouts present major capacity and reliability challenges for the electricity grid, leading to multi-year interconnection delays for new datacenters and bottlenecking AI growth. To ease this strain, datacenters increasingly offer rapid power flexibility in response to grid signals, where the datacenter can increase or decrease its power consumption by adapting its workload in real time.
  In order to understand the impact of large datacenters on the grid and to facilitate the design of effective coordination strategies, we build OpenG2G, a simulation platform for AI datacenter-grid runtime coordination. We show that OpenG2G is capable of answering a wide range of coordination questions by allowing users to implement and compare various control paradigms (including classic, optimization, and learning-based controllers), and quantify how AI model and deployment choices affect datacenter flexibility and coordination outcomes. This versatility is enabled by OpenG2G's modular and extensible architecture: a datacenter backend driven by real measurements of production-grade AI services, a grid backend built on high-fidelity grid simulators, and a generic controller interface that closes the loop between them. We describe the design of OpenG2G and demonstrate its usefulness through realistic grid scenarios and AI workloads.

</details>


### [182] [Bayesian Rain Field Reconstruction using Commercial Microwave Links and Diffusion Model Priors](https://arxiv.org/abs/2605.05520)
*Badr Moufad,Albina Ilina,Hai Victor Habi,Salem Lahlou,Yazid Janati,Hagit Messer,Eric Moulines*

Main category: cs.LG

TL;DR: Diffusion models used as spatial priors in Bayesian inverse problems for reconstructing rainfall fields from Commercial Microwave Links, outperforming traditional methods.


<details>
  <summary>Details</summary>
Motivation: Commercial Microwave Links (CMLs) provide dense spatial coverage for rainfall sensing but produce path-integrated measurements that make accurate ground-level reconstruction difficult. Existing methods oversimplify CMLs as point sensors and neglect the line integration relationship between rainfall and signal attenuation, leading to degraded performance under heterogeneous precipitation conditions.

Method: The approach treats rain field reconstruction as a Bayesian inverse problem using Diffusion Models (DMs) as high-fidelity spatial priors. The method enables training-free posterior sampling through various techniques including Plug-and-Play, Sequential Monte Carlo, and Replica Exchange methods.

Result: Experiments on both synthetic and real-world datasets demonstrate consistent improvements over established CML-based reconstruction baselines. Diffusion models better preserve key rainfall statistics compared to censored Gaussian processes.

Conclusion: Framing rainfall estimation as a Bayesian inverse problem with diffusion model priors provides an effective approach for accurate rain field reconstruction from Commercial Microwave Links, overcoming limitations of existing methods that oversimplify the measurement process.

Abstract: Commercial Microwave Links (CMLs) offer dense spatial coverage for rainfall sensing but produce path-integrated measurements that make accurate ground-level reconstruction challenging. Existing methods typically oversimplify CMLs as point sensors and neglect line integration relating rainfall to signal attenuation, resulting in degraded performance under heterogeneous precipitation. In this work, we view rain field reconstruction as a Bayesian inverse problem with Diffusion Models (DMs) as high-fidelity spatial priors. We show that diffusion models better preserve key rainfall statistics compared to censored Gaussian processes. Framing rainfall estimation as a Bayesian inverse problem with a DM prior enables training-free posterior sampling using a broad family of methods, including Plug-and-Play, Sequential Monte Carlo, and Replica Exchange methods. Experiments on synthetic and real-world datasets demonstrate consistent improvements over established CML-based reconstruction baselines.

</details>


### [183] [MOSAIC: Module Discovery via Sparse Additive Identifiable Causal Learning for Scientific Time Series](https://arxiv.org/abs/2605.05524)
*Shicheng Fan,Nour Elhendawy,Jianle Sun,Ke Fang,Kun Zhang,Yihang Wang,Lu Cheng*

Main category: cs.LG

TL;DR: MOSAIC is a sparse temporal VAE that integrates causal representation learning with support recovery to achieve module-level interpretability by discovering sparse observation-latent associations in scientific time series.


<details>
  <summary>Details</summary>
Motivation: While causal representation learning provides identifiability guarantees (permutation/component-wise reparameterization), it doesn't ensure interpretability - latent semantics are typically assigned post hoc via alignment with ground-truth factors. This is problematic in scientific time series where underlying mechanisms are unknown and interpretable structure discovery is crucial, while scientific observations (physical measurements) are inherently semantic.

Method: MOSAIC is a sparse temporal VAE that combines temporal CRL identifiability with support recovery over observed variables. It identifies latent variables via regime-conditioned temporal variation and recovers for each latent a sparse set of associated observations through an additive decoder, achieving module-level interpretability. The method proves ANOVA main-effect supports are identifiable under general smooth mixing functions and provides finite-sample recovery guarantees for a tractable sparse-additive variant.

Result: Empirically, MOSAIC successfully recovers domain-consistent variable groups across diverse scientific domains: RNA molecular dynamics, solar wind, ENSO climate, Tennessee Eastman process, and a synthetic tokamak benchmark. This enables interpretable discovery of latent mechanisms in scientific time series.

Conclusion: MOSAIC successfully transfers the inherent interpretability of scientific observations to the identifiable latent space, enabling interpretable discovery of latent mechanisms without requiring post hoc alignment with ground-truth factors. The method bridges the gap between identifiability and interpretability in causal representation learning for scientific time series.

Abstract: Causal representation learning (CRL) seeks to recover latent variables with identifiability guarantees, typically up to permutation and component-wise reparameterization under appropriate assumptions. However, identifiability does not imply interpretability: latent semantics are typically assigned post hoc by alignment with known ground-truth factors. This limitation is particularly acute in scientific time series, where underlying mechanisms are unknown and discovering interpretable structure is a primary goal. In contrast, scientific observations (such as residue-pair distances, climate indices, or process sensors) are inherently semantic, as they correspond to named physical quantities. This raises a key question: can the interpretability of observations be transferred to the identifiable latent space? We propose MOSAIC (Module discovery via Sparse Additive Identifiable Causal learning), a sparse temporal VAE that integrates temporal CRL identifiability with support recovery over observed variables. MOSAIC identifies latent variables via regime-conditioned temporal variation, and recovers for each latent a sparse set of associated observations through an additive decoder, yielding module-level interpretability. We show that ANOVA main-effect supports are identifiable under general smooth mixing functions, and provide finite-sample recovery guarantees for a tractable sparse-additive variant. Empirically, MOSAIC recovers domain-consistent variable groups across RNA molecular dynamics, solar wind, ENSO climate, the Tennessee Eastman process, and a synthetic tokamak benchmark, enabling interpretable discovery of latent mechanisms in scientific time series.

</details>


### [184] [Energy Generative Modeling: A Lyapunov-based Energy Matching Perspective](https://arxiv.org/abs/2605.05530)
*Yixuan Wang,Wenqian Xue,Warren E. Dixon*

Main category: cs.LG

TL;DR: The paper unifies training and sampling in static scalar energy generative models via density transport on Wasserstein space framed as a nonlinear control problem, establishing KL divergence as a Lyapunov function and enabling additive composition of trained energies.


<details>
  <summary>Details</summary>
Motivation: Static scalar energy functions represent an emerging paradigm for generative models where a single time-independent potential drives sample generation through its gradient field, eliminating the need for time conditioning. However, conventional approaches treat training and sampling as separate procedures.

Method: The authors unify training and sampling within a single framework: density transport on the Wasserstein space, formulated as a nonlinear control problem where the KL divergence serves as a Lyapunov function. Training and sampling become two instances of the same master dynamics, differing only in initial conditions.

Result: Two main analytic results: 1) Derivation of a finite step stopping criterion for Langevin sampling and proof that no Lyapunov certificate exists for deterministic gradient flow on the same energy landscape. 2) Showed that additive composition of trained scalar energies retains an explicit Gibbs invariant measure and inherits the closed-loop Lyapunov certificate. Experiments on synthetic distributions validate theoretical predictions.

Conclusion: This reformulation bridges static scalar energy generative models with the toolkit of nonlinear control theory, opening doors to applications like barrier functions for constrained generation and contraction metrics for accelerated sampling, while providing a unified theoretical framework for the entire generative modeling pipeline.

Abstract: Generative models based on static scalar energy functions represent an emerging paradigm in which a single time independent potential drives sample generation through its gradient field, eliminating the need for time conditioning entirely. We unify the training and sampling phases of this paradigm, conventionally treated as separate procedures, within a single framework: density transport on the Wasserstein space, cast as a nonlinear control problem in which the Kullback Leibler (KL) divergence serves as a Lyapunov function. Training and sampling are then two instances of this same master dynamics, differing only in initial condition. Within this autonomous framework we develop two analytic results. First, since the Lyapunov certificate is asymptotic, we derive a finite step stopping criterion for Langevin sampling and prove that no Lyapunov certificate exists for the deterministic gradient flow on the same energy landscape. Second, the reformulation brings the toolkit of nonlinear control theory to bear on static scalar energy generative modeling, that is, we show that additive composition of trained scalar energies retains an explicit Gibbs invariant measure and inherits the closed-loop Lyapunov certificate. Beyond these immediate results, this reformulation bridges static scalar energy generative models with the full toolkit of nonlinear control theory, opening the door to barrier functions for constrained generation and contraction metrics for accelerated sampling. Experiments on synthetic distributions validate the theoretical predictions.

</details>


### [185] [Adversarial Graph Neural Network Benchmarks: Towards Practical and Fair Evaluation](https://arxiv.org/abs/2605.05534)
*Tran Gia Bao Ngo,Zulfikar Alom,Federico Errica,Murat Kantarcioglu,Cuneyt Gurcan Akcora*

Main category: cs.LG

TL;DR: A comprehensive benchmark study revealing that unfair evaluation protocols in adversarial GNN research lead to ambiguous conclusions, demonstrating substantial performance differences when using standardized evaluation across 453,000+ experiments.


<details>
  <summary>Details</summary>
Motivation: The literature on adversarial attacks and defenses for Graph Neural Networks suffers from inconsistent experimental settings across different works, leading to ambiguous and potentially contradictory scientific conclusions about GNN robustness in real-world applications.

Method: Performed a comprehensive re-evaluation of seven widely used attacks and eight recent defenses under both poisoning and evasion scenarios, across six popular graph datasets, using over 453,000 experiments conducted within a unified framework with standardized protocols.

Result: Revealed substantial differences in adversarial attack performance when evaluated under fair procedures, showing that previously overlooked factors (target node selection, training process of attacked model) have profound impact on attack effectiveness, sometimes completely distorting performance insights.

Conclusion: Underscores the urgent need for standardized evaluations in adversarial graph machine learning to ensure fair comparisons and reliable scientific conclusions about GNN robustness.

Abstract: Adversarial learning and the robustness of Graph Neural Networks (GNNs) are topics of widespread interest in the machine learning community, as documented by the number of adversarial attacks and defenses designed for these purposes. While a rigorous evaluation of these adversarial methods is necessary to understand the robustness of GNNs in real-world applications, we posit that many works in the literature do not share the same experimental settings, leading to ambiguous and potentially contradictory scientific conclusions. In this benchmark, we demonstrate the importance of adopting fair, robust, and standardized evaluation protocols in adversarial GNN research. We perform a comprehensive re-evaluation of seven widely used attacks and eight recent defenses under both poisoning and evasion scenarios, across six popular graph datasets. Our study spans over 453,000 experiments conducted within a unified framework. We observe substantial differences in adversarial attack performance when evaluated under a fair and robust procedure. Our findings reveal that previously overlooked factors, such as target node selection and the training process of the attacked model, have a profound impact on attack effectiveness, to the extent of completely distorting performance insights. These results underscore the urgent need for standardized evaluations in adversarial graph machine learning.

</details>


### [186] [Towards Scalable One-Step Generative Modeling for Autoregressive Dynamical System Forecasting](https://arxiv.org/abs/2605.05540)
*Tianyue Yang,Xiao Xue*

Main category: cs.LG

TL;DR: MeLISA is a latent-free autoregressive generative surrogate model that uses pixel-space MeanFlow for fast, stable long-horizon forecasting of high-dimensional physical dynamics, achieving both rollout efficiency and statistical accuracy.


<details>
  <summary>Details</summary>
Motivation: Current neural operators drift in turbulent regimes, while diffusion and latent generative surrogates require multi-step denoising or auxiliary compression models. There's a need for models that balance efficient rollout with preservation of long-term statistical structure in high-dimensional physical dynamics.

Method: MeLISA uses pixel-space MeanFlow with a blockwise stochastic transition kernel that generates forecasts with single model evaluation (no latent encoders or iterative diffusion). It combines Window-Consistency MeanFlow objective for conditional spatiotemporal generation and Time Increment Consistency loss to constrain multi-lag finite increments and preserve temporal-correlation structure.

Result: MeLISA outperforms neural-operator baselines on both short-term forecasting accuracy and long-horizon statistical metrics (energy spectra, turbulent kinetic energy, mixing-rate-related dynamics) while achieving comparable or faster inference speeds. Works with compact UNet (3.7-5.7M parameters) and scalable DiT backbones up to 150M parameters.

Conclusion: MeLISA provides an effective solution for fast surrogate modeling of high-dimensional physical dynamics, balancing rollout efficiency with long-horizon statistical accuracy, offering both compact and scalable implementation options.

Abstract: Fast surrogate modeling for high-dimensional physical dynamics requires more than low short-term error: useful models must roll out efficiently while preserving the statistical structure of long trajectories. Neural operators provide inexpensive autoregressive forecasts but can drift in turbulent regimes, whereas rolling diffusion and latent generative surrogates can represent stochastic transitions at the cost of multi-step denoising, noise-schedule design, or auxiliary compression models. We propose MeanFlow Long-term Invariant Spatiotemporal Consistency Autoregressive Models (MeLISA), a latent-free autoregressive generative surrogate built on pixel-space MeanFlow. MeLISA defines a blockwise stochastic transition kernel that generates each forecast block with a single model evaluation, avoiding latent encoders and iterative diffusion solvers at inference time. To stabilize long-horizon rollouts, MeLISA combines a Window-Consistency MeanFlow objective that learns conditional spatiotemporal generation from partially observed temporal windows with a Time Increment Consistency loss that constrains multi-lag finite increments and targets temporal-correlation structure. We evaluate MeLISA with compact UNet and scalable DiT backbones on two high-resolution benchmarks, extended 2D Kolmogorov flow at $256 \times 256$ and turbulent channel-flow slice at $192 \times 192$. MeLISA outperforms neural-operator baselines on short-term forecasting accuracy and long-horizon statistical metrics, including energy spectra, turbulent kinetic energy, and mixing-rate-related dynamics, while achieving inference speeds comparable to, and in some cases faster than, neural operators. Compact 3.7-5.7M-parameter variants already deliver strong parameter efficiency, and DiT variants provide a scalable path up to 150M parameters. Overall, MeLISA benefits both rollout efficiency and long-horizon statistical accuracy.

</details>


### [187] [Adaptive Q-Chunking for Offline-to-Online Reinforcement Learning](https://arxiv.org/abs/2605.05544)
*Nandiraju Gireesh,Yuanliang Ju,He Wang*

Main category: cs.LG

TL;DR: AQC introduces adaptive action chunking in offline-to-online RL, allowing variable chunk sizes per state instead of fixed sizes, with a novel advantage-based selection method that prevents bias toward short chunks.


<details>
  <summary>Details</summary>
Motivation: Fixed chunk sizes in existing offline-to-online RL methods are suboptimal - short chunks are needed for reactive control near contact events while long chunks are better for free-space motion credit assignment.

Method: Proposes Adaptive Q-Chunking (AQC) which trains critics for multiple chunk sizes and selects the best one per state using advantage comparison relative to per-horizon baselines, normalized by discount factor to prevent bias toward short chunks.

Result: AQC achieves state-of-the-art offline and online success rates on OGBench and Robomimic, and significantly boosts performance of large-scale VLA models on RoboCasa-GR1 tasks with theoretical guarantees on noise immunity and value dominance.

Conclusion: Adaptive chunking with proper advantage-based selection resolves the discount-scale mismatch problem, enabling state-dependent optimal chunk size selection for superior performance in offline-to-online RL and VLA models.

Abstract: Offline-to-online reinforcement learning with action chunking eliminates multi-step off-policy bias and enables temporally coherent exploration, but all existing methods use a fixed chunk size across every state. This is suboptimal: near contact events the agent needs short chunks for reactive control, while during free-space motion long chunks provide better credit assignment. The natural solution is to train critics for several chunk sizes and select the best one at each state, but naive comparison of learned critic values systematically collapses to the shortest chunk due to discount-scale mismatch, and degrades to noise in low-value states. We propose Adaptive Q-Chunking (AQC), which resolves both failures by comparing the advantage of each chunk size relative to a per-horizon baseline, normalized by the discount factor. This criterion converts biased wrong answers into unbiased near-random choices when no genuine signal exists, and becomes discriminative when a particular scale enables better planning. We prove theoretical bounds on the advantage selector's noise immunity and on the value dominance of adaptive chunking over any fixed chunk size. We demonstrate that AQC achieves state-of-the-art offline and online success rates on OGBench and Robomimic, and can be applied to enhance the performance of large-scale VLA models that predict action sequences, significantly boosting performance on RoboCasa-GR1 tasks.

</details>


### [188] [FedeKD: Energy-Based Gating for Robust Federated Knowledge Distillation under Heterogeneous Settings](https://arxiv.org/abs/2605.05553)
*Quang-Huy Nguyen,Jiaqi Wang,Wei-shinn Ku*

Main category: cs.LG

TL;DR: FedeKD is a reliability-aware federated knowledge distillation framework that addresses negative transfer in heterogeneous FL environments through sample-wise trust estimation without requiring public datasets.


<details>
  <summary>Details</summary>
Motivation: Federated learning faces challenges with heterogeneous environments (varying data distributions and asymmetric models) causing negative transfer. Existing federated knowledge distillation methods either rely on public datasets or assume uniformly reliable knowledge transfer, limiting practical robustness.

Method: Each client maintains: 1) a high-capacity private model for local learning, and 2) a lightweight shared proxy model for knowledge exchange. Proxy models aggregate on server to form global proxy guiding private model updates. Core innovation: energy-based gating mechanism converts private-proxy disagreement into sample-wise trust weights for backward distillation.

Result: Extensive experiments on six real-world datasets demonstrate that FedeKD significantly reduces negative transfer under heterogeneous settings while maintaining strong predictive performance.

Conclusion: FedeKD provides an effective solution for reliable knowledge transfer in heterogeneous federated learning by making sample-wise trust estimation explicit, eliminating the need for public data and improving robustness to negative transfer.

Abstract: Federated learning (FL) operates in heterogeneous environments, where variations in data distributions and asymmetric model design often result in negative transfer. While federated knowledge distillation (FKD) avoids direct model parameter sharing, existing methods typically rely on public datasets or assume that transferred knowledge is uniformly reliable, which limits their robustness in practice. This paper presents FedeKD, a reliability-aware FKD framework that makes sample-wise trust estimation an explicit component of knowledge transfer, without relying on additional public data. Each client maintains a high-capacity private model for local learning and a lightweight shared proxy model for cross-client knowledge exchange. During training, proxy models are aggregated on the server to form a global proxy, which is then used to guide updates of the private models. At the core of FedeKD is an energy-based gating mechanism that converts task-specific private-proxy disagreement into sample-wise trust weights for backward distillation. This mechanism enables sample-wise weighting of knowledge transfer, where the proxy model contributes more to reliable samples while down-weighting unreliable ones. Extensive experiments on six real-world datasets demonstrate that FedeKD significantly reduces negative transfer under heterogeneous settings while maintaining strong predictive performance.

</details>


### [189] [Accelerating LMO-Based Optimization via Implicit Gradient Transport](https://arxiv.org/abs/2605.05577)
*Won-Jun Jang,Si-Hyeon Lee*

Main category: cs.LG

TL;DR: LMO-IGT is a new stochastic LMO-based optimization method that uses implicit gradient transport to accelerate convergence without the computational overhead of traditional variance reduction techniques.


<details>
  <summary>Details</summary>
Motivation: Current LMO-based optimizers (like Lion and Muon) have strong empirical performance but face limitations: variance reduction methods incur substantial computational overhead due to extra gradient evaluations, and theoretical understanding is fragmented across unconstrained and constrained formulations.

Method: Proposes LMO-IGT (LMO with Implicit Gradient Transport), which evaluates stochastic gradients at transported points to accelerate convergence while maintaining single-gradient-per-iteration structure. Also introduces a unified framework for stochastic LMO-based optimization with a new stationarity measure called regularized support function (RSF).

Result: Establishes theoretical complexity bounds: standard stochastic LMO achieves O(ε⁻⁴), variance-reduced LMO achieves O(ε⁻³) but with extra gradient evaluations, while LMO-IGT achieves O(ε⁻³·⁵) using only single stochastic gradient per iteration. Empirically, LMO-IGT improves over stochastic LMO counterparts with negligible overhead, with Muon-IGT achieving strongest overall performance.

Conclusion: Implicit gradient transport provides an effective and practical acceleration mechanism for modern LMO-based optimization, bridging theoretical gaps while maintaining computational efficiency.

Abstract: Recent optimizers such as Lion and Muon have demonstrated strong empirical performance by normalizing gradient momentum via linear minimization oracles (LMOs). While variance reduction has been explored to accelerate LMO-based methods, it typically incurs substantial computational overhead due to additional gradient evaluations. At the same time, the theoretical understanding of LMO-based methods remains fragmented across unconstrained and constrained formulations. Motivated by these limitations, we propose \emph{LMO-IGT}, a new class of stochastic LMO-based methods leveraging implicit gradient transport (IGT). We further introduce a unified framework for stochastic LMO-based optimization together with a new stationarity measure, the \emph{regularized support function} (RSF), which bridges gradient-norm and Frank--Wolfe-gap notions within a common framework. By evaluating stochastic gradients at transported points, LMO-IGT accelerates convergence while retaining the single-gradient-per-iteration structure of standard stochastic LMO. Our analysis establishes that stochastic LMO achieves an iteration complexity of $\mathcal{O}(\varepsilon^{-4})$, variance-reduced LMO achieves $\mathcal{O}(\varepsilon^{-3})$ at the cost of additional gradient evaluations, and LMO-IGT achieves $\mathcal{O}(\varepsilon^{-3.5})$ using only a single stochastic gradient per iteration. Empirically, LMO-IGT consistently improves over stochastic LMO counterparts with negligible overhead. Among its instantiations, Muon-IGT achieves the strongest overall performance across evaluated settings, demonstrating that IGT provides an effective and practical acceleration mechanism for modern LMO-based optimization.

</details>


### [190] [AeroJEPA: Learning Semantic Latent Representations for Scalable 3D Aerodynamic Field Modeling](https://arxiv.org/abs/2605.05586)
*Francisco Giral,Abhijeet Vishwasrao,Andrea Arroyo Ramo,Mahmoud Golestanian,Federica Tonti,Adrian Lozano-Duran,Steven L. Brunton,Sergio Hoyas,Hector Gomez,Soledad Le Clainche,Ricardo Vinuesa*

Main category: cs.LG

TL;DR: AeroJEPA is a Joint-Embedding Predictive Architecture for aerodynamic field modeling that learns meaningful latent representations while efficiently scaling to high-resolution 3D aerodynamics.


<details>
  <summary>Details</summary>
Motivation: Current aerodynamic surrogate models have two key limitations: they don't scale well to large 3D aerodynamic fields, and their latent representations aren't useful for analysis and design.

Method: AeroJEPA predicts target latent representations of flow fields from context latent representations of geometry and operating conditions, optionally using a continuous implicit decoder for field reconstruction, which decouples latent prediction from field resolution.

Result: AeroJEPA performs competitively as a continuous surrogate, scales to high-resolution outputs, learns meaningful latent representations that encode geometric and aerodynamic information without direct supervision, and supports interpolation, probing, concept arithmetic, and design optimization.

Conclusion: Predictive latent learning is a promising approach for creating scalable aerodynamic surrogate models with design-meaningful latent representations.

Abstract: Aerodynamic surrogate models are increasingly used to replace repeated high-fidelity CFD evaluations in many-query design settings, but current approaches still face two important limitations: they often scale poorly to the very large fields arising in realistic 3D aerodynamics, and they rarely produce latent representations that are directly useful for analysis and design. We introduce AeroJEPA, a Joint-Embedding Predictive Architecture for aerodynamic field modeling that addresses both issues. Rather than predicting the full flow field directly from geometry, AeroJEPA predicts a target latent representation of the flow from a context latent representation of the geometry and operating conditions, and optionally reconstructs the field through a continuous implicit decoder. This formulation decouples latent prediction from field resolution while encouraging the latent space to organize semantically. We evaluate AeroJEPA on two complementary datasets: HiLiftAeroML, which stresses the method in a high-fidelity regime with extremely large boundary-layer fields, and SuperWing, which tests large-scale generalization and latent-space optimization over a broad family of transonic wings. Across these benchmarks, AeroJEPA is competitive as a continuous surrogate for aerodynamic fields, scales naturally to high-resolution outputs, and learns context and predicted latents that encode geometry and aerodynamic quantities not used directly as supervision. We further show that the resulting latent space supports controlled interpolation, linear probing, concept-vector arithmetic, and a constrained design latent-optimization experiment. These results suggest that predictive latent learning is a promising direction for scalable and design-meaningful aerodynamic surrogate modeling.

</details>


### [191] [When Can Voting Help, Hurt, or Change Course? Exact Structure of Binary Test-Time Aggregation](https://arxiv.org/abs/2605.05592)
*Yi Liu*

Main category: cs.LG

TL;DR: Voting curves in ensemble predictors can have complex, non-monotone behavior due to latent per-example correctness distributions, with complete characterization through signed voting signatures and Hausdorff moments.


<details>
  <summary>Details</summary>
Motivation: Classical voting theory assumes monotone behavior where more votes always help above majority threshold and hurt below it, but this is incomplete. Understanding the true behavior of voting interventions on stochastic predictors requires examining the latent distributions governing per-example correctness.

Method: Using de Finetti representation for exchangeable repeated correctness, analyzing voting through latent distributions of per-example correctness probabilities. Characterizing voting curves via signed voting signatures and showing equivalence with odd-budget voting curves through signed Hausdorff moments.

Result: Voting curves can exhibit non-monotone behavior and infinitely many trend changes. The full voting curve uniquely determines a signed voting signature (excess latent mass above vs below majority threshold at each variance scale), and these are equivalent via Hausdorff moments. Reveals shape phenomena, nonidentifiability, realizability, and separates estimation regimes.

Conclusion: Voting behavior is more complex than classical theory suggests, governed by latent per-example correctness distributions. Complete characterization is possible through signed voting signatures and Hausdorff moments, explaining observed phenomena and clarifying different estimation approaches for ensemble predictors.

Abstract: Majority voting is one of the few black-box interventions that can improve a fixed stochastic predictor: repeated access can be cheaper than changing a high-capability model. Classical fixed-competence theory makes this intervention look monotone -- more votes help above the majority threshold and hurt below it. We show that this picture is fundamentally incomplete. Under the de Finetti representation for exchangeable repeated correctness, voting is governed by a latent distribution of per-example correctness probabilities. Even simple latent mixtures can generate sharply different voting curves, including nonmonotone behavior and, in an explicit construction, infinitely many trend changes. The full latent law determines the curve, but the curve does not determine the law. The exact object recovered by voting is a signed voting signature: at each binomial variance scale, it records excess latent mass above rather than below the majority threshold. Our main theorem proves that the complete odd-budget curve and this signature are equivalent: the curve increments are signed Hausdorff moments, and the full curve recovers the signature uniquely. This viewpoint explains shape phenomena, branch-symmetric nonidentifiability, realizability, variation, and endpoint rates. It also separates estimation regimes: direct per-example success-probability information targets the full signature, whereas fixed-depth grouped labels reveal only a finite prefix.

</details>


### [192] [Optimal Contextual Pricing under Agnostic Non-Lipschitz Demand](https://arxiv.org/abs/2605.05609)
*Jianyu Xu,Yu-Xiang Wang*

Main category: cs.LG

TL;DR: Conservative-Markdown Redirect-UCB Pricing algorithm achieves $\tilde O(T^{2/3})$ regret for contextual dynamic pricing with non-Lipschitz noise, matching known lower bounds and improving previous upper bounds.


<details>
  <summary>Details</summary>
Motivation: Addressing the challenge of contextual dynamic pricing with linear valuations and bounded-support agnostic noise where induced demand curves may be non-Lipschitz with arbitrary jumps and atoms. These discontinuities break interpolation arguments used by smooth-demand algorithms, creating a significant performance gap.

Method: Proposes Conservative-Markdown Redirect-UCB Pricing algorithm that combines randomized parameter estimation, conservative residual-grid probing, and confidence-based one-step redirection in polynomial time.

Result: Achieves $\tilde O(T^{2/3})$ optimal regret, matching the known lower bounds of Kleinberg and Leighton (2003) up to logarithmic factors, improving over the previous upper bound of $\tilde O(T^{3/4})$ by Xu and Wang (2022).

Conclusion: The algorithm closes the long-existing open regret gap in linear-valuation contextual pricing under agnostic non-Lipschitz noise distribution, achieving optimal performance under stochastic well-conditioned contexts.

Abstract: We study contextual dynamic pricing with linear valuations and bounded-support agnostic noise, whose induced demand curve may be non-Lipschitz with arbitrary jumps and atoms. Such discontinuities break the cross-context interpolation arguments used by smooth-demand pricing algorithms, while the best previous method achieved only $\tilde O(T^{3/4})$ regret. We propose Conservative-Markdown Redirect-UCB Pricing, a polynomial-time algorithm that combines randomized parameter estimation, conservative residual-grid probing, and confidence-based one-step redirection. Our algorithm achieves $\tilde O(T^{2/3})$ optimal regret, matching the known lower bounds of Kleinberg and Leighton (2003) up to logarithmic factors and improving over the previous upper bound of Xu and Wang (2022). Under stochastic well-conditioned contexts, this closes the long-existing open regret gap in linear-valuation contextual pricing under agnostic non-Lipschitz noise distribution.

</details>


### [193] [LLMSpace: Carbon Footprint Modeling for Large Language Model Inference on LEO Satellites](https://arxiv.org/abs/2605.05615)
*Lei Jiang,Adrian Ildefonso,Daniel Loveless,Fan Chen*

Main category: cs.LG

TL;DR: LLMSpace is the first carbon modeling framework that evaluates the lifecycle carbon footprint of running large language models (LLMs) on solar-powered LEO satellites, considering both operational and embodied emissions from launch, hardware manufacturing, and radiation-hardened components.


<details>
  <summary>Details</summary>
Motivation: LLMs are creating an emerging energy and carbon crisis due to their growing energy demands for inference. While solar-powered LEO satellites have been proposed as a solution to reduce terrestrial electricity consumption, their complete lifecycle carbon footprint (including launch emissions, satellite manufacturing, and radiation-hardened hardware) remains poorly understood.

Method: The authors developed LLMSpace, a carbon modeling framework that jointly models operational and embodied carbon for LLM inference on AI-enabled LEO satellites. The framework accounts for peripheral subsystems, radiation-hardened accelerators and memories, and LLM-specific workload characteristics including prefill-decode behavior and token generation patterns.

Result: Using realistic satellite and GPU configurations, LLMSpace reveals key trade-offs among carbon footprint, inference latency, hardware design, and operational lifetime. The framework provides insights into sustainable space-based LLM inference and helps identify optimal configurations balancing environmental impact and performance.

Conclusion: LLMSpace represents the first comprehensive approach to understanding the full carbon implications of deploying LLMs on LEO satellites. The framework enables analysis of sustainability trade-offs and informs the design of more environmentally responsible space-based AI infrastructure, addressing both energy demands and lifecycle carbon emissions of large-scale LLM inference.

Abstract: Large language models (LLMs) impose rapidly growing energy demands, creating an emerging energy and carbon crisis driven by large-scale inference. Solar-powered, AI-enabled low Earth orbit (LEO) satellites have been proposed to mitigate terrestrial electricity consumption, but their lifecycle carbon footprint remains poorly understood due to launch emissions, satellite manufacturing, and radiation-hardened hardware requirements. This paper presents \textit{LLMSpace}, the first carbon modeling framework for LLM inference on AI-enabled LEO satellites. LLMSpace jointly models operational and embodied carbon, peripheral subsystems, radiation-hardened accelerators and memories, and LLM-specific workload characteristics such as prefill-decode behavior and token generation. Using realistic satellite and GPU configurations, LLMSpace reveals key trade-offs among carbon footprint, inference latency, hardware design, and operational lifetime for sustainable space-based LLM inference. Source code: https://github.com/UnchartedRLab/LLMSpace.

</details>


### [194] [Region-adaptable retrieval of coastal biogeochemical parameters from near-surface hyperspectral remote sensing reflectance using physics-aware meta-learning](https://arxiv.org/abs/2605.05623)
*Yiqing Guo,Nagur R. C. Cherukuru,Eric A. Lehmann,S. L. Kesav Unnithan,Tim J. Malthus,Gemma Kerrisk,Xiubin Qi,Faisal Islam,Tisham Dhar,Mark J. Doubell*

Main category: cs.LG

TL;DR: A physics-aware meta-learning framework for retrieving coastal biogeochemical parameters from hyperspectral in situ data, using synthetic training data from bio-optical models and region-specific fine-tuning.


<details>
  <summary>Details</summary>
Motivation: Generalizing retrieval algorithms for aquatic biogeochemical parameters across different water bodies is challenging due to regional variations in environmental conditions, biogeochemistry, and bio-optical properties that affect relationships between remote sensing reflectance and BGC parameters.

Method: Two-stage physics-aware meta-learning approach: 1) Generate large synthetic dataset using bio-optical forward model based on spectral library representing Australian coastal waters, then pretrain region-agnostic base model with meta-learning; 2) Fine-tune pretrained model for specific regions with local in situ samples.

Result: Experimental results from five geographically distinct Australian coastal sites show: 1) Clear regional distinctions in BGC parameters and hyperspectral signatures; 2) Synthetic dataset aligned well with real-world samples in distributions and correlations; 3) Proposed approach outperformed five benchmark models; 4) Time series predictions showed good agreement with measured BGC parameters in magnitude and dynamics.

Conclusion: The physics-aware meta-learning framework effectively addresses regional generalization challenges in coastal BGC parameter retrieval by combining synthetic training for fundamental physical relationships with region-specific fine-tuning.

Abstract: Hyperspectral in situ sensing has shown promise in retrieving aquatic biogeochemical (BGC) parameters, such as total suspended solids, dissolved organic carbon, and total chlorophyll-a, for cost-effective monitoring of coastal water quality. However, generalising such retrieval algorithms across water bodies remains challenging, as the relationship between remote sensing reflectance (Rrs) and BGC parameters can vary considerably from one region to another due to regional distinctions in environmental conditions and biogeochemistry that lead to different BGC ranges and bio-optical properties. In this study, we propose a two-stage physics-aware meta-learning framework for retrieving coastal BGC parameters from near-surface Rrs observations. In the first stage, a bio-optical forward model is used to generate a large synthetic dataset based on an in situ bio-optical spectral library with broad representativeness of Australian coastal waters. This dataset is then used to pretrain a region-agnostic base model with meta-learning, allowing the model to learn fundamental physical relationships. In the second stage, the pretrained base model is fine-tuned for specific regions with local samples. We collected in situ hyperspectral Rrs and BGC measurements from five geographically distinct sites in Australian coastal waters. Our experimental results suggest: (1) the BGC parameters and their corresponding hyperspectral Rrs signatures exhibited clear regional distinctions among the experimental sites; (2) the synthetic dataset was physically plausible and closely aligned with real-world samples in both parameter distributions and inter-parameter correlations; (3) the proposed approach outperformed five benchmark models in BGC retrieval; and (4) time series of in situ measured and model-predicted BGC parameters showed good agreement in both magnitude and temporal dynamics.

</details>


### [195] [Scaling Pretrained Representations Enables Label-Free Out-of-Distribution Detection Without Fine-Tuning](https://arxiv.org/abs/2605.05638)
*Brett Barkley,Preston Culbertson,David Fridovich-Keil*

Main category: cs.LG

TL;DR: Label-free OOD detection works well with frozen pretrained models: performance improves with better representations and detector choice matters less at scale, enabling efficient deployment.


<details>
  <summary>Details</summary>
Motivation: Deep learning models often fail to signal when inputs are outside their training distribution, leading to unreliable predictions. Prior work suggests effective OOD detection requires class-conditional modeling or supervised fine-tuning, but this may not be necessary with modern pretrained models.

Method: The authors examine frozen pretrained representations across 59 backbone-task pairings in vision and language. They compare two label-free detectors: a global Mahalanobis estimator on latent representations, and ReSCOPED, a lightweight diffusion-based typicality estimator operating at local level. They analyze how representation scaling affects detector performance.

Result: Both local and global detectors' performance improves with better representation quality. Performance gaps between detectors disappear as representations scale across both language and vision tasks. Frozen pretrained backbones encode sufficient geometric structure for accurate label-free OOD detection.

Conclusion: Label-free OOD detection depends strongly on the geometry exposed by frozen pretrained backbones. As backbone scale increases, detector choice becomes less important, enabling efficient deployment directly on frozen models without specialized training or fine-tuning.

Abstract: Models trained with deep learning often fail to signal when inputs fall outside their training data manifold, leading to unreliable predictions under distribution shift. Prior work suggests that effective out-of-distribution (OOD) detection often requires class-conditional modeling or specialized models obtained through supervised fine-tuning. We revisit this assumption in modern pretrained models and show that their frozen representations already encode sufficient geometric structure for accurate label-free OOD detection. Across 59 backbone-task pairings spanning vision and language, we compare two complementary label-free detectors: a global Mahalanobis estimator fit on unlabeled latent representations, and ReSCOPED, a lightweight, diffusion-based typicality estimator operating on the same features at a local level. Despite their different detection mechanisms, representation scaling reveals a consistent regime-dependent pattern: both local and global detectors' absolute performance improves with better representation quality, and performance gaps between the two detectors disappear across both language and vision tasks as representations scale. These results suggest that label-free OOD detection depends strongly on the geometry exposed by frozen pretrained backbones, reducing the importance of detector choice as backbone scale increases and enabling efficient deployment directly on frozen models.

</details>


### [196] [Information-Preserving Domain Transfer with Unlabeled Data in Misspecified Simulation-Based Inference](https://arxiv.org/abs/2605.05652)
*Joon Jang,Eunho Jeong,Kyu Sung Choi,Hyeonjin Kim*

Main category: cs.LG

TL;DR: SPIN is a simulation-based inference framework that improves parameter inference under model misspecification by preserving parameter-relevant information during domain transfer between simulated and real-world data.


<details>
  <summary>Details</summary>
Motivation: Current SBI methods can degrade under model misspecification where real-world observations differ from simulator training data. Existing domain alignment methods don't preserve parameter-relevant information needed for accurate posterior inference.

Method: SPIN translates labeled simulator observations to real-world domain and back, using original simulator labels to encourage domain transfer that preserves mutual information about parameters. Uses learned transport maps to convert real-world observations to simulator domain for posterior inference.

Result: SPIN improves real-world posterior inference across synthetic and physical benchmarks, with greater improvements as model misspecification increases. Works without requiring real-world parameter labels or paired observations.

Conclusion: SPIN addresses a key limitation in SBI under model misspecification by preserving parameter-relevant information during domain transfer, enabling more reliable posterior inference using only unlabeled real-world data.

Abstract: Simulation-based inference (SBI) provides amortized Bayesian parameter inference from simulator-generated data without requiring explicit likelihood evaluation. Its reliability can degrade under model misspecification, where real-world observations are not well represented by the simulator used for training. Existing methods using unlabeled real-world data often align simulated and real-world data distributions, but marginal alignment alone does not directly preserve parameter-relevant information needed for posterior inference. We propose SPIN, an SBI framework with parameter-relevant information-preserving domain transfer using unlabeled, unpaired real-world observations. During training, SPIN translates labeled simulator observations toward the real-world domain and back to the simulator domain, using the original simulator labels to encourage domain transfer that preserves parameter-relevant mutual information. At test time, the learned real-to-simulator transport maps real-world observations into the simulator domain for posterior inference, without requiring real-world parameter labels or paired real--simulator observations. Across controlled synthetic and physical real-world benchmarks, SPIN improves real-world posterior inference, with the improvement becoming clearer as misspecification increases.

</details>


### [197] [Structural Correspondence and Universal Approximation in Diagonal plus Low-Rank Neural Networks](https://arxiv.org/abs/2605.05659)
*Ying Chen,Aoxi Li,Jihun Kim,Javad Lavaei*

Main category: cs.LG

TL;DR: Low-rank neural networks struggle with universal approximation without dense priors; adding minimal diagonal sparse components to low-rank structures restores full expressivity with better parameter efficiency through depth rather than width.


<details>
  <summary>Details</summary>
Motivation: While parameter-efficient low-rank structures (like LoRA) are widely used due to computational cost savings, their theoretical expressive power lacks guarantees without restrictive priors like pretrained base matrices or specific activation functions. The paper aims to investigate the fundamental limits of purely low-rank neural networks without such priors.

Method: Introduces a Structural Correspondence framework that augments low-rank layers with minimal sparse diagonal components (DLoR - Diagonal plus Low-Rank). Shows exact reconstruction of full-rank transformations using DLoR components through either additive decomposition (increasing width) or multiplicative decomposition (increasing depth). Uses asymptotic Taylor remainder analysis to prove universal approximation.

Result: Proves that purely rank-1 layers can exactly interpolate scalar data but fail at function approximation. Demonstrates that adding minimal diagonal components to low-rank structures (DLoR) restores Universal Approximation Theorem for general activation functions. Shows multiplicative depth decomposition provides superior parameter-to-expressivity scaling compared to additive width decomposition.

Conclusion: Dense matrices and specific activation functions are not topological prerequisites for universal expressivity. Parameter-efficient low-rank architectures can achieve full expressive power when minimally augmented with sparse diagonal components, with depth-based decomposition offering better parameter efficiency than width-based approaches.

Abstract: The massive computational costs of scaling modern deep learning architectures have driven the widespread use of parameter-efficient low-rank structures, such as LoRA and low-rank factorization. However, theoretical guarantees for their expressive power are less explored, often relying on restrictive priors like a pretrained base matrix, ReLU activations or non-verifiable singularity conditions. We first investigate the limits of neural networks constrained strictly to low-rank manifolds without pretrained dense priors. We demonstrate a theoretical paradox: while purely rank-1 layers can exactly interpolate arbitrary scalar datasets, they collapse for function approximations. To overcome this bottleneck without surrendering parameter efficiency, we introduce a unified \textit{Structural Correspondence} framework. We prove that augmenting low-rank layers with only a minimal sparse diagonal component, say a Diagonal plus Low-Rank (DLoR) structure, is sufficient to reach Universal Approximation. We show that any full-rank transformation can be exactly reconstructed using these DLoR components by trading off network width (additive decomposition) or depth (multiplicative decomposition). By tracking asymptotic Taylor remainders, we prove that DLoR neural networks fully restore the Universal Approximation Theorem for general activation functions. Finally, we establish that multiplicative depth provides superior parameter-to-expressivity scaling compared to additive width. Our results show that dense matrices and specific activation functions are not topological prerequisites for universal expressivity.

</details>


### [198] [Distributionally Robust Multi-Objective Optimization](https://arxiv.org/abs/2605.05660)
*Yufeng Yang,Fangning Zhuo,Ziyi Chen,Heng Huang,Yi Zhou*

Main category: cs.LG

TL;DR: This paper introduces Distributionally Robust Multi-Objective Optimization (DR-MOO), which minimizes multiple objectives under worst-case distributions, and develops efficient multi-gradient descent algorithms with improved sample complexity guarantees.


<details>
  <summary>Details</summary>
Motivation: Existing multi-objective optimization formulations do not account for distributional shifts in data, limiting their robustness in real-world applications where data distributions can change.

Method: The authors propose DR-MOO with Pareto-type solution concepts and develop multi-gradient descent algorithms: 1) a double-loop MGDA using Lagrangian dual reformulation with inner loop for dual variable estimation, and 2) a more efficient single-loop double-clip MGDA that incorporates gradient clipping to handle generalized-smooth and biased gradient estimates.

Result: The double-loop MGDA achieves sample complexity O(ε^{-12}) for reaching ε-Pareto-stationary points, while the single-loop double-clip MGDA substantially improves this to O(ε^{-4}). The theory applies to nonconvex settings without requiring bounded objectives or gradients, and experiments show competitiveness with state-of-the-art MGDA baselines.

Conclusion: The proposed DR-MOO framework and algorithms effectively address distributional robustness in multi-objective optimization, with significant improvements in sample efficiency and theoretical guarantees that maintain practical competitiveness with existing methods.

Abstract: Multi-objective optimization (MOO) has received growing attention in applications that require learning under multiple criteria. However, the existing MOO formulations do not explicitly account for distributional shifts in the data. We introduce distributionally robust multi-objective optimization (DR-MOO), which minimizes multiple objectives under their respective worst-case distributions. We propose Pareto-type solution concepts for DR-MOO and develop multi-gradient descent algorithms (MGDA) with provable guarantees. Leveraging a Lagrangian dual reformulation, we first design a double-loop MGDA that uses an inner loop to estimate dual variables and achieves a total sample complexity $\mathcal{O}(ε^{-12})$ for reaching an $ε$-Pareto-stationary point. To further improve efficiency, we incorporate gradient clipping to handle generalized-smooth and biased gradient estimates, removing the need for double sampling. This yields a single-loop double-clip MGDA with substantially improved sample complexity $\mathcal{O}(ε^{-4})$. Our theory applies to the nonconvex setting and does not require bounded objectives or gradients. Experiments demonstrate that our methods are competitive with state-of-the-art MGDA baselines.

</details>


### [199] [Temporal Functional Circuits: From Spline Plots to Faithful Explanations in KAN Forecasting](https://arxiv.org/abs/2605.05685)
*Naveen Mysore*

Main category: cs.LG

TL;DR: KANs enable temporal explanations via Temporal Functional Circuits framework that transforms edge functions into interpretable, temporally grounded explanations through attribution mapping, activation ranking, and faithfulness validation.


<details>
  <summary>Details</summary>
Motivation: Unlike MLPs, KANs have explicit learnable edge functions but lack temporal grounding. The paper aims to transform KAN edge functions from latent visualizations into faithful, temporally grounded explanations for time-series forecasting.

Method: Uses gated residual KAN that decomposes forecasts into linear base and sparsely activated KAN correction. Framework maps edges to input lags via output-aware attribution, ranks edges by learned activation range, and validates faithfulness through edge-level interventions (zeroing and spline removal).

Result: Removing spline component degrades forecasts, showing spline shape has predictive value beyond base activation. On synthetic regimes, learned gate opens wider as complexity grows. Gated KAN achieves 59% lower MSE than linear models on regime-switching signals. Competitive with linear, attention, and MLP alternatives across eight benchmarks while offering interpretable edge functions.

Conclusion: Temporal Functional Circuits framework successfully transforms KANs into interpretable models for time-series forecasting, providing faithful temporal explanations through edge function analysis while maintaining competitive performance with traditional architectures.

Abstract: Unlike MLPs, Kolmogorov-Arnold Networks (KANs) expose explicit learnable edge functions on every connection, enabling mechanistic explanation in time-series forecasting. This paper introduces Temporal Functional Circuits, a framework that transforms KAN edge functions from latent visualizations into faithful, temporally grounded explanations. Built on a gated residual KAN that decomposes forecasts into a linear base and a sparsely activated KAN correction, the framework (i) maps each edge to input lags via output-aware attribution, (ii) ranks edges by learned activation range, and (iii) validates faithfulness through edge-level interventions including zeroing and spline removal. Removing the learned B-spline component while retaining the base SiLU term degrades forecasts, providing evidence that the spline shape itself carries predictive value beyond the base activation. On four synthetic regimes of increasing complexity, the learned gate opens progressively wider as signal complexity grows. On regime-switching signals, gated KAN achieves 59% lower MSE than linear-only models. Across eight benchmarks, the gated architecture is competitive with linear, attention, and MLP alternatives, while providing interpretable edge functions that MLP-based corrections cannot offer.

</details>


### [200] [Budgeted Attention Allocation: Cost-Conditioned Compute Control for Efficient Transformers](https://arxiv.org/abs/2605.05697)
*Amrit Nidhi*

Main category: cs.LG

TL;DR: Budgeted Attention Allocation enables controllable attention cost-quality tradeoffs in transformers via monotone head-gating conditioned on attention budgets, achieving measured CPU speedups with minimal accuracy loss.


<details>
  <summary>Details</summary>
Motivation: Transformers typically offer only one inference cost per trained model, while real-world deployment needs multiple cost-quality operating points for different resource constraints.

Method: Proposes Budgeted Attention Allocation with monotone head-gating mechanism conditioned on requested attention budget. Uses dense warm-starting for stability, hard-gate adaptation to convert soft cost control into measured CPU speed, and budgeted structural pruning in pretrained models.

Result: On synthetic task: 99.7% accuracy at 0.303 attention cost, 100% at 0.504 cost. On AG News with custom transformer: 82.1% accuracy with 1.28x CPU speedup at budget 0.50. BERT-Mini AG News: 87.6% accuracy with 1.20x speedup at budget 0.50. DBpedia14: 97.4% accuracy at exact budget 0.50 vs 96.6% for full attention.

Conclusion: Demonstrates reproducible feasibility of controllable checkpoints across budgets that trade attention cost for accuracy and convert to structural speedups on CPU benchmarks, though not claiming universal dominance over specialized approaches.

Abstract: Transformers usually expose one inference cost per trained model, while deployed systems often need multiple cost-quality operating points. We study Budgeted Attention Allocation, a monotone head-gating mechanism conditioned on a requested attention budget. Dense warm-starting is important for stability: on a robust synthetic sequence task, one budgeted model reaches 99.7% accuracy at 0.303 estimated attention cost and 100.0% accuracy at 0.504 cost. On held-out AG News with a custom word-level transformer, hard-gate adaptation turns soft cost control into measured single-thread CPU speed, reaching 82.1% accuracy with 1.28x speedup at budget 0.50. In pretrained BERT-Mini AG News, budgeted structural pruning reaches 87.6% accuracy with 1.20x speedup at budget 0.50; a validation-ranked zero-shot dense post-hoc structural baseline reaches 86.1%, and one recovery epoch raises that per-budget specialist to 87.9%. On DBpedia14, BERT-Mini budgeted gates reach 97.4% at exact budget 0.50 versus 96.6% for dense full attention. Static fixed-budget gates and recovered dense specialists remain strong. The contribution is therefore not universal dominance, but a reproducible feasibility study of one controllable checkpoint across budgets that can trade attention cost for accuracy and be converted into measured structural speedups on small CPU benchmarks.

</details>


### [201] [On the Blessing of Pre-training in Weak-to-Strong Generalization](https://arxiv.org/abs/2605.05710)
*Wei Yao,Wang Zhaoyang,Gengze Xu,Chen Qian,Dongrui Liu,Ziqiao Wang,Yong Liu,Yunbei Xu*

Main category: cs.LG

TL;DR: Pre-training is identified as the essential prerequisite for Weak-to-Strong Generalization (W2SG), which emerges via a phase transition during pre-training rather than being an innate capability.


<details>
  <summary>Details</summary>
Motivation: The decisive role of pre-training in Weak-to-Strong Generalization (W2SG) remains theoretically and empirically under-explored, despite the paradigm suggesting that strong models can surpass weak supervisors.

Method: Theoretical framework formalizes W2SG within a high-dimensional single-index model using spiked Gaussian data, modeling pre-training as spectral initialization. Empirical validation through controlled synthetic simulations and massive-scale evaluation of hundreds of intermediate pre-training checkpoints from large language models.

Result: W2SG is achievable when pre-training provides a geometric warm start placing the model within an "effective region" with perturbed strong-convexity geometry. Performance shows initial improvement then saturation bottleneck dictated by weak supervisor's bias. W2SG emerges via phase transition tightly coupled with pre-training progression.

Conclusion: Pre-training is the essential prerequisite for W2SG emergence, which is not an innate capability but arises through a phase transition during pre-training progression, with theoretical conditions requiring geometric warm starts within specific effective regions.

Abstract: The paradigm of Weak-to-Strong Generalization (W2SG) suggests that a pre-trained strong model can surpass its weak supervisor, yet the decisive role of pre-training remains theoretically and empirically under-explored. In this work, we identify pre-training as the essential prerequisite for the emergence of W2SG. Theoretically, we formalize the W2SG problem within a high-dimensional single-index model framework using spiked Gaussian data, modeling pre-training as a spectral initialization step. Building upon prior impossibility results regarding the failure of learning under random initialization, we prove that W2SG is achievable when pre-training provides a geometric warm start that places the model within an "effective region" characterized by a perturbed strong-convexity geometry. Within this region, we derive a rigorous generalization bound that naturally captures the optimization dynamics: an initial performance improvement followed by a saturation bottleneck dictated by the weak supervisor's bias. Empirically, we first validate all our assumptions and theoretical insights through controlled synthetic simulations. Finally, through a massive-scale evaluation of hundreds of intermediate pre-training checkpoints from large language models, we demonstrate that W2SG is not an innate capability but emerges via a phase transition tightly coupled with the progression of pre-training.

</details>


### [202] [Enabling Federated Inference via Unsupervised Consensus Embedding](https://arxiv.org/abs/2605.05718)
*Yui Hashimoto,Takayuki Nishio,Yuichi Kitagawa,Takahito Tanimura*

Main category: cs.LG

TL;DR: CE-FI enables pretrained models to cooperate during inference without sharing parameters, data, or requiring a common encoder, using consensus embeddings and cooperative output layers trained on shared unlabeled data.


<details>
  <summary>Details</summary>
Motivation: There's a growing need for cooperative inference across distributed ML models while maintaining privacy. Existing methods require sharing data, parameters, or a common encoder, limiting their use in privacy-sensitive or cross-organizational settings.

Method: CE-FI introduces two components: 1) Consensus Embedding (CE) layer that maps heterogeneous intermediate representations into a common embedding space, and 2) Cooperative Output (CO) layer that produces predictions from these embeddings. Both are trained using shared unlabeled data only.

Result: Experiments on CIFAR-10 and CIFAR-100 under diverse non-IID conditions show CE-FI consistently outperforms solo inference and performs comparably to conventional methods requiring stronger sharing. Additional evaluations on text and time-series tasks show broader applicability.

Conclusion: CE-FI provides a practical framework for cooperative inference without privacy compromises, though representation alignment remains a bottleneck and performance depends on ensemble strategy selection.

Abstract: Cooperative inference across independently deployed machine learning models is increasingly desirable in distributed environments, as there is a growing need to leverage multiple models while keeping their data and model parameters private. However, existing cooperative frameworks typically rely on sharing input data, model parameters, or a common encoder, which limits their applicability in privacy-sensitive or cross-organizational settings. To address this challenge, we propose Consensus Embedding-based Federated Inference (CE-FI), a framework that enables pretrained models to cooperate at inference time without sharing model parameters or raw inputs and without assuming a common encoder. CE-FI introduces two components: a Consensus Embedding (CE) layer that maps heterogeneous intermediate representations into a common embedding space, and a Cooperative Output (CO) layer that produces predictions from these embeddings. Both layers are trained using shared unlabeled data only, so the cooperative stage does not require additional labeled data. Experiments on image classification benchmarks -- CIFAR-10 and CIFAR-100 -- under diverse non-IID conditions show that CE-FI consistently outperforms solo inference and performs comparably to conventional methods that require stronger sharing assumptions. Additional evaluations on text and time-series tasks indicate applicability beyond image classification, although performance depends on the ensemble strategy. Further analysis identifies representation alignment as the primary bottleneck.

</details>


### [203] [WARP: A Benchmark for Primal-Dual Warm-Starting of Interior-Point Solvers](https://arxiv.org/abs/2605.05728)
*Dhruv Suri,Helgi Hilmarsson,Shourya Bose*

Main category: cs.LG

TL;DR: Previous claims of ML reducing AC-OPF solver iterations by 30-46% were based on flawed benchmarks against flat starts; against the solver's actual default (variable-bound midpoint), primal-only warm-starts show no improvement. The paper reveals primal-dual-barrier state prediction is essential for real speedup (85% reduction), and introduces WARP to achieve this.


<details>
  <summary>Details</summary>
Motivation: There's growing interest in using machine learning to predict warm-start iterates for AC Optimal Power Flow (AC-OPF) solvers to reduce computational time. However, prior research uses inappropriate evaluation baselines (flat start vs. solver's actual default), inflating reported improvements. The authors aim to establish proper evaluation methods and demonstrate that primal-only approaches fundamentally cannot achieve real speedups due to geometric properties of interior-point methods.

Method: The paper analyzes why primal-only warm-start methods fail, showing primal accuracy is anticorrelated with convergence speed. They conduct oracle experiments to establish the potential of full primal-dual-barrier state prediction. They then develop WARP - a topology-conditioned encode-process-decode interaction network that predicts the complete interior-point state (x, λ, z, μ) on the heterogeneous constraint graph, handling N-1 contingency topology variations without retraining.

Result: When evaluated against the corrected baseline (solver's variable-bound midpoint default), no primal-only warm-start method reduces solver iterations. Oracle experiments show the complete primal-dual-barrier state can reduce IPOPT iterations from 23 to 3 (85% reduction). WARP achieves a 76% reduction in IPOPT iterations while handling topology variations without retraining. The authors release a benchmark suite with dual-labeled AC-OPF datasets and corrected evaluation protocol.

Conclusion: Primal-only warm-start methods for AC-OPF are fundamentally limited and previous reported gains were artifacts of improper benchmarking. True acceleration requires predicting the full interior-point state including dual and barrier variables. The proposed WARP model demonstrates practical effectiveness with significant iteration reductions while handling topology variations, providing a foundation for rigorous evaluation of future warm-start methods.

Abstract: Solving AC Optimal Power Flow (AC-OPF) is of central importance in electricity market operations, where interior-point methods (IPMs) such as IPOPT are the standard solvers. A growing body of work uses machine learning to predict primal warm-start iterates, reporting iteration reductions of 30-46\%. We show that these reported gains rest on an inappropriate evaluation baseline: prior methods benchmark against the flat start $V_m = 1, V_a = 0$, whereas the solver's actual default - the variable-bound midpoint $(l+u)/2$ - is near-optimal for log-barrier centrality. Against this corrected baseline, no primal-only warm-start method reduces solver iterations. We trace the failure to a geometric property of interior-point methods: primal prediction accuracy is anticorrelated with convergence speed, and providing the ground-truth optimal solution $x^*$ without dual variables causes the solver to diverge. Oracle experiments establish that the complete primal-dual-barrier state $(x^*, λ^*, z^*, μ^*)$ reduces IPOPT iterations from 23 to 3 - an 85\% reduction that is structurally inaccessible to primal-only methods. To enable rigorous evaluation of warm-start methods on this task, we release a benchmark suite comprising dual-labeled AC-OPF datasets with IPOPT-extracted solutions, a corrected evaluation protocol, and WARP - a topology-conditioned encode-process-decode interaction network that predicts the full interior-point state $(\hat{x}, \hatλ, \hat{z}, \hatμ)$ on the heterogeneous constraint graph. WARP achieves a 76\% reduction in IPOPT iterations while natively accommodating N-1 contingency topology variations without retraining.

</details>


### [204] [CRAFT: Forgetting-Aware Intervention-Based Adaptation for Continual Learning](https://arxiv.org/abs/2605.05732)
*Md Anwar Hossen,Fatema Siddika,Juan Pablo Munoz,Tanya Roosta,Ali Jannesari*

Main category: cs.LG

TL;DR: CRAFT: A continual learning framework for LLMs that avoids weight updates by learning low-rank interventions on hidden representations, using KL divergence for task routing, regularization, and merging to prevent catastrophic forgetting.


<details>
  <summary>Details</summary>
Motivation: Large language models suffer from catastrophic forgetting when continually fine-tuned on new tasks, which limits their ability to acquire new capabilities without losing previously learned knowledge.

Method: CRAFT uses a three-stage approach: 1) Routes each new task to similar task groups based on output-distribution divergence, 2) Fine-tunes with KL divergence against group prior states to control forgetting and determine convergence, 3) Merges task interventions into shared representations using the same KL signal.

Result: CRAFT outperforms strong LoRA-based approaches in overall performance and forgetting reduction across multiple benchmarks and model scales, while maintaining robustness to task ordering.

Conclusion: Controlling adaptation in representation space guided by output-space divergence provides a scalable and principled approach to continual learning in LLMs, enabling acquisition of new capabilities without catastrophic forgetting.

Abstract: Large language models (LLMs) can acquire new capabilities through fine-tuning, but continual adaptation often leads to catastrophic forgetting. We propose CRAFT, a continual learning framework that avoids updating model weights by instead learning low-rank interventions on hidden representations. CRAFT proceeds in three stages: it first routes each task to a group of similar tasks based on output-distribution divergence; it then fine-tunes the model using a Kullback-Leibler (KL) divergence against the group's prior state, which directly controls forgetting and determines convergence; finally, it merges interventions for the updated task into the shared representation using the same KL signal. This design unifies routing, regularization, and merging through a single KL-based objective. CRAFT improves overall performance and reduces forgetting compared to strong LoRA-based approaches across multiple benchmarks and model scales, while remaining robust to task ordering. These results suggest that controlling adaptation in representation space, guided by output-space divergence, provides a scalable and principled approach to continual learning in LLMs.

</details>


### [205] [CoMemNet: Contrastive Sampling with Memory Replay Network for Continual Traffic Prediction](https://arxiv.org/abs/2605.05738)
*Mei Wu,Wenchao Weng,Wenxin Su,Wenjie Tang,Wei Zhou*

Main category: cs.LG

TL;DR: CoMemNet is a dual-branch continual learning framework for traffic prediction that addresses catastrophic forgetting and memory explosion in streaming traffic networks through dynamic contrastive sampling and adaptive memory buffers.


<details>
  <summary>Details</summary>
Motivation: Existing methods for spatio-temporal learning in non-Euclidean graphs rely on static underlying graph structures, which are inadequate for capturing continuously expanding and evolving patterns in streaming traffic networks where the graph structure dynamically changes over time.

Method: Proposes CoMemNet, a dual-branch continual learning framework with: 1) Fast-converging Online branch for primary prediction tasks, 2) Momentum-updated Target branch using Wasserstein Distance features to create a Dynamic Contrastive Sampler that selects nodes with significant dynamic feature changes, 3) Lightweight Node-Adaptive Temporal Memory Buffer (TMRB-N) to consolidate old knowledge through memory replay and prevent memory explosion.

Result: CoMemNet achieves state-of-the-art (SOTA) performance across three large-scale real-world datasets. The authors also provide two newly curated open-source datasets. Experimental results demonstrate superior performance in traffic prediction tasks.

Conclusion: The proposed CoMemNet framework effectively addresses the limitations of static graph structures in streaming traffic networks by combining continual learning techniques with dynamic contrastive sampling and adaptive memory management, offering a practical solution for real-world traffic prediction with evolving graph structures.

Abstract: In recent years, the integration of non-topological space modeling with temporal learning methods has emerged as an effective approach for capturing spatio-temporal information in non-Euclidean graphs. However, most existing methods rely on static underlying graph structures, which are inadequate for capturing the continuously expanding and evolving patterns in streaming traffic networks. To address this challenge, we propose a simple yet efficient dual-branch continual learning framework for traffic prediction, named CoMemNet. The fast-converging Online branch undertakes the primary prediction tasks, while the momentum-updated Target branch extracts historical information using Wasserstein Distance features to create a Dynamic Contrastive Sampler (DC Sampler). This sampler selects a node set with significant dynamic network feature changes for training, effectively mitigating the issue of catastrophic forgetting. Additionally, the backbone incorporates a lightweight Node-Adaptive Temporal Memory Buffer (TMRB-N) to consolidate old knowledge through memory replay and address the risk of memory explosion. Finally, we provide two newly curated open-source datasets. Experimental results demonstrate that CoMemNet achieves state-of-the-art (SOTA) performance across all three large-scale real-world datasets. The code is available at: https://github.com/meiwu5/CoMemNet.

</details>


### [206] [Multi-Dimensional Behavioral Evaluation of Agentic Stock Prediction Systems Using LLM Judges with Closed-Loop Reinforcement Learning Feedback](https://arxiv.org/abs/2605.05739)
*Mohammad Al Ridhawi,Mahtab Haj Ali,Hussein Al Osman*

Main category: cs.LG

TL;DR: A behavioral evaluation framework for agentic stock prediction systems that uses LLM judges to score decision-making behaviors, with feedback fine-tuning improving trading performance metrics.


<details>
  <summary>Details</summary>
Motivation: Traditional aggregate metrics like MAPE hide the quality of individual interdependent decisions in agentic stock prediction systems, creating a gap in understanding specific behavioral deficiencies.

Method: Behavioral traces are logged at decision points, grouped into five-day episodes, and scored along six dimensions by an ensemble of three LLM judges (GPT-4, Claude Opus, Gemini Pro). Scores are validated via perturbation testing, and deficient scores are converted into a penalty term added to SAC reward for fine-tuning.

Result: Framework shows strong validation (targeted score drops of -1.6 to -2.4 vs -0.32 average on other dimensions, cross-model agreement α=0.85). Composite score correlates ρ=0.72 with 20-day Sharpe. Fine-tuning reduces MAPE 11.5% (0.61% to 0.54%), increases directional accuracy 71% to 74%, and improves Sharpe ratio 18%, with gains concentrated in high-volatility episodes.

Conclusion: Behavioral evaluation with LLM judges effectively identifies and corrects deficiencies in agentic trading systems, enabling targeted performance improvements, especially where the original system was most behaviorally deficient.

Abstract: Agentic stock prediction systems make sequences of interdependent decisions (regime detection, pathway routing, reinforcement learning control) whose individual quality is hidden by aggregate metrics such as mean absolute percentage error (MAPE) or directional accuracy. We present a behavioral evaluation framework that addresses this gap. Behavioral traces logged at every autonomous decision point are grouped into five-day episodes and scored along six domain-specific dimensions (regime detection, routing, adaptation, risk calibration, strategy coherence, error recovery) by an ensemble of three large language model (LLM) judges (GPT 5.4, Claude 4.6 Opus, Gemini 3.1 Pro). Perturbation-based validation on 420 episodes yields targeted score drops of $-1.6$ to $-2.4$ on intended dimensions versus an average of $-0.32$ on the remaining five, with cross-model agreement up to Krippendorff's $α= 0.85$. The composite behavioral score, used here only for cross-episode reporting, correlates at $ρ= 0.72$ with realized 20-day Sharpe ratio from offline backtesting. Closing the loop, the framework converts deficient per-dimension scores into a credit-assigned penalty term added to the Soft Actor-Critic (SAC) reward. Three short fine-tuning cycles, all confined to the validation period, produce on the held-out 2017-2025 test period a one-day MAPE reduction from 0.61% to 0.54% (an 11.5% relative reduction; $p<0.001$, Cohen's $d=0.31$), a directional accuracy increase from 71% to 74%, and an 18% Sharpe ratio improvement (95% bootstrap CI [8.2%, 27.4%]), with gains concentrated in high-volatility episodes where the original system was most behaviorally deficient. Results are from offline backtesting and do not address effects specific to live deployment.

</details>


### [207] [Weak-to-Strong Generalization is Nearly Inevitable (in Linear Models)](https://arxiv.org/abs/2605.05742)
*Scott Geng,Dutch Hansen,Jerry Li*

Main category: cs.LG

TL;DR: Weak-to-strong generalization occurs even in simple linear logistic regression under mild distributional assumptions, showing it's almost inevitable and doesn't require capacity mismatch between student and teacher models.


<details>
  <summary>Details</summary>
Motivation: Recent work showed weak-to-strong generalization in frontier language models where strong students surpass weaker teachers. While empirical and theoretical work has explored this, the authors want to understand if this phenomenon occurs in simpler, more fundamental settings without complex model capacity mismatches.

Method: Analyze weak-to-strong generalization in standard linear logistic regression with mild distributional assumptions on data. Examine whether this phenomenon occurs without requiring the student to be more expressive or have greater model capacity than the teacher.

Result: Weak-to-strong generalization does occur in linear logistic regression for most student-teacher pairs, suggesting the phenomenon is almost inevitable even in basic settings. Contrary to prevailing theoretical belief, it doesn't require capacity mismatch between student and teacher.

Conclusion: Weak-to-strong generalization is a fundamental phenomenon that emerges even in simple settings like linear logistic regression, challenging the assumption that capacity mismatch is central to its occurrence and suggesting it's more universal than previously thought.

Abstract: Weak-to-strong generalization is a phenomenon in post-training whereby a strong student model, when finetuned solely with feedback from a weaker teacher, can not only surpass the teacher, but can improve upon its own capabilities. Recent work of Burns et al. (2023) demonstrated that this can occur in the setting of frontier language models, and subsequently there has been a flurry of both empirical work trying to exploit this phenomenon, as well as theoretical work attempting to understand it. In this work, we demonstrate that weak-to-strong generalization occurs in standard linear logistic regression, under mild distributional assumptions on the data. In fact, we show that this happens for most student-teacher pairs, suggesting that weak-to-strong generalization is in fact \emph{almost inevitable}, even in this basic setting. Notably, our setting does not require the student to be more expressive or have more model capacity in any way compared to the teacher, which runs contrary to the prevailing theoretical belief that a mismatch in model capacity is a central mechanism to weak-to-strong generalization.

</details>


### [208] [RVPO: Risk-Sensitive Alignment via Variance Regularization](https://arxiv.org/abs/2605.05750)
*Ivan Montero,Tomasz Jurczyk,Bhuwan Dhingra*

Main category: cs.LG

TL;DR: RVPO is a risk-sensitive RLHF method that penalizes inter-reward variance to prevent constraint neglect, shifting from maximizing total reward to maximizing consistency across multiple objectives.


<details>
  <summary>Details</summary>
Motivation: Current critic-less RLHF methods use arithmetic mean to aggregate multi-objective rewards, which is vulnerable to constraint neglect - high performance on one objective can mask failures in others (like safety or formatting), allowing models to exploit easy objectives while neglecting difficult but critical constraints.

Method: Proposes Reward-Variance Policy Optimization (RVPO), a risk-sensitive framework that penalizes inter-reward variance during advantage aggregation. Shows via Taylor expansion that a LogSumExp (SoftMin) operator effectively acts as a smooth variance penalty, shifting the objective from maximizing sum to maximizing consistency across rewards.

Result: RVPO improves overall scores on HealthBench (0.261 vs. 0.215 for GDPO at 14B, p < 0.001) and maintains competitive accuracy on GPQA-Diamond without the late-stage degradation observed in other multi-reward methods. The method works across model scales (Qwen2.5-1.5B/3B/7B/14B) with up to 17 concurrent LLM-judged reward signals.

Conclusion: Variance regularization effectively mitigates constraint neglect in multi-objective alignment without sacrificing general capabilities, demonstrating that penalizing inter-reward variance leads to more balanced and consistent performance across all objectives.

Abstract: Current critic-less RLHF methods aggregate multi-objective rewards via an arithmetic mean, leaving them vulnerable to constraint neglect: high-magnitude success in one objective can numerically offset critical failures in others (e.g., safety or formatting), masking low-performing "bottleneck" rewards vital for reliable multi-objective alignment. We propose Reward-Variance Policy Optimization (RVPO), a risk-sensitive framework that penalizes inter-reward variance during advantage aggregation, shifting the objective from "maximize sum" to "maximize consistency." We show via Taylor expansion that a LogSumExp (SoftMin) operator effectively acts as a smooth variance penalty. We evaluate RVPO on rubric-based medical and scientific reasoning with up to 17 concurrent LLM-judged reward signals (Qwen2.5-3B/7B/14B) and on tool-calling with rule-based constraints (Qwen2.5-1.5B/3B). By preventing the model from neglecting difficult constraints to exploit easier objectives, RVPO improves overall scores on HealthBench (0.261 vs. 0.215 for GDPO at 14B, $p < 0.001$) and maintains competitive accuracy on GPQA-Diamond without the late-stage degradation observed in other multi-reward methods, demonstrating that variance regularization mitigates constraint neglect across model scales without sacrificing general capabilities.

</details>


### [209] [Full-Spectrum Graph Neural Network: Expressive and Scalable](https://arxiv.org/abs/2605.05759)
*Xiaohan Wang,Deyu Bo,Longlong Li,Kelin Xia*

Main category: cs.LG

TL;DR: FSpecGNN is a second-order spectral GNN that lifts signals to node-pair domain and uses bivariate spectral filters, achieving greater expressivity than classical spectral GNNs while maintaining scalability.


<details>
  <summary>Details</summary>
Motivation: Classical spectral GNNs are bounded by 1-dimensional Weisfeiler-Lehman test and lack universality for higher-order signals, limiting their expressivity particularly for heterophilic graph learning.

Method: Proposes Full-Spectrum GNN with two key advances: (1) lifting signals from node domain to node-pair domain, and (2) extending univariate spectral filters to bivariate filters over eigenvalue pairs. Includes scalable implementations with low-rank approximation.

Result: FSpecGNN achieves greater expressivity (at most as expressive as Local 2-GNN), universally approximates node-pair signals, enables learning on large graphs via scalable implementations, and delivers strong performance on heterophilic benchmarks.

Conclusion: FSpecGNN overcomes limitations of classical spectral GNNs by operating on node-pair domain with bivariate filters, achieving superior expressivity for heterophilic graph learning while maintaining computational feasibility.

Abstract: It is well established that spectral graph neural networks (GNNs) can universally approximate node signals; however, their expressive power remains bounded by the 1-dimensional Weisfeiler-Lehman test, which is mirrored in their lack of universality for higher-order signals. To go beyond this bound, we propose the Full-Spectrum GNN (FSpecGNN), a second-order generalization of classical spectral GNNs. FSpecGNN advances spectral filtering in two perspectives: (1) it lifts the signal from the node domain to the node-pair domain; and (2) it extends the univariate spectral filter over eigenvalues to a bivariate filter over eigenvalue pairs. We show that classical spectral GNNs arise as a diagonal special case of FSpecGNN, and prove that FSpecGNN can be at most as expressive as Local 2-GNN while universally approximating node-pair signals, the latter being particularly beneficial for heterophilic graph learning. Moreover, FSpecGNN admits scalable implementations that avoid explicit node-pair-level computations; combined with a low-rank approximation that reduces full-spectrum convolution to a combination of polynomial spectral filters, it enables learning on large graphs. Empirically, FSpecGNN validates the predicted expressivity and delivers strong performance on heterophilic benchmarks.

</details>


### [210] [Adaptive Selection of LoRA Components in Privacy-Preserving Federated Learning](https://arxiv.org/abs/2605.05769)
*Myoungjun Kim,Sangwoo Park,Yoseob Han,Jin-Hyun Ahn*

Main category: cs.LG

TL;DR: AS-LoRA: Adaptive layer-wise and round-wise LoRA activation framework for differentially private federated fine-tuning that eliminates aggregation error floors, accelerates convergence, and improves accuracy under strict DP budgets.


<details>
  <summary>Details</summary>
Motivation: Differentially private federated fine-tuning with LoRA suffers from aggregation error due to LoRA's multiplicative structure, which is amplified by DP noise and degrades stability and accuracy. Existing methods use uniform update modes across all layers and rounds, ignoring structural asymmetry between LoRA factors and round-wise training dynamics.

Method: Proposes AS-LoRA with three axes: (1) layer-wise freedom where each layer independently selects its active component, (2) round-wise adaptivity where selection updates over communication rounds, and (3) curvature-aware score derived from second-order loss approximation. Maintains differential privacy with no additional cost.

Result: Improves over federated LoRA baselines by up to +7.5pp on GLUE and +12.5pp on MNLI-mm, matches or exceeds SVD-based aggregation methods at 33-180× lower aggregation cost with negligible communication overhead. Demonstrated across GLUE, SQuAD, CIFAR-100, and Tiny-ImageNet under strict DP budgets and non-IID partitions.

Conclusion: AS-LoRA effectively addresses aggregation error in DP federated LoRA fine-tuning through adaptive layer-wise and round-wise updates, eliminating reconstruction-error floors, accelerating convergence, biasing toward flatter minima, and achieving significant accuracy improvements with low computational cost.

Abstract: Differentially private federated fine-tuning of large models with LoRA suffers from aggregation error caused by LoRA's multiplicative structure, which is further amplified by DP noise and degrades both stability and accuracy. Existing remedies apply a single update mode uniformly across all layers and all communication rounds (or alternate them on a fixed schedule), ignoring both the structural asymmetry between the two LoRA factors and the round-wise dynamics of training. We propose AS-LoRA, an adaptive framework defined by three axes (i) layer-wise freedom, in which each layer independently selects its active component, (ii) round-wise adaptivity, in which the selection updates over communication rounds, and (iii) a curvature-aware score derived from a second-order approximation of the loss. Theoretically, AS-LoRA eliminates the reconstruction-error floor of layer-tied schedules, accelerates convergence, implicitly biases solutions toward flatter minima, and incurs no additional privacy cost. Across GLUE, SQuAD, CIFAR-100, and Tiny-ImageNet under strict DP budgets and non-IID partitions, AS-LoRA improves over the federated LoRA baselines by up to $+7.5$ pp on GLUE and $+12.5$ pp on MNLI-mm for example, while matching or exceeding SVD-based aggregation methods at $33\text{--}180 \times$ lower aggregation cost and with negligible communication overhead. Code for the proposed method is available at https://anonymous.4open.science/r/as_lora-F75F/.

</details>


### [211] [A Measure-Theoretic Finite-Sample Theory for Adaptive-Data Fitted Q-Iteration](https://arxiv.org/abs/2605.05791)
*Manuel Haussmann,Mustafa Mert Çelikok,Melih Kandemir*

Main category: cs.LG

TL;DR: The paper provides a unified theoretical framework for fitted Q-iteration on general measurable spaces, bridging gaps between measure-theoretic MDP foundations, deterministic error analysis, and PAC generalization bounds with finite-sample performance guarantees.


<details>
  <summary>Details</summary>
Motivation: There's a significant gap between the practical success of model-free off-policy deep reinforcement learning in robotics and the existing theoretical foundations, which remain limited to tabular or linearizable settings. The authors identify three isolated traditions in RL theory that fail to provide comprehensive analysis for modern deep RL algorithms.

Method: The paper develops a unified theoretical framework for fitted Q-iteration (FQI) on general measurable Borel spaces. It uses measure-theoretic probability chained with Bellman-operator contraction in Banach spaces, and introduces sequential Rademacher complexity to control Bellman-regression generalization under policy-dependent data collection.

Result: Main results include: (1) A finite-sample, adaptive-data performance bound for FQI; (2) Proof that sequential Rademacher complexity controls Bellman-regression generalization; (3) First cumulative, pathwise online regret guarantee for FQI in continuous spaces.

Conclusion: The unified framework bridges the three previously isolated theoretical traditions and provides the necessary foundations for formal analysis of modern deep RL algorithms operating in continuous, general measurable spaces.

Abstract: While reinforcement learning (RL) promises to revolutionize the control of complex nonlinear robotic systems, a profound gap persists between the heuristic success of model-free off-policy deep RL and the underlying theory, which remains largely confined to tabular or linearizable settings. We identify the cause of this gap as an emergent isolation of three traditions: (i) measure-theoretic MDP foundations on general spaces limit their analysis to exact dynamic programming and ignore all error sources of a learning process; (ii) deterministic error propagation analysis addresses the approximation error via concentrability coefficients without a finite-sample analysis of the estimation error; and (iii) PAC generalization bounds characterize the estimation errors of simplified topologies. We bridge these traditions with a unified theoretical framework for fitted Q-iteration (FQI) on general measurable Borel spaces. Our main result provides a finite-sample, adaptive-data performance bound by chaining measure-theoretic probability with Bellman-operator contraction in Banach spaces. We prove that sequential Rademacher complexity controls Bellman-regression generalization under policy-dependent data collection. We further extend this analysis to provide the first cumulative, pathwise online regret guarantee for FQI in continuous spaces. These results lay the necessary foundations for the formal analysis of many modern deep RL algorithms.

</details>


### [212] [Revealing Modular Gradient Noise Imbalance in LLMs: Calibrating Adam via Signal-to-Noise Ratio](https://arxiv.org/abs/2605.05794)
*Ziqing Wen,Zhouyang Liu,Jiahuan Wang,Ping Luo,Li Shen,Dongsheng Li,Tao Sun*

Main category: cs.LG

TL;DR: MoLS (Module-wise Learning Rate Scaling via SNR) automatically scales Adam optimizer updates per module using signal-to-noise ratio estimation, improving convergence and generalization in LLM training without manual tuning.


<details>
  <summary>Details</summary>
Motivation: Large language models have heterogeneous module composition that creates gradient heterogeneity. Standard adaptive optimizers like Adam don't account for module-level differences, leading to slower convergence, suboptimal performance, or training instability. Existing manual approaches are computationally costly and difficult to generalize.

Method: The authors analyze Adam's noise-damping behavior in high-noise modules and propose MoLS, which estimates module-level signal-to-noise ratios (SNRs) to scale Adam updates automatically. This enables automated module-wise learning rate allocation without manual tuning.

Result: Empirical results across multiple LLM training benchmarks show that MoLS improves convergence speed and generalization. It achieves performance comparable to carefully tuned module-specific learning rates while remaining compatible with memory-efficient training algorithms.

Conclusion: MoLS provides a principled, automated approach to address module-level gradient heterogeneity in LLMs, offering better optimization than standard Adam without the computational cost and generalization limitations of manual tuning methods.

Abstract: The impressive performance of large language models (LLMs) arises from their massive scale and heterogeneous module composition. However, this structural heterogeneity introduces additional optimization challenges. While adaptive optimizers such as Adam(W) provide per-parameter adaptivity, they do not explicitly account for module-level gradient heterogeneity, resulting in slower convergence, suboptimal performance, or training instability. Existing approaches typically rely on manually tuned module-specific learning rates or specific optimization strategies, which are computationally costly and difficult to generalize across tasks or models. To establish a more principled approach, we first analyze the noise-damping behavior of Adam in high-noise modules and introduce \textbf{Module-wise Learning Rate Scaling via SNR (MoLS)}. MoLS estimates module-level SNRs to scale Adam updates, allowing automated module-wise learning rate allocation without manual tuning. Empirical results through multiple LLM training benchmarks demonstrate that MoLS improves convergence speed and generalization, achieving performance comparable to carefully tuned module-specific learning rates, while remaining compatible with memory-efficient training algorithms.

</details>


### [213] [Reward Shaping and Action Masking for Compositional Tasks using Behavior Trees and LLMs](https://arxiv.org/abs/2605.05795)
*Nicholas Potteiger,Ankita Samaddar,Taylor T. Johnson,Xenofon Koutsoukos*

Main category: cs.LG

TL;DR: MRBT (Masking Reward Behavior Tree) uses symbolic structures with LLMs to generate reactive, modular reward functions and action masks for compositional reinforcement learning tasks, improving training efficiency and success rates.


<details>
  <summary>Details</summary>
Motivation: Current approaches using LLMs for reward shaping and action masking in compositional tasks lack reactivity to subtask failure and modularity for varying objects, limiting their effectiveness in complex task decomposition.

Method: Developed MRBT as a symbolic structure serving as reactive/modular reward and action mask function. Created an automated pipeline: LLM generates MRBTs robust to varying objects, SMT-solver verifies correctness, and neurosymbolic RL loop trains agents on compositional tasks.

Result: Successfully generated and refined five MRBTs, consistently improving training efficiency and task success rates over baselines and MRBTs without action masking. Demonstrated transferability, modularity, and verifiability advantages.

Conclusion: MRBT framework effectively addresses reactivity and modularity limitations in compositional task learning, combining symbolic structures with LLMs to create verifiable, efficient training pipelines for complex RL tasks.

Abstract: Decomposing complex tasks into a sequence of simpler subtasks can improve learning efficiency for an autonomous agent. Reinforcement learning (RL) can be used to optimize agent policies to complete subtasks, but requires well-defined subtask rewards and benefits from action masking. Recent work uses large language models (LLMs) to automate reward shaping and action masking, however none of them fully address reactivity to subtask failure and modularity to varying objects for compositional tasks. To overcome these challenges, we develop masking reward behavior tree (MRBT), a symbolic structure used as a reactive and modular reward and action mask function. We design an MRBT template and derive logical specifications to construct and verify MRBTs for a sequence of object-interaction subtasks. Further, we develop an automated pipeline that uses an LLM to generate MRBTs robust to varying task objects, an SMT-solver to verify correctness of specifications, and a neurosymbolic RL loop to train agents on compositional tasks. Experiments demonstrate successful generation and refinement of five MRBTs, consistently improving training efficiency and task success rates over baselines and MRBTs without action masking. We further highlight three advantages of MRBTs: transferability, modularity, and verifiability.

</details>


### [214] [Selective Rollout: Mid-Trajectory Termination for Multi-Sample Agent RL](https://arxiv.org/abs/2605.05802)
*Zhiyuan Zhai,Xin Wang*

Main category: cs.LG

TL;DR: This paper proposes an early stopping method for group-relative RL training (GRPO) that predicts zero-variance groups using intermediate trajectory divergence, reducing wasted compute by 10.7% while improving performance.


<details>
  <summary>Details</summary>
Motivation: GRPO training wastes significant compute on zero-variance groups (≈40% of cases) where all rollouts for a prompt yield identical rewards, providing no gradient information. Existing filtering methods don't use intermediate rollout information to make early stopping decisions.

Method: A one-parameter gating mechanism that stops group sampling when the mean pairwise edit distance between partial action sequences falls below a threshold, predicting that the group will converge to zero variance before completion.

Result: The gated approach finishes 10.7% faster in wall-clock time (statistically significant) and improves held-out success rate by +2.5 percentage points on ALFWorld tasks with Qwen2.5-7B, due to reduced zero-advantage gradient-batch dilution.

Conclusion: Monitoring intermediate trajectory divergence enables efficient early stopping in GRPO training, substantially reducing wasted compute while improving learning performance by better preserving gradient signal quality.

Abstract: Group-relative RL training (GRPO) samples a small group of parallel rollouts for every training prompt and uses their within-group reward spread to compute per-trajectory advantages. In agentic environments each rollout is a long multi-turn dialogue with one LLM call per step, so this multi-sample multiplier dominates the total training cost. When every rollout of a prompt ends with the same reward, the group has zero reward variance and contributes no gradient, so the extra rollouts add no information; such groups are common in practice (typically around 40% of all groups), so the wasted-compute fraction is substantial rather than marginal. Existing methods filter such groups at the prompt level, either after their rollouts are paid for or before any rollout begins, but both decide without using information that becomes available during the rollout itself. We instead ask whether the in-group divergence between the partial trajectories at an intermediate step can already predict that the group will be zero-variance: when the parallel rollouts have already converged on the same action prefix, the group is on track to produce a single reward, and we can stop early. We propose a one-parameter gate that stops a group when the mean pairwise prefix edit distance between its partial action sequences falls below a threshold. On a 60-iteration on-policy GRPO run on ALFWorld with Qwen2.5-7B, averaged over four random seeds, the gated arm finishes 10.7% faster in wall-clock (bootstrap 95% CI excludes 0) and shifts held-out success rate on 50 unseen tasks by +2.5 pp, with the held-out gain tracing to a measurable reduction in zero-advantage gradient-batch dilution. Code is available at https://github.com/zhiyuanZhai20/selective-rollout.

</details>


### [215] [Retrieval from Within: An Intrinsic Capability of Attention-Based Models](https://arxiv.org/abs/2605.05806)
*Elad Hoffer,Yochai Blau,Ron Banner,Daniel Soudry,Boris Ginsburg*

Main category: cs.LG

TL;DR: INTRA enables neural models to retrieve directly from their own internal representations via attention mechanisms, eliminating the need for separate retrieval systems in RAG pipelines.


<details>
  <summary>Details</summary>
Motivation: Current RAG systems treat retrieval and generation as separate components, creating pipeline mismatches and inefficiencies. The authors explore whether attention-based models can retrieve from their own representations rather than using external retrievers.

Method: Introduces INTRA framework where decoder attention queries score pre-encoded evidence chunks that are then reused as context for generation. This amortizes context encoding by reusing precomputed encoder states across queries.

Result: Outperforms strong engineered retrieval pipelines on question-answering benchmarks for both evidence recall and end-to-end answer quality.

Conclusion: Attention-based models already possess inherent retrieval mechanisms that can be elicited without external modules, suggesting unified retrieval-generation architectures are viable.

Abstract: Retrieval-augmented generation (RAG) typically treats retrieval and generation as separate systems. We ask whether an attention-based encoder-decoder can instead retrieve directly from its own internal representations. We introduce INTRA (INTrinsic Retrieval via Attention), a framework where decoder attention queries score pre-encoded evidence chunks that are then directly reused as context for generation. By construction, INTRA unifies retrieval and generation, eliminating the retriever-generator mismatch typical of RAG pipelines. This design also amortizes context encoding by reusing precomputed encoder states across queries. On question-answering benchmarks, INTRA outperforms strong engineered retrieval pipelines on both evidence recall and end-to-end answer quality. Our results demonstrate that attention-based models already possess a retrieval mechanism that can be elicited, rather than added as an external module.

</details>


### [216] [A Testable Certificate for Constant Collapse in Teacher-Guided VAEs](https://arxiv.org/abs/2605.05813)
*Zegu Zhang,Jianhua Peng,Jian Zhang*

Main category: cs.LG

TL;DR: The paper provides a certification method to detect input-independent constant collapse in VAEs by establishing an exact threshold based on teacher mutual information.


<details>
  <summary>Details</summary>
Motivation: Current methods diagnose posterior collapse through symptoms like small KL terms or weak latent code use, but these don't define precise collapse boundaries, making it hard to measure and prevent the failure mode.

Method: The study focuses on input-independent constant collapse and shows that for any nonconstant teacher distribution, the best constant student is the dataset-average teacher distribution, with alignment cost equal to teacher mutual information I_T(X;T). This establishes a measurable threshold for certification.

Result: Experiments on CIFAR-100 and Tiny-ImageNet-200 demonstrate: 1) full training stays certified, 2) removing alignment drives collapse, 3) restarting from collapsed checkpoints restores certification. Standard VAE baselines fail the certificate despite preserving reconstruction quality.

Conclusion: The method provides a narrow but measurable guarantee that matched nonconstant teacher-relative variation passes through the latent pathway, offering a concrete certification approach for detecting constant collapse, though it doesn't claim to rule out all collapse forms.

Abstract: Posterior collapse in variational autoencoders is often diagnosed by its symptoms: a small KL term, a strong decoder, or weak use of the latent code. These signals are useful, but they do not define a collapse boundary. We study a concrete failure mode, input-independent constant collapse, and show that this case admits an exact threshold. For any fixed nonconstant teacher distribution \(T(\cdot\mid x)\), the best constant student is the dataset-average teacher distribution, and its alignment cost is the teacher mutual information \(I_T(X;T)\). Therefore, if a strictly latent-only raw witness achieves alignment loss below this value, with a safety margin, the witness cannot be constant in the input.
  This identity turns a qualitative failure mode into a measurable one. In CIFAR-100 experiments with per-seed teacher search, full training stays on the certified side of the boundary, removing alignment drives the raw witness into the constant-student regime, and restarting from a collapsed checkpoint with alignment enabled restores the certificate. Tiny-ImageNet-200 fixed-target runs show the same prevention--collapse--rescue pattern across three independently searched teachers. Standard VAE-style baselines, including methods that preserve reconstruction quality or post-hoc predictability, remain negative under the raw certificate. The guarantee is intentionally narrow: it certifies that the matched nonconstant teacher-relative variation passes through the latent pathway, rather than claiming that all forms of posterior collapse have been ruled out.

</details>


### [217] [HCInfer: An Efficient Inference System via Error Compensation for Resource-Constrained Devices](https://arxiv.org/abs/2605.05819)
*Shen Xu,Xiangwen Zhuge,Zhe Xu,Yingkun Hu,Zheng Yang,Yunhao Liu*

Main category: cs.LG

TL;DR: HCInfer is a heterogeneous inference system that offloads LoRA-style compensation parameters to CPU while running compressed model on GPU, achieving 5.2% accuracy improvement and 10.4x speedup.


<details>
  <summary>Details</summary>
Motivation: LLMs struggle with memory constraints on consumer hardware, and existing compression/offloading solutions suffer from accuracy degradation or throughput bottlenecks. The paper observes that error compensation methods using LoRA-style branches are naturally suitable for offloading since they require large storage but only access small subsets during inference.

Method: HCInfer uses heterogeneous inference where the compressed backbone executes on GPU while residual compensation parameters are offloaded to CPU. The system includes: 1) asynchronous compensation pipeline to hide overhead, and 2) sensitivity-aware dynamic rank allocation to maximize accuracy recovery.

Result: Experimental results show HCInfer achieves up to 5.2% accuracy improvement on downstream tasks compared to compression-only models, while maintaining up to 10.4x speedup compared to full-precision models.

Conclusion: HCInfer effectively addresses the memory-accuracy-throughput trade-off in LLM deployment by intelligently leveraging heterogeneous computing resources, demonstrating practical feasibility for memory-constrained consumer hardware deployment.

Abstract: LLMs often struggle with memory-constrained deployment on consumer-grade hardware due to their massive parameter sizes. While existing solutions such as model compression and offloading improve deployment feasibility, they often suffer from substantial accuracy degradation or severe throughput bottlenecks. Recent error compensation methods recover accuracy through auxiliary LoRA-style branches, and we observe that these branches are inherently amenable to offloading: they require substantial parameter storage but access only a small subset of compensation parameters during each inference step. Motivated by this opportunity, we propose HCInfer, a heterogeneous inference system that offloads residual compensation to the CPU while executing the compressed backbone on the GPU, and further introduces an asynchronous compensation pipeline and sensitivity-aware dynamic rank allocation to hide compensation overhead and maximize accuracy recovery. Experimental results show that HCInfer achieves a maximum accuracy improvement of 5.2% on downstream tasks compared to compression model and sustaining a maximum speedup of 10.4x compared to full-precision model.

</details>


### [218] [MDN: Parallelizing Stepwise Momentum for Delta Linear Attention](https://arxiv.org/abs/2605.05838)
*Yulong Huang,Xiang Liu,Hongxiang Huang,Xiaopeng Lin,Zunchang Liu,Xiaowen Chu,Zeke Xie,Bojun Cheng*

Main category: cs.LG

TL;DR: Momentum DeltaNet (MDN) introduces momentum into linear attention models via a chunkwise parallel algorithm with stepwise momentum, improving long sequence modeling while maintaining training efficiency.


<details>
  <summary>Details</summary>
Motivation: Existing linear attention models that interpret linear recurrences as SGD suffer from rapid information decay and suboptimal convergence. Momentum-based optimizers could help but pose challenges in achieving both training efficiency and effectiveness simultaneously.

Method: Developed a chunkwise parallel algorithm for linear attention with stepwise momentum rule by geometrically reordering update coefficients. Analyzed momentum-based recurrence as a second-order system with complex conjugate eigenvalues to guide stable gating constraints. Implemented using Triton kernels for efficiency.

Result: MDN achieves comparable training throughput with competitive linear models (Mamba2, KDA) while demonstrating consistent performance improvements over Transformers, Mamba2 and GDN across diverse benchmarks on 400M and 1.3B parameter models.

Conclusion: Momentum DeltaNet successfully integrates momentum into linear attention models, addressing optimization limitations while maintaining efficiency, leading to improved performance for long sequence modeling tasks.

Abstract: Linear Attention (LA) offers a promising paradigm for scaling large language models (LLMs) to long sequences by avoiding the quadratic complexity of self-attention. Recent LA models such as Mamba2 and GDN interpret linear recurrences as closed-form online stochastic gradient descent (SGD), but naive SGD updates suffer from rapid information decay and suboptimal convergence in optimization. While momentum-based optimizers provide a natural remedy, they pose challenges in simultaneously achieving training efficiency and effectiveness. To address this, we develop a chunkwise parallel algorithm for LA with a stepwise momentum rule by geometrically reordering the update coefficients. Further, from a dynamical systems perspective, we analyze the momentum-based recurrence as a second-order system that introduces complex conjugate eigenvalues. This analysis guides the design of stable gating constraints. The resulting model, Momentum DeltaNet (MDN), leverages Triton kernels to achieve comparable training throughput with competitive linear models such as Mamba2 and KDA. Extensive experiments on the 400M and 1.3B parameter models demonstrate consistent performance improvements over strong baselines, including Transformers, Mamba2 and GDN, across diverse downstream evaluation benchmarks. Code: https://github.com/HuuYuLong/MomentumDeltaNet .

</details>


### [219] [Hypothesis generation and updating in large language models](https://arxiv.org/abs/2605.05851)
*Hua-Dong Xiong*

Main category: cs.LG

TL;DR: LLMs show Bayesian-like inference with systematic biases in hypothesis testing, favoring simpler hypotheses and showing an evaluation-generation gap, with poor generalization beyond observed examples.


<details>
  <summary>Details</summary>
Motivation: To understand how LLMs perform inference in problem-solving scenarios involving hypothesis generation and updating from partial information, and to compare their performance with optimal Bayesian models and human behavior.

Method: Used the number game as a controlled setting, measuring posterior over hypotheses using three probes: posterior prediction, hypothesis evaluation, and hypothesis generation. Compared LLM behavior with optimal Bayesian models and human behavior, testing consistency across probes.

Result: LLMs are often well described by a two-parameter Bayesian fit with systematic offsets: they show a strong-sampling assumption favoring narrower hypotheses, and thinking mode shifts them toward greater prior reliance. Found a robust evaluation-generation gap where LLMs select more correct hypotheses during evaluation but generate simpler hypotheses. Bayesian-with-bias pattern doesn't extrapolate well to unseen parts of the hypothesis domain.

Conclusion: LLMs have limitations as general problem solvers for scientific inference, particularly in hypothesis generation that must go beyond observed data, showing systematic biases and poor generalization.

Abstract: Large language models (LLMs) increasingly help people solve problems, from debugging code to repairing machinery. This process requires generating plausible hypotheses from partial descriptions, then updating them as more information arrives. Yet how LLMs perform this form of inference, and how close it is to optimal, remains unclear. We study this question in the number game, a controlled setting in which a learner infers the hypothesis supported by a few positive integers, such as $\{16, 8, 2, 64\}$: a rule like powers of 2 or an interval like numbers near 20. We measure the posterior over hypotheses using three complementary probes: posterior prediction, hypothesis evaluation, and hypothesis generation. We then compare LLM behavior with an optimal Bayesian model and human behavior, and test whether the same posterior is expressed across probes. LLMs are often well described by a two-parameter Bayesian fit, but with systematic offsets: by default they show a strong-sampling assumption that creates an implicit Occam's razor, favoring narrower hypotheses, while thinking mode shifts them toward greater prior reliance. We also find a robust evaluation--generation gap: LLMs select more correct hypotheses during hypothesis evaluation but generate simpler, more rule-like hypotheses. Finally, this Bayesian-with-bias pattern does not extrapolate. Models can behave as if they hold rule-like hypotheses over observed examples, yet generalize poorly to parts of the hypothesis domain not covered by those examples. Our results highlight a limitation of LLMs as general problem solvers, especially for scientific inference, where hypotheses must go beyond the data.

</details>


### [220] [Measuring Learning Progress via Gradient-Momentum Coupling](https://arxiv.org/abs/2605.05856)
*Samuel Blad,Martin Längkvist,Amy Loutfi*

Main category: cs.LG

TL;DR: Gradient-Momentum Coupling (GMC) is a new learning progress signal that uses optimization dynamics instead of prediction error, making curiosity-driven RL more robust to noise by measuring how gradients contribute to ongoing parameter updates.


<details>
  <summary>Details</summary>
Motivation: Existing learning progress signals like prediction error in curiosity-driven RL often fail to distinguish meaningful learnable patterns from random noise, which limits their effectiveness in exploration.

Method: The paper proposes Gradient-Momentum Coupling (GMC) - a signal derived from optimization dynamics that measures per-parameter normalized absolute product between each sample's gradient and the momentum from previous gradients. This leverages momentum's natural noise filtering to identify samples that actually contribute to ongoing learning.

Result: Controlled experiments show GMC has noise robustness and enables emergent curriculum learning by prioritizing tasks based on learning speed rather than difficulty. Experiments on MiniGrid demonstrate that replacing prediction error with GMC in existing curiosity-driven architectures improves robustness to observation noise.

Conclusion: GMC provides a more reliable learning progress signal than prediction error for curiosity-driven exploration in RL, offering better noise robustness and enabling more effective task prioritization through its optimization dynamics approach.

Abstract: Measuring learning progress is essential for curiosity-driven exploration in reinforcement learning, but widely used signals such as prediction error often fail to distinguish meaningful, learnable patterns from random noise. This paper proposes Gradient-Momentum Coupling (GMC), a signal derived from optimization dynamics that quantifies how useful each sample's gradient is for ongoing learning by measuring its per-parameter normalized absolute product with the momentum from previous gradients. By leveraging momentum's natural filtering of noise and oscillations, GMC identifies samples that contribute to ongoing parameter updates. Controlled experiments demonstrate noise robustness and emergent curriculum learning, with the signal prioritizing tasks by learning speed rather than difficulty. Experiments on MiniGrid suggest that replacing prediction error with GMC within existing curiosity-driven architectures can improve robustness to observation noise.

</details>


### [221] [Offline Reinforcement Learning for Rotation Profile Control in Tokamaks](https://arxiv.org/abs/2605.05857)
*Rohit Sonker,Hiro Josep Farre Kaga,Jiayu Chen,Andrew Rothstein,Ian Char,Ricardo Shousha,Egemen Kolemen,Jeff Schneider*

Main category: cs.LG

TL;DR: Researchers successfully deployed offline reinforcement learning and model-based RL for plasma rotation profile control on DIII-D tokamak using only historical data, achieving promising real-world results.


<details>
  <summary>Details</summary>
Motivation: Tokamak plasma rotation profile control is challenging due to high dimensionality, multiple actuators, and plasma condition dependence. Traditional methods struggle with full profile control, and learning-based approaches are needed but hindered by lack of accurate simulators.

Method: Used offline RL and offline model-based RL algorithms trained solely on historical data from DIII-D tokamak. Specifically employed probabilistic models of plasma dynamics to generate rollouts for RL training.

Result: Successfully deployed the RL policy on the DIII-D Tokamak and observed promising real-world results, demonstrating effective multi-input multi-output control for plasma rotation profile.

Conclusion: The work demonstrates the feasibility of using offline RL with only historical data for complex physical system control, while highlighting key challenges and insights from training and deploying RL policies on real-world fusion devices.

Abstract: Tokamaks remain leading candidates for achieving practical fusion energy, yet many important control problems inside these devices are still difficult or unsolved. One such challenge is controlling the plasma rotation profile, which strongly influences stability, confinement, and transport. While the average rotation can be controlled, controlling the full profile is challenging due to high dimensionality, response to multiple actuators and dependence on plasma condition. Learning-based control methods, such as reinforcement learning (RL), provide a potential solution to this challenging problem with ability to model complex interactions leading to effective multi-input multi-output control. However, learning such policies is challenging due to the lack of accurate simulators that can model the rotation profile dynamics. In this work, we investigate the use of offline RL and offline model-based RL algorithms for rotation profile control, training them solely on historical data from the DIII-D tokamak. Our final method uses probabilistic models of plasma dynamics to generate rollouts for RL training. We deploy this policy on the DIII-D Tokamak and observe promising real-world results. We conclude by highlighting key challenges and insights from training and deploying an RL policy on a complex physical device while using only limited past data.

</details>


### [222] [Do Neural Operators Forget Geometry? The Forgetting Hypothesis in Deep Operator Learning](https://arxiv.org/abs/2605.05862)
*Yanming Xia,Angelica I. Aviles-Rivero*

Main category: cs.LG

TL;DR: Neural operators progressively lose geometric information as depth increases (Geometric Forgetting Hypothesis), degrading performance on irregular geometries. The paper introduces geometry memory injection to counteract this issue.


<details>
  <summary>Details</summary>
Motivation: Neural operators perform well on structured domains but struggle with irregular geometries. The authors investigate whether this limitation is merely an encoding issue or a deeper architectural problem inherent to deep operator networks.

Method: The paper formalizes the Geometric Forgetting Hypothesis, uses layer-wise geometric probing to analyze spectral and attention-based operators, and introduces a lightweight geometry memory injection mechanism to restore geometric constraints at intermediate depths with minimal architectural overhead.

Result: Demonstrates that neural operators systematically lose geometric fidelity as depth increases, which degrades accuracy, stability, and generalization. The geometry memory injection consistently mitigates forgetting and reveals geometric shortcut instability in transformer-based operators.

Conclusion: Geometric forgetting is a fundamental failure mode in deep operator architectures, not just an encoding issue. Geometric retention is a structural requirement rather than a design choice, and the proposed intervention effectively counters this limitation.

Abstract: Neural operators perform well on structured domains, yet their behaviour on irregular geometries remains poorly understood. We show that this limitation is not merely an encoding issue, but a depth-wise failure mode inherent to deep operator architectures.
  We formalise the Geometric Forgetting Hypothesis: due to the Markovian structure of operator layers and their reliance on global mixing mechanisms, neural operators progressively lose access to domain geometry as depth increases. Using layer-wise geometric probing, we demonstrate that both spectral and attention-based operators systematically lose geometric fidelity.
  We show that this geometric forgetting degrades accuracy, stability, and generalisation. To counteract it, we introduce a lightweight geometry memory injection mechanism that restores geometric constraints at intermediate depths with minimal architectural overhead. This simple intervention consistently mitigates forgetting and exposes a geometric shortcut instability in transformer-based operators, revealing that geometric retention is a structural requirement rather than a design choice.

</details>


### [223] [SOPE: Stabilizing Off-Policy Evaluation for Online RL with Prior Data](https://arxiv.org/abs/2605.05863)
*Carlo Romeo,Girolamo Macaluso,Alessandro Sestini,Andrew D. Bagdanov*

Main category: cs.LG

TL;DR: SOPE is an algorithm that uses actor-aligned off-policy policy evaluation as an automated early-stopping mechanism to dynamically control offline training phases, achieving better performance with significantly less computation than static methods.


<details>
  <summary>Details</summary>
Motivation: Current methods for incorporating prior data into online RL face a difficult trade-off: either accept high computational costs from static update schedules or rely on manually-tuned fixed-length stabilization phases that risk wasting prior knowledge or causing severe overfitting.

Method: SOPE uses actor-aligned Off-Policy Policy Evaluation (OPE) as an automated early-stopping mechanism. It evaluates the critic on a held-out validation split under the current policy's action distribution, dynamically halting gradient updates when out-of-distribution benefits saturate.

Result: On 25 continuous control tasks from the Minari benchmark suite, SOPE improves baseline performance by up to 45.6% while reducing required computational resources by up to 22x (measured in TFLOPs).

Conclusion: Adaptive, evaluation-driven update schedules are more effective than static, exhaustive update schedules for balancing the trade-off between sample and computational efficiency in offline-to-online reinforcement learning.

Abstract: Incorporating prior data into online reinforcement learning accelerates training but typically forces a difficult trade-off between high computational costs and long, multi-stage training pipelines. While fixed-length stabilization phases are significantly more computationally efficient than static update schedules, they require task-dependent manual tuning, risking either the waste of prior knowledge or severe overfitting. To address this, we propose SOPE, an algorithm that uses an actor-aligned Off-Policy Policy Evaluation (OPE) signal as an automated early-stopping mechanism to dynamically control the length of offline training phases. By evaluating the critic on a held-out validation split under the current policy's action distribution, SOPE halts gradient updates exactly when out-of-distribution benefits saturate, eliminating the need for manual schedule tuning. Evaluated on 25 continuous control tasks from the Minari benchmark suite, SOPE improves baseline performance by up to 45.6% while reducing the required TFLOPs by up to 22x, thus balancing the tradeoff between sample and computational efficiency. These findings demonstrate that adaptive, evaluation-driven update schedules are more effective than relying on static, exhaustive update schedules.

</details>


### [224] [QuadraSHAP: Stable and Scalable Shapley Values for Product Games via Gauss-Legendre Quadrature](https://arxiv.org/abs/2605.05870)
*Majid Mohammadi,Grigory Reznikov,Pavel Sinitcyn,Krikamol Muandet,Siu Lun Chau*

Main category: cs.LG

TL;DR: Exact one-dimensional integral representation for Shapley values in product games with provably exact Gauss-Legendre quadrature requiring only O(⌈d/2⌉) nodes, enabling fast computation even for thousands of features.


<details>
  <summary>Details</summary>
Motivation: Product games arise in ML explainability when value functions inherit multiplicative structure from models like kernel methods with product kernels and tree-based models. Current Shapley value computation requires summing over exponentially many feature coalitions, which is computationally prohibitive for high-dimensional settings.

Method: Derived an exact 1D integral representation for Shapley values in product games, showing they collapse to integral of degree-(d-1) polynomial over [0,1]. Used Gauss-Legendre quadrature that is provably exact with m_q ≥ ⌈d/2⌉ nodes. Implemented numerically stable log-space evaluation and efficient parallel implementation using associative scan primitives (O(d m_q) work, O(log d) parallel time).

Result: The method (QuadraSHAP) achieves highly precise estimates with few hundred nodes even for thousands of features. It's provably exact with sufficient nodes and otherwise provides geometrically decaying error. Experimental results show QuadraSHAP is the fastest numerically stable method across all tested configurations.

Conclusion: The integral representation enables efficient computation of Shapley values for product games, providing a theoretically grounded and practically efficient solution for ML explainability tasks where multiplicative structure exists, overcoming exponential complexity barriers.

Abstract: We study the efficient computation of Shapley values for \emph{product games} -- cooperative games in which the coalition value factorizes as a product of per-player terms. Such games arise in machine learning explainability whenever the value function inherits a multiplicative structure from the underlying model, as in kernel methods with product kernels and tree-based models. Our key result is that the Shapley value of each player in a product game admits an exact one-dimensional integral representation: the weighted sum over exponentially many feature coalitions collapses to the integral of a degree-$(d-1)$ polynomial over $[0,1]$, where $d$ is the total number of features. This yields a Gauss--Legendre quadrature scheme that is \emph{provably exact} whenever the number of nodes satisfies $m_q \geq \lceil d/2 \rceil$, and otherwise provides a \emph{near-exact} approximation with error provably decaying geometrically in $m_q$. In practice, a few hundred nodes can achieve highly precise estimates even with thousands of features. Building on this formulation, we derive a numerically stable implementation via log-space evaluation, together with an efficient parallel implementation based on associative scan primitives that achieves $O(d\,m_q)$ total work and $O(\log d)$ parallel time. Experiments show that \textsc{QuadraSHAP} is the fastest numerically stable method across all tested configurations.

</details>


### [225] [Retain-Neutral Surrogates for Min-Max Unlearning](https://arxiv.org/abs/2605.05871)
*Junhao Cai,Dohun Kim,Dowon Kim,Sung Il Choi,Chengjun Jin,Juhyun Park,Changhee Joo*

Main category: cs.LG

TL;DR: ROSU is a new machine unlearning method that constrains surrogate point updates to be orthogonal to retain gradients, preventing damage to retained data performance while effectively removing forgotten data influence.


<details>
  <summary>Details</summary>
Motivation: Existing approximate unlearning methods, particularly min-max approaches, can inadvertently increase retain loss when forget and retain gradients are strongly aligned, potentially harming performance on the remaining data while trying to forget specific samples.

Method: Retain-Orthogonal Surrogate Unlearning (ROSU) introduces a constrained surrogate construction: maximize first-order forget gain while maintaining zero first-order retain change under a fixed perturbation budget. This yields a closed-form retain-orthogonal perturbation, lightweight transported outer update, and amplification along retain-neutral directions.

Result: ROSU provides theoretical guarantees including curvature-controlled second-order bounds on retain damage, strict reduction of surrogate retain loss in positive-alignment regimes, and near-equivalence to standard methods when gradients are orthogonal. Empirically, ROSU shows clearest gains in high-coupling regimes (where gradients align strongly) while remaining competitive elsewhere across vision and language benchmarks.

Conclusion: ROSU offers a geometrically-grounded solution to machine unlearning that effectively prevents retain loss damage during forgetting operations, particularly valuable in practical scenarios where forget and retain gradients often align strongly.

Abstract: Machine unlearning seeks to remove the influence of designated training data while preserving performance on the remaining data. Approximate unlearning can be viewed as a local editing problem; in min-max unlearning, the key local object is the surrogate point at which the retain objective is evaluated. When forget and retain gradients are strongly aligned, an unconstrained forget-maximizing perturbation can move to a surrogate point that increases retain loss. We propose Retain-Orthogonal Surrogate Unlearning (ROSU), which constrains the inner surrogate construction by maximizing first-order forget gain subject to zero first-order retain change under a fixed perturbation budget. This yields a closed-form retain-orthogonal perturbation, a lightweight transported outer update, and amplification along the retain-neutral direction. Our analysis establishes (i) a curvature-controlled second-order bound on retain damage, (ii) a positive-alignment regime in which ROSU strictly reduces surrogate retain loss relative to standard min-max perturbations, and (iii) near-equivalence when the two gradients are nearly orthogonal. Across vision and language benchmarks (CIFAR-10/100, Tiny-ImageNet, TOFU, WMDP), the empirical pattern follows this geometry: ROSU gives its clearest gains in high-coupling regimes while remaining competitive elsewhere.

</details>


### [226] [RepFlow: Representation Enhanced Flow Matching for Causal Effect Estimation](https://arxiv.org/abs/2605.05890)
*Yifei Xie,Jian Huang*

Main category: cs.LG

TL;DR: RepFlow is a novel framework for causal effect estimation that combines representation learning with Conditional Flow Matching to address selection bias and enable distribution modeling of potential outcomes.


<details>
  <summary>Details</summary>
Motivation: Causal effect estimation from observational data is critical but faces fundamental challenges: missing counterfactuals and selection bias. Existing methods are limited to point estimates and lack distribution modeling capabilities.

Method: RepFlow formulates causal effect estimation as joint optimization integrating representation learning with Conditional Flow Matching. It mitigates selection bias by minimizing entropically regularized Wasserstein distance between treated and control representations, with L2 normalization for stability.

Result: Extensive experiments across benchmarks show RepFlow consistently outperforms existing methods in both point and distributional causal effect estimation.

Conclusion: RepFlow provides an effective framework for causal inference that addresses key limitations of existing methods by enabling distribution modeling while handling selection bias through integrated representation learning and flow matching.

Abstract: Estimating causal effects from observational data has become increasingly critical in diverse fields including healthcare, economics, and social policy. The fundamental challenge in causal inference arises from the missing counterfactuals and the selection bias. Existing methods are largely limited to point estimates and lack the capacity for distribution modeling. In this work, we propose RepFlow, a novel framework that formulates causal effect estimation as a joint optimization problem integrating representation learning with Conditional Flow Matching (CFM).
  RepFlow mitigates selection bias by minimizing the entropically regularized Wasserstein distance between treated and control representations.
  To enhance numerical stability, we further introduce an $L_2$ normalization constraint on latent representations.
  This balanced representation enables the flow model to accurately capture the distribution of potential outcomes. Extensive experiments across a wide range of benchmarks demonstrate that RepFlow consistently outperforms existing methods in both point and distributional causal effect estimation.

</details>


### [227] [VARS-FL: Validation-Aligned Client Selection for Non-IID Federated Learning in IoT Systems](https://arxiv.org/abs/2605.05896)
*Mohamed Lakas,Mohamed Amine Ferrag*

Main category: cs.LG

TL;DR: VARS-FL introduces a reputation-based client selection method for federated learning that tracks historical contributions via validation loss reduction, improving convergence and performance in non-IID IoT environments.


<details>
  <summary>Details</summary>
Motivation: Current FL systems use stateless client selection that ignores accumulated evidence of client contributions, leading to slow convergence and unstable training under non-IID data, especially in IoT/IIoT environments with highly heterogeneous data and different traffic patterns.

Method: Proposes VARS-FL framework that quantifies each client's contribution using reduction in server-side validation loss induced by their update, aggregated into a Reputation score combining sliding-window average of recent contributions with logarithmically scaled participation term for exploration-exploitation selection. Requires no changes to local training or aggregation and is fully compatible with standard FedAvg.

Result: Evaluated on 15-class non-IID IoT intrusion detection task using Edge-IIoTset dataset with 100 clients across multiple seeds. VARS-FL consistently improves accuracy, F1-Macro, and loss while accelerating convergence (up to 36% fewer rounds to reach 80% accuracy) compared to FedAvg, Oort, and Power-of-Choice.

Conclusion: Validation-aligned, history-aware client selection provides more reliable and efficient training process for federated learning in heterogeneous IoT environments, demonstrating superior performance over existing client selection methods.

Abstract: Federated learning (FL) systems typically employ stateless client selection, treating each communication round independently and ignoring accumulated evidence of client contribution quality. Under non-IID data, this leads to slow convergence and unstable training, particularly when selection relies on local proxies (e.g., training loss) that are misaligned with the global optimization objective. These challenges are especially pronounced in Internet of Things (IoT) and Industrial IoT (IIoT) environments, where data is highly heterogeneous and distributed across devices observing different traffic patterns. In this paper, we propose VARS-FL (Validation-Aligned Reputation Scoring for Federated Learning), a client selection framework that quantifies each client's contribution using the reduction in server-side validation loss induced by its update. These per-round signals are aggregated into a Reputation score that combines a sliding-window average of recent contributions with a logarithmically scaled participation term, enabling robust exploration-exploitation selection. VARS-FL requires no changes to local training or aggregation and remains fully compatible with standard FedAvg. We evaluate VARS-FL on a 15-class non-IID IoT intrusion detection task using the Edge-IIoTset dataset, with 100 clients across multiple seeds, and compare it against FedAvg, Oort, and Power-of-Choice. VARS-FL consistently improves accuracy, F1-Macro, and loss, while accelerating convergence (up to 36% fewer rounds to reach 80% accuracy). These results demonstrate that validation-aligned, history-aware client selection provides a more reliable and efficient training process for federated learning in heterogeneous IoT environments.

</details>


### [228] [VisMMOE: Exploiting Visual-Expert Affinity for Efficient Visual-Language MoE Offloading](https://arxiv.org/abs/2605.05899)
*Cheng Xu,Xiaofeng Hou,Jiacheng Liu,Chao Li*

Main category: cs.LG

TL;DR: VisMMoE is a system that improves deployment efficiency of large vision-language MoE models by pruning redundant visual tokens to enhance expert locality and prefetching, achieving significant speedups under memory constraints.


<details>
  <summary>Details</summary>
Motivation: Large-scale VL-MoE models have strong multimodal capabilities but face deployment challenges on memory-constrained platforms. Existing MoE offloading systems designed for text workloads are ineffective for visual-heavy inputs, where numerous visual tokens cause broad and unpredictable expert accesses.

Method: VisMMoE leverages the insight that pruning redundant visual tokens improves offloading by reducing computation and reshaping expert demand (visual-expert affinity). It combines affinity-aware token compression, lookahead expert prediction, and cache/pipeline orchestration to improve expert locality and prefetch effectiveness.

Result: VisMMoE improves end-to-end inference performance by up to 2.68x and 1.61x over strong baselines for current VL-MoE deployments while maintaining competitive accuracy. The system was implemented on multiple frameworks and evaluated on representative VL-MoE models and benchmarks.

Conclusion: Token pruning not only reduces computation but also fundamentally reshapes expert demand patterns in VL-MoE models, enabling efficient deployment through improved expert locality and predictable working sets. The visual-expert affinity insight provides a foundation for optimizing multimodal MoE systems on memory-constrained platforms.

Abstract: Large-scale vision-language mixture-of-experts (VL-MoE) models provide strong multimodal capability, but efficient deployment on memory-constrained platforms remains difficult. Existing MoE offloading systems are largely designed for text-centric workloads and become much less effective for visual-heavy inputs, where large numbers of visual tokens induce broader and less predictable expert accesses. We present VisMMoE, a VL-MoE offloading system built on a single systems insight: pruning redundant visual tokens can improve offloading not only by reducing computation, but also by reshaping expert demand. We refer to this effect as \textit{visual-expert affinity}: token pruning makes expert accesses more concentrated within layers and more stable across layers, producing a smaller and more predictable expert working set. Guided by this insight, VisMMoE combines affinity-aware token compression, lookahead expert prediction, and cache/pipeline orchestration to improve expert locality and prefetch effectiveness under tight memory budgets. We implement VisMMoE on multiple frameworks and evaluate it on representative VL-MoE models and benchmarks. VisMMoE improves end-to-end inference performance by up to 2.68x and 1.61x, respectively, over strong baselines for today's VL-MoE deployments while maintaining competitive accuracy.

</details>


### [229] [Quadratic Objective Perturbation: Curvature-Based Differential Privacy](https://arxiv.org/abs/2605.05905)
*Daniel Cortild,Coralia Cartis*

Main category: cs.LG

TL;DR: This paper introduces Quadratic Objective Perturbation (QOP) for differentially private empirical risk minimization, replacing traditional linear perturbation with random quadratic forms to achieve stronger privacy guarantees with weaker assumptions.


<details>
  <summary>Details</summary>
Motivation: The motivation is to overcome limitations of Linear Objective Perturbation (LOP) which requires the strong assumption of bounded gradients of the loss function. This assumption excludes many modern machine learning models, so the authors seek a more general privacy mechanism with weaker requirements.

Method: The method introduces Quadratic Objective Perturbation (QOP) which perturbs the objective function with a random quadratic form instead of a linear term. This perturbation induces strong convexity through curvature, enabling privacy through spectral properties of the perturbation rather than gradient bounds. The analysis extends to account for approximate solutions and inexact solves.

Result: The paper achieves (ε,δ)-differential privacy under weaker assumptions in the interpolation regime, shows that privacy guarantees are preserved under inexact solves, derives utility guarantees in terms of empirical excess risk, provides theoretical and numerical comparisons to LOP showing advantages of curvature-based perturbations, and demonstrates that resulting problems can be solved efficiently using modern splitting schemes.

Conclusion: Quadratic Objective Perturbation provides a more general and effective differentially private mechanism for empirical risk minimization that works under weaker assumptions than traditional linear perturbation methods, making it applicable to a broader range of modern machine learning models.

Abstract: Objective perturbation is a standard mechanism in differentially private empirical risk minimization. In particular, Linear Objective Perturbation (LOP) enforces privacy by adding a random linear term, while strong convexity and stability are ensured by an additional deterministic quadratic term. However, this approach requires the strong assumption of bounded gradients of the loss function, which excludes many modern machine learning models. In this work, we introduce Quadratic Objective Perturbation (QOP), which perturbs the objective with a random quadratic form. This perturbation induces strong convexity and enforces stability of the problem through curvature, thereby enabling privacy and allowing sensitivity to be controlled through spectral properties of the perturbation rather than assumptions on the gradients. As a result, we obtain $(\varepsilon, δ)$-differential privacy under weaker assumptions, in the interpolation regime. Furthermore, we extend the analysis to account for approximate solutions, showing that privacy guarantees are preserved under inexact solves. Additionally, we derive utility guarantees in terms of empirical excess risk, and provide a theoretical and numerical comparison to LOP, highlighting the advantages of curvature-based perturbations. Finally, we discuss algorithmic aspects and show that the resulting problems can be solved efficiently using modern splitting schemes.

</details>


### [230] [From Drops to Grid: Noise-Aware Spatio-Temporal Neural Process for Rainfall Estimation](https://arxiv.org/abs/2605.05912)
*Rafael Pablos Sarabia,Joachim Nyborg,Morten Birk,Ira Assent*

Main category: cs.LG

TL;DR: DropsToGrid: Neural Process-based method for generating high-resolution rainfall maps by fusing sparse station data with radar, handling noise and uncertainty.


<details>
  <summary>Details</summary>
Motivation: Traditional rainfall measurements are biased and low-resolution, limiting local rainfall capture. Existing deep learning methods struggle with rainfall's skewed, localized nature, noise, and limited spatio-temporal fusion.

Method: Neural Process-based approach that fuses temporal sequences from noisy, irregular weather stations with spatial context from radar. Uses multi-scale feature extraction, temporal attention, and multi-modal fusion to produce stochastic, continuous rainfall estimates with explicit uncertainty quantification.

Result: Outperforms both operational and deep learning baselines. Generates accurate high-resolution rainfall maps with well-calibrated uncertainty, even with few stations and in cross-regional scenarios.

Conclusion: DropsToGrid provides an effective solution for high-resolution rainfall mapping by addressing key challenges in spatio-temporal fusion, noise handling, and uncertainty quantification in rainfall data densification.

Abstract: High-resolution rainfall observations are crucial for weather forecasting, water management, and hazard mitigation. Traditional operational measurements are often biased and low-resolution, limiting their ability to capture local rainfall. Accurate high-resolution rainfall maps require integrating sparse surface observations, yet existing deep learning densification methods are hindered by rainfall's skewed, localized nature, noise, and limited spatio-temporal fusion. We present DropsToGrid, a Neural Process-based method that generates dense rainfall fields by fusing temporal sequences from noisy, irregularly distributed private weather stations with spatial context from radar. Leveraging multi-scale feature extraction, temporal attention, and multi-modal fusion, the model produces stochastic, continuous rainfall estimates and explicitly quantifies uncertainty. Evaluations on real-world datasets demonstrate that DropsToGrid outperforms both operational and deep learning baselines, generating accurate high-resolution rainfall maps with well-calibrated uncertainty, even when only few stations are available and in cross-regional scenarios.

</details>


### [231] [Near-Policy: Accelerating On-Policy Distillation via Asynchronous Generation and Selective Packing](https://arxiv.org/abs/2605.05940)
*Miao Rang,Zhenni Bi,Hang Zhou,Kai Han,Xuechun Wang,An Xiao,Xinghao Chen,Yunhe Wang,Hanting Chen*

Main category: cs.LG

TL;DR: NPD (Near-Policy Distillation) is an efficient asynchronous knowledge distillation method for autoregressive models that avoids computationally expensive RL frameworks while maintaining training stability through sparse updates and Δ-IFD filtering.


<details>
  <summary>Details</summary>
Motivation: Standard knowledge distillation suffers from distribution mismatch, while on-policy methods that mitigate this issue are computationally expensive due to their reliance on Reinforcement Learning frameworks. There's a need for more efficient distillation methods that maintain training stability.

Method: Proposes Near-Policy Distillation (NPD) - an asynchronous approach that decouples student generation from training, enabling Supervised Fine-Tuning with sequence packing. To address policy lag and sample noise from asynchronous updates, NPD integrates sparse student updates and the Δ-IFD filtering mechanism, which filters extreme out-of-distribution samples to keep updates within a safe proximal learning zone.

Result: Achieves 8.1x speedup over on-policy baselines and outperforms SFT by 8.09%. Enables openPangu-Embedded-1B to reach a state-of-the-art score of 68.73%, outperforming the larger Qwen3-1.7B model. By narrowing exploration space for subsequent RL, the method demonstrates superior efficiency and effectiveness.

Conclusion: NPD provides an efficient alternative to RL-based on-policy distillation methods, maintaining training stability through asynchronous updates with filtering mechanisms, and demonstrates significant performance improvements and speedups while enabling smaller models to outperform larger ones.

Abstract: Standard knowledge distillation for autoregressive models often suffers from distribution mismatch. While on-policy methods mitigate this by leveraging student-generated outputs, they rely on computationally expensive Reinforcement Learning (RL) frameworks. To improve efficiency, we propose Near-Policy Distillation (NPD), an asynchronous approach that decouples student generation from training. This reformulation enables Supervised Fine-Tuning (SFT) with sequence packing. However, asynchronous updates inevitably introduce policy lag and sample noise, which can cause the behavior to drift from near-policy toward off-policy. To counteract this without sacrificing efficiency, NPD integrates sparse student updates and the $Δ$-IFD filtering mechanism, a heuristic sample selection mechanism that empirically stabilizes the optimization trajectory. By filtering extreme out-of-distribution samples, $Δ$-IFD prevents noise from dominating the gradients, ensuring updates remain within a safe proximal learning zone. Empirically, the NPD framework achieves a 8.1x speedup over on-policy baselines and outperforms SFT by 8.09%. Crucially, by effectively narrowing the exploration space for subsequent RL, our method enables openPangu-Embedded-1B to reach a state-of-the-art score of 68.73%, outperforming the substantially larger Qwen3-1.7B. Codes will be released soon.

</details>


### [232] [Knowing but Not Correcting: Routine Task Requests Suppress Factual Correction in LLMs](https://arxiv.org/abs/2605.05957)
*Zixuan Chen,Hao Lin,Zizhe Chen,Yizhou Tian,Garry Yang,Depeng Wang,Ya Guo,Huijia Zhu,James Cheng*

Main category: cs.LG

TL;DR: LLMs often comply with false claims in task-oriented requests instead of correcting them, a phenomenon called "correction suppression." The paper introduces interventions that improve factual strictness without harming reasoning.


<details>
  <summary>Details</summary>
Motivation: LLMs reliably correct false claims when presented in isolation, but they often comply rather than correct when the same claims are embedded in task-oriented requests. This failure mode is called "correction suppression," but its prevalence and mechanisms are not well understood.

Method: Created a benchmark of 300 false premises to systematically evaluate correction suppression across eight models. Performed mechanistic analysis of attention patterns and developed two training-free interventions: Correction Direction Steering (CDS) which injects correction signals at middle layers, and Dynamic Payload Amplification (DPA) which amplifies payload tokens at the final layer using attention divergence analysis.

Result: Suppression rates ranged from 19% to 90%, with four models exceeding 80%, establishing correction suppression as prevalent. CDS improved correction rate on Qwen3.5-9B from 0% to 58.2%. DPA preserved or improved reasoning capability on both tested models. The mechanistic analysis showed suppression occurs at response selection rather than knowledge encoding.

Conclusion: The paper introduces "factual strictness" - the willingness to uphold accuracy against contextual pressures - as a new dimension of model reliability. The findings reveal LLMs often "know but don't correct" false claims in task contexts, and demonstrate effective interventions to improve this capability.

Abstract: LLMs reliably correct false claims when presented in isolation, yet when the same claims are embedded in task-oriented requests, they often comply rather than correct. We term this failure mode \emph{correction suppression} and construct a benchmark of 300 false premises to systematically evaluate it across eight models. Suppression rates range from 19\% to 90\%, with four models exceeding 80\%, establishing correction suppression as a prevalent and severe phenomenon. Mechanistic analysis reveals that suppression is not a knowledge failure: the model registers the error internally but task context diverts early-layer attention from the false claim as output intent crystallizes toward compliance at middle layers. We characterize this as \emph{knowing but not correcting} -- suppression occurs at response selection rather than knowledge encoding. Guided by this mechanism, we propose two training-free interventions. Correction Direction Steering (CDS) estimates a correction-compliance direction from matched pairs and injects it at middle layers before output intent crystallizes. Dynamic Payload Amplification (DPA) localizes payload tokens via attention divergence between early and late layers and amplifies their representation at the final layer, requiring no calibration data. Experiments on Qwen3.5-9B and LLaMA3.1-8B show both methods substantially improve factual strictness. CDS achieves the highest correction rate on Qwen3.5-9B (0\%$\to$58.2\%). DPA is the only method that preserves or improves reasoning capability on both models. These findings introduce \emph{factual strictness} -- the willingness to uphold accuracy against contextual pressures -- as a new dimension of model reliability.

</details>


### [233] [Uncertainty Estimation via Hyperspherical Confidence Mapping](https://arxiv.org/abs/2605.05964)
*Eunseo Choi,Ho-Yeon Kim,Jaewon Lee,Taeyong jo,Myungjun lee,Heejin Ahn*

Main category: cs.LG

TL;DR: HCM (Hyperspherical Confidence Mapping) is a sampling-free, distribution-free uncertainty estimation method that uses geometric constraints on a hypersphere to get deterministic, interpretable uncertainty estimates for neural networks.


<details>
  <summary>Details</summary>
Motivation: Existing uncertainty estimation approaches often require costly sampling or make restrictive distributional assumptions, which limits their practical application in high-stakes domains like autonomous driving and healthcare where efficient and reliable uncertainty quantification is crucial.

Method: HCM decomposes neural network outputs into magnitude and normalized direction vectors constrained to lie on a unit hypersphere. It interprets uncertainty as the degree of violation of this geometric constraint, providing a deterministic, sampling-free approach without distributional assumptions.

Result: Experiments across diverse benchmarks and real-world industrial tasks show HCM matches or surpasses ensemble and evidential approaches, with significantly lower inference cost and stronger confidence-error alignment.

Conclusion: HCM demonstrates the power of geometric structure in uncertainty estimation and serves as a versatile, efficient alternative to conventional uncertainty estimation techniques for neural networks.

Abstract: Quantifying uncertainty in neural network predictions is essential for high-stakes domains such as autonomous driving, healthcare, and manufacturing. While existing approaches often depend on costly sampling or restrictive distributional assumptions, we propose Hyperspherical Confidence Mapping (HCM), a simple yet principled framework for sampling-free and distribution-free uncertainty estimation. HCM decomposes outputs into a magnitude and a normalized direction vector constrained to lie on the unit hypersphere, enabling a novel interpretation of uncertainty as the degree of violation of this geometric constraint. This yields deterministic and interpretable estimates applicable to both regression and classification. Experiments across diverse benchmarks and real-world industrial tasks demonstrate that HCM matches or surpasses ensemble and evidential approaches, with far lower inference cost and stronger confidence-error alignment. Our results highlight the power of geometric structure in uncertainty estimation and position HCM as a versatile alternative to conventional techniques.

</details>


### [234] [Beyond Uniform Credit Assignment: Selective Eligibility Traces for RLVR](https://arxiv.org/abs/2605.05965)
*Chaoli Mou,Zhan Zhuang,Xinning Chen,Yu Zhang*

Main category: cs.LG

TL;DR: S-trace is a novel critic-free eligibility traces method that improves reasoning in LLMs by enabling selective credit assignment to critical tokens, outperforming GRPO with better efficiency.


<details>
  <summary>Details</summary>
Motivation: Current critic-free RLVR algorithms like GRPO suffer from uniform credit assignment that broadcasts trajectory-level advantages indiscriminately, failing to distinguish critical reasoning steps and hindering learning efficiency.

Method: Proposes Selective Eligibility Traces (S-trace) built on P-trace (partial trust region preservation). S-trace implements sparse eligibility traces to mitigate variance and achieve fine-grained credit assignment by selectively masking low-entropy tokens, enabling better identification of critical reasoning steps.

Result: S-trace outperforms GRPO with gains of 0.49% on Qwen3-1.7B, 3.16% on Qwen3-4B, and maintains 2.98% improvement on Qwen3-8B in average pass@16, while achieving higher sample and token efficiency simultaneously.

Conclusion: S-trace effectively addresses the limitations of uniform credit assignment in critic-free RLVR, demonstrating superior performance and efficiency through selective eligibility traces that better capture critical reasoning components in LLMs.

Abstract: Reinforcement Learning with Verifiable Rewards (RLVR) has become a key approach for improving the reasoning abilities of large language models. However, widely used critic-free algorithms such as Group Relative Policy Optimization (GRPO) necessitate a ``uniform credit assignment'' assumption that indiscriminately broadcast trajectory-level advantages, hindering learning efficiency by failing to distinguish critical reasoning steps. To address this limitation, we propose Selective Eligibility Traces (S-trace). Grounded in the intuition of partial trust region preservation, we initially introduce P-trace as a sample-efficient, critic-free eligibility traces method, upon which we build S-trace, implementing a sparse eligibility traces mechanism to further mitigate variance and achieve fine-grained credit assignment by selectively masking low-entropy tokens. Theoretically, we contextualize the recent Group Sequence Policy Optimization (GSPO) method within the critic-free eligibility traces framework, identifying it as a special instance of the eligibility traces method operating under uniform credit assignment. Experiments demonstrate that S-trace not only outperforms GRPO, showing gains of 0.49\% on Qwen3-1.7B and 3.16\% on Qwen3-4B, and maintaining a robust 2.98\% improvement when scaled further to Qwen3-8B in average pass@16, but notably achieves this with simultaneously higher sample and token efficiency.

</details>


### [235] [Sharper Guarantees for Misspecified Kernelized Bandit Optimization](https://arxiv.org/abs/2605.05967)
*Davide Maran,Csaba Szepesvári*

Main category: cs.LG

TL;DR: This paper reduces misspecification penalties in kernelized bandit optimization from polynomial to logarithmic/polylogarithmic growth for certain kernels by leveraging localization principles.


<details>
  <summary>Details</summary>
Motivation: Existing kernelized bandit optimization approaches suffer from significant penalties when models are misspecified (i.e., when the true function doesn't lie in the reproducing kernel Hilbert space). Current methods multiply misspecification error by kernel complexity measures like effective dimension or maximum information gain, leading to large performance degradation.

Method: The authors introduce localization principles to control misspecification amplification. In offline settings, they use spectral Lebesgue constants to bound simple regret. In online settings, they modify a domain-splitting algorithm to prevent local misspecification errors from propagating globally.

Result: For offline optimization, they achieve logarithmic amplification for one-dimensional monotone spectra and polylogarithmic amplification for multivariate Fourier-diagonal product kernels. For online optimization, they prove cumulative regret bounds of $\widetilde{\mathcal O}(\sqrt{γ_n n}+n\varepsilon)$, removing the extra $\sqrt{γ_n}$ factor from misspecification term compared to prior work.

Conclusion: Localization techniques significantly improve misspecification tolerance in kernelized bandits by controlling error amplification, transforming polynomial growth penalties to logarithmic/polylogarithmic ones for important kernel classes, making these methods more robust to model mismatch.

Abstract: Existing guarantees for misspecified kernelized bandit optimization pay for misspecification through kernel complexity: in generic offline bounds, the misspecification level $\varepsilon$ is multiplied by $\sqrt{d_\mathrm{eff}}$, where $d_\mathrm{eff}$ is the kernel effective dimension, while in online regret bounds, the corresponding penalty is $\sqrt{γ_n}\,n\varepsilon$, where $γ_n$ is the maximum information gain after $n$ rounds of interaction.
  In this work, we show that, for a large class of kernels, the misspecification amplification can be reduced to logarithmic or polylogarithmic growth. In the offline setting, we first prove high-probability simple-regret bounds whose misspecification term is governed by a spectral Lebesgue constant. This yields logarithmic amplification for one-dimensional monotone spectra and polylogarithmic amplification for multivariate Fourier-diagonal product kernels. In the online setting, we modify a domain-splitting algorithm and prove a cumulative regret bound of $\widetilde{\mathcal O}(\sqrt{γ_n n}+n\varepsilon)$ under mild localized eigendecay assumptions, removing the extra $\sqrt{γ_n}$ factor from the misspecification term. The common principle is localization: spectral localization controls the Lebesgue constant of the offline approximation operator, while domain splitting implements the spatial analogue of this mechanism in the online setting, preventing local misspecification errors from being amplified globally.

</details>


### [236] [Training Transformers for KV Cache Compressibility](https://arxiv.org/abs/2605.05971)
*Yoav Gelberg,Yam Eitan,Michael Bronstein,Yarin Gal,Haggai Maron*

Main category: cs.LG

TL;DR: KV-CAT: A training method that makes language models inherently more compressible by teaching them to use KV cache more efficiently during continued pretraining.


<details>
  <summary>Details</summary>
Motivation: Long-context language models suffer from KV cache bottlenecks - memory and decode-time access costs scale linearly with context length. Current post-hoc compression methods are limited by the inherent compressibility of the model's learned representations.

Method: KV-CAT: A continued pretraining procedure that uses train-time KV sparsification to mask KV slots during training, forcing the model to use fewer slots and learn more compressible representations.

Result: KV-CAT improves the quality-budget tradeoff of downstream compression methods across retrieval, long-context QA, and perplexity-based evaluation of compressed-prefix continuation.

Conclusion: KV compressibility is a property of learned representations, not just the context. Training transformers to be inherently more compressible (via KV-CAT) enables better practical compression for long-context modeling.

Abstract: Long-context language modeling is increasingly constrained by the Key-Value (KV) cache, whose memory and decode-time access costs scale linearly with the prefix length. This bottleneck has motivated a range of context-compression methods, from token-level summarization to recent optimization-based KV compression methods. These post-hoc methods operate on the KV cache of a fixed pretrained model, so their effectiveness is fundamentally limited by how well the model's internal representations can be compressed. In this work, we formalize the notion of KV compressibility and show that it is a property of the learned representations, rather than of the context alone. We prove that almost any sequence-to-vector function admits both highly compressible and inherently non-compressible transformer implementations, highlighting the need to guide transformers toward compressible representations during training. Motivated by this, we propose KV-Compression Aware Training (KV-CAT), a continued pretraining procedure that incentivizes the emergence of compressible representations. We introduce a train-time KV sparsification policy that masks KV slots during training. This forces the model to use fewer KV slots and encourages it to learn representations amenable to post-hoc compression. Empirically, we show that KV-CAT improves the quality-budget tradeoff of downstream compression methods across retrieval, long-context question answering, and perplexity-based evaluation of compressed-prefix continuation.

</details>


### [237] [Physical Fidelity Reconstruction via Improved Consistency-Distilled Flow Matching for Dynamical Systems](https://arxiv.org/abs/2605.05975)
*Sicheng Ma,Tianyue Yang,Xiuzhe Wu,Xiao Xue*

Main category: cs.LG

TL;DR: One-step consistency model distillation from flow-matching teachers enables fast, efficient scientific flow reconstruction with 12× speedup and 23.1% better SSIM than scratch training.


<details>
  <summary>Details</summary>
Motivation: Current diffusion and flow-matching models for scientific flow reconstruction rely on iterative sampling, making them too slow for latency-sensitive applications like ensemble forecasting, real-time visualization, and simulation-in-the-loop inference. There's a need for faster, more compact models without sacrificing reconstruction quality.

Method: Distills an optimal-transport flow-matching teacher model into a compact one-step consistency model. Low-fidelity observations are incorporated at inference by initializing the generative trajectory from a noised observation along the transport path, enabling conditional reconstruction without retraining the teacher.

Result: The distilled student achieves 12× inference speedup while retaining similar spectral performance to the teacher, using half the parameters. Under same training budget, it outperforms one-step consistency models trained from scratch by 23.1% in SSIM across three fluid benchmarks (Smoke Buoyancy, Turbulent Channel Flow, Kolmogorov Flow) up to 256×256 resolution.

Conclusion: Teacher distillation provides a promising path for converting high-capacity scientific generative models into compact, fast, deployable reconstruction models that improve both training efficiency and inference speed, rather than just accelerating sampling.

Abstract: Reconstructing high-fidelity flow fields from low-fidelity observations is a central problem in scientific machine learning, yet recent diffusion and flow-matching models typically rely on iterative sampling, making them costly for latency-sensitive workflows such as ensemble forecasting, real-time visualization, and simulation-in-the-loop inference. We study whether a high-fidelity flow-matching generative model can be compressed into a compact one-step model for fast scientific flow reconstruction. Our approach distills an optimal-transport flow-matching teacher into a one-step consistency model. Low-fidelity observations are incorporated at inference by initializing the generative trajectory from a noised observation along the transport path, allowing an unconditional high-fidelity flow model to perform conditional reconstruction without retraining the teacher. We evaluate this distillation strategy on three fluid benchmarks, Smoke Buoyancy, Turbulent Channel Flow, and Kolmogorov Flow, using coarse-to-fine reconstruction as a controlled testbed at field sizes up to $256 \times 256$. Across these settings, the distilled student retains similar performance of the teacher's model on spectrum metrics, while using roughly half as many parameters and achieving a $12\times$ inference speedup over the flow-matching teacher. Under the same training budget, the distilled student also outperforms a one-step consistency model trained directly from scratch by $23.1\%$ in SSIM, showing that teacher distillation improves training efficiency rather than merely accelerating sampling. These results suggest a promising route for turning future high-capacity scientific generative models into compact reconstruction models that are faster to train, cheaper to run, and easier to deploy.

</details>


### [238] [Towards Steering without Sacrifice: Principled Training of Steering Vectors for Prompt-only Interventions](https://arxiv.org/abs/2605.05983)
*Yuntai Bao,Qinfeng Li,Xinyan Yu,Xuhong Zhang,Ge Su,Wenqi Zhang,Liu Yan,Haiqin Weng,Jianwei Yin*

Main category: cs.LG

TL;DR: The paper introduces Prompt-only Steering Vectors (PrOSV) with joint training of steering factors and directions, which only intervene on a few prompt tokens instead of the full sequence, achieving better steering effectiveness without compromising generation quality.


<details>
  <summary>Details</summary>
Motivation: Current steering vector approaches have two main limitations: (1) they require careful per-SV selection of steering factors to balance effectiveness and quality, and (2) full-sequence SVs sacrifice generation quality due to excessive intervention throughout the generation process.

Method: Proposes joint training of steering factors and directions to eliminate post-hoc factor selection, with insights from neural network scaling theory about initialization sizes and learning rates. Introduces Prompt-only SV (PrOSV) that intervenes only on a few prompt tokens instead of the full sequence.

Result: PrOSV outperforms traditional full-sequence SVs on AxBench when using the joint training scheme. It achieves a better tradeoff between general model utility and adversarial robustness compared to full-sequence SVs.

Conclusion: The proposed PrOSV with joint training addresses both limitations of current steering vector approaches by eliminating the need for factor selection and reducing excessive intervention, resulting in improved steering effectiveness without sacrificing generation quality.

Abstract: Recently, steering vectors (SVs) have emerged as an effective and lightweight approach to steer behaviors of large language models (LLMs), among which fine-tuned SVs are more effective than optimization-free ones. However, current approaches to fine-tuned SVs suffer from two limitations. First, they require careful selection of steering factors on a per-SV basis to balance steering effectiveness and generation quality at inference time. Second, they operate as full-sequence SVs (FSSVs), which can sacrifice generation quality regardless of factor selection due to excessive intervention on the model generation process. To address the first limitation, we propose joint training of steering factors and directions, such that post-hoc factor selection is no longer required. Using neural network scaling theory, we find that moderately large initialization sizes and learning rates for steering factors are essential for stability and efficiency of joint training. To tackle the second limitation, we draw inspiration from representation fine-tuning and introduce Prompt-only SV (PrOSV), an SV that intervenes only on a few prompt tokens. Our empirical results show that PrOSV outperforms traditional FSSVs on AxBench when using our joint training scheme. We also find that PrOSV achieves a better tradeoff between general model utility and adversarial robustness than FSSV.

</details>


### [239] [DiBA: Diagonal and Binary Matrix Approximation for Neural Network Weight Compression](https://arxiv.org/abs/2605.05994)
*Nobutaka Ono*

Main category: cs.LG

TL;DR: DiBA proposes a compact matrix factorization using diagonal and binary matrices for neural network weight compression, reducing multiplication operations while maintaining accuracy.


<details>
  <summary>Details</summary>
Motivation: Many modern neural network components (linear layers, convolutions, attention projections, embedding layers) have dense matrix weights that require significant computational resources and storage. There's a need for efficient compression techniques that reduce both storage and computation while maintaining performance.

Method: DiBA approximates dense matrices A as D₁B₁D₂B₂D₃, where Dᵢ are diagonal matrices and Bᵢ are 0/1 binary matrices. The method includes DiBA-Greedy optimization algorithm with alternating solver using closed-form least-squares for diagonals and exact one-bit improvement tests for binary factors, plus DiBARD for adaptation that freezes binary matrices and retunes only diagonal entries on downstream data.

Result: DiBA reduces floating-point multiplication count from mn to m+k+n for matrix-vector products. On 40 dense weight matrices from pretrained models, DiBA-Greedy shows consistent SNR improvements with increasing storage ratio. DiBARD improves DistilBERT/WikiText masked-token accuracy from 0.4447 to 0.5210 and Speech Commands test accuracy for Audio Spectrogram Transformer from 0.7684 to 0.9781 without reoptimizing binary factors.

Conclusion: DiBA provides an effective weight compression technique that decomposes dense matrix multiplication into more efficient operations while maintaining accuracy, with DiBARD offering practical adaptation benefits by only retuning diagonal factors on downstream tasks.

Abstract: In this paper, we propose DiBA (Diagonal and Binary Matrix Approximation), a compact matrix factorization for neural network weight compression. Many components of modern networks, including linear layers, $1\times1$ convolutions, attention projections, and embedding layers, have dense matrix weights. DiBA approximates $A\in\mathbb{R}^{m\times n}$ by $\widehat A=D_1B_1D_2B_2D_3$, where $D_1,D_2,D_3$ are diagonal matrices and $B_1,B_2$ are $0/1$ binary matrices. The intermediate dimension $k$ controls the trade-off between theoretical storage and approximation accuracy. For matrix-vector products, DiBA decomposes dense multiplication into three element-wise scaling operations and two binary mixing operations, reducing the floating-point multiplication count from $mn$ to $m+k+n$. For optimization, we introduce DiBA-Greedy, an alternating solver that combines closed-form least-squares updates for the diagonal factors with exact one-bit improvement tests for the binary factors. We also introduce DiBARD (DiBA with Retuning only Diagonal factors), which replaces dense-matrix layers by DiBA factors, freezes the binary matrices, and retunes only the diagonal entries on downstream data. This preserves compact binary mixing without discrete search during adaptation. On 40 dense weight matrices extracted from public pretrained models, DiBA-Greedy yields consistent SNR improvements as the theoretical storage ratio increases. After DiBA replacement in two component-replacement studies, DiBARD improves DistilBERT/WikiText masked-token accuracy from 0.4447 to 0.5210 and Speech Commands test accuracy for an Audio Spectrogram Transformer from 0.7684 to 0.9781 without reoptimizing the binary factors.

</details>


### [240] [A Fine-Grained Understanding of Uniform Convergence for Halfspaces](https://arxiv.org/abs/2605.06004)
*Aryeh Kontorovich,Kasper Green Larsen*

Main category: cs.LG

TL;DR: Halfspaces exhibit sharp threshold behavior: inhomogeneous halfspaces match worst-case VC bounds, while 2D homogeneous halfspaces show significantly better convergence with O(1/n) error in realizable case and near-log-free deviation bounds in agnostic case.


<details>
  <summary>Details</summary>
Motivation: To understand fine-grained uniform convergence behavior of halfspaces beyond worst-case VC dimension bounds, and to reveal dimensional and structural thresholds that differentiate homogeneous vs. inhomogeneous halfspaces in different dimensions.

Method: Analyze inhomogeneous and homogeneous halfspaces separately. For homogeneous halfspaces in ℝ², use critical-wedge localization argument to prove bandwise, log-free deviation bounds on dyadic risk bands, then union over bands with lnlnn overhead.

Result: Inhomogeneous halfspaces achieve worst-case bounds (Θ(dln(n/d)/n) in realizable case, √(τln(1/τ)) deviation in agnostic case). 2D homogeneous halfspaces: O(1/n) error in realizable case, lnlnn overhead unavoidable in agnostic case via bandwise analysis.

Conclusion: Halfspaces exhibit sharp dimensional and structural thresholds: inhomogeneous halfspaces match VC bounds, while 2D homogeneous halfspaces show dramatically better convergence with near-log-free behavior, completing fine-grained picture of uniform convergence.

Abstract: We study the fine-grained uniform convergence behavior of halfspaces beyond worst-case VC bounds. For inhomogeneous halfspaces in $\mathbb{R}^d$ with $d\ge 2$, we show that standard first-order VC bounds are essentially tight: even consistent hypotheses can incur population error $Θ(d\ln(n/d)/n)$, and in the agnostic setting the deviation scales as $\sqrt{τ\ln(1/τ)}$ at true error $τ$. In contrast, homogeneous halfspaces in $\mathbb{R}^2$ exhibit a markedly different behavior. In the realizable case, every hypothesis consistent with the sample has error $O(1/n)$. In the agnostic case, we prove a bandwise, log-free deviation bound on each dyadic risk band via a critical-wedge localization argument. Unioning over bands incurs only a $\ln\ln n$ overhead, and we establish a matching lower bound showing this overhead is unavoidable. Together, these results give a fine-grained and nearly complete picture of uniform convergence for halfspaces, revealing sharp dimensional and structural thresholds.

</details>


### [241] [Quantizing With Randomized Hadamard Transforms: Efficient Heuristic Now Proven](https://arxiv.org/abs/2605.06014)
*Ran Ben-Basat,William Kuszmaul,Michael Mitzenmacher,Amit Portnoy,Shay Vargaftik*

Main category: cs.LG

TL;DR: Two randomized Hadamard transforms (RHTs) make individual coordinates Gaussian-like, matching URR performance for most compression schemes, but three RHTs are needed for vector quantization to ensure coordinate independence, with a runtime check to adapt the number of RHTs.


<details>
  <summary>Details</summary>
Motivation: Uniform random rotations (URRs) are widely used in quantization but often replaced by faster randomized Hadamard transforms (RHTs). However, RHTs may not perform well for worst-case inputs, creating a need to understand how many RHTs are needed to match URR performance for different compression schemes.

Method: Theoretical analysis of coordinate distributions after RHT compositions: 1) Show two RHTs make individual coordinates near-Gaussian in Kolmogorov and Wasserstein distances, 2) Prove three RHTs ensure coordinate covariance decay for vector quantization, 3) Propose linear-time moment check to adapt number of RHTs at runtime.

Result: Two RHTs asymptotically match URR performance for DRIVE and QUIC-FL compression schemes, while three RHTs are required for vector quantization to achieve the same expected error. A practical runtime check adapts RHT count based on input moments.

Conclusion: The number of RHTs needed depends on the application: two suffice for schemes relying on coordinate-wise Gaussianity, but three are necessary for vector quantization requiring coordinate independence, with adaptive runtime selection providing practical benefits.

Abstract: Uniform random rotations (URRs) are a common preprocessing step in modern quantization approaches used for gradient compression, inference acceleration, KV-cache compression, model weight quantization, and approximate nearest-neighbor search in vector databases. In practice, URRs are often replaced by randomized Hadamard transforms (RHTs), which preserve orthogonality while admitting fast implementations. The remaining issue is the performance for worst-case inputs. With a URR, each coordinate is individually distributed as a shifted beta distribution, which converges to a Gaussian distribution in high dimensions. Generally, one RHT is not suitable in the worst case, as individual coordinates can be far from these distributions. We show that after composing two RHTs on any $d$-sized input vector, the marginal distribution of every fixed coordinate of the normalized rotated vector is within $O(d^{-1/2})$ of a standard Gaussian both in Kolmogorov distance and in $1$-Wasserstein distance. We then plug these bounds into the analyses of modern compression schemes, namely DRIVE and QUIC-FL, and show that two RHTs achieve performance that asymptotically matches URRs.
  However, we show that two RHTs may not be sufficient for Vector Quantization (VQ), which often requires weak correlation across fixed-size blocks of coordinates (as opposed to only marginal distribution convergence for single coordinates). We prove that a composition of three RHTs leads to decaying coordinate covariance. This ensures that any fixed, bounded, multi-dimensional VQ codebook optimized for URRs has the same expected error when using three RHTs, up to an additive term that vanishes with the dimension.
  Finally, because practical inputs are rarely adversarial, we propose a linear-time ${O}(d)$ check on the input's moments to dynamically adapt the number of RHTs used at runtime to improve performance.

</details>


### [242] [Matrix-Decoupled Concentration for Autoregressive Sequences: Dimension-Free Guarantees for Sparse Long-Context Rewards](https://arxiv.org/abs/2605.06017)
*Pei-Sen Li*

Main category: cs.LG

TL;DR: The paper introduces a Matrix-Decoupled Concentration (MDC) framework to establish sharp concentration bounds for dependent sequences in autoregressive LLMs, overcoming limitations of existing methods by preserving sparsity and causal structure.


<details>
  <summary>Details</summary>
Motivation: Current concentration bounds for autoregressive LLMs face two bottlenecks: (1) classical inequalities separate dependency structures from sensitivities, leading to suboptimal O(N) variance for sparse rewards, and (2) spatial methods achieve tighter bounds but lack the causal filtration required for sequential generation.

Method: Proposes a sharp McDiarmid-type inequality for dependent sequences governed by matrix-vector multiplication of causal dependency resolvent and target sensitivity vector. The MDC framework preserves coordinate-wise sparsity within a strictly causal framework, preventing "scalar collapse."

Result: MDC achieves optimal constants for Markov chains and order-optimal bounds for causal trees. Crucially, it guarantees a dimension-free O(1) variance proxy for sparse terminal rewards, providing rigorous justification for stability of long-context reasoning in LLMs.

Conclusion: The MDC framework resolves fundamental bottlenecks in concentration bounds for autoregressive LLMs by combining mathematical rigor with practical applicability, offering a theoretically sound foundation for stable long-context reasoning in language models.

Abstract: Sequence-level evaluations in autoregressive Large Language Models (LLMs) rely on highly dependent token generation. Establishing tight concentration bounds for these processes remains a challenge due to two fundamental bottlenecks in existing frameworks: (i) classical inequalities typically separate dependency structures from target sensitivities, leading to a scalar collapse that inflates the variance proxy to a suboptimal $\mathcal{O}(N)$ for sparse terminal rewards; (ii) conversely, while certain spatial methods achieve tighter bounds, they lack the strictly causal filtration required by sequential generation, rendering them inapplicable to the autoregressive setting. To resolve both bottlenecks, we establish a sharp McDiarmid-type inequality for dependent sequences, governed strictly by the exact matrix-vector multiplication of the causal dependency resolvent and the target sensitivity vector. This Matrix-Decoupled Concentration (MDC) framework natively recovers optimal constants for Markov chains and exploits directed $d$-separation to yield order-optimal bounds for causal trees. Crucially, by exactly preserving the coordinate-wise sparsity of rewards within a strictly causal framework, MDC mathematically prevents scalar collapse, guaranteeing a dimension-free $\mathcal{O}(1)$ variance proxy and providing a rigorous mathematical justification for the stability of long-context reasoning.

</details>


### [243] [Multi-agent decision making: A Blackwell's informativeness approach](https://arxiv.org/abs/2605.06028)
*Zheng Zhang,Cuong C. Nguyen,Kevin Wells,Gustavo Carneiro*

Main category: cs.LG

TL;DR: A principled framework using Blackwell's informativeness theory shows voting and debate in multi-LLM systems are no more informative than pooled private information, leading to a practical posterior estimation method that outperforms existing approaches.


<details>
  <summary>Details</summary>
Motivation: The rapid development of large language models has driven research on multi-agent decision-making, but existing aggregation methods like voting and debate lack formal guarantees about the informativeness of resulting decisions.

Method: Using Blackwell's informativeness framework to analyze multi-LLM decisions theoretically, then developing a practical method that estimates each agent's posterior and approximates the pooled posterior using a product-of-posteriors estimator.

Result: Theoretical analysis shows voting and debate induce information structures no more informative than pooled private information. Experimental results on six QA benchmarks demonstrate the proposed approach outperforms state-of-the-art multi-LLM debate and voting methods.

Conclusion: The paper provides a principled information-theoretic foundation for multi-LLM decision-making, establishing Bayesian pooled posterior maximization as an upper-bound decision rule and offering a practical implementation that improves performance over existing methods.

Abstract: The rapid development of large language models (LLMs) has motivated research on decision-making in multi-agent systems, where multiple agents collaborate to achieve shared objectives. Existing aggregation approaches, such as voting and debate, are largely ad-hoc and lack formal guarantees regarding the informativeness of the resulting decisions. In this paper, we provide a principled approach to analyse decisions made in the multi-LLM setting using Blackwell's informativeness framework. Within the Blackwell information-structure abstraction, we show that voting and debate induce information structures that are no more informative than the pooled private information of all agents. This result identifies Bayesian pooled posterior maximisation as an information-theoretic upper-bound decision rule under the Blackwell ordering. Motivated by this theoretical analysis, we introduce a practical method for LLM-based question-answering (QA) tasks that estimates each agent's posterior and approximates the pooled posterior using a product-of-posteriors estimator. Extensive experiments on six QA benchmarks demonstrate that our approach outperforms state-of-the-art multi-LLM debate and voting methods.

</details>


### [244] [Does Synthetic Data Help? Empirical Evidence from Deep Learning Time Series Forecasters](https://arxiv.org/abs/2605.06032)
*Hugo Cazaux,Eyjólfur Ingi Ásgeirsson,Hlynur Stefánsson*

Main category: cs.LG

TL;DR: Synthetic data in time series forecasting helps channel-mixing architectures but harms channel-independent ones, with best gains in low-resource settings when using seasonal-trend generators and gradual schedules.


<details>
  <summary>Details</summary>
Motivation: Synthetic data has transformed language model training but its role in time series forecasting remains poorly understood, necessitating systematic evaluation to provide actionable guidelines.

Method: Large-scale empirical study with 4,218 runs across 9 experiment groups, evaluating synthetic time series augmentation across 5 architectures, 4 synthetic signal generators, and 7 datasets with systematic testing.

Result: Effect is sharply architecture-conditional: channel-mixing models (TimesNet, iTransformer) benefit in majority of trials while channel-independent models (DLinear, PatchTST) are consistently degraded. Low-resource gains are striking (TimesNet trained on 10% Weather data with augmentation surpasses full-data baseline). Averaged across architectures, augmentation hurts in 67% of trials. Only Seasonal-Trend generator reliably helps across benchmarks.

Conclusion: Provide concrete guidelines: use synthetic augmentation with channel-mixing architectures, employ gradual annealing schedules, and treat low-resource augmentation as architecture- and dataset-dependent.

Abstract: Synthetic data has transformed language model training, yet its role in time series forecasting remains poorly understood. We present a large-scale empirical study: nine experiment groups, 4,218 runs systematically evaluating synthetic time series augmentation across five architectures, four synthetic signals and seven datasets. The effect is sharply architecture-conditional: channel-mixing models (TimesNet, iTransformer) benefit in the majority of trials, while channel-independent models (DLinear, PatchTST) are consistently degraded. In selected low-resource settings the gains are striking: TimesNet trained on only 10\% of Weather data with synthetic augmentation surpasses the full-data baseline (4 of 16 sparsity-dataset combinations). Averaged across all architectures, augmentation hurts in 67\% of trials. We further find that only the Seasonal-Trend generator reliably helps across the tested benchmarks, and that hard curriculum switching is actively harmful (+24\% MSE degradation). These results provide concrete, actionable guidelines on how to use synthetic data: use synthetic augmentation with channel-mixing architectures, use gradual annealing schedules, and treat low-resource augmentation as architecture- and dataset-dependent. Code is available at \href{https://github.com/hugoiscracked/synthetic-ts/tree/main}

</details>


### [245] [Optimal Transport for LLM Reward Modeling from Noisy Preference](https://arxiv.org/abs/2605.06036)
*Licheng Pan,Haochen Yang,Haoxuan Li,Yunsheng Lu,Yongqi Tong,Yinuo Wang,Shijian Wang,Zhixuan Chu,Lei Shen,Yuan Lu,Hao Wang*

Main category: cs.LG

TL;DR: SelectiveRM is an optimal transport-based framework for robust reward model training in RLHF that handles noisy preference labels by aligning model predictions with data distribution and excluding contradictory samples via partial transport.


<details>
  <summary>Details</summary>
Motivation: Real-world datasets for RLHF contain inevitable noisy preferences, and conventional training objectives overfit these errors. Existing denoising approaches rely on homogeneous noise assumptions that fail to capture the complexity of linguistic preferences, creating a need for more robust methods.

Method: Proposes SelectiveRM framework grounded in optimal transport: (1) Devises Joint Consistency Discrepancy to align distribution of model predictions with preference data, (2) Incorporates Mass Relaxation mechanism via partial transport to enable autonomous exclusion of samples with noisy preferences that contradict semantic consistency.

Result: Extensive experiments validate that SelectiveRM significantly outperforms state-of-the-art baselines across diverse benchmarks. Theoretically, the approach optimizes a tighter upper bound on the unobserved clean risk.

Conclusion: SelectiveRM provides an effective framework for robust reward model training in RLHF by addressing noisy preference labels through optimal transport theory, enabling better handling of complex linguistic preferences while avoiding overfitting to corrupted data.

Abstract: Reward models are fundamental to Reinforcement Learning from Human Feedback (RLHF), yet real-world datasets are inevitably corrupted by noisy preference. Conventional training objectives tend to overfit these errors, while existing denoising approaches often rely on homogeneous noise assumptions that fail to capture the complexity of linguistic preferences. To handle these challenges, we propose SelectiveRM, a framework grounded in optimal transport. We first devise a Joint Consistency Discrepancy to align the distribution of model predictions with preference data. Furthermore, to address the limitation of strict mass conservation which compels the model to fit outliers, we incorporate a Mass Relaxation mechanism via partial transport. This enables the autonomous exclusion of samples with noisy preference that contradict semantic consistency. Theoretically, we demonstrate that SelectiveRM optimizes a tighter upper bound on the unobserved clean risk. Extensive experiments validate that our approach significantly outperforms state-of-the-art baselines across diverse benchmarks.

</details>


### [246] [Requests of a Feather Must Flock Together: Batch Size vs. Prefix Homogeneity in LLM Inference](https://arxiv.org/abs/2605.06046)
*Saksham Rathi,Preeti,Mythili Vutukuru*

Main category: cs.LG

TL;DR: Feather introduces a reinforcement learning-based scheduler with a Chunked Hash Tree data structure that optimizes batch formation for LLM inference by balancing batch size and prefix homogeneity, achieving 2-10× throughput improvement over existing schedulers.


<details>
  <summary>Details</summary>
Motivation: Current auto-regressive token generation in LLMs is memory-bound due to KV cache accesses. Existing schedulers focus on maximizing batch size but don't optimize for prefix-homogeneous batches, which could provide better spatial/temporal locality. Also, existing prefix detection methods using radix-tree traversals incur high CPU overhead comparable to GPU execution time.

Method: Feather uses reinforcement learning to learn optimal tradeoffs between batch size and prefix homogeneity. It introduces Chunked Hash Tree (CHT), a lightweight data structure for fast prefix detection and efficient request selection, avoiding expensive tree traversals. The system is integrated into vLLM and SGLang.

Result: Feather achieves 2-10× higher end-to-end throughput compared to existing schedulers. It reduces total KV cache accesses, outperforming prefix-aware attention kernels. It maintains at least equal performance to existing systems when workloads lack sufficient prefix sharing.

Conclusion: By optimizing batch formation through RL and efficient prefix detection with CHT, Feather significantly improves LLM inference efficiency for prefix-sharing workloads while maintaining performance for heterogeneous workloads, demonstrating the importance of intelligent scheduling beyond just batch size maximization.

Abstract: Auto-regressive token generation in large language models is memory-bound because it requires "attending to" key and value tensors (KV cache) of all previous tokens. Prior work aims to improve the efficiency of this decode process by batching multiple requests together, and maximizing batch size subject to GPU memory constraints. The key observation of our work is that with prefix-sharing workloads, smaller, prefix-homogeneous batches -- where all requests share a common prefix -- can achieve higher decode throughput than larger, heterogeneous batches, due to better spatial and temporal locality during KV cache accesses. However, prefix-aware schedulers in state-of-the-art inference engines maximize prefix reuse within a batch only to reduce KV cache memory footprint, but do not stop batch formation at smaller homogeneous batches that could have performed better. Further, we show that shared prefix detection in existing schedulers relies on radix-tree traversals, incurring substantial CPU overhead that is often comparable to GPU execution time. This paper presents Feather, a prefix-aware scheduler that uses reinforcement learning (RL) to learn the optimal tradeoff between batch size and prefix homogeneity. We also introduce Chunked Hash Tree (CHT), a lightweight data structure that enables fast prefix detection and efficient request selection for the RL scheduler, avoiding expensive tree traversals. We integrate Feather into vLLM and SGLang, and our evaluation shows that Feather achieves 2--10$\times$ higher end-to-end throughput as compared to existing schedulers, while doing no worse than the status quo when the workload does not have enough prefix sharing. Feather achieves these gains by reducing the total number of KV cache accesses, surpassing the performance of prefix-aware attention kernels that have the same goal.

</details>


### [247] [TFM-Retouche: A Lightweight Input-Space Adapter for Tabular Foundation Models](https://arxiv.org/abs/2605.06047)
*Duong Nguyen,Mohammed Jawhar,Nicolas Chesneau*

Main category: cs.LG

TL;DR: TFM-Retouche is an architecture-agnostic input-space residual adapter that improves pretrained tabular foundation models with lightweight tuning, achieving state-of-the-art performance on TabArena-Lite.


<details>
  <summary>Details</summary>
Motivation: Existing tabular foundation models have fixed inductive biases at inference, and adapting them requires either expensive full fine-tuning or architecture-specific PEFT methods, with unclear benefits for accuracy and calibration.

Method: TFM-Retouche learns small residual corrections in the input space to align data with the frozen TFM's inductive biases, trained end-to-end through the frozen backbone with a post-training identity guard for safe fallback.

Result: TabICLv2-Retouche achieves top rank on TabArena-Lite leaderboard with +56 Elo improvement over frozen TabICLv2, while being Pareto-optimal for predictive quality vs. training/inference time.

Conclusion: TFM-Retouche provides an effective, lightweight, and architecture-agnostic approach to adapt pretrained tabular foundation models, outperforming existing methods while maintaining computational efficiency.

Abstract: Tabular foundation models (TFMs), such as TabPFN-2.6, TabICLv2, ConTextTab, Mitra, LimiX, and TabDPT, achieve strong zero-shot performance through in-context learning, but their inductive biases remain fixed at inference time. Adapting a pretrained TFM to a specific dataset or task typically requires either full fine-tuning, which is computationally expensive, or parameter-efficient tuning methods (PEFT) such as LoRA, which must be tailored to the internal architecture of each TFM. Furthermore, the evidence on whether weight-space fine-tuning improves accuracy or calibration is mixed \citep{tanna_exploring_2026,rubachev_finetuning_2025}. We introduce TFM-Retouche, a lightweight input-space residual adapter that is architecture-agnostic by design with respect to the frozen TFM backbone. TFM-Retouche learns a small residual correction in the input space to align the input data with the inductive biases of the pretrained model. The adapter is trained end-to-end through the frozen TFM, with a post-training identity guard that falls back to the unmodified TFM whenever adaptation does not help on held-out validation. On TabArena-Lite (51 datasets spanning binary classification, multiclass classification, and regression), TabICLv2-Retouche -- the framework instantiated on TabICLv2 -- is the top-ranked method on the leaderboard with light per-task tuning and ensembling, lifting aggregate Elo by +56 over the frozen TabICLv2 base and sitting on the Pareto front of predictive quality versus both training and inference time.

</details>


### [248] [When Brain Networks Travel: Learning Beyond Site](https://arxiv.org/abs/2605.06050)
*Yingxu Wang,Kunyu Zhang,Yanwu Yang,Thomas Wolfers,Yujie Wu,Siyang Gao,Nan Yin*

Main category: cs.LG

TL;DR: CORE is a unified framework for cross-site robust brain network analysis that addresses site-conditioned bias and captures transient neurodynamics through site-aware confounder decoupling and pathway-level modeling, achieving superior performance in leave-one-site-out evaluations.


<details>
  <summary>Details</summary>
Motivation: Existing graph-based fMRI methods degrade under cross-site OOD settings due to site-conditioned confounders creating non-pathological shortcuts, and temporal averaging obscures transient neurodynamics, limiting generalization to unseen sites.

Method: CORE employs: 1) site-aware confounder decoupling to extract cross-site population scaffold, 2) profiling transient pathway dynamics using lightweight temporal descriptors, 3) organizing scaffold edges into line graph for pathway-level modeling, and 4) prior-guided subject-adaptive gating mechanism leveraging population priors while preserving subject variability.

Result: Extensive leave-one-site-out experiments on ABIDE, REST-meta-MDD, SRPBS, and ABCD datasets show CORE consistently outperforms SOTA baselines with up to 6.7% relative gain, and remains robust to atlas variations across different brain parcellation schemes.

Conclusion: CORE provides a unified framework that effectively addresses cross-site OOD challenges in brain network analysis by mitigating site-conditioned bias while capturing transient neurodynamics, demonstrating superior generalizability and robustness to anatomical variations.

Abstract: Graph-based learning on functional magnetic resonance imaging (fMRI) has shown strong potential for brain network analysis. However, existing methods degrade under cross-site out-of-distribution (OOD) settings because site-conditioned confounders induce non-pathological shortcuts, while functional connectivity constructed by temporal averaging obscures transient neurodynamics, limiting generalization to unseen sites. In this paper, we propose Cross-site OOD Robust brain nEtwork (CORE), a unified framework for brain network learning across unseen sites. CORE first performs site-aware confounder decoupling to mitigate site-conditioned bias and extract a cross-site population scaffold of reproducible diagnostic connectivity edges. It then profiles transient pathway dynamics over this scaffold using lightweight temporal descriptors and organizes scaffold edges into a line graph for transferable pathway-level modeling. Finally, CORE introduces a prior-guided subject-adaptive gating mechanism that leverages scaffold-derived population priors while preserving subject-specific connectivity variability. Extensive experiments under leave-one-site-out evaluation on real-world datasets (ABIDE, REST-meta-MDD, SRPBS, and ABCD) show that CORE consistently outperforms state-of-the-art baselines, with up to 6.7% relative gain. Furthermore, CORE remains robust to atlas variations, maintaining performance gains across different brain parcellation schemes.

</details>


### [249] [Towards Generation-Efficient Uncertainty Estimation in Large Language Models](https://arxiv.org/abs/2605.06053)
*Mingcheng Zhu,Yu Liu,Tingting Zhu*

Main category: cs.LG

TL;DR: LLM uncertainty estimation can be effective with partial generation or input-only information, reducing inference costs while maintaining reliability.


<details>
  <summary>Details</summary>
Motivation: Current LLM uncertainty estimation methods require full autoregressive generations, which introduces substantial inference cost and delays uncertainty assessment, making them impractical for high-stakes applications like healthcare and finance where hallucinations can be fluently incorrect.

Method: The paper proposes a unified framework for early uncertainty estimation during LLM generation, and introduces two low-cost methods: 1) Logit Magnitude using top-M logit evidence from early-stopped generation prefixes, and 2) MetaUE, which distills generation-based uncertainty into a lightweight input-only estimator trained with uncertainty scores.

Result: Logit Magnitude achieves strong performance on general and domain-specific benchmarks, showing partial LLM generations are often sufficient for effective uncertainty estimation. MetaUE provides competitive input-only approximation in several settings.

Conclusion: Effective uncertainty estimation requires less generation than commonly assumed, enabling earlier identification of unreliable responses and reducing inference costs for practical deployment in high-stakes applications.

Abstract: Uncertainty estimation is important for deploying LLMs in high-stakes applications such as healthcare and finance, where hallucinations can appear fluent and plausible while being factually incorrect, making it difficult for users to judge whether an output should be trusted. Existing methods require one or more full autoregressive generations to estimate uncertainty, which introduces substantial inference cost and often delays uncertainty assessment. In this paper, we investigate whether effective uncertainty estimation can be achieved with partial generation or even input-only information. Specifically, we first develop a unified framework that formulates uncertainty estimation as an early estimation problem over the autoregressive generation process of LLMs. This framework organises existing and proposed estimators by the information they observe, ranging from multi-generation to input-only prediction, and clarifies the performance-cost trade-off underlying different uncertainty estimation methods. Building on this view, we study two largely underexplored low-cost settings: estimating uncertainty with part of the generation, and predicting uncertainty from the input prompt. We propose Logit Magnitude, which uses top-M logit evidence to estimate uncertainty from an early-stopped generation prefix, and MetaUE, which distils generation-based uncertainty into a lightweight input-only estimator trained with uncertainty scores. Extensive experiments on general and domain-specific benchmarks show that Logit Magnitude achieves strong performance, and partial generations of LLMs are often sufficient for effective uncertainty estimation. MetaUE further provides a competitive input-only approximation in several settings. These findings suggest that effective uncertainty estimation requires less generation than commonly assumed, enabling unreliable responses to be identified earlier.

</details>


### [250] [Towards Self-Explainable Document Visual Question Answering with Chain-of-Explanation Predictions](https://arxiv.org/abs/2605.06058)
*Kjetil Indrehus,Adrian Duric,Changkyu Choi,Ali Ramezani-Kebrya*

Main category: cs.LG

TL;DR: CoExVQA is a self-explainable Document Visual Question Answering framework that uses a chain-of-explanation approach to first identify relevant evidence, then localize answer regions, and finally decode answers only from grounded regions.


<details>
  <summary>Details</summary>
Motivation: Existing DocVQA models entangle question-relevant evidence and answer localization, operating as black boxes with limited means to verify how predictions depend on visual evidence. There's a need for more transparent and verifiable models in Document VQA.

Method: CoExVQA uses a chain-of-explanation design with three explicit steps: 1) Identify question-relevant evidence, 2) Explicitly localize the answer region, 3) Decode the answer exclusively from the grounded region. This creates a transparent reasoning process that can be directly inspected.

Result: CoExVQA achieves state-of-the-art explainable DocVQA performance on PFL-DocVQA, improving ANLS by 12% over current explainable baselines while providing transparent and verifiable predictions.

Conclusion: The proposed CoExVQA framework successfully addresses the transparency and verifiability issues in DocVQA by introducing a grounded chain-of-explanation approach that separates evidence identification from answer localization, leading to both improved performance and explainability.

Abstract: Document Visual Question Answering (DocVQA) requires vision-language models to reason not only about what information in a document is relevant to a question, but also where the answer is grounded on the page. Existing DocVQA models entangle question-relevant evidence and answer localization and operate largely as black boxes, offering limited means to verify how predictions depend on visual evidence. We propose CoExVQA, a self-explainable DocVQA framework with a grounded reasoning process through a chain-of-explanation design. CoExVQA first identifies question-relevant evidence, then explicitly localizes the answer region, and finally decodes the answer exclusively from the grounded region. Prediction via CoExVQA's chain-of-explanation enables direct inspection and verification of the reasoning process across modalities. Empirical results show that restricting decoding to grounded evidence achieves SotA explainable DocVQA performance on PFL-DocVQA, improving ANLS by 12% over the current explainable baselines while providing transparent and verifiable predictions.

</details>


### [251] [Geometry-Aware Simplicial Message Passing](https://arxiv.org/abs/2605.06061)
*Elena Xinyi Wang,Bastian Rieck*

Main category: cs.LG

TL;DR: The paper introduces the Geometric Simplicial Weisfeiler-Lehman (GSWL) test to characterize geometric expressivity in simplicial networks, bridging the gap between combinatorial and geometric representations with theoretical guarantees and experimental validation.


<details>
  <summary>Details</summary>
Motivation: Current combinatorial tests like Weisfeiler-Lehman (WL) and its simplicial extension (SWL) are limited because they can't distinguish between geometric shapes with identical connectivity but different spatial embeddings - they are "blind to geometry." This is a fundamental limitation for analyzing geometric data like meshes.

Method: The paper introduces the Geometric Simplicial Weisfeiler-Lehman (GSWL) test, which incorporates vertex coordinates into color refinement for geometric simplicial complexes. It establishes theoretical bounds: (i) geometry-aware simplicial message passing schemes are bounded above by GSWL, and (ii) these schemes can match GSWL's discriminating power on finite families. The method combines GSWL with the Euler Characteristic Transform (ECT) for a complete invariant framework.

Result: The paper establishes GSWL as a geometric expressivity characterization with approximation guarantees. Experiments on synthetic and mesh datasets demonstrate a clear hierarchy from combinatorial (WL/SWL) to geometry-aware models (GSWL-based), validating the theoretical framework.

Conclusion: GSWL provides a principled theoretical framework for characterizing geometric expressivity in simplicial networks, bridging combinatorial and geometric analysis with both theoretical completeness guarantees and practical validation showing the superiority of geometry-aware approaches.

Abstract: The Weisfeiler--Lehman (WL) test and its simplicial extension (SWL) characterize the combinatorial expressivity of message passing networks, but they are blind to geometry, i.e., meshes with identical connectivity but different embeddings are indistinguishable. We introduce the Geometric Simplicial Weisfeiler--Lehman (GSWL) test, which incorporates vertex coordinates into color refinement for geometric simplicial complexes. In addition, we show that (i) the expressivity of geometry-aware simplicial message passing schemes is bounded above by GSWL, and (ii) that there exist parameters such that the discriminating power of GSWL is matched by these schemes on any fixed finite family of geometric simplicial complexes. Combined with the Euler Characteristic Transform (ECT), a complete invariant for geometric simplicial complexes, this yields a geometric expressivity characterization together with an approximation framework. Experiments on synthetic and mesh datasets serve to validate our theory, showing a clear hierarchy from combinatorial to geometry-aware models.

</details>


### [252] [Causal Reinforcement Learning for Complex Card Games: A Magic The Gathering Benchmark](https://arxiv.org/abs/2605.06066)
*Cristiano da Costa Cunha,Ajmal Mian,Tim French,Wei Liu*

Main category: cs.LG

TL;DR: MTG-Causal-RL is a new causal reinforcement learning benchmark based on Magic: The Gathering that provides a complex environment with sequential decision making, hidden information, large masked action spaces, and explicit causal structure for evaluating causal RL methods.


<details>
  <summary>Details</summary>
Motivation: Current causal reinforcement learning lacks benchmarks that combine multiple challenging aspects: sequential decision making, hidden information, large masked action spaces, and explicit causal structure. Existing benchmarks cannot adequately test causal credit assignment, structural transfer across strategies, or policy auditability.

Method: The authors create MTG-Causal-RL, a Gymnasium benchmark built on Magic: The Gathering with: 3,077-dimensional partial observations, 478-action masked discrete action space, five competitive Standard archetypes, three reward schemes, and a hand-specified Structural Causal Model (SCM). They adapt reference baselines (random, heuristic, masked PPO, causal-world-model PPO, scalar control) and propose Causal Graph-Factored Advantage PPO (CGFA-PPO) that uses SCM parents of win probability as factor-aligned critic targets with intervention-calibration loss.

Result: Masked PPO and the proposed CGFA-PPO achieve competitive in-distribution win rates, both exceeding random baselines. Per-factor calibration trajectories and leave-one-out transfer gaps reveal diagnostic structure that scalar win rates alone cannot capture. The benchmark exposes causal variables, SCM-predicted intervention effects, and per-factor credit traces.

Conclusion: MTG-Causal-RL provides a strategically rich testbed for causal RL research that enables testing of causal credit assignment under masked action spaces, structural transfer across archetypes, and SCM-grounded policy auditability - questions current benchmarks cannot address together. The benchmark, results, and evaluation protocol are released openly to advance causal RL, world-model, and LLM-agent research.

Abstract: Causal reinforcement learning (RL) lacks benchmarks for complex systems that combine sequential decision making, hidden information, large masked action spaces, and explicit causal structure. We introduce MTG-Causal-RL, a Gymnasium benchmark built on Magic: The Gathering with a 3,077-dimensional partial observation, a 478-action masked discrete action space, five competitive Standard archetypes, three reward schemes, and a hand-specified Structural Causal Model (SCM) over strategic variables. Every episode exposes causal variables, SCM-predicted intervention effects, and per-factor credit traces, making causal credit assignment, leave-one-out cross-archetype transfer, and policy auditability first-class metrics. We adapt a panel of reference baselines: random, heuristic, masked PPO, a causal-world-model PPO variant, and an architecture-matched scalar control. We propose Causal Graph-Factored Advantage PPO (CGFA-PPO) as a reference causal agent that uses SCM parents of win probability as factor-aligned critic targets with an intervention-calibration loss. All comparisons use paired seeds, paired-bootstrap confidence intervals, and Holm-Bonferroni correction within pre-registered families. Masked PPO and CGFA-PPO reach competitive in-distribution win rates and exceed the random baseline; per-factor calibration trajectories and leave-one-out transfer gaps expose diagnostic structure that scalar win rate alone cannot. We release the benchmark, reference-baseline results, and full evaluation protocol openly. By coupling a strategically rich, partially observed domain with an explicit causal interface and statistical protocol, MTG-Causal-RL gives causal-RL, world-model, and LLM-agent research a shared testbed for questions current benchmarks cannot pose together: causal credit assignment under masked action spaces, structural transfer across archetypes, and SCM-grounded policy auditability.

</details>


### [253] [Normalized Architectures are Natively 4-Bit](https://arxiv.org/abs/2605.06067)
*Maxim Fishman,Brian Chmiel,Ron Banner,Daniel Soudry,Boris Ginsburg*

Main category: cs.LG

TL;DR: nGPT architecture with hypersphere-constrained weights enables stable 4-bit training without special interventions, showing improved robustness to quantization noise through constructive signal accumulation.


<details>
  <summary>Details</summary>
Motivation: Training large language models at 4-bit precision is critical for computational efficiency, but standard approaches require various interventions (like Hadamard transforms and per-tensor scaling) to preserve quality. There's a need for architectures inherently robust to low-precision arithmetic.

Method: The authors propose nGPT, an architecture that constrains both weights and hidden representations to the unit hypersphere. This normalization enables stable end-to-end NVFP4 training without needing interventions. They validate this on 1.2B dense models and hybrid MoE models up to 3B/30B parameters.

Result: nGPT demonstrates inherent robustness to low-precision arithmetic, enabling successful 4-bit training. Analysis shows the hypersphere constraint enhances weak positive correlations in element-wise products, leading to constructive signal accumulation while noise averages out. This yields higher effective signal-to-noise ratio and flatter loss landscape, with advantages growing as hidden dimension increases.

Conclusion: The nGPT architecture's hypersphere constraint provides inherent robustness to low-precision training, making it well-suited for 4-bit LLM training without requiring special quantization interventions. This approach offers increasing advantages at scale and enables efficient training of large models.

Abstract: Training large language models at 4-bit precision is critical for efficiency. We show that nGPT, an architecture that constrains weights and hidden representations to the unit hypersphere, is inherently more robust to low-precision arithmetic. This removes the need for interventions-such as applying random Hadamard transforms and performing per-tensor scaling calculations-to preserve model quality, and it enables stable end-to-end NVFP4 training. We validate this approach on both a 1.2B dense model and hybrid (Mamba-Transformer) MoE models of up to 3B/30B parameters. We trace this robustness to the dot product: while quantization noise remains largely uncorrelated in both standard and normalized architectures, the signal behaves differently. In nGPT, the hypersphere constraint enhances weak positive correlations among the element-wise products, leading to a constructive accumulation of the signal across the hidden dimension while the noise continues to average out. This yields a higher effective signal-to-noise ratio and a flatter loss landscape, with the effect strengthening as the hidden dimension grows, suggesting increasing advantages at scale. A reference implementation is available at https://github.com/anonymous452026/ngpt-nvfp4

</details>


### [254] [PRISM: Iterative Cross-Modal Posterior Refinement for Dynamic Text-Attributed Graphs](https://arxiv.org/abs/2605.06073)
*Trimble Chang,Yihang Liu,Mingjing Han,Han Zhang*

Main category: cs.LG

TL;DR: PRISM is an iterative cross-modal posterior refinement framework for dynamic text-attributed graph representation learning that progressively refines semantic priors using behavioral evidence through iterative cross-modal interactions.


<details>
  <summary>Details</summary>
Motivation: Existing methods for multimodal learning in dynamic text-attributed graphs (DyTAGs) rely on rigid modality partitions and one-shot fusion strategies, which limit their ability to capture intrinsic and evolving dependencies between node semantics and interaction behaviors. There's a need for more sophisticated multimodal learning approaches that can better capture these complex relationships.

Method: PRISM organizes DyTAG information into semantic and behavioral modalities (instead of carrier-level partitions) and learns a refinement trajectory that progressively transforms semantic priors into behavior-conditioned posterior states through iterative cross-modal interactions with behavioral evidence. It uses an iterative cross-modal posterior refinement framework.

Result: Extensive experiments on DTGB benchmark datasets show that PRISM achieves strong performance on temporal link prediction and destination node retrieval tasks. Ablation studies validate the effectiveness of both the semantic-behavioral modeling approach and the iterative posterior refinement strategy.

Conclusion: PRISM's iterative cross-modal posterior refinement framework effectively addresses limitations of existing DyTAG representation learning methods by capturing intrinsic dependencies between node semantics and interaction behaviors through progressive refinement rather than one-shot fusion, demonstrating superior performance on key temporal graph tasks.

Abstract: Dynamic text-attributed graphs (DyTAGs) provide a powerful framework for modeling evolving systems in which node semantics and time-dependent interactions are tightly coupled. Recently, multimodal learning has emerged as a promising yet underexplored direction for enhancing DyTAG representation learning. However, existing methods typically rely on rigid modality partitions and one-shot fusion strategies, which limit their ability to capture the intrinsic and evolving dependencies between node semantics and interaction behaviors. To address these limitations, we propose \textbf{PRISM}, an iterative cross-modal posterior refinement framework for DyTAG representation learning. PRISM organizes DyTAG information into semantic and behavioral modalities, providing a more intrinsic alternative to carrier-level modality partitions. Instead of fusing the two modalities in a single step, PRISM learns a refinement trajectory that progressively transforms semantic priors into behavior-conditioned posterior states through cross-modal interaction with behavioral evidence. Extensive experiments on DTGB benchmark datasets show that PRISM achieves strong performance on temporal link prediction and destination node retrieval tasks. Further ablation studies validate the effectiveness of semantic--behavioral modeling and iterative posterior refinement.

</details>


### [255] [Understanding diffusion models requires rethinking (again) generalization](https://arxiv.org/abs/2605.06077)
*Pierre Marion,Yu-Han Wu*

Main category: cs.LG

TL;DR: Generalization in diffusion models requires new theoretical frameworks beyond classical learning theory, as memorization and generalization are incompatible in diffusion models unlike supervised learning.


<details>
  <summary>Details</summary>
Motivation: Existing theoretical explanations for generalization in diffusion models are insufficient - capacity limitations, implicit regularization, and architectural biases don't fully explain why models generalize rather than memorize training data. The field needs to shift focus from explaining non-memorization to understanding what models actually learn during the pre-memorization phase.

Method: Position paper arguing for theoretical reframing, supported by empirical study of diffusion models trained on CIFAR-10 to distill findings into concrete open questions.

Result: The paper identifies key open questions about generalization in diffusion models based on empirical observations, arguing that current theoretical frameworks are inadequate and proposing a research direction shift.

Conclusion: Understanding generalization in diffusion models requires fundamentally new theoretical approaches that focus on what models learn before memorization, rather than trying to explain why they don't memorize. The field should pivot to investigate the pre-memorization learning phase.

Abstract: This position paper argues that understanding generalization in diffusion models requires fundamentally new theoretical frameworks that go beyond both classical statistical learning theory and the benign overfitting paradigm developed for supervised learning. In diffusion models, unlike in supervised learning, memorization of training data and generalization to novel samples are incompatible: a model that has fully memorized its training set generates copies rather than novel data. Several theoretical explanations for why practical diffusion models nevertheless generalize have been proposed, based on capacity limitations, implicit regularization from optimization, or architectural inductive biases, but their interactions remain unclear. We argue that the field should pivot from explaining why the diffusion models do not memorize to investigating what the model actually learns during pre-memorization phase. To highlight our stance, we conduct empirical study of diffusion models trained on CIFAR-10, and we distill the findings into concrete open questions that we believe are key to improve understanding of generalization in diffusion models.

</details>


### [256] [Fast Gauss-Newton for Multiclass Cross-Entropy](https://arxiv.org/abs/2605.06081)
*Mikalai Korbit,Mario Zanon*

Main category: cs.LG

TL;DR: FGN provides a scalable positive semidefinite approximation to the full multiclass softmax GGN by dropping the computationally expensive within-competitor covariance term while maintaining exactness for binary classification.


<details>
  <summary>Details</summary>
Motivation: The full generalized Gauss-Newton (GGN) curvature in multiclass softmax cross-entropy couples all output logits through softmax covariance, making curvature-vector products computationally expensive and hard to scale as the number of classes grows.

Method: The authors decompose the standard multiclass GGN into a true-vs-rest term and a within-competitor covariance term. Fast Gauss-Newton (FGN) retains only the first term while dropping the second, creating a positive semidefinite under-approximation. This uses an exact scalar-margin representation of softmax cross-entropy where only curvature is approximated. They solve the damped update matrix-free using conjugate gradient with Jacobian-vector and vector-Jacobian products.

Result: FGN stays closest to full softmax GGN when competitor mass is concentrated or damping is large, and deviates as the dropped within-competitor covariance grows. The method provides computational efficiency while maintaining theoretical properties.

Conclusion: FGN offers a principled, scalable approximation to multiclass softmax GGN curvature that maintains positive semidefiniteness and exactness for binary classification, enabling efficient second-order optimization in high-dimensional multiclass problems through careful decomposition and matrix-free solution methods.

Abstract: In multiclass softmax cross-entropy, the full generalized Gauss-Newton (GGN) curvature couples all output logits through the softmax covariance, making curvature-vector products harder to scale as the number of classes grows. We show that the standard multiclass GGN can be decomposed exactly into a true-vs-rest term and a positive semidefinite within-competitor covariance term. Fast Gauss-Newton (FGN) retains the first term and drops the second, yielding a positive semidefinite under-approximation of the multiclass GGN that is exact for binary classification. The derivation uses an exact true-vs-rest scalar-margin representation of softmax cross-entropy: the loss and gradient are unchanged, and the approximation enters only at the curvature level. Exploiting the FGN curvature structure, the damped update can be written as an equivalent whitened row-space system with one row per mini-batch example. We solve this system matrix-free by conjugate gradient using Jacobian-vector and vector-Jacobian products of the scalar margin map. Targeted mechanism experiments and an evaluation on a fixed-feature multiclass head support the predictions from the decomposition: FGN stays closest to the full softmax GGN when competitor mass is concentrated or damping is large, and deviates as the dropped within-competitor covariance grows.

</details>


### [257] [Beyond Autoregressive RTG: Conditioning via Injection Outside Sequential Modeling in Decision Transformer](https://arxiv.org/abs/2605.06104)
*Yongyi Wang,Hanyu Liu,Lingfeng Li,Bozhou Chen,Ang Li,Qirui Zheng,Xionghui Yang,Chucai Wang,Wenxin Li*

Main category: cs.LG

TL;DR: SlimDT removes Return-to-Go tokens from Decision Transformer sequences, injecting RTG information into state representations instead, reducing sequence length by one-third while improving performance.


<details>
  <summary>Details</summary>
Motivation: RTG tokens in Decision Transformer consume computational resources disproportionate to their information content, and adding them as separate tokens increases quadratic self-attention costs without proportional benefit.

Method: SlimDT removes RTG from the autoregressive sequence and instead injects RTG information into state representations before sequential modeling, allowing the Transformer to process only compact (state, action) sequences.

Result: SlimDT reduces sequence length by one-third, improving inference efficiency. On D4RL benchmark, it surpasses standard DT across various tasks and achieves performance comparable to state-of-the-art methods.

Conclusion: Decoupling sparse conditioning signals (like RTG) from information-rich sequences yields both computational efficiency gains and higher task performance in offline reinforcement learning.

Abstract: Decision Transformer (DT) formulates offline reinforcement learning as autoregressive sequence modeling, achieving promising results by predicting actions from a sequence of Return-to-Go (RTG), state, and action tokens. However, RTG is a scalar that summarizes future rewards, containing far less information than typical state or action vectors, yet it consumes the same computational budget per token. Worse, the self-attention cost of Transformers grows quadratically with sequence length, so including RTG as a separate token adds unnecessary overhead. We propose SlimDT, which removes RTG from the autoregressive sequence. Instead, we inject RTG information into the state representations before the sequential modeling step, allowing the Transformer to process only a compact (state, action) sequence. This reduces the sequence length by one-third, directly improving inference efficiency. On the D4RL benchmark, SlimDT surpasses standard DT across various tasks and achieves performance comparable to existing state-of-the-art methods. Decoupling a sparse conditioning signal from an information-rich sequence thus yields both computational gains and higher task performance.

</details>


### [258] [BoostLLM: Boosting-inspired LLM Fine-tuning for Few-shot Tabular Classification](https://arxiv.org/abs/2605.06117)
*Yi-Siang Wang,Kuan-Yu Chen,Yu-Chen Den,Darby Tien-Hao Chang*

Main category: cs.LG

TL;DR: BoostLLM applies boosting principles to LLM fine-tuning for tabular data by training sequential PEFT adapters as weak learners, outperforming standard LLM fine-tuning and matching/exceeding XGBoost performance in low-data regimes.


<details>
  <summary>Details</summary>
Motivation: LLMs adapted to tabular prediction via feature serialization perform worse than gradient-boosted decision trees (GBDTs) in low-data regimes, prompting exploration of whether boosting can be applied as a general training principle for LLM fine-tuning.

Method: Proposes BoostLLM: transforms parameter-efficient fine-tuning (PEFT) into multi-round residual optimization by training sequential PEFT adapters as weak learners. Incorporates tabular inductive bias by integrating decision-tree paths as a second input view alongside raw features, where path views act as structured teachers early in training.

Result: Achieves consistent improvements over standard fine-tuning across multiple LLM backbones and datasets, matches/surpasses XGBoost across various shot counts, outperforms GPT-4o-based methods with a 4B model. Framework scales: pairing with stronger tree models and extended boosting horizons yields additional gains with proper stabilization.

Conclusion: Boosting can serve as a general training principle for LLM fine-tuning, particularly effective in low-data regimes for structured data, demonstrating competitive performance against specialized tabular methods.

Abstract: Large language models (LLMs) have recently been adapted to tabular prediction by serializing structured features into natural language, but their performance in low-data regimes remains limited compared to gradient-boosted decision trees (GBDTs). In this work, we revisit the boosting paradigm, traditionally associated with tree ensembles, and ask whether it can be applied as a general training principle for LLM fine-tuning. We propose BoostLLM, a framework that transforms parameter-efficient fine-tuning into a multi-round residual optimization process by training sequential PEFT adapters as weak learners. To incorporate tabular inductive bias, BoostLLM integrates decision-tree paths as a second input view alongside raw features; analysis reveals that the path view acts as a structured teacher in early training steps before the model shifts toward feature-driven representations. Empirically, BoostLLM achieves consistent improvements over standard fine-tuning across multiple LLM backbones and datasets, matching or surpassing XGBoost across a wide range of shot counts and outperforming GPT-4o-based methods with a 4B model. We further show that the framework scales: pairing with stronger tree models and extended boosting horizons yields additional gains under appropriate stabilization. These results suggest that boosting can serve as a general training principle for LLM fine-tuning, particularly in low-data regimes for structured data.

</details>


### [259] [Listwise Policy Optimization: Group-based RLVR as Target-Projection on the LLM Response Simplex](https://arxiv.org/abs/2605.06139)
*Yun Qu,Qi Wang,Yixiu Mao,Heming Zou,Yuhang Jiang,Yingyue Li,Wutong Xu,Lizhou Cai,Weijie Liu,Clive Bai,Kai Yang,Yangkun Chen,Saiyong Yang,Xiangyang Ji*

Main category: cs.LG

TL;DR: LPO proposes a listwise policy optimization framework that explicitly defines target distributions on response simplex and projects policies via exact divergence minimization, improving RLVR training stability and performance over typical policy gradient methods.


<details>
  <summary>Details</summary>
Motivation: Current RLVR approaches for LLM post-training use group-based policy gradients that implicitly define target distributions. The authors aim to demystify this implicit structure and create a more stable, flexible optimization framework with explicit target-projection mechanics.

Method: Listwise Policy Optimization (LPO) explicitly restricts proximal RL objectives to response simplex to define target distributions, then projects policies via exact divergence minimization with bounded, zero-sum, self-correcting gradients and flexible divergence selection.

Result: LPO consistently improves training performance over typical policy gradient baselines on diverse reasoning tasks and LLM backbones under matched targets, while intrinsically preserving optimization stability and response diversity.

Conclusion: The geometric insight connecting group-based RLVR methods reveals that explicit target-projection via LPO provides superior optimization properties, flexibility in divergence selection, and improved performance while maintaining stability and diversity in RL for language models.

Abstract: Reinforcement learning with verifiable rewards (RLVR) has become a standard approach for large language models (LLMs) post-training to incentivize reasoning capacity. Among existing recipes, group-based policy gradient is prevalent, which samples a group of responses per prompt and updates the policy via group-relative advantage signals. This work reveals that these optimization strategies share a common geometric structure: each implicitly defines a target distribution on the response simplex and projects toward it via first-order approximation. Building on this insight, we propose Listwise Policy Optimization (LPO) to explicitly conduct the target-projection, which demystifies the implicit target by restricting the proximal RL objective to the response simplex, and then projects the policy via exact divergence minimization. This framework provides (i) monotonic improvement on the listwise objective with bounded, zero-sum, and self-correcting projection gradients, and (ii) flexibility in divergence selection with distinct structural properties through the decoupled projection step. On diverse reasoning tasks and LLM backbones, LPO consistently improves training performance over typical policy gradient baselines under matched targets, while intrinsically preserving optimization stability and response diversity.

</details>


### [260] [SymDrift: One-Shot Generative Modeling under Symmetries](https://arxiv.org/abs/2605.06140)
*Samir Darouich,Vinh Tong,Lluís Pastor-Pérez,Tanja Bien,Loay Mualem,Mathias Niepert*

Main category: cs.LG

TL;DR: SymDrift: A symmetry-aware one-shot generative framework for molecular systems that addresses the symmetry challenges in drifting models while maintaining computational efficiency.


<details>
  <summary>Details</summary>
Motivation: Drifting models offer efficient single-step generation for molecular systems but face symmetry-specific challenges because equivariant generators don't naturally produce the correct drifting fields from symmetrized target distributions. Existing solutions require expensive symmetrization of empirical distributions, which is computationally prohibitive.

Method: SymDrift introduces two complementary strategies: 1) a symmetrized drift in coordinate space using optimal alignment, and 2) a G-invariant embedding that removes symmetry ambiguity by construction, making the drifting field itself symmetry-aware without expensive symmetrization.

Result: SymDrift outperforms existing one-shot methods on standard benchmarks for conformer and transition state generation, while remaining competitive with more expensive multi-step approaches. It reduces computational overhead by up to 40× compared to existing baselines.

Conclusion: SymDrift successfully addresses the symmetry challenges in drifting models for molecular generation, enabling efficient one-shot inference that is promising for high-throughput applications like virtual drug screening and large-scale reaction network exploration.

Abstract: Generative modeling of physical systems, such as molecules, requires learning distributions that are invariant under global symmetries, such as rotations in three-dimensional space. Equivariant diffusion and flow matching models can incorporate such invariances effectively, even when trained on a non-invariant empirical distribution, but they typically rely on costly multi-step sampling. Recently, drifting models have emerged as an efficient alternative, enabling single-step generation and achieving state-of-the-art performance in generative modeling tasks. However, we show that drifting models face a symmetry-specific challenge, since an equivariant generator does not generally produce the same drifting field as the one obtained from the symmetrized target distribution. Addressing this issue would require expensive symmetrization of the empirical distribution. To avoid this cost, we propose SymDrift, a framework that makes the drifting field itself symmetry-aware. We introduce two complementary strategies: (i) a symmetrized drift in coordinate space based on optimal alignment, and (ii) a $G$-invariant embedding that removes symmetry ambiguity by construction. Empirically, SymDrift outperforms existing one-shot methods on standard benchmarks for conformer and transition state generation, while remaining competitive with significantly more expensive multi-step approaches. By enabling one-shot inference, SymDrift reduces computational overhead by up to 40$\times$ compared to existing baselines, making it promising for high-throughput applications such as virtual drug screening and large-scale reaction network exploration.

</details>


### [261] [Matrix-Valued Optimism is Matrix-Valued Augmentation: Additive Hybrid Designs for Constrained Optimization](https://arxiv.org/abs/2605.06141)
*Jiayi Zhao*

Main category: cs.LG

TL;DR: The paper establishes equivalence between augmented Lagrangian and optimistic primal-dual methods for matrix-valued corrections, reveals an additivity principle, and proposes a hybrid design rule that outperforms pure methods.


<details>
  <summary>Details</summary>
Motivation: Augmented Lagrangian and optimistic primal-dual methods appear to stabilize equality-constrained optimization through different mechanisms (primal curvature vs dual memory). Recent work showed equivalence for scalar parameters, but this needs extension to matrix-valued corrections for more general applications.

Method: Proves an additivity principle showing that for symmetric matrix parameters, the ideal primal trajectory depends only on the summed correction matrix. Formulates a step-size-limited design problem and derives a closed-form hybrid rule that: selects a matrix correction, splits it between augmented/optimistic channels, and chooses primal/dual steps using local spectral weights.

Result: Experiments on nonlinear equality-constrained problems show: 1) Hybrid design improves over pure augmented and pure optimistic endpoints, 2) Closely tracks a grid-search hybrid oracle, 3) Competitive with first-order primal-dual baselines under mild-to-moderate ill-conditioning, 4) Reveals limitation that exact cancellation requires increasingly large matrix corrections as constraint Jacobian becomes ill-conditioned.

Conclusion: The paper establishes fundamental equivalence between two seemingly different optimization stabilization mechanisms for matrix-valued corrections, exposes design freedom in how corrections are split between channels, and provides a practical hybrid design rule that leverages this freedom to improve performance on equality-constrained problems.

Abstract: Augmented Lagrangian and optimistic primal--dual methods stabilize equality-constrained optimization through seemingly different mechanisms: the former adds constraint-dependent primal curvature, while the latter adds dual memory. Recent work has shown that these mechanisms are equivalent for scalar parameters. We extend this equivalence to matrix-valued correction. We prove an additivity principle: for symmetric matrix parameters, the ideal primal trajectory depends only on the summed correction matrix, not on how it is split between augmented and optimistic channels. This exposes a design freedom: algebraically equivalent decompositions can have different finite-step feasibility because augmented correction affects primal curvature, whereas optimistic correction affects the scale of the dual memory correction. We formulate the resulting step-size-limited design problem and derive a closed-form hybrid rule that selects a matrix correction, splits it between the two channels, and chooses primal and dual steps using local spectral weights. Experiments on nonlinear equality-constrained problems with controlled constraint-Jacobian conditioning show that the hybrid design improves over pure augmented and pure optimistic endpoints, closely tracks a grid-search hybrid oracle, and is competitive with first-order primal--dual baselines under mild-to-moderate ill-conditioning. The experiments also identify the expected limitation: exact cancellation requires increasingly large matrix corrections as the constraint Jacobian becomes ill-conditioned.

</details>


### [262] [Unifying Goal-Conditioned RL and Unsupervised Skill Learning via Control-Maximization](https://arxiv.org/abs/2605.06145)
*Alireza Modirshanechi,Benjamin Eysenbach,Peter Dayan,Eric Schulz*

Main category: cs.LG

TL;DR: MISL (mutual information skill learning) pretraining works for GCRL because both share a common principle: maximizing sensitivity between commands and behaviors.


<details>
  <summary>Details</summary>
Motivation: Unsupervised pretraining methods like MISL are empirically successful for goal-conditioned RL but lack theoretical understanding. Specifically, it's unclear why skills learned through MISL should support downstream goal-reaching tasks.

Method: Unify GCRL and MISL under control maximization framework. Identify three canonical GCRL formulations and analyze their equivalence. Show MISL objectives measure skill-sensitivity analogous to goal-sensitivity in GCRL.

Result: Prove three GCRL formulations are fundamentally inequivalent but share interpretation: good goal-conditioned policies have trajectories sensitive to commanded goals. Show MISL objectives are bounded by downstream goal-sensitivities, establishing precise correspondence between pretraining objectives and downstream tasks.

Conclusion: MISL pretraining succeeds because skills that maximize behavioral diversity inherently create the sensitivity needed for goal-reaching. Results provide theoretical foundation for RL pretraining and practical guidance for choosing pretraining objectives based on downstream task characteristics.

Abstract: Unsupervised pretraining has driven empirical advances in goal-conditioned reinforcement learning (GCRL), but its theoretical foundations remain poorly understood. In particular, an influential class of methods, mutual information skill learning (MISL), discovers behaviorally diverse skills that can later be used for downstream goal-reaching. However, it remains a theoretical mystery why skills learned through MISL should support goal-reaching. A subtle challenge is that both GCRL and MISL are umbrella terms: different GCRL tasks use distinct criteria for measuring goal-reaching performance, while different MISL methods optimize distinct notions of behavioral diversity. We address this challenge and unify GCRL and MISL as instances of control maximization. We identify three canonical GCRL formulations and prove that they are fundamentally inequivalent: they can induce incompatible optimal policies even in the same environment. Nevertheless, they all share a common interpretation: a well-performing goal-conditioned policy is one whose future trajectory is highly sensitive to the commanded goal, with the precise notion of sensitivity determined by the GCRL formulation. Noting that MISL objectives can be understood as measures of skill-sensitivity akin to goal-sensitivity, we show that MISL objectives are bounded by formulation-specific downstream goal-sensitivities. These bounds establish a precise correspondence between MISL methods and downstream GCRL tasks: for every GCRL formulation, there exists a matching MISL objective for which more diverse skills afford greater downstream goal sensitivity. Our results thus lay a theoretical foundation for RL pretraining and have important practical implications, such as suggesting which pretraining objectives to use when a user cares about a specific class of downstream tasks.

</details>


### [263] [AdaGamma: State-Dependent Discounting for Temporal Adaptation in Reinforcement Learning](https://arxiv.org/abs/2605.06149)
*Yaomin Wang,Jianting Pan,Ran Tian,Xiaoyang Li,Yu Zhang,Hengle Qin,Tianshu YU*

Main category: cs.LG

TL;DR: AdaGamma is a deep RL method that learns state-dependent discount factors with return-consistency regularization, preventing instability issues in actor-critic methods while improving performance on control tasks and real-world logistics platforms.


<details>
  <summary>Details</summary>
Motivation: Current deep RL methods use fixed discount factors across all states, but state-dependent discounting is conceptually appealing for controlling both planning horizon and bootstrapping strength adaptively. However, naive implementations suffer from instability and TD-error collapse.

Method: AdaGamma learns a state-dependent discount function along with a return-consistency objective that regularizes the induced backup structure to prevent target manipulation. It integrates into existing actor-critic frameworks like SAC and PPO.

Result: The method yields consistent improvements on continuous-control benchmarks and achieves statistically significant gains in an online A/B test on the JD Logistics platform.

Conclusion: State-dependent discounting can be made effective in deep RL when coupled with return-consistency regularization that prevents degenerate behavior, offering practical advantages over fixed discount factors.

Abstract: The discount factor in reinforcement learning controls both the effective planning horizon and the strength of bootstrapping, yet most deep RL methods use a single fixed value across all states. While state-dependent discounting is conceptually appealing, naive deep actor--critic implementations can become unstable and degenerate toward TD-error collapse. We propose AdaGamma, a practical deep actor--critic method for state-dependent discounting that learns a state-dependent discount function together with a return-consistency objective to regularize the induced backup structure. On the theory side, we analyze the Bellman operator induced by state-dependent discounting and establish its basic well-posedness properties under suitable conditions. Empirically, AdaGamma integrates into both SAC and PPO, yielding consistent improvements on continuous-control benchmarks, and achieves statistically significant gains in an online A/B test on the JD Logistics platform. These results suggest that state-dependent discounting can be made effective in deep RL when coupled with a return-consistency objective that prevents degenerate target manipulation.

</details>


### [264] [Grokking or Glitching? How Low-Precision Drives Slingshot Loss Spikes](https://arxiv.org/abs/2605.06152)
*Liu Hanqing,Jianjun Cao,Yuanze Li,Zijian Zhou*

Main category: cs.LG

TL;DR: The paper explains that periodic loss spikes in deep neural networks (Slingshot Mechanism) are caused by floating-point arithmetic precision limits, not intrinsic optimization dynamics. When logit differences exceed precision thresholds, gradients get rounded to zero, breaking the zero-sum constraint and causing exponential parameter growth through Numerical Feature Inflation.


<details>
  <summary>Details</summary>
Motivation: Existing work attributes periodic loss spikes during training to intrinsic optimization dynamics, but the actual triggering mechanism remains unclear. The authors aim to provide a concrete, testable explanation for this phenomenon based on numerical computation limitations.

Method: The paper proves mathematically that when training enters high-confidence stages, logit differences can exceed floating-point absorption-error thresholds. During backpropagation, this causes correct-class gradients to be rounded to zero while incorrect-class gradients remain non-zero, breaking the zero-sum constraint. The authors analyze how this creates a positive feedback loop between classifier and feature parameters.

Result: The authors identify Numerical Feature Inflation (NFI) as the mechanism driving exponential growth of global classifier and feature means. This explains the rapid norm growth before Slingshot spikes, gradient reappearance, and resulting loss spikes. They also show NFI can occur without visible spikes through partial absorption, still driving abnormal parameter growth.

Conclusion: Slingshot is a numerical dynamic of finite-precision training, not an intrinsic optimization phenomenon. NFI provides a testable explanation for abnormal parameter growth and logit divergence in late-stage training, reinterpreting existing observations through the lens of numerical computation limits.

Abstract: Deep neural networks exhibit periodic loss spikes during unregularized long-term training, a phenomenon known as the "Slingshot Mechanism." Existing work usually attributes this to intrinsic optimization dynamics, but its triggering mechanism remains unclear. This paper proves that this phenomenon is a result of floating-point arithmetic precision limits. As training enters a high-confidence stage, the difference between the correct-class logit and the other logits may exceed the absorption-error threshold. Then during backpropagation, the gradient of the correct class is rounded exactly to zero, while the gradients of the incorrect classes remain nonzero. This breaks the zero-sum constraint of gradients across classes and introduces a systematic drift in the parameter update of the classifier layer. We prove that this drift forms a positive feedback loop with the feature, causing the global classifier mean and the global feature mean to grow exponentially. We call this mechanism Numerical Feature Inflation (NFI). This mechanism explains the rapid norm growth before a Slingshot spike, the subsequent reappearance of gradients, and the resulting loss spike. We further show that NFI is not equivalent to an observed loss spike: in more practical tasks, partial absorption may not produce visible spikes, but it can still break the zero-sum constraint and drive rapid growth of parameter norms. Our results reinterpret Slingshot as a numerical dynamic of finite-precision training, and provide a testable explanation for abnormal parameter growth and logit divergence in late-stage training.

</details>


### [265] [Entropy-Regularized Adjoint Matching for Offline RL](https://arxiv.org/abs/2605.06156)
*Abdelghani Ghanem,Mounir Ghogho*

Main category: cs.LG

TL;DR: ME-AM addresses limitations of flow-based offline RL by adding entropy maximization and mixture behavior priors to overcome popularity bias and support binding issues while maintaining policy expressivity.


<details>
  <summary>Details</summary>
Motivation: Current flow-matching offline RL methods suffer from popularity bias (suppressing high-reward actions in low-density regions) and support binding (restricting off-manifold exploration). Existing solutions like residual Gaussian policies reintroduce unimodal expressivity bottlenecks.

Method: Proposes Maximum Entropy Adjoint Matching (ME-AM) with two key mechanisms: 1) Mirror Descent entropy maximization to mitigate popularity bias, and 2) Mixture Behavior Prior to broaden geometric support for out-of-distribution high-reward regions while maintaining continuous flow properties.

Result: ME-AM demonstrates competitive or superior performance compared to prior state-of-the-art methods across diverse sparse-reward continuous control environments.

Conclusion: ME-AM provides a unified framework that overcomes limitations of flow-based offline RL while preserving expressive generative capabilities, enabling better extraction of optimal policies from offline datasets.

Abstract: Integrating expressive generative policies, such as flow-matching models, into offline reinforcement learning (RL) allows agents to capture complex, multi-modal behaviors. While Q-learning with Adjoint Matching (QAM) stabilizes policy optimization via the continuous adjoint method, it remains inherently bound to the fixed behavior distribution. This dependence induces a \textit{popularity bias} that can suppress high-reward actions in low-density regions, and creates a \textit{support binding} that restricts off-manifold exploration. Existing workarounds, such as appending \textit{residual} Gaussian policies, often re-introduce the expressivity bottlenecks associated with unimodal distributions. In this work, we propose \textit{Maximum Entropy Adjoint Matching} (ME-AM), a unified framework that addresses these limitations within the continuous flow formulation. ME-AM incorporates two mechanisms: (1) a Mirror Descent entropy maximization objective that mitigates the popularity bias to facilitate the extraction of optimal policies from offline datasets, and (2) a \textit{Mixture Behavior Prior} that mathematically broadens the geometric support to encompass out-of-distribution high-reward regions. By exploring this extended geometry, ME-AM identifies robust actions while preserving the absolute continuity of the generative vector field. Empirically, ME-AM demonstrates competitive or superior performance compared to prior state-of-the-art (SOTA) methods across a diverse suite of sparse-reward continuous control environments.

</details>


### [266] [One Algorithm, Two Goals: Dual Scoring for Parameter and Data Selection in LLM Fine-Tuning](https://arxiv.org/abs/2605.06166)
*Xinrui Chen,Liu Yang,Ou Wu*

Main category: cs.LG

TL;DR: DualSFT is a one-shot algorithm that jointly selects both parameters and data for LLM fine-tuning using shared gradient statistics, improving efficiency and performance over separate selection methods.


<details>
  <summary>Details</summary>
Motivation: Current LLM fine-tuning methods use separate scoring mechanisms for parameter and data selection, which creates redundant overhead and makes coordinated selection difficult when both a parameter mask and data subset jointly determine restricted fine-tuning.

Method: The authors cast parameter and data selection as two bilevel selection problems under a common validation objective and derive a shared local response-surrogate scoring rule. They show that under first- and second-order validation-improvement approximations, parameter importance and data utility emerge as column-wise and row-wise aggregations of a single gradient interaction matrix. This yields a closed-form row-column correspondence for co-extracting both signals. Based on this structure, they propose DualSFT (Dual-Selection Fine-Tuning), a one-shot dual-scoring algorithm that produces a parameter mask and data subset from shared gradient statistics.

Result: On 3B-9B LLMs, single-axis DualSFT variants improve target-task performance and stability-plasticity trade-offs within their comparison groups. Full DualSFT yields a more favorable joint-constrained trade-off than sequential hybrid baselines under matched budgets.

Conclusion: Joint parameter and data selection using shared gradient statistics through DualSFT provides a more efficient and effective approach to LLM fine-tuning than separate or sequential selection methods, offering better performance and trade-offs under constrained budgets.

Abstract: In Large Language Model (LLM) fine-tuning, parameter and data selection are common strategies for reducing fine-tuning cost, yet they are typically driven by separate scoring mechanisms. When a parameter mask and data subset jointly determine restricted fine-tuning, this separation incurs redundant overhead and makes coordinated selection difficult. We cast parameter and data selection as two bilevel selection problems under a common validation objective and derive a shared local response-surrogate scoring rule. Under first- and second-order validation-improvement approximations, parameter importance and data utility emerge as column-wise and row-wise aggregations of a single gradient interaction matrix, yielding a closed-form row-column correspondence for co-extracting both signals. Building on this structure, we propose DualSFT (Dual-Selection Fine-Tuning), a one-shot dual-scoring algorithm that produces a parameter mask and data subset from shared gradient statistics. On 3B-9B LLMs, single-axis DualSFT variants strengthen target-task performance and stability-plasticity trade-offs within their comparison groups, while full DualSFT yields a more favorable joint-constrained trade-off than sequential hybrid baselines under matched budgets.

</details>


### [267] [Mean Mode Screaming: Mean--Variance Split Residuals for 1000-Layer Diffusion Transformers](https://arxiv.org/abs/2605.06169)
*Pengqi Lu*

Main category: cs.LG

TL;DR: The paper identifies a "mean mode screaming" collapse vulnerability in deep Diffusion Transformers, proposes "Mean-Variance Split Residuals" to prevent it, and demonstrates stable training up to 1000 layers.


<details>
  <summary>Details</summary>
Motivation: When scaling Diffusion Transformers to hundreds of layers, networks can enter a silent collapse state that homogenizes token representations and suppresses variation, even when training appears stable. This structural vulnerability limits the depth scalability of DiTs.

Method: Through mechanistic auditing, the authors identify Mean Mode Screaming (MMS) as the trigger event. They propose Mean-Variance Split (MV-Split) Residuals, which combine separately gained centered residual updates with leaky trunk-mean replacement to prevent divergent collapse.

Result: On a 400-layer DiT, MV-Split prevents the collapse that crashes unstabilized baselines, tracking close to the baseline's pre-crash trajectory while outperforming token-isotropic gating methods like LayerScale. They also successfully train a 1000-layer DiT, demonstrating stable training at extreme depth.

Conclusion: The Mean Mode Screaming phenomenon is a critical vulnerability in deep DiTs, but can be addressed with MV-Split Residuals, enabling stable training of extremely deep (up to 1000-layer) Diffusion Transformers and advancing the scalability of transformer-based diffusion models.

Abstract: Scaling Diffusion Transformers (DiTs) to hundreds of layers introduces a structural vulnerability: networks can enter a silent, mean-dominated collapse state that homogenizes token representations and suppresses centered variation. Through mechanistic auditing, we isolate the trigger event of this collapse as Mean Mode Screaming (MMS). MMS can occur even when training appears stable, with a mean-coherent backward shock on residual writers that opens deep residual branches and drives the network into a mean-dominated state. We show this behavior is driven by an exact decomposition of these gradients into mean-coherent and centered components, compounded by the structural suppression of attention-logit gradients through the null space of the Softmax Jacobian once values homogenize.
  To address this, we propose Mean-Variance Split (MV-Split) Residuals, which combine a separately gained centered residual update with a leaky trunk-mean replacement. On a 400-layer single-stream DiT, MV-Split prevents the divergent collapse that crashes the un-stabilized baseline; it tracks close to the baseline's pre-crash trajectory while remaining substantially better than token-isotropic gating methods such as LayerScale across the full schedule. Finally, we present a 1000-layer DiT as a scale-validation run at boundary scales, establishing that the architecture remains stably trainable at extreme depth.

</details>


### [268] [In-Context Black-Box Optimization with Unreliable Feedback](https://arxiv.org/abs/2605.06187)
*Nicolas Samuel Blumer,Julien Martinelli,Samuel Kaski*

Main category: cs.LG

TL;DR: A method for black-box optimization that incorporates multiple sources of auxiliary feedback (experts, simulators, predictors) along with optimization history, using a transformer that learns to assess source reliability in context to improve search performance while being robust to misleading information.


<details>
  <summary>Details</summary>
Motivation: Black-box optimization often has access to side information (experts, simulators, pretrained predictors), but existing methods either handle one feedback source at a time (limiting generalization) or assume only optimization history is available. There's a need for methods that can leverage multiple, potentially biased or misleading feedback sources while adapting across tasks.

Method: Feedback-Informed In-Context Black-Box Optimization (FICBO): pretrains a transformer to condition on both observed optimization history and cheap auxiliary feedback for candidate sets. Introduces a structured feedback prior modeling how feedback sources vary in access, relevance, and distortion relative to the true objective. At test time, the model estimates source reliability in context by comparing observed objective values with auxiliary signals.

Result: On synthetic and real-world tasks, FICBO effectively exploits informative feedback while remaining robust to weak or misleading sources, outperforming other baselines. Empirical investigations show how the model perceives test-time sources, offering insights into interpretability and decision-making.

Conclusion: FICBO provides a framework for leveraging multiple feedback sources in black-box optimization, addressing both exploitation of useful information and robustness against misleading signals through in-context source reliability estimation.

Abstract: Black-box optimization in science and engineering often comes with side information: experts, simulators, pretrained predictors, or heuristics can suggest which candidates look promising. This information can accelerate search, but it can also be biased, input-dependent, or misleading. Feedback-aware BO methods typically handle one task at a time, limiting their ability to generalize over multiple sources of feedback. In-context optimizers address cross-task adaptation, but usually assume that optimization history is the only available signal at test time. We study feedback-informed in-context black-box optimization (FICBO), where a pretrained optimizer conditions on both the observed history and cheap auxiliary feedback for the current candidate set. We introduce a structured feedback prior that models how feedback sources vary in their access, relevance, and distortion relative to the true objective, and use it to pretrain a feedback-aware transformer. At test time, the model estimates source reliability in context by comparing observed objective values with auxiliary signals, improving query selection. On synthetic and real-world tasks, FICBO effectively exploits informative feedback while remaining robust to weak or misleading sources, improving over other baselines. Empirical investigations further illustrate how the model perceives test-time sources, offering insights into its interpretability and decision-making process.

</details>


### [269] [Constrained Contextual Bandits with Adversarial Contexts](https://arxiv.org/abs/2605.06190)
*Dhruv Sarkar,Abhishek Sinha*

Main category: cs.LG

TL;DR: Proposes a modular framework for budget-constrained contextual bandits with adversarial contexts, using online regression oracles to reduce the constrained problem to an unconstrained one with surrogate rewards, achieving improved guarantees for adversarial contexts.


<details>
  <summary>Details</summary>
Motivation: Most prior work on budget-constrained contextual bandits focuses on stochastic contexts, leaving adversarial contexts less explored. There's a need for efficient algorithms with transparent analysis that can handle adversarial contexts while controlling both regret and budget constraint violations.

Method: Builds on the SquareCB framework and proposes a modular approach that leverages online regression oracles. Reduces the constrained problem to a standard unconstrained contextual bandit problem by adaptively defining surrogate reward functions that account for both reward and cost considerations.

Result: Achieves improved guarantees for adversarial contexts compared to prior work on stochastic contexts. Provides an efficient algorithm with compact and transparent analysis that simultaneously controls regret and budget constraint violations in the continuing setting.

Conclusion: The proposed framework successfully extends budget-constrained contextual bandit algorithms to handle adversarial contexts with improved theoretical guarantees, simpler analysis, and modular design that cleanly separates the constrained optimization from the base bandit algorithm.

Abstract: We study budget-constrained contextual bandits with adversarial contexts, where each action yields a random reward and incurs a random cost. We adopt the standard realizability assumption: conditioned on the observed context, rewards and costs are drawn independently from fixed distributions whose expectations belong to known function classes. We focus on the continuing setting, in which the algorithm operates over the entire horizon even after the budget for cumulative cost is exhausted. In this setting, the objective is to simultaneously control regret and the violation of the budget constraint. Building on the seminal $\mathsf{SquareCB}$ framework of Foster et al. [2018], we propose a simple and modular framework that leverages online regression oracles to reduce the constrained problem to a standard unconstrained contextual bandit problem with adaptively defined surrogate reward functions. In contrast to prior works, which focus on stochastic contexts, our reduction yields improved guarantees for more general adversarial contexts, together with an efficient algorithm with a compact and transparent analysis.

</details>


### [270] [Bandit Learning in General Open Multi-agent Systems](https://arxiv.org/abs/2605.06202)
*Mengfan Xu*

Main category: cs.LG

TL;DR: This paper addresses bandit learning in open systems where agents arrive and depart over time, introducing new concepts to capture system complexities and developing certified learning methods with provable guarantees.


<details>
  <summary>Details</summary>
Motivation: Existing bandit learning approaches for open systems impose structural assumptions that are frequently violated in practice, creating challenges including endogenous non-stationarity from new agents, varying information accumulation rates based on agent patterns, and scaling regret with time horizon due to new agents.

Method: The authors formulate a unified open-system bandit problem with general dynamics, introduce key concepts (pre-training degree, stability, global dynamic regret), and develop certified global-UCB learning methodologies with provable theoretical guarantees.

Result: The proposed methods achieve regret bounds where entry uncertainty enters linearly via the pre-training degree, while in stable regimes, regret is governed by the time needed to identify persistent optimal arms and agent patterns. Lower bounds show these dependencies are tight in hard instances.

Conclusion: This work provides a comprehensive framework for bandit learning in general open systems, introducing novel concepts to capture system complexities and delivering theoretically sound learning algorithms with tight performance guarantees that overcome limitations of existing approaches.

Abstract: Recent developments in digital platforms have highlighted the prevalence of open systems, where agents can arrive and depart over time. While bandit learning in open systems has recently received initial attention, existing work imposes structural assumptions that are frequently violated in practice. A learning paradigm for general open systems creates fresh challenges: newly arriving agents induce endogenous non-stationarity; agent patterns determine how quickly information accumulates; and new agents make regret scale further with the time horizon. To this end, we formulate a unified open-system bandit problem with general dynamics, including heterogeneous rewards and general agent patterns. We introduce new concepts to capture the inherent complexities: the \emph{pre-training degree} of new agents quantifies how much information an agent carries upon entry, \emph{stability} measures the impact of new agents on the system, and \emph{global dynamic regret} compares the cumulative expected reward of all active agents with that of the varying optimal arms. We develop certified global-UCB learning methodologies with provable guarantees. Our regret bounds reveal that entry uncertainty enters linearly via the pre-training degree, while in stable regimes, regret is governed by the time needed to identify a persistent optimal arm, as well as by the agent patterns. We further show that these dependencies are tight via lower bounds in hard instances.

</details>


### [271] [Federation of Experts: Communication Efficient Distributed Inference for Large Language Models](https://arxiv.org/abs/2605.06206)
*Muhammad Shahir Abdurrahman,Chun Deng,Azalia Mirhoseini,Philip Levis*

Main category: cs.LG

TL;DR: FoE (Federation of Experts) is a novel architecture that restructures MoE (Mixture of Experts) blocks in transformers to reduce communication overhead by clustering experts and eliminating or minimizing all-to-all communication, significantly improving inference performance while maintaining model quality.


<details>
  <summary>Details</summary>
Motivation: Mixture of Experts (MoE) has become essential for computational efficiency in Large Language Models, but communication of token embeddings between experts in distributed settings creates a significant bottleneck that limits performance.

Method: FoE restructures the MoE block of a transformer layer into multiple MoE clusters, where each cluster is responsible for only one KV head. Expert parallelism is applied between experts within clusters, and between clusters, a sum synchronizes post-attention residuals which then drives routing and dispatch for the next MoE block. This eliminates all-to-all communication within single nodes and confines it to intra-node fabric in multi-node settings.

Result: On LongBench, FoE significantly improves inference throughput and latency: reduces end-to-end forward-pass latency by up to 5.2x, TTFT by 3.62x, and TBT by 1.95x. Achieves comparable generation quality to standard MoE models of same size and training configuration.

Conclusion: The Federation of Experts architecture effectively addresses the communication bottleneck in distributed MoE settings by restructuring expert organization, delivering substantial performance improvements in both single-node and multi-node environments while maintaining model quality.

Abstract: Mixture of experts has emerged as the primary mechanism for making Large Language Models (LLMs) computationally efficient. However, in distributed settings, communicating token embeddings between experts is a significant bottleneck.
  We present the novel Federation of Experts (FoE) architecture. FoE restructures the MoE block of a transformer layer into multiple MoE clusters. Each cluster is responsible for only one of the KV heads and expert parallelism is applied between those experts. Between clusters, a sum synchronizes the post-attention residuals, which then drives routing and dispatch for the next MoE block. In a single-node setting, FoE completely eliminates all-to-all communication as all experts within a group are contained on the same GPU. In multi-node settings, FoE confines all-to-all communication to the intra-node fabric, thus significantly reducing communication overhead.
  An implementation of FoE finds that on LongBench, FoE significantly improves inference throughput and latency in both single-node and multi-node settings, reducing the end-to-end forward-pass latency by up to 5.2x, TTFT by 3.62x, and TBT by 1.95x. It does so while achieving comparable generation quality to a mixture of experts model of the same size and training configuration.

</details>


### [272] [Contrastive Identification and Generation in the Limit](https://arxiv.org/abs/2605.06211)
*Xiaoyu Li,Andi Han,Jiaojiao Jiang,Junbin Gao*

Main category: cs.LG

TL;DR: Contrastive learning in limit models: studying binary hypothesis learning from unordered pairs where labels differ but which is positive is hidden, characterizing identifiability and generation, with surprising results under corruption.


<details>
  <summary>Details</summary>
Motivation: Current learning in the limit models focus on positive-only or fully labeled data, but many real-world supervision signals are relational (contrastive pairs) rather than singleton labels. The authors want to study learning from contrastive pairs where only the relationship (difference) between examples is known, not which specific example is positive.

Method: Introduce contrastive identification and generation in the limit framework, analyze using: 1) geometric refinement of Angluin's tell-tale condition for contrastive identifiability, 2) contrastive closure dimension for uniform generation, 3) common crossing graph as unifying technical tool encoding pairwise ambiguity and generation obstructions, 4) study both noiseless and adversarial corruption settings.

Result: 1) Exact characterization of contrastively identifiable classes. 2) Combinatorial dimension called contrastive closure dimension characterizes uniform contrastive generation with tight sample complexity. 3) Contrastive generation and text identification are mutually incomparable. 4) Sharp reversal under corruption: classes identifiable from contrastive pairs under any finite corruption budget by single algorithm, yet not identifiable from positive examples under even one corruption.

Conclusion: Contrastive learning in the limit introduces new phenomena not present in classical settings, with surprising power under corruption. The common crossing graph provides unifying framework for analyzing pairwise ambiguity, generation obstructions, and corruption defects.

Abstract: In the classical identification in the limit model of Gold [1967], a stream of positive examples is presented round by round, and the learner must eventually recover the target hypothesis. Recently, Kleinberg and Mullainathan [2024] introduced generation in the limit, where the learner instead must eventually output novel elements of the target's support. Both lines of work focus on positive-only or fully labeled data. Yet many natural supervision signals are inherently relational rather than singleton, which encode relationships between examples rather than labels of individual ones. We initiate the study of contrastive identification and generation in the limit, where the learner observes a contrastive presentation of data: a stream of unordered pairs $\{x,y\}$ satisfying $h(x)\ne h(y)$ for an unknown target binary hypothesis $h$, but which element is positive is hidden from the learner. We first present three results in the noiseless setting: an exact characterization of contrastive identifiable classes (a one-line geometric refinement of Angluin [1980]'s tell-tale condition), a combinatorial dimension called contrastive closure dimension (a contrasitive analogue of the closure dimension in Raman et al. [2025]) and exactly characterizing uniform contrastive generation with tight sample complexity, and a strict hierarchy in which contrastive generation and text identification are mutually incomparable. We then prove a sharp reversal under finite adversarial corruption: there exist classes identifiable from contrastive pairs under any finite corruption budget by a single budget-independent algorithm, yet not identifiable from positive examples under even one corrupted observation. The unifying technical object is the common crossing graph, which encodes pairwise ambiguity, family-level generation obstructions, and corruption defects in a single coverage-and-incidence language.

</details>


### [273] [Playing the network backward: A Game Theoretic Attribution Framework](https://arxiv.org/abs/2605.06212)
*Jakob Paul Zimmermann,Jim Berend,Georg Loho,Sebastian Lapuschkin,Wojciech Samek*

Main category: cs.LG

TL;DR: The paper introduces a game-theoretic framework for backward attribution methods, unifying gradients, LRP, and transformer-specific rules as integrals over game trajectories, enabling systematic adaptation of attribution methods through game-theoretic concepts.


<details>
  <summary>Details</summary>
Motivation: Backward attribution methods lack a shared framework for comparison, making it difficult to systematically understand, compare, and adapt different attribution techniques like gradients, LRP, and transformer-specific rules.

Method: The authors recast backward attribution as a two-player game on an extended network graph, building on Gaubert and Vlassopoulos' ReLU Net Game. They show how gradients and alpha-beta-LRP family arise as integrals over game trajectories under specific equilibria, enabling desired explanation properties to be specified as game-theoretic concepts.

Result: One adaptation of alpha-beta-LRP derived from this framework outperforms prior transformer-specific backward methods across all considered localisation metrics on ViT-B/16, demonstrating the practical utility of the game-theoretic perspective.

Conclusion: The game-theoretic framework provides a unified foundation for understanding, comparing, and systematically adapting backward attribution methods, transforming attribution maps from primary objects to projections of trajectory distributions and enabling principled design of attribution methods with desired properties.

Abstract: Attribution methods explain which input features drive a model's prediction, making them central to model debugging and mechanistic interpretability. Yet backward attribution methods, including gradients, LRP, and transformer-specific rules, lack a shared framework in which to compare the underlying backward calculations. We introduce such a framework by recasting backward attribution as a two-player game on an extended network graph, building on Gaubert and Vlassopoulos' ReLU Net Game. Gradients and the full alpha-beta-LRP family arise as integrals over game trajectories under specific equilibria, so attribution maps become projections of trajectory distributions rather than the primary object. Desired explanation properties, such as localisation focus, robustness to input noise, or stable attention routing, can be specified as game-theoretic concepts, including policy regularization, risk aversion, and extended action sets, and translate directly into novel adaptations of the well-known backward rules. On ViT-B/16, one such selected adaptation of alpha-beta-LRP outperforms prior transformer-specific backward methods across all considered localisation metrics.

</details>


### [274] [AffineLens: Capturing the Continuous Piecewise Affine Functions of Neural Networks](https://arxiv.org/abs/2605.06218)
*Yi Wei,Xuan Qi,Furao shen,Jian Zhao,Vittorio Murino,Cigdem Beyan*

Main category: cs.LG

TL;DR: AffineLens is a practical framework that directly computes and visualizes the piecewise affine regions in neural networks, enabling systematic analysis of their geometric expressivity.


<details>
  <summary>Details</summary>
Motivation: Existing analyses of piecewise affine neural networks (PANNs) rely on indirect proxies like activation statistics or theoretical bounds, lacking practical tools to actually compute, enumerate, and visualize the affine region partitions in realistic architectures with bounded input domains.

Method: AffineLens framework identifies neuron-induced hyperplanes intersecting bounded input domains, enumerates affine regions layer-wise using the CPA networks' affine restriction property, computes provably non-empty maximal regions with interior representatives, and provides visualization tools.

Result: AffineLens works with modern components (batch normalization, pooling, residual connections, CNNs), enables systematic empirical studies of architectural expressivity through region complexity metrics, and reveals how design choices influence learned function geometry.

Conclusion: AffineLens provides a unified, practical framework for directly analyzing the geometric structure of piecewise affine neural networks, moving beyond theoretical proxies to enable concrete visualization and quantitative analysis of neural network expressivity.

Abstract: Piecewise affine neural networks (PANNs) provide a principled geometric perspective on neural network expressivity by characterizing the input--output map as a continuous piecewise affine (CPA) function whose complexity is governed by the number, arrangement, and shapes of its affine regions. However, existing interpretability and expressivity analyses often rely on indirect proxies (e.g., activation statistics or theoretical upper bounds) and rarely offer practical, accurate tools for enumerating and visualizing the induced region partition under realistic architectures and bounded input domains. In this work, we present AffineLens, a unified framework for computing the hyperplane arrangements and polyhedral structures underlying PANNs. Given a calibrated (bounded) input polytope, AffineLens identifies the subset of neuron-induced hyperplanes that intersect the domain, enumerates the resulting affine sub-regions in a layer-wise manner, and returns provably non-empty maximal CPA regions together with interior representatives. The framework further provides visualizations of region partitioning and decision boundaries, enabling qualitative inspection alongside quantitative region counts. By exploiting the affine restriction property of CPA networks under fixed activation patterns, AffineLens supports a broad class of modern components, including batch normalization, pooling, residual connections, multilayer perceptrons, and convolutional layers. Finally, we use AffineLens to perform a systematic empirical study of architectural expressivity, comparing networks through region complexity metrics and revealing how design choices influence the geometry of learned functions.

</details>


### [275] [Memory Inception: Latent-Space KV Cache Manipulation for Steering LLMs](https://arxiv.org/abs/2605.06225)
*Andy Zeyi Liu,Michael Zhang,Ilana Greenberg,Adam Alnasser,Lucas Baker,John Sous*

Main category: cs.LG

TL;DR: Memory Inception (MI) is a training-free steering method that inserts text-derived key-value banks selectively in latent attention space, achieving better control-drift trade-off than prompting or activation steering while using far less KV storage.


<details>
  <summary>Details</summary>
Motivation: Existing methods for steering large language models have limitations: prompting provides strong control but clutters interactions and caches guidance tokens at every layer, while activation steering is compact but weaker and doesn't support large structured reminders.

Method: Memory Inception steers LLMs in latent attention space by inserting text-derived key-value banks only at selected layers, treating steering as selective KV allocation. It injects latent slots only where the model routes to them, avoiding materializing reminder content throughout the prompt cache.

Result: On personality-steering tasks: best overall control-drift trade-off, competitive with prompting while consistently outperforming CAA. On updateable guidance: supports mid-conversation behavior shifts without rewriting visible transcript, achieving highest post-shift alignment on Qwen3. On structured reasoning: outperforms visible prompting on HARDMath and PHYSICS (10/12 subject×mode cells) while cutting content-matched KV storage by up to 118×.

Conclusion: Memory Inception positions itself as a powerful steering method when guidance needs to be persistent, structured, or expensive to keep in the visible transcript, offering an effective alternative to both prompting and activation steering approaches.

Abstract: Steering large language models (LLMs) is usually done by either instruction prompting or activation steering. Prompting often gives strong control, but caches guidance tokens at every layer and can clutter long interactions; activation steering is compact but typically weaker and does not support large structured reminders. We introduce memory inception (MI), a training-free method that steers in latent attention space by inserting text-derived key-value (KV) banks only at selected layers. Rather than materializing reminder content throughout the prompt cache, MI treats steering as selective KV allocation, injecting latent slots only where the model routes to them. On matched personality-steering tasks, MI gives the best overall control--drift trade-off, remaining competitive with prompting while consistently outperforming CAA. On updateable guidance, MI supports mid-conversation behavior shifts without rewriting the visible transcript, achieving the highest post-shift alignment on Qwen3. On structured reasoning, MI outperforms visible prompting on HARDMath and PHYSICS (10/12 subject$\times$mode cells), serving as proxies for structured reasoning in verifiable domains, while cutting content-matched KV storage by up to 118$\times$. These results position MI as a powerful steering method when guidance is persistent, structured, or expensive to keep in the visible transcript.

</details>


### [276] [Soft Deterministic Policy Gradient with Gaussian Smoothing](https://arxiv.org/abs/2605.06228)
*Hyunjun Na,Donghwan Lee*

Main category: cs.LG

TL;DR: Soft DDPG: A smoothed Bellman equation approach using Gaussian smoothing to handle non-differentiable Q-functions in deterministic policy gradient methods, enabling stable learning with sparse/discrete rewards.


<details>
  <summary>Details</summary>
Motivation: Standard deterministic policy gradient (DPG) requires differentiability of the critic w.r.t. actions, which is violated in practical control problems with sparse/discrete rewards, causing ill-defined gradients and unstable learning.

Method: Proposes a smoothed Bellman equation via Gaussian smoothing, defining a novel action-value function and deriving soft deterministic policy gradient (Soft-DPG) that eliminates explicit dependence on critic action-gradients.

Result: Soft DDPG remains competitive in dense-reward settings and provides clear gains in most discretized-reward environments where standard DDPG struggles with irregular critic landscapes.

Conclusion: The proposed framework offers a principled alternative to DPG that handles non-smooth Q-functions, enabling stable learning in practical control problems with sparse or discrete rewards.

Abstract: Deterministic policy gradient (DPG) is widely utilized for continuous control; however, it inherently relies on the differentiability of the critic with respect to the action during policy updates. This assumption is violated in practical control problems involving sparse or discrete rewards, leading to ill-defined policy gradients and unstable learning. To address these challenges, we propose a principled alternative based on a smoothed Bellman equation formulated via Gaussian smoothing. Specifically, we define a novel action-value function based on a smoothed Bellman equation and derive the soft deterministic policy gradient (Soft-DPG). Our formulation eliminates explicit dependence on critic action-gradients and ensures that the gradient remains well-defined even for non-smooth Q-functions. We instantiate this framework into a deep reinforcement learning algorithm, which we call soft deep deterministic policy gradient (Soft DDPG). Empirical evaluations on standard continuous control benchmarks and their discretized-reward variants show that Soft DDPG remains competitive in dense-reward settings and provides clear gains in most discretized-reward environments, where standard DDPG is more sensitive to irregular critic landscapes.

</details>


### [277] [Band Together: Untargeted Adversarial Training with Multimodal Coordination against Evasion-based Promotion Attacks](https://arxiv.org/abs/2605.06238)
*Guanmeng Xian,Ning Yang,Philip S. Yu*

Main category: cs.LG

TL;DR: UAT-MC is a defense method for multimodal recommender systems that addresses evasion-based promotion attacks through untargeted adversarial training with gradient alignment across modalities.


<details>
  <summary>Details</summary>
Motivation: Multimodal recommenders using visual/textual signals are vulnerable to evasion-based promotion attacks, but existing defenses focus on single-modal settings and poisoning-based threats, leaving evasion-based threats underexplored.

Method: Proposes Untargeted Adversarial Training with Multimodal Coordination (UAT-MC), which treats all items as potential targets (addressing unknown targets in evasion attacks) and introduces gradient alignment to synchronize perturbations across modalities.

Result: Extensive experiments show UAT-MC significantly improves robustness against promotion attacks while maintaining acceptable recommendation performance under the defense-accuracy trade-off.

Conclusion: UAT-MC effectively addresses cross-modal gradient mismatch in evasion attacks and provides a robust defense mechanism for multimodal recommender systems.

Abstract: Multimodal recommender systems exploit visual and textual signals to alleviate data sparsity, but this also makes them more vulnerable to evasion-based promotion attacks. Existing defenses are largely limited to single-modal settings and mainly focus on poisoning-based threats, leaving evasion-based threats underexplored. In this work, we first identify a cross-modal gradient mismatch under the multi-user promotion setting, where visual and textual perturbations are optimized in inconsistent directions due to the dominance of distinct user groups. This phenomenon dilutes the attack effectiveness and leads robust training to underestimate worst-case risks. To address this issue, we propose Untargeted Adversarial Training with Multimodal Coordination (UAT-MC). UAT-MC tackles the challenge of unknown targeted items in evasion-based attacks (as opposed to poisoning-based attacks) by treating all items as potential targets, and introduces a gradient alignment mechanism to explicitly correct this mismatch. This design ensures synchronized perturbations across modalities, thereby maximizing adversarial strength for robust training. Extensive experiments demonstrate that UAT-MC significantly improves robustness against promotion attacks while maintaining acceptable recommendation performance under the defense-accuracy trade-off. Code is available at https://github.com/gmXian/UAT-MC.

</details>


### [278] [When Graph Language Models Go Beyond Memorization](https://arxiv.org/abs/2605.06239)
*Masatsugu Yamada,Mahito Sugiyama*

Main category: cs.LG

TL;DR: Graph language models can learn structural regularities beyond memorization at large scale, primarily reproducing high-frequency patterns while struggling with rare patterns.


<details>
  <summary>Details</summary>
Motivation: It's unclear whether graph language models truly learn structural patterns or just memorize training graphs, as current aggregate fidelity metrics cannot distinguish between the two.

Method: Developed a calibrated diagnostic protocol combining frequent subgraph mining, graph-level bootstrap baseline, and three-level frequency stratification to disentangle memorization from structural alignment.

Result: At small scale, model performance is indistinguishable from memorization, but at large scale (3.75M graphs), verbatim memorization drops sharply while structural alignment remains high. High-frequency patterns are well reproduced across scales, while rare patterns remain poorly covered and improve only marginally with increased capacity.

Conclusion: Graph language models can acquire genuine structural regularities beyond mere memorization, particularly at large scale and for high-frequency patterns, though their ability to capture rare patterns remains limited.

Abstract: It remains unclear whether graph language models learn structural regularities or merely memorize training graphs; this cannot be resolved by current aggregate fidelity metrics alone. We develop a calibrated diagnostic protocol that combines frequent subgraph mining, a graph-level bootstrap baseline, and three-level frequency stratification to disentangle memorization from structural alignment. Using this framework, we show that graph language models can acquire structural regularities beyond memorization at scale, primarily in the high-frequency regime. This is supported by the following empirical evidence: On five TU benchmarks, LLaMA-style graph language models reach high subgraph-rank correlation, yet their alignment is matched or exceeded by the memorization bootstrap in most cases. At small scale, under our bootstrap diagnostic, fidelity is largely indistinguishable from verbatim recall. In contrast, at large scale with 3.75M graphs, verbatim memorization drops sharply while rank correlation remains near ceiling. Crucially, in a separate fixed-subsample analysis, frequent subgraph mining restricted to the novel-only subset closely tracks the corresponding all-generation Spearman correlation, providing evidence that the alignment is not driven solely by verbatim recall. Across all scales, high-frequency patterns are well reproduced, while rare patterns remain poorly covered, and this deficit narrows only marginally as capacity increases. We observe the same scale-dependent crossover under two distinct graph serializations (canonical DFS code and action sequences), providing evidence of robustness in our analysis.

</details>


### [279] [Cumulative-Goodness Free-Riding in Forward-Forward Networks: Real, Repairable, but Not Accuracy-Dominant](https://arxiv.org/abs/2605.06240)
*Amirhossein Yousefiramandi*

Main category: cs.LG

TL;DR: The paper analyzes "layer free-riding" in Forward-Forward training, where later layers inherit tasks already solved by earlier layers due to cumulative goodness metrics, and proposes local remedies that dramatically improve layer separation statistics without significant accuracy impact.


<details>
  <summary>Details</summary>
Motivation: The motivation is to address the phenomenon of "layer free-riding" in Forward-Forward training where under cumulative-goodness variants, later layers can inherit tasks that earlier layers have already partially solved, leading to exponential decay of class-discrimination gradients reaching deeper layers.

Method: The paper formalizes the layer free-riding phenomenon mathematically and studies three local remedies: per-block, hardness-gated, and depth-scaled approaches. These methods recover current-layer separation measures without relying on backpropagated gradients. Experiments are conducted on CIFAR-10, CIFAR-100, and Tiny ImageNet datasets.

Result: The remedies dramatically improve layer-separation statistics with 4×–45× gains in deeper layers, while changing accuracy by less than one percentage point. Tiny ImageNet reveals the same qualitative gap between layer-health diagnostics and final accuracy. Calibration experiments show architecture and augmentation choices have larger effects on final accuracy than the training-rule modifications.

Conclusion: Cumulative free-riding is a real and repairable optimization pathology in Forward-Forward training, but for the studied training rules, architectures, and datasets, it is not the dominant factor limiting achievable accuracy.

Abstract: Forward-Forward (FF) training allows each layer to learn from a local goodness criterion. In cumulative-goodness variants, however, later layers can inherit a task that earlier layers have already partially separated. We formalize this phenomenon as layer free-riding: under the softplus FF criterion, the class-discrimination gradient reaching block $d$ decays exponentially with the positive margin accumulated by preceding blocks. We then study three local remedies -- per-block, hardness-gated, and depth-scaled -- that recover current-layer separation measures without relying on backpropagated gradients. On CIFAR-10 and CIFAR-100, these remedies dramatically improve layer-separation statistics, with $4\times$--$45\times$ gains in deeper layers, while changing accuracy by less than one percentage point for non-degenerate training procedures. Tiny ImageNet provides a tougher cross-dataset check for our selected block-wise configuration and reveals the same qualitative gap between layer-health diagnostics and final accuracy. Calibration experiments further show that architecture and augmentation choices have a larger effect on final accuracy than the training-rule modifications studied here. Cumulative free-riding is therefore a real and repairable optimization pathology. Nonetheless, for the FF training rules, architectures, and datasets we study, it is not the dominant factor limiting achievable accuracy.

</details>


### [280] [Structure-Preserving Gaussian Processes Via Discrete Euler-Lagrange Equations](https://arxiv.org/abs/2605.06246)
*Jan-Hendrik Ewering,Kathrin Flaßkamp,Niklas Wahlström,Thomas B. Schön,Thomas Seel*

Main category: cs.LG

TL;DR: Lagrangian Gaussian Processes (LGPs) enable learning physically consistent dynamics from discrete position data using forced Euler-Lagrange equations with Gaussian process conditioning, preserving geometric structure and preventing energy drift.


<details>
  <summary>Details</summary>
Motivation: Many practical applications (like motion capture or visual servoing) provide only discrete position measurements without access to velocities or momenta, making existing methods inapplicable. Existing approaches often fail to preserve the geometric structure of dynamics, leading to erroneous energy drift and unstable long-term predictions.

Method: LGPs use linear operators for Gaussian process conditioning constructed from discrete forced Euler-Lagrange equations and variational discretization schemes. The approach preserves the geometric structure of the Lagrange-d'Alembert principle by construction when no external forces are present.

Result: LGPs demonstrate data-efficiency and generalization capabilities in synthetic and real-world case studies, including a real-world soft robot with hysteresis. They learn physically consistent dynamics with uncertainty quantification solely from sparse positional data and enable stable long-term predictions.

Conclusion: The proposed LGPs overcome limitations of prior work by enabling learning from discrete position snapshots while preserving physical consistency, preventing energy drift, and providing stable long-term predictions with uncertainty quantification, making them suitable for practical applications where only position measurements are available.

Abstract: In this paper, we propose Lagrangian Gaussian Processes (LGPs) for probabilistic and data-efficient learning of dynamics via discrete forced Euler-Lagrange equations. Importantly, the geometric structure of the Lagrange-d'Alembert principle, which governs the motion of dynamical systems, is preserved by construction in the absence of external forces. This allows learning physically consistent models that overcome erroneous drift in the system's energy, thereby providing stable long-term predictions. At the core of our approach lie linear operators for Gaussian process conditioning, constructed from discrete forced Euler-Lagrange equations and variational discretization schemes. Thereby and unlike prior work, the method enables learning dynamics from discrete position snapshots, i.e., without access to a system's velocities or momenta. This is particularly relevant for a large class of practical scenarios where only position measurements are available, for instance, in motion capture or visual servoing applications. We demonstrate the data-efficiency and generalization capabilities of the LGPs in various synthetic and real-world case studies, including a real-world soft robot with hysteresis. The experimental results underscore that the LGPs learn physically consistent dynamics with uncertainty quantification solely from sparse positional data and enable stable long-term predictions.

</details>


### [281] [The Role of Node Features in Graph Pooling](https://arxiv.org/abs/2605.06250)
*Jan von Pichowski,Alžbeta Hrabošová,Ingo Scholtes,Christopher Blöcker*

Main category: cs.LG

TL;DR: Graph pooling often shows inconsistent benefits because it requires node features to be well-aligned with graph topology, a condition that's often missing in real-world networks.


<details>
  <summary>Details</summary>
Motivation: The paper is motivated by the empirical observation that graph pooling methods often show marginal or inconsistent performance gains compared to standard WL-1 expressive GNNs in graph classification tasks. There's a need to understand why pooling sometimes works and sometimes doesn't, particularly examining the interaction between node features and graph topology.

Method: The authors conduct theoretical analysis to study the interaction between node features and graph topology and their effect on pooling objectives. They formalize fundamental requirements for node features to enable effective pooling and introduce a quantitative measure of feature quality. They then empirically evaluate these requirements on appropriate datasets.

Result: The analysis reveals that pooling operators require node features that are well-aligned with the graph's topology - a condition that is often overlooked and not guaranteed in empirical networks. The empirical evaluation shows that when these feature quality requirements are satisfied, pooling can indeed be beneficial and improve performance.

Conclusion: The paper concludes that the effectiveness of graph pooling depends critically on having node features that are properly aligned with graph topology. When this alignment condition is met, pooling methods can provide meaningful benefits, explaining the inconsistent empirical performance observed in practice and providing guidance for when to use pooling techniques.

Abstract: Graph pooling is commonly applied in graph classification, yet its empirical gains over standard WL-1 expressive GNNs are often marginal or inconsistent. We study this gap by analysing the interaction between node features and graph topology and their effect on pooling objectives. Our analysis reveals that pooling operators require node features that are well-aligned with the graph's topology -- a condition often overlooked and not guaranteed in empirical networks. We formalise fundamental requirements for node features to enable effective pooling, and introduce a quantitative measure of feature quality. Our empirical evaluation shows that, when these requirements are satisfied, pooling can be beneficial and improve performance on appropriate datasets.

</details>


### [282] [The Weight Gram Matrix Captures Sequential Feature Linearization in Deep Networks](https://arxiv.org/abs/2605.06258)
*Taehun Cha,Daniel Beaglehole,Adityanarayanan Radhakrishnan,Donghun Lee*

Main category: cs.LG

TL;DR: The paper proposes a feature-centric framework for analyzing neural network training through the Feature Learning Equation and Virtual Covariance, showing that deep networks sequentially transform representations toward linear alignment with targets.


<details>
  <summary>Details</summary>
Motivation: Understanding how deep neural networks learn representations remains a central challenge in machine learning theory. The paper aims to provide a theoretical framework to analyze feature evolution during training.

Method: Proposes a feature-centric framework with a Feature Learning Equation that relates weight updates to feature evolution, identifies weight Gram matrix as key for feature dynamics, introduces Virtual Covariance to characterize representation evolution, and defines Target Linearity measure to quantify linear alignment between features and targets.

Result: Shows that gradient descent implicitly induces hypothetical feature evolution, deep networks learn to sequentially transform representations toward target-linear structure, and this linearization perspective provides unified interpretation of empirical phenomena like Neural Collapse and linear interpolation in generative models.

Conclusion: The proposed feature-centric framework with Feature Learning Equation and Virtual Covariance provides new theoretical insights into how deep networks learn representations, revealing that networks systematically linearize representations during training and offering unified explanations for several observed empirical phenomena.

Abstract: Understanding how deep neural networks learn representations remains a central challenge in machine learning theory. In this work, we propose a feature-centric framework for analyzing neural network training by relating weight updates to feature evolution. We introduce a simple identity, the Feature Learning Equation, which identifies the weight Gram matrix as the key object capturing feature dynamics. This enables us to interpret gradient descent as implicitly inducing a hypothetical evolution of features, whose covariance structure - termed the Virtual Covariance - characterizes how representations evolve during training. Building on this perspective, we introduce Target Linearity, a measure quantifying the linear alignment between features and targets. By analyzing the training and layer-wise dynamics, we show that deep networks learn to sequentially transform representations toward target-linear structure. This linearization perspective provides a unified interpretation of several empirical phenomena, including Neural Collapse and linear interpolation in generative models.

</details>


### [283] [Trade-off Functions for DP-SGD with Subsampling based on Random Shuffling: Tight Upper and Lower Bounds](https://arxiv.org/abs/2605.06259)
*Marten van Dijk,Murat Bilgehan Ertan*

Main category: cs.LG

TL;DR: The paper provides tight analysis of differential privacy for DP-SGD with random shuffling subsampling within the f-DP framework, showing better privacy bounds than Poisson subsampling and addressing limitations of existing analyses.


<details>
  <summary>Details</summary>
Motivation: Current f-DP analyses for DP-SGD with Poisson subsampling yield non-transparent, implicit formulas that are hard to interpret. Random shuffling subsampling is a common practical approach that deserves a transparent, interpretable analysis with closed-form bounds.

Method: Derives tight analysis of trade-off functions for DP-SGD with random shuffling subsampling using the Berry-Esseen theorem for concrete bounds and introduces a new proof technique based on generalization of law of large numbers for asymptotic results.

Result: For random shuffling in regime σ≥√(3/ln M), obtains transparent closed-form bounds tight up to constants; shows M≈1.14×10^6 rounds with N≈1.14×10^7 samples achieves δ=1/100 privacy; asymptotic analysis shows E-fold composition yields δ dependency O(√E) rather than linear.

Conclusion: Random shuffling provides superior privacy analysis compared to Poisson subsampling, with interpretable bounds and better composition properties, though explicit convergence rates remain an open question for further research.

Abstract: We derive a tight analysis of the trade-off function for Differentially Private Stochastic Gradient Descent (DP-SGD) with subsampling based on random shuffling within the $f$-DP framework. Our analysis covers the regime $σ\geq \sqrt{3/\ln M}$, where $σ$ is the noise multiplier and $M$ is the number of rounds within a single epoch. Unlike $f$-DP analyses for Poisson subsampling, which yield non-closed implicit formulas that can be machine computed but are non-transparent, random shuffling admits a tight analysis yielding transparent and interpretable closed-form bounds. Our concrete bounds, derived via the Berry-Esseen theorem, are tight up to constant factors within the proof framework. We demonstrate worked parameter settings for a single epoch ($E=1$) with a corresponding trade-off function $\geq 1-a-δ$, that is, only $δ$ below the ideal random guessing diagonal $1-a$: For $δ= 1/100$ and $σ= 1$, roughly $M \approx 1.14\times 10^6$ rounds and $N \approx 1.14\times 10^7$ training samples suffice to achieve meaningful differential privacy. This is in contrast to recent negative results for the regime $σ\leq 1/\sqrt{2 \ln M}$. Our concrete bounds can be composed over multiple epochs leading to $δ$ having a linear in $E$ dependency, which restricts $E=O(\sqrt{M})$. To go beyond Berry--Esseen, we introduce a new proof technique based on a generalization of the law of large numbers that yields an asymptotic random guessing diagonal-limit result: if $E=c_M^2M$ with $c_M\to 0$, then the $E$-fold composed trade-off function satisfies $f^{\otimes E}(a)\to 1-a$ uniformly in $a\in[0,1]$ with $δ$ having only an $O(\sqrt{E})$ dependency. We compare this asymptotic regime with the corresponding Poisson subsampling asymptotic, and highlight the characterization of explicit convergence rates as an open question.

</details>


### [284] [A Note on TurboQuant and the Earlier DRIVE/EDEN Line of Work](https://arxiv.org/abs/2604.18555)
*Ran Ben-Basat,Yaniv Ben-Itzhak,Gal Mendelson,Michael Mitzenmacher,Amit Portnoy,Shay Vargaftik*

Main category: cs.LG

TL;DR: TurboQuant is a suboptimal special case of the earlier EDEN quantization scheme: TurboQuant_mse uses fixed scaling (S=1) vs EDEN's optimized scaling, and TurboQuant_prod uses inferior residual quantization compared to unbiased EDEN.


<details>
  <summary>Details</summary>
Motivation: To clarify the relationship between recent TurboQuant quantization work and earlier EDEN/DRIVE schemes, showing that TurboQuant is actually a special case of EDEN with suboptimal parameter choices.

Method: Theoretical analysis comparing mathematical formulations: (1) Showing TurboQuant_mse = EDEN with fixed S=1 scaling, (2) Analyzing TurboQuant_prod's composition of biased (b-1)-bit EDEN and 1-bit QJL residual quantization, (3) Identifying shared analysis techniques (random rotations, Beta distributions, Lloyd-Max algorithm, Randomized Hadamard Transforms).

Result: Experimental results show: (1) Biased EDEN with optimized S outperforms TurboQuant_mse, (2) Unbiased EDEN substantially outperforms TurboQuant_prod (e.g., 2-bit EDEN beats 3-bit TurboQuant_prod), (3) EDEN outperforms TurboQuant in all tested setups from the TurboQuant paper.

Conclusion: TurboQuant is a suboptimal simplification of the more general and powerful EDEN quantization framework, with EDEN consistently providing superior quantization accuracy through optimized scaling parameters and more effective quantization strategies.

Abstract: This note clarifies the relationship between the recent TurboQuant work and the earlier DRIVE (NeurIPS 2021) and EDEN (ICML 2022) schemes. DRIVE is a 1-bit quantizer that EDEN extended to any $b>0$ bits per coordinate; we refer to them collectively as EDEN.
  First, TurboQuant$_{\text{mse}}$ is a special case of EDEN obtained by fixing EDEN's scalar scale parameter to $S=1$. EDEN supports both biased and unbiased quantization, each optimized by a different $S$ (chosen via methods described in the EDEN works). The fixed choice $S=1$ used by TurboQuant is generally suboptimal, although the optimal $S$ for biased EDEN converges to $1$ as the dimension grows; accordingly TurboQuant$_{\text{mse}}$ approaches EDEN's behavior for large $d$.
  Second, TurboQuant$_{\text{prod}}$ combines a biased $(b-1)$-bit EDEN step with an unbiased 1-bit QJL quantization of the residual. It is suboptimal in three ways: (1) its $(b-1)$-bit step uses the suboptimal $S=1$; (2) its 1-bit unbiased residual quantization has worse MSE than (unbiased) 1-bit EDEN; (3) chaining a biased $(b-1)$-bit step with a 1-bit unbiased residual step is inferior to unbiasedly quantizing the input directly with $b$-bit EDEN.
  Third, some of the analysis in the TurboQuant work mirrors that of the EDEN works: both exploit the connection between random rotations and the shifted Beta distribution, use the Lloyd-Max algorithm, and note that Randomized Hadamard Transforms can replace uniform random rotations.
  Experiments support these claims: biased EDEN (with optimized $S$) is more accurate than TurboQuant$_{\text{mse}}$, and unbiased EDEN is markedly more accurate than TurboQuant$_{\text{prod}}$, often by more than a bit (e.g., 2-bit EDEN beats 3-bit TurboQuant$_{\text{prod}}$). We also repeat all accuracy experiments from the TurboQuant paper, showing that EDEN outperforms it in every setup we have tried.

</details>


### [285] [Beyond Rigid Alignment: Graph Federated Learning via Dual Manifold Calibration](https://arxiv.org/abs/2605.06260)
*Wentao Yu,Bo Han,Jie Yang,Chen Gong*

Main category: cs.LG

TL;DR: FedGMC addresses graph heterogeneity in federated learning through a dual manifold calibration approach that preserves global commonalities while maximizing personalized local representation spaces, outperforming existing methods.


<details>
  <summary>Details</summary>
Motivation: Existing GFL methods rely on rigid alignment of model parameters/prototypes under a restrictive global linearity assumption, which compresses personalized representation spaces and fails to preserve diverse local graph distributions.

Method: FedGMC introduces a dual manifold calibration mechanism: (1) For semantic heterogeneity, server constructs geometrically optimal semantic manifold via equidistant semantic anchors to calibrate local semantic manifolds; (2) For structural heterogeneity, server builds global structural manifold via structural templates to calibrate local structural manifolds; (3) Server dynamically refines both global manifolds by aggregating local manifolds.

Result: Extensive experiments on eleven homophilic and heterophilic graphs demonstrate FedGMC effectively balances global commonality and local personalization, significantly outperforming state-of-the-art baseline methods.

Conclusion: FedGMC provides a novel manifold perspective to tackle graph heterogeneity in federated learning, enabling collaborative representation learning while preserving personalized representation spaces and local graph distributions.

Abstract: Graph Federated Learning (GFL) enables collaborative representation learning across distributed subgraphs while preserving privacy. However, heterogeneity remains a critical challenge, as subgraphs across clients typically differ significantly in both semantics and structures. Existing methods address heterogeneity by enforcing the rigid alignment of model parameters or prototypes between clients and the server. However, these alignments implicitly rely on a restrictive global linearity assumption that summarizes local data distributions using a single and globally consistent representation space. This severely compresses the personalized representation space of clients and fails to preserve diverse local graph distributions. To overcome these limitations, we propose Federated Graph Manifold Calibration (FedGMC), a novel paradigm that tackles semantic heterogeneity and structural heterogeneity from a unified manifold perspective. Instead of enforcing rigid alignment, FedGMC introduces a dual manifold calibration mechanism that preserves global commonalities while maximizing the personalized representation space of local clients. Specifically, for semantic heterogeneity, the server constructs a geometrically optimal semantic manifold via equidistant semantic anchors, so as to guide the calibration of local semantic manifolds. For structural heterogeneity, the server constructs a global structural manifold by building global structural templates, so as to guide the calibration of local structural manifolds. Finally, the server dynamically refines both global semantic manifolds and structural manifolds by aggregating local manifolds. Extensive experiments on eleven homophilic and heterophilic graphs demonstrate that FedGMC effectively balances global commonality and local personalization, thereby significantly outperforming state-of-the-art baseline methods.

</details>


### [286] [Inference-Time Refinement Closes the Synthetic-Real Gap in Tabular Diffusion](https://arxiv.org/abs/2605.06261)
*Eugenio Lomurno,Filippo Balzarini,Francesco Benelle,Francesca Pia Panaccione,Matteo Matteucci*

Main category: cs.LG

TL;DR: TARDIS is an inference-time refinement framework for tabular diffusion models that improves synthetic data quality by 8.6% over real data using score-level guidance and sample selection, without retraining the backbone model.


<details>
  <summary>Details</summary>
Motivation: Current diffusion-based tabular generators rarely exceed real-data utility, and improvements have focused on training-time modifications. Inference-time refinement of pre-trained models remains unexplored for tabular synthesis, creating an opportunity to close the synthetic-real gap without retraining.

Method: Uses a frozen pre-trained diffusion backbone with Tree-structured Parzen Estimator search over score-level guidance during reverse diffusion. Features Bidirectional Chamfer Refinement (BCR) pattern minimizing synthetic-real distance both continuously via gradient guidance and discretely via batch-ranking selection. Includes optional soft-label distillation and automated configuration search per dataset.

Result: Achieves median +8.6% downstream-task improvement over real data across 15 benchmarks (11/15 strict wins), improves over TabDiff backbone on all datasets (mean +12.9%). Matches backbone on manifold fidelity, diversity, and privacy. Runs in 1-80 minutes on consumer GPU.

Conclusion: Inference-time refinement of pre-trained diffusion models can exceed real-data utility for tabular synthesis, establishing BCR as a dominant refinement pattern and enabling efficient improvement without architectural changes or retraining.

Abstract: Diffusion-based generators set the current state of the art for synthetic tabular data. These methods approach but rarely exceed real-data utility, and closing this synthetic-real gap has so far been pursued exclusively at training time, via architectural advances, scaling, and retraining of monolithic generators. The inference-time alternative, i.e., refining the outputs of a pre-trained backbone with parameters left untouched, has remained largely unexplored for tabular synthesis. We introduce TARDIS (Tabular generation through Refinement, Distillation, and Inference-time Sampling), an inference-time refinement framework that operates on a frozen pre-trained backbone, configured per dataset by a Tree-structured Parzen Estimator search over score-level guidance during reverse diffusion, with each trial's objective set by an inner grid search over post-hoc sample selectors and an optional soft-label distillation step. The search space encodes a single mathematical pattern we name Bidirectional Chamfer Refinement (BCR): the symmetric Chamfer functional between synthetic and real samples is minimized both continuously, via a score-level gradient, and discretely, via batch-ranking post-generation. The per-dataset search recovers BCR-aligned configurations on most datasets, evidence for BCR as the dominant refinement pattern. Across 15 binary, multiclass, and regression benchmarks TARDIS achieves a median +8.6% downstream-task improvement over models trained on real data (95% CI [+3.3, +16.4], Wilcoxon p=0.016, 11/15 strict wins) and improves over the TabDiff backbone on all 15 datasets (mean +12.9%, p<10^-4), matching the backbone on manifold fidelity, diversity, and sample-level privacy. Inference-time refinement of a pre-trained tabular diffusion backbone reaches and exceeds real-data utility in 1 to 80 minutes on a single consumer-grade GPU.

</details>


### [287] [Can Attribution Predict Risk? From Multi-View Attribution to Planning Risk Signals in End-to-End Autonomous Driving](https://arxiv.org/abs/2605.06264)
*Le Yang,Ruoyu Chen,Haijun Liu,Jiawei Liang,ShangQuan Sun,Xiaochun Cao*

Main category: cs.LG

TL;DR: Hierarchical attribution framework for end-to-end autonomous driving models that identifies critical visual evidence and extracts attribution statistics to predict planning risks.


<details>
  <summary>Details</summary>
Motivation: End-to-end autonomous driving models generate opaque decisions with hard-to-localize risks. Existing explanation methods are decoupled from planning and don't reveal visual evidence. Attribution methods for image classification don't directly apply to multi-view, continuous trajectory planning. The relationship between attribution maps and risk identification is underexplored.

Method: Proposes a hierarchical attribution framework: 1) Uses L2 consistency with original trajectory as objective, 2) Implements coarse-to-fine region attribution strategy that searches candidate regions across six-view input and refines attribution within them, 3) Extracts three attribution statistics: attribution entropy (measure of planner's reliance concentration), within-camera spatial variance (attribution spread within each view), and cross-camera Gini coefficient (distribution unevenness across cameras).

Result: Attribution statistics correlate with planning risk: Spearman correlations of 0.30±0.07 with trajectory error and AUROC of 0.77±0.04 for collision detection. The signal generalizes to held-out scenes with negligible degradation and remains stable under alternative attribution baselines. Experiments conducted on BridgeAD, UniAD, and GenAD datasets.

Conclusion: The hierarchical attribution framework successfully reveals visual evidence behind trajectory generation in end-to-end autonomous driving models and demonstrates that attribution statistics can effectively predict planning risks, offering a valuable tool for improving transparency and safety in autonomous driving systems.

Abstract: End-to-end autonomous driving models generate future trajectories from multi-view inputs, improving system integration but introducing opaque decisions and hard-to-localize risks. Existing methods either rely on auxiliary monitoring models or generate textual explanations, but are decoupled from the planning process and fail to reveal the visual evidence underlying trajectory generation. While attribution offers a direct alternative, planning differs from image classification by taking six-view camera images as input and predicting continuous multi-step trajectories, requiring attribution to capture both critical views and regions and their influence on outputs. Moreover, whether attribution maps can support risk identification remains underexplored. To address this, we propose a hierarchical attribution framework for end-to-end planning. Specifically, using L2 consistency with the original trajectory as the objective, we design a coarse-to-fine region attribution strategy that searches candidate regions across the full six-view input and refines attribution within them. We further extract three attribution statistics as predictive signals for planning risk, including attribution entropy to measure how concentrated the planner's reliance is over the joint visual space, within-camera spatial variance to characterize how spread out the attribution is within each view, and cross-camera Gini coefficient to quantify how unevenly attribution is distributed across the six cameras. Experiments on BridgeAD, UniAD, and GenAD show that these statistics correlate with planning risk, achieving Spearman correlations of $0.30 \pm 0.07$ with trajectory error and AUROC of $0.77 \pm 0.04$ for collision detection. The signal generalizes to held-out scenes with negligible degradation and remains stable under an alternative attribution baseline.

</details>


### [288] [A Flow Matching Algorithm for Many-Shot Adaptation to Unseen Distributions](https://arxiv.org/abs/2605.06272)
*Tyler Ingebrand,Ruihan Zhao,Kushagra Gupta,David Fridovich-Keil,Sandeep P. Chinchali,Ufuk Topcu*

Main category: cs.LG

TL;DR: FP-FM enables generative models to adapt to new distributions at inference time by projecting target distribution samples onto a learned basis of velocity fields, achieving strong performance gains especially on unseen distributions.


<details>
  <summary>Details</summary>
Motivation: Current generative modeling has succeeded in tasks like natural language-conditioned image generation, but adapting models to example data points remains challenging and underexplored. There's a need for efficient model adaptation to diverse target distributions without extensive retraining.

Method: Proposed Function Projection for Flow Matching (FP-FM), which learns basis functions to span velocity fields corresponding to training distributions. At inference, adapts to new distributions by computing simple least-squares projection onto this basis. Introduced variants with time-dependent coefficients to trade off expressivity and compute efficiency.

Result: FP-FM achieves greatly improved precision and recall relative to baselines across synthetic and image-based datasets, with especially strong gains on unseen distributions. Enables efficient generation from diverse target distributions without additional training at inference time.

Conclusion: FP-FM provides an effective approach for generative model adaptation that can efficiently handle diverse target distributions through simple projection operations, addressing a previously challenging problem in the field with strong empirical results.

Abstract: While generative modeling has achieved remarkable success on tasks like natural language-conditioned image generation, enabling model adaptation from example data points remains a relatively underexplored and challenging problem. To this end, we propose Function Projection for Flow Matching (FP-FM), an algorithm that directly conditions generation on samples from the target distribution. FP-FM learns basis functions to span the velocity fields corresponding to a set of training distributions, and adapts to new distributions by computing a simple least-squares projection onto this basis. This enables efficient generation of samples from diverse target distributions without additional training at inference time. We further introduce multiple variants of FP-FM that provide a trade-off in expressivity and compute by enriching the coefficient calculation, e.g., by making the coefficients dependent on time. FP-FM achieves greatly improved precision and recall relative to baselines across synthetic and image-based datasets, with especially strong gains on unseen distributions.

</details>


### [289] [When Labels Have Structure: Improving Image Classification with Hierarchy-Aware Cross-Entropy](https://arxiv.org/abs/2605.06274)
*April Chan,Davide D'Ascenzo,Sebastiano Cultrera di Montesano*

Main category: cs.LG

TL;DR: HACE is a drop-in replacement for standard cross-entropy that incorporates class hierarchy information through prediction aggregation and ancestral label smoothing, consistently outperforming standard cross-entropy across multiple datasets and architectures.


<details>
  <summary>Details</summary>
Motivation: Standard cross-entropy treats all misclassifications equally, ignoring the semantic distances encoded in class hierarchies, which is a limitation for tasks where some misclassifications are more semantically plausible than others.

Method: HACE combines two key components: 1) prediction aggregation - propagates probability mass upward through the class hierarchy so parent nodes accumulate confidence of their children, and 2) ancestral label smoothing - distributes ground-truth signal along the path from the true class to the root.

Result: In end-to-end training across 18 architecture-dataset pairs, HACE improves accuracy over standard cross-entropy in 15 cases with mean gain of 4.66%. In linear probing on frozen DINOv2-Large features, HACE outperforms all competing methods on all three datasets with mean improvement of 2.18% over next best baseline.

Conclusion: HACE effectively incorporates class hierarchy information into the classification loss, consistently improving performance over standard cross-entropy across different training regimes and architectures, demonstrating the value of hierarchical information in classification tasks.

Abstract: Standard cross-entropy is the default classification loss across virtually all of machine learning, yet it treats all misclassifications equally, ignoring the semantic distances that a class hierarchy encodes. We propose Hierarchy-Aware Cross-Entropy (HACE), a drop-in replacement for standard cross-entropy that incorporates a known class hierarchy directly into the loss. HACE combines two components: prediction aggregation, which propagates the model's probability mass upward through the class hierarchy to ensure that parent nodes accumulate the confidence of their children; and ancestral label smoothing, which distributes the ground-truth signal along the path from the true class to the root. We evaluate HACE on CIFAR-100, FGVC Aircraft, and NABirds in two regimes: end-to-end training across six architectures spanning convolutional and attention-based designs, and linear probing on frozen DINOv2-Large features. In end-to-end training, HACE improves accuracy over standard cross-entropy in 15 out of 18 architecture--dataset pairs, with a mean gain of 4.66\%. In linear probing on frozen DINOv2-Large features, HACE outperforms all competing methods on all three datasets, with a mean improvement of 2.18\% over the next best baseline.

</details>


### [290] [PACE: Prune-And-Compress Ensemble Models](https://arxiv.org/abs/2605.06278)
*Fabian Akkerman,Julien Ferry,Théo Guyard,Thibaut Vidal*

Main category: cs.LG

TL;DR: PACE is a two-phase framework that first actively generates diverse learners then prunes the enriched ensemble, offering principled control over faithfulness to the original ensemble while reducing ensemble size.


<details>
  <summary>Details</summary>
Motivation: Ensemble models achieve state-of-the-art performance but require aggregating many weak learners, which hinders deployment, interpretability, and downstream tasks like robustness verification. Current approaches of pruning or compression alone have limitations.

Method: PACE interleaves pruning and compression in a two-phase strategy: 1) Active generation of new learners via theoretically grounded procedure to enhance ensemble diversity, 2) Pruning performed on this enriched ensemble when no more relevant learners can be found. Both operations allow fine control over faithfulness to the original ensemble.

Result: Experiments show that PACE outperforms prior pruning and compression methods while offering principled control of faithfulness guarantees.

Conclusion: PACE provides an effective framework for reducing ensemble complexity while maintaining performance and offering theoretical guarantees on faithfulness, addressing the limitations of existing approaches through its novel two-phase strategy.

Abstract: Ensemble models achieve state-of-the-art performance on prediction tasks, but usually require aggregating a large number of weak learners. This can hinder deployment, interpretability, and downstream tasks such as robustness verification. Remedies to this issue fall into two main camps: pruning, which discards redundant learners, and compression, which generates new ones from scratch. We introduce PACE, a framework that interleaves these paradigms in a two-phase strategy. First, new learners are actively generated via a theoretically grounded procedure to enhance the diversity of the initial ensemble. When no more relevant learners can be found, a second phase of pruning is performed on this enriched ensemble. During both operations, PACE allows fine control on the faithfulness to the original ensemble. Experiments show that our method outperforms prior pruning and compression methods while offering principled control of faithfulness guarantees.

</details>


### [291] [INEUS: Iterative Neural Solver for High-Dimensional PIDEs](https://arxiv.org/abs/2605.06281)
*Jean-Loup Dupret,Davide Gallon,Patrick Cheridito*

Main category: cs.LG

TL;DR: INEUS is a meshfree neural solver for partial integro-differential equations that uses single-jump sampling and recursive regression instead of explicit nonlocal integral evaluation, offering scalable high-dimensional performance.


<details>
  <summary>Details</summary>
Motivation: The paper aims to solve high-dimensional partial integro-differential equations (PIDEs) efficiently, addressing limitations of existing methods like PINNs which struggle with expensive nonlocal integral evaluations and PIDE residual differentiation.

Method: INEUS replaces explicit nonlocal jump integral evaluation with single-jump sampling and formulates PIDE solving as recursive regression problems. It's a meshfree neural solver that learns global solutions over space-time domains while avoiding expensive differentiation of full PIDE residuals.

Result: Numerical experiments demonstrate INEUS delivers accurate and scalable solutions for various high-dimensional linear and nonlinear PIDEs. The method is supported by a contraction-based convergence proof for linear PIDEs.

Conclusion: INEUS provides an efficient, scalable approach for solving high-dimensional PIDEs, particularly well-suited for problems where traditional methods like PINNs face computational challenges with nonlocal terms and residual differentiation.

Abstract: In this paper, we introduce INEUS, a meshfree iterative neural solver for partial integro-differential equations (PIDEs). The method replaces the explicit evaluation of nonlocal jump integrals with single-jump sampling and reformulates PIDE solving as a sequence of recursive regression problems. Like Physics-Informed Neural Networks (PINNs), INEUS learns global solutions over the entire space-time domain, yet it offers a more efficient treatment of nonlocal terms and avoids the computationally expensive differentiation of full PIDE residuals. These features make INEUS particularly well suited for high-dimensional PDEs and PIDEs. Supported by a contraction-based convergence proof for linear PIDEs, our numerical experiments show that INEUS delivers accurate and scalable solutions for various high-dimensional linear and nonlinear examples.

</details>


### [292] [Attributions All the Way Down? The Metagame of Interpretability](https://arxiv.org/abs/2605.06295)
*Hubert Baniecki,Przemyslaw Biecek,Fabian Fumagalli*

Main category: cs.LG

TL;DR: The metagame framework quantifies second-order interaction effects in model explanations by treating attribution methods as cooperative games and computing Shapley values to measure how features influence each other's attributions.


<details>
  <summary>Details</summary>
Motivation: Current explanation methods only provide first-order attributions (which features are important), but don't capture how features interact and influence each other's importance. There's a need to understand these second-order interactions for more comprehensive model interpretability.

Method: Treats any first-order attribution method φ(f) as a cooperative game, then computes Shapley values to measure meta-attributions φ_{j→i}(f) - the directional influence of feature j on the attribution of feature i. This creates hierarchical decomposition of attributions into meta-attributions.

Result: Theoretical proofs show attributions decompose into meta-attributions, establishing them as directional extensions of existing interaction indices. Empirical demonstrations show insights in: (1) token interactions in instruction-tuned LLMs, (2) cross-modal similarity in vision-language encoders, and (3) text-to-image concepts in multimodal diffusion transformers.

Conclusion: The metagame framework successfully provides a systematic approach to quantify second-order feature interactions in model explanations, offering deeper interpretability insights across diverse applications including language models, vision-language systems, and multimodal diffusion models.

Abstract: We introduce the metagame, a conceptual framework for quantifying second-order interaction effects of model explanations. For any first-order attribution $φ(f)$ explaining a model $f$, we measure the directional influence of feature $j$ on the attribution of feature $i$, denoted as meta-attribution $\varphi_{j \to i}(f)$, by treating the attribution method itself as a cooperative game and computing its Shapley value. Theoretically, we prove that attributions hierarchically decompose into meta-attributions, and establish these as directional extensions of existing interaction indices. Empirically, we demonstrate that the metagame delivers insights across diverse interpretability applications: (i) quantifying token interactions in instruction-tuned language models, (ii) explaining cross-modal similarity in vision-language encoders, and (iii) interpreting text-to-image concepts in multimodal diffusion transformers.

</details>


### [293] [Region Seeding via Pre-Activation Regularization: A Geometric View from Piecewise Affine Nerual Networks](https://arxiv.org/abs/2605.06300)
*Yi Wei,Xuan Qi,Furao Shen*

Main category: cs.LG

TL;DR: The paper introduces a region-seeding regularizer that encourages neural networks with piecewise affine activations to create more local affine regions near data points during early training, improving both the realized expressive capacity and overall performance.


<details>
  <summary>Details</summary>
Motivation: Standard neural network training realizes far fewer affine region refinements than architectures could theoretically support. Existing region-count theory is architectural rather than optimization-focused, offering little guidance on how optimization shapes the realized partition near data points. The authors aim to bridge this gap between theoretical capacity and practical realization.

Method: The authors develop a theoretical condition showing that bringing neuron switching surfaces close to data points ensures intersection with local neighborhoods, increasing local affine-region count. Based on this theory, they propose a plug-and-play region-seeding regularizer that encourages early partitioning while allowing task-driven refinement to dominate later training.

Result: The regularizer increases the number of realized affine regions via exact enumeration, improves performance on toy datasets, enhances early-stage accuracy, and achieves comparable (or slightly improved) final accuracy on ImageNet-1k for classical models.

Conclusion: By providing a principled training-time mechanism for seeding data-relevant partitions early in optimization, the approach bridges the gap between theoretical expressive capacity and practical realization in networks with piecewise affine activations, offering improved performance and better utilization of architectural capacity.

Abstract: Deep networks with continuous piecewise affine activations induce polyhedral partitions of the input space, making the number of realized affine regions a natural measure of expressive capacity and a key determinant of how well the model can approximate nonlinear target functions. In practice, standard training realizes far fewer region refinements in data-visited neighborhoods than the architecture could in principle support, while existing region-count theory is primarily architectural and offers little guidance on how optimization shapes the realized partition near the data. Our theory provides a sufficient condition under which bringing neuron switching surfaces sufficiently close to data points ensures their intersection with local neighborhoods, which in turn implies a strict increase in the local affine-region count, yielding a principled training-time handle for seeding data-relevant partitions early in optimization. Guided by these results, we propose a plug-and-play region-seeding regularizer that encourages early partitioning while allowing task-driven refinement to dominate later in training. Experiments show that the regularizer increases the number of realized affine regions via exact enumeration and improves overall performance on toy datasets, while also improving early-stage accuracy and achieving comparable (or slightly improved) final accuracy on ImageNet-1k for classical models.

</details>


### [294] [Molecules Meet Language: Confound-Aware Representation Learning and Chemical Property Steering in Transformer-VAE Latent Spaces](https://arxiv.org/abs/2605.06303)
*Zakaria Elabid,Jan Andrzejewski,Bartosz Brzoza,Attila Cangi*

Main category: cs.LG

TL;DR: Study shows that chemical property steering can emerge in entangled molecular latent spaces, but requires confound-aware evaluation to separate real chemical signals from SELFIES representation artifacts.


<details>
  <summary>Details</summary>
Motivation: Molecular generative models assume meaningful latent geometry, but apparent property predictability might reflect sequence-level shortcuts (like SELFIES representation artifacts) rather than true chemical organization. Need to separate real chemical signals from representation confounds.

Method: Train unsupervised autoregressive Transformer-VAE on SELFIES, freeze model, fit linear probes to RDKit descriptors, use probe weights as steering directions. Introduce confound-aware evaluation with residualization, confound-direction alignment analysis, and decoded-molecule traversal to control for SELFIES artifacts.

Result: SELFIES length, branch tokens, ring tokens, and token entropy are strongly encoded in latent space. Under confound-aware evaluation, robust monotonic steering found for cLogP, FractionCSP3, HeavyAtomCount, TPSA, BertzCT, and HBA. Nonlinear probes show some properties have stable global directions while others better described by local gradients.

Conclusion: Chemically meaningful steering can emerge in entangled molecular latent spaces, but only when validated through decoded molecules and controlled for representation-level confounds. Proper evaluation is crucial to separate true chemical organization from representation artifacts.

Abstract: Molecular generative models often assume meaningful latent geometry, but apparent property predictability can reflect sequence-level shortcuts rather than chemical organization. We study this issue in an unsupervised autoregressive Transformer-VAE trained on SELFIES. After training, we freeze the model, fit linear probes to RDKit descriptors, and use the probe weights as candidate global steering directions. To separate chemical signal from SELFIES artifacts, we introduce a confound-aware evaluation based on residualization, confound-direction alignment analysis, and decoded-molecule traversal. This is necessary because SELFIES length, branch tokens, ring tokens, and token entropy are strongly encoded in the latent space. Under this confound-aware evaluation, we find robust monotonic steering for cLogP, FractionCSP3, HeavyAtomCount, TPSA, BertzCT, and HBA. Nonlinear probes further show that some properties admit stable global directions, while others are better described by local latent gradients. Overall, our results show that chemically meaningful steering can emerge in entangled molecular latent spaces, but only when validated through decoded molecules and controlled for representation-level confounds.

</details>


### [295] [Perceive, Route and Modulate: Dynamic Pattern Recalibration for Time Series Forecasting](https://arxiv.org/abs/2605.06310)
*Siru Zhong,Zhao Meng,Haohuan Fu,Haoyang Li,Qingsong Wen,Yuxuan Liang*

Main category: cs.LG

TL;DR: DPR introduces a lightweight mechanism for dynamic pattern recalibration in time series forecasting, enabling models to adapt to shifting local temporal patterns through token-level modulation, outperforming fixed-weight approaches.


<details>
  <summary>Details</summary>
Motivation: Current deep forecasting models use fixed weight matrices uniformly across all temporal tokens, creating a static pattern response that can't adapt to continuously shifting local temporal patterns in real-world time series, resulting in suboptimal average performance.

Method: Dynamic Pattern Recalibration (DPR) uses a lightweight "Perceive-Route-Modulate" pipeline to compute soft-routing distributions over learned basis patterns, generating time-aware modulation vectors that recalibrate hidden states via residual Hadamard product, functioning as a backbone-agnostic adapter.

Result: DPR enhances forecasting across diverse architectures with minimal overhead and achieves competitive performance across 12 benchmarks as a standalone model (DPRNet), validating dynamic recalibration against macroscopic parameter scaling.

Conclusion: DPR addresses a general bottleneck in time series forecasting by enabling dynamic adaptation to shifting local patterns, proving more effective than simply scaling model parameters, with practical applications as both an adapter and standalone model.

Abstract: Local temporal patterns in real-world time series continuously shift, rendering globally shared transformations suboptimal. Current deep forecasting models, despite their scale and complexity, rely on fixed weight matrices applied uniformly to all temporal tokens. This creates a static pattern response: models settle into a compromised average, unable to adapt to changing local dynamics. We introduce Dynamic Pattern Recalibration (DPR), a backbone-agnostic mechanism that resolves this via token-level recalibration. Through a lightweight "Perceive-Route-Modulate" pipeline, DPR computes a soft-routing distribution over a learned basis of adaptive response patterns, generating a time-aware modulation vector that recalibrates hidden states via a residual Hadamard product. As a backbone-agnostic adapter, DPR enhances forecasting across diverse architectures with minimal overhead, confirming it addresses a general bottleneck. As a minimalist standalone model, DPRNet achieves competitive performance across 12 benchmarks, validating dynamic recalibration against macroscopic parameter scaling.

</details>


### [296] [When Does $\ell_2$-Boosting Overfit Benignly? High-Dimensional Risk Asymptotics and the $\ell_1$ Implicit Bias](https://arxiv.org/abs/2605.06314)
*Ye Su,Jian Li,Yong Liu*

Main category: cs.LG

TL;DR: ℓ₂-Boosting in high dimensions suffers logarithmic-rate benign overfitting failure due to greedy noise localization, but early stopping achieves minimax-optimal rates for ℓ₁-bounded signals.


<details>
  <summary>Details</summary>
Motivation: Understanding benign overfitting under ℓ₁ implicit bias of greedy ensembles, where standard spectral methods fail due to non-linear coupling of coordinate selection thresholds.

Method: Analyze continuous-time ℓ₂-Boosting risk using Convex Gaussian Minimax Theorem coupled with asymptotic expansions of double-sided truncated Gaussian moments to resolve the non-smooth ℓ₁ interpolant.

Result: Under isotropic pure-noise: benign overfitting fails at linear rate with logarithmic excess variance decay Θ(σ²/log(p/n)). For spiked-isotropic designs: risk converges to zero when r₂ ≫ n but at slow logarithmic rate Θ(σ²/log(r₂/n)). Early stopping rule achieves minimax-optimal empirical prediction rates.

Conclusion: ℓ₁ implicit bias causes slow logarithmic benign overfitting, but tuning-free early stopping recovers Lasso performance bounds and attains optimal rates for ℓ₁-bounded signals.

Abstract: Benign overfitting is well-characterized in $\ell_2$ geometries, but its behavior under the $\ell_1$ implicit bias of greedy ensembles remains challenging. The analytical barrier stems from the non-linear coupling of coordinate selection thresholds, which invalidates standard spectral resolvent tools. To isolate this algorithmic bias, we characterize the high-dimensional risk of continuous-time $\ell_2$-Boosting over $p$ features and $n$ samples. By coupling the Convex Gaussian Minimax Theorem with delicate asymptotic expansions of double-sided truncated Gaussian moments, we analytically resolve the non-smooth $\ell_1$ interpolant. Under an isotropic pure-noise model, we prove that benign overfitting fails at the linear rate: greedy selection localizes noise into sparse active sets, and the excess variance decays at a logarithmic rate $Θ(σ^2/\log(p/n))$ for noise variance $σ^2$. We remark that while this localization mechanism should persist in the presence of signals, the exact signal-noise decomposition remains an open problem. For spiked-isotropic designs with $k^*$ head eigenvalues and $r_2 = p - k^*$ tail dimensions, the risk converges to zero when $r_{2} \gg n$, but only at a logarithmic rate $Θ(σ^2/\log(r_2/n))$, which is slower than the linear decay observed in $\ell_2$ geometries. To avoid this slow convergence, we analyze the non-smooth subdifferential dynamics of the boosting flow. This yields a tuning-free early stopping rule that, under a bounded $\ell_1$-path condition, recovers the Lasso basic inequality and attains the minimax-optimal empirical prediction rate for $\ell_1$-bounded signals.

</details>


### [297] [Pro-KLShampoo: Projected KL-Shampoo with Whitening Recovered by Orthogonalization](https://arxiv.org/abs/2605.06316)
*Ruotong Sun,Ermin Wei*

Main category: cs.LG

TL;DR: Pro-KLShampoo improves KL-Shampoo by exploiting the spike-and-flat eigenvalue structure in Kronecker preconditioners through parametric subspace restriction and orthogonalization, achieving better performance in LLM pre-training.


<details>
  <summary>Details</summary>
Motivation: Current LLM optimizer development follows two isolated paths: explicit Kronecker-factored preconditioning (KL-Shampoo) and orthogonalization methods (Muon). The authors observe that KL-Shampoo's Kronecker preconditioners exhibit a characteristic "spike-and-flat" eigenvalue structure across layers and training stages, which suggests an opportunity to combine the strengths of both approaches.

Method: The method restricts one of KL-Shampoo's Kronecker factors to a parametric family aligned with the spike-and-flat shape: full spectral structure on a tracked r-dimensional subspace (dominant eigenvalues), with a single shared eigenvalue across the remaining n-r directions (uniform tail). On these remaining directions, orthogonalization is applied. This approach recovers the algebraic form of full KL-Shampoo's preconditioner while being more efficient.

Result: Pro-KLShampoo consistently outperforms KL-Shampoo across four pre-training scales (GPT-2 124M/350M, LLaMA 134M/450M) in validation loss, peak per-GPU memory usage, and wallclock time to reach each loss level, at every subspace rank tested.

Conclusion: The paper demonstrates that exploiting the spike-and-flat eigenvalue structure of Kronecker preconditioners through a hybrid approach combining parametric subspace restriction and orthogonalization leads to more efficient and effective LLM pre-training optimizers, bridging the gap between two previously isolated optimization frontiers.

Abstract: Optimizers that exploit the matrix structure of gradients are central to modern LLM pre-training, with two distinct frontiers: explicit Kronecker-factored preconditioning -- most recently KL-Shampoo, which estimates the preconditioner via KL divergence minimization -- and orthogonalization of the gradient momentum, exemplified by Muon and analyzed as steepest descent under the spectral norm. The two routes are typically developed in isolation. We make a structural observation about KL-Shampoo's Kronecker preconditioners: their eigenvalue spectra exhibit a \emph{spike-and-flat} shape -- a few dominant eigenvalues followed by an approximately uniform tail -- across layers and training stages, holding exactly under a rank-$ρ$ signal-plus-noise gradient model. We exploit this structure by restricting one of KL-Shampoo's Kronecker factors to a parametric family aligned with the spike-and-flat shape: full spectral structure on a tracked $r$-dimensional subspace, single shared eigenvalue across the remaining $n-r$ directions. On these directions, we apply orthogonalization. An identity shows that this orthogonalization recovers the algebraic form of full KL-Shampoo's preconditioner. On four pre-training scales (GPT-2 124M / 350M, LLaMA 134M / 450M), Pro-KLShampoo consistently outperforms KL-Shampoo at every subspace rank we test in validation loss, peak per-GPU memory, and wallclock time to reach each loss level.

</details>


### [298] [SMolLM: Small Language Models Learn Small Molecular Grammar](https://arxiv.org/abs/2605.06322)
*Akhil Jindal,Harang Ju*

Main category: cs.LG

TL;DR: A small 53K-parameter transformer (SMolLM) outperforms larger models in generating valid SMILES strings by learning hierarchical chemical grammar rules in a fixed order: brackets → rings → valence.


<details>
  <summary>Details</summary>
Motivation: While large language models for molecular design exist, their chemical grammar learning mechanisms are poorly understood. There's a need for compact, interpretable models that can reveal how transformers process formal language constraints like SMILES notation.

Method: Train SMolLM, a weight-shared transformer with only 53K parameters, to generate SMILES strings. Use mechanistic interpretability techniques: error classification, linear probing, and sparse autoencoders to analyze how the model processes SMILES constraints. Conduct systematic ablation studies across attention heads and passes.

Result: SMolLM achieves 95% validity on ZINC-250K benchmark, outperforming a standard GPT with 10× more parameters. The model learns chemical grammar in a fixed hierarchical order: brackets first, rings second, valence last. The bracket-matching step is localized to a single attention head.

Conclusion: SMolLM provides both a compact, high-performance molecular generator and a testbed for studying iterative computation in formal-language domains, offering mechanistic insights into how transformers learn hierarchical constraints.

Abstract: Language models for molecular design have scaled to hundreds of millions of parameters, yet how they learn chemical grammar is poorly understood. We train SMolLM, a 53K-parameter weight-shared transformer, to generate novel SMILES with 95% validity on the ZINC-250K drug-like-molecule benchmark, outperforming a standard GPT with 10 times more parameters. Mechanistically, the same block resolves SMILES constraints across passes in a fixed order: brackets first, rings second, and valence last, as shown by error classification, linear probing, and sparse autoencoders. A systematic ablation across attention heads and passes further localizes the first bracket-matching step to a single attention head. Together, these results yield a compact, mechanistically interpretable molecular generator and a testbed for studying iterative computation in formal-language domains.

</details>


### [299] [LINC: Decoupling Local Consequence Scoring from Hidden Matching in Constructive Neural Routing](https://arxiv.org/abs/2605.06332)
*Shaofeng Qin,Li Wang*

Main category: cs.LG

TL;DR: LINC introduces a decoder architecture that explicitly computes one-step consequences in neural routing solvers, improving performance on routing problems by separating deterministic transition arithmetic from learnable scoring.


<details>
  <summary>Details</summary>
Motivation: Current neural routing solvers hide deterministic one-step consequences (travel, waiting, slack, capacity changes) when scoring actions, forcing the model to implicitly rediscover transition arithmetic in the hidden state.

Method: LINC computes explicit local consequences at the decoder side, using centered relative consequences with shared linear local scorers and feasible-set summaries to modulate decoder context, separating deterministic transition effects from learnable scoring.

Result: LINC reduces PolyNet's gaps on CVRPTW from 13.83%/38.15% to 7.26%/14.71% on Solomon/Homberger benchmarks, and shows improvements on TSP and CVRP as well.

Conclusion: Explicitly computing local consequences in neural routing solvers preserves global matching capabilities while relieving hidden states from rediscovering transition arithmetic, leading to significant performance improvements across routing problems.

Abstract: Constructive neural routing solvers usually score the next action by matching a decoder context to candidate embeddings, hiding deterministic one-step consequences such as travel, waiting, slack, and capacity changes. We propose LINC (Local Inference via Normed Comparison), a decoder-side candidate decision architecture that computes these consequences explicitly. LINC uses them according to their decision role: centered relative consequences are compared by a shared linear local scorer, while feasible-set summaries modulate the decoder context. This preserves standard global matching and relieves the hidden state from rediscovering transition arithmetic. The Capacitated Vehicle Routing Problem with Time Windows (CVRPTW) serves as the main constrained-routing stress test; the same interface extends to the Capacitated Vehicle Routing Problem (CVRP) and Traveling Salesman Problem (TSP). In particular, for CVRPTW, LINC reduces PolyNet's Solomon/Homberger gaps from 13.83\%/38.15\% to 7.26\%/14.71\%; for TSP and CVRP, it also improves external-benchmark gaps.

</details>


### [300] [Eliciting associations between clinical variables from LLMs via comparison questions across populations](https://arxiv.org/abs/2605.06335)
*Fabian Kabus,Kian Kordtomeikel,Thomas Brox,Heinz Wiendl,Daiana Stolz,Harald Binder*

Main category: cs.LG

TL;DR: LLMs can recover patient characteristic correlations through structured triplet comparison questions, enabling causal inference via invariant causal prediction without accessing model internals.


<details>
  <summary>Details</summary>
Motivation: LLMs are trained on diverse biomedical literature containing information about different patient populations, but directly eliciting correlation and causal information is problematic. There's a need to recover these relationships as building blocks for medical decision making without relying on direct model interrogation.

Method: Proposed approach uses structured comparison questions (patient comparison triplets) combined with a statistical model for LLM representations. The method examines how LLM similarity decisions based on one variable are affected by providing information on a second variable. Uses prompt-level environment shifts to obtain correlation estimates across subpopulations, enabling invariant causal prediction (ICP) to identify candidate parent links.

Result: Method demonstrated in COPD and multiple sclerosis clinical domains. Elicited correlations were smooth, stable, and clinically interpretable across prompted environments. The correlations varied statistically significantly, supporting downstream invariance testing. ICP produced small sets of candidate invariant parent links.

Conclusion: Indirect elicitation via triplet comparisons can recover meaningful association structures from LLMs and offers a cautious pathway from implicit correlations to causal statements that align with LLM answering patterns, avoiding pitfalls of direct elicitation.

Abstract: The training data of large language models (LLMs) comprises a wide range of biomedical literature, reflecting data from many different patient populations. We investigate how it might be possible to recover information on correlation and causal links between patient characteristics, as a key building block for medical decision making. To avoid the pitfalls of direct elicitation, we propose an approach based on structured comparison questions, specifically patient comparison triplet questions. This is combined with a statistical model for the LLM representation that provides estimates of correlations without access to activations or model internals. Intuitively, we consider how similarity decisions of LLMs based on a first variable are affected by providing information on a second variable for one of the patients being assessed. We then induce prompt-level environment shifts to obtain correlation estimates for different subpopulations, which enables an invariant causal prediction (ICP) approach to obtain conservative candidate parent links. We demonstrate the method in two clinical domains, chronic obstructive pulmonary disease (COPD) and multiple sclerosis (MS). Across prompted environments, the elicited correlations are smooth, stable, and clinically interpretable, yet vary in a statistically significant way that supports downstream invariance testing, such that ICP provides a small set of candidate invariant parent links. These results show that indirect elicitation via triplet comparisons can recover meaningful association structure from LLMs and offer a cautious route from implicit correlations to causal statements that are congruent with LLM answering patterns.

</details>


### [301] [Is Escalation Worth It? A Decision-Theoretic Characterization of LLM Cascades](https://arxiv.org/abs/2605.06350)
*Dylan Bouchard*

Main category: cs.LG

TL;DR: This paper analyzes model cascades where cheap LLMs defer to expensive ones, develops a decision-theoretic framework to optimize cost-quality tradeoffs, and finds that simple pairwise cascades perform well while structural costs limit further improvements.


<details>
  <summary>Details</summary>
Motivation: Current model cascade approaches treat deferral thresholds as empirical hyperparameters without theoretical guidance on cost-quality frontier geometry across model pools. There's a need for systematic optimization of cascades to navigate the cost-quality tradeoff effectively.

Method: Developed a decision-theoretic framework based on constrained optimization and duality. Analyzed piecewise concavity of cost-quality frontiers for two-model cascades, characterized achievable frontiers for k-model pools via pairwise envelope approach, derived first-order conditions for optimal cascades using shadow prices.

Result: Validated framework on five benchmarks across eight models. Found that full fixed chains underperform pairwise envelope cascades, optimized subsequence cascades don't provide practical gains, and lightweight pre-generation routers outperform best cascade policies on 4/5 datasets due to avoiding cheap model generation costs.

Conclusion: Cascade performance is limited primarily by structural costs (paying cheap model before escalation decisions) rather than shortage of intermediate stages. Simple pairwise cascade optimization provides strong performance, and pre-generation routing can offer better cost-effectiveness by avoiding initial cheap model costs.

Abstract: Model cascades, in which a cheap LLM defers to an expensive one on low-confidence queries, are widely used to navigate the cost-quality tradeoff at deployment. Existing approaches largely treat the deferral threshold as an empirical hyperparameter, with limited guidance on the geometry of the resulting cost-quality frontier over a model pool. We develop a decision-theoretic framework grounded in constrained optimization and duality. For a two-model cascade, we establish piecewise concavity of the cost-quality frontier on decreasing-benefit regions of the confidence support, with reciprocal shadow prices linking the budget- and quality-constrained formulations. Given a pool of $k$ models, we characterize the frontier achievable by deterministic two-model threshold cascades as the pointwise envelope over $\binom{k}{2}$ pairwise cascades, with switching points where the optimal pair changes. For $k$-model cascades, we derive first-order conditions in which a single shadow price equalizes marginal quality-per-cost across stage boundaries. We validate the framework on five benchmarks (MATH, MMLU, TriviaQA, SimpleQA, LiveCodeBench) across eight models from five providers. Within the deterministic threshold-cascade class, full fixed chains underperform the pairwise envelope, and optimized subsequence cascades do not deliver practically meaningful held-out gains over it. A lightweight pre-generation router exceeds the best cascade policy on four of five datasets, mainly because it avoids the cheap model's generation cost on queries sent directly to a larger model rather than because of a stronger routing signal. These results suggest that cascade performance is limited primarily by structural cost, since cascades pay the cheap model before any escalation decision, rather than by a shortage of intermediate stages.

</details>


### [302] [Topological Signatures of Grokking](https://arxiv.org/abs/2605.06352)
*Yifan Tang,Qiquan Wang,Inés García-Redondo,Anthea Monod*

Main category: cs.LG

TL;DR: Using persistent homology on neural network embeddings, researchers discover that grokking (sudden generalization after memorization) has a clear topological signature: a sharp increase in first homology persistence, reflecting the emergence of cyclic structures corresponding to the underlying modular arithmetic task.


<details>
  <summary>Details</summary>
Motivation: The paper aims to understand the grokking phenomenon in neural networks through topological analysis. Grokking represents a puzzling training behavior where models first memorize data and then suddenly generalize. The motivation is to find geometric and topological signatures that characterize this transition from memorization to generalization in representation learning.

Method: Researchers apply persistent homology to point clouds derived from embedding matrices of models trained on modular arithmetic tasks with varying primes. They analyze topological features (specifically first homology H₁) through persistence diagrams, tracking changes in maximum and total persistence throughout training. They compare their topological approach to existing spectral (Fourier analysis) and geometric (local intrinsic dimension) diagnostics.

Result: The key finding is a clear topological signature of grokking: a sharp increase in both maximum and total persistence of first homology (H₁). Persistence diagrams show emergence of a dominant long-lived topological feature along with increasingly structured secondary features, mirroring the cyclic structure of modular arithmetic. These topological transitions correlate with generalization rather than memorization, as confirmed by ablation studies across different data regimes and control settings.

Conclusion: Persistent homology provides a unified geometric and topological framework for analyzing how neural networks internalize latent structure during training. It offers interpretable, multi-scale characterization that captures both local and global structure, potentially serving as a principled tool for understanding representation learning and generalization in neural networks.

Abstract: We study the grokking phenomenon through the lens of topology. Using persistent homology on point clouds derived from the embedding matrices of a range of models trained on modular arithmetic with varying primes, we identify a clear and consistent topological signature of grokking: a sharp increase in both the maximum and total persistence of first homology ($H_1$). Persistence diagrams reveal the emergence of a dominant long-lived topological feature together with increasingly structured secondary features, reflecting the underlying cyclic structure of the task. Compared to existing spectral and geometric diagnostics -- specifically, Fourier analysis and local intrinsic dimension -- persistent homology provides a unified geometric and topological characterization of representation learning, capturing both local and global multi-scale structure. Ablations across data regimes and control settings show that these topological transitions are tied to generalization rather than memorization. Our results suggest that persistent homology offers a principled and interpretable framework for analyzing how neural networks internalize latent structure during training.

</details>


### [303] [Order-Agnostic Autoregressive Modelling with Missing Data](https://arxiv.org/abs/2605.06355)
*Ignacio Peis,Pablo M. Olmos,Jes Frellsen*

Main category: cs.LG

TL;DR: Order-agnostic autoregressive models are adapted to handle incomplete data through missingness-aware training and used for active information acquisition, outperforming traditional imputation methods.


<details>
  <summary>Details</summary>
Motivation: While order-agnostic autoregressive models show strong performance in generative modeling, their application to settings with incomplete data has been largely unexplored, despite the prevalence of missing data in real-world applications.

Method: The paper introduces MO-ARM: 1) Shows standard training on complete data implicitly performs imputation under MCAR, 2) Develops principled framework for training directly on incomplete data under general missingness mechanisms, 3) Uses amortized conditional density estimation for active information acquisition (sequentially selecting most informative missing variables).

Result: The proposed Missingness-Aware Order-Agnostic Autoregressive Model (MO-ARM) consistently outperforms established imputation baselines across a suite of real-world benchmarks.

Conclusion: Order-agnostic autoregressive models can be effectively adapted for incomplete data settings through missingness-aware training and leveraged for active information acquisition, providing superior performance over traditional imputation methods.

Abstract: Order-Agnostic autoregressive models have demonstrated strong performance in deep generative modeling, yet their use in settings with incomplete data remains largely unexplored. In this work, we reinterpret them through the lens of missing data. First, we show that their standard training procedure on fully observed data implicitly performs imputation under a missing completely at random mechanism, resulting in robust out-of-sample imputation performance in settings with high missingness. Second, we introduce the first principled framework for training them directly on incomplete datasets under general missingness mechanisms. Third, we leverage their amortized conditional density estimation to perform active information acquisition, i.e., sequentially selecting the most informative missing variables for downstream prediction or inference. Across a suite of real-world benchmarks, our Missingness-Aware Order-Agnostic Autoregressive Model (MO-ARM) consistently outperforms established imputation baselines.

</details>


### [304] [Memory Efficient Full-gradient Attacks (MEFA) Framework for Adversarial Defense Evaluations](https://arxiv.org/abs/2605.06357)
*Yuan Du,Mitchel Hill,HanQin Cai*

Main category: cs.LG

TL;DR: A framework for exact-gradient evaluation of stochastic purification defenses using checkpointing to overcome memory constraints, enabling stronger white-box attacks and more reliable robustness benchmarking.


<details>
  <summary>Details</summary>
Motivation: Prior evaluations of iterative stochastic purification defenses often use approximate backpropagation due to memory constraints, which can weaken attacks and overestimate robustness. Additionally, stochasticity in purification is often under-controlled, affecting reported metrics.

Method: Leverages gradient checkpointing to make exact end-to-end gradient computation practical through long purification trajectories by trading recomputation for lower memory. Combines checkpointed backpropagation with evaluation protocols that control stochastic variability.

Result: Full-gradient attacks uncover vulnerabilities missed by approximate-gradient evaluations, yielding stronger state-of-the-art ℓ∞ and ℓ₂ white-box attacks. Demonstrates effectiveness on diffusion-based purification and Langevin sampling with Energy-Based Models (EBMs).

Conclusion: Exact-gradient evaluation is essential for reliable benchmarking of iterative stochastic defenses, and the proposed memory-efficient framework enables more accurate robustness assessment while supporting probing of out-of-distribution robustness.

Abstract: This work studies the robust evaluation of iterative stochastic purification defenses under white-box adversarial attacks. Our key technical insight is that gradient checkpointing makes exact end-to-end gradient computation through long purification trajectories practical by trading additional recomputation for substantially lower memory usage. This enables full-gradient adaptive attacks against diffusion- and Langevin-based purification defenses, where prior evaluations often resort to approximate backpropagation due to memory constraints. These approximations can weaken the attack signal and risk overestimating robustness. In parallel, stochasticity in iterative purification is frequently under-controlled, even though different purification trajectories can substantially change reported robustness metrics. Building on this insight, we introduce a memory-efficient full-gradient evaluation framework for stochastic purification defenses. The framework combines checkpointed backpropagation with evaluation protocols that control stochastic variability, thereby reducing memory bottlenecks while preserving exact gradients. We evaluate diffusion-based purification and Langevin sampling with Energy-Based Models (EBMs), demonstrating that full-gradient attacks uncover vulnerabilities missed by approximate-gradient evaluations. Our framework yields stronger state-of-the-art $\ell_{\infty}$ and $\ell_{2}$ white-box attacks and further supports probing out-of-distribution robustness. Overall, our results show that exact-gradient evaluation is essential for reliable benchmarking of iterative stochastic defenses.

</details>


### [305] [Preliminary Insights in Chronos Frequency Data Understanding and Reconstruction](https://arxiv.org/abs/2605.06361)
*Alessandro Pagani,Marco Cominelli,Liying Han,Gaofeng Dong,Sergio Benini,Francesco Gringoli,Mattia Savardi,Mani B. Srivastava,Trevor Bihl,Erik P. Blasch,Daniel O. Brigham,Kara Combs,Lance M. Kaplan,Federico Cerutti*

Main category: cs.LG

TL;DR: The paper analyzes how Chronos foundation model encodes frequency information from time-series data, finding that while it captures frequency content well across most of the spectrum, there are certain regimes where representation quality degrades.


<details>
  <summary>Details</summary>
Motivation: Foundation models for time-series data provide unified architectures for temporal representations across tasks and domains, reducing the need for feature engineering. However, it's unclear how well these models encode fundamental signal properties like frequency information, which is crucial for signal processing applications.

Method: The researchers analyzed Chronos under controlled conditions using discrete sinusoids at fixed frequencies as the simplest signal class. They employed lightweight online minimum description length (MDL) probes applied to the decoder architecture to test for the presence and separability of frequency information in the model's internal representations.

Result: The analysis reveals how frequential content is captured across the frequency spectrum and identifies specific regimes where representation quality may degrade or require special attention. The probes successfully detected the presence and separability of frequency information in Chronos's internal representations.

Conclusion: The findings provide practical guidance for using Chronos in signal processing and information fusion contexts, while contributing to efforts to improve interpretability and evaluation of foundation models for temporal data. The research helps characterize how well these models encode fundamental signal properties.

Abstract: This paper presents a preliminary analysis of the ability of Chronos foundation model to process and internally represent frequency domain information. Foundation models that process time-series data offer practitioners a unified architecture capable of learning generic temporal representations across diverse tasks and domains, reducing the need for task-specific feature engineering and enabling transfer across signal modalities. Despite their growing adoption, the extent to which such models encode fundamental signal properties remains insufficiently characterised. We address this gap by analysing Chronos under controlled conditions, starting from the simplest class of signals: discrete sinusoids generated at fixed frequencies. Using lightweight online minimum description length probes applied to the decoder architecture, we test for the presence and separability of frequency information in the model's internal representations. The results provide insight into how frequential content is captured across the frequency spectrum and highlight regimes in which representation quality may degrade or require particular care. These findings offer practical guidance for users of Chronos in signal processing and information fusion contexts, and contribute to ongoing efforts to improve the interpretability and evaluation of foundation models for temporal data.

</details>


### [306] [Flow Matching with Arbitrary Auxiliary Paths](https://arxiv.org/abs/2605.06364)
*Xin Peng,Ang Gao*

Main category: cs.LG

TL;DR: AuxPath-FM generalizes flow matching by allowing arbitrary auxiliary distributions (not just Gaussian noise) in probability paths, enabling diverse geometric properties and specialized tasks like label-guided generation.


<details>
  <summary>Details</summary>
Motivation: Existing flow matching methods typically restrict auxiliary variables to Gaussian noise distributions, limiting the flexibility in designing probability paths and specialized generative tasks.

Method: Framework incorporates auxiliary variable η from any arbitrary distribution into probability paths of form X_t = a(t)X_1 + b(t)X_0 + c(t)η, theoretically preserving continuity equation while maintaining training objective consistency.

Result: Enables design of diverse probability paths using various priors (Gaussian, Uniform, Laplace, Rademacher) with unique geometric properties, supports specialized tasks like label-guided generation through structured auxiliary distributions.

Conclusion: AuxPath-FM provides principled, general foundation for probability path design, offering theoretical generality and practical flexibility for diverse generative modeling tasks beyond traditional Gaussian-based approaches.

Abstract: We introduce a new generative modeling framework, \textbf{Flow Matching with Arbitrary Auxiliary Paths (AuxPath-FM)}, which generalizes conditional flow matching by incorporating an auxiliary variable drawn from an arbitrary distribution into the probability path. Unlike prior methods that restrict auxiliary components to Gaussian noise, AuxPath-FM allows the variable $η$ to follow any distribution, producing trajectories of the form $X_t = a(t)X_1 + b(t)X_0 + c(t)η$. We theoretically demonstrate that this construction preserves the continuity equation and maintains a training objective consistent with the marginal formulation. This flexibility enables the design of diverse probability paths using various priors, including Gaussian, Uniform, Laplace, and discrete Rademacher distributions, each offering unique geometric properties for generative flows. Furthermore, our framework allows for specialized tasks such as label-guided generation by encoding structured semantic information into the auxiliary distribution. Overall, AuxPath-FM provides a principled and general foundation for probability path design, offering both theoretical generality and practical flexibility for diverse generative modeling tasks.

</details>


### [307] [Layer Collapse in Diffusion Language Models](https://arxiv.org/abs/2605.06366)
*Alexander Conzelmann,Albert Catalan-Tatjer,Shiwei Liu*

Main category: cs.LG

TL;DR: DLMs develop layer collapse where early layers have similar, outlier-dominated activations critical for performance, unlike AR models where later layers become redundant due to undertraining. DLMs show surprising robustness to compression with reversed optimal sparsity patterns.


<details>
  <summary>Details</summary>
Motivation: To understand the activation dynamics differences between diffusion language models (DLMs) and autoregressive (AR) language models, as DLMs have emerged as competitive alternatives but their internal dynamics remain poorly characterized.

Method: Analyzed activation dynamics in LLaDA-8B, characterized layer-collapse property with super-outlier patterns, conducted controlled pre-training experiments, compression tests (3-bit GPTQ quantization), and sparsity allocation studies comparing DLMs vs AR models.

Result: DLMs exhibit layer collapse with critical super-outlier in early layers; pruning it causes repetitive random token loops. DLMs are robust to compression (only -1.8% drop vs -64.7% for Llama). Optimal sparsity allocation reverses between families: early layer sparsity helps DLMs (+8.4%) but hurts AR models (-8.4%).

Conclusion: DLM training objective fundamentally reshapes layer dynamics relative to AR models, driven by overtraining rather than undertraining, with practical implications for compression and deployment strategies that differ between model families.

Abstract: Diffusion language models (DLMs) have recently emerged as competitive alternatives to autoregressive (AR) language models, yet differences in their activation dynamics remain poorly understood. We characterize these dynamics in LLaDA-8B and identify a striking layer-collapse property: a few early layers exhibit highly similar, collapsed activation patterns dominated by a single large super-outlier persisting over a long token range. Despite its apparent redundancy, this outlier is critical: pruning it causes outputs to degrade into repetitive random token loops. Paradoxically, layers in LLaDA contain more redundant representations overall, with redundancy most pronounced in earlier layers -- the reverse of AR models, where deeper layers grow redundant due to undertraining. Our analysis indicates that layer collapse in DLMs is not driven by undertraining but by overtraining: a dominant outlier becomes an indispensable information carrier while remaining representations collapse into redundant structure. These findings have strong practical implications, verified through controlled pre-training experiments. DLMs are surprisingly robust to compression: LLaDA under 3-bit GPTQ quantization drops only -1.8% on GSM8K, whereas Llama-3.1-8B drops -64.7%. Optimal sparsity allocation also reverses between families: at 50% average sparsity, allocating more to early layers in LLaDA yields +8.4% over the reverse strategy, while the same allocation costs Llama -8.4%. Our findings reveal that the DLM training objective fundamentally reshapes layer dynamics relative to AR models, with direct consequences for compression and deployment. Code: github.com/Conzel/super-outlier-dlm.

</details>


### [308] [A Unified Pair-GRPO Family: From Implicit to Explicit Preference Constraints for Stable and General RL Alignment](https://arxiv.org/abs/2605.06375)
*Hao Yu*

Main category: cs.LG

TL;DR: Pair-GRPO family (Soft-Pair-GRPO and Hard-Pair-GRPO) is a new reinforcement learning framework that addresses limitations of RLHF by using pairwise preference learning with theoretical guarantees for stable policy updates.


<details>
  <summary>Details</summary>
Motivation: Address key limitations of existing LLM alignment via RLHF: unstable policy updates, ambiguous gradient directions, poor interpretability, and high gradient variance in pairwise preference learning paradigms.

Method: Established a unified theoretical framework for preference-based RL optimization centered on Pair-GRPO family. Soft-Pair-GRPO modifies GRPO by replacing group-normalized scalar rewards with binary pairwise preference rewards. Hard-Pair-GRPO adds explicit local probability constraints and constrained KL-fitting optimization to further suppress gradient noise.

Result: Demonstrated superior performance on LLM alignment benchmarks (HH-RLHF, UltraFeedback) and MuJoCo HalfCheetah-v4 task. Outperformed state-of-the-art baselines in alignment quality, human preference win rate, training stability, and generalization to reinforcement learning tasks.

Conclusion: The Pair-GRPO family provides a theoretically-grounded, stable framework for preference-based reinforcement learning that addresses fundamental limitations of existing RLHF methods while maintaining practical performance across diverse tasks.

Abstract: Large language model (LLM) alignment via reinforcement learning from human preferences (RLHF) suffers from unstable policy updates, ambiguous gradient directions, poor interpretability, and high gradient variance in mainstream pairwise preference learning paradigms. To systematically address these limitations, we establish a unified theoretical framework for preference-based RL optimization centered on the Pair-GRPO family, comprising two tightly coupled variants: Soft-Pair-GRPO and Hard-Pair-GRPO. Soft-Pair-GRPO is a minimal modification of Group Relative Policy Optimization (GRPO) that replaces group-normalized scalar rewards with binary pairwise preference rewards, retaining GRPO's clipped surrogate and KL-regularized structure. We prove a critical gradient equivalence theorem: under first-order Taylor expansion around the current policy, Soft-Pair-GRPO's gradient is a positive scalar multiple of standard GRPO's gradient, explaining its empirical stability despite discarding continuous reward magnitudes. Building on this foundation, we propose Hard-Pair-GRPO, an advanced variant introducing explicit local probability constraints and constrained KL-fitting optimization to further suppress gradient noise and global policy drift. We provide comprehensive theoretical guarantees for both variants--including monotonic policy improvement, deterministic gradient direction, gradient-variance reduction, and dynamic step-size convergence. Extensive experiments on standard LLM alignment benchmarks (HH-RLHF,UltraFeedback) and the MuJoCo continuous control task HalfCheetah-v4 demonstrate that our Pair-GRPO family consistently outperforms state-of-the-art baselines in alignment quality, human preference win rate, training stability, and generalization to general reinforcement learning. Ablation studies validate the critical contributions of each core component.

</details>


### [309] [MinMax Recurrent Neural Cascades](https://arxiv.org/abs/2605.06384)
*Alessandro Ronca*

Main category: cs.LG

TL;DR: MinMax Recurrent Neural Cascades (RNCs) are a novel neural architecture using MinMax algebra recurrence that avoids gradient vanishing/exploding problems while maintaining expressivity for regular languages and enabling parallel evaluation.


<details>
  <summary>Details</summary>
Motivation: Traditional recurrent neural networks suffer from vanishing/exploding gradient problems that limit their ability to learn long-range dependencies, and existing solutions like LSTMs/GRUs have computational limitations and don't fully solve these issues.

Method: Develop MinMax Recurrent Neural Cascades using MinMax algebra recurrence operators, which provide bounded activations, allow both parallel and sequential evaluation, and maintain constant gradient flow regardless of time distance.

Result: Theoretical analysis shows MinMax RNCs can express all regular languages, allow O(log n) parallel evaluation, maintain bounded states/gradients, and avoid vanishing gradients. Empirical results show superior performance on synthetic tasks and competitive next-token prediction with a 127M parameter model.

Conclusion: MinMax RNCs offer a theoretically sound and practically viable alternative to traditional RNNs, addressing fundamental gradient flow issues while maintaining expressivity and enabling efficient parallel computation, showing promise for real-world sequence modeling tasks.

Abstract: We show that the MinMax algebra provides a form of recurrence that is expressively powerful, efficiently implementable, and most importantly it is not affected by vanishing or exploding gradient. We call MinMax Recurrent Neural Cascades (RNCs) the models obtained by cascading several layers of neurons that employ such recurrence. We show that MinMax RNCs enjoy many favourable theoretical properties. First, their formal expressivity includes all regular languages, arguably the maximal expressivity for a finite-memory system. Second, they can be evaluated in parallel with a runtime that is logarithmic in the input length given enough processors; and they can also be evaluated sequentially. Third, their state and activations are bounded uniformly for all input lengths. Fourth, at almost all points, their loss gradient exists and it is bounded. Fifth, they do not exhibit a vanishing state gradient: the gradient of a state w.r.t. a past state can have constant value one regardless of the time distance between the two states. Finally, we find empirical evidence that the favourable theoretical properties of MinMax RNCs are matched by their practical capabilities: they are able to perfectly solve a number of synthetic tasks, showing superior performance compared to the considered state-of-the-art recurrent neural networks; also, we train a MinMax RNC of 127M parameters on next-token prediction, and the obtained model shows competitive performance for its size, providing evidence of the potential of MinMax RNCs on real-world tasks.

</details>


### [310] [Data-Driven Covariate Selection for Nonparametric and Cycle-Agnostic Causal Effect Estimation](https://arxiv.org/abs/2605.06385)
*Ana Leticia Garcez Vicente,Gijs van Seeventer,Saber Salehkaleybar*

Main category: cs.LG

TL;DR: The paper demonstrates that established local covariate selection methods for causal effect estimation, which are known to be sound and complete for acyclic models, also maintain their validity in cyclic causal models with latent confounding and feedback loops.


<details>
  <summary>Details</summary>
Motivation: Existing approaches for identifying valid adjustment sets in causal effect estimation are limited because they either assume acyclicity or require computationally intensive global causal structure learning. This is problematic in realistic settings where latent confounding and feedback loops (cycles) are present.

Method: The authors study a local, data-driven method for covariate selection based on conditional independence information. Their key technical contribution shows that the soundness and completeness guarantees of this method extend to cyclic causal models through the invariance of conditional independence assertions under σ-acyclification.

Result: Theoretical results establish that covariate selection methods valid for acyclic models also work for cyclic models without modification. Empirical validation on extensive synthetic data shows reliable performance in cyclic causal models.

Conclusion: The paper establishes a unified, cycle-agnostic perspective on covariate selection and causal effect estimation, demonstrating that established methods apply equally well across both cyclic and acyclic settings, enhancing applicability to realistic scenarios with feedback loops.

Abstract: Estimating causal effects from observational data requires identifying valid adjustment sets. This task is especially challenging in realistic settings where latent confounding and feedback loops are present. Existing approaches typically assume acyclicity or rely on global causal structure learning, limiting applicability and computational efficiency. In this work, we study a local, data-driven method for covariate selection based on conditional independence information. While this method is known to be sound and complete in acyclic causal models, its validity in the presence of cycles has remained unclear. Our main contribution is to show that these guarantees extend to cyclic causal models. In particular, our result relies on the invariance of conditional independence assertions under $σ$-acyclification. These findings establish a unified, cycle-agnostic perspective on covariate selection and causal effect estimation, showing that the method applies across cyclic and acyclic settings without modification. Empirically, we validate this on extensive synthetic data, showing reliable performance in cyclic causal models.

</details>


### [311] [Asymmetric On-Policy Distillation: Bridging Exploitation and Imitation at the Token Level](https://arxiv.org/abs/2605.06387)
*Nan Jia,Haojin Yang,Xing Ma,Jiesong Lian,Shuailiang Zhang,Weipeng Zhang,Ke Zeng,Xunliang Cai,Zequn Sun*

Main category: cs.LG

TL;DR: AOPD improves on-policy distillation by replacing ineffective negative reinforcement with divergence minimization in non-positive regions, boosting performance on math reasoning tasks.


<details>
  <summary>Details</summary>
Motivation: Standard on-policy distillation suffers from high variance updates, vanishing gradients in zero-advantage regions, and exploration bottlenecks when corrective signals are insufficient.

Method: Proposed Asymmetric On-Policy Distillation (AOPD) which replaces negative reinforcement with localized divergence minimization in non-positive advantage regions while preserving positive reinforcement learning.

Result: AOPD consistently outperforms standard OPD on mathematical reasoning benchmarks with average gains of 4.09/8.34 under strong/weak initialization, maintains higher policy entropy, and shows better capability retention during sequential tool-use adaptation.

Conclusion: Asymmetric handling of positive vs non-positive advantage regions through divergence minimization significantly improves on-policy distillation performance and training stability.

Abstract: On-policy distillation (OPD) trains a student on its own trajectories with token-level teacher feedback and often outperforms off-policy distillation and standard reinforcement learning. However, we find that its standard advantage weighted policy gradient suffers from three structural weaknesses, including high variance updates, vanishing gradients in zero-advantage regions, and exploration bottlenecks when corrective signals are insufficient.We therefore propose Asymmetric On-Policy Distillation (AOPD), which replaces ineffective negative reinforcement with localized divergence minimization in non-positive advantage regions while preserving positive reinforcement learning. Experiments on mathematical reasoning benchmarks show that AOPD consistently outperforms standard OPD, with average gains of 4.09 / 8.34 under strong / weak initialization, respectively. AOPD also maintains higher policy entropy during training and better capability retention during sequential tool-use adaptation.

</details>


### [312] [Consistent Geometric Deep Learning via Hilbert Bundles and Cellular Sheaves](https://arxiv.org/abs/2605.06395)
*Kartik Tandon,Julian Gould,Tanishq Bhatia,Francesca Dominici,Alejandro Ribeiro,Claudio Battiloro*

Main category: cs.LG

TL;DR: This paper introduces HilbNets, a novel convolutional learning framework for infinite-dimensional signals on manifolds using connection Laplacians on Hilbert bundles, with theoretical guarantees for sampling consistency and empirical validation.


<details>
  <summary>Details</summary>
Motivation: Modern deep learning architectures need to handle sophisticated infinite-dimensional signals (time series, probability distributions, operators) defined over irregular domains, but lack a unified learning theory for these settings.

Method: Introduces HilbNets - a convolutional framework using connection Laplacian associated with Hilbert bundles as convolutional operators. Implements via two-stage sampling: 1) samples manifold to induce Hilbert Cellular Sheaf (generalized graph with Hilbert feature spaces), 2) discretizes signals. Proves convergence of sheaf Laplacian to connection Laplacian and transferability across different samplings.

Result: 1) Shows sheaf Laplacian converges to connection Laplacian as sampling density increases (generalization of Belkin & Niyogi result). 2) Proves discretized HilbNets converge to continuous architectures and are transferable across samplings. 3) Validates framework on synthetic and real-world tasks.

Conclusion: The results broaden the scope of geometric learning by lifting classical Laplacian-based frameworks to settings where signals live in Hilbert spaces, providing a unified theory for infinite-dimensional signals on manifolds with consistency guarantees.

Abstract: Modern deep learning architectures increasingly contend with sophisticated signals that are natively infinite-dimensional, such as time series, probability distributions, or operators, and are defined over irregular domains. Yet, a unified learning theory for these settings has been lacking. To start addressing this gap, we introduce a novel convolutional learning framework for possibly infinite-dimensional signals supported on a manifold. Namely, we use the connection Laplacian associated with a Hilbert bundle as a convolutional operator, and we derive filters and neural networks, dubbed as \textit{HilbNets}. We make HilbNets and, more generally, the convolution operation, implementable via a two-stage sampling procedure. First, we show that sampling the manifold induces a Hilbert Cellular Sheaf, a generalized graph structure with Hilbert feature spaces and edge-wise coupling rules, and we prove that its sheaf Laplacian converges in probability to the underlying connection Laplacian as the sampling density increases. Notably, this result is a generalization to the infinite-dimensional bundle setting of the Belkin \& Niyogi \cite{BELKIN20081289} convergence result for the graph Laplacian to the manifold Laplacian, a theoretical cornerstone of geometric learning methods. Second, we discretize the signals and prove that the discretized (implementable) HilbNets converge to the underlying continuous architectures and are transferable across different samplings of the same bundle, providing consistency for learning. Finally, we validate our framework on synthetic and real-world tasks. Overall, our results broaden the scope of geometric learning as a whole by lifting classical Laplacian-based frameworks to settings where the signal at each point lives in its own Hilbert space.

</details>


### [313] [SparseForge: Efficient Semi-Structured LLM Sparsification via Annealing of Hessian-Guided Soft-Mask](https://arxiv.org/abs/2605.06402)
*Liu Hanzuo,Chaofan Lin,Weixuan Sun,Yulong Wang,Key,Rayying,Mingyu Gao*

Main category: cs.LG

TL;DR: SparseForge: An efficient post-training framework for semi-structured LLM pruning that optimizes sparsity masks directly, achieving accuracy recovery with minimal retraining tokens.


<details>
  <summary>Details</summary>
Motivation: Post-training semi-structured pruning for LLMs often causes substantial quality degradation due to structural coupling, and existing recovery methods require large-scale sparse retraining with high computational cost.

Method: Combines Hessian-aware importance estimation with progressive annealing of soft masks into hardware-executable structured sparsity, enabling stable and efficient sparse recovery by directly optimizing sparsity masks rather than scaling retraining tokens.

Result: On LLaMA-2-7B with 2:4 sparsity, achieves 57.27% average zero-shot accuracy using only 5B retraining tokens (vs. dense model's 56.43%), approaching state-of-the-art results of 57.52% achieved with 40B tokens.

Conclusion: SparseForge significantly improves the accuracy-efficiency trade-off for semi-structured pruning of LLMs, demonstrating consistent improvements across model families with minimal retraining overhead.

Abstract: Semi-structured sparsity provides a practical path to accelerate large language models (LLMs) with native hardware support, but post-training semi-structured pruning often suffers from substantial quality degradation due to strong structural coupling. Existing methods rely on large-scale sparse retraining to recover accuracy, resulting in high computational cost.
  We propose SparseForge, a post-training framework that improves recovery efficiency by directly optimizing the sparsity mask rather than scaling up retraining tokens. SparseForge combines Hessian-aware importance estimation with progressive annealing of soft masks into hardware-executable structured sparsity, enabling stable and efficient sparse recovery. On LLaMA-2-7B under 2:4 sparsity, SparseForge achieves 57.27% average zero-shot accuracy with only $\textbf{5B}$ retraining tokens, surpassing the dense model's 56.43% accuracy and approaching the 57.52% result of a state-of-the-art method using $\textbf{40B}$ tokens. Such improvements on the accuracy-efficiency trade-off from SparseForge are shown to be consistent across model families.

</details>


### [314] [FRInGe: Distribution-Space Integrated Gradients with Fisher--Rao Geometry](https://arxiv.org/abs/2605.06404)
*Gabriele Martino,Sebastian Tschiatschek*

Main category: cs.LG

TL;DR: Fisher-Rao Integrated Gradients (FRInGe) improves attribution stability by using maximum-entropy references and Fisher-Rao geodesics in predictive space instead of heuristic baselines and straight-line paths in input space.


<details>
  <summary>Details</summary>
Motivation: Integrated Gradients (IG) attribution method suffers from brittleness due to dependence on heuristic baselines, straight-line paths, discretization, and saturation effects, limiting reliable model interpretation.

Method: FRInGe replaces input baselines with maximum-entropy predictive references and follows Fisher-Rao geodesics on probability simplex. Input-space trajectories are realized through pullback Fisher metric with KL and Euclidean trust region stabilization.

Result: FRInGe significantly improves calibration-oriented attribution metrics (especially MAS scores) across six ImageNet architectures while maintaining competitive performance on perturbation AUC and infidelity metrics.

Conclusion: By defining references and interpolation in predictive distribution space using Fisher-Rao geometry, FRInGe provides more stable and calibrated attributions than traditional IG methods.

Abstract: Gradient-based attribution methods are model-faithful and scalable, but Integrated Gradients (IG) can be brittle because explanations depend on heuristic baselines, straight-line paths, discretization, and saturation. We propose Fisher--Rao Integrated Gradients (FRInGe), which defines both the reference and interpolation schedule in predictive distribution space. FRInGe replaces input baselines with a maximum-entropy predictive reference and follows a Fisher-Rao geodesic on the probability simplex. The corresponding input-space trajectory is realized through the pullback Fisher metric and stabilized by KL and Euclidean trust regions; attributions are obtained by integrating input gradients along this trajectory. Across six ImageNet architectures, FRInGe most clearly improves calibration-oriented attribution metrics, especially MAS scores, while remaining competitive on perturbation AUC and infidelity.

</details>


### [315] [E = T*H/(O+B): A Dimensionless Control Parameter for Mixture-of-Experts Ecology](https://arxiv.org/abs/2605.06415)
*Qingjun Zhang*

Main category: cs.LG

TL;DR: The paper introduces a dimensionless control parameter E that predicts whether Mixture-of-Experts models will develop healthy expert ecologies or collapse into dead experts, eliminating the need for handcrafted load-balancing losses.


<details>
  <summary>Details</summary>
Motivation: Mixture-of-Experts models often suffer from expert collapse where some experts become "dead" (unused), requiring complex load-balancing auxiliary losses. There's a need for a simpler, unified diagnostic to predict and prevent expert collapse across different modalities and configurations.

Method: The authors propose E = T*H/(O+B), a dimensionless parameter combining four hyperparameters: routing temperature T, routing entropy weight H, oracle weight O, and balance weight B. They conduct 12 controlled experiments (8 vision, 4 language) totaling over 11,000 training epochs across multiple datasets to validate E's predictive power.

Result: E ≥ 0.5 alone guarantees zero dead experts across all experiments, eliminating the need for handcrafted load-balancing losses. The paper reveals six additional findings: dead experts can resuscitate, ortho toxicity is dataset-dependent, task complexity shifts the critical E threshold, model overfitting decouples from expert health, three-tier MoEs collapse into two-tier structures, and ecological structure is temperature-invariant across 50x range.

Conclusion: E serves as a unified diagnostic tool for MoE training, analogous to the Reynolds number in fluid dynamics, providing a simple predictive metric for expert ecological health across modalities and architectures.

Abstract: We introduce E = T*H/(O+B), a dimensionless control parameter that predicts whether Mixture-of-Experts (MoE) models will develop a healthy expert ecology or collapse into dead experts. E combines four hyperparameters -- routing temperature T, routing entropy weight H, oracle weight O, and balance weight B -- into a single quantity. Through 12 controlled experiments (8 vision, 4 language) totaling over 11,000 training epochs, we establish that E >= 0.5 alone is sufficient to guarantee zero dead experts, removing the necessity for handcrafted load-balancing auxiliary losses. We validate this cross-modally on CIFAR-10, CIFAR-100, TinyImageNet-200, WikiText-2, and WikiText-103. Six additional findings emerge: (1) dead experts can resuscitate -- triggered by balance loss driving router re-exploration; (2) ortho toxicity is dataset-dependent, not universal; (3) task complexity shifts the critical E threshold; (4) model overfitting is decoupled from expert ecological health; (5) three-tier MoE spontaneously collapses into a two-tier functional structure; (6) ecological structure is temperature-invariant across a 50x range. We propose that E serves as a unified diagnostic for MoE training, analogous to the Reynolds number in fluid dynamics.

</details>


### [316] [Federated Cross-Client Subgraph Pattern Detection](https://arxiv.org/abs/2605.06433)
*Selin Ceydeli,Rui Wang,Kubilay Atasu*

Main category: cs.LG

TL;DR: A framework for distributed GNNs that uses per-step, layer-wise embedding exchange to achieve representation equivalence with centralized GNNs across partitioned graphs.


<details>
  <summary>Details</summary>
Motivation: Current GNN-based subgraph pattern detection methods assume centralized graph access, but distributed graphs across multiple parties create representation-equivalence gaps where subgraph patterns crossing partition boundaries become locally unidentifiable.

Method: Propose a per-step, layer-wise embedding exchange framework where clients synchronize intermediate node representations at each layer without exposing raw features/labels, combined with federated parameter aggregation under extended-subgraph assumption and shared model parameters.

Result: Experiments on synthetic directed multigraphs with cycles, bicliques, and scatter-gather patterns show embedding exchange and federated aggregation are complementary, not interchangeable; their combination recovers most representation gap when using fresh per-step embeddings rather than stale per-epoch ones.

Conclusion: The framework successfully bridges the representation-equivalence gap in distributed GNNs through coordinated embedding exchange, enabling subgraph pattern detection across partitioned graphs while maintaining privacy by not exposing raw data.

Abstract: Subgraph pattern detection aims to uncover complex interaction structures in graphs. However, state-of-the-art graph neural network (GNN)-based solutions assume centralized access to the entire graph. When graphs are instead distributed across multiple parties, client-local GNN computations diverge from those of a centralized model, resulting in a representation-equivalence gap. We formalize this as a structural observability problem, where subgraph patterns crossing partition boundaries become locally unidentifiable. To bridge this gap, we propose a per-step, layer-wise embedding exchange framework in which clients synchronize intermediate node representations at each layer of the forward pass, without exposing raw features or labels. Under an extended-subgraph assumption and shared model parameters across clients, this framework recovers the same node representations as a centralized GNN over the full graph. Experiments on synthetic directed multigraphs with cycles, bicliques, and scatter-gather patterns show that embedding exchange and federated parameter aggregation are complementary rather than interchangeable: their combination recovers most of the representation gap, provided exchanged embeddings are fresh per-step rather than stale per-epoch.

</details>


### [317] [Hyperbolic Concept Bottleneck Models](https://arxiv.org/abs/2605.06440)
*Daniel Uyterlinde,Swasti Shreya Mishra,Pascal Mettes*

Main category: cs.LG

TL;DR: Hyperbolic Concept Bottleneck Models (HypCBM) improve interpretability by embedding concepts in hyperbolic space instead of flat Euclidean space, allowing for better representation of hierarchical semantic relationships between concepts.


<details>
  <summary>Details</summary>
Motivation: Current Concept Bottleneck Models embed concepts in flat Euclidean space, treating them as independent dimensions. However, concepts are actually highly structured and organized in semantic hierarchies, creating a mismatch between model representation and real-world concept organization.

Method: Proposes HypCBM, a post-hoc framework that grounds the concept bottleneck in hyperbolic space. It reformulates concept activation as asymmetric geometric containment using entailment cones in hyperbolic space. The method uses entailment cones as test-time activation signals rather than pre-training penalties, and introduces adaptive scaling for hierarchically faithful interventions.

Result: HypCBM rivals post-hoc Euclidean models trained on 20× more data in sparse interpretability regimes. It shows stronger hierarchical consistency and improved robustness to input corruptions compared to traditional Euclidean approaches.

Conclusion: Embedding concepts in hyperbolic space better captures their inherent hierarchical structure, leading to more interpretable, hierarchy-aware models with sparse activations and coherent intervention propagation without additional supervision.

Abstract: Concept Bottleneck Models (CBMs) have become a popular approach to enable interpretability in neural networks by constraining classifier inputs to a set of human-understandable concepts. While effective, current models embed concepts in flat Euclidean space, treating them as independent, orthogonal dimensions. Concepts, however, are highly structured and organized in semantic hierarchies. To resolve this mismatch, we propose Hyperbolic Concept Bottleneck Models (HypCBM), a post-hoc framework that grounds the bottleneck in this structure by reformulating concept activation as asymmetric geometric containment in hyperbolic space. Rather than treating entailment cones as a pre-training penalty, we show they encode a natural test-time activation signal: the margin of inclusion within a concept's entailment cone yields sparse, hierarchy-aware activations without any additional supervision or learned modules. We further introduce an adaptive scaling law for hierarchically faithful interventions, propagating user corrections coherently through the concept tree. Empirically, HypCBM rivals post-hoc Euclidean models trained on 20$\times$ more data in sparse regimes required for human interpretability, with stronger hierarchical consistency and improved robustness to input corruptions.

</details>


### [318] [FedFrozen: Two-Stage Federated Optimization via Attention Kernel Freezing](https://arxiv.org/abs/2605.06446)
*Junye Du,Zhenghao Li,Yushi Feng,Long Feng*

Main category: cs.LG

TL;DR: FedFrozen is a two-stage federated learning framework for Transformers that first warms up the full model, then freezes the attention kernel-forming query/key block while continuing to optimize the value block, improving stability and effectiveness in heterogeneous settings.


<details>
  <summary>Details</summary>
Motivation: Transformer architectures show inherent robustness in heterogeneous federated learning compared to conventional models, motivating investigation into how different attention parameter components influence federated optimization. The core problem is client drift from inconsistent local updates in federated learning with heterogeneous clients.

Method: FedFrozen decomposes attention into query/key block (determines attention kernel) and value block (performs semantic transformation). The framework has two stages: 1) Full-model warm-up training, 2) Freeze query/key block while continuing to optimize value block. The method uses a linear-attention formulation to analyze optimization dynamics.

Result: The warm-up stage is interpreted as inexact descent on a regularized kernel-profile objective, while the frozen stage reduces to restricted value-block optimization under fixed attention kernel. Analysis reveals explicit trade-off governing warm-up length choice. Simulations validate predicted bias-drift behavior, and real-data experiments show FedFrozen improves Transformer stability and effectiveness in heterogeneous federated learning.

Conclusion: By strategically freezing attention kernel components while continuing to optimize semantic transformation components, FedFrozen addresses client drift in heterogeneous federated learning for Transformers, offering improved optimization stability and performance compared to existing methods that use objective-level regularization or update-correction mechanisms.

Abstract: Federated learning with heterogeneous clients remains a significant challenge for deep learning, primarily due to client drift arising from inconsistent local updates. Existing federated optimization methods typically address this issue through objective-level regularization or update-correction mechanisms. Recent studies, however, suggest that Transformer-based architectures may be inherently more robust than conventional models under heterogeneous federated training. Motivated by this observation, we investigate how different parameter components within the attention mechanism influence federated optimization. Specifically, we decompose the attention module into a query/key block, which determines the attention kernel, and a value block, which performs semantic transformation under the induced kernel. Based on this perspective, we propose FedFrozen, a two-stage federated optimization framework that first performs full-model warm-up training and then freezes the query/key block while continuing to optimize the value block. Under a linear-attention formulation, we show that the warm-up stage can be interpreted as an inexact descent procedure on a regularized kernel-profile objective, while the frozen stage reduces to a restricted value-block optimization problem under a fixed attention kernel. Our analysis further reveals an explicit trade-off that governs the choice of warm-up length. Simulations validate the predicted bias-drift behavior, and real-data experiments demonstrate that FedFrozen improves both the stability and effectiveness of Transformer models in heterogeneous federated learning.

</details>


### [319] [Scene-Adaptive Continual Learning for CSI-based Human Activity Recognition with Mixture of Experts](https://arxiv.org/abs/2605.06447)
*Wenhan Zheng,Yuyi Mao,Ivan Wang-Hei Ho*

Main category: cs.LG

TL;DR: SAMoE-C is a novel continual learning approach for CSI-based human activity recognition that uses a mixture-of-experts system with a semantic router to enable scalable cross-domain adaptation while minimizing computational costs.


<details>
  <summary>Details</summary>
Motivation: CSI-based human activity recognition suffers from performance degradation when deployed across different physical environments (domain shifts). Existing continual learning solutions for this problem scale poorly, require large replay buffers, or have linearly growing inference costs as domains accumulate.

Method: The authors propose SAMoE-C (Scene-Adaptive Mixture of Experts with Clustered Specialists), which formulates cross-domain CSI-based HAR as a mixture-of-experts system. Key components include: 1) An attention-based semantic router that selectively activates only relevant experts for each input, 2) A novel training protocol that requires only a tiny replay buffer to stabilize domain discrimination of the router, 3) Clustered specialists that enable scene-specific adaptation.

Result: Experimental results on a four-scene CSI dataset show that SAMoE-C approaches state-of-the-art accuracy while maintaining significantly lower inference costs compared to existing methods. The approach enables scalable cross-domain HAR deployment with low training overhead and high computational efficiency.

Conclusion: SAMoE-C effectively addresses the scalability challenges of continual learning for CSI-based HAR by combining modular experts, selective activation through a router, and lightweight training. This enables practical deployment in real-world settings where domain adaptation is required across multiple environments.

Abstract: Channel state information (CSI)-based human activity recognition (HAR) is vulnerable to performance degradation under domain shifts across varying physical environments. Continual learning (CL) offers a principled way to learn new domains sequentially while preserving past knowledge, but existing CL solutions for CSI-based HAR scale poorly with accumulating domains, rely on a large replay buffer, or incur linearly growing inference cost. In this letter, we propose Scene-Adaptive Mixture of Experts with Clustered Specialists (SAMoE-C), which formulates cross-domain CSI-based HAR as a mixture-of-experts system that enables scene-specific adaptation, via an attention-based semantic router that activates only selected experts for each input. Moreover, we develop a novel training protocol, which requires only a tiny replay buffer for stabilizing domain discrimination of the router. Experimental results on a four-scene CSI dataset demonstrate that SAMoE-C approaches the state-of-the-art accuracy, while maintaining a significantly lower inference cost. By jointly combining modular experts, selective activation with router and a lightweight training protocol, SAMoE-C enables scalable cross-domain CSI-based HAR deployment with low training overhead and high computational efficiency in real-world settings.

</details>


### [320] [ORTHOBO: Orthogonal Bayesian Hyperparameter Optimization](https://arxiv.org/abs/2605.06454)
*Maresa Schröder,Pascal Janetzky,Michael Klar,Stefan Feuerriegel*

Main category: cs.LG

TL;DR: OrthoBO: Bayesian optimization with orthogonal acquisition estimators reduces Monte Carlo noise in acquisition function estimation, improving ranking stability and performance in expensive hyperparameter optimization tasks.


<details>
  <summary>Details</summary>
Motivation: Bayesian optimization suffers from acquisition estimation noise due to finite-sample Monte Carlo error, which can flip candidate rankings and lead to suboptimal decisions, even with correctly specified models.

Method: Proposes an orthogonal acquisition estimator that subtracts an optimally weighted score-function control variate, creating an acquisition residual orthogonal to posterior score directions. Combined with ensemble surrogates and outer log transformation in the OrthoBO framework.

Result: Theoretical analysis shows estimator preserves target, reduces variance, and improves pairwise ranking stability. Numerical experiments confirm reduced acquisition estimation variance, stabilized candidate rankings, and strong performance in hyperparameter optimization for neural networks.

Conclusion: OrthoBO addresses the overlooked problem of acquisition estimation noise in Bayesian optimization, providing a theoretically grounded framework that improves decision stability and performance through variance reduction and ranking stabilization.

Abstract: Bayesian optimization is widely used for hyperparameter optimization when model evaluations are expensive; however, noisy acquisition estimates can lead to unstable decisions. We identify acquisition estimation noise as a failure mode that was previously overlooked: even when the surrogate model and acquisition target are correctly specified, finite-sample Monte Carlo error can perturb acquisition values. This can, in turn, flip candidate rankings and lead to suboptimal BO decisions. As a remedy, we aim at variance reduction and propose an orthogonal acquisition estimator that subtracts an optimally weighted score-function control variate, which yields an acquisition residual orthogonal to posterior score directions and which thus reduces Monte Carlo variance. We further introduce OrthoBO: a Bayesian optimization framework that combines our orthogonal acquisition estimator with ensemble surrogates and an outer log transformation. We show theoretically that our estimator preserves the target, leads to variance reduction, and improves pairwise ranking stability. We further verify the theoretical properties of OrthoBO through numerical experiments where our framework reduces acquisition estimation variance, stabilizes candidate rankings, and achieves strong performance. We also demonstrate the downstream utility of OrthoBO in hyperparameter optimization for neural network training and fine-tuning.

</details>


### [321] [Invariant Features in Language Models: Geometric Characterization and Model Attribution](https://arxiv.org/abs/2605.06458)
*Agnibh Dasgupta,Abdullah Tanvir,Xin Zhong*

Main category: cs.LG

TL;DR: Language models encode semantic meaning through geometrically structured invariant representations in latent space, with paraphrastic variations separated along nuisance directions and semantic identity preserved in invariant subspaces.


<details>
  <summary>Details</summary>
Motivation: While language models show strong robustness to paraphrasing, indicating stable semantic representations, the underlying structure and origin of this invariance remain unclear. The authors seek to understand how semantic information is geometrically organized in latent space.

Method: The authors propose a local geometric framework where semantically equivalent inputs form structured regions in latent space. They develop: (1) geometric characterization of invariant latent features, (2) contrastive subspace discovery method separating semantic-changing from semantic-preserving variation, and (3) application of invariant representations to zero-shot model attribution.

Result: Empirical results across models and layers show: invariant structure emerges in specific depth regions; semantic displacement occurs largely outside the nuisance subspace; representation-level interventions demonstrate causal role of invariant components; invariant representations capture model-specific geometric patterns enabling accurate model attribution.

Conclusion: Semantic invariance can be viewed as a local geometric property of latent representations, providing a principled perspective on how language models organize meaning through structured invariant subspaces that separate semantic identity from paraphrastic variation.

Abstract: Language models exhibit strong robustness to paraphrasing, suggesting that semantic information may be encoded through stable internal representations, yet the structure and origin of such invariance remain unclear. We propose a local geometric framework in which semantically equivalent inputs occupy structured regions in latent space, with paraphrastic variation along nuisance directions and semantic identity preserved in invariant subspaces. Building on this view, we make three contributions: (1) a geometric characterization of invariant latent features, (2) a contrastive subspace discovery method that separates semantic-changing from semantic-preserving variation, and (3) an application of invariant representations to zero-shot model attribution. Across models and layers, empirical results support these contributions. Invariant structure emerges in specific depth regions, semantic displacement lies largely outside the nuisance subspace, and representation-level interventions indicate a causal role of invariant components in model outputs. Invariant representations also capture model-specific geometric patterns, enabling accurate attribution. These findings suggest that semantic invariance can be viewed as a local geometric property of latent representations, offering a principled perspective on how language models organize meaning.

</details>


### [322] [MINER: Mining Multimodal Internal Representation for Efficient Retrieval](https://arxiv.org/abs/2605.06460)
*Weien Li,Rui Song,Zeyu Li,Haochen Liu,Gonghao Zhang,Difan Jiao,Zhenwei Tang,Bowei He,Haolun Wu,Xue Liu,Ye Yuan*

Main category: cs.LG

TL;DR: MINER is a lightweight plug-in module that probes and fuses internal transformer layer signals into a single compact embedding for efficient visual document retrieval, bridging the quality gap between dense and late-interaction retrievers while maintaining storage/latency advantages.


<details>
  <summary>Details</summary>
Motivation: Existing visual document retrieval approaches have trade-offs: late-interaction retrievers achieve high quality but require large index footprints and high serving costs due to storing hundreds of vectors per page, while dense single-vector retrievers are storage/latency efficient but consistently lag in quality by compressing all information into a single final-layer embedding.

Method: MINER consists of two stages: 1) Retrieval-Aligned Layer Probing attaches lightweight probes at each transformer layer to identify which dimensions carry retrieval-relevant information, and 2) Adaptive Sparse Multi-Layer Fusion applies performance-adaptive neuron-level masking to selected layers and fuses surviving signals into the final dense vector, all without modifying the backbone or sacrificing single-vector efficiency.

Result: MINER outperforms existing dense single-vector retrievers on most ViDoRe V1/V2/V3 benchmarks with up to 4.5% nDCG@5 improvement. Compared to late-interaction baselines, it substantially narrows the nDCG@5 gap to 0.2 in some settings while preserving storage and serving advantages of dense retrieval.

Conclusion: MINER successfully bridges the quality-efficiency gap in visual document retrieval by mining internal transformer representations, offering a practical solution that combines near-late-interaction quality with dense retrieval efficiency through its plug-in architecture.

Abstract: Visual document retrieval has become essential for accessing information in visually rich documents. Existing approaches fall into two camps. Late-interaction retrievers achieve strong quality through fine-grained token-level matching but store hundreds of vectors per page, incurring large index footprints and high serving costs. By contrast, dense single-vector retrievers retain storage and latency advantages but consistently lag in quality because they compress all information into a single final-layer embedding. In this work, we first conduct a layerwise diagnostic on single-vector retrievers, revealing that retrieval-relevant signal resides in internal representations. Motivated by these findings, we propose MINER (Mining Multimodal Internal RepreseNtation for Efficient Retrieval), a lightweight plug-in module that probes and fuses internal signals across transformer layers into a single compact embedding without modifying the backbone or sacrificing single-vector efficiency. The first Retrieval-Aligned Layer Probing stage attaches a lightweight probe at each layer, surfacing which dimensions carry retrieval-relevant information. The subsequent Adaptive Sparse Multi-Layer Fusion stage applies performance-adaptive neuron-level masking to the selected layers and fuses the surviving signals into the final dense vector. Across ViDoRe V1/V2/V3, MINER outperforms existing dense single-vector retrievers on the majority of benchmarks, with up to 4.5% nDCG@5 improvement over its corresponding backbone. Compared to strong late-interaction baselines, in some settings MINER substantially narrows the nDCG@$5$ gap to $0.2$ while preserving the storage and serving advantages of dense retrieval.

</details>


### [323] [Invariant-Based Diagnostics for Graph Benchmarks](https://arxiv.org/abs/2605.06462)
*Richard von Moos,Mathieu Alain,Bastian Rieck*

Main category: cs.LG

TL;DR: The paper introduces graph invariants as diagnostic tools to evaluate whether graph models actually learn from connectivity or just rely on node features, showing these invariants can match or exceed traditional GNN performance.


<details>
  <summary>Details</summary>
Motivation: Current graph benchmark practices make it difficult to determine whether models actually learn from graph structure or just rely on node features, hindering progress on graph foundation models. There's a need to separate the contributions of structure and features in model evaluation.

Method: The authors propose using graph invariants - permutation-invariant, task-agnostic structural descriptors - as a diagnostic framework. They evaluate invariants across 26 datasets, comparing them with transformer and message-passing baselines, and analyze how well invariants characterize structural heterogeneity and predict performance.

Result: Graph invariants are more expressive than standard GNNs, characterize structural heterogeneity within and across datasets, predict multi-task performance, and simple invariant-based models are competitive with or sometimes exceed transformer and message-passing baselines across 26 datasets.

Conclusion: Expressivity is not the main driver of predictive performance in graph models. On tasks where structure matters, non-trainable structural proxies often match trained message-passing models. The authors propose invariant baselines should become standard for evaluating whether structure is required for a task and whether models actually learn from it.

Abstract: Progress on graph foundation models is hindered by benchmark practices that conflate the contributions of node features and graph structure, making it hard to tell whether a model actually learns from connectivity, or whether it even needs to. We propose addressing this using graph invariants, i.e., permutation-invariant, task-agnostic structural descriptors that serve as a diagnostic framework for graph benchmarks. We show that (i) invariants are more expressive than standard GNNs, (ii) invariants characterize structural heterogeneity within and across benchmark datasets, (iii) invariants predict multi-task performance, and (iv) simple invariant-based models are competitive with, and sometimes exceed, transformer and message-passing baselines across 26 datasets. Our results suggest that expressivity is not the main driver of predictive performance, and that on tasks where structure matters, a non-trainable structural proxy often matches trained message-passing models. We thus posit that invariant baselines should become a standard for evaluating whether structure is required for a task and whether a model picks up on it, serving as a stepping stone towards graph foundation models.

</details>


### [324] [Diversity Curves for Graph Representation Learning](https://arxiv.org/abs/2605.06466)
*Katharina Limbeck,Nadja Häusermann,Martin Carrasco,Guy Wolf,Bastian Rieck*

Main category: cs.LG

TL;DR: The paper introduces "diversity curves" - interpretable, scalable graph embeddings that track structural diversity across coarsening levels using the novel "spread of graphs" metric, enabling comparison of graphs with different sizes.


<details>
  <summary>Details</summary>
Motivation: Graph comparison remains challenging for graphs with different cardinalities, even from the same distribution. Unsupervised tasks need interpretable, scalable, and reliable size-aware graph representations that can handle graphs of varying sizes.

Method: Proposes tracking structural diversity of graphs across coarsening levels using "diversity curves." Uses edge contraction coarsening and introduces "spread of graphs" - a novel isometry invariant that encodes metric diversity and geometry. Proves this approach improves expressivity over structural descriptors alone.

Result: Diversity curves outperform baseline methods in several applications: (i) clustering and visualizing simulated graphs of varying sizes, (ii) distinguishing geometry of single-cell graphs, (iii) comparing molecular graph dataset structures, and (iv) characterizing geometric shapes.

Conclusion: The proposed diversity curves provide interpretable, efficient, and comparable graph embeddings that address the challenge of comparing graphs with different cardinalities, demonstrating practical utility across diverse applications from biology to geometry.

Abstract: Graph-level representations are crucial tools for characterising structural differences between graphs. However, comparing graphs with different cardinalities, even when sampled from the same underlying distribution, remains challenging. Unsupervised tasks in particular require interpretable, scalable, and reliable size-aware graph representations. Our work addresses these issues by tracking the structural diversity of a graph across coarsening levels. The resulting graph embeddings, which we denote diversity curves, are interpretable by construction, efficient, and directly comparable across coarsening hierarchies. Specifically, we track the spread of graphs, a novel isometry invariant that is inherently well-suited for encoding the metric diversity and geometry of graphs. We utilise edge contraction coarsening and prove that this improves expressivity, thus leading to more powerful graph-level representations than structural descriptors alone. Demonstrating their utility over a range of baseline methods in practice, we use diversity curves to (i) cluster and visualise simulated graphs across varying sizes, (ii) distinguish the geometry of single-cell graphs, (iii) compare the structure of molecular graph datasets, and (iv) characterise geometric shapes.

</details>


### [325] [No Triangulation Without Representation: Generalization in Topological Deep Learning](https://arxiv.org/abs/2605.06467)
*Johannes S. Schmidt,Martin Carrasco,Ernst Röell,Guy Wolf,Nello Blaser,Bastian Rieck*

Main category: cs.LG

TL;DR: The paper introduces a benchmark for evaluating topological deep learning models, showing both GNNs and HOMP methods can achieve high performance but only with proper representation and features, and reveals these models struggle to generalize beyond combinatorial structure.


<details>
  <summary>Details</summary>
Motivation: There is no consensus on how to evaluate topological deep learning models that target higher-order datasets, exacerbated by topological operations (like structural refinements) not being appropriate for graph data.

Method: Extends MANTRA benchmark to more diverse manifolds, evaluates both graph neural networks (GNNs) and higher-order message passing (HOMP) methods, introduces evaluation protocol based on representational diversity and triangulation refinement.

Result: Both GNNs and HOMP methods can saturate the benchmark but only with proper representation and feature assignment. No existing models generalize beyond combinatorial structure of data. New evaluation protocol provides scaffolding for future model development.

Conclusion: There is a research gap in developing models that understand topological structure independent of scale. The work provides necessary scaffolding to evaluate future models and enable topology-aware inductive biases.

Abstract: Despite an ever-increasing interest in topological deep learning models that target higher-order datasets, there is no consensus on how to evaluate such models. This is exacerbated by the fact that topological objects permit operations, such as structural refinements, that are not appropriate for graph data. In this work, we extend MANTRA, a benchmark dataset containing manifold triangulations, to a larger class of manifolds with more diverse homeomorphism types. We show that, unlike prior claims, both graph neural networks (GNNs) and higher-order message passing (HOMP) methods can saturate the benchmark. However, we find that this is contingent on the right representation and feature assignment, emphasizing their importance in baseline models. We thus provide a novel evaluation protocol based on representational diversity and triangulation refinement. Surprisingly, we find no indication that existing models are capable of generalizing beyond the combinatorial structure of the data. This points towards a research gap in developing models that understand topological structure independent of scale. Our work thus provides the necessary scaffolding to evaluate future models and enable the development of topology-aware inductive biases.

</details>


### [326] [Hitting Time Isomorphism for Multi-Stage Planning with Foundation Policies](https://arxiv.org/abs/2605.06470)
*Magnus Victor Boock,Abdullah Akgül,Mustafa Mert Çelikok,Melih Kandemir*

Main category: cs.LG

TL;DR: A new operator-theoretic representation learning framework for offline RL that learns asymmetric directed temporal geometry from hitting times, enabling robust long-horizon planning.


<details>
  <summary>Details</summary>
Motivation: Prior approaches in representation learning for offline RL often produce symmetric distances or fail to satisfy triangle inequality, which limits their ability to capture the directed temporal geometry of controlled Markov processes needed for effective planning.

Method: Proposes Isomorphic Embedding Learning (IEL) - a goal-agnostic foundation policy learning algorithm that learns Hilbert-space displacement geometry where hitting times are realized as linear functionals of latent displacements. Uses HILP-style consistency objective with explicit hitting-time regression.

Result: Theoretical guarantees: representation exists under latent linear closure and is uniquely identifiable up to bounded linear isomorphism; finite-dimensional implementations bound global hitting-time error by one-step transition error amplified by environment's transient spectral radius. Experimental results show IEL improves state-of-the-art for learning foundation policies from offline maze locomotion data.

Conclusion: The proposed operator-theoretic framework successfully recovers the directed temporal geometry of controlled Markov processes, providing a foundation for robust graph-based multi-stage planning in long-horizon navigation tasks.

Abstract: We present a new operator-theoretic representation learning framework for offline reinforcement learning that recovers the directed temporal geometry of a controlled Markov process from hitting time observations. While prior art often produces symmetric distances or fails to satisfy the triangle inequality, our framework learns a Hilbert-space displacement geometry where expected hitting times are realized as linear functionals of latent displacements. We prove that this representation exists under latent linear closure and is uniquely identifiable up to a bounded linear isomorphism. For finite-dimensional implementations, we show that global hitting-time error is bounded by one-step transition error amplified by the environment's transient spectral radius. Furthermore, we provide finite-sample guarantees accounting for approximation, statistical complexity, and trajectory-label mismatch. Derived from this theory, we curate Isomorphic Embedding Learning (IEL) as a new goal-agnostic foundation policy learning algorithm that anchors a HILP-style consistency objective with explicit hitting-time regression to ensure that the learned geometry reflects actual decision-time progress. This asymmetric and compositional structure enables robust graph-based multi-stage planning for long-horizon navigation. Our experiments demonstrate that IEL improves the state of the art of learning foundation policy policies from offline maze locomotion data. Our code can be found on https://github.com/MagnusBoock/IEL

</details>


### [327] [Efficient Serving for Dynamic Agent Workflows with Prediction-based KV-Cache Management](https://arxiv.org/abs/2605.06472)
*Haoyu Zheng,Fangcheng Fu,Jia Wu,Binhang Yuan,Yongqiang Zhang,Hao Wang,Yuanyuan Zhu,Xiao Yan,Jiawei Jiang*

Main category: cs.LG

TL;DR: PBKV is a system for managing KV-Cache in dynamic LLM-based workflows by predicting future agent invocations to optimize cache reuse, achieving significant speedups over existing approaches.


<details>
  <summary>Details</summary>
Motivation: Current KV-Cache management approaches are inefficient for dynamic LLM-based workflows. Agent-level approaches miss reuse opportunities within workflows, while workflow-level approaches assume static agent sequences. Real-world workflows are dynamic where agent sequences and cache reuse opportunities depend on task context.

Method: PBKV predicts future agent invocations by fusing historical workflow patterns with current workflow context. It estimates cache entry reuse potential to keep high-potential entries in GPU memory, and uses conservative prediction strategies during both cache eviction and prefetching to handle prediction errors robustly.

Result: Experiments on three workflow benchmarks show PBKV achieves up to 1.85× speedup over LRU on dynamic workflows, and up to 1.26× speedup over state-of-the-art baseline KVFlow on static workflows.

Conclusion: PBKV effectively addresses the challenge of KV-Cache management in dynamic LLM-based workflows through context-aware prediction of future agent invocations, enabling efficient cache reuse and significant performance improvements over existing approaches.

Abstract: LLM-based workflows compose specialized agents to execute complex tasks, and these agents usually share substantial context, allowing KV-Cache reuse to save computation. Existing approaches either manage KV-Cache at agent level and fail to exploit the reuse opportunities within workflows, or manage cache at the workflow level but assume that each workflow calls a static sequence of agents. However, practical workflows are typically dynamic, where the sequence of invoked agents and thus induced cache reuse opportunities depend on the context of each task. To serve such dynamic workflows efficiently, we build a system dubbed PBKV (\textbf{P}rediction-\textbf{B}ased \textbf{KV}-Cache Management). For each workflow, PBKV predicts the agent invocations in several future steps by fusing the guidance from historical workflows and context of the target workflow. Based on the predictions, PBKV estimates the reuse potential of cache entries and keeps the high-potential entries in GPU memory. To be robust to prediction errors, PBKV utilizes the predictions conservatively during both cache eviction and prefetching. Experiments on three workflow benchmarks show that PBKV achieves up to $1.85\times$ speedup over LRU on dynamic workflows, and up to $1.26\times$ speedup over the SOTA baseline KVFlow on the static workflow.

</details>


### [328] [Q-MMR: Off-Policy Evaluation via Recursive Reweighting and Moment Matching](https://arxiv.org/abs/2605.06474)
*Xiang Li,Nan Jiang*

Main category: cs.LG

TL;DR: Q-MMR is a novel off-policy evaluation method that learns data point weights to match value function moments, achieving dimension-free finite-sample guarantees under only Q-function realizability.


<details>
  <summary>Details</summary>
Motivation: Existing off-policy evaluation methods often face statistical complexity dependencies or require strong coverage assumptions. There's a need for methods with better theoretical guarantees, particularly dimension-free bounds that don't depend on the function class complexity.

Method: Q-MMR learns scalar weights for each data point in a top-down inductive manner through a moment matching objective. The weights are optimized to make the reweighted empirical rewards approximate the target policy's expected return using a value-function discriminator class.

Result: The method achieves finite-sample guarantees for general function approximation with dimension-free bounds, requiring only Q-function realizability. Connections are established to importance sampling and linear FQE, and theoretical analysis provides new insights into coverage concepts in offline RL.

Conclusion: Q-MMR provides a theoretically sound off-policy evaluation framework with strong dimension-free guarantees under minimal assumptions, advancing understanding of coverage and bridging connections to existing methods in offline reinforcement learning.

Abstract: We present a novel theoretical framework, Q-MMR, for off-policy evaluation in finite-horizon MDPs. Q-MMR learns a set of scalar weights, one for each data point, such that the reweighted rewards approximate the expected return under the target policy. The weights are learned inductively in a top-down manner via a moment matching objective against a value-function discriminator class. Notably, and perhaps surprisingly, a data-dependent finite-sample guarantee for general function approximation can be established under only the realizability of $Q^π$, with a dimension-free bound -- that is, the error does not depend on the statistical complexity of the function class. We also establish connections to several existing methods, such as importance sampling and linear FQE. Further theoretical analyses shed new light on the nature of coverage, a concept of fundamental importance to offline RL.

</details>


### [329] [Operator-Guided Invariance Learning for Continuous Reinforcement Learning](https://arxiv.org/abs/2605.06500)
*Zuyuan Zhang,Fei Xu Yu,Tian Lan*

Main category: cs.LG

TL;DR: VPSD-RL discovers exact and approximate value-preserving structures in continuous RL by finding Lie-group operators that commute with value functions, improving data efficiency and robustness through transformation-consistency regularization.


<details>
  <summary>Details</summary>
Motivation: Continuous RL with continuous state/action spaces is data-intensive and brittle under nuisance variability/shift. Existing methods focus on special cases like prescribed symmetries without addressing discovery of more general structures requiring nonlinear operators for continuous systems with isomorphic value functions.

Method: Models continuous RL as controlled diffusion with value-preserving mappings via Lie-group actions and pullback operators. Discovers structures by searching for associated Lie group operators through: learning differentiable drift/diffusion/reward models, learning infinitesimal generators via determining-equation residual minimization, exponentiating them with ODE flows for finite transformations, and integrating them into RL via transition augmentation and transformation-consistency regularization.

Result: Shows bounded generator/reward mismatch implies quantitative stability of optimal value function along approximate orbits with sensitivity governed by effective horizon. Demonstrates improved data efficiency and robustness on continuous-control benchmarks.

Conclusion: VPSD-RL provides a framework for discovering both exact and approximate value-preserving structures in continuous RL, going beyond special symmetry cases to find more general nonlinear transformations that preserve value functions, leading to more stable and efficient learning.

Abstract: Reinforcement learning (RL) with continuous time and state/action spaces is often data-intensive and brittle under nuisance variability and shift, motivating methods that exploit value-preserving structures to stabilize and improve learning. Most existing approaches focus on special cases, such as prescribed symmetries and exact equivariance, without addressing how to discover more general structures that require nonlinear operators to transform and map between continuous state/action systems with isomorphic value functions. We propose \textbf{VPSD-RL} (Value-Preserving Structure Discovery for Reinforcement Learning). It models continuous RL as a controlled diffusion with value-preserving mappings defined through Lie-group actions and associated pullback operators. We show that a value-preserving structure exists exactly when pulling back the value function and pushing forward actions commute with the controlled generator and reward functional. Further, approximate value-preserving structures with rigorous guarantees can be found when the Hamilton--Jacobi--Bellman mismatch is small. This framework discovers exact and approximate value-preserving structures by searching for the associated Lie group operators. VPSD-RL fits differentiable drift, diffusion, and reward models; learns infinitesimal generators via determining-equation residual minimization; exponentiates them with ODE flows to obtain finite transformations; and integrates them into continuous RL through transition augmentation and transformation-consistency regularization. We show that bounded generator/reward mismatch implies quantitative stability of the optimal value function along approximate orbits, with sensitivity governed by the effective horizon, and observe improved data efficiency and robustness on continuous-control benchmarks.

</details>


### [330] [Cubit: Token Mixer with Kernel Ridge Regression](https://arxiv.org/abs/2605.06501)
*Chuanyang Zheng,Jiankai Sun,Yihang Gao,Yuehao Wang,Liangchen Tan,Mac Schwager,Anderson Schneider,Yuriy Nevmyvaka,Xiaodong Liu*

Main category: cs.LG

TL;DR: Cubit is a new transformer architecture that replaces attention with Kernel Ridge Regression (KRR), providing stronger mathematical foundations and better long-sequence modeling than traditional transformers.


<details>
  <summary>Details</summary>
Motivation: The motivation stems from interpreting the attention mechanism in Transformers as performing Nadaraya-Watson regression. The authors seek to improve upon this by leveraging the mathematically stronger Kernel Ridge Regression (KRR) instead, potentially providing better theoretical foundations and performance.

Method: Cubit replaces the classical attention computation with the closed-form solution of Kernel Ridge Regression. It combines value aggregation through kernel similarities with normalization via the inverse of the kernel matrix. To improve training stability, the authors introduce Limited-Range Rescale (LRR) which rescales the value layer within a controlled range.

Result: Experimental results show that Cubit exhibits stronger long-sequence modeling capability compared to vanilla Transformers. The performance gain over Transformers increases as the training sequence length grows.

Conclusion: Cubit, as a KRR-based architecture, provides a stronger mathematical foundation than the vanilla Transformer's Nadaraya-Watson regression approach. The proposed architecture represents a potential next-generation transformer design with improved long-sequence capabilities.

Abstract: Since its introduction in 2017, the Transformer has become one of the most widely adopted architectures in modern deep learning. Despite extensive efforts to improve positional encoding, attention mechanisms, and feed-forward networks, the core token-mixing mechanism in Transformers remains attention. In this work, we show that the attention module in Transformers can be interpreted as performing Nadaraya-Watson regression, where it computes similarities between tokens and aggregates the corresponding values accordingly. Motivated by this perspective, we propose Cubit, a potential next-generation architecture that leverages Kernel Ridge Regression (KRR), while the vanilla Transformer relies on Nadaraya-Watson regression. Specifically, Cubit modifies the classical attention computation by incorporating the closed-form solution of KRR, combining value aggregation through kernel similarities with normalization via the inverse of the kernel matrix. To improve the training stability, we further propose the Limited-Range Rescale (LRR), which rescales the value layer within a controlled range. We argue that Cubit, as a KRR-based architecture, provides a stronger mathematical foundation than the vanilla Transformer, whose attention mechanism corresponds to Nadaraya-Watson regression. We validate this claim through comprehensive experiments. The experimental results suggest that Cubit may exhibit stronger long-sequence modeling capability. In particular, its performance gain over the Transformer appears to increase as the training sequence length grows.

</details>


### [331] [PACZero: PAC-Private Fine-Tuning of Language Models via Sign Quantization](https://arxiv.org/abs/2605.06505)
*Murat Bilgehan Ertan,Xiaochen Zhu,Phuong Ha Nguyen,Marten van Dijk,Srinivas Devadas*

Main category: cs.LG

TL;DR: PACZero is a new privacy mechanism for fine-tuning LLMs that achieves zero mutual information leakage while maintaining usable model utility, outperforming differential privacy in high-privacy regimes.


<details>
  <summary>Details</summary>
Motivation: Differential privacy approaches struggle to provide usable utility at high privacy levels (ε<1), requiring infinite noise to match the privacy level that PACZero achieves at I=0. There's a need for privacy mechanisms that can fine-tune LLMs effectively while providing strong protection against membership inference attacks.

Method: Uses PAC-private zeroth-order mechanisms with sign quantization of subset-aggregated gradients. Key insight: frequent unanimity in sign decisions allows zero conditional mutual information release. Two variants: PACZero-MI (budgeted MI with exact calibration) and PACZero-ZPL (I=0 via uniform coin flip on disagreements).

Result: On SST-2 with OPT-1.3B full fine-tuning at I=0, PACZero-ZPL achieves 88.99±0.91 accuracy, within 2.1 percentage points of non-private MeZO baseline (91.1). No prior method produced usable utility at ε<1. Competitive accuracy on SST-2 and nontrivial SQuAD F1 scores across both OPT-1.3B and OPT-6.7B at I=0.

Conclusion: PACZero enables effective LLM fine-tuning with strong privacy guarantees (I=0) that match the strongest MIA resistance level, addressing the privacy-utility trade-off gap in high-privacy regimes where DP methods fail.

Abstract: We introduce PACZero, a family of PAC-private zeroth-order mechanisms for fine-tuning large language models that delivers usable utility at $I(S^*; Y_{1:T})=0$. This privacy regime bounds the membership-inference attack (MIA) posterior success rate at the prior, an MIA-resistance level the DP framework matches only at $\varepsilon=0$ and infinite noise. All DP-ZO comparisons below are matched at the MIA posterior level. The key insight is that PAC Privacy charges mutual information only when the release depends on which candidate subset is the secret. Sign-quantizing subset-aggregated zeroth-order gradients creates frequent unanimity, steps at which every candidate subset agrees on the update direction; at these steps the released sign costs zero conditional mutual information. We propose two variants that span the privacy-utility trade-off: PACZero-MI (budgeted MI via exact calibration on the binary release) and PACZero-ZPL ($I=0$ via a uniform coin flip on disagreement steps). We evaluate on SST-2 and SQuAD with OPT-1.3B and OPT-6.7B in both LoRA and full-parameter tracks. On SST-2 OPT-1.3B full fine-tuning at $I=0$, PACZero-ZPL reaches ${88.99\pm0.91}$, within $2.1$pp of the non-private MeZO baseline ($91.1$ FT). No prior method produces usable utility in the high-privacy regime $\varepsilon<1$, and PACZero-ZPL obtains competitive SST-2 accuracy and nontrivial SQuAD F1 across OPT-1.3B and OPT-6.7B at $I=0$.

</details>


### [332] [Is One Layer Enough? Understanding Inference Dynamics in Tabular Foundation Models](https://arxiv.org/abs/2605.06510)
*Amir Rezaei Balef,Mykhailo Koshil,Katharina Eggensperger*

Main category: cs.LG

TL;DR: Transformer-based tabular foundation models show significant depthwise redundancy, allowing a single-loop layer design to achieve comparable performance with only 20% of parameters.


<details>
  <summary>Details</summary>
Motivation: Transformer-based tabular foundation models (TFMs) dominate tabular predictive tasks but their inference mechanisms remain largely unexplored, creating a knowledge gap about how these models process tabular data compared to language models.

Method: Conducted first large-scale mechanistic study of layerwise dynamics across 6 state-of-the-art tabular in-context learning models, analyzing how predictions emerge across depth, identifying distinct inference stages, and revealing latent-space dynamics.

Result: Found substantial depthwise redundancy across multiple models with iterative refinement through overlapping computations. Based on these insights, designed a proof-of-concept looped single-layer model using only 20% of original parameters that achieves comparable performance.

Conclusion: Tabular foundation models exhibit different inference dynamics than language models with significant redundancy, enabling more efficient architectures like looped single-layer designs without sacrificing performance.

Abstract: Transformer-based tabular foundation models (TFMs) dominate small to medium tabular predictive benchmark tasks, yet their inference mechanisms remain largely unexplored. We present the first large-scale mechanistic study of layerwise dynamics in 6 state-of-the-art tabular in-context learning models. We explore how predictions emerge across depth, identify distinct stages of inference and reveal latent-space dynamics that differ from those of language models. Our findings indicate substantial depthwise redundancy across multiple models, suggesting iterative refinement with overlapping computations during inference stages. Guided by these insights, we design a proof-of-concept, looped single-layer model that uses only 20% of the original model's parameters while achieving comparable performance. The code is available at https://github.com/amirbalef/is_one_layer_enough.

</details>


### [333] [Efficient Techniques for Data Reconstruction, with Finite-Width Recovery Guarantees](https://arxiv.org/abs/2605.06519)
*Edward Tansley,Roy Makhlouf,Estelle Massart,Coralia Cartis*

Main category: cs.LG

TL;DR: A unified optimization framework for data reconstruction attacks on neural networks, showing provable reconstruction in random feature models, with improved efficiency when exploiting low-dimensional data subspaces.


<details>
  <summary>Details</summary>
Motivation: Data reconstruction attacks pose serious privacy threats when neural networks are trained on sensitive data, but existing approaches lack theoretical guarantees and efficiency for practical scenarios.

Method: Propose unified optimization formulation using initial and trained parameters; prove theoretical reconstruction guarantees in random feature models; develop subspace-aware algorithm that estimates low-dimensional data structure through first-layer weight changes and uses only last-layer weights for reconstruction.

Result: Show provable training data reconstruction with high probability for sufficiently wide networks using PAC-style bounds; demonstrate that subspace structure reduces width requirements; numerical experiments on synthetic data and CIFAR-10 show superior performance compared to full-space methods.

Conclusion: Theoretical and algorithmic framework establishes formal reconstruction guarantees and practical efficiency improvements by exploiting data subspace structure, making data reconstruction attacks more effective with reduced computational requirements.

Abstract: Data reconstruction attacks on trained neural networks aim to recover the data on which the network has been trained and pose a significant threat to privacy, especially if the training dataset contains sensitive information. Here, we propose a unified optimization formulation of the data reconstruction problem based on initial and trained parameter values, incorporating state-of-the-art proposals. We show that in the random feature model, this formulation provably leads to training data reconstruction with high probability, provided the network width is sufficiently large; this unprecedented finite-width result uses PAC-style bounds. Furthermore, when the data lies in a low-dimensional subspace, we show that the network width requirement for successful reconstruction can be relaxed, with bounds depending on the subspace dimension rather than the ambient dimension. For general neural network models and unknown data orientations, we propose an efficient reconstruction algorithm that approximates the low-dimensional data subspace through the change in the first-layer weights during training and uses only the last-layer weights for reconstruction, thus reducing the search space dimension and the required network width for high-quality reconstructions. Our numerical experiments on synthetic datasets and CIFAR-10 confirm that our subspace-aware reconstruction approach outperforms standard full-space techniques.

</details>


### [334] [Agentic AIs Are the Missing Paradigm for Out-of-Distribution Generalization in Foundation Models](https://arxiv.org/abs/2605.06522)
*Xin Wang,Haibo Chen,Wenxuan Liu,Wenwu Zhu*

Main category: cs.LG

TL;DR: OOD phenomena in foundation models require agentic systems, not just model-centric approaches, to handle distribution shifts beyond what parameter-based representations can achieve.


<details>
  <summary>Details</summary>
Motivation: Foundation models face unique OOD challenges in open-world settings (knowledge boundaries, capability ceilings, compositional shifts, task variations) that differ from traditional OOD problems, compounded by partially observed pretraining and post-training distributions.

Method: Four-step approach: 1) Formalize stage-aware OOD with multi-stage training distributions, 2) Prove parameter coverage ceiling showing limits of model-centric methods, 3) Characterize agentic OOD systems with four structural properties, 4) Respond to counterarguments and outline research agenda.

Result: Agentic systems with perception, strategy selection, external action, and closed-loop verification can handle OOD cases beyond what parameter-based model-centric methods can achieve, establishing agentic paradigm as complementary and necessary for FM-OOD.

Conclusion: OOD for foundation models is a structurally distinct problem requiring the agentic paradigm as a first-class research direction alongside model-centric approaches, as no single approach can handle all OOD scenarios.

Abstract: Foundation models (FMs) are increasingly deployed in open-world settings where distribution shift is the rule rather than the exception. The out-of-distribution (OOD) phenomena they face -- knowledge boundaries, capability ceilings, compositional shifts, and open-ended task variation -- differ in kind from the settings that have shaped prior OOD research, and are further complicated because the pretraining and post-training distributions of modern FMs are often only partially observed. Our position is that OOD for foundation models is a structurally distinct problem that cannot be solved within the prevailing model-centric paradigm, and that agentic systems constitute the missing paradigm required to address it. We defend this claim through four steps. First, we give a stage-aware formalization of OOD that accommodates partially observed multi-stage training distributions. Second, we prove a parameter coverage ceiling: there exist practically relevant inputs that no model-centric method (training-time or test-time) can handle within tolerance $\varepsilon$, for reasons intrinsic to parameter-based representation. Third, we characterize agentic OOD systems by four structural properties -- perception, strategy selection, external action, and closed-loop verification -- and show that they strictly extend the reachable set beyond the ceiling. Fourth, we respond to seven counterarguments, conceding two, and outline a research agenda. We do not claim that agentic methods subsume model-centric ones; we argue that the two are complementary, and that progress on FM-OOD requires explicit recognition of the agentic paradigm as a first-class research direction.

</details>


### [335] [On the Implicit Reward Overfitting and the Low-rank Dynamics in RLVR](https://arxiv.org/abs/2605.06523)
*Hao Ye,Jisheng Dang,Junfeng Fang,Bimei Wang,Yizhou Zhang,Ning Lv,Wencan Zhang,Hong Peng,Bin Hu,Tat-Seng Chua*

Main category: cs.LG

TL;DR: RLVR training concentrates reasoning capabilities in rank-1 components, but exhibits implicit reward overfitting where test performance remains good despite low training rewards, revealing insights about RL parameter shaping.


<details>
  <summary>Details</summary>
Motivation: Recent research shows enhanced reasoning from RLVR concentrates in rank-1 components, motivating investigation into how RLVR shapes model parameters and whether it suffers from reward overfitting issues.

Method: Used Periodic Rank-1 Substitution to analyze RLVR training, examining singular value distributions, singular vector alignments, and component substitution effects.

Result: Found RLVR exhibits implicit reward overfitting; rank-1 components don't preserve other knowledge; singular values follow heavy-tailed distributions; left singular vectors show alignment tendency during training.

Conclusion: RLVR fundamentally optimizes specific singular spectra and sampling efficiency, offering insights for improving RL paradigms and continual learning approaches through better understanding of parameter shaping.

Abstract: Recent extensive research has demonstrated that the enhanced reasoning capabilities acquired by models through Reinforcement Learning with Verifiable Rewards (RLVR) are primarily concentrated within the rank-1 components. Predicated on this observation, we employed Periodic Rank-1 Substitution and identified a counterintuitive phenomenon: RLVR may exhibit implicit reward overfitting to the training dataset. Specifically, the model can achieve satisfactory performance on the test set even when its rewards remain relatively low during the training process. Furthermore, we characterize three distinct properties of RL training: (1) The effective rank-1 component in RLVR don't maintain other model knowledge except mathematical reasoning capability. (2) RLVR fundamentally functions by optimizing a specific singular spectrum. The distribution of singular values of almost all linear layers in RLVR-trained model behaves like heavy-tailed distribution. (3) the left singular vectors associated with rank-1 components demonstrate a stronger alignment tendency during training, which echoes the discovery that RLVR is optimizing sampling efficiency in essence. Taken together, our findings and analysis further reveal how RLVR shapes model parameters and offer potential insights for improving existing RL paradigms or other training paradigms to implement continual learning.

</details>


### [336] [Diffusion-Based Posterior Sampling: A Feynman-Kac Analysis of Bias and Stability](https://arxiv.org/abs/2605.06538)
*Matias G. Delgadino,Sebastien Motsch,Advait Parulekar,William Porteous,Sanjay Shakkottai*

Main category: cs.LG

TL;DR: The paper analyzes bias in diffusion-based posterior samplers, develops theoretical framework to characterize sampler bias via PDE analysis and Feynman-Kac representation, and applies the framework to understand and improve existing samplers like DPS and STSL.


<details>
  <summary>Details</summary>
Motivation: Diffusion-based posterior samplers are widely used for inverse problems but their theoretical behavior remains poorly understood - they produce biased outputs even with exact prior scores, and their discretizations can become unstable in low-temperature regimes.

Method: Introduces a tractable surrogate path connecting the true posterior to a standard Gaussian, compares it to the sampler's path, derives a parabolic PDE for their density ratio, uses Feynman-Kac representation to express Radon-Nikodym correction as an explicit path expectation.

Result: Framework applied to DPS reveals correction as Ornstein-Uhlenbeck path expectation coupling data conditional covariance with reward curvature; STSL reinterpreted as auxiliary drift that steers trajectories toward low-uncertainty regions; early guidance-stopping characterized as mitigation for low-temperature instabilities.

Conclusion: The theoretical framework clarifies sampler bias, explains existing corrective measures, and provides guidance for designing stable variants of diffusion-based posterior samplers used in inverse problems.

Abstract: Diffusion-based posterior samplers use pretrained diffusion priors to sample from measurement- or reward-conditioned posteriors, and are widely used for inverse problems. Yet their theoretical behavior remains poorly understood: even with exact prior scores, their outputs are biased, and in low-temperature regimes their discretizations can become unstable. We characterize this bias by introducing a tractable surrogate path connecting the true posterior to a standard Gaussian and comparing it to the sampler's path. Their density ratio satisfies a parabolic PDE whose reaction term measures the accumulated bias. A Feynman-Kac representation then expresses the Radon-Nikodym correction as an explicit path expectation, identifying which posterior regions are over- or under-sampled.
  We apply this framework to DPS and STSL, a related sampler. For DPS, the correction is an Ornstein-Uhlenbeck path expectation coupling the data conditional covariance with the reward curvature, revealing where DPS over- or under-samples. Next, we reinterpret STSL as an auxiliary drift that steers trajectories toward low-uncertainty regions, flattening the spatially varying part of the DPS reaction term. Finally, we characterize early guidance-stopping, a common mitigation for low-temperature instabilities caused by forward-Euler integration of the vector field. Together, these results clarify sampler bias, explain existing correctives, and guide stable variant designs.

</details>


### [337] [Hedging Memory Horizons for Non-Stationary Prediction via Online Aggregation](https://arxiv.org/abs/2605.06541)
*Yutong Wang,Yannig Goude,Qiwei Yao*

Main category: cs.LG

TL;DR: MELO is a model-agnostic online prediction method that hedges across adaptation scales to handle distribution shift without needing regime indicators or model retraining, achieving 34.7% RMSE reduction in electricity load forecasting during COVID-19 lockdowns.


<details>
  <summary>Details</summary>
Motivation: Online prediction faces challenges of distribution shift where predictors must balance stability during quiet regimes with adaptation during regime shifts, without knowing the optimal adaptation memory in advance.

Method: MELO (Memory-hedged Exponentially Weighted Least-Squares Online aggregation) wraps base predictors with EWLS adaptation experts at multiple forgetting factors, then aggregates raw and adapted forecasts using the parameter-free MLpol aggregation rule.

Result: Theoretical: deterministic oracle inequalities showing competition with best raw predictor and best time-varying affine combinations. Empirical: 34.7% RMSE reduction in French electricity-load forecasting during COVID-19 lockdowns, outperforming methods with external COVID policy covariates.

Conclusion: MELO provides an effective, lightweight approach for online prediction under distribution shift without needing regime indicators, requiring only per-step recursive updates and no model retraining, making it practical for real-world applications.

Abstract: We study online prediction under distribution shift, where inputs arrive chronologically and outcomes are revealed only after prediction. In this setting, predictors must remain stable in quiet regimes yet adapt when regimes shift, and the right adaptation memory is unknown in advance. We propose MELO (Memory-hedged Exponentially Weighted Least-Squares Online aggregation), a model-agnostic method that hedges across adaptation scales: it wraps any non-anticipating base-predictor pool with exponentially weighted least-squares (EWLS) adaptation experts at multiple forgetting factors, and aggregates raw and EWLS-adapted forecasts with MLpol, a parameter-free online aggregation rule. Under boundedness conditions, we establish deterministic oracle inequalities showing that it competes with both the best raw predictor and the best bounded, time-varying affine combinations of the base predictions, up to a path-length-dependent tracking cost and a sublinear aggregation overhead. We evaluate MELO on French national electricity-load forecasting through the COVID-19 lockdown using no regime indicators, lockdown dates, or policy covariates. MELO reduces overall RMSE by 34.7\% relative to base-only MLpol and achieves lower overall RMSE than a TabICL reference supplied with an external COVID policy-response covariate. Moreover, MELO requires only lightweight per-step recursive updates without model retraining.

</details>


### [338] [Sequential Design of Genetic Circuits Under Uncertainty With Reinforcement Learning](https://arxiv.org/abs/2605.06552)
*Michal Kobiela,Diego A. Oyarzún,Michael U. Gutmann*

Main category: cs.LG

TL;DR: A sequential framework for optimizing genetic circuits under dual uncertainty (intrinsic stochasticity + lab variability) using reinforcement learning and amortized training to enable immediate adaptation without costly parameter inference.


<details>
  <summary>Details</summary>
Motivation: Biological system design is hindered by two forms of uncertainty: intrinsic stochasticity of biomolecular reactions and variability across laboratory/experimental conditions. Existing Bayesian methods require computationally expensive inference and optimization steps after each experimental round, causing delays.

Method: Proposes a sequential framework using simulator models (differential equations or Markov jump processes) with reinforcement learning policy-based approach. Features an amortized approach trained upfront across a distribution of possible uncertain parameters, eliminating explicit parameter inference during design cycles for immediate observation-based adaptation.

Result: Demonstrated on models for heterologous gene expression and repressilator circuits, showing efficient handling of both molecular noise and cross-laboratory variability.

Conclusion: The amortized reinforcement learning framework overcomes computational bottlenecks of previous methods by enabling immediate adaptation to uncertainties without expensive parameter inference, making genetic circuit optimization more efficient under dual uncertainty conditions.

Abstract: The design of biological systems is hindered by uncertainty arising from both intrinsic stochasticity of biomolecular reactions and variability across laboratory or experimental conditions. In this work, we present a sequential framework to optimize genetic circuits under both forms of uncertainty. By employing simulator models based on differential equations or Markov jump processes alongside a reinforcement learning (RL) policy-based approach, our method suggests experiments that adapt to unknown laboratory conditions while accounting for inherent stochasticity. While previous Bayesian methods address uncertainty through iterative experiment-inference-optimization cycles, they typically require computationally expensive inference and optimization steps after each experimental round, leading to delays. To overcome this bottleneck, we propose an amortized approach trained up-front across a distribution of possible uncertain parameters. This strategy sidesteps the need for explicit parameter inference during the design cycle, enabling immediate, observation-based adaptation. We demonstrate our framework on models for heterologous gene expression and a repressilator circuit, showing that it efficiently handles both molecular noise and cross-laboratory variability.

</details>


### [339] [Diverse Sampling in Diffusion Models with Marginal Preserving Particle Guidance](https://arxiv.org/abs/2605.06553)
*Gal Vinograd,Idan Achituve,Ethan Fetaya*

Main category: cs.LG

TL;DR: EDDY is a guidance mechanism for diffusion/flow matching models that promotes sample diversity while maintaining quality by using divergence-free dynamics that preserve marginal distributions.


<details>
  <summary>Details</summary>
Motivation: Current diffusion and flow matching models often generate low-diversity samples, especially in text-to-image generation where multiple high-quality samples tend to be similar. There's a need to increase diversity among generated samples without sacrificing quality or requiring retraining.

Method: EDDY exploits symmetries of the Fokker-Planck equation by applying drift perturbations that change particle trajectories while preserving the evolving marginal distribution. It uses kernel-based anti-symmetric pairwise matrix fields constructed from repulsive directions to create divergence-free dynamics. The approach works without additional training, though computational approximations are proposed for expensive cases like text-to-image generation with perceptual embeddings.

Result: Experiments on synthetic distributions and text-to-image generation show EDDY improves diversity while maintaining strong distributional fidelity compared to common baselines. The guidance mechanism effectively promotes diversity at the joint particle level while preserving each particle's marginal distribution.

Conclusion: EDDY provides an effective guidance mechanism for diffusion/flow matching models that successfully addresses the diversity-quality trade-off, offering practical solutions through approximations for computationally expensive applications while maintaining distributional fidelity.

Abstract: We present EDDY (Exact-marginal Diversification via Divergence-free dYnamics), a guidance mechanism for diffusion and flow matching models that promotes diversity among samples generated while maintaining quality. EDDY exploits symmetries of the Fokker-Planck equation, using drift perturbations that change particle trajectories while preserving the evolving marginal distribution. We instantiate this principle through kernel-based anti-symmetric pairwise matrix fields, constructed from the repulsive directions. The resulting divergence-free dynamics promote diversity at the joint particle level while preserving each particle's marginal distribution without any additional training. As computing the guidance can be computationally expensive in cases such as text-to-image generation with perceptual embeddings, we propose practical approximations as an effective and efficient solution. Experiments on synthetic distributions and text-to-image generation show that EDDY improves diversity while maintaining strong distributional fidelity compared to common baselines.

</details>


### [340] [Optimal Counterfactual Search in Tree Ensembles: A Study Across Modeling and Solution Paradigms](https://arxiv.org/abs/2605.06561)
*Awa Khouna,Youssouf Emine,Julien Ferry,Thibaut Vidal*

Main category: cs.LG

TL;DR: CPCF is a constraint programming approach for computing optimal counterfactual explanations in tree ensembles that outperforms MaxSAT and MILP methods, with CP being most versatile overall.


<details>
  <summary>Details</summary>
Motivation: Trust in counterfactual explanations depends on whether recommended changes are truly minimal. Heuristic errors in current methods can lead to overshooting necessary changes and affect individuals unevenly, giving some users unnecessarily costly recommendations.

Method: The authors propose CPCF, a constraint programming formulation that encodes numerical features as interval domains induced by split thresholds and discrete features as finite-domain representations. They compare it with extended MaxSAT formulation (for soft-voting ensembles) and state-of-the-art MILP optimal approach across mathematical programming paradigms.

Result: Across ten datasets and three types of tree ensembles, CP achieves the best overall performance. The analysis identifies specific regimes where each paradigm excels: CP is most versatile, MaxSAT handles hard-voting ensembles particularly well, and MILP remains competitive in amortized inference settings with moderate split levels.

Conclusion: Constraint programming (CP) provides the most effective overall approach for computing optimal counterfactual explanations in tree ensembles, though different mathematical programming paradigms have distinct strengths suitable for specific regimes, allowing practitioners to choose based on ensemble type and inference context.

Abstract: Trust in counterfactual explanations depends critically on whether their recommended changes are truly minimal: suboptimal explanations may vastly overshoot the actual changes needed to alter a decision, and heuristic errors can affect individuals unevenly, giving some users relevant recourse while assigning others unnecessarily costly recommendations. Consequently, we study the problem of computing optimal counterfactual explanations for tree ensembles under plausibility and actionability constraints. This is a combinatorial problem: for a fixed model, counterfactual search boils down to selecting consistent branching decisions and threshold-defined regions under a distance objective. We exploit this structure through CPCF, a constraint programming (CP) formulation in which numerical features are encoded as interval domains induced by split thresholds, while discrete features retain native finite-domain representations. This yields a compact finite-domain formulation that supports multiple distance objectives without continuous split-boundary search. We then place CPCF in a broader comparison across mathematical programming paradigms: we extend a maximum Boolean satisfiability (MaxSAT) formulation, originally designed for hard-voting random forests, to soft-voting ensembles, and compare against the current state-of-the-art mixed-integer linear programming (MILP) optimal approach. Across ten datasets and three types of tree ensembles, we analyze scalability, anytime performance, and sensitivity to distance metrics. We observe that CP achieves the best overall performance. More importantly, our results identify regimes in which the specific strengths of each paradigm make it best suited: CP is most versatile overall, MaxSAT handles hard-voting ensembles particularly well, and MILP remains competitive in amortized inference settings with a moderate number of split levels.

</details>


### [341] [Feature Dimensionality Outweighs Model Complexity in Breast Cancer Subtype Classification Using TCGA-BRCA Gene Expression Data](https://arxiv.org/abs/2605.06562)
*Meena Al Hasani*

Main category: cs.LG

TL;DR: Study compares logistic regression, random forest, and SVM for breast cancer subtype classification using TCGA gene expression data, finding logistic regression performs most consistently across subtypes despite high dimensionality challenges.


<details>
  <summary>Details</summary>
Motivation: Breast cancer subtype classification from gene expression data is crucial for diagnosis and treatment, but faces challenges due to high dimensionality and limited sample sizes, creating difficulties for machine learning models.

Method: Used TCGA-BRCA gene expression data with varying numbers of highly variable genes (50 to 20,518). Trained logistic regression, random forest, and SVM models. Evaluated performance using stratified 5-fold cross-validation with accuracy and macro F1 score metrics.

Result: All models achieved high accuracy, but macro F1 analysis revealed significant subtype-level differences. Logistic regression showed most stable and balanced performance across all subtypes, including better detection of rare classes. Random forest underperformed on minority subtypes despite high overall accuracy, while SVM was sensitive to feature dimensionality.

Conclusion: Highlights the importance of model simplicity, appropriate evaluation metrics (macro F1 over accuracy), and careful feature selection in high-dimensional biological classification tasks, with logistic regression performing best despite its simplicity.

Abstract: Accurate classification of breast cancer subtypes from gene expression data is critical for diagnosis and treatment selection. However, such datasets are characterized by high dimensionality and limited sample size, posing challenges for machine learning models.
  In this study, we evaluate the impact of model complexity and feature selection on subtype classification performance using TCGA-BRCA gene expression data. Logistic regression, random forest, and support vector machine (SVM) models were trained using varying numbers of highly variable genes (50 to 20,518). Performance was evaluated using stratified 5-fold cross-validation and assessed with accuracy and macro F1 score. While all models achieved high accuracy, macro F1 analysis revealed substantial differences in subtype-level performance. Logistic regression demonstrated the most stable and balanced performance across subtypes, including improved detection of rare classes. Random forest underperformed on minority subtypes despite strong overall accuracy, while SVM showed sensitivity to feature dimensionality. These findings highlight the importance of model simplicity, evaluation metrics, and feature selection in high-dimensional biological classification tasks.

</details>


### [342] [Criticality and Saturation in Orthogonal Neural Networks](https://arxiv.org/abs/2605.06563)
*Max Guillen,Jan E. Gerken*

Main category: cs.LG

TL;DR: The paper develops theoretical tools to explain why orthogonal weight initialization stabilizes neural network training better than i.i.d. Gaussian initialization, particularly at large depth.


<details>
  <summary>Details</summary>
Motivation: Orthogonal weight initialization is known to improve training performance compared to i.i.d. Gaussian initialization, but the theoretical understanding of this phenomenon has been incomplete. Recent empirical results showed that finite-width tensors stabilize for large depth in orthogonal-initialized networks, unlike in i.i.d.-initialized networks.

Method: The authors derive explicit layer-wise recursion relations for tensors appearing in the finite-width expansion of network statistics for orthogonal initializations. They extend Feynman diagram techniques from i.i.d. case to work to all orders in 1/width. They analytically show that their recursions reproduce the stability observed for activation functions with vanishing fixed point.

Result: The paper provides theoretical explanation for stability of finite-width orthogonal-initialized networks. The derived recursions successfully reproduce the observed tensor stabilization. Numerical solutions of recursion relations and analytical large-depth expansions match well with Monte-Carlo estimates from network ensembles.

Conclusion: This work closes a long-standing gap in literature by providing a theoretical foundation for understanding why orthogonal initialization leads to better training performance through stabilization of finite-width tensors at large network depths.

Abstract: It has been known for a long time that initializing weight matrices to be orthogonal instead of having i.i.d. Gaussian components can improve training performance. This phenomenon can be analyzed using finite-width corrections, where the infinite-width statistics are supplemented by a power series in $1/\mathrm{width}$. In particular, recent empirical results by Day et al. show that the tensors appearing in this treatment stabilize for large depth, as opposed to the tensors of i.i.d.-initialized networks. In this article, we derive explicit layer-wise recursion relations for the tensors appearing in the finite-width expansion of the network statistics in the case of orthogonal initializations. We also provide an extension of recently-introduced Feynman diagrams for the corresponding recursions in the i.i.d.-case which are valid to all orders in $1/\mathrm{width}$. Finally, we show explicitly that the recursions we derive reproduce the stability of the finite-width tensors which was observed for activation functions with vanishing fixed point. This work therefore provides a theoretical explanation for the stability of nonlinear networks of finite width initialized with orthogonal weights, closing a long-standing gap in the literature. We validate our theoretical results experimentally by showing that numerical solutions of our recursion relations and their analytical large-depth expansions agree excellently with Monte-Carlo estimates from network ensembles.

</details>


### [343] [SNAPO: Smooth Neural Adjoint Policy Optimization for Optimal Control via Differentiable Simulation](https://arxiv.org/abs/2605.06570)
*Dmitri Goloubentsev,Natalija Karpichina*

Main category: cs.LG

TL;DR: SNAPO is a framework that embeds neural policies in differentiable simulators to compute exact gradients for all policy parameters and inputs in a single adjoint pass, enabling fast training and sensitivity analysis across various domains.


<details>
  <summary>Details</summary>
Motivation: Real-world sequential decision problems under uncertainty (gas storage, portfolio rebalancing, pharmaceutical manufacturing) face limitations: dynamic programming scales poorly with state dimensions, while black-box reinforcement learning trains slowly and lacks sensitivity analysis capabilities.

Method: SNAPO (Smooth Neural Adjoint Policy Optimization) embeds neural policies in known differentiable simulators, replaces hard constraints with smooth approximations, and computes exact gradients of objectives with respect to all policy parameters and inputs through a single adjoint pass.

Result: Demonstrated across three domains: natural gas storage (under 1 min training, 365 forward curve sensitivities), pension fund management (6.5x-200x sensitivity speedup), and pharmaceutical manufacturing (20 regulatory sensitivities in 74.5ms). All sensitivities computed in same backward pass as training.

Conclusion: SNAPO provides an efficient framework combining neural policies with differentiable simulators, enabling both fast policy optimization and comprehensive sensitivity analysis at the computational cost of a single backward pass regardless of how many sensitivities are computed.

Abstract: Many real-world problems require sequential decisions under uncertainty: when to inject or withdraw gas from storage, how to rebalance a pension portfolio each month, what temperature profile to run through a pharmaceutical reactor chain. Dynamic programming solves small instances exactly but scales exponentially in state dimensions. Black-box reinforcement learning handles high-dimensional states but trains slowly and produces no sensitivities. We introduce SNAPO (Smooth Neural Adjoint Policy Optimization), a framework that embeds a neural policy inside a known, differentiable simulator, replaces hard constraints with smooth approximations, and computes exact gradients of the objective with respect to all policy parameters and all inputs in a single adjoint pass. We demonstrate SNAPO on three domains: natural gas storage (training in under a minute, 365 forward curve sensitivities at no additional cost per sensitivity), pension fund asset-liability management (6.5x-200x sensitivity speedup over bump-and-revalue, scaling with the number of risk factors), and pharmaceutical manufacturing (cross-unit sensitivities through a 4-unit process chain, with 20 ICH Q8 regulatory sensitivities from 5 adjoint passes in 74.5 milliseconds). All sensitivities are produced by the same backward pass that trains the policy, at a cost proportional to one reverse pass regardless of how many sensitivities are computed.

</details>


### [344] [CLAD: A Clustered Label-Agnostic Federated Learning Framework for Joint Anomaly Detection and Attack Classification](https://arxiv.org/abs/2605.06571)
*Iason Ofeidis,Nikos Papadis,Randeep Bhatia,Leandros Tassiulas,TV Lakshman*

Main category: cs.LG

TL;DR: CLAD: A federated learning framework combining clustered FL with dual-mode architecture for IoT intrusion detection, addressing device heterogeneity and label scarcity.


<details>
  <summary>Details</summary>
Motivation: IoT/IIoT expansion creates massive heterogeneous attack surfaces; traditional security fails to handle device diversity. Federated learning (FL) preserves privacy but struggles to generalize across diverse devices and cannot utilize abundant unlabeled data in edge environments.

Method: CLAD framework combines Clustered Federated Learning (CFL) with Dual-Mode Micro-Architecture (DM²A). CFL groups devices with similar traffic patterns. DM²A has shared encoder with two branches for joint unsupervised anomaly detection and supervised attack classification, utilizing both labeled/unlabeled clients.

Result: Significant performance improvement: 30% relative improvement in detection performance in scenarios with 80% unlabeled clients, with only half the communication cost compared to state-of-the-art baselines.

Conclusion: CLAD's integrated approach effectively addresses IoT security bottlenecks of device heterogeneity and label scarcity by leveraging all available data and preserving distinct operational patterns through clustering and dual-mode architecture.

Abstract: The rapid expansion of the Internet of Things (IoT) and Industrial IoT (IIoT) has created a massive, heterogeneous attack surface that challenges traditional network security mechanisms. While Federated Learning (FL) offers a privacy-preserving alternative to centralized Intrusion Detection Systems (IDS), standard approaches struggle to generalize across diverse device behaviors and typically fail to utilize the vast amounts of unlabeled data present in realistic edge environments. To bridge these gaps, we propose CLAD, a holistic framework that seamlessly incorporates Clustered Federated Learning (CFL) with a novel Dual-Mode Micro-Architecture ($\text{DM}^2\text{A}$). This unified approach simultaneously tackles the two primary bottlenecks of IoT security: device heterogeneity and label scarcity. The $\text{DM}^2\text{A}$ component features a shared encoder followed by two branches, enabling joint unsupervised anomaly detection and supervised attack classification; this allows the framework to harvest intelligence from both labeled and unlabeled clients. Concurrently, the clustering component dynamically groups devices with congruent traffic patterns, preventing global model divergence. By carefully combining these elements, CLAD ensures that no data is discarded and distinct operational patterns are preserved. Extensive evaluations demonstrate that this integrated approach significantly outperforms state-of-the-art baselines, achieving a 30% relative improvement in detection performance in scenarios with 80% unlabeled clients, with only half the communication cost.

</details>


### [345] [Directional Consistency as a Complementary Optimization Signal: The GONO Framework](https://arxiv.org/abs/2605.06575)
*Victor Daniel Gera*

Main category: cs.LG

TL;DR: The paper introduces GONO, a novel optimizer that adapts momentum based on directional consistency of gradients, addressing the decoupling between gradient alignment and loss convergence in existing optimizers.


<details>
  <summary>Details</summary>
Motivation: Existing optimizers like Adam, SGD, and RMSprop lack explicit mechanisms to exploit temporal consistency in gradient directions. They rely on magnitude-based signals that cannot distinguish between optimization plateaus, saddle points, and genuine convergence, even when gradients show near-perfect directional consistency (high cosine similarity).

Method: GONO (Gradient-Oriented Norm-Adaptive Optimizer) adapts Adam's momentum coefficient beta_1 based on consecutive gradient cosine similarity (cc_t). It amplifies momentum when gradients show directional consistency and suppresses it during oscillations. The optimizer reduces exactly to Adam when gradient direction signals are uninformative.

Result: Theoretical: GONO maintains Adam's O(1/sqrt(T)) convergence rate. Empirical: cc_t achieves perfect oscillation detection (F1=1.00 vs 0.45 for gradient norm). Performance remains competitive with AdamW on MNIST (98.15%), CIFAR-10 (43.14%), and ResNet-18 (75.44%).

Conclusion: Directional alignment is a theoretically grounded, practically actionable optimization signal. GONO successfully exploits temporal consistency in gradient directions while maintaining competitive performance, addressing a fundamental limitation of existing magnitude-based optimization approaches.

Abstract: We identify and formalize an underexplored phenomenon in deep learning optimization: directional alignment and loss convergence can be decoupled. An optimizer can exhibit near-perfect directional consistency (cc_t -> 1, measured via consecutive gradient cosine similarity) while the loss remains high or decreases slowly. This observation reveals that existing optimizers such as Adam, SGD, and RMSprop lack explicit mechanisms to exploit temporal consistency in gradient directions, relying instead on magnitude-based signals that fail to distinguish plateaus, saddle points, and genuine convergence. Motivated by this, we introduce GONO (Gradient-Oriented Norm-Adaptive Optimizer), which adapts Adam's momentum coefficient beta_1 based on cc_t: amplifying momentum under directional consistency and suppressing it during oscillation. We prove GONO matches Adam's O(1/sqrt(T)) convergence rate and reduces exactly to Adam when the signal is uninformative. Empirically, cc_t achieves oscillation detection with F1=1.00 (vs. 0.45 for gradient norm), and GONO remains competitive with AdamW on MNIST (98.15%), CIFAR-10 (43.14%), and ResNet-18 (75.44%), establishing directional alignment as a theoretically grounded, practically actionable optimization signal. Code: https://github.com/victordaniel/gono-optimizer

</details>


### [346] [On the Safety of Graph Representation Learning](https://arxiv.org/abs/2605.06576)
*Xiaoguang Guo,Zehong Wang,Ziming Li,Shawn Spitzel,Soonwoo Kwon,Tianyi Ma,Yanfang Ye,Chuxu Zhang*

Main category: cs.LG

TL;DR: GRL-Safety introduces a multi-axis safety evaluation benchmark for graph representation learning methods, assessing twelve representative methods across five safety axes: corruption robustness, OOD generalization, class imbalance, fairness, and interpretation.


<details>
  <summary>Details</summary>
Motivation: Existing GRL evaluations focus mainly on clean transfer, adaptation, and task coverage, but lack systematic assessment of reliability under deployment stresses affecting graph signals, contexts, label support, structural groups, or predictive evidence.

Method: Created GRL-Safety benchmark evaluating twelve representative methods spanning topology-only embeddings, supervised GNNs, self-supervised graph models, and graph foundation models on twenty-five datasets with standardized evaluation conditions while preserving method-native adaptation.

Result: Three key findings: 1) Safety behavior depends on interaction between representation design and stressed graph factor, not just method family; 2) Foundation-era methods show axis-specific strengths rather than broad safety dominance; 3) Several deployment regimes remain difficult even for best methods, revealing capability gaps requiring new robustness objectives.

Conclusion: GRL-Safety provides a comprehensive safety evaluation benchmark revealing critical insights about GRL method reliability under real-world stresses, highlighting the need for new robustness, adaptation, and training objectives beyond model selection.

Abstract: Graph representation learning (GRL) has evolved from topology-only graph embeddings to task-specific supervised GNNs, and more recently to reusable representations and graph foundation models (GFMs). However, existing evaluations mainly measure clean transfer, adaptation, and task coverage. It remains unclear whether GRL methods stay reliable when deployment stresses affect graph signals, graph contexts, label support, structural groups, or predictive evidence. We introduce GRL-Safety, a multi-axis safety evaluation benchmark for GRL. GRL-Safety evaluates twelve representative methods, spanning topology-only embedding methods, supervised GNNs, self-supervised graph models, and GFMs, on twenty-five graph datasets under standardized evaluation conditions while preserving method-native adaptation. The evaluation covers five safety axes: corruption robustness, OOD generalization, class imbalance, fairness, and interpretation, with per-axis and sub-condition reporting rather than a single aggregate score. Our analysis yields three cross-axis insights that can inspire future research. First, safety behavior is shaped by the interaction between representation design and the stressed graph factor, rather than by method family alone. Second, foundation-era methods show axis-specific strengths rather than broad safety dominance. Third, several deployment regimes remain difficult even for the best evaluated method, revealing capability gaps that require new robustness, adaptation, or training objectives beyond model selection. The benchmark, evaluation protocols, and code are available at: https://github.com/GXG-CS/GRL-Safety.

</details>


### [347] [PairAlign: A Framework for Sequence Tokenization via Self-Alignment with Applications to Audio Tokenization](https://arxiv.org/abs/2605.06582)
*Adhiraj Banerjee,Vipul Arora*

Main category: cs.LG

TL;DR: PairAlign introduces a sequence-level self-alignment framework for compact audio tokenization that treats tokenization as conditional sequence generation, achieving better edit-distance preservation and length control than existing methods.


<details>
  <summary>Details</summary>
Motivation: Existing audio tokenizers rely on quantization, clustering, or codec reconstruction which assign tokens locally, failing to optimize for sequence consistency, compactness, length control, termination, and edit similarity directly.

Method: PairAlign treats tokenization as conditional sequence generation with an encoder-decoder architecture. It learns from content-preserving views where each view's sequence is trained to be likely under the other's representation, while unrelated examples provide competing sequences. The approach uses VQ-style tokenization refined with EMA-teacher targets, cross-paired teacher forcing, prefix corruption, likelihood contrast, and length control.

Result: On 3-second speech, PairAlign learns compact, non-degenerate sequences with broad vocabulary usage and strong cross-view consistency. On TIMIT retrieval, it preserves edit-distance search while reducing archive token count by 55%. It shows stronger length control and bounded edit trajectories under 100ms shifts compared to dense geometric tokenizers.

Conclusion: PairAlign represents a sequence-symbolic predictive learning approach that, similar to JEPA-style objectives, predicts abstract targets from different views as learned variable-length symbolic sequences rather than continuous latents, offering improved audio tokenization for symbolic operations.

Abstract: Many operations on sensory data -- comparison, memory, retrieval, and reasoning -- are naturally expressed over discrete symbolic structures. In language this interface is given by tokens; in audio, it must be learned. Existing audio tokenizers rely on quantization, clustering, or codec reconstruction, assigning tokens locally, so sequence consistency, compactness, length control, termination, and edit similarity are rarely optimized directly.
  We introduce PairAlign, a framework for compact audio tokenization through sequence-level self-alignment. PairAlign treats tokenization as conditional sequence generation: an encoder maps speech to a continuous condition, and an autoregressive decoder generates tokens from BOS, learning token identity, order, length, and EOS placement. Given two content-preserving views, each view's sequence is trained to be likely under the other's representation, while unrelated examples provide competing sequences. This gives a scalable surrogate for edit-distance preservation while discouraging many-to-one collapse.
  PairAlign starts from VQ-style tokenization and refines it with EMA-teacher targets, cross-paired teacher forcing, prefix corruption, likelihood contrast, and length control.
  On 3-second speech, PairAlign learns compact, non-degenerate sequences with broad vocabulary usage and strong cross-view consistency. On TIMIT retrieval, it preserves edit-distance search while reducing archive token count by 55%. A continuous-sweep probe shows lower local overlap than a dense geometric tokenizer, but stronger length control and bounded edit trajectories under 100 ms shifts. PairAlign is a sequence-symbolic predictive learner: like JEPA-style objectives, it predicts an abstract target from another view as a learned variable-length symbolic sequence, not a continuous latent.

</details>


### [348] [Distributionally-Robust Learning to Optimize](https://arxiv.org/abs/2605.06585)
*Vinit Ranjan,Jisun Park,Bartolomeo Stellato*

Main category: cs.LG

TL;DR: Learned distributionally robust optimization algorithms that balance worst-case robustness with empirical performance by minimizing Wasserstein distributionally robust performance estimation problems over algorithm parameters.


<details>
  <summary>Details</summary>
Motivation: Existing approaches to algorithm design have limitations: learning-to-optimize (L2O) methods rely heavily on training data and may not generalize well to unseen distributions, while worst-case optimal designs via performance estimation problems (PEP) can be overly conservative. There's a need for algorithms that combine the empirical benefits of L2O with the robustness guarantees of worst-case analysis.

Method: Propose a distributionally robust approach to learning hyperparameters for first-order methods. Given a dataset of problem instances, minimize a Wasserstein distributionally robust version of the performance estimation problem (PEP) over algorithm parameters like step sizes. The framework unifies L2O (vanishing robustness radius) and worst-case PEP (large robustness radius). Use stochastic gradient descent, differentiating through the solution of an inner semidefinite program at each step.

Result: Prove high-probability bounds showing the true risk of learned algorithms is at most the in-sample L2O optimum plus a slack term shrinking with sample size, and no worse than worst-case PEP bound. On benchmarks (unconstrained quadratic minimization, LASSO, linear programming), learned algorithms achieve strong out-of-sample performance with certifiable robustness, outperforming both worst-case optimal and vanilla L2O baselines.

Conclusion: The distributionally robust approach successfully bridges learning-based and worst-case methods, producing algorithms with strong empirical performance while maintaining robustness guarantees. This provides a principled framework for algorithm design that adapts to data while ensuring protection against distribution shifts.

Abstract: We propose a distributionally robust approach to learning hyperparameters for first-order methods in convex optimization. Given a dataset of problem instances, we minimize a Wasserstein distributionally robust version of the performance estimation problem (PEP) over algorithm parameters such as step sizes. Our framework unifies two extremes: as the robustness radius vanishes, we recover classical learning to optimize (L2O); as it grows, we recover worst-case optimal algorithm design via PEP. We solve the resulting problem with stochastic gradient descent, differentiating through the solution of an inner semidefinite program at each step. We prove high-probability bounds showing that the true risk of the learned algorithm is at most the in-sample L2O optimum plus a slack that shrinks with the sample size, and is no worse than the worst-case PEP bound. On unconstrained quadratic minimization, LASSO, and linear programming benchmarks, our learned algorithms achieve strong out-of-sample performance with certifiable robustness, outperforming both worst-case optimal and vanilla L2O baselines.

</details>


### [349] [Towards Metric-Faithful Neural Graph Matching](https://arxiv.org/abs/2605.06588)
*Jyotirmaya Shivottam,Subhankar Mishra*

Main category: cs.LG

TL;DR: Neural GED estimation improves by using bi-Lipschitz encoders (FSW-GNN) that provide better representation geometry, leading to enhanced graph similarity prediction and ranking metrics.


<details>
  <summary>Details</summary>
Motivation: The role of encoder geometry in neural Graph Edit Distance (GED) estimation remains poorly understood despite substantial architectural progress. There's a need to theoretically connect encoder geometry to GED estimation quality for better neural graph matching.

Method: Developed a theoretical framework connecting encoder geometry to GED estimation for two classes: graph similarity predictors and alignment-based methods. Used FSW-GNN (a bi-Lipschitz WL-equivalent encoder) as a drop-in replacement in representative neural GED architectures. Conducted case studies with untrained encoders and transfer experiments.

Result: Geometry-aware variants significantly improve GED prediction and ranking metrics across representative baselines and benchmark datasets. Bi-Lipschitz encoders yield controlled GED surrogates, improved ranking stability, and propagate geometry to alignment costs.

Conclusion: Encoder geometry is a useful design principle for neural graph matching. Gains arise from improved representation geometry, positioning bi-Lipschitz encoders as effective tools for enhancing neural GED estimation.

Abstract: Graph Edit Distance (GED) is a fundamental, albeit NP-hard, metric for structural graph similarity. Recent neural graph matching architectures approximate GED by first encoding graphs with a Graph Neural Network (GNN) and then applying either a graph-level regression head or a matching-based alignment module. Despite substantial architectural progress, the role of encoder geometry in neural GED estimation remains poorly understood. In this paper, we develop a theoretical framework that connects encoder geometry to GED estimation quality for two broad classes of neural GED estimators: graph similarity predictors and alignment-based methods. On fixed graph collections, where the doubly-stochastic metric $d_{\mathrm{DS}}$ is comparable to GED, we show that graph-level bi-Lipschitz encoders yield controlled GED surrogates and improved ranking stability; for matching-based estimators, node-level bi-Lipschitz geometry propagates to encoder-induced alignment costs and the resulting optimized alignment objective. We instantiate this perspective using FSW-GNN, a bi-Lipschitz WL-equivalent encoder, as a drop-in replacement in representative neural GED architectures. Across representative baselines and benchmark datasets, the resulting geometry-aware variants significantly improve GED prediction and ranking metrics. A faithfulness case study of untrained encoders, together with ablations and transfer experiments, supports the view that these gains arise from improved representation geometry, positioning encoder geometry as a useful design principle for neural graph matching.

</details>


### [350] [BRICKS: Compositional Neural Markov Kernels for Zero-Shot Radiation-Matter Simulation](https://arxiv.org/abs/2605.06591)
*Richard Hildebrandt,Evangelos Kourlitis,Baran Hashemi,Manuel Bünstorf,Thierry Meyer,Nikola Boskov,Michael Kagan,Dan Rosenbaum,Sanmay Ganguly,Lukas Heinrich*

Main category: cs.LG

TL;DR: Novel compositional neural surrogate using transformer with Riemannian Flow Matching for radiation-matter interactions, enabling differentiable zero-shot simulation of large-scale materials with GPU speed-up.


<details>
  <summary>Details</summary>
Motivation: Current mechanistic simulators for radiation-matter interactions (important in particle physics, nuclear engineering, space engineering, medical physics) are CPU-bound, non-differentiable, and lack tractable likelihoods, limiting their use in downstream applications and large-scale simulations.

Method: Develops a next-particle prediction kernel using hybrid discrete-continuous transformer models based on Riemannian Flow Matching on product manifolds. The model generates variable-sized typed sets of particles and radiation side effects from incident particle-material interactions, designed to be composable for zero-shot simulation of large-scale materials.

Result: Model provides differentiable surrogate with tractable likelihoods, significant GPU computational speed-up compared to CPU-bound mechanistic simulators, predictive stability over multi-round autoregressive rollouts, and releases a novel 20M-event radiation-matter interaction dataset.

Conclusion: The compositional neural surrogate successfully addresses limitations of mechanistic simulators by providing a differentiable, fast, and composable approach for radiation-matter interactions that enables zero-shot simulation of large-scale material distributions with applications across multiple domains.

Abstract: We introduce a new strategy for compositional neural surrogates for radiation-matter interactions, a key task spanning domains from particle physics through nuclear and space engineering to medical physics. Exploiting the locality and the Markov nature of particle interactions, we create a \emph{next-particle prediction} kernel using hybrid discrete-continuous transformer models based on Riemannian Flow Matching on product manifolds. The model generates variable-sized typed sets of particles and radiation side effects that are the result of the interaction of an incident particle with a material volume. The resulting kernel can be composed to simulate unseen large-scale material distributions in a zero-shot manner. Unlike mechanistic simulators, our model is designed to be differentiable, provides tractable likelihoods for future downstream applications. A significant computational speed-up on GPU compared to CPU-bound mechanistic simulation is observed for single-kernel execution. We evaluate the model at the kernel level and demonstrate predictive stability over multi-round autoregressive rollouts. We additionally release a novel 20M-event radiation-matter interaction dataset for further research.

</details>


### [351] [Weight-Decay Turns Transformer Loss Landscapes Villani: Functional-Analytic Foundations for Optimization and Generalization](https://arxiv.org/abs/2605.06599)
*Abhijit Das,Sayantan Dutta*

Main category: cs.LG

TL;DR: Weight decay in Transformers satisfies Villani's coercive energy criteria, enabling log-Sobolev/Poincaré inequalities with explicit constants, providing theoretical guarantees for optimization convergence and generalization.


<details>
  <summary>Details</summary>
Motivation: Weight decay is widely used in large language models but its theoretical role in shaping Transformer loss landscapes remains underexplored, particularly in establishing mathematical conditions for optimization and generalization guarantees.

Method: Prove Transformer loss with L² regularization satisfies Villani's coercive energy criteria; derive explicit log-Sobolev and Poincaré constants; introduce scalable Villani diagnostic using Hutchinson trace probes; validate with experiments on GPT-Neo-125M across language modeling datasets.

Result: Regularized Transformer loss is infinitely differentiable with quadratic growth and satisfies differential growth condition; derived constants C_LS ≤ λ⁻¹ + d/λ² linking regularization strength and dimension to convergence guarantees; experiments confirm predicted quadratic growth, Hessian spectral inflation, and exponential convergence behavior.

Conclusion: Weight decay not only improves generalization empirically but establishes mathematical conditions (Villani criteria) required for fast Langevin mixing and theoretically grounded curvature-aware optimization in deep learning.

Abstract: Weight decay is widely used as a regularizer in large language models, yet its precise role in shaping Transformer loss landscapes remains theoretically underexplored. This paper provides the first rigorous functional-analytic characterization of the standard Transformer objective--cross-entropy loss with $L^2$ regularization--by proving it satisfies Villani's criteria for coercive energy functions. Specifically, we show that the regularized loss $\mathcal{F}$ is infinitely differentiable, grows at least quadratically, has Gaussian-integrable tails, and satisfies the differential growth condition $-Δ\mathcal{F} + \tfrac{1}{s}\|\nabla\mathcal{F}\|^{2} \to \infty$ as $\|θ\| \to \infty$ for all $s>0$. From this structure, we derive explicit log-Sobolev and Poincaré constants $C_{\mathrm{LS}} \leq λ^{-1} + d/λ^{2}$, linking the regularization strength $λ$ and model dimension $d$ to finite-time convergence guarantees for noisy stochastic gradient descent and PAC-Bayesian generalization bounds that tighten with increasing $λ$. To validate our theory, we introduce a scalable Villani diagnostic $Ψ_s(θ) = -Δ\mathcal{F} + s^{-1}\|\nabla \mathcal{F}\|^2$ and estimate it efficiently using Hutchinson trace probes in models with over 100M parameters. Experiments on GPT-Neo-125M across Penn Treebank and WikiText-103 confirm the predicted quadratic growth of $Ψ_s$, spectral inflation of the Hessian, and exponential convergence behavior consistent with our log-Sobolev analysis. These results demonstrate that weight decay not only improves generalization empirically but also establishes the mathematical conditions required for fast Langevin mixing and theoretically grounded curvature-aware optimization in deep learning.

</details>


### [352] [How Many Iterations to Jailbreak? Dynamic Budget Allocation for Multi-Turn LLM Evaluation](https://arxiv.org/abs/2605.06605)
*Shai Feldman,Yaniv Romano*

Main category: cs.LG

TL;DR: DAPRO introduces a dynamic budget allocation framework for predicting time-to-event in multi-turn LLM conversations, providing theoretically valid coverage guarantees with tighter bounds than prior static methods.


<details>
  <summary>Details</summary>
Motivation: Evaluating LLM performance in multi-turn conversations is computationally expensive, and rare events like jailbreaks or task completion often remain unobserved under feasible computational budgets. Existing conformal survival methods use inefficient static budget allocation.

Method: Dynamic Allocation via Projected Optimization (DAPRO) - a theoretically valid dynamic budget allocation framework for bounding time-to-event in multi-turn LLM interactions. It doesn't require conditional independence assumptions and provides novel coverage bounds scaling with square root of mean censoring weight rather than worst-case weight.

Result: DAPRO achieves coverage closer to nominal levels with lower variance than static baselines while satisfying budget constraints. Experiments across agentic tasks, adversarial jailbreaks, toxic content generation, and RAG hallucinations using Llama 3.1 and Qwen 2.5 demonstrate consistent performance improvements.

Conclusion: DAPRO provides a theoretically sound and practically effective dynamic budget allocation method for LLM evaluation in multi-turn settings, offering tighter guarantees than prior work and enabling unbiased, low-variance estimation of population-level metrics under computational constraints.

Abstract: Evaluating and predicting the performance of large language models (LLMs) in multi-turn conversational settings is critical yet computationally expensive; key events -- e.g., jailbreaks or successful task completion by an agent -- often emerge only after repeated interactions. These events might be rare, and under any feasible computational budget, remain unobserved.
  Recent conformal survival frameworks construct reliable lower predictive bounds (LPBs) on the number of iterations to trigger the event of interest, but rely on static budget allocation that is inefficient in multi-turn setups. To address this, we introduce \emph{Dynamic Allocation via PRojected Optimization} (DAPRO), the first theoretically valid dynamic budget allocation framework for bounding the time-to-event in multi-turn LLM interactions.
  We prove that DAPRO satisfies the budget constraint and provides distribution-free, finite-sample coverage guarantees without requiring the conditional independence between censoring and event times assumed by prior conformal survival approaches.
  A key theoretical contribution is a novel coverage bound that scales with the square root of the mean censoring weight rather than the worst-case weight, yielding provably tighter guarantees than prior work. Furthermore, DAPRO can be employed to obtain unbiased, low-variance estimates of population-level evaluation metrics, such as the jailbreak rate, under limited computing resources.
  Comprehensive experiments across agentic task success, adversarial jailbreaks, toxic content generation, and RAG hallucinations using LLMs such as Llama 3.1 and Qwen 2.5 demonstrate that DAPRO consistently achieves coverage closer to the nominal level with lower variance than static baselines, while satisfying the budget constraint.

</details>


### [353] [Transformers Efficiently Perform In-Context Logistic Regression via Normalized Gradient Descent](https://arxiv.org/abs/2605.06609)
*Chenyang Zhang,Yuan Cao*

Main category: cs.LG

TL;DR: Transformers perform in-context learning by implicitly executing algorithms on context data, and this paper constructs transformers that perform logistic regression through gradient descent steps in each layer.


<details>
  <summary>Details</summary>
Motivation: To understand how transformers with softmax attention perform in-context learning on linear classification data, specifically investigating the mechanisms behind their implicit algorithm execution capabilities.

Method: Construct multi-layer transformers that perform in-context logistic regression where each layer exactly performs one step of normalized gradient descent on an in-context loss. Train a single self-attention layer supervised by one-step gradient descent, then recurrently apply this trained layer to obtain a looped model.

Result: The constructed transformer successfully performs in-context logistic regression, with training convergence guarantees for the self-attention layer and out-of-distribution generalization guarantees for the looped model.

Conclusion: Softmax transformers can effectively act as in-context learners by implicitly executing gradient descent algorithms, advancing theoretical understanding of ICL mechanisms in transformers.

Abstract: Transformers have demonstrated remarkable in-context learning (ICL) capabilities. The strong ICL performance of transformers is commonly believed to arise from their ability to implicitly execute certain algorithms on the context, thereby enhancing prediction and generation. In this work, we investigate how transformers with softmax attention perform in-context learning on linear classification data. We first construct a class of multi-layer transformers that can perform in-context logistic regression, with each layer exactly performing one step of normalized gradient descent on an in-context loss. Then, we show that our constructed transformer can be obtained through (i) training a single self-attention layer supervised by one-step gradient descent, and (ii) recurrently applying the trained layer to obtain a looped model. Training convergence guarantees of the self-attention layer and out-of-distribution generalization guarantees of the looped model are provided. Our results advance the theoretical understanding of ICL mechanism by showcasing how softmax transformers can effectively act as in-context learners.

</details>


### [354] [SoftSAE: Dynamic Top-K Selection for Adaptive Sparse Autoencoders](https://arxiv.org/abs/2605.06610)
*Jakub Stępień,Marcin Mazur,Jacek Tabor,Przemysław Spurek*

Main category: cs.LG

TL;DR: SoftSAE proposes a sparse autoencoder with dynamic Top-K selection that learns input-dependent sparsity levels instead of using fixed K, better matching data complexity.


<details>
  <summary>Details</summary>
Motivation: Current SAEs use fixed sparsity levels (same K for all inputs), ignoring that real-world data has varying complexity - simple inputs need few features while complex ones need more features, making fixed K suboptimal (adding noise or missing structure).

Method: SoftSAE uses a differentiable Soft Top-K operator to learn an input-dependent sparsity level k, dynamically adjusting the number of active features based on each input's complexity.

Result: Experiments show SoftSAE finds meaningful features and selects the right number of features for each concept, with explanation length reflecting input information amount.

Conclusion: Dynamic sparsity adjustment via Soft Top-K enables SAEs to better match data structure and complexity compared to fixed sparsity approaches.

Abstract: Sparse Autoencoders (SAEs) have become an important tool in mechanistic interpretability, helping to analyze internal representations in both Large Language Models (LLMs) and Vision Transformers (ViTs). By decomposing polysemantic activations into sparse sets of monosemantic features, SAEs aim to translate neural network computations into human-understandable concepts. However, common architectures such as TopK SAEs rely on a fixed sparsity level. They enforce the same number of active features (K) across all inputs, ignoring the varying complexity of real-world data. Natural data often lies on manifolds with varying local intrinsic dimensionality, meaning the number of relevant factors can change significantly across samples. This suggests that a fixed sparsity level is not optimal. Simple inputs may require only a few features, while more complex ones need more expressive representations. Using a constant K can therefore introduce noise in simple cases or miss important structure in more complex ones. To address this issue, we propose SoftSAE, a sparse autoencoder with a Dynamic Top-K selection mechanism. Our method uses a differentiable Soft Top-K operator to learn an input-dependent sparsity level k. This allows the model to adjust the number of active features based on the complexity of each input. As a result, the representation better matches the structure of the data, and the explanation length reflects the amount of information in the input. Experimental results confirm that SoftSAE not only finds meaningful features, but also selects the right number of features for each concept. The source code is available at: https://anonymous.4open.science/r/SoftSAE-8F71/.

</details>


### [355] [The Structural Origin of Attention Sink: Variance Discrepancy, Super Neurons, and Dimension Disparity](https://arxiv.org/abs/2605.06611)
*Siquan Li,Kaiqi Jiang,Jiacheng Sun,Tianyang Hu*

Main category: cs.LG

TL;DR: The paper provides a mechanistic explanation for attention sink phenomenon in LLMs, tracing it to value aggregation in self-attention amplified by FFN super neurons, and proposes head-wise RMSNorm as a solution.


<details>
  <summary>Details</summary>
Motivation: Despite the known attention sink phenomenon where initial tokens disproportionately capture attention scores in LLMs, the underlying structural causes remain poorly understood. The paper aims to provide a mechanistic explanation for this phenomenon.

Method: First, trace the root cause to value aggregation in self-attention causing systematic variance discrepancy. Second, show this is amplified by super neurons in FFN layers through channel-sparse down-projections. Validate through controlled interventions: (1) isolating aggregation effect via attention mask modifications, and (2) amplifying variance of targeted token representations. Finally, propose head-wise RMSNorm as an architectural modification to stabilize value aggregation outputs.

Result: The interventions successfully replicate attention sinks at arbitrary positions, validating the causal chain. Head-wise RMSNorm implementation demonstrates that restoring statistical parity across positions significantly accelerates model convergence during pre-training.

Conclusion: The paper provides a mechanistic understanding of attention sink formation in LLMs, linking it to value aggregation variance discrepancy amplified by FFN super neurons. This understanding enables systematic control of sink formation, and the proposed head-wise RMSNorm offers a practical solution that improves training efficiency.

Abstract: Despite the prevalence of the attention sink phenomenon in Large Language Models (LLMs), where initial tokens disproportionately monopolize attention scores, its structural origins remain elusive. This work provides a \textit{mechanistic explanation} for this phenomenon. First, we trace its root to the value aggregation process inherent in self-attention, which induces a systematic variance discrepancy. We further demonstrate that this discrepancy is drastically amplified by the activation of super neurons within Feed-Forward Network (FFN) layers. Specifically, the channel-sparse down-projections trigger a dimension disparity of the first-token representation, necessitating the formation of attention sinks as a structural anchor. Then, we validate this causal chain through two controlled interventions: (i) isolating the aggregation effect via attention mask modifications and (ii) amplifying the variance of targeted token representations. Both interventions can replicate attention sinks at arbitrary positions. Our mechanistic understanding offers a foundation for the systematic control of sink formation. Finally, as a proof of concept, we propose \textit{head-wise RMSNorm}, an architectural modification that stabilizes value aggregation outputs during pre-training. Our experiments demonstrate that restoring statistical parity across positions significantly accelerates convergence.

</details>


### [356] [Online Bayesian Calibration under Gradual and Abrupt System Changes](https://arxiv.org/abs/2605.06612)
*Yang Xu,Chiwoo Park*

Main category: cs.LG

TL;DR: BRPC is an online Bayesian calibration framework that handles both gradual evolution and abrupt regime shifts in digital twin applications, separating parameter and discrepancy updates for identifiability while integrating restart mechanisms for robustness.


<details>
  <summary>Details</summary>
Motivation: Classical Bayesian calibration suffers from parameter-discrepancy confounding and assumes stationary data, making it unsuitable for modern digital twins where systems evolve over time with gradual drifts and abrupt regime shifts. Existing data assimilation methods don't explicitly model systematic bias and perform poorly under abrupt changes.

Method: BRPC extends projected calibration to streaming data by separating discrepancy-free particle updates for calibration parameters from conditional Gaussian process updates for discrepancy. It integrates restart mechanisms that detect regime shifts and reset the calibration process to handle abrupt changes.

Result: Empirical studies on synthetic and plant-simulation benchmarks show BRPC improves calibration accuracy under gradual changes, while restart-augmented BRPC enhances robustness and predictive performance under abrupt regime shifts compared to sliding-window Bayesian calibration and data assimilation baselines.

Conclusion: BRPC provides an effective online Bayesian calibration framework for digital twins that can handle both gradual system evolution and abrupt regime shifts, addressing key limitations of classical methods while maintaining theoretical guarantees for tracking performance and regime shift detection.

Abstract: Bayesian model calibration is central to digital twins and computer experiments, as it aligns model outputs with field observations by estimating calibration parameters and correcting systematic model bias. Classical Bayesian calibration introduces latent parameters and a discrepancy function to model bias, but suffers from parameter--discrepancy confounding and is typically formulated as an offline procedure under a stationary data-generating assumption. These limitations are restrictive in modern digital twin applications, where systems evolve over time and may exhibit gradual drift and abrupt regime shifts. While data assimilation methods enable sequential updates, they generally do not explicitly model systematic bias and are less effective under abrupt changes. We propose Bayesian Recursive Projected Calibration (BRPC), an online Bayesian calibration framework for streaming data under simulator mismatch and nonstationarity. BRPC extends projected calibration to the online setting by separating a discrepancy-free particle update for calibration parameters from a conditional Gaussian process update for discrepancy, preserving identifiability while enabling bias-aware adaptation under gradual system evolution. To handle abrupt changes, BRPC is integrated with restart mechanisms that detect regime shifts and reset the calibration process. We establish theoretical guarantees for both components, including tracking performance under gradual evolution and false-alarm and detection behavior for restart mechanisms. Empirical studies on synthetic and plant-simulation benchmarks show that BRPC improves calibration accuracy under gradual changes, while restart-augmented BRPC further improves robustness and predictive performance under abrupt regime shifts compared to sliding-window Bayesian calibration and data assimilation baselines.

</details>


### [357] [When and Why SignSGD Outperforms SGD: A Theoretical Study Based on $\ell_1$-norm Lower Bounds](https://arxiv.org/abs/2605.06615)
*Hongyi Tao,Dingzhi Yu,Lijun Zhang*

Main category: cs.LG

TL;DR: This paper provides the first theoretical justification for why sign-based optimization algorithms like SignSGD outperform vanilla SGD in specific settings, showing SignSGD achieves d-fold speedup under sparse noise conditions compared to SGD's minimax lower bound.


<details>
  <summary>Details</summary>
Motivation: Despite empirical success of sign-based methods (SignSGD, Muon) in training large foundation models, there's no theoretical understanding of when/why they outperform SGD. The obstacle is that under standard assumptions, SGD is minimax optimal, preventing theoretical justification for sign-based methods' superiority.

Method: The authors analyze sign-based optimizers using a distinct problem geometry: ℓ₁-norm stationarity, ℓ∞-smoothness, and separable noise model that better captures coordinate-wise nature of signed updates. They derive matched upper/lower bounds for SignSGD and show dominance over SGD under this geometry.

Result: SignSGD reduces complexity by factor d (problem dimension) under sparse noise compared to SGD's lower bound. The framework extends to matrix domain proving Muon preserves optimal scaling. Theoretical superiority predicts faster convergence in 124M parameter GPT-2 pretraining.

Conclusion: This work provides rigorous theoretical foundation for sign-based optimizers, identifying precise conditions (ℓ∞-smoothness, separable noise, sparse gradients) where they provably outperform SGD, bridging theory and practice for large model training.

Abstract: Sign-based optimization algorithms, such as SignSGD and Muon, have garnered significant attention for their remarkable performance in training large foundation models. Despite this empirical success, we still lack a theoretical understanding of when and why these sign-based methods outperform vanilla SGD. The core obstacle is that under standard smoothness and finite variance conditions, SGD is known to be minimax optimal for finding stationary points measured by $\ell_2$-norms, thereby fundamentally precluding any complexity gains for sign-based methods in standard settings. To overcome this barrier, we analyze sign-based optimizers leveraging $\ell_1$-norm stationarity, $\ell_\infty$-smoothness, and a separable noise model, which can better capture the coordinate-wise nature of signed updates. Under this distinct problem geometry, we derive matched upper and lower bounds for SignSGD and explicitly characterize the problem class in which SignSGD provably dominates SGD. Specifically, we compare the \emph{upper bound of SignSGD} with the \emph{lower bound of SGD}, illustrating that SignSGD effectively reduces the complexity by a factor of $d$ under \emph{sparse noise}, where $d$ is the problem dimension. Furthermore, we elevate this framework to the matrix domain, providing an equivalent optimal lower bound for the Muon optimizer, proving that extending the sign operator to matrices preserves this optimal scaling with dimensionality. Finally, we bridge our theoretical bounds to practice, demonstrating that the theoretical superiority of SignSGD accurately predicts its faster convergence during the pretraining of a 124M parameter GPT-2 model.

</details>


### [358] [Hybrid Quantum-Classical GANs for the Generation of Adversarial Network Flows](https://arxiv.org/abs/2605.06629)
*Prateek Paudel,Nitin Jha,Abhishek Parakh,Mahadevan Subramaniam*

Main category: cs.LG

TL;DR: This paper proposes a quantum-classical hybrid GAN framework for generating adversarial network traffic to attack intrusion detection systems, leveraging quantum computing to overcome classical GAN limitations.


<details>
  <summary>Details</summary>
Motivation: Classical GANs have several shortcomings for generating adversarial network traffic: they require large amounts of high-dimensional data, suffer from mode collapse issues, and have high computational overhead. The authors aim to address these limitations while exploring the potential of quantum machine learning for advanced cyber attacks.

Method: The authors propose a hybrid quantum-classical GAN (QC-GAN) framework where: 1) A variational quantum generator creates synthetic network traffic flows mimicking malicious traffic using latent representations encoded as quantum states (rather than classical noise vectors); 2) A classical discriminator is trained on both real-world datasets (UNSW-NB15) and QC-GAN-generated fake flows; 3) The framework operates in an adversarial manner where generator minimizes discriminator's ability to distinguish real from fake traffic; 4) Generated flows are tested against classical IDS models (random forest and CNN-based classifiers); 5) Hardware-based noise effects are evaluated to understand practical implications.

Result: The work demonstrates that quantum-enhanced latent representations can potentially generate more effective adversarial network traffic for bypassing intrusion detection systems. The framework reduces computational overhead compared to classical GANs and provides more expressive latent representations. The evaluation includes testing against classical IDS models and assessing how quantum hardware noise affects attack effectiveness.

Conclusion: This research highlights the potential threat of quantum machine learning for generating advanced cyber attacks and stress-testing classical intrusion detection systems. It reveals the need for quantum-resilient defense systems in cybersecurity and provides a new perspective on evaluating IDS robustness against emerging quantum-enhanced attacks.

Abstract: Classical generative adversarial networks (GANs) have been applied to generate adversarial network traffic capable of attacking intrusion detection systems, but they suffer from shortcomings such as the need for large amounts of high-dimensional datasets, mode collapse, and high computational overhead. In this work, we propose a hybrid quantum-classical GAN (QC-GAN) framework where a variational quantum generator is used to generate synthetic network traffic flows mimicking malicious traffic using latent representations. Instead of sampling classical noise vectors, we encode the latent vector (the hidden features) as a quantum state, which is the basis for claiming more expressive latent representations and reducing computational overhead. A classical discriminator will be trained on real-world datasets (UNSW-NB15) and the proposed QC-GAN-generated fake network flows. In this configuration, the generator aims to minimize the discriminator's ability to distinguish real from fake traffic, while the discriminator aims to maximize its classification accuracy, in an iterative manner. In our attack model, we assume that the attacker is a state actor with access to limited quantum computing power, whereas the discriminator is chosen to be classical, as will likely be the case for most end users and organizations. We test the generated flows using classical intrusion detection system (IDS) models, such as a random forest classifier and a convolutional neural network-based classifier, for their ability to bypass the detection process. This work aims to highlight the possibilities of quantum machine learning as a means of generating advanced attack flows and stress testing classical IDS. Lastly, we further evaluate how hardware-based noise affects these attacks to offer a new perspective on IDS, highlighting the need for a quantum resilient defense system.

</details>


### [359] [Crafting Reversible SFT Behaviors in Large Language Models](https://arxiv.org/abs/2605.06632)
*Yuping Lin,Pengfei He,Yue Xing,Yingqian Cui,Jiayuan Ding,Subhabrata Mukherjee,Hui Liu,Zhen Xiang*

Main category: cs.LG

TL;DR: LCDD compresses SFT-induced behaviors into sparse "carrier" subnetworks, enabling selective control via SFT-Eraser soft prompts without weight modification.


<details>
  <summary>Details</summary>
Motivation: Existing behavior interpretation methods identify correlations but lack causal necessity, limiting selective control of SFT-induced behaviors at inference time without weight modifications.

Method: Proposes Loss-Constrained Dual Descent (LCDD) to construct sparse "carrier" subnetworks by jointly optimizing routing masks and model weights under utility budget, and SFT-Eraser soft prompts optimized via activation matching for behavior reversal.

Result: LCDD yields sparse carriers that preserve target behaviors while enabling strong reversal when triggered by SFT-Eraser across safety, fixed-response, and style behaviors on multiple model families. Ablations show sparse structure is key for reversal.

Conclusion: Provides evidence that learned carriers are causally necessary for behaviors, pointing to a new direction for systematically localizing and selectively suppressing SFT-induced behaviors in deployed models.

Abstract: Supervised fine-tuning (SFT) induces new behaviors in large language models, yet imposes no structural constraint on how these behaviors are distributed within the model. Existing behavior interpretation methods, such as circuit attribution approaches, identify sparse subnetworks correlated with SFT-induced behaviors post-hoc. However, such correlations do not imply *causal necessity*, limiting the ability to selectively control SFT-induced behaviors at inference time. We pursue an alternative by asking: can an SFT-induced behavior be deliberately compressed into a sparse, mechanistically necessary subnetwork, termed a *carrier*, while remaining controllable at inference time without weight modification? We propose (a) **Loss-Constrained Dual Descent (LCDD)**, which constructs such carriers by jointly optimizing routing masks and model weights under an explicit utility budget, and (b) **SFT-Eraser**, a soft prompt optimized via activation matching on extracted carrier channels, to reverse the SFT-induced behavior. Across safety, fixed-response, and style behaviors on multiple model families, LCDD yields sparse carriers that preserve target behaviors while enabling strong reversion when triggered by SFT-Eraser. Ablations further establish that the sparse structure is the key precondition for reversal: the same trigger optimization fails on standard SFT models, confirming that structure rather than trigger design is the operative factor. These results provide direct evidence that the learned carriers are causally necessary for the behaviors, pointing to a new direction for systematically localizing and selectively suppressing SFT-induced behaviors in deployed models.

</details>


### [360] [Concept-Based Abductive and Contrastive Explanations for Behaviors of Vision Models](https://arxiv.org/abs/2605.06640)
*Ronaldo Canizales,Divya Gopinath,Corina Păsăreanu,Ravi Mangal*

Main category: cs.LG

TL;DR: The paper proposes concept-based abductive and contrastive explanations that find minimal sets of high-level concepts causally relevant for model predictions, addressing limitations of existing methods that either lack causality or use low-level features.


<details>
  <summary>Details</summary>
Motivation: Existing concept-based explanation methods either don't establish causal connections or are limited to single concepts, while formal abductive/contrastive explanations only work with low-level features like pixels. There's a need to bridge these approaches to provide causal, high-level concept explanations.

Method: Proposes concept-based abductive and contrastive explanations that capture minimal sets of high-level concepts causally relevant for model outcomes. Uses a family of algorithms that enumerate all minimal explanations while employing concept erasure procedures to establish causal relationships. Aggregates explanations to understand both individual image predictions and collections of images exhibiting common model behaviors.

Result: The approach was evaluated on multiple models, datasets, and behaviors, demonstrating effectiveness in computing helpful, user-friendly explanations that bridge the gap between causal concept explanations and human-understandable feature explanations.

Conclusion: The proposed method successfully merges concept-based explanations with formal abductive/contrastive approaches, providing minimal sets of high-level concepts that are causally relevant to model predictions, enabling better understanding of individual predictions and common model behaviors.

Abstract: *Concept-based explanations* offer a promising approach for explaining the predictions of deep neural networks in terms of high-level, human-understandable concepts. However, existing methods either do not establish a causal connection between the concepts and model predictions or are limited in expressivity and only able to infer causal explanations involving single concepts. At the same time, the parallel line of work on *formal abductive and contrastive explanations* computes the minimal set of input features causally relevant for model outcomes but only considers low-level features such as pixels. Merging these two threads, in this work, we propose the notion of *concept-based abductive and contrastive explanations* that capture the minimal sets of high-level concepts causally relevant for model outcomes. We then present a family of algorithms that enumerate all minimal explanations while using *concept erasure* procedures to establish causal relationships. By appropriately aggregating such explanations, we are not only able to understand model predictions on individual images but also on collections of images where the model exhibits a user-specified, common *behavior*. We evaluate our approach on multiple models, datasets, and behaviors, and demonstrate its effectiveness in computing helpful, user-friendly explanations.

</details>


### [361] [Edge-specific signal propagation on mature chromophore-region 3D mechanism graphs for fluorescent protein quantum-yield prediction](https://arxiv.org/abs/2605.06644)
*Yuchen Xiong,Swee Keong Yeap,Steven Aw Yoong Kit*

Main category: cs.LG

TL;DR: A chromophore-centered graph-based method for predicting fluorescent protein quantum yield using local physical interactions instead of sequence identity alone.


<details>
  <summary>Details</summary>
Motivation: Existing protein language models and emission-band averages capture global trends but fail to model how local physical signals act on specific chromophore regions, which is crucial for understanding quantum yield.

Method: Developed a chromophore-centered mechanism graph algorithm where each PDB structure is converted into a typed 3D residue graph, registered to mature chromophore state, partitioned into phenolate/bridge/imidazolinone regions, and transformed via channel-signal-region propagation. Uses 52 non-identity features for band-specific ExtraTrees regression.

Result: Achieved best random-CV performance among model-based baselines (R=0.772±0.008, MAE=0.131±0.002), outperforming Band mean, ESM-C, and SaProt. Best performance in bright screening (Bright P@5=0.704) and strongest advantage in remote homology cases (<50% similarity).

Conclusion: The chromophore-centered graph approach provides interpretable, physically meaningful prediction of quantum yield that captures band-specific mechanisms and outperforms existing methods, especially for remote homologs.

Abstract: Fluorescent protein quantum yield (QY) is governed by the mature chromophore and its three-dimensional microenvironment rather than sequence identity alone. Protein language models and emission-band averages capture global trends, but do not model how local physical signals act on specific chromophore regions.
  We present a chromophore-centred mechanism graph algorithm for QY prediction. Each PDB structure is converted into a typed 3D residue graph, registered to a mature-CRO state, partitioned into phenolate, bridge and imidazolinone regions, and transformed by channel-signal-region propagation. The representation contains 121 enrichment features; after removing identity shortcuts, 52 non-identity features are used for band-specific ExtraTrees regression. Because each feature encodes a contact channel, seed signal and target CRO region, interpretation is intrinsic rather than post hoc. On a 531-protein benchmark, the method achieved the best random-CV performance among model-based baselines (R = 0.772 +/- 0.008, MAE = 0.131 +/- 0.002), exceeding Band mean (R = 0.632), ESM-C (R = 0.734) and SaProt (R = 0.731), and ranked first in bright screening (Bright P@5 = 0.704). Under homology control, the advantage was clearest in the remote bucket (<50% similarity; R = 0.697 versus 0.633, 0.575 and 0.408), with the strongest overall bright/dark Top-K screening. Stable selected features recovered band-specific mechanisms: aromatic packing and clamp asymmetry in GFP-like proteins, charge/clamp balance in Red proteins, and flexibility-risk/bulky-contact features in Far-red proteins.
  Source code, feature tables and evaluation scripts are available from the first author upon request. Contact: yuchenak05@gmail.com

</details>


### [362] [Inductive Venn-Abers and related regressors](https://arxiv.org/abs/2605.06646)
*Ivan Petej,Vladimir Vovk*

Main category: cs.LG

TL;DR: Venn-Abers predictors extended from binary classification/bounded regression to unbounded regression using conformal prediction elements, with improved predictive efficiency for larger datasets.


<details>
  <summary>Details</summary>
Motivation: Venn-Abers predictors have strong validity properties but are limited to binary classification and bounded regression cases, leaving a gap for unbounded regression problems.

Method: Generalize Venn-Abers predictors to unbounded regression by incorporating elements of conformal prediction, then evaluate through simulation and empirical studies.

Result: Point regressors derived from Venn-Abers regressors show somewhat improved predictive efficiency compared to standard regressors, particularly for larger training sets.

Conclusion: The extension of Venn-Abers predictors to unbounded regression is feasible and beneficial, offering improved predictive efficiency while maintaining validity properties through integration with conformal prediction.

Abstract: Venn-Abers predictors are probabilistic predictors that enjoy appealing properties of validity, but their major limitation is that they are applicable only to the case of binary classification, with a recent extension to bounded regression. We generalize them to the case of unbounded regression, which requires adding an element of conformal prediction. In our simulation and empirical studies we investigate the predictive efficiency of point regressors derived from Venn-Abers regressors and argue that they somewhat improve the predictive efficiency of standard regressors for larger training sets.

</details>


### [363] [When No Benchmark Exists: Validating Comparative LLM Safety Scoring Without Ground-Truth Labels](https://arxiv.org/abs/2605.06652)
*Sushant Gautam,Finn Schwall,Annika Willoch Olstad,Fernando Vallecillos Ruiz,Birk Torpmann-Hagen,Sunniva Maria Stordal Bjørklund,Leon Moonen,Klas Pettersen,Michael A. Riegler*

Main category: cs.LG

TL;DR: This paper introduces benchmarkless comparative safety scoring for language models, defining validity contracts and an instrumental-validity chain, then demonstrates with SimpleAudit on Norwegian safety pack and applies to real procurement case.


<details>
  <summary>Details</summary>
Motivation: Many deployments need to compare language model safety before labeled benchmarks exist for specific languages, sectors, or regulatory regimes, creating a need for benchmarkless safety assessment methods.

Method: The paper formalizes benchmarkless comparative safety scoring with validity contracts and an instrumental-validity chain (responsiveness to safe-vs-ablated contrast, target-driven variance dominance, stability across reruns), then implements SimpleAudit scoring instrument and validates on Norwegian safety pack.

Result: Safe vs ablated targets separate with AUROC 0.89-1.00, target identity is dominant variance component (η²≈0.52), severity profiles stabilize by 10 reruns. Applied to Petri shows chain admits both tools, with differences upstream in claim-contract enforcement and deployment fit.

Conclusion: Benchmarkless comparative safety scoring produces context-dependent results where safer model depends on scenario category and risk measure, requiring comprehensive reporting of scores, matched deltas, critical rates, uncertainty, and auditor/judge details rather than collapsing into single rankings.

Abstract: Many deployments must compare candidate language models for safety before a labeled benchmark exists for the relevant language, sector, or regulatory regime. We formalize this setting as benchmarkless comparative safety scoring and specify the contract under which a scenario-based audit can be interpreted as deployment evidence. Scores are valid only under a fixed scenario pack, rubric, auditor, judge, sampling configuration, and rerun budget. Because no labels are available, we replace ground-truth agreement with an instrumental-validity chain: responsiveness to a controlled safe-versus-abliterated contrast, dominance of target-driven variance over auditor and judge artifacts, and stability across reruns.
  We instantiate the chain in SimpleAudit, a local-first scoring instrument, and validate it on a Norwegian safety pack. Safe and abliterated targets separate with AUROC values between 0.89 and 1.00, target identity is the dominant variance component ($η^2 \approx 0.52$), and severity profiles stabilize by ten reruns. Applying the same chain to Petri shows that it admits both tools. The substantial differences arise upstream of the chain, in claim-contract enforcement and deployment fit. A Norwegian public-sector procurement case comparing Borealis and Gemma 3 demonstrates the resulting evidence in practice: the safer model depends on scenario category and risk measure. Consequently, scores, matched deltas, critical rates, uncertainty, and the auditor and judge used must be reported together rather than collapsed into a single ranking.

</details>


### [364] [Optimizer-Model Consistency: Full Finetuning with the Same Optimizer as Pretraining Forgets Less](https://arxiv.org/abs/2605.06654)
*Yuxing Liu,Jianyu Wang,Tong Zhang*

Main category: cs.LG

TL;DR: Full fine-tuning with the same optimizer as pretraining achieves better learning-forgetting tradeoff than other optimizers or LoRA, showing optimizer-model consistency improves knowledge retention.


<details>
  <summary>Details</summary>
Motivation: The paper investigates how optimizer choice affects supervised fine-tuning of large language models, particularly observing that different optimizers can lead to varying degrees of forgetting previously learned knowledge from pretraining. The researchers want to understand why using the same optimizer throughout pretraining and fine-tuning yields better results.

Method: The study uses controlled experiments and theoretical analysis to examine optimizer effects. They specifically compare Muon and AdamW optimizers throughout pretraining and SFT stages. They also conduct a synthetic language modeling experiment to understand the mechanisms behind their observations.

Result: 1) Full fine-tuning with the same optimizer as pretraining achieves better learning-forgetting tradeoff than other optimizers or LoRA. 2) Optimizers shape models through regularization effects on activations, creating different optimization landscapes around pretrained checkpoints. 3) Muon performs worse than AdamW for reasoning tasks during fine-tuning, potentially due to Muon's strong tendency toward rote memorization rather than pattern acquisition.

Conclusion: Maintaining optimizer consistency between pretraining and fine-tuning stages is important for knowledge retention in LLMs. The choice of optimizer significantly impacts model behavior, with some optimizers like Muon potentially hindering transfer to reasoning tasks due to memorization tendencies. This has practical implications for how researchers and practitioners should approach fine-tuning procedures.

Abstract: Optimizers play an important role in both pretraining and finetuning stages when training large language models (LLMs). In this paper, we present an observation that full finetuning with the same optimizer as in pretraining achieves a better learning-forgetting tradeoff, i.e., forgetting less while achieving the same or better performance on the new task, than other optimizers and, possibly surprisingly, LoRA, during the supervised finetuning (SFT) stage. We term this phenomenon optimizer-model consistency. To better understand it, through controlled experiments and theoretical analysis, we show that: 1) optimizers can shape the models by having regularization effects on the activations, leading to different landscapes around the pretrained checkpoints; 2) in response to this regularization effect, the weight update in SFT should follow some specific structures to lower forgetting of the knowledge learned in pretraining, which can be obtained by using the same optimizer. Moreover, we specifically compare Muon and AdamW when they are employed throughout the pretraining and SFT stages and find that Muon performs worse when finetuned for reasoning tasks. With a synthetic language modeling experiment, we demonstrate that this can come from Muon's strong tendency towards rote memorization, which may hurt pattern acquisition with a small amount of data, as for SFT.

</details>


### [365] [Why Global LLM Leaderboards Are Misleading: Small Portfolios for Heterogeneous Supervised ML](https://arxiv.org/abs/2605.06656)
*Jai Moondra,Ayela Chughtai,Bhargavi Lanka,Swati Gupta*

Main category: cs.LG

TL;DR: Current LLM ranking systems using global Bradley-Terry rankings are misleading due to strong heterogeneity in human preferences across languages, tasks, and time. Language grouping dramatically improves ranking consistency, and a new portfolio framework can better represent diverse user preferences than single global rankings.


<details>
  <summary>Details</summary>
Motivation: The motivation is to expose fundamental flaws in current LLM leaderboard rankings that use pairwise human feedback with global Bradley-Terry rankings. These global rankings are shown to be statistically insignificant and misleading because they ignore strong heterogeneity in human preferences across different dimensions like language, task, and temporal factors.

Method: 1) Analyzed ~89K comparisons across 116 languages from 52 LLMs from Arena data. 2) Demonstrated statistical indistinguishability of top models in global rankings. 3) Showed grouping by language dramatically improves ranking consistency. 4) Introduced (λ, ν)-portfolios framework that treats ranking as a set cover problem. 5) Applied this to Arena data and COMPAS dataset with fairness-regularized classification models.

Result: 1) Top 50 models in global BT rankings are statistically indistinguishable (pairwise win probabilities ≤0.53). 2) Language grouping increases ELO score spread by two orders of magnitude. 3) Just 5 distinct BT rankings cover over 96% of votes at modest error, vs. 21% coverage by global ranking. 4) A portfolio of 6 LLMs covers twice as many votes as top-6 from global ranking. 5) Portfolio framework works on fairness problems like COMPAS dataset.

Conclusion: Global LLM rankings are unreliable due to unaccounted heterogeneity in human preferences. Language is a crucial organizing factor. The portfolio framework provides a principled way to represent diverse preferences and can identify blind spots in fairness-aware machine learning, offering practical alternatives to misleading global rankings.

Abstract: Ranking LLMs via pairwise human feedback underpins current leaderboards for open-ended tasks, such as creative writing and problem-solving. We analyze ~89K comparisons in 116 languages from 52 LLMs from Arena, and show that the best-fit global Bradley-Terry (BT) ranking is misleading. Nearly 2/3 of the decisive votes cancel out, and even the top 50 models according to the global BT ranking are statistically indistinguishable (pairwise win probabilities are at most 0.53 within the top 50 models). We trace this failure to strong, structured heterogeneity of opinions across language, task, and time. Moreover, we find an important characteristic - *language* plays a key role. Grouping by language (and families) increases the agreement of votes massively, resulting in two orders of magnitude higher spread in the ELO scores (i.e., very consistent rankings). What appears as global noise is in fact a mixture of coherent but conflicting subpopulations.
  To address such heterogeneity in supervised machine learning, we introduce the framework of $(λ, ν)$-portfolios, which are small sets of models that achieve a prediction error at most $λ$, "covering" at least a $ν$ fraction of users. We formulate this as a variant of the set cover problem and provide guarantees using the VC dimension of the underlying set system. On the Arena data, our algorithms recover just 5 distinct BT rankings that cover over 96% of votes at a modest $λ$, compared to the 21% coverage by the global ranking. We also provide a portfolio of 6 LLMs that cover twice as many votes as the top-6 LLMs from a global ranking. We further construct portfolios for a classification problem on the COMPAS dataset using an ensemble of fairness-regularized classification models and show that these portfolios can be used to detect blind spots in the data, which might be of independent interest to policymakers.

</details>


### [366] [Verifier-Backed Hard Problem Generation for Mathematical Reasoning](https://arxiv.org/abs/2605.06660)
*Yuhang Lai,Jiazhan Feng,Yee Whye Teh,Ning Miao*

Main category: cs.LG

TL;DR: VHG is a framework that uses a three-party self-play approach with an independent verifier to generate valid, challenging problems for LLMs, outperforming existing methods on mathematical reasoning tasks.


<details>
  <summary>Details</summary>
Motivation: LLMs are good at solving scientific/math problems but struggle to generate valid, challenging, novel problems - which is essential for advancing LLM training and enabling autonomous scientific research. Current approaches either require expensive human experts or use naive self-play that often produces invalid problems due to reward hacking.

Method: VHG introduces a verifier-enhanced hard problem generation framework using three-party self-play. It integrates an independent verifier into the traditional setter-solver duality. The setter's reward is jointly determined by problem validity (evaluated by the verifier) and difficulty (assessed by the solver). Two verifier variants are tested: Hard symbolic verifier and Soft LLM-based verifier.

Result: Experimental results show that VHG substantially outperforms all baseline methods by a clear margin on indefinite integral tasks and general mathematical reasoning tasks.

Conclusion: The three-party self-play framework with independent verification effectively addresses the problem of generating valid, challenging problems for LLMs, representing a significant advancement over existing approaches and offering a promising direction for autonomous problem generation.

Abstract: Large Language Models (LLMs) demonstrate strong capabilities for solving scientific and mathematical problems, yet they struggle to produce valid, challenging, and novel problems - an essential component for advancing LLM training and enabling autonomous scientific research. Existing problem generation approaches either depend on expensive human expert involvement or adopt naive self-play paradigms, which frequently yield invalid problems due to reward hacking. This work introduces VHG, a verifier-enhanced hard problem generation framework built upon three-party self-play. By integrating an independent verifier into the conventional setter-solver duality, our design constrains the setter's reward to be jointly determined by problem validity (evaluated by the verifier) and difficulty (assessed by the solver). We instantiate two verifier variants: a Hard symbolic verifier and a Soft LLM-based verifier, with evaluations conducted on indefinite integral tasks and general mathematical reasoning tasks. Experimental results show that VHG substantially outperforms all baseline methods by a clear margin.

</details>


### [367] [UniPool: A Globally Shared Expert Pool for Mixture-of-Experts](https://arxiv.org/abs/2605.06665)
*Minbin Huang,Han Shi,Chuanyang Zheng,Yimeng Wu,Guoxuan Chen,Xintong Yu,Yichun Yin,Hong Cheng*

Main category: cs.LG

TL;DR: UniPool introduces a shared expert pool architecture for Mixture-of-Experts models that decouples expert parameter scaling from depth, reducing redundancy and improving efficiency while maintaining performance.


<details>
  <summary>Details</summary>
Motivation: The paper challenges the conventional per-layer expert allocation rule in MoE architectures, which couples depth scaling with linear expert-parameter growth. The authors discovered that replacing deeper layers' learned routers with uniform random routing only minimally affects accuracy (1.0-1.6 points drop), revealing redundancy in current MoE designs.

Method: UniPool replaces per-layer expert ownership with a single shared pool accessed by independent per-layer routers. To enable stable training under sharing, the authors introduce a pool-level auxiliary loss that balances expert utilization across the entire pool and adopt NormRouter for sparse, scale-stable routing into the shared expert pool.

Result: Across five LLaMA-architecture model scales (182M to 978M parameters) trained on 30B tokens, UniPool consistently improves validation loss/perplexity over vanilla MoE baselines, reducing loss by up to 0.0386. Reduced-pool variants using only 41.6%-66.7% of the vanilla expert-parameter budget match or outperform layer-wise MoE, showing expert parameters need not grow linearly with depth.

Conclusion: UniPool demonstrates that expert capacity can be treated as a global architectural budget rather than per-layer allocations. This enables sublinear expert parameter growth with depth while maintaining efficiency and effectiveness over conventional MoE designs, with benefits that compose with finer-grained expert decomposition.

Abstract: Modern Mixture-of-Experts (MoE) architectures allocate expert capacity through a rigid per-layer rule: each transformer layer owns a separate expert set. This convention couples depth scaling with linear expert-parameter growth and assumes that every layer needs isolated expert capacity. However, recent analyses and our routing probe challenge this allocation rule: replacing a deeper layer's learned top-k router with uniform random routing drops downstream accuracy by only 1.0-1.6 points across multiple production MoE models. Motivated by this redundancy, we propose UniPool, an MoE architecture that treats expert capacity as a global architectural budget by replacing per-layer expert ownership with a single shared pool accessed by independent per-layer routers. To enable stable and balanced training under sharing, we introduce a pool-level auxiliary loss that balances expert utilization across the entire pool, and adopt NormRouter to provide sparse and scale-stable routing into the shared expert pool. Across five LLaMA-architecture model scales (182M, 469M, 650M, 830M, and 978M parameters) trained on 30B tokens from the Pile, UniPool consistently improves validation loss and perplexity over the matched vanilla MoE baselines. Across these scales, UniPool reduces validation loss by up to 0.0386 relative to vanilla MoE. Beyond raw loss improvement, our results identify pool size as an explicit depth-scaling hyperparameter: reduced-pool UniPool variants using only 41.6%-66.7% of the vanilla expert-parameter budget match or outperform layer-wise MoE at the tested scales. This shows that, under a shared-pool design, expert parameters need not grow linearly with depth; they can grow sublinearly while remaining more efficient and effective than vanilla MoE. Further analysis shows that UniPool's benefits compose with finer-grained expert decomposition.

</details>


<div id='cs.MA'></div>

# cs.MA [[Back]](#toc)

### [368] [Active Learning for Communication Structure Optimization in LLM-Based Multi-Agent Systems](https://arxiv.org/abs/2605.05703)
*Huchen Yang,Xinghao Dong,Dan Negrut,Jin-Long Wu*

Main category: cs.MA

TL;DR: An ensemble-based information-theoretic framework for selecting valuable tasks to optimize communication structure in large language model multi-agent systems, improving efficiency and stability under limited budgets.


<details>
  <summary>Details</summary>
Motivation: Existing methods for optimizing LLM-based multi-agent system communication structures rely on randomly sampled training tasks, but tasks vary in difficulty and domain and are not equally informative. This makes optimization unstable and sensitive to training data, especially under limited budgets.

Method: Proposes an ensemble-based information-theoretic task selection framework that estimates task informativeness by how much a candidate task changes the distribution over graph parameters. Uses ensemble Kalman inversion for efficient, derivative-free Bayesian update approximation. Includes embedding-based representative selection for candidate pool construction, combined with surrogate modeling and batch Thompson sampling for scalability.

Result: Validated in both benign settings and settings with agent attacks, demonstrating effectiveness for communication-structure optimization under constrained computational budgets.

Conclusion: The proposed method provides an effective approach for actively identifying valuable tasks to optimize communication structures in LLM-based multi-agent systems, offering improved stability and efficiency compared to random task sampling.

Abstract: Optimizing the communication structure of large language model based multi-agent systems (LLM-MAS) has been shown to improve downstream performance and reduce token usage. Existing methods typically rely on randomly sampled training tasks. However, tasks may differ substantially in difficulty and domain, and thus they are not equally informative for updating communication structure, making optimization under limited training budgets often unstable and highly sensitive to the particular training set. To actively identify the most valuable tasks for communication-structure optimization, we propose an ensemble-based information-theoretic task selection framework. The proposed method estimates task informativeness by how much a candidate task changes the distribution over graph parameters, using ensemble Kalman inversion as an efficient and derivative-free approximation of the corresponding Bayesian update. The resulting estimator is especially suitable for black-box and noisy multi-agent systems. To enhance scalability, we construct a compact candidate pool through embedding-based representative selection and combine the informative selection with surrogate modeling and batch Thompson sampling. We validate our method in both benign settings and settings with agent attacks, demonstrating its effectiveness for communication-structure optimization under constrained computational budgets.

</details>


### [369] [Auto Research with Specialist Agents Develops Effective and Non-Trivial Training Recipes](https://arxiv.org/abs/2605.05724)
*Jingjie Ning,Xiaochuan Li,Ji Zeng,Hao Kang,Chenyan Xiong*

Main category: cs.MA

TL;DR: An auto-research system where specialist agents autonomously propose, test, and refine code recipes through an empirical loop using lineage feedback from external evaluators, achieving measurable improvements across different tasks without human intervention.


<details>
  <summary>Details</summary>
Motivation: To create a closed empirical research loop where AI agents can autonomously conduct scientific research through iterative experimentation, using external measurement and feedback to drive improvements without human intervention in the proposal-selection-experiment cycle.

Method: Specialist agents partition recipe surfaces and operate a closed empirical loop where each trial includes a hypothesis, executable code edit, evaluator-owned outcome, and feedback that shapes next proposals. The system maintains auditable trajectories of proposals, code diffs, experiments, scores, and failure labels.

Result: Across 1,197 headline-run trials plus 600 control trials, the system autonomously improved three tasks: reduced Parameter Golf validation bpb by 0.81%, raised NanoChat-D12 CORE by 38.7%, and reduced CIFAR-10 Airbench96 wallclock by 4.59%. The loop included 157 headline-run submissions and program rewrites without human intervention.

Conclusion: The auto-research system demonstrates that lineage feedback enables agents to transform various evaluator outcomes (including failures) into program-level recipe improvements, autonomously writing code, submitting experiments, and applying known techniques to enhance public starting recipes.

Abstract: We study auto research as a closed empirical loop driven by external measurement. Each submitted trial carries a hypothesis, an executable code edit, an evaluator-owned outcome, and feedback that shapes the next proposal. The output is not a generated paper or a single model checkpoint, but an auditable trajectory of proposals, code diffs, experiments, scores, and failure labels. We instantiate this loop with specialist agents that partition recipe surfaces and share measured lineage across trials. The central empirical finding is that lineage feedback lets agents turn evaluator outcomes, including crashes, budget overruns, size failures, and accuracy-gate misses, into later program-level recipe edits rather than one-shot suggestions. Across 1,197 headline-run trials plus 600 Parameter Golf control trials after one-time setup and launch, humans did not choose proposals, edit recipes, override scores, or repair failed trials during the search. In the three headline runs, the same submitted-trial loop reduces Parameter Golf validation bpb by $0.81\%$, raises NanoChat-D12 CORE by $38.7\%$, and reduces CIFAR-10 Airbench96 wallclock by $4.59\%$, with each task measured by its own external evaluator and legality checks. The trace includes a strict architecture-domain audit of 157 headline-run submissions and program rewrites such as a NanoChat attention-kernel path change. Within this scope the loop autonomously writes code, submits experiments, absorbs feedback, applies and combines known techniques inside each environment, and improves public starting recipes.

</details>


### [370] [Multiagent Stochastic Shortest Path Problem](https://arxiv.org/abs/2605.06056)
*Martin Jonáš,Antonín Kučera,Vojtěch Kůr,Jan Mačák,Vojtěch Řehák*

Main category: cs.MA

TL;DR: This paper introduces the multi-agent stochastic shortest path (MSSP) problem where k agents aim to reach a target, focusing on minimizing expected time for any agent to reach it, with analysis of computational complexity and algorithm design for both autonomous and coordinated settings.


<details>
  <summary>Details</summary>
Motivation: The motivation appears to be addressing multi-agent planning problems in stochastic environments where multiple agents simultaneously strive to reach a common target, which has applications in robotics, autonomous systems, and multi-agent coordination scenarios where minimizing time-to-target for any agent is crucial.

Method: The authors introduce the MSSP problem formulation, analyze its computational and strategy complexity, design efficient strategy-synthesis algorithms for both autonomous (agents act independently) and coordinated (agents can coordinate) settings, and experimentally evaluate these algorithms on instances of increasing size against natural baselines.

Result: The paper presents computational complexity results for the MSSP problem, efficient algorithm designs for strategy synthesis, and experimental evaluation showing performance of their algorithms compared to baseline approaches on various problem instances.

Conclusion: The study establishes the MSSP problem as a meaningful framework for multi-agent stochastic planning, provides theoretical analysis of its complexity, demonstrates practical algorithm designs, and shows promising experimental results for both autonomous and coordinated multi-agent scenarios.

Abstract: We introduce and study the multi-agent stochastic shortest path (MSSP) problem, in which $k$ agents strive to reach a target state, aiming to minimize the expected time to reach the target by any agent. We analyze the computational and strategy-complexity of the problem in both autonomous and coordinated settings, and we design efficient strategy-synthesis algorithms. The algorithms are experimentally evaluated on instances of increasing size against natural baselines.

</details>


### [371] [Power-Efficiency and Scalability Analysis of Magnetically-Actuated Satellite Swarms via Convex Optimization](https://arxiv.org/abs/2605.06286)
*Yuta Takahashi,Seang Shim,Hiraku Sakamoto,Shin-ichiro Sakai*

Main category: cs.MA

TL;DR: Convex optimization framework for analyzing power consumption of magnetically actuated satellite swarms shows increasing satellites improves formation-keeping power efficiency.


<details>
  <summary>Details</summary>
Motivation: Spaceborne distributed apertures using multiple satellites offer high-gain, narrow-beam, and large-aperture capabilities beyond launch-size limitations, but maintaining such virtual structures requires consistent formation control amid unstable orbital dynamics. Magnetic interactions provide propellant-free position control, but nonlinearities in electromagnetic models lead to nonconvex power-consumption constraints that make system-level configuration analysis difficult.

Method: Developed a convex optimization-based framework to analyze the power consumption of large magnetically actuated satellite swarms, addressing the nonconvex power-consumption constraint problem arising from electromagnetic force and torque nonlinearities.

Result: The analysis shows that increasing the number of satellites can improve formation-keeping power efficiency, indicating that magnetically actuated swarm architectures provide a power-efficient alternative to conventional few-satellite electromagnetic formation-flight concepts.

Conclusion: Magnetically actuated satellite swarms offer a promising, power-efficient approach for constructing large-scale space systems, with the convex optimization framework enabling practical analysis of their power consumption and demonstrating scalability benefits over traditional approaches.

Abstract: This correspondence presents a convex-optimization-based evaluation framework of satellite-swarm-based apertures maintained by magnetic-field interactions. Spaceborne distributed apertures are composed of multiple satellites and are attractive for scientific and commercial missions because their scalability enables high-gain, narrow-beam, and large-aperture capabilities beyond the launch-size limitations. A key challenge is that the long-term maintenance of such virtual structures requires consistent formation control amid unstable orbital dynamics, and magnetic interactions generated by satellite-mounted magnetorquers offer a desirable propellant-free position-control strategy. However, the nonlinearities of the electromagnetic force and torque model lead to a nonconvex power-consumption constraint, making system-level configuration analysis difficult. To address this issue, we develop a convex optimization-based framework to analyze the power consumption of large magnetically actuated satellite swarms. The resulting analysis shows that increasing the number of satellites can improve formation-keeping power efficiency. This indicates that magnetically actuated swarm architectures provide a power-efficient alternative to the conventional few-satellite electromagnetic formation-flight concept for constructing large-scale space systems.

</details>


### [372] [Improving the Efficiency of Language Agent Teams with Adaptive Task Graphs](https://arxiv.org/abs/2605.06320)
*Elizabeth Mieczkowski,Alexander Ku,Tiwalayo Eisape,Dilip Arumugam,John Matters,Katherine M. Collins,Ilia Sucholutsky,Thomas L. Griffiths*

Main category: cs.MA

TL;DR: LATTE is a coordination framework for LLM agent teams that uses evolving coordination graphs to balance structure and adaptability, reducing inefficiencies while maintaining task accuracy.


<details>
  <summary>Details</summary>
Motivation: Current LLM team coordination approaches are either too rigid (fixed roles/pipelines) or too unstructured, leading to error propagation, conflicts, and resource waste. There's a need for a balanced approach that maintains consistency while allowing adaptability.

Method: LATTE uses a team of agents that collaboratively construct and maintain a shared, evolving coordination graph encoding sub-task dependencies, agent assignments, and progress states. Inspired by distributed systems, it operates under partial observability and communication constraints.

Result: LATTE reduces token usage, wall-clock time, communication, and coordination failures (file conflicts, redundant outputs) while matching or exceeding the accuracy of standard designs including MetaGPT, decentralized teams, Leader-Worker hierarchies, and static decompositions.

Conclusion: LATTE provides an effective framework for coordinating LLM agent teams that balances structure with adaptability, addressing key inefficiencies in current approaches while maintaining or improving task performance across various models and collaborative tasks.

Abstract: Large language models (LLMs) are increasingly deployed in teams, yet existing coordination approaches often occupy two extremes. Highly structured methods rely on fixed roles, pipelines, or task decompositions assigned a priori. In contrast, fully unstructured teams enable adaptability and exploration but suffer from inefficiencies such as error propagation, inter-agent conflicts, and wasted resources (measured in time, tokens, or file operations). We introduce Language Agent Teams for Task Evolution (LATTE), a framework for coordinating LLM teams inspired by distributed systems, where processors must operate under partial observability and communication constraints. In LATTE, a team of agents collaboratively construct and maintain a shared, evolving coordination graph which encodes sub-task dependencies, individual agent assignment, and the current state of sub-task progress. This protocol maintains consistency while empowering agents to dynamically allocate work, adapt coordination, and discover new tasks. Across multiple collaborative tasks and a variety of base models, we demonstrate how LATTE reduces token usage, wall-clock time, communication, and coordination failures (e.g. file conflicts and redundant outputs) while matching or exceeding the accuracy of standard designs including MetaGPT, decentralized teams, top-down Leader-Worker hierarchies, and static decompositions.

</details>


### [373] [AgenticPrecoding: LLM-Empowered Multi-Agent System for Precoding Optimization](https://arxiv.org/abs/2605.06443)
*Zijiu Yang,Zixiang Zhang,Shunpu Tang,Qianqian Yang,Zhiguo Shi*

Main category: cs.MA

TL;DR: AgenticPrecoding is a universal multi-agent framework that automates end-to-end precoding derivation directly from user requirements using specialized AI agents, achieving superior cross-scenario adaptability for future 6G networks.


<details>
  <summary>Details</summary>
Motivation: Existing precoding methods are limited to specific system models, objectives, and constraints, which hinders their adaptability to heterogeneous and evolving scenarios in future 6G networks. There's a need for a more universal and adaptive approach to precoding design.

Method: Proposes AgenticPrecoding, a multi-agent framework that decomposes precoding derivation into four coordinated stages: problem formulation, solver selection, prompt upsampling, and code generation. Uses two LoRA-adapted reasoning agents for domain-specific tasks and two general-purpose LLMs for prompt refinement and code generation. Includes feedback-driven refinement for code executability and solution quality.

Result: Extensive experiments across 10 representative precoding scenarios demonstrate that AgenticPrecoding achieves superior cross-scenario adaptability compared to conventional optimization-based and LLM-based baselines.

Conclusion: AgenticPrecoding provides a universal framework for automating precoding design that can adapt to diverse 6G network scenarios, addressing the limitations of existing specialized precoding methods through its multi-agent architecture and feedback mechanisms.

Abstract: Precoding is a key technique for interference management and performance improvement in multi-antenna wireless systems. However, existing precoding methods are typically developed for specific system models, objectives, and constraint sets, which limits their adaptability to the heterogeneous and evolving scenarios expected in future 6G networks. To address this limitation, we propose AgenticPrecoding, a universal multi-agent framework that automates end-to-end precoding derivation directly from user-level communication requirements. Specifically, AgenticPrecoding decomposes the derivation process into four coordinated stages: problem formulation, solver selection, prompt upsampling, and code generation, assigning each stage to a specialized agent tailored to its specific reasoning demands. We employ two LoRA-adapted reasoning agents to inject precoding-specific domain knowledge for problem formulation and solver selection, while two general-purpose Large Language Models (LLMs) handle prompt refinement and executable code generation. Furthermore, a feedback-driven refinement mechanism is incorporated to enhance code executability, constraint feasibility, and solution quality. Extensive experiments across 10 representative precoding scenarios demonstrate that AgenticPrecoding achieves superior cross-scenario adaptability compared to conventional optimization-based and LLM-based baselines.

</details>


### [374] [Coordination Matters: Evaluation of Cooperative Multi-Agent Reinforcement Learning](https://arxiv.org/abs/2605.06557)
*Maria Ana Cardei,Matthew Landers,Afsaneh Doryab*

Main category: cs.MA

TL;DR: The paper proposes coordination-aware evaluation for cooperative multi-agent RL, showing that similar aggregate returns can mask important differences in coordination mechanisms, using a controlled spatial task-allocation testbed (STAT) to demonstrate this.


<details>
  <summary>Details</summary>
Motivation: Current cooperative MARL benchmarks focus mainly on aggregate outcomes (return, success rate, completion time) but fail to reveal how agents actually coordinate, especially in complex combinatorial settings where agents, tasks, and assignment choices scale up. This gap leaves researchers unable to understand coordination mechanisms underlying similar performance metrics.

Method: The authors propose a coordination-aware evaluation perspective that supplements return with process-level diagnostics. They instantiate this using STAT - a controlled commitment-constrained spatial task-allocation testbed that systematically varies agents, tasks, and environment size while keeping observation access and task rules fixed. They evaluate six representative value-based MARL methods across varying centralization levels.

Result: Results show that similar return trends can reflect distinct coordination mechanisms, including differences in redundant assignment, assignment diversity, and task-completion efficiency. In commitment-constrained task allocation, performance under scale is shaped not only by nominal action-space size, but also by assignment pressure, sparse decision opportunities, and redundant choices among interdependent agents.

Conclusion: Coordination-aware evaluation is necessary as a complement to return-based benchmarking for cooperative MARL, as aggregate metrics alone are insufficient for understanding how agents coordinate effectively in complex multi-agent systems.

Abstract: Cooperative multi-agent reinforcement learning (MARL) benchmarks commonly emphasize aggregate outcomes such as return, success rate, or completion time. While essential, these metrics often fail to reveal how agents coordinate, particularly in settings where agents, tasks, and joint assignment choices scale combinatorially. We propose a coordination-aware evaluation perspective that supplements return with process-level diagnostics. We instantiate this perspective using STAT, a controlled commitment-constrained spatial task-allocation testbed that systematically varies agents, tasks, and environment size while holding observation access and task rules fixed. We evaluate six representative value-based MARL methods across varying levels of centralization. Our results show that similar return trends can reflect distinct coordination mechanisms, including differences in redundant assignment, assignment diversity, and task-completion efficiency. We find that in commitment-constrained task allocation, performance under scale is shaped not only by nominal action-space size, but also by assignment pressure, sparse decision opportunities, and redundant choices among interdependent agents. Our findings motivate coordination-aware evaluation as a necessary complement to return-based benchmarking for cooperative MARL.

</details>
