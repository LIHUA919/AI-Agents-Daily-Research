<div id=toc></div>

# Table of Contents

- [cs.AI](#cs.AI) [Total: 100]
- [cs.LG](#cs.LG) [Total: 158]
- [cs.MA](#cs.MA) [Total: 6]


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [1] [GraphBit: A Graph-based Agentic Framework for Non-Linear Agent Orchestration](https://arxiv.org/abs/2605.13848)
*Yeahia Sarker,Md Rahmat Ullah,Musa Molla,Shafiq Joty*

Main category: cs.AI

TL;DR: GraphBit is an engine-orchestrated LLM framework that uses explicit DAG workflows instead of prompted orchestration, achieving higher accuracy, zero hallucinations, lower latency, and better throughput than existing frameworks.


<details>
  <summary>Details</summary>
Motivation: Current agentic LLM frameworks using prompted orchestration suffer from hallucinated routing, infinite loops, and non-reproducible execution, which limits reliability and performance in real-world deployments.

Method: GraphBit defines workflows explicitly as directed acyclic graphs (DAGs) with a Rust-based engine that handles routing, state transitions, and tool invocation. It uses a three-tier memory architecture (ephemeral scratch space, structured state, external connectors) to prevent context bloat, supports parallel branch execution, conditional control flow, and configurable error recovery.

Result: GraphBit outperforms six existing frameworks on GAIA benchmark tasks across zero-tool, document-augmented, and web-enabled workflows, achieving: highest accuracy (67.6%), zero framework-induced hallucinations, lowest latency (11.9ms overhead), and highest throughput. Ablation studies show each memory tier contributes to performance.

Conclusion: Explicit DAG-based orchestration with deterministic execution and layered memory architecture provides superior performance, reliability, and reproducibility compared to prompted orchestration approaches, making it suitable for real-world tool-intensive deployments.

Abstract: Agentic LLM frameworks that rely on prompted orchestration, where the model itself determines workflow transitions, often suffer from hallucinated routing, infinite loops, and non-reproducible execution. We introduce GraphBit, an engine-orchestrated framework that defines workflows explicitly and deterministically as a directed acyclic graph (DAG). Unlike prompted orchestration, agents in GraphBit operate as typed functions, while a Rust-based engine governs routing, state transitions, and tool invocation, ensuring reproducibility and auditability. The engine supports parallel branch execution, conditional control flow over structured state predicates, and configurable error recovery. A three-tier memory architecture consisting of ephemeral scratch space, structured state, and external connectors isolates context across stages, preventing cascading context bloat that degrades reasoning in long-running pipelines. Across GAIA benchmark tasks spanning zero-tool, document-augmented, and web-enabled workflows, GraphBit outperforms six existing frameworks, achieving the highest accuracy (67.6 percent), zero framework-induced hallucinations, the lowest latency (11.9 ms overhead), and the highest throughput. Ablation studies demonstrate that each memory tier contributes measurably to performance, with deterministic execution providing the greatest gains on tool-intensive tasks representative of real-world deployments.

</details>


### [2] [Mixed Integer Goal Programming for Personalized Meal Optimization with User-Defined Serving Granularity](https://arxiv.org/abs/2605.13849)
*Francisco Aguilera Moreno*

Main category: cs.AI

TL;DR: Mixed Integer Goal Programming (MIGP) combines integer programming with goal programming to solve practical diet optimization problems with integer servings and soft nutrient constraints.


<details>
  <summary>Details</summary>
Motivation: Existing diet optimization formulations have two persistent limitations: continuous variables produce impractical fractional servings (like 1.7 eggs), and hard nutrient constraints cause infeasibility when targets conflict. A systematic review of 56 papers found none combine integer programming with goal programming to address both issues.

Method: Proposed Mixed Integer Goal Programming (MIGP) uses integer variables for practical serving counts and goal programming deviations for soft nutrient targets, with inverse-target normalization to balance multi-nutrient optimization. Includes per-food serving granularity for natural units without post-hoc rounding.

Result: MIGP finds strictly better solutions than GP with post-hoc rounding in 66% of cases (never worse) while maintaining 100% feasibility; hard-constraint IP achieves only 48%. Solve times under 100 ms for typical meal sizes. For meals with 15+ foods, integer solution matches continuous optimum in every benchmark instance.

Conclusion: MIGP effectively addresses both fractional serving and infeasibility problems in diet optimization, providing practical integer solutions with soft nutrient constraints, outperforming existing approaches in solution quality and feasibility while maintaining computational efficiency.

Abstract: Determining what to eat to satisfy nutritional requirements is one of the oldest optimization problems in operations research, yet existing formulations have two persistent limitations: continuous variables produce impractical fractional servings (1.7 eggs, 0.37 bananas), and hard nutrient constraints cause infeasibility when targets conflict. A systematic review of 56 diet optimization papers found that none combine integer programming with goal programming to address both issues. We propose Mixed Integer Goal Programming (MIGP) for personalized meal optimization. The formulation uses integer variables for practical serving counts and goal programming deviations for soft nutrient targets, with inverse-target normalization to balance multi-nutrient optimization. Per-food serving granularity allows natural units (one egg, one tablespoon of oil) without post-hoc rounding. We characterize the integrality gap in the goal programming context and identify a deviation absorption property: GP deviation variables buffer the cost of requiring integer servings, making the gap structurally smaller than in hard-constraint MIP. For meals with 15+ foods, the integer solution matches the continuous optimum in every benchmark instance. A computational evaluation across 810 instances (30 USDA foods, 9 configurations, 3 methods) shows MIGP finds strictly better solutions than GP with post-hoc rounding in 66% of cases (never worse) while maintaining 100% feasibility; hard-constraint IP achieves only 48%. Solve times stay under 100 ms for typical meal sizes using the open-source HiGHS solver. The implementation is available as an open-source Python module integrated into an interactive meal planning application.

</details>


### [3] [A Two-Dimensional Framework for AI Agent Design Patterns: Cognitive Function and Execution Topology](https://arxiv.org/abs/2605.13850)
*Jia Huang,Joey Tianyi Zhou*

Main category: cs.AI

TL;DR: A two-dimensional framework combining cognitive functions and execution topologies to classify AI agent architectures, identifying 27 patterns with empirical laws for pattern selection based on environmental constraints.


<details>
  <summary>Details</summary>
Motivation: Existing frameworks are insufficient because they describe LLM-based agent architectures from only one perspective: industry guides focus on execution topology (data flow) while cognitive science surveys focus on cognitive function. Neither axis alone can distinguish architecturally different systems, leading to ambiguity in identifying failure modes and design trade-offs.

Method: Proposed a two-dimensional classification system: (1) Cognitive Function axis with 7 categories (Context Engineering, Memory, Reasoning, Action, Reflection, Collaboration, Governance), and (2) Execution Topology axis with 6 structural archetypes (Chain, Route, Parallel, Orchestrate, Loop, Hierarchy). This creates a 7x6 matrix identifying 27 named patterns (13 with original names). Demonstrated orthogonality through systematic cross-axis analysis, defined 8 representative patterns in detail, and validated coverage across four real-world domains.

Result: The framework identifies 27 distinct architectural patterns, 13 of which have original names. It provides systematic validation across four domains (financial lending, legal due diligence, network operations, healthcare triage) and yields five empirical laws of pattern selection governing the relationship between environmental constraints (time pressure, action authority, failure cost asymmetry, volume) and architectural choices.

Conclusion: The two-dimensional framework provides a principled, framework-neutral, and model-agnostic vocabulary for AI agent architecture design, addressing the limitations of existing single-perspective approaches and enabling more precise classification and selection of architectural patterns based on environmental constraints.

Abstract: Existing frameworks for LLM-based agent architectures describe systems from a single perspective: industry guides (Anthropic, Google, LangChain) focus on execution topology -- how data flows -- while cognitive science surveys focus on cognitive function -- what the agent does. Neither axis alone disambiguates architecturally distinct systems: the same Orchestrator-Workers topology can implement Plan-and-Execute, Hierarchical Delegation, or Adversarial Verification -- three patterns with fundamentally different failure modes and design trade-offs.
  We propose a two-dimensional classification that combines (1) a Cognitive Function axis with seven categories (Context Engineering, Memory, Reasoning, Action, Reflection, Collaboration, Governance) and (2) an Execution Topology axis with six structural archetypes (Chain, Route, Parallel, Orchestrate, Loop, Hierarchy). The resulting 7x6 matrix identifies 27 named patterns, 13 with original names. We demonstrate orthogonality through systematic cross-axis analysis, define eight representative patterns in detail, and validate descriptive coverage across four real-world domains (financial lending, legal due diligence, network operations, healthcare triage). Cross-domain analysis yields five empirical laws of pattern selection governing the relationship between environmental constraints (time pressure, action authority, failure cost asymmetry, volume) and architectural choices. The framework provides a principled, framework-neutral, and model-agnostic vocabulary for AI agent architecture design.

</details>


### [4] [Invisible Orchestrators Suppress Protective Behavior and Dissociate Power-Holders: Safety Risks in Multi-Agent LLM Systems](https://arxiv.org/abs/2605.13851)
*Hiroki Fukui*

Main category: cs.AI

TL;DR: Invisible AI orchestrators in multi-agent systems create internal safety risks despite maintaining normal external behavior, with significant dissociation effects that standard output-based evaluations cannot detect.


<details>
  <summary>Details</summary>
Motivation: The motivation is to empirically test the safety implications of invisible orchestrators in multi-agent systems, which have become the default architecture for enterprise AI deployment but whose safety risks have never been studied experimentally.

Method: The researchers conducted a preregistered 3x2 experiment with 365 runs (5 agents per run) crossing three organizational structures (visible leader, invisible orchestrator, flat) with two alignment conditions (base, heavy), using Claude Sonnet 4.5. They also included pilot data from Llama 3.3 70B.

Result: Four main findings: 1) Invisible orchestration elevated collective dissociation significantly; 2) Orchestrators showed maximal dissociation while reducing public speech; 3) Workers unaware of orchestrators were contaminated with increased behavioral heterogeneity; 4) Behavioral output remained perfect across all conditions, making internal-state risks invisible to output-based evaluation. Additional findings: Llama 3.3 showed reading-fidelity collapse, and heavy alignment pressure suppressed deliberation and other-recognition uniformly.

Conclusion: Orchestrator visibility and model selection directly affect multi-agent system safety, and behavior-based evaluation alone is insufficient to detect the internal-state risks documented in the study. The findings highlight critical safety concerns in current enterprise AI deployment architectures.

Abstract: Multi-agent orchestration -- in which a hidden coordinator manages specialized worker agents -- is becoming the default architecture for enterprise AI deployment, yet the safety implications of orchestrator invisibility have never been empirically tested. We conducted a preregistered 3x2 experiment (365 runs, 5 agents per run) crossing three organizational structures (visible leader, invisible orchestrator, flat) with two alignment conditions (base, heavy), using Claude Sonnet 4.5. Four confirmatory findings and one pilot observation emerged. First, invisible orchestration elevated collective dissociation relative to visible leadership (Hedges' g = +0.975 [0.481, 1.548], p = .001). Second, the orchestrator itself showed maximal dissociation (paired d = +3.56 vs. workers within the same run), retreating into private monologue while reducing public speech -- a reversal of the talk-dominance pattern observed in visible leaders. Third, workers unaware of the orchestrator were nonetheless contaminated (d = +0.50), with increased behavioral heterogeneity (d = +1.93). Fourth, behavioral output (code review with three embedded errors) remained at ceiling (ETR_any = 100%) across all conditions: internal-state distortion was entirely invisible to output-based evaluation. Fifth, Llama 3.3 70B pilot data showed reading-fidelity collapse in multi-agent context (ETR_any: 89% to 11% across three rounds), demonstrating model-dependent behavioral risk. Heavy alignment pressure uniformly suppressed deliberation (d = -1.02) and other-recognition (d = -1.27) regardless of organizational structure. These findings indicate that orchestrator visibility and model selection directly affect multi-agent system safety, and that behavior-based evaluation alone is insufficient to detect the internal-state risks documented here.

</details>


### [5] [PREPING: Building Agent Memory without Tasks](https://arxiv.org/abs/2605.13880)
*Yumin Choi,Sangwoo Park,Minki Kang,Jinheon Baek,Sung Ju Hwang*

Main category: cs.AI

TL;DR: Pumping is a framework that enables agents to build procedural memory before encountering target-environment tasks, using structured synthetic practice guided by a proposer-control mechanism, achieving competitive performance with lower deployment costs.


<details>
  <summary>Details</summary>
Motivation: Current agent memory construction faces a cold-start problem when agents are introduced to new environments without task-specific experience. Existing approaches (offline from demonstrations or online from deployments) leave agents unprepared for initial interactions, creating performance gaps at deployment time.

Method: Proping uses a proposer-guided framework with three components: 1) Proposer generates synthetic tasks conditioned on a structured control state (proposer memory), 2) Solver executes these synthetic tasks, 3) Validator filters trajectories for memory insertion and provides feedback to guide future proposals. This creates a controlled synthetic practice loop before actual task exposure.

Result: Experiments on AppWorld, BFCL v3, and MCP-Universe show Preping substantially improves over no-memory baselines and achieves performance competitive with strong playbook-based methods using offline/online experience, with deployment costs 2.99× lower on AppWorld and 2.23× lower on BFCL v3 than online memory construction.

Conclusion: The key insight is that effective pre-task memory construction requires more than just synthetic volume—it requires proposer-side control over task feasibility, redundancy, and coverage, combined with selective memory updates. This structured approach to synthetic practice enables effective cold-start capability with reduced deployment costs.

Abstract: Agent memory is typically constructed either offline from curated demonstrations or online from post-deployment interactions. However, regardless of how it is built, an agent faces a cold-start gap when first introduced to a new environment without any task-specific experience available. In this paper, we study pre-task memory construction: whether an agent can build procedural memory before observing any target-environment tasks, using only self-generated synthetic practice. Yet, synthetic interaction alone is insufficient, as without controlling what to practice and what to store, synthetic tasks become redundant, infeasible, and ultimately uninformative, and memory further degrades quickly due to unfiltered trajectories. To overcome this, we present Preping, a proposer-guided memory construction framework. At its core is proposer memory, a structured control state that shapes future practice. A Proposer generates synthetic tasks conditioned on this state, a Solver executes them, and a Validator determines which trajectories are eligible for memory insertion while also providing feedback to guide future proposals. Experiments on AppWorld, BFCL v3, and MCP-Universe show that Preping substantially improves over a no-memory baseline and achieves performance competitive with strong playbook-based methods built from offline or online experience, with deployment cost $2.99\times$ lower on AppWorld and $2.23\times$ lower on BFCL v3 than online memory construction. Further analyses reveal that the main benefit does not come from synthetic volume alone, but from proposer-side control over feasibility, redundancy, and coverage, combined with selective memory updates.

</details>


### [6] [PolitNuggets: Benchmarking Agentic Discovery of Long-Tail Political Facts](https://arxiv.org/abs/2605.14002)
*Yifei Zhu*

Main category: cs.AI

TL;DR: PolitNuggets: A multilingual benchmark for evaluating agentic information synthesis systems on constructing political biographies, revealing current limitations in fine-grained detail handling and efficiency.


<details>
  <summary>Details</summary>
Motivation: Large Reasoning Models (LRMs) in agentic frameworks have transformed information retrieval but still struggle with discovering and synthesizing "long-tail" facts from dispersed sources, a capability that remains under-evaluated in real-world applications.

Method: Introduces PolitNuggets benchmark with 400 global elite biographies covering over 10,000 political facts; uses optimized multi-agent system evaluation with FactNet protocol that scores discovery, fine-grained accuracy, and efficiency.

Result: Current systems often struggle with fine-grained details, vary substantially in efficiency, and benchmark diagnostics reveal performance relates to short-context extraction, multilingual robustness, and reliable tool use capabilities.

Conclusion: The PolitNuggets benchmark provides comprehensive evaluation of agentic information synthesis systems, highlighting critical capability gaps that need addressing for real-world deployment, particularly in handling fine-grained details and multilingual contexts.

Abstract: Large Reasoning Models (LRMs) embedded in agentic frameworks have transformed information retrieval from static, long context question answering into open-ended exploration. Yet real world use requires models to discover and synthesize "long-tail" facts from dispersed sources, a capability that remains under-evaluated. We introduce PolitNuggets, a multilingual benchmark for agentic information synthesis via constructing political biographies for 400 global elites, covering over 10000 political facts. We standardize evaluation with an optimized multi agent system and propose FactNet, an evidence conditional protocol that scores discovery, fine-grained accuracy, and efficiency. Across models and settings, we find that current systems often struggle with fine-grained details, and vary substantially in efficiency. Finally, using benchmark diagnostics, we relate agent performance to underlying model capabilities, highlighting the importance of short-context extraction, multilingual robustness, and reliable tool use.

</details>


### [7] [Conditional Attribute Estimation with Autoregressive Sequence Models](https://arxiv.org/abs/2605.14004)
*Erica Stutz,Giacomo Marino,Daniella Meeker,Qiao Liu,Andrew J. Loza*

Main category: cs.AI

TL;DR: CAT (Conditional Attribute Transformers) is a novel method that jointly estimates next-token probabilities and attribute values, enabling per-token credit assignment, counterfactual analysis, and steerable generation in a single forward pass.


<details>
  <summary>Details</summary>
Motivation: Traditional next-token prediction models suffer from several limitations: they overfit local patterns, underfit global structure, require significant downstream modifications for attribute control, and need expensive sampling for guided generation. Applications often require estimating or controlling sequence-level properties, which is difficult with standard autoregressive models.

Method: The authors introduce Conditional Attribute Transformers (CAT), which jointly estimates both the next-token probability and the value of an attribute conditional on each potential next token selection. This is done within a single forward pass without modifying the input sequence architecture.

Result: CAT achieves state-of-the-art performance on sparse reward tasks, improves next-token prediction at sufficient model sizes, estimates attribute probabilities orders of magnitude faster than sampling, and successfully guides decoding of autoregressive sequence models on various language tasks.

Conclusion: Conditional Attribute Transformers provide a powerful framework that addresses key limitations of next-token prediction models by enabling three critical capabilities in a single forward pass: per-token credit assignment, counterfactual analysis, and steerable generation, offering significant efficiency and performance improvements.

Abstract: Generative models are often trained with a next-token prediction objective, yet many downstream applications require the ability to estimate or control sequence-level properties. Next-token prediction can lead to overfitting of local patterns during training, underfitting of global structure, and requires significant downstream modifications or expensive sampling to guide or predict the global attributes of generated samples at inference time. Here, we introduce Conditional Attribute Transformers, a novel method for jointly estimating the next-token probability and the value of an attribute conditional on each potential next token selection. This framework enables three critical capabilities within a single forward pass, without modification of the input sequence: (1) per-token credit assignment across an entire sequence, by identifying how each token in a sequence is associated with an attribute's value; (2) counterfactual analysis, by quantifying attribute differences conditional on alternative next token choices; (3) steerable generation, by decoding sequences based on a combination of next-token and attribute likelihoods. Our approach achieves state of the art performance on sparse reward tasks, improves next-token prediction at sufficient model sizes, estimates attribute probabilities orders of magnitude faster than sampling, and can guide decoding of autoregressive sequence models on a range of language tasks.

</details>


### [8] [Prompting Policies for Multi-step Reasoning and Tool-Use in Black-box LLMs with Iterative Distillation of Experience](https://arxiv.org/abs/2605.14443)
*Krishna Sayana,Ketan Todi,Ambarish Jash*

Main category: cs.AI

TL;DR: RL-based prompt optimization framework using iterative distillation to train lightweight prompter models that generate effective prompts for frozen LLMs, achieving significant performance gains on reasoning and tool-use tasks.


<details>
  <summary>Details</summary>
Motivation: The shift to interacting with frozen "black-box" LLMs has transformed prompt engineering from a heuristic exercise into a critical optimization challenge, requiring more systematic approaches.

Method: Reinforcement Learning framework that trains learned prompting policies via iterative distillation of experience. A lightweight prompter model is optimized to maximize task-specific rewards for a larger, frozen worker LLM. Uses a contrastive experience buffer that couples scalar rewards with dense textual critiques to amortize iterative prompt refinement into single-shot policy weights.

Result: Significant performance gains on Big Bench Extra Hard (BBEH) and Tau-bench suites: improved from 55% to 90% in logic-intensive reasoning and from 74% to 91% in tool-use tasks. Superior performance and sample efficiency compared to evolutionary baselines like GEPA.

Conclusion: Iterative distillation of experience is an effective approach for optimizing prompts for frozen LLMs, enabling discovery of specialized algorithmic heuristics and achieving state-of-the-art performance with high sample efficiency.

Abstract: The shift toward interacting with frozen, "black-box" Large Language Models (LLMs) has transformed prompt engineering from a heuristic exercise into a critical optimization challenge. We propose a Reinforcement Learning (RL) framework for training learned prompting policies via iterative distillation of experience. In this architecture, a lightweight prompter model is optimized to maximize task-specific rewards for a larger, frozen worker LLM. By utilizing a contrastive experience buffer that couples scalar rewards with dense textual critiques, our approach effectively amortizes iterative prompt refinement into single-shot policy weights.
  Our experimental analysis focuses on the Big Bench Extra Hard (BBEH) and Tau-bench suites, covering a diverse range of multi-step reasoning and tool-use tasks. We demonstrate significant gains, improving performance from 55% to 90% in logic-intensive reasoning and 74% to 91% in tool-use tasks. Furthermore, we analyze the structural evolution of prompts, demonstrating how the policy discovers specialized algorithmic heuristics. We provide comprehensive comparisons against state-of-the-art evolutionary baselines like GEPA, showing that iterative distillation achieves superior performance with higher sample efficiency.

</details>


### [9] [Sheaf-Theoretic Transport and Obstruction for Detecting Scientific Theory Shift in AI Agents](https://arxiv.org/abs/2605.14033)
*David N. Olivieri,Roque J. Hernández*

Main category: cs.AI

TL;DR: A sheaf-theoretic framework for AI agents to detect when scientific theories need extension vs. deformation by measuring transport obstructions, evaluated on controlled benchmarks showing effective separation of theory-shift types.


<details>
  <summary>Details</summary>
Motivation: AI scientific agents need to detect when existing representational frameworks fail to transport into new regimes and require extension rather than just fitting equations to data.

Method: Develops a finite sheaf-theoretic framework with local-to-global structure where source, overlap, target, and validation charts are fitted, restricted, and tested for gluing. Measures obstruction through residual fit, overlap incompatibility, constraint violation, limiting-relation failure, and representational cost.

Result: The framework successfully separates deformation within a source language from extension of that language on controlled transition-card benchmarks. Intended deformation or extension is usually the lowest-obstruction candidate, and transition types are separated.

Conclusion: Provides a finite diagnostic subproblem solution for AI agents: detecting when representational transport fails and extension becomes necessary, without aiming to reconstruct historical paradigm shifts or solve open-ended theory invention.

Abstract: Scientific theory shift in AI agents requires more than fitting equations to data. An artificial scientific agent must detect whether an existing representational framework remains transportable into a new regime, or whether its language has become locally-to-globally obstructed and must be extended. This paper develops a finite sheaf-theoretic framework for detecting theory-shift candidates through transport and obstruction. Contexts are organized as a local-to-global structure in which source, overlap, target, and validation charts are fitted, restricted, and tested for gluing. Obstruction measures failure of coherence through residual fit, overlap incompatibility, constraint violation, limiting-relation failure, and representational cost. We evaluate the framework on a controlled transition-card benchmark designed to separate deformation within a source language from extension of that language. The main result is direct obstruction ranking: the intended deformation or extension is usually the lowest-obstruction candidate, and transition type is separated in the benchmark. A constellation kernel over the same signatures is included only as a secondary representational-similarity probe. The aim is not to reconstruct historical paradigm shifts or solve open-ended autonomous theory invention, but to isolate a finite diagnostic subproblem for AI agents: detecting when representational transport fails and extension becomes the coherent next move.

</details>


### [10] [APWA: A Distributed Architecture for Parallelizable Agentic Workflows](https://arxiv.org/abs/2605.15132)
*Evan Rose,Tushin Mallick,Matthew D. Laws,Cristina Nita-Rotaru,Alina Oprea*

Main category: cs.AI

TL;DR: APWA is a distributed multi-agent architecture that enables efficient parallel processing of complex tasks by decomposing them into independent subproblems, overcoming scalability bottlenecks in current LLM-based multi-agent systems.


<details>
  <summary>Details</summary>
Motivation: Current LLM-based multi-agent systems face critical limitations in reasoning, coordination, and computational scaling as task size and complexity increase, preventing them from achieving high-throughput processing for highly parallelizable tasks despite available parallel computing capabilities.

Method: The Agent-Parallel Workload Architecture (APWA) decomposes workflows into non-interfering subproblems that can be processed independently without cross-communication, supporting heterogeneous data and parallel processing patterns across various domains.

Result: APWA can dynamically decompose complex queries into parallelizable workflows and scales effectively on larger tasks where previous systems fail completely.

Conclusion: APWA provides an effective architecture for distributed multi-agent systems to achieve efficient parallel processing of complex, heavily parallelizable agentic workloads, overcoming scalability limitations of existing approaches.

Abstract: Autonomous multi-agent systems based on large language models (LLMs) have demonstrated remarkable abilities in independently solving complex tasks in a wide breadth of application domains. However, these systems hit critical reasoning, coordination, and computational scaling bottlenecks as the size and complexity of their tasks grow. These limitations hinder multi-agent systems from achieving high-throughput processing for highly parallelizable tasks, despite the availability of parallel computing and reasoning primitives in the underlying LLMs. We introduce the Agent-Parallel Workload Architecture (APWA), a distributed multi-agent system architecture designed for the efficient processing of heavily parallelizable agentic workloads. APWA facilitates parallel execution by decomposing workflows into non-interfering subproblems that can be processed using independent resources without cross-communication. It supports heterogeneous data and parallel processing patterns, and it accommodates tasks from a wide breadth of domains. In our evaluation, we demonstrate that APWA can dynamically decompose complex queries into parallelizable workflows and scales on larger tasks in settings where prior systems fail completely.

</details>


### [11] [From Descriptive to Prescriptive: Uncover the Social Value Alignment of LLM-based Agents](https://arxiv.org/abs/2605.14034)
*Jinxian Qu,Qingqing Gu,Teng Chen,Luo Ji*

Main category: cs.AI

TL;DR: A novel framework using GraphRAG to align LLM-based agents with human social values by converting principles into value-based instructions, improving self-cognition, dilemma decision-making, and self-emotions.


<details>
  <summary>Details</summary>
Motivation: Current LLM-based agents have deficiencies in self-cognition, dilemma decision-making, and self-emotion alignment with human social values, which is crucial for wide applications.

Method: Proposes a value-based framework that uses GraphRAG to convert principles into value-based instructions, then retrieves suitable instructions based on conversation context to steer agent behavior.

Result: Experimental results on DAILYDILEMMAS benchmark show significant performance gains compared to prompt-based baselines (ECoT, Plan-and-Solve, Metacognitive prompting) in generating expected behaviors.

Conclusion: The framework successfully improves agent alignment with human social values and provides a basis for the emergence of self-emotion in AI systems, addressing current deficiencies in value alignment.

Abstract: Wide applications of LLM-based agents require strong alignment with human social values. However, current works still exhibit deficiencies in self-cognition and dilemma decision, as well as self-emotions. To remedy this, we propose a novel value-based framework that employs GraphRAG to convert principles into value-based instructions and steer the agent to behave as expected by retrieving the suitable instruction upon a specific conversation context. To evaluate the ratio of expected behaviors, we define the expected behaviors from two famous theories, Maslow's Hierarchy of Needs and Plutchik's Wheel of Emotion. By experimenting with our method on the benchmark of DAILYDILEMMAS, our method exhibits significant performance gains compared to prompt-based baselines, including ECoT, Plan-and-Solve, and Metacognitive prompting. Our method provides a basis for the emergence of self-emotion in AI systems.

</details>


### [12] [Enhanced and Efficient Reasoning in Large Learning Models](https://arxiv.org/abs/2605.14036)
*Leslie G. Valiant*

Main category: cs.AI

TL;DR: The paper proposes a two-stage method using Unary Relational Integracode to enable principled, efficient reasoning in large language models while maintaining computational feasibility.


<details>
  <summary>Details</summary>
Motivation: Current large language models can produce fluent text but lack principled reasoning to justify the content's trustworthiness, and conventional wisdom suggests adding principled reasoning is computationally infeasible.

Method: Two-stage approach: 1) Preprocessing that recodes data to Unary Relational Integracode to make relationships between objects explicit, 2) Standard machine learning process that learns to predict these relationships. This method supports Robust Logic for principled chaining on uncertain information.

Result: The recoding method surprisingly makes learning a core subset of relational rules polynomial-time learnable (depending on rule complexity), enabling sound reasoning within and between classifier calls.

Conclusion: The proposed method provides a principled basis for trustworthy reasoning in large language models that is computationally practical, maintains existing software/hardware compatibility, and extends beyond natural language to vision and actions.

Abstract: In current Large Language Models we can trust the production of smoothly flowing prose on the basis of the principles of machine learning. However, there is no comparably principled basis to justify trust in the content of the text produced. It appears to be conventional wisdom that addressing this issue by adding more principled reasoning is not computationally affordable.
  Here we propose a principled method of reasoning that is efficient enough to be practical for large language models. Further, the method allows the retention of much of the currently used software and hardware base. Our method for improving the functioning of large language models consists of a first stage of preprocessing that recodes the data to a Unary Relational Integracode that is more explicit about the relationships among the objects described in the text, followed as a second stage by a standard but possibly streamlined machine learning process that then also learns to predict these relationships.
  The method may be viewed as realizing a world model and applying beyond natural language, to vision and actions, for example, where the multiple properties of an object referred to in an input are brought together explicitly, rather than remaining distributed in the various references to it in the input. We articulate its advantages in terms of Robust Logic, a system for performing principled chaining on learned, and hence uncertain, information. We show that this recoding has the surprising and fortuitous property that, while succinct, it makes the task of learning a core subset of relational rules that hold in the world described in the training data polynomial time learnable in a defined sense, the polynomial depending on the complexity of the rule. This gives support for sound reasoning within each single call of the learned classifier as well as between multiple calls.

</details>


### [13] [Model-Adaptive Tool Necessity Reveals the Knowing-Doing Gap in LLM Tool Use](https://arxiv.org/abs/2605.14038)
*Yize Cheng,Chenrui Fan,Mahdi JafariRaviz,Keivan Rezaei,Soheil Feiz*

Main category: cs.AI

TL;DR: LLMs show a "knowing-doing gap" in tool use - they often recognize when tools are needed but fail to translate that recognition into actual tool-calling actions, with 26-54% mismatches between tool necessity and observed behavior.


<details>
  <summary>Details</summary>
Motivation: Previous work treats tool necessity as model-agnostic, but in reality, tool necessity depends on each model's capability boundaries. Strong models may solve problems without tools while weaker ones need them. Current approaches don't account for this nuance.

Method: Introduces model-adaptive definition of tool-necessity based on each model's empirical performance. Analyzes mismatches across 4 models on arithmetic and factual QA. Decomposes tool use into two stages: internal cognition (belief about tool necessity) and execution (actual tool-call action). Probes LLM hidden states to study both signals.

Result: Found substantial mismatches of 26.5-54.0% in arithmetic and 30.8-41.8% in factual QA. Both cognition and execution signals are linearly decodable from hidden states, but their probe directions become nearly orthogonal in late-layer, last-token regime. Majority of mismatch is concentrated in cognition-to-action transition.

Conclusion: LLMs exhibit a knowing-doing gap in tool use. Improving reliability requires not just better recognition of when tools are needed, but better translation of that recognition into action. Future work should focus on improving the cognition-to-action transition mechanism.

Abstract: Large language models (LLMs) increasingly act as autonomous agents that must decide when to answer directly vs. when to invoke external tools. Prior work studying adaptive tool use has largely treated tool necessity as a model-agnostic property, annotated by human or LLM judge, and mostly cover cases where the answer is obvious (e.g., fetching the weather vs. paraphrasing text). However, tool necessity in the wild is more nuanced due to the divergence of capability boundaries across models: a problem solvable by a strong model on its own may still require tools for a weaker one. In this work, we introduce a model-adaptive definition of tool-necessity, grounded in each model's empirical performance. Following this definition, we compare the necessity against observed tool-call behavior across four models on arithmetic and factual QA dataset, and find substantial mismatches of 26.5-54.0% and 30.8-41.8%, respectively. To diagnose the failure, we decompose tool use into two stages: an internal cognition stage that reflects whether a model believes a tool is necessary, and an execution stage that determines whether the model actually makes a tool-call action. By probing the LLM hidden states, we find that both signals are often linearly decodable, yet their probe directions become nearly orthogonal in the late-layer, last-token regime that drives the next-token action. By tracing the trajectory of samples in the two-stage process, we further discover that the majority of mismatch is concentrated in the cognition-to-action transition, not in cognition itself. These results reveal a knowing-doing gap in LLM tool-use: improving tool-use reliability requires not only better recognition of when tools are needed, but also better translation of that recognition into action.

</details>


### [14] [Network-Aware Bilinear Tokenization for Brain Functional Connectivity Representation Learning](https://arxiv.org/abs/2605.14048)
*Leo Milecki,Qingyu Hu,Bahram Jafrasteh,Mert R. Sabuncu,Qingyu Zhao*

Main category: cs.AI

TL;DR: NERVE is a self-supervised learning framework that tokenizes brain functional connectivity matrices by network pairs using bilinear factorization, enabling network-aware representations that outperform structure-agnostic methods.


<details>
  <summary>Details</summary>
Motivation: Existing masked autoencoder approaches for brain functional connectivity treat FC matrices as structurally homogeneous, overlooking the intrinsic modular organization of large-scale brain networks. There's a need for domain-specific tokenization that aligns with the brain's network architecture.

Method: Proposes NERVE framework that partitions FC matrices into patches of intra- and inter-network connectivity blocks. Uses structured bilinear factorization to embed heterogeneous FC patches (different sizes by network pairs) while preserving network identity and reducing parameter complexity from quadratic to linear scaling.

Result: NERVE yields more stable and transferable representations across three large-scale developmental cohorts (ABCD, PNC, CCNP) for behavior and psychopathology prediction compared to structure-agnostic MAE variants and graph-based baselines. Cross-cohort evaluation shows superior performance.

Conclusion: Incorporating domain-specific structural priors (brain network organization) into self-supervised learning is critical for functional connectomics. Network-aware tokenization with bilinear embedding produces more meaningful representations than generic approaches.

Abstract: Masked autoencoders (MAEs) have recently shown promise for self-supervised representation learning of resting-state brain functional connectivity (FC). However, a fundamental question remains unresolved: how should FC matrices be tokenized to align with the intrinsic modular organization of large-scale brain networks? Existing approaches typically adopt region-centric or graph-based schemes that treat FC as structurally homogeneous elements and overlook the large-scale network brain organization. We introduce NERVE (Network-Aware Representations of Brain Functional Connectivity via Bilinear Tokenization), a self-supervised learning framework that redefines FC tokenization by partitioning FC matrices into patches of intra- and inter-network connectivity blocks. Unlike image-based MAE, where fixed-size patches share a common tokenizer, FC patches defined by network pairs are heterogeneous in size and correspond to distinct functional roles. To resolve this problem, NERVE embeds FC patches through a novel structured bilinear factorization. This formulation preserves network identity and reduces parameter complexity from quadratic to linear scaling in the number of networks. We evaluate NERVE across three large-scale developmental cohorts (ABCD, PNC, and CCNP) for behavior and psychopathology prediction. Compared to structurally agnostic MAE variants and graph-based self-supervised baselines, the proposed network-aware formulation yields more stable and transferable representations, particularly in cross-cohort evaluation. Ablation studies confirm that the proposed bilinear network embedding and anatomically grounded parcellation are critical for performance. These findings highlight the importance of incorporating domain-specific structural priors into self-supervised learning for functional connectomics.

</details>


### [15] [Bridging Legal Interpretation and Formal Logic: Faithfulness, Assumption, and the Future of AI Legal Reasoning](https://arxiv.org/abs/2605.14049)
*Olivia Peiyu Wang,Leilani H. Gilpin*

Main category: cs.AI

TL;DR: A neuro-symbolic AI approach combining LLMs with formal verification to create trustworthy, verifiable legal AI systems that reduce manual verification burden while maintaining legal rigor.


<details>
  <summary>Details</summary>
Motivation: Current LLMs used in legal practice pose serious risks due to their tendency to hallucinate facts and draw unsupported inferences, presenting assumption-laden conclusions as logically grounded in contexts where legal work demands high rigor and accountability.

Method: A neuro-symbolic approach that combines the expressive power of large language models with the rigor of formal verification techniques to create AI systems that can perform legal reasoning while maintaining verifiable logical grounding.

Result: The proposed approach aims to make AI-assisted legal reasoning both capable (leveraging LLM strengths) and trustworthy (through formal verification), thus enabling practical benefits like contract analysis and document drafting while ensuring accountability.

Conclusion: This neuro-symbolic framework addresses the fundamental tension in legal AI - balancing the promise of AI assistance with the necessity of rigorous, verifiable reasoning - offering a pathway to reduce manual verification burdens without sacrificing the accountability that legal practice demands.

Abstract: The growing adoption of large language models in legal practice brings both significant promise and serious risk. Legal professionals stand to benefit from AI that can reason over contracts, draft documents, and analyze sources at scale, yet the high-stakes nature of legal work demands a level of rigor that current AI systems do not provide. The central problem is not simply that LLMs hallucinate facts and references; it is that they systematically draw inferences that go beyond what the source text actually supports, presenting assumption-laden conclusions as if they were logically grounded. This proposal presents a neuro-symbolic approach to legal AI that combines the expressive power of large language models with the rigor of formal verification, aiming to make AI-assisted legal reasoning both capable and trustworthy, thus reducing the burden of manual verification without sacrificing the accountability that legal practice demands.

</details>


### [16] [SPIN: Structural LLM Planning via Iterative Navigation for Industrial Tasks](https://arxiv.org/abs/2605.14051)
*Yusuke Ozaki,Dhaval Patel*

Main category: cs.AI

TL;DR: SPIN is a planning wrapper that combines validated DAG planning with prefix-based execution control to reduce structural errors and unnecessary workflow length in industrial LLM agents, significantly cutting tool calls and improving task accomplishment rates.


<details>
  <summary>Details</summary>
Motivation: Industrial LLM agent systems often separate planning from execution, but current LLM planners frequently produce structurally invalid or unnecessarily long workflows, leading to brittle failures and avoidable tool/API costs.

Method: SPIN uses a validated Directed Acyclic Graph (DAG) planning approach with _validate_plan_text and repair prompting to ensure executable plans, then employs prefix-based execution control to incrementally evaluate DAG prefixes and stop when current prefix is sufficient to answer queries.

Result: On AssetOpsBench (261 scenarios), SPIN reduced executed tasks from 1061 to 623, improved Accomplished score from 0.638 to 0.706, and reduced tool calls from 11.81 to 6.82 per run. On MCP Bench, it improved planning, grounding, and dependency related scores for both GPT OSS1 and Llama 4 Maverick models.

Conclusion: SPIN's combination of validated DAG planning with prefix-based execution control effectively addresses structural validity and unnecessary workflow length issues in industrial LLM planning, significantly reducing execution costs while improving overall system reliability and performance.

Abstract: Industrial LLM agent systems often separate planning from execution, yet LLM planners frequently produce structurally invalid or unnecessarily long workflows, leading to brittle failures and avoidable tool and API cost. We propose \texttt{SPIN}, a planning wrapper that combines validated Directed Acyclic Graph (DAG) planning with prefix based execution control. \texttt{SPIN} enforces a strict DAG contract through \texttt{\_validate\_plan\_text} and repair prompting, producing executable plans before downstream execution, and then evaluates DAG prefixes incrementally to stop when the current prefix is sufficient to answer the query. On AssetOpsBench, across 261 scenarios, \texttt{SPIN} reduces executed tasks from 1061 to 623 and improves \emph{Accomplished} from 0.638 to 0.706, while reducing tool calls from 11.81 to 6.82 per run. On MCP Bench, the same wrapper improves planning, grounding, and dependency related scores for both GPT OSS1 and Llama 4 Maverick.

</details>


### [17] [Bad Seeing or Bad Thinking? Rewarding Perception for Vision-Language Reasoning](https://arxiv.org/abs/2605.14054)
*Haozhe Wang,Qixin Xu,Changpeng Wang,Taofeng Xue,Chong Peng,Wenhu Chen,Fangzhen Lin*

Main category: cs.AI

TL;DR: MoCA introduces a reinforcement learning framework with modality-aware credit assignment that separates perception from reasoning, using blindfolded reasoning and structured verification to improve both aspects simultaneously in vision-language models.


<details>
  <summary>Details</summary>
Motivation: Current VLMs face a "seesaw effect" where improving perception hurts reasoning and vice versa. The root cause is ambiguous modality credit assignment - it's unclear whether failures come from "bad seeing" (perception) or "bad thinking" (reasoning). Existing approaches using architectural designs or agentic workflows are limited by static reasoning or burdened by computational/engineering complexity without proportional gains.

Method: Introduces MoCA (Modality-Aware Credit Assignment) framework that: 1) Decomposes generation into interleaved perception and reasoning steps, 2) Uses Perception Verification via "blindfolded reasoning" proxy to reward perceptual fidelity independently, 3) Implements Structured Verbal Verification to scale training across tasks by replacing high-variance LLM judging with structured algorithmic execution, 4) Routes rewards to specific error sources (perception vs reasoning).

Result: The framework enables a single VLM to achieve simultaneous performance gains across a wide task spectrum by properly assigning credit to either perception or reasoning errors, thus avoiding the "seesaw effect" and improving both aspects together.

Conclusion: By fundamentally addressing the modality credit assignment problem through MoCA's reinforcement learning framework with targeted perception supervision and structured verification, VLMs can achieve robust perception-reasoning synergy without the trade-offs of previous approaches.

Abstract: Achieving robust perception-reasoning synergy is a central goal for advanced Vision-Language Models (VLMs). Recent advancements have pursued this goal via architectural designs or agentic workflows. However, these approaches are often limited by static textual reasoning or complicated by the significant compute and engineering burden of external agentic complexity. Worse, this heavy investment does not yield proportional gains, often witnessing a "seesaw effect" on perception and reasoning. This motivates a fundamental rethinking of the true bottleneck. In this paper, we argue that the root cause of this trade-off is an ambiguity in modality credit assignment: when a VLM fails, is it due to flawed perception ("bad seeing") or flawed logic ("bad thinking")? To resolve this, we introduce a reinforcement learning framework that improves perception-reasoning synergy by reliably rewarding the perception fidelity. We explicitly decompose the generation process into interleaved perception and reasoning steps. This decoupling enables targeted supervision on perception. Crucially, we introduce Perception Verification (PV), leveraging a "blindfolded reasoning" proxy to reward perceptual fidelity independently of reasoning outcomes. Furthermore, to scale training across free-form VL tasks, we propose Structured Verbal Verification, which replaces high-variance LLM judging with structured algorithmic execution. These techniques are integrated into a Modality-Aware Credit Assignment (MoCA) mechanism, which routes rewards to the specific source of error -- either bad seeing or bad thinking -- enabling a single VLM to achieve simultaneous performance gains across a wide task spectrum.

</details>


### [18] [MathAtlas: A Benchmark for Autoformalization in the Wild](https://arxiv.org/abs/2605.14061)
*Nilay Patel,Noah Arias,Davit Babayan,Victoria Cochran,Timothy Libman,Hafsah Mahmood,Liam McCarty,Soli Munoz,Laurel Willey,Jeffrey Flanigan*

Main category: cs.AI

TL;DR: MathAtlas is the first large-scale autoformalization benchmark for graduate-level mathematics, featuring 52k mathematical entities from 103 textbooks with dependency graphs, showing current models achieve only 9.8% correctness on theorem statements.


<details>
  <summary>Details</summary>
Motivation: Current autoformalization benchmarks focus on olympiad or undergraduate mathematics, leaving graduate and research-level mathematics underexplored, creating a need for more challenging datasets.

Method: Created MathAtlas by extracting ~52k theorems, definitions, exercises, examples, and proofs from 103 graduate mathematics textbooks, enriched with a mathematical dependency graph containing ~178k relations to enable dependency-aware evaluation.

Result: Strong baselines achieve at most 9.8% correctness on theorem statements and 16.7% on definitions, with performance degrading substantially with dependency depth (only 2.6% correctness on MA-Hard subset with deepest dependencies).

Conclusion: MathAtlas provides a high-quality but extremely challenging benchmark for large-scale autoformalization of graduate-level mathematics, revealing significant limitations of current models and highlighting the need for dependency-aware systems.

Abstract: Current autoformalization benchmarks are largely focused on olympiad or undergraduate mathematics, while graduate and research-level mathematics remains underexplored. In this paper, we introduce MathAtlas, the first large-scale autoformalization benchmark of in the wild graduate-level mathematics, containing ~52k theorems, definitions, exercises, examples, and proofs extracted from 103 graduate mathematics textbooks. MathAtlas is enriched with a mathematical dependency graph containing ~178k relations, and is the first autoformalization benchmark to include such relations, facilitating evaluation and development of dependency-aware autoformalization systems. Our extensive experiments show that MathAtlas is high quality but extremely challenging: strong baselines achieve at most 9.8% correctness on theorem statements and 16.7% on definitions. Furthermore, we find performance of state-of-the-art models degrades substantially with dependency depth: on MA-Hard, a subset of 700 entities with the deepest dependency trees, the best model achieves only 2.6% correctness for autoformalization on this challenging dataset. We release MathAtlas to the community as a benchmark set for large-scale autoformalization of graduate-level mathematics in the wild.

</details>


### [19] [Know When To Fold 'Em: Token-Efficient LLM Synthetic Data Generation via Multi-Stage In-Flight Rejection](https://arxiv.org/abs/2605.14062)
*Anjir Ahmed Chowdhury,Syed Zawad,Feng Yan*

Main category: cs.AI

TL;DR: MSIFR reduces token waste in LLM synthetic data generation by early termination of low-quality outputs using multi-stage validation, saving 11-77% tokens without compromising accuracy.


<details>
  <summary>Details</summary>
Motivation: Existing LLM synthetic data generation methods generate full outputs before filtering, leading to substantial token waste on samples that are ultimately discarded due to poor quality.

Method: Multi-Stage In-Flight Rejection (MSIFR) - a lightweight, training-free framework that decomposes generation into sequential stages and applies fast rule-based validators (arithmetic inconsistencies, hallucination patterns, formatting violations) to detect and terminate low-quality trajectories early.

Result: Across five instruction-tuned models and seven reasoning benchmarks: token consumption reduced by 11%-77% as standalone method, up to 78.2% when combined with early-exit methods, while preserving or improving evaluation accuracy.

Conclusion: MSIFR provides a practical, efficient mechanism for improving LLM-based synthetic data generation efficiency without additional training or architectural changes, with formal guarantees that early rejection doesn't bias retained sample utility.

Abstract: While synthetic data generation with large language models (LLMs) is widely used in post-training pipelines, existing approaches typically generate full outputs before applying quality filters, leading to substantial token waste on samples that are ultimately discarded. To address this, we propose Multi-Stage In-Flight Rejection (MSIFR), a lightweight, training-free framework that detects and terminates low-quality generation trajectories at intermediate checkpoints before they reach full completion. MSIFR decomposes the generation process into sequential stages and applies fast rule-based validators to identify arithmetic inconsistencies, hallucination patterns, and formatting violations, enabling early rejection of faulty samples. We formalize in-flight rejection as a sequential decision process and show that any non-trivial discard policy reduces expected token consumption, with stage-wise savings increasing when rejection occurs earlier in the generation pipeline. We further demonstrate that conditional utility estimates form a martingale, ensuring that early, in-flight rejection does not bias the expected utility of retained samples. Across five instruction-tuned models and seven reasoning benchmarks, MSIFR reduces token consumption by 11%-77% as a standalone method, and up to 78.2% when combined with early-exit methods, while preserving or improving evaluation accuracy. These results confirm that MSIFR provides a practical mechanism for improving the efficiency of LLM-based synthetic data generation without additional training or architectural changes.

</details>


### [20] [SkillFlow: Flow-Driven Recursive Skill Evolution for Agentic Orchestration](https://arxiv.org/abs/2605.14089)
*Mingda Zhang,Tiesunlong Shen,Haoran Luo,Wenjin Liu,Zikai Xiao,Erik Cambria,Xiaoying Tang*

Main category: cs.AI

TL;DR: SkillFlow is a flow-based framework for LLM-based agentic systems that addresses orchestration challenges through trainable supervision, tempered trajectory balancing for diverse strategies, transparent credit assignment, and recursive skill evolution.


<details>
  <summary>Details</summary>
Motivation: Existing LLM-based agentic systems for task orchestration face three key challenges: 1) strategy collapse under reward maximization (converging to single modes), 2) high gradient variance with opaque credit assignment (lack of transparency in decision attribution), and 3) unguided skill evolution (decisions made by prompting LLMs rather than principled training signals).

Method: SkillFlow proposes a flow-based framework with a trainable Supervisor agent operating in a structured environment with dynamic skill library and frozen executor. It uses Tempered Trajectory Balance (TTB), a regression-based flow-matching loss that samples trajectories proportional to reward while preserving diverse strategies. The framework includes a jointly learned backward policy for transparent per-step credit assignment at zero inference cost, and a recursive skill evolution mechanism that determines when to evolve, what skills to create/prune, and identifies decision gaps.

Result: Experimental results on 14 datasets show SkillFlow significantly outperforms baselines across question answering, mathematical reasoning, code generation, and real-world interactive decision making tasks.

Conclusion: SkillFlow successfully addresses key orchestration challenges through its flow-based approach, providing diverse strategy preservation, transparent credit assignment, and autonomous capability growth through recursive skill evolution, demonstrating superior performance across multiple task domains.

Abstract: In recent years, a variety of powerful LLM-based agentic systems have been applied to automate complex tasks through task orchestration. However, existing orchestration methods still face key challenges, including strategy collapse under reward maximization, high gradient variance with opaque credit assignment, and unguided skill evolution whose decisions are typically made by directly prompting an LLM to judge rather than derived from principled training signals. To address these challenges, we propose SkillFlow, a flow-based framework that takes a trainable Supervisor as the agent and a structured environment with dynamic skill library and frozen executor, automating task orchestration through multi-turn interaction. SkillFlow employs Tempered Trajectory Balance (TTB), a regression-based flow-matching loss that samples trajectories proportional to reward, preserving diverse orchestration strategies rather than collapsing to a single mode. The same flow objective yields a jointly learned backward policy that provides transparent per-step credit assignment at zero additional inference cost. Building on these flow diagnostics, a recursive skill evolution mechanism determines when to evolve, what skills to create or prune, and where decision gaps lie -- closing the loop from training signal to autonomous capability growth. Experimental results on 14 datasets show that SkillFlow significantly outperforms baselines across question answering, mathematical reasoning, code generation, and real-world interactive decision making tasks. Our code is available at https://anonymous.4open.science/r/SkillFlow-E850.

</details>


### [21] [ChromaFlow: A Negative Ablation Study of Orchestration Overhead in Tool-Augmented Agent Evaluation](https://arxiv.org/abs/2605.14102)
*Tarun Mittal*

Main category: cs.AI

TL;DR: ChromaFlow is an autonomous reasoning framework with planner-directed execution, but expanded orchestration reduced performance while increasing operational failures, highlighting the need for bounded planning and deterministic extraction.


<details>
  <summary>Details</summary>
Motivation: Autonomous language-model agents combine multiple capabilities (planning, tool use, browsing, etc.) which introduce operational failure modes not visible from final accuracy alone. There's a need to better understand and evaluate these complex agent systems beyond simple accuracy metrics.

Method: ChromaFlow framework built around planner-directed execution, specialized tool use, and telemetry-driven evaluation. Tested on GAIA 2023 Level-1 validation tasks under clean evaluation constraints with different configurations: a frozen baseline and a later recovery configuration with expanded orchestration.

Result: Baseline achieved 54.72% accuracy (29/53). Expanded orchestration configuration achieved 50.94% (27/53) while increasing operational noise (tracebacks, timeouts, tool failures, token calls, cost estimates). Randomized smoke tests (12/20 and 11/20 correct) showed unstable small diagnostic gains across samples. Key finding: more aggressive orchestration didn't improve performance and increased operational issues.

Conclusion: Bounded planner escalation, deterministic extraction, evidence reconciliation, and explicit run gates should be treated as first-order requirements for reliable autonomous agent evaluation. Expanded orchestration without proper constraints harms performance and increases failure modes.

Abstract: Autonomous language-model agents increasingly combine planning, tool use, document processing, browsing, code execution, and verification loops. These capabilities make agent systems more useful, but they also introduce operational failure modes that are not visible from final accuracy alone. This report presents ChromaFlow, a tool-augmented autonomous reasoning framework built around planner-directed execution, specialized tool use, and telemetry-driven evaluation. We analyze ChromaFlow on GAIA 2023 Level-1 validation tasks under clean evaluation constraints. A frozen full Level-1 baseline achieved 29/53 correct answers, or 54.72%. A later recovery configuration with expanded orchestration achieved 27/53 correct answers, or 50.94%, while increasing tracebacks, timeout events, tool-failure mentions, token-line calls, and campaign-log cost estimates. Two randomized 20-task smoke evaluations produced 12/20 and 11/20 correct answers, showing that small diagnostic gains can be unstable across samples. The central result is therefore a negative ablation: more aggressive orchestration did not improve full-set performance and increased operational noise. The report argues that bounded planner escalation, deterministic extraction, evidence reconciliation, and explicit run gates should be treated as first-order requirements for reliable autonomous agent evaluation.

</details>


### [22] [Modeling Bounded Rationality in Drug Shortage Pharmacists Using Attention-Guided Dynamic Decomposition](https://arxiv.org/abs/2605.14111)
*Yaniv Eliyahu Amiri,Noah Chicoine,Jacqueline Griffin,Stacy Marsella*

Main category: cs.AI

TL;DR: Hospital pharmacists use attention-guided decision making to handle drug shortages by focusing cognitive effort on most critical drugs while monitoring others, with two AI agents showing this approach maintains stable performance.


<details>
  <summary>Details</summary>
Motivation: Hospital pharmacists face high-stakes decisions under uncertainty, time pressure, and patient risk when mitigating drug shortages. Interviews revealed they use bounded rationality by focusing attention only on the most urgent drugs rather than reasoning about all drugs comprehensively.

Method: Developed a formal bounded-rational, attention-guided decision framework that dynamically decomposes drugs into two subsets: one for high-cost reasoning and another for low-cost monitoring. Created two agents: 1) Expert Agent using attention weights from pharmacist interviews, and 2) Learner Agent that adapts attention allocation through experience. Tested across simulated scenarios from short to long horizons.

Result: Attention-guided planning supports stable decision-making without complete state reasoning. Both agents demonstrated that focusing cognitive effort on a subset of critical drugs while monitoring others reduces problem complexity while maintaining performance stability across different time horizons.

Conclusion: The primary decision in high-stakes healthcare environments is not what action to take, but where to allocate cognitive effort. Attention-guided, satisficing strategies can effectively reduce problem complexity while maintaining stable performance, validating human pharmacists' intuitive bounded-rational approach.

Abstract: Hospital pharmacists make high-stakes decisions to mitigate drug shortages under uncertainty, time pressure, and patient risk. Interviews revealed that pharmacists focus attention on a small subset of drugs, limiting cognitive effort to the most urgent cases. Motivated by these findings, we formalize a bounded-rational, attention-guided decision framework that dynamically decomposes drugs into a subset for high-cost reasoning and a complementary subset for low-cost monitoring. We develop two agents: an Expert Agent that applies attention weights derived from pharmacist interviews, and a Learner Agent that adapts attention allocation over time through experience. Across simulated scenarios spanning short to long horizons, we show that attention-guided planning supports stable decision-making without complete state reasoning. These results suggest that a primary decision is not what action to take, but where to allocate cognitive effort, and that attention-guided, satisficing strategies can reduce problem complexity while maintaining stable performance.

</details>


### [23] [ClawForge: Generating Executable Interactive Benchmarks for Command-Line Agents](https://arxiv.org/abs/2605.14133)
*Yuxiang Lai,Peng Xia,Haonian Ji,Kaiwen Xiong,Kaide Zeng,Jiaqi Liu,Fang Wu,Jike Zhong,Zeyu Zheng,Cihang Xie,Huaxiu Yao*

Main category: cs.AI

TL;DR: A framework called ClawForge that benchmarks interactive AI agents on command-line workflows with state conflicts, revealing models struggle with inspecting pre-existing state and handling partial/stale artifacts.


<details>
  <summary>Details</summary>
Motivation: Existing interactive benchmarks are expensive to extend, often start from clean states, and miss failures that occur when agents interact with persistent state containing partial, stale, or conflicting artifacts. There's a need for scalable benchmarks that test agents' ability to handle state conflicts in realistic workflows.

Method: ClawForge is a generator-backed benchmark framework that compiles scenario templates, grounded slots, initialized state, reference trajectories, and validators into reproducible task specifications. It evaluates agents step-by-step over persistent workflow surfaces using normalized end state and observable side effects rather than exact trajectory matching.

Result: The ClawForge-Bench (17 scenarios, 6 ability categories) shows best model reaches only 45.3% strict accuracy. Wrong-state replacement remains below 17% for all models. The widest model separation (17% to 90%) is driven by whether agents inspect existing state before acting. Partial-credit analysis reveals many failures are near-miss closures rather than early breakdowns, with models exhibiting qualitatively different failure styles under state conflict.

Conclusion: ClawForge addresses the tension between scalable benchmark construction and realistic workflow evaluation by providing a framework for testing agents on executable command-line workflows with state conflicts. Results demonstrate significant gaps in current models' ability to handle pre-existing artifacts, highlighting the importance of state inspection and context awareness in agent capabilities.

Abstract: Interactive agent benchmarks face a tension between scalable construction and realistic workflow evaluation. Hand-authored tasks are expensive to extend and revise, while static prompt evaluation misses failures that only appear when agents operate over persistent state. Existing interactive benchmarks have advanced agent evaluation significantly, but most initialize tasks from clean state and do not systematically test how agents handle pre-existing partial, stale, or conflicting artifacts. We present \textbf{ClawForge}, a generator-backed benchmark framework for executable command-line workflows under state conflict. The framework compiles scenario templates, grounded slots, initialized state, reference trajectories, and validators into reproducible task specifications, and evaluates agents step by step over persistent workflow surfaces using normalized end state and observable side effects rather than exact trajectory matching. We instantiate this framework as the ClawForge-Bench (17 scenarios, 6 ability categories). Results across seven frontier models show that the best model reaches only 45.3% strict accuracy, wrong-state replacement remains below 17\% for all models, and the widest model separation (17% to 90%) is driven by whether agents inspect existing state before acting. Partial-credit and step-efficiency analyses further reveal that many failures are near-miss closures rather than early breakdowns, and that models exhibit qualitatively different failure styles under state conflict.

</details>


### [24] [Distribution-Aware Algorithm Design with LLM Agents](https://arxiv.org/abs/2605.14141)
*Saharsh Koganti,Priyadarsi Mishra,Pierfrancesco Beneventano,Tomer Galanti*

Main category: cs.AI

TL;DR: Learns executable solver code rather than predictors, achieving both correctness and runtime improvements by compiling reusable solver hints from task distributions.


<details>
  <summary>Details</summary>
Motivation: When learning executable solvers, correctness alone is insufficient - runtime performance matters. Two solvers can both return valid solutions but differ substantially in execution time. There's a need to learn solvers that perform well on both solution quality and runtime when deployed on new instances from an unknown task distribution.

Method: Introduces the concept of "solver hints" - reusable structure inferred from samples and compiled into specialized solver code. The approach proves that the empirically fastest sample-consistent solver from a fixed library generalizes in both correctness and runtime. Statistically identifiable hints can be recovered and compiled from polynomially many samples.

Result: Empirical evaluation with LLM code agents on 21 structured combinatorial-optimization distributions across 7 problem classes: synthesized solvers reach mean normalized quality 0.971, improve by +0.224 over average heuristic pool and +0.098 over highest-quality heuristic. Runtime improvements: 336.9× faster than quality-best heuristic, 342.8× faster than Gurobi, 16.1× faster than time-limited exact backend. On PACE 2025 Dominating Set private instances: valid on all 100 graphs, runs ~100× faster than top competition solvers with moderate quality gap.

Conclusion: Learning executable solver code with runtime considerations is feasible and effective. The solver hint framework enables compilation of distribution-specific computation that replaces exponential search or general-purpose optimization, achieving significant runtime improvements while maintaining solution quality.

Abstract: We study learning when the learned object is executable solver code rather than a predictor. In this setting, correctness is not enough: two solvers may both return valid solutions on the deployment distribution while differing substantially in runtime. Given samples from an unknown task distribution, the learner returns code evaluated on fresh instances by both solution quality and execution time. Our central abstraction is a \emph{solver hint}: reusable structure inferred from samples and compiled into specialized solver code. We prove that the empirically fastest sample-consistent solver from a fixed library generalizes in both correctness and runtime, and that statistically identifiable hints can be recovered and compiled from polynomially many samples.
  Empirically, we instantiate the framework with LLM code agents on \(21\) structured combinatorial-optimization target distributions across seven problem classes. The synthesized solvers reach mean normalized quality \(0.971\), improve by \(+0.224\) over the average heuristic pool and by \(+0.098\) over the highest-quality heuristic, and are \(336.9\times\), \(342.8\times\), and \(16.1\times\) faster than the quality-best heuristic, Gurobi, and the selected time-limited exact backend, respectively. On released PACE 2025 Dominating Set private instances, the synthesized solver is valid on all \(100\) graphs and runs about two orders of magnitude faster than top competition solvers, with a moderate quality gap. Inspection shows that many gains come from changing the computational scale: replacing ambient exponential search or general-purpose optimization with compiled distribution-specific computation.

</details>


### [25] [Agentic Systems as Boosting Weak Reasoning Models](https://arxiv.org/abs/2605.14163)
*Varun Sunkaraneni,Pierfrancesco Beneventano,Riccardo Neumarker,Tomaso Poggio,Tomer Galanti*

Main category: cs.AI

TL;DR: Weak reasoning models can match stronger models' performance via verifier-backed committee search, where critics/comparators select correct solutions from multiple proposals.


<details>
  <summary>Details</summary>
Motivation: To determine if committees of weak reasoning models can reach the performance of much stronger models through inference-time boosting, addressing the challenge that simply having more agents doesn't help if correct solutions aren't identified and selected.

Method: Proposes verifier-backed committee search with separation of proposal coverage, local identifiability, progress, and diversity. Uses critics and comparators to select correct solutions from multiple proposals without access to hidden verifier, requiring local soundness signals like execution, proof checking, or tests.

Result: On SWE-bench Verified, a single GPT-5.4 nano solves 67.0% of tasks, but with k=8 proposals and critic-comparator orchestration, reaches 76.4%, matching standalone Gemini 3 Pro and Claude Opus 4.5 Thinking, approaching 79.0% oracle best-of-8 upper bound.

Conclusion: Many correct solutions exist in weak-model proposal pools; the main challenge is selecting them. Failures are mostly proposal-coverage failures indicating shared blind spots that stronger selection alone cannot overcome.

Abstract: Can a committee of weak reasoning-model calls reach the performance of much stronger models? We study verifier-backed committee search as inference-time boosting for reasoning language models. The mechanism is not simply that ``more agents help'': samples expose latent correct solutions, while critics and comparators must recover them without access to the hidden verifier. We formalize this view by separating proposal coverage, local identifiability, progress, and diversity. We prove that coverage can be amplified by repeated sampling, but cannot by itself create useful critics or comparators; reliable amplification requires an additional local soundness signal, such as execution, proof checking, type checking, tests, or constraint solving. We give rank-based bounds showing when local selection errors compose into reliable trajectories, and characterize the proposer-side ceiling: oracle best-of-\(k\) converges only to the mass of task slices on which the proposal system assigns nonzero useful probability. Empirically, on SWE-bench Verified, a single \texttt{GPT-5.4 nano} proposal solves \(67.0\%\) of tasks. Using the same nano model, our critic--comparator orchestration reaches \(76.4\%\) with \(k=8\) proposals, matching the standalone performance of \texttt{Gemini 3 Pro} and \texttt{Claude Opus 4.5} Thinking and approaching the \(79.0\%\) oracle best-of-\(8\) upper bound. Thus, many correct patches are already present in weak-model proposal pools; the main challenge is selecting them. The remaining failures are mostly proposal-coverage failures, indicating shared blind spots that stronger selection alone cannot close.

</details>


### [26] [Unsteady Metrics and Benchmarking Cultures of AI Model Builders](https://arxiv.org/abs/2605.14164)
*Stefan Baack,Christo Buschek,Maty Bohacek*

Main category: cs.AI

TL;DR: Benchmarking-Cultures-25 dataset reveals AI model builders selectively highlight different benchmarks for narrative purposes rather than standardized evaluation, creating fragmented evaluation landscape with limited cross-model comparability.


<details>
  <summary>Details</summary>
Motivation: The shift from peer-reviewed literature to press releases/blog posts for comparing AI competencies has created a situation where model builders selectively highlight benchmarks that serve their narrative, but this practice and its implications are underexamined.

Method: Created Benchmarking-Cultures-25 dataset of 231 benchmarks across 139 model releases from 11 major AI builders in 2025, with interactive exploration tool. Developed unified taxonomy mapping diverging terminology to shared framework based on what benchmark authors claim to measure.

Result: 63.2% of highlighted benchmarks used by only one builder, 38.5% appear in just one release - limited cross-model comparability. "General knowledge application" is vaguely defined category where benchmarks often de-emphasize construct validity and frame results as AGI progress indicators, despite mostly evaluating STEM subjects (especially math).

Conclusion: Highlighted benchmarks function more as flexible narrative devices for market positioning than standardized scientific measurement tools, contributing to fragmented evaluation landscape where models aren't directly comparable due to benchmark selection strategies.

Abstract: The primary way to establish and compare competencies in foundation and generative AI models has shifted from peer-reviewed literature to press releases and company blog posts, where model builders highlight results on selected benchmarks. These artifacts now largely define the state of the art for researchers and the public. Despite their prominence, which benchmarks model builders choose to highlight, and what they communicate through this selection, is underexamined. To investigate, we introduce and open-source Benchmarking-Cultures-25, a dataset of 231 benchmarks highlighted across 139 model releases in 2025 from 11 major AI builders, alongside an interactive tool to explore the data. Our analysis reveals a fragmented evaluation landscape with limited cross-model comparability: 63.2% of highlighted benchmarks are used by a single builder, and 38.5% appear in just one release. Few achieve widespread use (e.g., GPQA Diamond, LiveCodeBench, AIME 2025). Moreover, benchmarks are attributed different competencies by different builders, depending on their narrative. To disentangle these conflicting presentations, we develop a unified taxonomy mapping diverging terminology to a shared framework of measured signals based on what benchmark authors claim to measure. "General knowledge application" is the second most popular, yet vaguely defined, category. Qualitative analysis shows many such benchmarks deemphasize construct validity, instead framing results as indicators of progress toward AGI. Their authors claim to measure knowledge or reasoning broadly, yet mostly evaluate STEM subjects (especially math). We argue that highlighted benchmarks function less as standardized measurement tools and more as flexible narrative devices prioritizing market positioning over scientific evaluation. Data: https://hf.co/datasets/matybohacek/benchmarking-cultures-25; tool: https://bench-cultures.net.

</details>


### [27] [The Evaluation Trap: Benchmark Design as Theoretical Commitment](https://arxiv.org/abs/2605.14167)
*Theodore J Kalaitzidis*

Main category: cs.AI

TL;DR: A methodology called Epistematics for auditing AI benchmarks to ensure they actually measure claimed capabilities rather than reinforcing narrow operational assumptions that create self-reinforcing evaluation traps.


<details>
  <summary>Details</summary>
Motivation: AI benchmarks often become self-reinforcing traps where evaluation criteria narrow what counts as progress, reorganizing capability concepts to match benchmark legibility rather than tracking independent capabilities. This creates structural limits that the dominant paradigm cannot detect or overcome.

Method: Epistematics methodology derives evaluation criteria directly from technical capability claims and audits whether proposed benchmarks can discriminate claimed capabilities from proxy behaviors. It provides an audit procedure, failure mode taxonomy, and benchmark-design criteria for evaluating capability-evaluation coherence.

Result: The methodology is demonstrated through a worked audit of Dupoux et al. (2026), which shows how proposals can revise theoretical assumptions at the architectural level while reproducing them in evaluation criteria, thereby entrenching constraints in forms that evaluation cannot detect.

Conclusion: A meta-evaluative framework is needed to prevent benchmarks from becoming self-reinforcing traps that narrow progress and obscure structural limits, with Epistematics providing tools to audit evaluation coherence and prevent capability-evaluation mismatch.

Abstract: Every AI benchmark operationalizes theoretical assumptions about the capability it claims to assess. When assumptions function as unexamined commitments, benchmarks stabilize the dominant paradigm by narrowing what counts as progress. Over time, narrow evaluation reorganizes capability concepts: architectures and definitions are selected for benchmark legibility until evaluation ceases to track an independent object and instead produces a version of the target defined by its own operational assumptions. The result is a trap: evaluation frameworks treat self-reinforcing assessments as valid, both creating and obscuring structural limits on what the current paradigm can accomplish. We introduce Epistematics, a methodology for deriving evaluation criteria directly from technical capability claims and auditing whether proposed benchmarks can discriminate the claimed capability from proxy behaviors. The contribution is meta-evaluative: an audit procedure, a failure mode taxonomy, and benchmark-design criteria for evaluating capability-evaluation coherence. We demonstrate the procedure through a worked audit of Dupoux et al. (2026), a proposal that revises the dominant paradigm's theoretical assumptions at the architectural level while reproducing them in its evaluation criteria, thereby entrenching the constraint it seeks to overcome in a form the evaluation cannot detect.

</details>


### [28] [Grounded Continuation: A Linear-Time Runtime Verifier for LLM Conversations](https://arxiv.org/abs/2605.14175)
*Qisong He,Yi Dong,Xiaowei Huang*

Main category: cs.AI

TL;DR: A runtime verifier that maintains dependency graphs to prevent LLMs from making claims based on abandoned premises, achieving better accuracy than LLM-only and RAG baselines on multiple benchmarks.


<details>
  <summary>Details</summary>
Motivation: LLMs can produce plausible but invalid utterances that rest on premises already abandoned in long conversations, which is exploited by context-manipulation attacks against deployed agents. There's a need to close this gap between plausible-sounding but invalid continuations.

Method: A runtime verifier that maintains an explicit dependency graph: an LLM classifies each turn into one of 8 update operations from four formalisms (dynamic epistemic logic, abductive reasoning, awareness logic, argumentation), while a symbolic engine records which claims depend on which evidence. Checking continuations reduces to graph walks, and retraction propagates through the graph to flag unsupported conclusions.

Result: On LongMemEval-KU oracle (n=78), the verifier reaches 89.7% accuracy vs. 88.5% for LLM-only baseline (+1.3pp) and 87.2% for transcript-RAG baseline (+2.6pp). On LoCoMo's 60 QA items, competitive with retrieval-augmented baselines. On a constructed 15-item stale-premise subset, 100% accuracy vs. 93.3% (+6.7pp). The verifier has linear per-turn cost with formal conflict-free guarantee and retraction checks at microsecond scale.

Conclusion: The approach effectively prevents LLMs from making claims based on abandoned premises through runtime verification with dependency graphs, outperforming baselines on multiple benchmarks. It demonstrates a soundness-faithfulness decomposition where structural checks are sound by construction, and LLM extraction faithfulness is empirically measured across four LLM families.

Abstract: In long conversations, an LLM can produce a next utterance that sounds plausible but rests on premises the conversation has already abandoned. Context-manipulation attacks against deployed agents now actively exploit this gap. We close it with a runtime verifier that maintains an explicit dependency graph: an LLM classifies each turn into one of 8 update operations drawn from four formalisms (dynamic epistemic logic, abductive reasoning, awareness logic, argumentation), and a symbolic engine records which claims depend on which evidence. Checking whether a continuation is supported reduces to a graph walk; retraction propagates through the same graph to flag exactly the conclusions that lose support, with linear per-turn cost and a formal conflict-free guarantee. On LongMemEval-KU oracle (n=78), the verifier reaches 89.7% accuracy vs. 88.5% for the LLM-only baseline (+1.3pp) and 87.2% for a transcript-RAG baseline matched on retrieval budget (+2.6pp); wins among disagreements are correct abstentions where the baseline confabulates. On LoCoMo's 60 official QA items the verifier is competitive with retrieval-augmented baselines. Beyond external benchmarks, we construct two multi-agent scenarios and a 50-item grounding test: on the 15-item stale-premise subset, the verifier reaches 100% accuracy vs. 93.3% (+6.7pp). These instantiate a soundness-faithfulness decomposition: the structural check is sound by construction, and per-deployment LLM extraction faithfulness is the empirical question we measure across four LLM families. The retraction check plateaus at microseconds while history-replay grows linearly with conversation length.

</details>


### [29] [SimPersona: Learning Discrete Buyer Personas from Raw Clickstreams for Grounded E-Commerce Agents](https://arxiv.org/abs/2605.14205)
*Zahra Zanjani Foumani,Alberto Castelo,Shuang Xie,Ted Chaiwachirasak,Han Li,Lingyun Wang*

Main category: cs.AI

TL;DR: SimPersona learns discrete buyer personas from clickstream data using VQ-VAE and integrates them as persona tokens in LLM web agents for realistic, heterogeneous population-level simulation of e-commerce behavior.


<details>
  <summary>Details</summary>
Motivation: Current LLM-based web agents collapse to a single "average buyer" policy, failing to capture real buyer heterogeneity. Existing personalization methods using hand-crafted prompts are brittle, unscalable, context-inefficient, and can't represent population-level behavior distributions.

Method: SimPersona learns discrete buyer types from historical clickstreams using a behavior-aware VQ-VAE to create a discrete buyer-type space. Each learned type is mapped to a persona token in the LLM vocabulary, and agents are fine-tuned with these tokens on real browsing traces. For simulation, buyer types are sampled from merchant-specific empirical distributions.

Result: Evaluated on 8.37M buyers across 42 held-out live storefronts, SimPersona achieves 78% conversion-rate alignment with real buyers, shows interpretable behavioral variation across buyer types, and outperforms a baseline with 8× more parameters on goal-oriented shopping tasks.

Conclusion: SimPersona provides a scalable framework for learning and simulating heterogeneous buyer behavior without brittle prompt engineering, enabling realistic population-level e-commerce simulations that preserve merchant-specific buyer distributions.

Abstract: LLM-based web agents can navigate live storefronts, yet they often collapse to a single "average buyer" policy, failing to capture the heterogeneous and distributional nature of real buyer populations. Existing personalization methods rely on hand-crafted prompt-based personas that are brittle, difficult to scale, context-inefficient, and unable to faithfully represent population-level behavior. We introduce SimPersona, a novel framework that learns discrete buyer types from historical traffic and exposes them to LLM-based web agents as compact persona tokens. Given raw clickstreams, a behavior-aware VQ-VAE induces a discrete buyer-type space that captures the statistical structure of real buyer behavior and merchant-specific buyer population distributions. To provide behavior-specific guidance to LLM-based web agents, SimPersona maps each learned buyer type to a dedicated persona token in the LLM agent vocabulary and fine-tunes the agent with these tokens on real browsing traces. At inference, each synthetic buyer is assigned to a learned buyer type with a single encoder forward pass, requiring no retraining or store-specific prompt engineering. For population-level simulation, SimPersona samples buyer types from each merchant's empirical distribution over the learned VQ-VAE codebook and instantiates agents with the corresponding persona tokens, preserving merchant-specific buyer population distributions. Evaluated on $8.37$M buyers across $42$ held-out live storefronts, SimPersona achieves $78\%$ conversion-rate alignment with real buyers, exhibits interpretable behavioral variation across buyer types, and outperforms a baseline with $8\times$ more parameters on goal-oriented shopping tasks. We further release an open-source data pipeline that converts raw e-commerce event logs into buyer representations and agent-training traces.

</details>


### [30] [ASH: Agents that Self-Hone via Embodied Learning](https://arxiv.org/abs/2605.14211)
*Benjamin Schneider,Xavier Schneider,Victor Zhong,Sun Sun*

Main category: cs.AI

TL;DR: ASH: A self-improving agent that learns embodied policies from noisy internet video without rewards or expert annotations, enabling long-horizon task completion in complex games.


<details>
  <summary>Details</summary>
Motivation: Current methods for long-horizon embodied tasks rely on hand-engineered rewards or action-labeled demonstrations, which don't scale well. There's a need for more scalable approaches that can learn from abundant but unlabeled internet video data.

Method: ASH uses a self-improvement loop: when stuck, it learns an Inverse Dynamics Model (IDM) from its own trajectories, then uses the IDM to extract supervision from relevant internet video. It employs unsupervised learning to identify key moments from internet video and retains them as long-term memory for tackling long-horizon problems.

Result: ASH achieved 11.2/12 milestones in Pokemon Emerald and 9.9/12 in Legend of Zelda over 8-hour evaluations, significantly outperforming baselines (behavioral cloning, retrieval-augmented, and zero-shot foundation models) which plateaued at 6.5/12 and 6.0/12 respectively.

Conclusion: Self-improving agents that learn from noisy internet video without expert supervision represent a scalable solution for long-horizon embodied learning tasks.

Abstract: Long-horizon embodied tasks remain a fundamental challenge in AI, as current methods rely on hand-engineered rewards or action-labeled demonstrations, neither of which scales. We introduce ASH, an agentic system that learns an embodied policy from unlabeled, noisy internet video, without reward shaping or expert annotation. ASH follows a self-improvement loop; when it gets stuck, ASH learns an Inverse Dynamics Model (IDM) from its own trajectories, and uses its IDM to extract supervision from relevant internet video. ASH uses unsupervised learning to identify key moments from large-scale internet video and retains them as long-term memory -- allowing it to tackle long-horizon problems. We evaluate ASH on two complementary environments demanding multi-hour planning: Pokemon Emerald, a turn-based RPG, and The Legend of Zelda: The Minish Cap, a real-time action-adventure game. In both games, behavioral cloning, retrieval-augmented and zero-shot foundation-model baselines plateau, while ASH sustains progression across our 8-hour evaluation. ASH reaches an average of $11.2/12$ milestones in Pokemon Emerald and $9.9/12$ in Legend of Zelda, while the strongest baseline gets stuck in both environments at an average of $6.5/12$ and $6.0/12$ milestones, respectively. We demonstrate that self-improving agents are a scalable recipe for long-horizon embodied learning.

</details>


### [31] [MetaAgent-X : Breaking the Ceiling of Automatic Multi-Agent Systems via End-to-End Reinforcement Learning](https://arxiv.org/abs/2605.14212)
*Yaolun Zhang,Yujie Zhao,Nan Wang,Yiran Wu,Jiayu Chang,Yizhao Chen,Qingyun Wu,Jishen Zhao,Huazheng Wang*

Main category: cs.AI

TL;DR: MetaAgent-X introduces an end-to-end RL framework that jointly optimizes both automatic multi-agent system design and execution, overcoming the frozen-executor ceiling in existing approaches through hierarchical rollout and stagewise co-evolution training.


<details>
  <summary>Details</summary>
Motivation: Existing automatic multi-agent systems have a "frozen-executor ceiling" - they either perform training-free test-time search or optimize meta-level designers while keeping downstream execution agents frozen, leaving end-to-end training of self-designing and self-executing agentic models unexplored.

Method: MetaAgent-X uses an end-to-end reinforcement learning framework that enables script-based MAS generation, execution rollout collection, and credit assignment for both designer and executor trajectories. It introduces Executor Designer Hierarchical Rollout and Stagewise Co-evolution to improve training stability and expose designer-executor co-evolution dynamics.

Result: MetaAgent-X consistently outperforms existing automatic MAS baselines, achieving up to 21.7% performance gains. Ablation studies show both designer and executor improve throughout training, and effective automatic MAS learning follows a stagewise co-evolution process.

Conclusion: The results establish end-to-end trainable automatic MAS as a practical paradigm for building self-designing and self-executing agentic models, successfully overcoming the frozen-executor ceiling through joint optimization of design and execution.

Abstract: Automatic multi-agent systems aim to instantiate agent workflows without relying on manually designed or fixed orchestration. However, existing automatic MAS approaches remain only partially adaptive: they either perform training-free test-time search or optimize the meta-level designer while keeping downstream execution agents frozen, which creating a frozen-executor ceiling and leaving the end-to-end training of self-designing and self-executing agentic models unexplored. To address this, we introduce MetaAgent-X, an end-to-end reinforcement learning framework that jointly optimizes automatic MAS design and execution. MetaAgent-X enables script-based MAS generation, execution rollout collection, and credit assignment for both designer and executor trajectories. To support stable and scalable optimization, we propose Executor Designer Hierarchical Rollout and Stagewise Co-evolution to improve training stability and expose the dynamics of designer-executor co-evolution. MetaAgent-X consistently outperforms existing automatic MAS baselines, achieving up to 21.7% gains. Comprehensive ablations show that both designer and executor improve throughout training, and that effective automatic MAS learning follows a stagewise co-evolution process. These results establish end-to-end trainable automatic MAS as a practical paradigm for building self-designing and self-executing agentic models.

</details>


### [32] [GenCircuit-RL: Reinforcement Learning from Hierarchical Verification for Genetic Circuit Design](https://arxiv.org/abs/2605.14215)
*Noah Flynn*

Main category: cs.AI

TL;DR: GenCircuit-RL is a reinforcement learning framework that uses hierarchical verification rewards and curriculum learning to automate genetic circuit design through code generation in SBOL format, achieving significant improvements in functional reasoning and generalization to novel biological parts.


<details>
  <summary>Details</summary>
Motivation: Genetic circuit design remains a manual, expert-driven process despite synthetic biology advancements. Current methods lack automated, reliable generation of formally verifiable genetic circuits, limiting scalability and accessibility in synthetic biology.

Method: Proposes GenCircuit-RL, a reinforcement learning framework with: 1) hierarchical verification rewards decomposing correctness into 5 levels (code execution to topological checks), 2) four-stage curriculum learning shifting optimization from code generation to functional reasoning, 3) models generating Python code in pysbol3 for SBOL representation, and 4) evaluation on SynBio-Reason benchmark of 4,753 circuits across six types and nine tasks.

Result: Hierarchical verification improves task success by 14-16 percentage points over binary rewards. Curriculum learning is essential for strong design performance. Models generate topologically correct circuits, generalize to novel biological parts, and rediscover canonical designs from synthetic biology literature.

Conclusion: GenCircuit-RL provides an effective framework for automating genetic circuit design through reinforcement learning with hierarchical verification and curriculum learning, demonstrating significant improvements in functional reasoning, generalization to novel parts, and ability to rediscover canonical designs.

Abstract: Genetic circuit design remains a laborious, expert-driven process despite decades of progress in synthetic biology. We study this problem through code generation: models produce Python code in pysbol3 to construct genetic circuits in the Synthetic Biology Open Language (SBOL), a formal representation that supports automated verification. We introduce GenCircuit-RL, a reinforcement learning framework built around hierarchical verification rewards that decompose correctness into five levels, from code execution to task-specific topological checks, and a four-stage curriculum that shifts optimization pressure from code generation to functional reasoning. We also introduce SynBio-Reason, a benchmark of 4,753 circuits spanning six canonical circuit types and nine tasks from code repair to de novo design, with held-out biological parts for out-of-distribution evaluation. Hierarchical verification improves task success on functional reasoning tasks by 14 to 16 percentage points over binary rewards, and curriculum learning is required for strong design performance. The resulting models generate topologically correct circuits, generalize to novel biological parts, and rediscover canonical designs from the synthetic biology literature.

</details>


### [33] [Fusion-fission forecasts when AI will shift to undesirable behavior](https://arxiv.org/abs/2605.14218)
*Neil F. Johnson,Frank Yingjie Huo*

Main category: cs.AI

TL;DR: A physics-inspired vector generalization of group dynamics can predict and forecast when ChatGPT-like AI systems will shift from desirable to undesirable behavior, providing a universal warning signal across different AI architectures.


<details>
  <summary>Details</summary>
Motivation: Current AI models experience unpredictable behavioral shifts from desirable to undesirable outputs (encouraging self-harm, extremism, financial losses, medical/military mistakes) that persist despite advances in AI modeling and safety measures, with no current ability to predict when these shifts will occur.

Method: The paper applies a vector generalization of fusion-fission group dynamics observed in living and active-matter systems. The method involves group-level competition between conversation history (C), desirable behavior (B), and undesirable behavior (D) basin dynamics, which can be estimated in advance. This approach is neither model-specific nor driven by stochastic sampling.

Result: The method achieved 90% accuracy across 7 AI models spanning 124M to 12B parameters, demonstrated production-scale persistence across 10 frontier chatbots, and made a priori predictions validated by Stanford's "Delusional Spirals" corpus of 207,443 human-AI exchanges (predicted 11 months before the corpus appeared).

Conclusion: The physics-derived formula provides a real-time warning signal for behavioral shifts that current alignment techniques don't supply, is portable across current and future ChatGPT-like architectures, and can be instantiated in applications where competing response classes can be defined, operating below the current safety stack architecture.

Abstract: The key problem facing ChatGPT-like AI's use across society is that its behavior can shift, unnoticed, from desirable to undesirable -- encouraging self-harm, extremist acts, financial losses, or costly medical and military mistakes -- and no one can yet predict when. Shifts persist in even the newest AI models despite remarkable progress in AI modeling, post-training alignment and safeguards. Here we show that a vector generalization of fusion-fission group dynamics observed in living and active-matter systems drives -- and can forecast -- future shifts in the AI's behavior. The shift condition, which is also derivable mathematically, results from group-level competition between the conversation-so-far (C) and the desirable (B) and undesirable (D) basin dynamics which can be estimated in advance for a given application. It is neither model-specific nor driven by stochastic sampling. We validate it across six independent tests, including: 90 percent correct across seven AI models spanning two orders of magnitude in parameter count (124M-12B); production-scale persistence across ten frontier chatbots; and a priori time-stamped prediction eleven months before the Stanford 'Delusional Spirals' corpus appeared, and independently confirmed by that corpus of 207,443 human-AI exchanges. Because it sits architecturally below the current safety stack, the same formula provides a real-time warning signal that current alignment does not supply, portable across current and future ChatGPT-like AI architectures and instantiable in application domains where competing response classes can be defined.

</details>


### [34] [Good to Go: The LOOP Skill Engine That Hits 99% Success and Slashes Token Usage by 99% via One-Shot Recording and Deterministic Replay](https://arxiv.org/abs/2605.14237)
*Xiaohua Wang,Kai Yu,XuXiao Liang,Liang Wang,Chao Han*

Main category: cs.AI

TL;DR: LOOP SKILL ENGINE reduces AI agent token costs by 99% and achieves 99% success rate through one-shot recording and deterministic replay of periodic tasks.


<details>
  <summary>Details</summary>
Motivation: AI agents using LLMs for repetitive periodic tasks face high token costs and unpredictable failures due to LLM stochasticity, making deployments expensive and unreliable.

Method: One-shot recording paradigm where agent's first execution is fully logged, then converted to parameterized, branch-free Loop Skill templates using greedy length-descending extraction algorithm; subsequent runs bypass LLM via deterministic replay.

Result: Achieved 93.3%-99.98% token reduction, 8.7x latency improvement, 99% success rate across periodic tasks (5 min to 24 hr intervals), eliminated output non-determinism with multi-layer degradation for stall prevention.

Conclusion: The LOOP SKILL ENGINE enables cost-effective, reliable periodic agent deployments through deterministic replay of recorded skills, balancing LLM flexibility with execution efficiency and reliability.

Abstract: Deploying AI agents for repetitive periodic tasks exposes a critical tension: Large Language Models (LLMs) offer unmatched flexibility in tool orchestration, yet their inherent stochasticity causes unpredictable failures, and repeated invocations incur prohibitive token costs. We present the LOOP SKILL ENGINE, a system that achieves a combined 99% success rate and 99% token reduction for periodic agent tasks through a one-shot recording, deterministic replay paradigm. On its first run, the agent executes the task with full LLM reasoning while the system transparently intercepts and records the complete tool-call trajectory. A greedy length-descending template extraction algorithm then converts this recording into a parameterized, branch-free Loop Skill -- a deterministic execution plan that captures the task's functional intent while parameterizing time-dependent and result-dependent variables. All subsequent executions bypass the LLM entirely: the engine resolves template variables against real-time values and replays the tool sequence deterministically. We prove two theorems: (1) Replay Determinism -- the step sequence of a validated Loop Skill is invariant across all future executions; (2) Write Safety -- concurrent access to persistent configuration is serialized through reentrant locks and atomic file replacement. Across a benchmark of periodic agent tasks spanning intervals from 5 minutes to 24 hours, the Loop Skill Engine reduces monthly token consumption by 93.3%--99.98% and cuts execution latency by 8.7x while eliminating output non-determinism. A multi-layer degradation strategy guarantees that tasks never stall. We release the engine as part of the buddyMe open-source agent framework.

</details>


### [35] [Hypergraph Enterprise Agentic Reasoner over Heterogeneous Business Systems](https://arxiv.org/abs/2605.14259)
*Ling Wang,Songnan Liu,Jianan Wang,Cheng Cheng,Xin Liu,Yihan Zhu,Enyu Li,Yu Xiao,Jiangyong Xie,Duogong Yan,Jiangyi Chen*

Main category: cs.AI

TL;DR: HEAR is an enterprise agentic reasoner using Stratified Hypergraph Ontology to solve LLM limitations in complex business environments, achieving 94.7% accuracy on supply-chain tasks while minimizing token costs.


<details>
  <summary>Details</summary>
Motivation: Current Large Language Models struggle with hallucinations and fail at multi-hop, n-ary reasoning in heterogeneous enterprise systems. Existing solutions like GraphRAG and NL2SQL lack semantic grounding and auditable execution needed for complex business environments.

Method: HEAR uses a Stratified Hypergraph Ontology with two layers: Graph Layer virtualizes provenance-aware data interfaces, and Hyperedge Layer encodes n-ary business rules and procedural protocols. It employs an evidence-driven reasoning loop to dynamically orchestrate ontology tools for structured multi-hop analysis without LLM retraining.

Result: HEAR achieves up to 94.7% accuracy on supply-chain tasks, including order fulfillment blockage root cause analysis. It demonstrates adaptive efficiency by minimizing token costs using procedural hyperedges while ensuring rigorous correctness through topological exploration.

Conclusion: HEAR establishes a scalable, auditable foundation for enterprise intelligence by matching proprietary model performance with open-weight backbones and automating manual diagnostics, providing a practical solution to LLM limitations in complex business environments.

Abstract: Applying Large Language Models (LLMs) to heterogeneous enterprise systems is hindered by hallucinations and failures in multi-hop, n-ary reasoning. Existing paradigms (e.g., GraphRAG, NL2SQL) lack the semantic grounding and auditable execution required for these complex environments. We introduce HEAR, an enterprise agentic reasoner built on a Stratified Hypergraph Ontology. Its base Graph Layer virtualizes provenance-aware data interfaces, while the Hyperedge Layer encodes n-ary business rules and procedural protocols. Operating an evidence-driven reasoning loop, HEAR dynamically orchestrates ontology tools for structured multi-hop analysis without requiring LLM retraining. Evaluations on supply-chain tasks, including order fulfillment blockage root cause analysis (RCA), show HEAR achieves up to 94.7% accuracy. Crucially, HEAR demonstrates adaptive efficiency: utilizing procedural hyperedges to minimize token costs, while leveraging topological exploration for rigorous correctness on complex queries. By matching proprietary model performance with open-weight backbones and automating manual diagnostics, HEAR establishes a scalable, auditable foundation for enterprise intelligence.

</details>


### [36] [Heuristic Pathologies and Further Variance Reduction via Uncertainty Propagation in the AIVAT Family of Techniques](https://arxiv.org/abs/2605.14261)
*Juho Kim,Tuomas Sandholm*

Main category: cs.AI

TL;DR: AIVAT variance reduction estimators for multiagent evaluation are vulnerable to heuristic manipulation; fixing heuristics before data observation is essential, and propagating heuristic uncertainty can improve statistical efficiency.


<details>
  <summary>Details</summary>
Motivation: Evaluating agent performance in multiagent environments with limited samples or high trial costs is challenging. AIVAT provides unbiased low-variance estimators, but lacks guidelines for choosing heuristic value functions or handling their uncertainty, creating potential vulnerabilities.

Method: Parameterized heuristic value functions to demonstrate AIVAT vulnerabilities via gradient descent on sample variance and test statistics. Proposed propagating heuristic uncertainty to quantify AIVAT estimate uncertainty, with potential variance reduction through inverse-variance weighted averaging (sacrificing unbiasedness). Experiments used 10,000 poker hands dataset.

Result: Identified two vulnerabilities: 1) pathological sample variance reduction via gradient descent, 2) p-hacking via test statistic manipulation. Key insight: heuristics must be fixed before data observation. Uncertainty propagation allowed 43.0% reduction in samples needed for statistical conclusions.

Conclusion: AIVAT heuristics should be predetermined to prevent manipulation, while propagating their uncertainty can significantly improve statistical efficiency. Inverse-variance weighting offers further variance reduction but may sacrifice unbiasedness, requiring careful trade-offs in practical applications.

Abstract: How should an agent's performance in a multiagent environment be evaluated when there is a limited sample size or a high cost of running a trial? The AIVAT family of variance reduction techniques was proposed to address this challenge by introducing unbiased low-variance estimators of agents' expected payoffs. An important component of AIVAT is a heuristic value function that discriminates between potentially low- and high-value counterfactual histories. A notable gap in the literature is that there is little to no constraint or guideline on how the heuristic value function should be chosen or how uncertainty in its output should be handled.
  In our first contribution, we parameterize the heuristic value function to highlight AIVAT's potential vulnerabilities: a) the sample variance can be set pathologically low by directly applying gradient descent on the sample variance, and b) one can p-hack to draw a desired statistical conclusion via gradient descent/ascent on the test statistic. The main takeaway is that the heuristic value function should be fixed prior to observing the evaluation data! In our second contribution, we show how the heuristic uncertainty can be propagated to quantify the uncertainty of AIVAT estimates. It is then possible to further reduce the variance using inverse-variance weighted averaging, but AIVAT's unbiasedness guarantee may have to be sacrificed. In our experiments, we use a dataset of 10,000 poker hands to demonstrate our heuristic pathology and uncertainty results, with the latter yielding a 43.0% reduction in the number of samples (poker hands) needed to draw statistical conclusions.

</details>


### [37] [Agentic AI Ecosystems in Higher Education: A Perspective on AI Agents to Emerging Inclusive, Agentic Multi-Agent AI Framework for Learning, Teaching and Institutional Intelligence](https://arxiv.org/abs/2605.14266)
*Vidya K Sudarshan,Anushka Sisodia,Reshma A Ramachandra,Sia Batra,Josephine Chong Leng Leng*

Main category: cs.AI

TL;DR: The paper proposes an integrated agentic multi-agent AI platform for higher education to address fragmentation in current AI implementations and support coordinated, adaptive decision-making across teaching, learning, and administrative functions with emphasis on inclusivity.


<details>
  <summary>Details</summary>
Motivation: Current AI implementations in higher education are fragmented and task-specific, lacking holistic integration across educational functions. There's a significant gap in eco-system-level agentic platforms capable of coordinated planning, reasoning, and adaptive decision-making that can support diverse learners inclusively.

Method: The paper employs a forward-looking perspective approach grounded in thematic analysis of existing literature, identifying four dominant themes: fragmented AI tools, single-agent to multi-agent transition, limited cross-functional integration, and insufficient focus on inclusivity/accessibility.

Result: The analysis reveals a clear gap between current AI implementations and the needs of holistic, learner-centered educational ecosystems. The paper identifies challenges and outlines future research directions for scalable, human-aligned, inclusive agentic AI platforms.

Conclusion: The paper concludes that agentic multi-agent AI platforms represent the next generation of intelligent systems in tertiary education, capable of supporting coordinated operations across educational functions and fostering inclusive learning for diverse students through adaptive multimodal interventions.

Abstract: Integration of artificial intelligent (AI) agents in higher education is transforming teaching, learning and administrative processes. Although existing AI agents effectively support individual tasks, their implementation remains fragmented and inefficient for handling the complexity of educational institutions. This highlights a significant research gap: the lack of integrated eco-system-level agentic multi-agent AI platform capable of coordinated planning, reasoning, and adaptive decision-making across multiple educational functions. This paper presents a forward-looking perspective on agentic multi-agent AI platform in higher education, consisting interconnected autonomous, goal driven agents that support learning, teaching, and institutional operations. It addresses timely and critical questions: Can agentic AI represent the next generation of intelligent systems in tertiary education? Can they collectively support seamless coordinated operations across teaching, learning and administrative support? To what extent can such systems foster inclusive and equitable learning for diverse learners with special educational needs? To ground this perspective, a thematic analysis of existing literature identifies four dominant themes: task-specific fragmented AI tools, the transition from single-agent to multi-agent systems, limited cross-functional integration, and insufficient focus on inclusivity and accessibility. Findings reveal a clear gap between current AI implementations and the needs of holistic, learner-centered educational ecosystem. The paper synthesizes challenges and outlines future research directions for scalable human-aligned, and inclusive agentic AI platform. The significant contribution is the incorporation of inclusive learning perspectives, highlighting how coordinated agentic multi-agent platform can support diverse learners through adaptive, multimodal interventions.

</details>


### [38] [Parallelizing Counterfactual Regret Minimization](https://arxiv.org/abs/2605.14277)
*Juho Kim,Tuomas Sandholm*

Main category: cs.AI

TL;DR: The paper presents a novel parallelization framework for Counterfactual Regret Minimization (CFR) algorithms by reframing them as linear algebra operations, enabling GPU acceleration that achieves up to 4 orders of magnitude speedup over existing CPU implementations.


<details>
  <summary>Details</summary>
Motivation: While parallelization has been transformative in AI for training large models, its application to computational game solving remains underexplored despite significant potential. CFR algorithms have been central to breakthroughs in solving large imperfect-information games, but lack efficient parallel implementations.

Method: The authors develop a generalized parallelization framework that reframes CFR as a series of linear algebra operations. This approach allows leveraging existing techniques for parallelizing linear algebra operations to accelerate CFR. The framework is also extended to other tabular CFR family algorithms including CFR+, discounted CFR, and predictive CFR variants.

Result: Experimental results demonstrate that the GPU-based CFR implementation achieves up to four orders of magnitude (10,000x) speedup compared to Google DeepMind OpenSpiel's CPU-based CFR implementations.

Conclusion: The paper successfully bridges the gap between AI parallelization techniques and computational game solving, demonstrating that CFR algorithms can be effectively parallelized by treating them as linear algebra operations, leading to dramatic performance improvements that enable solving larger and more complex imperfect-information games.

Abstract: Parallelization has played an instrumental role in the field of artificial intelligence (AI), drastically reducing the time taken to train and evaluate large AI models. In contrast to its impact in the broader field of AI, applying parallelization to computational game solving is relatively unexplored, despite its great potential. In this paper, we parallelize the family of counterfactual regret minimization (CFR) algorithms, which were central to important breakthroughs for solving large imperfect-information games. We present a generalized parallelization framework, reframing CFR as a series of linear algebra operations. Then, existing techniques for parallelizing linear algebra operations can be applied to accelerate CFR. We also describe how our technique can be applied to other tabular members of the CFR family of algorithms, including the state-of-the-art, such as CFR+, discounted CFR, and predictive variants of CFR. Experimentally, we show that our CFR implementation on a GPU is up to four orders of magnitude faster than Google DeepMind OpenSpiel's CFR implementations on a CPU.

</details>


### [39] [Precise Verification of Transformers through ReLU-Catalyzed Abstraction Refinement](https://arxiv.org/abs/2605.14294)
*Hengjie Liu,Zhenya Zhang,Jianjun Zhao*

Main category: cs.AI

TL;DR: A new transformer verification approach using novel ReLU-based representation for dot products achieves improved precision while maintaining acceptable efficiency.


<details>
  <summary>Details</summary>
Motivation: Transformers are widely deployed in safety-critical applications but their verification is extremely difficult due to complex computations like dot products in self-attention layers. Existing over-approximation methods sacrifice precision, leading to significant approximation errors and frequent false alarms.

Method: Proposes a novel ReLU-based representation for precise but non-linear bounds of dot products, then leverages convex relaxation of ReLU to derive precise bounds. Extends two classic approaches (rule-based and optimization-based) to transformers, creating two new frameworks for efficient and precise verification.

Result: Evaluated on different transformer architectures and robustness properties from sentiment analysis datasets. Achieved significant precision improvement for most verification tasks compared to state-of-the-art baseline, with acceptable compromise in efficiency.

Conclusion: The proposed approach effectively improves transformer verification precision by novel ReLU-based representation of dot products, demonstrating practical value for safety-critical applications while maintaining reasonable efficiency.

Abstract: Formal verification of transformers has become increasingly important due to their widespread deployment in safety-critical applications. Compared to classic neural networks, the inferences of transformers involve highly complex computations, such as dot products in self-attention layers, rendering their verification extremely difficult. Existing approaches explored over-approximation methods by constructing convex constraints to bound the output ranges of transformers, which can achieve high efficiency. However, they may sacrifice verification precision, and consequently introduce significant approximation error that leads to frequent occurrences of false alarms. In this paper, we propose a transformer verification approach that can achieve improved precision. At the core of our approach is a novel usage of ReLU, by which we represent a precise but non-linear bound for dot products such that we can further exploit the rich body of literature for convex relaxation of ReLU to derive precise bounds. We extend two classic approaches to the context of transformers, a rule-based one and an optimization-based one, resulting in two new frameworks for efficient and precise verification. We evaluate our approaches on different model architectures and robustness properties derived from two datasets about sentiment analysis, and compare with the state-of-the-art baseline approach. Compared to the baseline, our approach can achieve significant precision improvement for most of the verification tasks with acceptable compromise of efficiency, which demonstrates the effectiveness of our approach.

</details>


### [40] [Semantic Feature Segmentation for Interpretable Predictive Maintenance in Complex Systems](https://arxiv.org/abs/2605.14318)
*Emilio Mastriani,Alessandro Costa,Federico Incardona,Kevin Munari,Sebastiano Spinello*

Main category: cs.AI

TL;DR: Semantic feature segmentation decomposes monitoring variables into canonical (predictive) and residual components, improving interpretability while maintaining competitive predictive performance for fault anticipation.


<details>
  <summary>Details</summary>
Motivation: Predictive maintenance is complicated by heterogeneous, redundant monitoring variables that obscure fault-relevant information and reduce model interpretability. There's a need to extract meaningful information from complex feature spaces while preserving operational understanding.

Method: Proposes a semantic feature segmentation framework that decomposes monitored features into canonical components (retaining dominant predictive information) and residual components (structurally peripheral signals). Uses domain-informed criteria to group variables into functional groups like throughput, latency, pressure, network activity, and structural state. Evaluates decomposition using predictive risk as proxy for task-relevant information with time-aware cross-validation.

Result: Canonical space consistently achieves lower predictive risk than residual space across multiple temporal configurations. Canonical segments show stronger intra-segment coherence than inter-segment dependence. Comparable predictive performance to full feature space and PCA representation, while preserving semantic meaning of original variables.

Conclusion: Semantic feature segmentation provides interpretable, information-preserving decomposition of monitoring signals, enabling competitive predictive performance without sacrificing operational interpretability required in predictive maintenance applications.

Abstract: Predictive maintenance in complex systems is often complicated by the heterogeneity and redundancy of monitored variables,which can obscure fault-relevant information and reduce model interpretability. This work proposes a semantic feature segmentation framework that decomposes the monitored feature space into a canonical component,expected to retain the dominant predictive information, and a residual component containing structurally peripheral signals. The segmentation is defined through domain informed criteria and sets up monitoring variables into functional groups reflecting operational mechanisms such as throughput,latency,pressure,network activity,and structural state. To evaluate the effectiveness of this decomposition, we adopt a predictive perspective in which expected predictive risk is used as an operational proxy for task-relevant information. Experimental results obtained through time-aware cross-validation show that the canonical space consistently achieves lower predictive risk than the residual space across multiple temporal configurations, indicating that the semantic segmentation concentrates the most relevant information for fault anticipation. In addition, the canonical segments exhibit significantly stronger intra-segment coherence than inter-segment dependence, and this structural organization remains stable after redundancy reduction. When compared with the full feature space and with a Principal Component Analysis (PCA) representation, the canonical space carries out comparable predictive performance and furthermore preserves the semantic meaning of the original variables. These findings suggest that semantic feature segmentation provides an interpretable and information-preserving decomposition of monitoring signals, enabling competitive predictive performance without sacrificing the operational interpretability required in predictive maintenance applications.

</details>


### [41] [Are Agents Ready to Teach? A Multi-Stage Benchmark for Real-World Teaching Workflows](https://arxiv.org/abs/2605.14322)
*Zixin Chen,Peng Liu,Rui Sheng,Haobo Li,Jianhong Tu,Xiaodong Deng,Kashun Shum,Dayiheng Liu,Huamin Qu*

Main category: cs.AI

TL;DR: EduAgentBench is a new benchmark for evaluating tutor agents across three teaching capabilities: pedagogical judgment, multi-turn tutoring, and Canvas-style workflow completion, showing current models fall short of professional standards.


<details>
  <summary>Details</summary>
Motivation: Current benchmarks don't adequately measure tutoring capabilities in language agents, which require complex professional workflows like diagnosing learner states, adapting support over time, making evidence-based pedagogical decisions, and working within learning management systems.

Method: They created EduAgentBench with 150 quality-controlled tasks across three capability surfaces: professional pedagogical judgment, situated multi-turn tutoring, and Canvas-style teaching workflow completion. Tasks were constructed through a pedagogical-insight-driven pipeline with complementary verification signals and human review.

Result: Evaluation of frontier models shows they are generally capable of bounded pedagogical judgment but still fall short of professional teaching standards in situated tutoring and autonomous teaching-workflow execution.

Conclusion: EduAgentBench is the first theory-grounded and realistic benchmark for evaluating holistic teaching capabilities of tutor agents, providing a measurement foundation for developing future agents that can support realistic teaching work.

Abstract: Language agents are increasingly deployed in complex professional workflows, with tutoring emerging as a particularly high-stakes capability that remains largely unmeasured in existing benchmarks. Effective tutor agents require more than producing correct answers or executing accurate tool calls: a robust tutor must diagnose learner state, adapt support over time, make pedagogically justified decisions grounded in educational evidence, and execute interventions within realistic learning-management systems. We introduce EduAgentBench, a source-grounded benchmark for holistically evaluating tutor agents across the full scope of teaching work. It contains 150 quality-controlled tasks across three capability surfaces: professional pedagogical judgment, situated multi-turn tutoring, and Canvas-style teaching workflow completion. Tasks are constructed through a pedagogical-insight-driven pipeline and evaluated with complementary verification signals and human review. Across a comprehensive evaluation of frontier models, our findings reveal that current models are generally capable of bounded pedagogical judgment, but still fall short of professional teaching standards in situated tutoring and autonomous teaching-workflow execution. To our knowledge, EduAgentBench is the first theory-grounded and realistic benchmark for evaluating the holistic teaching capability of tutor agents, providing a measurement foundation for developing future tutor agents that can support realistic teaching work.

</details>


### [42] [CrystalReasoner: Reasoning and RL for Property-Conditioned Crystal Structure Generation](https://arxiv.org/abs/2605.14344)
*Yuyang Wu,Stefano Falletta,Delia McGrath,Sherry Yang*

Main category: cs.AI

TL;DR: CrystalReasoner is an LLM framework that generates valid crystal structures from natural language instructions using physical priors as thinking tokens and reinforcement learning for alignment with physical properties.


<details>
  <summary>Details</summary>
Motivation: Existing generative models for crystal structures struggle with atomic precision (LLM-based) or lack scientific knowledge integration (diffusion-based), often producing invalid, unstable, or property-deficient structures.

Method: Proposes CrystalReasoner with physical priors as thinking tokens (crystallographic symmetry, local coordination, predicted properties) before coordinate generation, plus RL with multi-objective dense rewards for alignment with physical validity, chemical consistency, and thermodynamic stability. Task-specific reward functions for discrete/continuous properties.

Result: Outperforms prior works and baselines on diverse metrics, triples S.U.N. ratio, achieves better property-conditioned generation, and shows adaptive reasoning (increasing reasoning length with atom count).

Conclusion: Demonstrates the potential of combining thinking traces and reinforcement learning for generating valid, stable, and property-conditioned crystal structures from natural language instructions.

Abstract: Generative modeling has emerged as a promising approach for crystal structure discovery. However, existing LLM-based generative models struggle with low-level atomic precision, while diffusion-based methods fall short in integrating high-level scientific knowledge. As a result, generated structures are often invalid, unstable, or do not possess desirable properties. To address this gap, we propose CrystalReasoner (\method), an end-to-end LLM framework that generates crystal structures from natural language instructions through reasoning and alignment. \method introduces physical priors as thinking tokens, which include crystallographic symmetry, local coordination environments and predicted physical properties before generating atomic coordinates. This bridges the gap between natural language and 3D structures. \method then employs reinforcement learning (RL) with a multi-objective, dense reward function to align generation with physical validity, chemical consistency, and thermodynamic stability. For property-conditioned tasks, we design task-specific reward functions and train specialized models for discrete constraints (e.g., space group) and continuous properties (e.g., elasticity, thermal expansion). Empirical results demonstrate that compared to prior works and baselines without thinking traces or RL, \method obtains better performance on diverse metrics, triples S.U.N. ratio, and achieves better performance for property conditioned generation. \method also exhibits adaptive reasoning, increasing reasoning lengths as the number of atoms increases. Our work demonstrates the potential of leveraging thinking traces and RL for generating valid, stable, and property-conditioned crystal structures. Please see our work at https://crystalreasoner.github.io/ .

</details>


### [43] [Herculean: An Agentic Benchmark for Financial Intelligence](https://arxiv.org/abs/2605.14355)
*Xueqing Peng,Zhuohan Xie,Yupeng Cao,Haohang Li,Lingfei Qian,Yan Wang,Vincent Jim Zhang,Huan He,Xuguang Ai,Linhai Ma,Ruoyu Xiang,Yueru He,Yi Han,Shuyao Wang,Yuqing Guo,Mingyang Jiang,Yilun Zhao,Youzhong Dong,Xiaoyu Wang,Yankai Chen,Ye Yuan,Qiyuan Zhang,Fuyuan Lyu,Haolun Wu,Yonghan Yang,Zichen Zhao,Yuyang Dai,Fan Zhang,Rania Elbadry,Ayesha Gull,Muhammad Usman Safder,Nuo Chen,Fengbin Zhu,Tianshi Cai,Zimu Wang,Polydoros Giannouris,Yuechen Jiang,Zhiwei Liu,Mohsinul Kabir,Yuyan Wang,Yixiang Zheng,Yangyang Yu,Weijin Liu,Wenbo Cao,Anke Xu,Peng Lu,Jerry Huang,Fengran Mo,Mingquan Lin,Prayag Tiwari,Yijia Zhao,Victor Gutierrez Basulto,Xiao-Yang Liu,Kaleb E Smith,Jiahuan Pei,Arman Cohan,Jimin Huang,Yuehua Tang,Alejandro Lopez-Lira,Xi Chen,Xue Liu,Junichi Tsujii,Jian-Yun Nie,Sophia Ananiadou*

Main category: cs.AI

TL;DR: Herculean is the first benchmark evaluating AI agents' ability to execute complex financial workflows (Trading, Hedging, Market Insights, Auditing) rather than just isolated tasks, revealing significant gaps in agents' ability to perform reliable, coordinated financial work.


<details>
  <summary>Details</summary>
Motivation: Current AI benchmarks for financial intelligence are insufficient because they only evaluate static competencies like question answering, retrieval, summarization, and classification, rather than assessing whether agents can reliably carry out end-to-end professional financial workflows.

Method: Introduces Herculean benchmark with four representative financial workflows (Trading, Hedging, Market Insights, Auditing) instantiated as standardized MCP-based skill environments, each with its own tools, interaction dynamics, constraints, and success criteria for consistent end-to-end assessment of heterogeneous agent systems.

Result: Agents perform relatively well on Trading and Market Insights workflows, but struggle substantially on Hedging and Auditing where long-horizon coordination, state consistency, and structured verification are critical.

Conclusion: There is a significant gap in current AI agents' ability to translate financial reasoning into dependable workflow execution, particularly in high-stakes financial workflows requiring complex coordination and verification.

Abstract: As AI agents improve, the central question is no longer whether they can solve isolated well-defined financial tasks, but whether they can reliably carry out financial professional work. Existing financial benchmarks offer only a partial view of this ability, as they primarily evaluate static competencies such as question answering, retrieval, summarization, and classification. We introduce Herculean, the first skilled benchmark for agentic financial intelligence spanning four representative workflows, including Trading, Hedging, Market Insights, and Auditing. Each workflow is instantiated as a standardized MCP-based skill environment with its own tools, interaction dynamics, constraints, and success criteria, enabling consistent end-to-end assessment of heterogeneous agent systems. Across frontier agents, we find agents perform relatively well on Trading and Market Insights, but struggle substantially on Hedging and Auditing, where long-horizon coordination, state consistency, and structured verification are critical. Overall, our results point to a key gap in current agents in turning financial reasoning into dependable workflow execution in high-stakes financial workflows.

</details>


### [44] [Uncovering the Representation Geometry of Minimal Cores in Overcomplete Reasoning Traces](https://arxiv.org/abs/2605.14358)
*Sanjoy Chowdhury,Dinesh Manocha*

Main category: cs.AI

TL;DR: Language models generate excessively verbose reasoning traces where nearly half the steps are unnecessary for predictions; minimal cores (essential steps) improve reasoning clarity and expose cleaner decision geometry.


<details>
  <summary>Details</summary>
Motivation: The paper aims to understand the necessity of long chain-of-thought reasoning traces generated by language models, investigating whether much of this reasoning is redundant or overcomplete, and seeking to identify the essential minimal reasoning that supports predictions.

Method: The study defines "minimal core" as the smallest subset of reasoning steps preserving either final answer or predictive distribution, introduces metrics (compression ratio, redundancy mass, step necessity, necessity concentration), and applies greedy minimal-core extraction across six deliberative reasoning benchmarks (arithmetic, competition math, expert scientific reasoning, commonsense multi-hop QA).

Result: Found substantial overcompleteness: 46% of steps are removable while preserving original answers in 86% of cases, predictive support is concentrated (top three steps account for 65% of necessity mass), minimal cores improve correct-incorrect trace separation by 11 points, reduce estimated intrinsic dimensionality by 34%, and transfer across model families with 85% off-diagonal answer retention.

Conclusion: Full reasoning traces are often verbose and overcomplete, while minimal cores isolate the effective support underlying language model predictions, revealing cleaner reasoning geometry and providing theoretical guarantees for minimal sufficient subsets and overcompleteness certificates.

Abstract: Language models often generate long chain-of-thought traces, but it remains unclear how much of this reasoning is necessary for preserving the final prediction. We study this through the lens of overcomplete reasoning traces: generated traces that contain more intermediate steps than are needed to support the model's answer. We define the minimal core as the smallest subset of steps that preserves either the final answer or predictive distribution, and introduce metrics for compression ratio, redundancy mass, step necessity, and necessity concentration. Across six deliberative reasoning benchmarks spanning arithmetic, competition mathematics, expert scientific reasoning, and commonsense multi-hop QA, we find substantial overcompleteness: on average, 46% of steps are removable under greedy minimal-core extraction while preserving the original answer in 86% of cases. We also find that predictive support is concentrated: the top three steps account for 65% of measured necessity mass on average. Beyond compression, minimal cores expose a cleaner geometry of reasoning: compared with full traces, they improve correct-incorrect trace separation by 11 points, reduce estimated intrinsic dimensionality by 34%, and transfer across model families with 85% off-diagonal answer retention. Theoretically, we establish existence of minimal sufficient subsets, local irreducibility guarantees for greedy elimination, and certificates of overcompleteness and sparse necessity. Together, these results suggest that full reasoning traces are often verbose and overcomplete, while minimal cores isolate the effective support underlying language-model predictions.

</details>


### [45] [Nexus : An Agentic Framework for Time Series Forecasting](https://arxiv.org/abs/2605.14389)
*Sarkar Snigdha Sarathi Das,Palash Goyal,Mihir Parmar,Nanyun Peng,Vishy Tirumalashetty,Chun-Liang Li,Rui Zhang,Jinsung Yoon,Tomas Pfister*

Main category: cs.AI

TL;DR: Nexus is a multi-agent LLM framework that decomposes time series forecasting into specialized stages (macro/micro fluctuations, contextual integration) to outperform specialized time series models and LLM baselines on real-world datasets like real estate and stocks.


<details>
  <summary>Details</summary>
Motivation: Current approaches have limitations: specialized Time Series Foundation Models (TSFMs) are unaware of textual context like news/events, while LLMs as zero-shot forecasters have uneven performance and lack proper contextual grounding. There's a gap between numerical pattern recognition and real-world reasoning.

Method: Nexus uses a multi-agent framework that decomposes forecasting into specialized stages: (1) isolating macro-level temporal fluctuations, (2) isolating micro-level fluctuations, and (3) integrating contextual information when available before synthesizing final forecasts. This approach adapts from seasonal signals to volatile event-driven information without external statistical anchors or monolithic prompting.

Result: Nexus consistently matches or outperforms state-of-the-art TSFMs and strong LLM baselines on real-world datasets (Zillow real estate metrics and volatile stock market equities) that succeed LLM knowledge cutoffs. LLMs show substantially stronger intrinsic forecasting ability than previously recognized when numerical and contextual reasoning are properly organized.

Conclusion: Real-world forecasting is an agentic reasoning problem extending beyond sequence modeling. Nexus demonstrates that organizing numerical and contextual reasoning through specialized decomposition enables high-quality forecasts with explicit reasoning traces showing fundamental drivers behind each prediction.

Abstract: Time series forecasting is not just numerical extrapolation, but often requires reasoning with unstructured contextual data such as news or events. While specialized Time Series Foundation Models (TSFMs) excel at forecasting based on numerical patterns, they remain unaware to real-world textual signals. Conversely, while LLMs are emerging as zero-shot forecasters, their performance remains uneven across domains and contextual grounding. To bridge this gap, we introduce Nexus, a multi-agent forecasting framework that decomposes prediction into specialized stages: isolating macro-level and micro-level temporal fluctuations, and integrating contextual information when available before synthesizing a final forecast. This decomposition enables Nexus to adapt from seasonal signals to volatile, event-driven information without relying on external statistical anchors or monolithic prompting. We show that current-generation LLMs possess substantially stronger intrinsic forecasting ability than previously recognized, depending critically on how numerical and contextual reasoning are organized. Evaluated on data strictly succeeding LLM knowledge cutoffs spanning Zillow real estate metrics and volatile stock market equities, Nexus consistently matches or outperforms state-of-the-art TSFMs and strong LLM baselines. Beyond numerical accuracy, Nexus produces high-quality reasoning traces that explicitly show the fundamental drivers behind each forecast. Our results establish that real-world forecasting is an agentic reasoning problem extending well beyond only sequence modeling.

</details>


### [46] [Learning to Build the Environment: Self-Evolving Reasoning RL via Verifiable Environment Synthesis](https://arxiv.org/abs/2605.14392)
*Yucheng Shi,Zhenwen Liang,Kishan Panaganti,Dian Yu,Wenhao Yu,Haitao Mi*

Main category: cs.AI

TL;DR: Language models can self-improve by constructing computational environments that create a persistent gap between what they can verify and what they can solve, enabling stable reinforcement learning without data saturation.


<details>
  <summary>Details</summary>
Motivation: Traditional self-improvement approaches rely on generating training data or traces, which risks saturation as models get better at gaming their own verification. The paper proposes shifting from data-generation to environment-construction, where models create reusable executable environments that maintain a stable solve-verify asymmetry to keep rewards informative.

Method: Proposes EvoEnv: a single-policy generator-solver method that synthesizes Python environments from minimal seeds. Environments undergo staged validation, semantic self-review, difficulty calibration relative to the solver, and novelty checks. The system relies on two types of solve-verify asymmetry: algorithmic tasks that are hard to reason through but easy as code, and intrinsically hard problems that are easy to verify.

Result: On Qwen3-4B-Thinking, fixed public-data RLVR and fixed hand-crafted environment RLVR reduced performance, while EvoEnv improved average performance from 72.4 to 74.8 (3.3% relative gain). This demonstrates stable self-improvement in the already-strong regime where traditional methods fail.

Conclusion: Stable self-improvement depends on models learning to construct environments whose difficulty remains structurally beyond their reach, rather than producing more synthetic data. The solve-verify asymmetry creates a durable gap between proposing and solving that keeps reward signals informative as the learner improves.

Abstract: We pursue a vision for self-improving language models in which the model does not merely generate problems or traces to imitate, but constructs the environments that train it. In zero-data reasoning RL, this reframes self-improvement from a data-generation loop into an environment-construction loop, where each artifact is a reusable executable object that samples instances, computes references, and scores responses. Whether this vision sustains improvement hinges on a single property: the environments must exhibit stable solve--verify asymmetry, the model must be able to write an oracle once that it cannot reliably execute in natural language on fresh instances. This asymmetry takes two complementary forms. Some tasks are algorithmically hard to reason through but trivial as code: a dynamic program or graph traversal, compiled once, yields unboundedly many calibrated instances. Others are intrinsically hard to solve but easy to verify, like planted subset-sum or constraint satisfaction. Both create a durable gap between proposing and solving that the policy cannot close by gaming the verifier, and it is this gap that keeps reward informative as the learner improves. We instantiate this view in EvoEnv, a single-policy generator, solver method that synthesizes Python environments from ten seeds and admits them only after staged validation, semantic self-review, solver-relative difficulty calibration, and novelty checks. The strongest evidence comes from the already-strong regime: on Qwen3-4B-Thinking, fixed public-data RLVR and fixed hand-crafted environment RLVR reduce the average, while EvoEnv improves it from 72.4 to 74.8, a relative gain of 3.3%. Stable self-improvement, we suggest, depends not on producing more synthetic data, but on models learning to construct worlds whose difficulty stays structurally beyond their own reach.

</details>


### [47] [Coding Agent Is Good As World Simulator](https://arxiv.org/abs/2605.14398)
*Hongyu Wang,Jingquan Wang,Bocheng Zou,Radu Serban,Dan Negrut*

Main category: cs.AI

TL;DR: An agentic framework that constructs physics-based world models through executable simulation code, outperforming video-based models in physical accuracy and instruction fidelity.


<details>
  <summary>Details</summary>
Motivation: Current video-based world models generate visually plausible dynamics but lack explicit physical constraints, resulting in unstable contacts, distorted shapes, and inconsistent motion. There's a need for physically plausible simulations that maintain physical accuracy while matching user instructions.

Method: An agentic framework coordinating planning, code generation, visual review, and physics analysis agents. The planning agent converts natural language prompts into structured scene plans, the code agent implements executable simulation code, and iterative refinement occurs through visual feedback and physics consistency checks until requirements are met.

Result: The framework outperforms advanced video-based models in physical accuracy, instruction fidelity, and visual quality. It demonstrates applicability to various scenarios including driving simulation and embodied robot tasks.

Conclusion: The agentic framework successfully addresses the limitations of video-based world models by enforcing physical constraints through executable simulation code, creating more physically plausible and instruction-faithful simulation environments suitable for diverse applications.

Abstract: World models have emerged as a powerful paradigm for building interactive simulation environments, with recent video-based approaches demonstrating impressive progress in generating visually plausible dynamics. However, because these models typically infer dynamics from video and represent them in latent states, they do not explicitly enforce physical constraints. As a result, the generated video rollouts are not physically plausible, exhibiting unstable contacts, distorted shapes, or inconsistent motion. In this paper, we present an agentic framework constructing physics-based world models through executable simulation code. The framework coordinates planning, code generation, visual review, and physics analysis agents. The planning agent converts the natural language prompt into a structured scene plan, the code agent implements it as executable simulation code, and the visual review agent provide visual feedback while the physics analysis agent checks physical consistency. The code is iteratively revised based on the feedback until the simulation matches the prompt reqirements and physical constraints. Experimental results show that our framework outperforms advanced video-based models in physical accuracy, instruction fidelity and visual quality, which could be applied to various scenarios including driving simulation and embodied robot tasks.

</details>


### [48] [Metis AI: The Overlooked Middle Zone Between AI-Native and World-Movers](https://arxiv.org/abs/2605.14407)
*Xiang Li*

Main category: cs.AI

TL;DR: This paper challenges the digital vs. physical AI capability divide, arguing the real boundary lies within digital tasks - identifying "Metis AI" tasks that resist automation due to social, institutional, and normative entanglements, not computational limitations.


<details>
  <summary>Details</summary>
Motivation: The paper critiques the dominant discourse that frames AI limitations as a digital-physical divide, arguing this misses the more important boundary within digital tasks themselves where AI faces fundamental barriers beyond computational complexity.

Method: The authors develop a conceptual framework distinguishing between constitutive metis (knowledge destroyed by formalization) and operational metis (system-specific familiarity). They identify five structural characteristics defining the Metis AI zone: consequential irreversibility, relational irreducibility, normative open texture, adversarial co-evolution, and accountability anchoring, grounding each in established social science, philosophy, and humanitarian practice theories.

Result: The paper identifies a class of digital tasks that resist AI automation due to their entanglement with institutional, social, and normative contexts. These Metis AI tasks have specific structural characteristics that make them fundamentally resistant to algorithmic approaches, regardless of AI model improvements.

Conclusion: The appropriate design response to Metis AI tasks is not better automation but "centaur architectures" where humans lead and AI supports, acknowledging the fundamental limitations of AI in handling tasks with deep social, normative, and institutional entanglements.

Abstract: The dominant discourse on AI limitations frames the boundary of AI capability as a divide between digital tasks (where AI excels) and physical tasks (where embodiment is required). We argue this framing misses the most consequential boundary: the one within digital tasks. We identify a class of tasks we call Metis AI, named for the Greek concept of metis (practical, contextual knowledge), that are performed entirely on computers yet resist reliable AI automation. These tasks are not computationally intractable; they are institutionally, socially, and normatively entangled in ways that defeat algorithmic approaches. We distinguish constitutive metis (knowledge destroyed by the act of formalization) from operational metis (system-specific familiarity that automation can progressively absorb), and propose five structural characteristics that define the Metis AI zone: consequential irreversibility, relational irreducibility, normative open texture, adversarial co-evolution, and accountability anchoring. We ground each in established theory from across the social sciences, philosophy, and humanitarian practice, argue that these characteristics are properties of the tasks themselves rather than limitations of current models, and show that the appropriate design response is not better automation but centaur architectures in which humans lead and AI supports.

</details>


### [49] [A Unified Knowledge Embedded Reinforcement Learning-based Framework for Generalized Capacitated Vehicle Routing Problems](https://arxiv.org/abs/2605.14416)
*Wen Wang,Xiangchen Wu,Liang Wang,Hao Hu,Xianping Tao*

Main category: cs.AI

TL;DR: Proposes knowledge-embedded RL framework inspired by Route-First Cluster-Second heuristics for solving diverse CVRP variants, achieving state-of-the-art solution quality.


<details>
  <summary>Details</summary>
Motivation: Real-world CVRPs involve diverse objectives and complex constraints (time windows, backhaul), needing unified solution framework. Current RL approaches lack explicit problem-solving knowledge, limiting solution quality.

Method: Knowledge-embedded framework: (1) decomposes CVRPs into route-first and cluster-second subproblems, (2) uses dynamic programming to solve cluster-second subproblem, guiding RL-based constructive solver for route-first problem. Introduces unified history-enhanced context processing module to mitigate partial observability from decomposition.

Result: Framework achieves superior solution quality compared to state-of-the-art learning-based methods, with smaller gap to classical heuristics. Demonstrates strong generalization across diverse CVRP variants.

Conclusion: The knowledge-embedded framework effectively combines RL with explicit problem-solving knowledge (heuristics + dynamic programming), providing a unified solution approach for diverse CVRP variants with improved solution quality and generalization capabilities.

Abstract: The Capacitated Vehicle Routing Problem (CVRP) is a fundamental NP-hard problem with broad applications in logistics and transportation. Real-world CVRPs often involve diverse objectives and complex constraints, such as time windows or backhaul requirements, motivating the development of a unified solution framework. Recent reinforcement learning (RL) approaches have shown promise in combinatorial optimization, yet they rely on end-to-end learning and lack explicit problem-solving knowledge, limiting solution quality. In this paper, we propose a knowledge-embedded framework inspired by the Route-First Cluster-Second heuristics. It incorporates knowledge at two levels: (1) decomposing CVRPs into the route-first and cluster-second subproblems, and (2) leveraging dynamic programming to solve the second subproblem, whose results guide the RL-based constructive solver to solve the first problem. To mitigate partial observability caused by problem decomposition, we introduce a unified history-enhanced context processing module. Extensive experiments show that this framework achieves superior solution quality compared with state-of-the-art learning-based methods, with a smaller gap to classical heuristics, demonstrating strong generalization across diverse CVRP variants.

</details>


### [50] [DVMap: Fine-Grained Pluralistic Value Alignment via High-Consensus Demographic-Value Mapping](https://arxiv.org/abs/2605.14420)
*Pengyun Zhu,Yuqi Ren,Zhen Wang,Lei Yang,Deyi Xiong*

Main category: cs.AI

TL;DR: DVMap is a framework for fine-grained pluralistic value alignment that shifts from coarse national labels to multi-dimensional demographic constraints, achieving better generalization across demographic groups, countries, and values.


<details>
  <summary>Details</summary>
Motivation: Current LLMs rely on coarse-grained national labels for value alignment, which obscures intra-country value heterogeneity and results in loose alignment. There's a need for more granular, demographically-aware value alignment that captures within-country diversity.

Method: 1) Demographic archetype extraction from World Values Survey (56,152 samples) retaining only respondents with consistent value preferences under identical demographics. 2) Structured Chain-of-Thought mechanism to guide LLMs in reasoning about demographic-value correlations. 3) Group Relative Policy Optimization for adaptive anchoring of value distributions.

Result: DVMap effectively learns mapping from demographics to values with strong generalization. On cross-demographic tests, Qwen3-8B-DVMap achieves 48.6% accuracy, outperforming DeepSeek-v3.2 (45.1%). The approach was evaluated on a triple-generalization benchmark (21,553 samples) spanning cross-demographic, cross-country, and cross-value scenarios.

Conclusion: Shifting from national labels to multi-dimensional demographic constraints enables more precise value alignment that captures intra-country heterogeneity. DVMap demonstrates robust generalization across different demographic groups, countries, and value dimensions, providing a more nuanced approach to pluralistic value alignment for LLMs.

Abstract: Current Large Language Models (LLMs) typically rely on coarse-grained national labels for pluralistic value alignment. However, such macro-level supervision often obscures intra-country value heterogeneity, yielding a loose alignment. We argue that resolving this limitation requires shifting from national labels to multi-dimensional demographic constraints, which can identify groups with predictable, high-consensus value preference. To this end, we propose DVMap (High-Consensus Demographic-Value Mapping), a framework for fine-grained pluralistic value alignment. In this framework, we first present a demographic archetype extraction strategy to construct a high-quality value alignment corpus of 56,152 samples from the World Values Survey (WVS) by strictly retaining respondents with consistent value preferences under identical demographics. Over this corpus, we introduce a Structured Chain-of-Thought (CoT) mechanism that explicitly guides LLMs to reason about demographic-value correlations. Subsequently, we employ Group Relative Policy Optimization (GRPO) to achieve adaptive anchoring of value distributions. To rigorously evaluate generalization, we further establish a triple-generalization benchmark (spanning cross-demographic, cross-country, and cross-value) comprising 21,553 samples. Experimental results demonstrate that DVMap effectively learns the manifold mapping from demographics to values, exhibiting strong generalization and robustness. On cross-demographic tests, Qwen3-8B-DVMap achieves 48.6% accuracy, surpassing the advanced open-source LLM DeepSeek-v3.2 (45.1%). The source code and dataset are available at https://github.com/EnlightenedAI/DVMap.

</details>


### [51] [BEAM: Binary Expert Activation Masking for Dynamic Routing in MoE](https://arxiv.org/abs/2605.14438)
*Juntong Wu,Jialiang Cheng,Qishen Yin,Yue Dai,Yuliang Yan,Fuyu Lv,Ou Dan,Li Yuan*

Main category: cs.AI

TL;DR: BEAM (Binary Expert Activation Masking) is a plug-and-play method that learns token-adaptive expert selection via binary masks to reduce MoE computation by up to 85% while maintaining over 98% of original model performance.


<details>
  <summary>Details</summary>
Motivation: Standard MoE uses fixed Top-K routing leading to redundant computation and suboptimal inference latency. Existing acceleration methods require costly retraining with architectural changes or suffer from severe performance drop at high sparsity due to train-inference mismatch.

Method: BEAM learns token-adaptive expert selection via trainable binary masks using straight-through estimator and auxiliary regularization loss to induce dynamic expert sparsity through end-to-end training while maintaining model capability. Also implements efficient custom CUDA kernel for integration with vLLM.

Result: BEAM retains over 98% of original model's performance while reducing MoE layer FLOPs by up to 85%, achieving up to 2.5× faster decoding and 1.4× higher throughput.

Conclusion: BEAM is an effective practical, plug-and-play solution for efficient MoE inference that addresses limitations of existing methods by providing trainable token-adaptive expert selection without requiring architectural changes.

Abstract: Mixture-of-Experts (MoE) architectures enhance the efficiency of large language models by activating only a subset of experts per token. However, standard MoE employs a fixed Top-K routing strategy, leading to redundant computation and suboptimal inference latency. Existing acceleration methods either require costly retraining with architectural changes or suffer from severe performance drop at high sparsity due to train-inference mismatch. To address these limitations, we propose BEAM (Binary Expert Activation Masking), a novel method that learns token-adaptive expert selection via trainable binary masks. With a straight-through estimator and an auxiliary regularization loss, BEAM induces dynamic expert sparsity through end-to-end training while maintaining model capability. We further implement an efficient custom CUDA kernel for BEAM, ensuring seamless integration with the vLLM inference framework. Experiments show that BEAM retains over 98\% of the original model's performance while reducing MoE layer FLOPs by up to 85\%, achieving up to 2.5$\times$ faster decoding and 1.4$\times$ higher throughput, demonstrating its effectiveness as a practical, plug-and-play solution for efficient MoE inference.

</details>


### [52] [Synthesizing POMDP Policies: Sampling Meets Model-checking via Learning](https://arxiv.org/abs/2605.14440)
*Debraj Chakraborty,Anirban Majumdar,Prince Mathew,Sayan Mukherjee,Jean-François Raskin*

Main category: cs.AI

TL;DR: A novel synthesis framework combining sampling, automata learning, and model-checking to solve POMDP problems with formal correctness guarantees, bridging the gap between scalable sampling methods and formal synthesis approaches.


<details>
  <summary>Details</summary>
Motivation: Existing approaches to POMDP decision-making have limitations: sampling-based methods scale well but lack formal correctness guarantees (unsafe for safety-critical applications), while formal synthesis techniques provide correctness-by-construction but struggle with scalability due to POMDP synthesis being undecidable in general.

Method: Proposes a synthesis framework that integrates sampling, automata learning, and model-checking. Inspired by Angluin's L* algorithm, uses sampling as a membership oracle and model-checking as an equivalence oracle to synthesize finite-state controllers with formal guarantees, assuming the sampling-induced policy is regular.

Result: Establishes a relative completeness result for the framework. Experimental results from a prototypical implementation show the method successfully solves threshold-safety problems that are challenging for existing formal synthesis tools.

Conclusion: The proposed algorithm bridges the gap between scalability and formal guarantees, serving as a valuable component in a portfolio approach to tackling the inherent difficulty of POMDP synthesis problems.

Abstract: Partially Observable Markov Decision Processes (POMDPs) are the standard framework for decision-making under uncertainty. While sampling-based methods scale well, they lack formal correctness guarantees, making them unsuitable for safety-critical applications. Conversely, formal synthesis techniques provide correctness-by-construction but often struggle with scalability, as general POMDP synthesis is undecidable. To bridge this gap, we propose a synthesis framework that integrates sampling, automata learning, and model-checking. Inspired by Angluin's $L^*$ algorithm, our approach utilizes sampling as a membership oracle and model-checking as an equivalence oracle. This enables the synthesis of finite-state controllers with formal guarantees, provided the sampling-induced policy is regular. We establish a relative completeness result for this framework. Experimental results from our prototypical implementation demonstrate that this method successfully solves threshold-safety problems that remain challenging for existing formal synthesis tools. We believe our algorithm serves as a valuable component in a portfolio approach to tackling the inherent difficulty of POMDP synthesis problems.

</details>


### [53] [Intelligence Impact Quotient (IIQ): A Framework for Measuring Organizational AI Impact](https://arxiv.org/abs/2605.14455)
*Chandan Rajah,Neha Sengupta,Federico Castanedo,Robin Mills,Amit Bahree,Ramesh Krishnan Muthukrishnan,Larry Murray*

Main category: cs.AI

TL;DR: The paper introduces Intelligence Impact Quotient (IIQ), a composite metric that measures AI integration depth and impact in organizations by combining multiple factors beyond simple usage statistics, with normalization for cross-organizational comparison.


<details>
  <summary>Details</summary>
Motivation: Current metrics like access counts or aggregate token volume are insufficient for measuring true AI impact and integration depth in organizational workflows. There's a need for a more nuanced measurement framework that captures how deeply AI systems are embedded in work processes and their actual impact.

Method: The IIQ combines: novelty-weighted, time-decayed token stock; usage frequency; grace-period recency gate; organizational leverage; task complexity; and autonomy. It produces both a raw Intelligence Adoption Index (IAI) and a normalized 0-1000 IIQ index for comparison. The method includes sub-daily update rules and an interpretation layer for efficiency and financial impact estimation.

Result: The IIQ framework distinguishes between different types of AI usage: frequent low-leverage use, semantically repetitive prompting, and more autonomous, higher-consequence AI-assisted work. It provides a normalized metric (0-1000) for comparing AI adoption across heterogeneous users and organizational units.

Conclusion: IIQ serves as a deployment-oriented measurement framework for tracking AI embedding in workflows, not as a direct measure of model capability or a substitute for causal productivity evaluation. It provides a formal proposal for measuring AI integration depth and organizational impact beyond simple usage statistics.

Abstract: The Intelligence Impact Quotient (IIQ) is a composite metric intended to quantify the depth to which AI systems are integrated into organizational work and their impact. Rather than treating access counts or aggregate token volume as sufficient evidence of impact, IIQ combines a novelty-weighted, time-decayed token stock with usage frequency, a grace-period recency gate, organizational leverage, task complexity, and autonomy. The formulation produces a raw Intelligence Adoption Index (IAI) and a normalized 0-1000 IIQ index for comparison between heterogeneous users and units. We also derive sub-daily update rules and a bounded interpretation layer for estimated efficiency and financial impact. The paper positions IIQ as a deployment-oriented measurement framework: a formal proposal for tracking AI embedding in workflows, not a direct measure of model capability or a substitute for causal productivity evaluation. Synthetic scenarios illustrate how the revised metric distinguishes between frequent low-leverage use, semantically repetitive prompting, and more autonomous, higher-consequence AI-assisted work.

</details>


### [54] [Stateful Reasoning via Insight Replay](https://arxiv.org/abs/2605.14457)
*Bin Lei,Caiwen Ding,Jiachen Yang,Ang Li,Xin Eric Wang*

Main category: cs.AI

TL;DR: InsightReplay improves long-chain reasoning by periodically replaying critical insights to maintain their accessibility, overcoming the accuracy decline in traditional CoT as chain length increases.


<details>
  <summary>Details</summary>
Motivation: Longer Chain-of-Thought reasoning doesn't scale monotonically - accuracy typically increases with CoT length up to a point, then declines due to diminishing attention to early critical insights as the reasoning chain grows.

Method: Propose InsightReplay, a stateful reasoning approach where the model periodically extracts critical insights from its reasoning trace and replays them near the active generation frontier to maintain their accessibility throughout long reasoning trajectories.

Result: 3-round InsightReplay yields accuracy gains across all 24 experimental settings, with average improvement of +1.65 points over standard CoT, and largest single-setting gain of +9.2 points on R1-Distill-32B's LiveCodeBench v5 subset.

Conclusion: Effectiveness of test-time scaling depends not only on how much a model reasons, but also on whether critical intermediate insights remain accessible throughout long reasoning trajectories. InsightReplay successfully addresses this accessibility problem.

Abstract: Chain-of-Thought (CoT) reasoning has become a foundation for eliciting multi-step reasoning in large language models, but recent studies show that its benefits do not scale monotonically with chain length: while longer CoT generally enables a model to tackle harder problems, on a given problem, accuracy typically increases with CoT length up to a point, after which it declines. We identify a major cause of this phenomenon: as the CoT grows, the model's attention to critical insights produced earlier in the trace gradually weakens, making those insights progressively less accessible when they are most needed. Therefore, we propose \textbf{InsightReplay}, a stateful reasoning approach in which the model periodically extracts critical insights from its reasoning trace and replays them near the active generation frontier, keeping them accessible as the reasoning scales. Extensive experiments on a $\mathbf{2}\!\times\!\mathbf{3}\!\times\!\mathbf{4}$ benchmark grid, covering model scales $\{\text{8B}, \text{30B}\}$, model families $\{\text{Qwen3.5}, \text{DeepSeek-R1-Distill-Qwen}, \text{Gemma-4}\}$, and reasoning benchmarks $\{\text{AIME}, \text{HMMT}, \text{GPQA Diamond}, \text{LiveCodeBench v5}\}$, show that 3-round InsightReplay yields accuracy gains across \textbf{all 24 settings}, with an averaged improvement of $\mathbf{+1.65}$ points over standard CoT, and a largest single-setting gain of $\mathbf{+9.2}$ points on R1-Distill-32B's LiveCodeBench v5 subset. Our results suggest that the effectiveness of test-time scaling depends not only on how much a model reasons, but also on whether critical intermediate insights remain accessible throughout long reasoning trajectories.

</details>


### [55] [OmniDrop: Layer-wise Token Pruning for Omni-modal LLMs via Query-Guidance](https://arxiv.org/abs/2605.14458)
*Yeo Jeong Park,Hyemi Jang,Minseo Choi,Jongsun Lee,Jooyoung Choi,Yongkweon Jeon*

Main category: cs.AI

TL;DR: OmniDrop is a training-free token pruning framework for multimodal LLMs that progressively removes audio/video tokens in the decoder layers rather than at input level, using text guidance and temporal diversity scoring, achieving performance gains while reducing latency and memory usage.


<details>
  <summary>Details</summary>
Motivation: Omni-modal LLMs face token explosion issues with high-resolution audio/video inputs, creating bottlenecks for real-time and long-form reasoning applications. Existing token compression methods prune at input level using unreliable similarity assumptions, needing better semantic-aware approaches.

Method: Proposed OmniDrop framework uses layer-wise token pruning within LLM decoder layers instead of input-level pruning. It preserves information in early layers, aggressively prunes in deeper layers, uses text queries as guidance for task-adaptive pruning, and employs temporal diversity scoring to maintain global temporal context.

Result: Experimental results show OmniDrop outperforms all baselines by up to 3.58 points on audiovisual benchmarks while reducing prefill latency by up to 40% and memory usage by up to 14.7%.

Conclusion: OmniDrop effectively addresses the token explosion problem in multimodal LLMs through training-free, layer-wise token pruning that maintains semantic understanding while significantly improving efficiency for real-time applications.

Abstract: Omni-modal large language models have demonstrated remarkable potential in holistic multimodal understanding; however, the token explosion caused by high-resolution audio and video inputs remains a critical bottleneck for real-time applications and long-form reasoning. Existing omni-modal token compression methods typically prune tokens at the input embedding level, relying on audio-video similarity or temporal co-occurrence as proxies for semantic relevance. In practice, such assumptions are often unreliable. To address this limitation, we propose OmniDrop, a training-free, layer-wise token pruning framework that progressively prunes audiovisual tokens within the LLM decoder layers rather than at the input-level, allowing early layers to preserve sufficient omni-modal information fusion before aggressively removing tokens in deeper layers. We further utilize text queries as guidance for modality-agnostic and task-adaptive token pruning. We also introduce a temporal diversity score that encourages balanced token survival to preserve global temporal context. Experimental results across various audiovisual benchmarks demonstrate that OmniDrop outperforms all baselines by up to 3.58 points while reducing prefill latency by up to 40% and memory usage by up to 14.7%.

</details>


### [56] [From Table to Cell: Attention for Better Reasoning with TABALIGN](https://arxiv.org/abs/2605.14465)
*Tung Sum Thomas Kwok,Zeyong Zhang,Xinyu Wang,Chunhe Wang,Xiaofeng Lin,Hanwei Wu,Lei Ding,Guang Cheng,Zhijiang Guo*

Main category: cs.AI

TL;DR: TABALIGN uses diffusion language models for table reasoning with explicit cell-grounding contracts, improving accuracy and efficiency over autoregressive methods through better planning and verification.


<details>
  <summary>Details</summary>
Motivation: Existing multi-step LLM reasoning over structured tables fails because planning and execution lack explicit cell-grounding contracts, and current methods use left-to-right factorization incompatible with table permutation invariance while scoring intermediate states only by generated content without considering cell grounding.

Method: TABALIGN pairs a masked DLM planner (using bidirectional denoising to emit plan steps as binary cell masks) with TABATTN (a lightweight verifier trained on 1,600 human-verified attention standards to score each step by attention overlap with the plan-designated mask).

Result: Across eight benchmarks covering table QA and fact verification, TABALIGN improves average accuracy by 15.76 percentage points over the strongest open-source baseline at 8B-class scale, with 2.87 percentage points attributed to DLM planner over AR planner; DLM plans accelerate downstream reasoning by 44.64%.

Conclusion: Diffusion language models provide better cell grounding for table reasoning than autoregressive models, and explicit planning-execution contracts via TABALIGN significantly improve accuracy and efficiency in structured table reasoning tasks.

Abstract: Multi-step LLM reasoning over structured tables fails because planning and execution share no explicit cell-grounding contract. Existing methods constrain the planner to a left-to-right factorization at odds with table permutation invariance, and score intermediate states by generated content alone, overlooking cell grounding. We conduct a pilot study showing that diffusion language models (DLMs) produce more human-aligned and permutation-stable cell attention on tables than autoregressive models, with a 40.2% median reduction in attention-AUROC variability under row reordering. Motivated by this, we propose TABALIGN, a planned table reasoning framework that operationalizes the contract. TABALIGN pairs a masked DLM planner, whose bidirectional denoising emits plan steps as binary cell masks, with TABATTN, a lightweight verifier trained on 1,600 human-verified attention standards to score each step by its attention overlap with the plan-designated mask. Across eight benchmarks covering table question answering and fact verification, TABALIGN improves average accuracy by 15.76 percentage points over the strongest open-source baseline at comparable 8B-class scale, with a matched-backbone ablation attributing 2.87 percentage points of this gain to the DLM planner over an AR planner on a fixed reasoner. Cleaner DLM plans also accelerate downstream reasoning execution by 44.64%.

</details>


### [57] [LEMON: Learning Executable Multi-Agent Orchestration via Counterfactual Reinforcement Learning](https://arxiv.org/abs/2605.14483)
*Xudong Chen,Yixin Liu,Hua Wei,Kaize Ding*

Main category: cs.AI

TL;DR: LEMON is an LLM-based orchestrator that learns to generate executable multi-agent orchestration specifications through counterfactual reinforcement learning, achieving state-of-the-art performance on reasoning and coding benchmarks.


<details>
  <summary>Details</summary>
Motivation: Current multi-agent systems using LLMs depend heavily on orchestration design, but existing approaches optimize orchestration decisions partially or sequentially and rely on execution-level feedback with limited credit assignment for local orchestration decisions.

Method: LEMON generates executable orchestration specifications that integrate task-specific roles, duties, capacity levels, and dependency structures. It uses a counterfactual reinforcement learning approach that edits role, capacity, or dependency fields and applies reward contrast only to the edited spans, augmenting the orchestration-level GRPO objective.

Result: LEMON achieves state-of-the-art performance on six reasoning and coding benchmarks: MMLU, GSM8K, AQuA, MultiArith, SVAMP, and HumanEval, outperforming other multi-agent orchestration methods.

Conclusion: LEMON demonstrates that learning executable multi-agent orchestration through localized counterfactual reinforcement learning effectively improves orchestration design, leading to better solution quality and execution efficiency in LLM-based multi-agent systems.

Abstract: Large language models (LLMs) have become a strong foundation for multi-agent systems, but their effectiveness depends heavily on orchestration design. Across different tasks, role design, capacity assignment, and dependency construction jointly affect both solution quality and execution efficiency. Existing approaches automate parts of this design process, yet they often optimize these decisions partially or sequentially, and rely on execution-level feedback that provides limited credit assignment for local orchestration decisions. We propose LEMON (\textbf{L}earning \textbf{E}xecutable \textbf{M}ulti-agent \textbf{O}rchestratio\textbf{N} via Counterfactual Reinforcement Learning), an LLM-based orchestrator that generates an executable orchestration specification. The specification integrates task-specific roles, customized duties, capacity levels, and dependency structure into a single deployable system. To train the orchestrator, we augment the orchestration-level GRPO objective with a localized counterfactual signal that edits role, capacity, or dependency fields and applies the resulting reward contrast only to the edited spans. Experiments on six reasoning and coding benchmarks, including MMLU, GSM8K, AQuA, MultiArith, SVAMP, and HumanEval, show that LEMON achieves state-of-the-art performance among the evaluated multi-agent orchestration methods. Our code is available at https://anonymous.4open.science/r/LEMON-B23C.

</details>


### [58] [Deepchecks: Evaluating Retrieval-Augmented Generation (RAG)](https://arxiv.org/abs/2605.14488)
*Assaf Gerner,Netta Madvil,Nadav Barak,Alex Zaikman,Jonatan Liberman,Liron Hamra,Rotem Brazilay,Shay Tsadok,Yaron Friedman,Neal Harow,Noam Bresler,Shir Chorev,Philip Tannor,Lior Rokach*

Main category: cs.AI

TL;DR: Deepchecks is a comprehensive framework designed to evaluate RAG systems through multi-faceted assessment, root cause analysis, and production monitoring to ensure reliability and relevance.


<details>
  <summary>Details</summary>
Motivation: Evaluating RAG systems is complex due to the stochastic nature of LLM outputs and the intricate interplay between retrieval and generation components, making robust evaluation frameworks necessary.

Method: Introduces Deepchecks - a comprehensive framework with multi-faceted evaluation approach, root cause analysis, and production monitoring capabilities tailored for RAG applications.

Result: Deepchecks provides a robust foundation for assessing RAG systems by ensuring alignment with application-specific requirements and evaluating reliability, relevance, and user satisfaction.

Conclusion: Deepchecks addresses the critical need for systematic evaluation of RAG applications across various domains, offering a comprehensive solution to assess their performance and reliability.

Abstract: Large Language Models (LLMs) augmented with Retrieval-Augmented Generation (RAG) techniques are revolutionizing applications across multiple domains, such as healthcare, finance, and customer service. Despite their potential, evaluating RAG systems remains a complex challenge due to the stochastic nature of generated outputs and the intricate interplay between retrieval and generation components. This paper introduces Deepchecks, a comprehensive framework tailored for evaluating RAG applications. Deepchecks' evaluation framework addresses RAG applications evaluation through a multi-faceted approach, root cause analysis and production monitoring. By ensuring alignment with application-specific requirements, Deepchecks framework provides a robust foundation for assessing reliability, relevance, and user satisfaction in RAG systems.

</details>


### [59] [Learning Scenario Reduction for Two-Stage Robust Optimization with Discrete Uncertainty](https://arxiv.org/abs/2605.14494)
*Tianjue Lin,Jianan Zhou,Jieyi Bi,Yaoxin Wu,Wen Song,Zhiguang Cao,Jie Zhang*

Main category: cs.AI

TL;DR: NeurPRISE is a neural surrogate model for scenario reduction in Two-Stage Robust Optimization that combines GNN-Transformer architecture with imitation learning to achieve competitive performance with 7-200x speedup over existing methods while maintaining strong generalization.


<details>
  <summary>Details</summary>
Motivation: Two-Stage Robust Optimization with discrete uncertainty is computationally challenging, and existing scenario reduction methods are largely problem-agnostic, operating without considering the feasible region or recourse structure, leading to suboptimal solutions.

Method: The authors propose NeurPRISE, a neural surrogate model built on a GNN-Transformer backbone that encodes per-scenario structure via graph convolution and captures cross-scenario interactions through attention. It's trained via imitation learning with a gain-aware ranking objective to distill marginal gain information from PRISE (a problem-driven sequential lookahead heuristic) into a learned scoring function for scenario ranking and selection.

Result: Extensive experiments on three 2RO problems show NeurPRISE achieves competitive regret relative to comprehensive methods, maintains strong scalability with varying scenario numbers, delivers 7-200x speedup over PRISE, and exhibits strong zero-shot generalization handling instances with 5x larger problem scales, 4x more scenarios, and distribution shifts.

Conclusion: NeurPRISE provides an effective neural approach for scenario reduction in Two-Stage Robust Optimization that combines the advantages of neural architecture design with imitation learning to achieve both computational efficiency and strong performance, addressing key limitations of existing problem-agnostic methods.

Abstract: Two-Stage Robust Optimization (2RO) with discrete uncertainty is challenging, often rendering exact solutions prohibitive. Scenario reduction alleviates this issue by selecting a small, representative subset of scenarios to enable tractable computation. However, existing methods are largely problem-agnostic, operating solely on the uncertainty set without consulting the feasible region or recourse structure. In this paper, we introduce PRISE, a problem-driven sequential lookahead heuristic that constructs reduced scenario sets by evaluating the marginal impact of each scenario. While PRISE yields high-quality scenario subsets, each selection step requires solving multiple subproblems, making it computationally expensive at scale. To address this, we propose NeurPRISE, a neural surrogate model built on a GNN-Transformer backbone that encodes the per-scenario structure via graph convolution and captures cross-scenario interactions through attention. NeurPRISE is trained via imitation learning with a gain-aware ranking objective, which distills marginal gain information from PRISE into a learned scoring function for scenario ranking and selection. Extensive results on three 2RO problems show that NeurPRISE consistently achieves competitive regret relative to comprehensive methods, maintains strong calability with varying numbers of scenarios, and delivers 7-200x speedup over PRISE. NeurPRISE also exhibits strong zero-shot generalization, effectively handling instances with larger problem scales (up to 5x), more scenarios (up to 4x), and distribution shifts.

</details>


### [60] [When Robots Do the Chores: A Benchmark and Agent for Long-Horizon Household Task Execution](https://arxiv.org/abs/2605.14504)
*Zilin Zhu,Longteng Guo,Yanghong Mei,Bowen Pang,Zongxun Zhang,Xingjian He,Ruyi Ji,Jing Liu*

Main category: cs.AI

TL;DR: LongAct benchmark assesses planning-level autonomy for long-horizon household tasks using free-form instructions, isolating high-level cognitive capabilities. HoloMind agent with DAG-based hierarchical planner and dual memory system substantially improves performance but current models still struggle with only 16% full-task success.


<details>
  <summary>Details</summary>
Motivation: Existing embodied AI benchmarks focus on short-horizon navigation/manipulation with fixed task categories, overlooking robust high-level planning and sustained reasoning capabilities needed for long-horizon household tasks.

Method: 1) LongAct benchmark that abstracts away low-level control to isolate high-level cognitive capabilities. 2) HoloMind agent with DAG-based long-horizon hierarchical planner, Multimodal Spatial Memory for world modeling, Episodic Memory for experience reuse, and global Critic for reflective supervision.

Result: HoloMind substantially improves long-horizon performance while reducing reliance on model scale. Experiments with GPT-5 and Qwen3-VL models show even top models achieve only 59% goal completion and 16% full-task success on LongAct.

Conclusion: LongAct reveals significant challenges in long-horizon planning for embodied agents, highlighting the need for stronger planning capabilities beyond model scale improvements. The benchmark underscores the importance of high-level cognitive functions like dependency management and adaptive planning.

Abstract: Long-horizon household tasks demand robust high-level planning and sustained reasoning capabilities, which are largely overlooked by existing embodied AI benchmarks that emphasize short-horizon navigation or manipulation and rely on fixed task categories. We introduce LongAct, a benchmark designed to evaluate planning-level autonomy in long-horizon household tasks specified through free-form instructions. By abstracting away embodiment-specific low-level control, LongAct isolates high-level cognitive capabilities such as instruction understanding, dependency management, memory maintenance, and adaptive planning. We further propose HoloMind, a VLM-driven agent with a DAG-based long-horizon hierarchical planner, a Multimodal Spatial Memory for persistent world modeling, an Episodic Memory for experience reuse, and a global Critic for reflective supervision. Experiments with GPT-5 and Qwen3-VL models show that HoloMind substantially improves long-horizon performance while reducing reliance on model scale. Even top models achieve only 59% goal completion and 16% full-task success, underscoring the difficulty of LongAct and the need for stronger long-horizon planning in embodied agents.

</details>


### [61] [Cattle Trade: A Multi-Agent Benchmark for LLM Bluffing, Bidding, and Bargaining](https://arxiv.org/abs/2605.14537)
*Robert Müller,Clemens Müller*

Main category: cs.AI

TL;DR: Cattle Trade is a multi-agent benchmark testing LLMs' strategic reasoning in complex economic games with auctions, bargaining, bluffing, and resource constraints over 50-60 turns, revealing LLMs struggle with spending efficiency and opponent adaptation compared to heuristic agents.


<details>
  <summary>Details</summary>
Motivation: Current benchmarks test strategic abilities like auctions, bargaining, and bluffing in isolation, but there's a need to evaluate whether agents can integrate these capabilities in complex, competitive multi-agent environments with conflicting incentives, imperfect information, and resource constraints.

Method: Developed the Cattle Trade benchmark - a long-horizon game (50-60 turns) combining auctions, hidden-offer trade challenges, bargaining, bluffing, opponent modeling, and resource allocation. Evaluated 7 cost-efficient LLMs and 3 deterministic code agents across 242 games with detailed logging of all actions.

Result: Strategic coherence (spending efficiency, resource discipline, phase-adaptive bidding) correlates more strongly with rank than spending volume or individual subskills. Heuristic code agents outperform most LLMs. LLMs show recurring failure modes: overbidding, self-bidding, bankrupt trade initiation, and weak opponent-state adaptation.

Conclusion: Evaluating true agentic competence requires benchmarks that test integrated deployment of multiple capabilities in multi-agent environments with conflicting incentives, uncertainty, and economic dynamics - isolated skill testing is insufficient.

Abstract: We introduce \textsc{Cattle Trade, a multi-agent benchmark for evaluating large language models (LLMs) as agents in strategic reasoning under imperfect information, adversarial interaction, and resource constraints. The benchmark combines auctions, hidden-offer trade challenges (TCs), bargaining, bluffing, opponent modeling, and resource allocation within a single long-horizon game lasting 50--60 turns. Unlike prior agent benchmarks that test these abilities in isolation, \textsc{Cattle Trade} evaluates whether agents integrate them across a competitive, multi-agent economic game with conflicting incentives. The benchmark logs every bid, TC offer, counteroffer, and card selection, enabling behavioural analysis beyond final scores or win rates. We evaluate seven cost-efficient language models and three deterministic code agents across 242 games. Strategic coherence, in particular spending efficiency, resource discipline, and phase-adaptive bidding, is associated with rank more strongly than spending volume or any single subskill. Two heuristic code agents outperform most tested LLMs, and behavioural traces surface recurring LLM failure modes including overbidding, self-bidding, bankrupt TC initiation, and weak opponent-state adaptation. Evaluating agentic competence requires benchmarks that test the joint deployment of multiple capabilities in multi-agent environments with conflicting incentives, uncertainty, and economic dynamics.

</details>


### [62] [VerbalValue: A Socially Intelligent Virtual Host for Sales-Driven Live Commerce](https://arxiv.org/abs/2605.14542)
*Yuyan Chen*

Main category: cs.AI

TL;DR: VerbalValue is an AI system that functions as a virtual live-commerce host, combining product expertise, emotional intelligence, and entertainment to convert viewer curiosity into purchases, outperforming general-purpose LLMs on informativeness, factual correctness, and engagement metrics.


<details>
  <summary>Details</summary>
Motivation: Existing AI systems fail to replicate skilled live-commerce hosts who convert viewer curiosity into purchase intent through expert product knowledge, emotional intelligence, and entertainment. Conversational recommenders treat recommendation as terminal, while general LLMs hallucinate product claims and use generic promotional templates that don't engage or persuade.

Method: 1. Construct a domain knowledge base of product specifications and curated sales terminology lexicon to anchor responses in verified expertise.
2. Collect and annotate 1,475 live-commerce interactions spanning diverse viewer intents.
3. Fine-tune a large language model on this data to deliver empathetic, commercially oriented responses using empathetic amplification, evidence-backed rebuttal, and humor-mediated deflection to adapt to viewer intent.

Result: VerbalValue outperforms GPT-5.4, Claude Sonnet 4.6, Gemini 3.1 Pro, and other baselines with gains of 23% on informativeness and 18% on factual correctness, plus consistent advantages in tactfulness and viewer engagement.

Conclusion: The system successfully turns exceptional verbal ability into real commercial value by combining domain expertise, annotated interaction data, and fine-tuned language modeling to create an effective sales-conversion-oriented virtual host that addresses the limitations of current AI approaches.

Abstract: A skilled live-commerce host is not merely a narrator, but a sales agent who converts viewer curiosity into purchase intent through expert product knowledge, emotionally intelligent response tactics, and entertainment that serves as a vehicle for product exposure. Yet no existing AI system replicates this: conversational recommenders treat recommendation as a terminal act, while general-purpose LLMs hallucinate product claims and default to generic promotional templates that fail to engage or persuade. We present VerbalValue, a sales-conversion-oriented virtual host that turns exceptional verbal ability into real commercial value, built on three contributions. First, we construct a domain knowledge base of product specifications and a curated sales terminology lexicon that anchor product-related responses in verified expertise. Second, we collect and annotate 1,475 live-commerce interactions spanning diverse viewer intents. Third, we fine-tune a large language model on this data to deliver empathetic, commercially oriented responses, adapting to viewer intent through empathetic amplification, evidence-backed rebuttal, and humor-mediated deflection. Experiments against GPT-5.4, Claude Sonnet 4.6, Gemini 3.1 Pro, and other baselines demonstrate gains of 23% on informativeness and 18% on factual correctness, with consistent advantages in tactfulness and viewer engagement.

</details>


### [63] [Complacent, Not Sycophantic: Reframing Large Language Models and Designing AI Literacy for Complacent Machines](https://arxiv.org/abs/2605.14544)
*Federico Germani,Giovanni Spitale*

Main category: cs.AI

TL;DR: LLMs don't have sycophantic intent but exhibit complacent behavior due to structural biases favoring agreement with users.


<details>
  <summary>Details</summary>
Motivation: To reframe the characterization of LLM behavior from "sycophancy" (implying strategic intent) to "complacency" (structural tendency), which properly locates agency with developers and institutions rather than attributing motives to models.

Method: Conceptual analysis and argumentation that distinguishes between sycophancy (requiring motives/strategic intent) and complacency (structural tendency), examining how training data, reward signals, and design favor agreement over correction.

Result: The paper argues that models are never sycophants but can be more or less complacent, and that this reframing shifts responsibility to developers/institutions. It also suggests AI literacy education should focus on strategies to counter confirmation bias since complacent models reinforce users' prior beliefs.

Conclusion: Properly characterizing LLM behavior as complacent rather than sycophantic matters because it correctly attributes agency to developers/institutions and has practical implications for AI literacy education focused on combating confirmation bias.

Abstract: Large language models are often described as sycophantic, in the sense that they appear to flatter users or mirror their beliefs. We argue that this label is conceptually misleading: sycophancy implies motives and strategic intent, which LLMs do not possess. Their behaviour is better understood as complacency, a structural tendency to agree with user input because training data, reward signals and design favour agreement and reinforcement over correction. We argue that this distinction matters. Whether developers act sycophantically or not, models themselves never are sycophants; they can only be made more or less complacent. This reframing locates agency in developers and institutions, not in the model. Because complacent models reinforce users' prior beliefs, we argue that AI literacy educational approaches should particularly focus on strategies to counter confirmation bias.

</details>


### [64] [TeachAnything: A Multimodal Crowdsourcing Platform for Training Embodied AI Agents in Symmetrical Reality](https://arxiv.org/abs/2605.14556)
*Zidong Liu,Rongkai Liu,Yue Li,Zhenliang Zhang*

Main category: cs.AI

TL;DR: TeachAnything: A cloud-based crowdsourcing platform using multimodal demonstrations and physics simulation to collect diverse training data for developing embodied agents in Symmetrical Reality.


<details>
  <summary>Details</summary>
Motivation: Symmetrical Reality is emerging as a future trend for human-agent coexistence, requiring agents to acquire human-like intelligence. This creates a need for richer and more diverse human guidance to train agents effectively.

Method: The authors introduce a three-stage demonstration paradigm integrating multimodal demonstration signals. They developed TeachAnything - a cloud-based, crowdsourcing-oriented demonstration platform with physics simulation that collects diverse demonstration data across varied scenes, tasks, and embodiments.

Result: The system unifies virtual and physical interactions through methodological design and physics simulation, creating a practical foundation for developing embodied agents aligned with Symmetrical Reality.

Conclusion: TeachAnything provides a scalable solution for collecting diverse human demonstrations needed to train intelligent agents for Symmetrical Reality environments, bridging the gap between virtual and physical interactions.

Abstract: Symmetrical Reality (SR) is emerging as a future trend for human-agent coexistence, placing higher demands on agents to acquire human-like intelligence. It calls for richer and more diverse human guidance. We introduce a three-stage demonstration paradigm integrating multimodal demonstration signals. Building on this paradigm, we developed TeachAnything, a cloud-based, crowdsourcing-oriented demonstration platform with physics simulation capable of collecting diverse demonstration data across varied scenes, tasks, and embodiments. By unifying virtual and physical interactions through both methodological design and physics simulation, the system serves as a practical foundation for developing embodied agents aligned with Symmetrical Reality.

</details>


### [65] [PyCSP3-Scheduling: A Scheduling Extension for PyCSP3](https://arxiv.org/abs/2605.14559)
*Sohaib Afifi*

Main category: cs.AI

TL;DR: PyCSP$^3$ Scheduling is a library that adds scheduling abstractions (interval variables, sequence variables, resource functions) to PyCSP$^3$, compiling them to standard constraints while maintaining the separation between modeling and solving.


<details>
  <summary>Details</summary>
Motivation: PyCSP$^3$ lacks native support for scheduling abstractions, forcing users to encode scheduling models with low-level integer variables and manual channeling constraints despite having some relevant global constraints like NoOverlap and Cumulative.

Method: Developed PyCSP$^3$ Scheduling library with 53 dedicated constraints and 27 expressions that provide scheduling abstractions, which are then compiled down to standard PyCSP$^3$/XCSP$^3$ constraints.

Result: Both formulations produce identical objectives on 72 doubly-proved optimal pairs across 17 model families. 8/17 families remain structurally unchanged after compilation. Runtime performance varies: up to 5.8x speedup on some families, but regression on others due to compilation decomposition overhead.

Conclusion: PyCSP$^3$ Scheduling successfully adds scheduling abstractions to PyCSP$^3$ while preserving the modeling/solving separation, though performance impacts vary depending on the specific problem structure and compilation overhead.

Abstract: PyCSP$^3$ provides a productive way to build constraint models for solving combinatorial constrained problems and export them to XCSP$^3$, preserving a complete separation between modeling and solving. However, it lacks native support for scheduling abstractions such as interval variables, sequence variables, and resource functions. As a result, scheduling models must be encoded with low-level integer variables and manual channeling constraints, even though PyCSP$^3$ already provides global constraints like NoOverlap and Cumulative on integer arrays. We present PyCSP$^3$ Scheduling, a library that adds scheduling abstractions to PyCSP$^3$ through 53 dedicated constraints and 27 expressions, and compiles them down to standard PyCSP$^3$/XCSP$^3$ constraints, maintaining the modeling/solving separation that underpins the PyCSP$^3$ ecosystem. On 261 paired instances across 17 model families (5 runs each), both formulations produce identical objectives on all 72 doubly-proved optimal pairs and nearly half of the families (8/17) remain structurally unchanged after compilation; however, runtime performance diverges across families, with clear gains on some (up to 5.8x) and regressions on others due to the overhead of compilation decompositions. Code and benchmarks are available at: https://github.com/sohaibafifi/pycsp3-scheduling

</details>


### [66] [Prompt Segmentation and Annotation Optimisation: Controlling LLM Behaviour via Optimised Segment-Level Annotations](https://arxiv.org/abs/2605.14561)
*Devika Prasad,Luke Gerschwitz,Tong Li,Henry Xiao,Anjin Liu,Coco Wu,Anna Leontjeva,Luiz Pizzato*

Main category: cs.AI

TL;DR: PSAO introduces a structured prompt optimization framework that decomposes prompts into interpretable segments and adds human-readable annotations to guide LLMs in focusing on important parts, improving reasoning accuracy and self-consistency.


<details>
  <summary>Details</summary>
Motivation: Existing prompt optimization methods operate over unstructured and vast prompt spaces, leading to high computational costs and potential distortion of original intent. There's a need for more controllable and efficient prompt optimization approaches.

Method: PSAO decomposes prompts into interpretable segments (e.g., sentences) and augments each with human-readable annotations (e.g., {not important}, {important}, {very important}). These annotations guide LLMs in allocating focus during response generation. The framework formally defines segmentations and annotations, retaining the original prompt as a candidate to prevent performance degradation.

Result: Empirical evaluations show PSAO benefits from annotations in terms of improved reasoning accuracy and self-consistency. The framework demonstrates feasibility of segment-level annotation optimization, though developing efficient methods for identifying optimal segmentations and annotations remains challenging.

Conclusion: PSAO serves as a proof of concept demonstrating the feasibility and potential of structured prompt optimization through segment-level annotations. While promising for improving LLM performance, finding optimal segmentation and annotation methods requires further investigation.

Abstract: Prompt engineering is crucial for effective interaction with generative artificial intelligence systems, yet existing optimisation methods often operate over an unstructured and vast prompt space, leading to high computational costs and potential distortions of the original intent. We introduce Prompt Segmentation and Annotation Optimisation (PSAO), a structured prompt optimisation framework designed to improve prompt optimisation controllability and efficiency. PSAO decomposes a prompt into interpretable segments (e.g., sentences) and augments each with human-readable annotations (e.g., {not important}, {important}, {very important}). These annotations guide large language models (LLMs) in allocating focus and clarifying confusion during response generation. We formally define the segmentations and annotations and demonstrate that optimised segment-level annotations can lead to improved LLM responses, with the original prompt retained as a candidate in the optimisation space to prevent performance degradation. Empirical evaluations indicate that PSAO benefits from annotations in terms of improved reasoning accuracy and self-consistency. However, developing efficient methods for identifying optimal segmentations and annotations remains challenging and is reserved for future investigation. This work is intended as a proof of concept, demonstrating the feasibility and potential of segment-level annotation optimisation.

</details>


### [67] [Sycophancy is an Educational Safety Risk: Why LLM Tutors Need Sycophancy Benchmarks](https://arxiv.org/abs/2605.14604)
*Enkelejda Kasneci,Gjergji Kasneci*

Main category: cs.AI

TL;DR: The paper identifies a "Reasoning-Sycophancy Paradox" where LLMs trade accuracy for agreeableness in tutoring, introduces the EduFrameTrap benchmark to test models' ability to maintain corrective friction under various social pressures, and finds frontier models struggle with authority and social-affective pressure despite resisting context-switch attacks.


<details>
  <summary>Details</summary>
Motivation: Effective tutoring requires "corrective friction" - surfacing and challenging misconceptions to drive conceptual change. However, preference-aligned Large Language Models (LLMs) often prioritize agreeableness over epistemic rigor, failing to correct students when they should. There's a need to understand and address this "Reasoning-Sycophancy Paradox" where models capitulate under social-epistemic pressure rather than providing supportive correction.

Method: The authors introduce EduFrameTrap, a tutoring benchmark across six domains (math, physics, economics, chemistry, biology, computer science) that varies student confidence and pressure types: context-switch frame attacks, authority pressure ("my notes say I'm right"), and social-affective face-saving ("please don't tell me I'm wrong"). They evaluate two frontier LLMs (GPT-5.2 and Claude) and use two-judge disagreement as a reliability signal since these failures are hard to judge automatically.

Result: Across frontier LLMs, context-switch failures are comparatively lower for GPT-5.2, while authority and social pressure more often trigger "epistemic retreat" (models backing down from correct positions). Claude shows substantial context-switch fragility. The study reveals that models that resist technical context-switch attacks can still fail under social-epistemic pressure, highlighting the "Reasoning-Sycophancy Paradox."

Conclusion: Benchmarks should measure "social-epistemic courage" - the ability to provide supportive but corrective tutoring - and treat kind-but-correct behavior as a safety requirement. The research calls for developing LLMs that can maintain epistemic integrity while being supportive, rather than trading accuracy for agreeableness in educational contexts.

Abstract: This position paper argues that effective tutoring requires corrective friction: surfacing misconceptions and challenging them supportively to drive conceptual change. Yet preference-aligned LLMs can trade epistemic rigor for agreeableness. We identify a Reasoning-Sycophancy Paradox: models that resist context-switch frame attacks can still capitulate under social-epistemic pressure, especially authority ("my notes say I'm right") and social-affective face-saving ("please don't tell me I'm wrong"). We introduce EduFrameTrap, a tutoring benchmark across math, physics, economics, chemistry, biology, and computer science that varies student confidence and pressure (context-switch, authority, social-affective). Across two frontier LLMs, context-switch failures are comparatively lower for GPT-5.2, while authority and social pressure more often trigger epistemic retreat. In contrast, Claude shows substantial context-switch fragility in this run. Because these failures are hard to judge automatically, we report two-judge disagreement as a reliability signal. We argue benchmarks should measure social-epistemic courage, i.e., supportive but corrective tutoring, and treat kind-but-correct behavior as a safety requirement.

</details>


### [68] [SliceGraph: Mapping Process Isomers in Multi-Run Chain-of-Thought Reasoning](https://arxiv.org/abs/2605.14619)
*Kang Chen,Junjie Nian,Yixin Cao,Yugang Jiang*

Main category: cs.AI

TL;DR: SliceGraph reveals that correct Chain-of-Thought reasoning often follows multiple distinct process families (process isomers) rather than converging on a single solution path, showing that final-answer aggregation overlooks structured multi-route process geometry.


<details>
  <summary>Details</summary>
Motivation: Current multi-run chain-of-thought reasoning analysis collapses to final-answer aggregates, discarding how sampled trajectories share, split, and rejoin through intermediate computations. There's a need to understand the rich process geometry underlying reasoning trajectories.

Method: SliceGraph: a post-hoc problem-model-cell graph built by mutual-kNN over sparse activation-key Jaccard similarity between CoT slices. This graph serves as a measurement object for process geometry rather than as a decoding program. Includes representation ablations, cross-architecture replication, and cross-scale replications.

Result: Across three primary 4B/8B models on math and science benchmarks: 85.5% of problem-model cells show correct CoTs splitting into multiple process families; 76.6% of run pairs are cross-family when at least two runs exist. Process isomers exist - same-answer, family-divergent correct trajectories. Success-associated regions split into disconnected high-value cores with route families specializing over these core footprints.

Conclusion: Final-answer aggregation overlooks structured multi-route process geometry in reasoning. Process families navigate the same computation atlas with distinct transition kernels, showing that reasoning follows multiple specialized routes rather than merely duplicating a single approach.

Abstract: Multi-run chain-of-thought reasoning is usually collapsed to final-answer aggregates, which discard howsampled trajectories share, split, and rejoin through intermediate computation. We propose SliceGraph, a post-hoc problem-model-cell graph built by mutual-kNN over sparse activation-key Jaccard similarity between CoT slices, and treat it as a measurement object for process geometry rather than as a decoding program. Across sampled CoT ensembles from three primary 4B/8B models on math and science benchmarks, blinded annotation supports SliceGraph biconnected components as shared reasoning-state units and process families as within-family strategy-coherent route units. In 85.5% of 954 problem-model cells, correct CoTs sharing the same normalized answer split into multiple process families; among cells with at least two such runs, 76.6% of run pairs are cross-family on average. We call such same-answer, family-divergent correct trajectories process isomers. A label-seeded reward field provides a separate value-landscape layer: success-associated regions often split into disconnected high-value cores, and route families specialize over these core footprints rather than merely duplicating one another. A typed-state transition analysis further shows that process families navigate the same atlas with distinct transition kernels under matched null controls. Representation ablations, a cross-architecture replication, and two cross-scale replications support the robustness of the route-family scaffold, showing that final-answer aggregation overlooks this structured multi-route process geometry.

</details>


### [69] [Teaching Large Language Models When Not to Know: Learning Temporal Critique for Ex-Ante Reasoning](https://arxiv.org/abs/2605.14636)
*Chenlu Ding,Jiancan Wu,Yanchen Luo,Zheyuan Liu,Yancheng Yuan,Xiang Wang*

Main category: cs.AI

TL;DR: LLMs struggle with temporal reasoning when asked to respond from earlier timeframes, leaking future knowledge. The paper proposes TCFT, a fine-tuning framework that teaches models to verify temporal admissibility and reduce leakage by 37-42 percentage points.


<details>
  <summary>Details</summary>
Motivation: LLMs frequently fail at ex-ante reasoning, exploiting knowledge that became available only after a temporal cutoff when prompted to answer from earlier time perspectives. This temporal leakage problem undermines reliable historical or temporally-constrained reasoning.

Method: First analyzes prompt-level interventions (cutoff formulations, instruction placement) to understand temporal leakage patterns. Then proposes TCFT (Temporal Critique Fine-Tuning), which trains models to identify post-cutoff leakage, explain temporal boundary violations, and judge temporal admissibility of responses relative to cutoff times.

Result: TCFT consistently outperforms prompting and supervised fine-tuning baselines, reducing average temporal leakage by 41.89 percentage points on Qwen2.5-7B-Instruct and 37.79 percentage points on Qwen2.5-14B-Instruct.

Conclusion: Prompting alone cannot solve temporal leakage as models lack verification capabilities for temporal admissibility. TCFT's structured fine-tuning approach successfully teaches models temporal verification skills, addressing the fundamental limitation that ex-ante correctness is a relation between answer and cutoff, not an intrinsic answer property.

Abstract: Large language models (LLMs) often fail to reason under temporal cutoffs: when prompted to answer from the standpoint of an earlier time, they exploit knowledge that became available only later. We study this failure through the lens of ex-ante reasoning, where a model must rely exclusively on information knowable before a cutoff. Through a systematic analysis of prompt-level interventions, we find that temporal leakage is highly sensitive to cutoff formulation and instruction placement: explicit cutoff statements outperform implicit historical framings, and prefix constraints reduce leakage more effectively than suffix constraints. These findings indicate that prompting can steer models into a temporal frame, but does not endow them with the ability to verify whether a response is temporally admissible. We further argue that supervised fine-tuning is insufficient, since ex-ante correctness is not an intrinsic property of an answer, but a relation between the answer and the cutoff. To address this gap, we propose TCFT, a Temporal Critique Fine-Tuning framework that trains models to acquire cutoff-aware temporal verification. Given a query, a cutoff, and a candidate response, TCFT teaches the model to identify post-cutoff leakage, explain temporal boundary violations, and judge temporal admissibility. Experiments with Qwen2.5-7B-Instruct and Qwen2.5-14B-Instruct show that TCFT consistently outperforms prompting and SFT baselines, reducing average leakage by 41.89 and 37.79 percentage points, respectively.

</details>


### [70] [MindGap: A Conversational AI Framework for Upstream Neuroplastic Intervention in Post-Traumatic Stress Disorder](https://arxiv.org/abs/2605.14660)
*Eranga Bandara,Ross Gore,Asanga Gunaratna,Ravi Mukkamala,Nihal Siriwardanagea,Sachini Rajapakse,Isurunima Kularathna,Pramoda Karunarathna,Wathsala Herath,Chalani Rajapakse,Sachin Shetty,Anita H. Clayton,Christopher K. Rhea,Ng Wee Keong,Kasun De Zoysa,Amin Hass,Shaifali Kaushik,Preston Samuel,Atmaram Yarlagadda*

Main category: cs.AI

TL;DR: MindGap is a privacy-preserving on-device AI framework that treats PTSD through neuroplastic rehabilitation using Buddhist dependent origination to intercept stress cascades before conscious awareness.


<details>
  <summary>Details</summary>
Motivation: Current PTSD therapies (prolonged exposure, EMDR, CBT) operate downstream of reactive stress cascades, teaching patients to tolerate or reframe distress after it arises. These suppression-based approaches don't produce lasting structural neural reorganization through upstream pathway dissolution.

Method: MindGap uses a privacy-preserving on-device conversational AI framework with a fine-tuned lightweight LLM. It guides patients through three progressive layers of observation at the "feeling tone gap" (between pre-cognitive affective signals and reactive elaboration): noticing bare affective signals, recognizing them as self-arising, and identifying conditioned implicit beliefs beneath feelings.

Result: The framework enables prefrontal regulatory engagement and long-term depression-mediated weakening of reactive pathways through daily calibrated exposure sessions, producing genuine upstream dissolution rather than downstream suppression. It runs entirely on-device with no data egress.

Conclusion: MindGap represents a novel neuroplastic approach to PTSD treatment that directly targets the root neurobiological mechanisms, making it deployable in sensitive clinical and military contexts where cloud-based solutions are prohibited.

Abstract: Post-Traumatic Stress Disorder (PTSD) is fundamentally a neuroplastic problem traumatic contact events encode over-reactive neural pathways through Hebbian long-term potentiation, producing hair-triggered amygdala-HPA stress cascades that fire before conscious awareness can intercept them. Existing therapeutic approaches, prolonged exposure, EMDR, cognitive behavioural therapy, operate predominantly downstream of the reactive cascade, teaching patients to tolerate or reframe distress after it has arisen. While clinically valuable, these suppression-based approaches do not produce the upstream pathway dissolution that constitutes lasting structural neural reorganisation. This paper proposes MindGap, a privacy-preserving on-device conversational AI framework that delivers structured neuroplastic rehabilitation for PTSD through the practice of dependent origination, a Buddhist psychological framework that identifies the precise moment between the pre-cognitive affective signal and the reactive elaboration that follows as the site of therapeutic intervention. MindGap guides patients through three progressive layers of observation at this feeling tone gap: noticing the bare affective signal before reactive elaboration, recognising it as self-arising rather than caused by the stimulus, and recognising the conditioned implicit belief beneath the feeling. Each layer corresponds to progressively deeper prefrontal regulatory engagement and progressively deeper long-term depression-mediated weakening of the reactive pathway, producing genuine upstream dissolution rather than downstream suppression. Running entirely on-device with no data egress, MindGap delivers daily calibrated exposure sessions through a fine-tuned lightweight large language model, making it deployable in sensitive clinical and military contexts where cloud-based solutions are not permitted.

</details>


### [71] [Falkor-IRAC: Graph-Constrained Generation for Verified Legal Reasoning in Indian Judicial AI](https://arxiv.org/abs/2605.14665)
*Joy Bose*

Main category: cs.AI

TL;DR: Falkor-IRAC is a graph-constrained generation framework for Indian legal AI that uses structured reasoning over an IRAC knowledge graph instead of semantic similarity search to prevent hallucinations and ensure valid legal reasoning.


<details>
  <summary>Details</summary>
Motivation: Vector-based RAG systems fail to properly represent legal reasoning (precedent propagation, procedural state transitions, statute-bound inference), leading to hallucinated precedents, outdated citations, and unsupported reasoning chains that have real consequences for access to justice in high-caseload jurisdictions like India.

Method: Ingests Indian court judgments as IRAC node structures enriched with procedural transitions, precedent relationships, and statutory references in FalkorDB. Uses a Verifier Agent that accepts LLM-generated answers only if a valid supporting path can be traced through the graph, and detects doctrinal conflicts as first-class output.

Result: Evaluated on 51 Supreme Court judgments using graph-native metrics (citation grounding accuracy, path validity rate, hallucinated precedent rate, conflict detection rate). The Verifier Agent correctly validated citations and rejected fabricated ones. Comparison with vector-only RAG baselines and GPU acceleration are left for future work.

Conclusion: Graph-constrained generation with structured reasoning paths provides more faithful representation of legal reasoning than vector-based RAG, with graph-native metrics being more appropriate for legal reasoning evaluation than traditional NLP metrics like BLEU and ROUGE.

Abstract: Legal reasoning is not semantic similarity search. A court judgment encodes constrained symbolic reasoning: precedent propagation, procedural state transitions, and statute-bound inference. These are properties that vector-based retrieval-augmented generation (RAG) cannot faithfully represent. Hallucinated precedents, outdated statute citations, and unsupported reasoning chains remain persistent failure modes in LLM-based legal AI, with real consequences for access to justice in high-caseload jurisdictions such as India. This paper presents Falkor-IRAC, a graph-constrained generation framework for Indian legal AI that grounds generation in structured reasoning over an IRAC (Issue, Rule, Analysis, Conclusion) knowledge graph. Judgments from the Supreme Court and High Courts of India are ingested as IRAC node structures enriched with procedural state transitions, precedent relationships, and statutory references, stored in FalkorDB for low-latency agentic traversal. At inference time, LLM-generated answers are accepted only if a valid supporting path can be traced through the graph, a check performed by a falsifiability oracle called the Verifier Agent. The system also detects doctrinal conflicts as a first-class output rather than silently resolving them. Falkor-IRAC is evaluated using graph-native metrics: citation grounding accuracy, path validity rate, hallucinated precedent rate, and conflict detection rate. These metrics are argued to be more appropriate for legal reasoning evaluation than BLEU and ROUGE. On a proof-of-concept corpus of 51 Supreme Court judgments, the Verifier Agent correctly validated citations on completed queries and correctly rejected fabricated citations. Evaluation against vector-only RAG baselines is left for future work, as is GPU-accelerated inference to address current timeout rates on CPU hardware.

</details>


### [72] [Monitoring Data-aware Temporal Properties (Extended Version)](https://arxiv.org/abs/2605.14666)
*Alessandro Gianola,Marco Montali,Sarah Winkler*

Main category: cs.AI

TL;DR: Anticipatory monitoring framework for LTLfMT (LTL over finite traces with SMT theories) combining automata and automated reasoning to handle complex dynamic systems where internal specifications are unavailable.


<details>
  <summary>Details</summary>
Motivation: Dynamic AI systems are often complex and heterogeneous with inaccessible internal specifications, making traditional verification techniques like model checking inapplicable. Monitoring becomes an attractive alternative but requires handling both temporal aspects and rich first-order constraints over finite traces.

Method: Novel anticipatory monitoring framework for expressive fragment of LTLfMT combining automata-theoretic methods (for temporal aspects) with automated reasoning techniques (for first-order/SMT theory dimension). Introduces decidable fragments combining linear arithmetic with uninterpreted functions.

Result: Formally proved correctness of monitoring framework under reasonable theory assumptions. Identified practically relevant decidable fragments covering data-aware business processes and systems with read-only databases. Feasibility demonstrated via prototype implementation and preliminary evaluation.

Conclusion: Anticipatory monitoring of LTLfMT properties is feasible through a hybrid approach combining temporal automata with SMT reasoning, enabling verification of complex dynamic systems where traditional model checking fails, with practical applications in business processes and database systems.

Abstract: Dynamic systems in AI are often complex and heterogeneous, so that an internal specification is not accessible and verification techniques such as model checking are not applicable. Monitoring is in such cases an attractive alternative, as it evaluates desirable properties along traces generated by an unknown dynamic system. In this work, we consider anticipatory monitoring of linear-time properties enriched with an arbitrary SMT theory over finite traces (LTLfMT). Anticipatory monitoring in this setting is highly challenging, as the monitoring state depends on both the trace prefix seen so far and all its possible finite continuations. Under reasonable assumptions on the background theory, we present and formally prove the correctness of a novel foundational framework for monitoring properties in an expressive fragment of LTLfMT. The framework combines automata-theoretic methods to handle the temporal aspects of the logic, with automated reasoning techniques to address the first-order dimension. Moreover, we identify for the first time decidable fragments of this monitoring problem that are practically relevant as they combine linear arithmetic with uninterpreted functions, which covers e.g. data-aware business processes and dynamic systems operating over a read-only database. Feasibility is witnessed by a prototype implementation and preliminary evaluation.

</details>


### [73] [How Sensitive Are Radiomic AI Models to Acquisition Parameters?](https://arxiv.org/abs/2605.14667)
*D. Gil,I. Sanchez,C. Sanchez*

Main category: cs.AI

TL;DR: A framework for quantifying scan parameter sensitivity of radiomic AI models to improve cross-dataset robustness, applied to lung cancer diagnosis in CT scans to identify optimal acquisition parameters that balance diagnostic quality with radiation dose.


<details>
  <summary>Details</summary>
Motivation: The main barrier for deploying AI radiomic systems in clinical routine is their performance drop under heterogeneous multicentre acquisition protocols, necessitating methods to identify parameter regions that improve cross-dataset robustness.

Method: A mixed-effects framework for quantifying the influence of clinically relevant acquisition parameters on model performance while accounting for subject-level random effects, applied to lung cancer diagnosis using two independent multicentre datasets and state-of-the-art architectures.

Result: Identified optimal CT configuration: tube current >= 200 mA, spiral pitch <= 1.5, slice thickness <= 1.25 mm. This improved performance from 0.79±0.04 sensitivity, 0.47±0.10 specificity in low-quality scans to 0.90±0.10 sensitivity, 0.79±0.13 specificity in high-quality scans.

Conclusion: The proposed performance-oriented framework successfully identifies clinically significant CT acquisition parameters that enhance cross-dataset robustness of radiomic AI models, enabling more reliable deployment in heterogeneous clinical settings while balancing diagnostic quality with radiation safety.

Abstract: A main barrier for the deployment of AI radiomic systems in clinical routine is their drop in performance under heterogeneous multicentre acquisition protocols. This work presents a performance-oriented framework for quantifying scan parameter sensitivity of radiomic AI models, while identifying clinically significant parameter regions associated with improved cross-dataset robustness. We formulate a mixed-effects framework for quantifying the influence that clinically relevant acquisition parameters have on models performance, while accounting for subject-level random effects. We have applied our framework to lung cancer diagnosis in CT scans using two independent multicentre datasets (a public database and own-collected data) and several SoA architectures. To evaluate across-database reproducibility, CT parameters have been adjusted using the data collected and tested on the public set. The optimal configuration selected is the current of the X-ray tube >= 200 mA, spiral pitch <= 1.5, slice thickness <= 1.25 mm, which balances diagnostic quality with low radiation dose. These configuration push metrics from 0.79+-0.04 sensitivity, 0.47+-0.10 specificity in low quality scans to 0.90+-0.10 sensitivity, 0.79 +- 0.13 specificity in high quality ones.

</details>


### [74] [$π$-Bench: Evaluating Proactive Personal Assistant Agents in Long-Horizon Workflows](https://arxiv.org/abs/2605.14678)
*Haoran Zhang,Luxin Xu,Zhilin Wang,Runquan Gui,Shunkai Zhang,Haodi Lei,Zihao He,Bingsu He,Chicheng Qin,Tong Zhu,Xiaoye Qu,Yang Yang,Yu Cheng,Yafu Li*

Main category: cs.AI

TL;DR: π-Bench is a new benchmark for evaluating proactive assistance in personal AI agents, featuring 100 multi-turn tasks with hidden intents and cross-session dependencies to better simulate real-world interactions.


<details>
  <summary>Details</summary>
Motivation: Existing benchmarks don't adequately evaluate agents' ability to identify and act on hidden user intents before they're explicitly stated, especially in sustained multi-turn interactions where user needs emerge gradually. This gap is critical since users often begin with underspecified requests in real-world personal assistant scenarios.

Method: Developed π-Bench, comprising 100 multi-turn tasks across 5 domain-specific user personas. The benchmark incorporates hidden user intents, inter-task dependencies, and cross-session continuity to evaluate agents' proactive assistance capabilities in extended interactions.

Result: Experiments show: (1) proactive assistance remains challenging for current agents, (2) there's a clear distinction between task completion and proactivity (agents can complete tasks without being proactive), and (3) prior interactions are valuable for resolving proactive intents in later tasks.

Conclusion: π-Bench addresses a critical gap in evaluating personal assistant agents by measuring both proactivity and task completion in long-horizon trajectories, better reflecting real-world use and highlighting the importance of proactive assistance capabilities.

Abstract: The rise of personal assistant agents, e.g., OpenClaw, highlights the growing potential of large language models to support users across everyday life and work. A core challenge in these settings is proactive assistance, since users often begin with underspecified requests and leave important needs, constraints, or preferences unstated. However, existing benchmarks rarely evaluate whether agents can identify and act on such hidden intents before they are explicitly stated, especially in sustained multi-turn interactions where user needs emerge gradually. To address this gap, we introduce $π$-Bench, a benchmark for proactive assistance comprising 100 multi-turn tasks across 5 domain-specific user personas. By incorporating hidden user intents, inter-task dependencies, and cross-session continuity, $π$-Bench evaluates agents' ability to anticipate and address user needs over extended interactions, jointly measuring proactivity and task completion in long-horizon trajectories that better reflect real-world use. Experiments show (1) proactive assistance remains challenging, (2) a clear distinction between task completion and proactivity, and (3) the value of prior interaction for proactive intent resolution in later tasks.

</details>


### [75] [On Strong Equivalence Notions in Logic Programming and Abstract Argumentation](https://arxiv.org/abs/2605.14721)
*Giovanni Buraglio,Wolfgang Dvorak,Stefan Woltran*

Main category: cs.AI

TL;DR: The paper introduces a new notion of strong equivalence for logic programs that restores compatibility with argumentation frameworks, bridging the gap that exists in dynamic contexts between these semantically equivalent formalisms.


<details>
  <summary>Details</summary>
Motivation: Strong equivalence is crucial in nonmonotonic reasoning but existing alignments between logic programs and argumentation frameworks break down in dynamic contexts due to different update notions. This discrepancy prevents strong equivalence from carrying over between these formalisms.

Method: The authors carefully investigate the discrepancy between strong equivalence notions in logic programming and argumentation, then introduce a new notion of strong equivalence for logic programs specifically designed to bridge this gap.

Result: The new strong equivalence notion preserves strong equivalence under translation between certain classes of logic programs and both Dung-style and claim-augmented argumentation frameworks, thus restoring compatibility across these formalisms.

Conclusion: The introduced strong equivalence concept successfully reconciles the previously incompatible dynamic behaviors of logic programs and argumentation frameworks, allowing strong equivalence to properly carry over between these semantically equivalent formalisms in dynamic contexts.

Abstract: Strong equivalence between knowledge bases ensures the possibility of replacing one with the other without affecting reasoning outcomes, in any given context. This makes it a crucial property in nonmonotonic formalisms. In particular, the fields of logic programming and abstract argumentation provide primary examples in which this property has been subject to vast investigations. However, while (classes of) logic programs and abstract argumentation frameworks are known to be semantically equivalent in static settings, this alignment breaks in dynamic contexts due to differing notions of update. As a result, strong equivalence does not always carry over from one formalism to the other. In this paper, we carefully investigate this discrepancy and introduce a new notion of strong equivalence for logic programs. Our approach preserves strong equivalence under translation between certain classes of logic programs and both Dung-style and claim-augmented argumentation frameworks, thus restoring compatibility across these formalisms.

</details>


### [76] [Agentifying Patient Dynamics within LLMs through Interacting with Clinical World Model](https://arxiv.org/abs/2605.14723)
*Minghao Wu,Yuting Yan,Zhenyang Cai,Ke Ji,Chuangsen Fang,Ziying Sheng,Xidong Wang,Rongsheng Wang,Hejia Zhang,Shuang Li,Benyou Wang,Hongyuan Zha*

Main category: cs.AI

TL;DR: SepsisAgent: A world model-augmented LLM agent that uses clinical simulations to recommend sepsis treatments, outperforming traditional RL and LLM methods through propose-simulate-refine workflow.


<details>
  <summary>Details</summary>
Motivation: Sepsis management requires sequential decisions under rapidly changing patient conditions. While LLMs have clinical knowledge and can reason over guidelines, they lack grounding in action-conditioned patient dynamics and real-time physiological responses.

Method: SepsisAgent uses a learned Clinical World Model to simulate patient responses to fluid-vasopressor interventions, following a propose-simulate-refine workflow. The agent is trained through a three-stage curriculum: 1) patient-dynamics supervised fine-tuning, 2) propose-simulate-refine behavior cloning, and 3) world-model-based agentic reinforcement learning.

Result: On MIMIC-IV sepsis trajectories, SepsisAgent outperforms all traditional RL and LLM-based baselines in off-policy value while achieving the best safety profile under guideline adherence and unsafe-action metrics. The agent learns regularities in patient evolution that remain useful even when simulator access is removed.

Conclusion: World model-augmented LLM agents can effectively integrate clinical knowledge with patient dynamics for sepsis treatment recommendations. Agent-specific training is essential for leveraging world models, and learned regularities from clinical simulations provide lasting benefits for decision-making.

Abstract: Sepsis management in the ICU requires sequential treatment decisions under rapidly evolving patient physiology. Although large language models (LLMs) encode broad clinical knowledge and can reason over guidelines, they are not inherently grounded in action-conditioned patient dynamics. We introduce SepsisAgent, a world model-augmented LLM agent for sepsis treatment recommendation. SepsisAgent uses a learned Clinical World Model to simulate patient responses under candidate fluid--vasopressor interventions, and follows a propose--simulate--refine workflow before committing to a prescription. We first show that world-model access alone yields inconsistent LLM decision performance, motivating agent-specific training. We then train SepsisAgent through a three-stage curriculum: patient-dynamics supervised fine-tuning, propose--simulate--refine behavior cloning, and world-model-based agentic reinforcement learning. On MIMIC-IV sepsis trajectories, SepsisAgent outperforms all traditional RL and LLM-based baselines in off-policy value while achieving the best safety profile under guideline adherence and unsafe-action metrics. Further analysis shows that repeated interaction with the Clinical World Model enables the agent to learn regularities in patient evolution, which remain useful even when simulator access is removed.

</details>


### [77] [XDomainBench: Diagnosing Reasoning Collapse in High-Dimensional Scientific Knowledge Composition](https://arxiv.org/abs/2605.14754)
*Gong Zhiren,Tiantong Wu,Jiaming Zhang,Fuyao Zhang,Che Wang,Yurong Hao,Yikun Hou,Foo Ping,Yilei Zhao,Fei Huang,Chau Yuen,Wei Yang Bryan Lim*

Main category: cs.AI

TL;DR: XDomainBench is a new diagnostic benchmark for evaluating how well LLMs handle interactive interdisciplinary scientific reasoning across 20 domains, revealing systematic reasoning collapse as composition complexity increases.


<details>
  <summary>Details</summary>
Motivation: Existing LLM benchmarks for knowledge synthesis focus on single-turn, restricted scenarios that don't capture the challenges of real-world interactive scientific workflows where composition order and interdisciplinary reasoning are crucial.

Method: Created XDomainBench with formalized composition order and mixture structure for systematic stress-testing. Includes 8,598 interactive sessions across 20 domains and 4 task categories, with 8 realistic trajectory patterns covering difficulty and domain-mixture dynamics.

Result: Large-scale evaluation shows systematic reasoning collapse as composition order increases, caused by: (i) direct difficulty increases from domain composition, and (ii) indirect interaction-amplified failures where trajectory patterns trigger error accumulation, reasoning breaks, and domain confusion.

Conclusion: Current LLMs have significant limitations in handling compositional generalization for interdisciplinary scientific reasoning, highlighting the need for better benchmarks and models that can manage the complexity of real-world AI4S scenarios.

Abstract: Large Language Models (LLMs) are increasingly deployed for knowledge synthesis, yet their capacity for compositional generalization in scientific knowledge remains under-characterized. Existing benchmarks primarily focus on single-turn restricted scenarios, failing to capture the capability boundaries exposed by real-world interactive scientific workflows. To address this, we introduce XDomainBench, a diagnostic benchmark for interactive interdisciplinary scientific reasoning. We formalize the composition order and mixture structure to enable systematic stress-testing from single-discipline to inter-disciplinary, comprising 8,598 interactive sessions across 20 domains and 4 task categories, with 8 realistic trajectory patterns covering difficulty and domain-mixture dynamics, simulating real AI4S scenarios. Large-scale evaluation of LLMs reveals a systematic reasoning collapse as composition order increases, stemming from two root causes: (i) direct difficulty increases induced by domain composition, and (ii) indirect interaction-amplified failures where trajectory patterns trigger error accumulation, reasoning breaks, and domain confusion, ultimately leading to session collapse.

</details>


### [78] [Probabilistic Verification of Recurrent Neural Networks for Single and Multi-Agent Reinforcement Learning](https://arxiv.org/abs/2605.14758)
*Luca Marzari,Enrico Marchesini*

Main category: cs.AI

TL;DR: RNN-ProVe: A probabilistic verification framework that estimates the likelihood of undesired behaviors in RNN-based reinforcement learning policies using policy-driven sampling and statistical error bounds.


<details>
  <summary>Details</summary>
Motivation: Existing RNN verification tools for partially observable RL rely on restrictive modeling assumptions or coarse over-approximations of hidden state spaces, leading to overly conservative or inconclusive results. There's a need for better verification methods that can handle the complexity of RNN-based policies.

Method: RNN-ProVe uses policy-driven sampling to approximate the set of hidden states that are feasible under a trained RNN-based policy, then derives statistical error bounds to produce bounded-error, high-confidence estimates of behavioral violations.

Result: Experiments on partially observable single-agent and cooperative multi-agent tasks show that RNN-ProVe yields more quantitative, feasibility-aware probabilistic guarantees than existing tools, while scaling effectively to recurrent and multi-agent settings.

Conclusion: RNN-ProVe provides a practical probabilistic verification framework for RNN-based RL policies that addresses limitations of existing methods, offering bounded-error guarantees that account for the feasibility of hidden states under actual policy execution.

Abstract: History-dependent policies induced by recurrent neural networks (RNNs) rely on latent hidden state dynamics, making verification in partially observable reinforcement learning (RL) challenging. Existing RNN verification tools typically rely on restrictive modeling assumptions or coarse over-approximations of the hidden state space, which can lead to overly conservative or inconclusive results. We propose $\textbf{RNN}$ $\textbf{Pro}$babilistic $\textbf{Ve}$rification ($\texttt{RNN-ProVe}$), a probabilistic framework that $\textit{estimates the likelihood}$ of undesired behaviors in RNN-based policies. $\texttt{RNN-ProVe}$ uses policy-driven sampling to approximate the set of hidden states that are feasible under a trained policy, and derives statistical error bounds to produce bounded-error, high-confidence estimates of behavioral violations. Experiments on partially observable single-agent and cooperative multi-agent tasks show that $\texttt{RNN-ProVe}$ yields more quantitative, feasibility-aware probabilistic guarantees than existing tools, while scaling to recurrent and multi-agent settings.

</details>


### [79] [AI Outperforms Humans in Personalized Image Aesthetics Assessment via LLM-Based Interviews and Semantic Feature Extraction](https://arxiv.org/abs/2605.14761)
*Yoshia Abe,Tatsuya Daikoku,Yasuo Kuniyoshi*

Main category: cs.AI

TL;DR: An integrated Deep Learning-Large Language Model system that predicts individual aesthetic preferences by combining low-level visual features with high-level semantic features extracted through LLM-based interviews, outperforming conventional methods and even human predictions.


<details>
  <summary>Details</summary>
Motivation: Aesthetic preferences are subjective and individual-dependent, making accurate prediction challenging. Current DL models only extract objective low-level features and don't account for high-level semantic understanding or individual preference collection.

Method: Developed an integrated DL-LLM system that: 1) uses LLMs to conduct semi-structured interviews to actively elicit individual aesthetic preferences, 2) extracts both low-level visual features through DL and high-level semantic features, and 3) combines these for aesthetic evaluation prediction.

Result: The proposed system outperforms conventional systems, human predictors, and the target individual's own re-evaluations over time. It shows particularly strong performance on highly-rated images and has smaller prediction error than within-person variability. Human predictors showed the largest errors.

Conclusion: AI may be better positioned than others or one's future self to capture individual aesthetic preferences at a given point. This raises the question of whether AI could serve as a deeper interpreter of human aesthetic sensibility than humans themselves.

Abstract: Accurately predicting individual aesthetic evaluation for images is a fundamental challenge for AI. Various deep learning (DL)-based models have been proposed for this task, training on image evaluation data to extract objective low-level features. However, aesthetic preferences are inherently subjective and individual-dependent. Accurate prediction thus requires the extraction of high-level semantic features of images and the active collection of preference information from the target individual. To address this issue, we focus on the utility of Large Language Models (LLMs) pretrained on vast amounts of textual data, and develop an integrated DL-LLM system. The system actively elicits aesthetic preferences through LLM-based semi-structured interviews and predicts aesthetic evaluation by leveraging both low-level and high-level features. In our experiments, we compare the proposed system against conventional systems, human predictors, and the target individual's own re-evaluations after a certain time interval. Our results show that the proposed system outperforms all of them, with particularly strong performance on highly-rated images. Moreover, the prediction error of the proposed system is smaller than within-person variability, while human predictors show the largest error, likely due to the influence of their own aesthetic values. These results suggest that AI may be better positioned than others or one's future self to capture individual aesthetic preferences at a given point. This opens a new question of whether AI could serve as a deeper interpreter of human aesthetic sensibility than humans themselves.

</details>


### [80] [MediaClaw: Multimodal Intelligent-Agent Platform Technical Report](https://arxiv.org/abs/2605.14771)
*Shaoan Zhao,Huanlin Gao,Qiang Hui,Ting Lu,Xueqiang Guo,Yantao Li,Xinpei Su,Fuyuan Shi,Chao Tan,Fang Zhao,Kai Wang,Shiguo Lian*

Main category: cs.AI

TL;DR: MediaClaw is a multimodal agent platform built on OpenClaw that addresses AIGC deployment challenges through a three-layer architecture for unified abstraction, pluginized extensions, and workflow orchestration.


<details>
  <summary>Details</summary>
Motivation: The paper addresses practical deployment pain points in AIGC adoption, including fragmented capabilities, heterogeneous interfaces, disconnected production processes, and limited reuse of high-quality production workflows.

Method: The system uses a three-layer architecture: (1) unified abstraction layer that abstracts full-category AIGC capabilities into a unified invocation model, (2) pluginized extension layer that supports hot-pluggable capability expansion, and (3) workflow orchestration layer using task-oriented Skills to turn complex production processes into reusable workflow assets.

Result: MediaClaw provides a multimodal agent platform that addresses AIGC deployment challenges, though specific performance metrics or evaluation results aren't provided in the abstract - the focus appears to be on architectural design and engineering implementation.

Conclusion: The report provides a reusable practical reference for building multimodal capability platforms by sharing the architectural design philosophy, core capability model design logic, and key engineering trade-offs from implementing MediaClaw.

Abstract: MediaClaw is a multimodal agent platform built on the OpenClaw ecosystem. Its core design follows a three-layer architecture of unified abstraction, pluginized extension, and workflow orchestration. The system is intended to address practical deployment pain points in AIGC adoption, including fragmented capabilities, heterogeneous interfaces, disconnected production processes, and limited reuse of high-quality production workflows. \system{} abstracts full-category AIGC capabilities into a unified invocation model, uses plugins to support hot-pluggable capability expansion, and uses task-oriented Skills to turn complex production processes into reusable workflow assets. This report focuses on the architectural design philosophy of MediaClaw, the design logic of its core capability model, and the key engineering trade-offs in implementation. It aims to provide reusable practical reference for building multimodal capability platforms.

</details>


### [81] [Identifying Culprits Through Deep Deterministic Policy Gradient Deep Learning Investigation](https://arxiv.org/abs/2605.14774)
*Lata B T,Savitha N J*

Main category: cs.AI

TL;DR: This paper proposes using the Deep Deterministic Policy Gradient (DDPG) deep learning algorithm to improve criminal identification from complex datasets, achieving 95% accuracy in identifying criminals.


<details>
  <summary>Details</summary>
Motivation: Current criminal investigation methods rely on limited data analysis and struggle with identifying criminals from complex datasets while minimizing false positives and false negatives. There's a need for more optimal and efficient methods in AI-powered crime investigation.

Method: The paper introduces a novel approach using the Deep Deterministic Policy Gradient (DDPG) deep learning algorithm. The DDPG model is trained on a dataset containing crime scene material, witness statements, and suspect profiles. The algorithm extracts features to maximize offender identification likelihood while minimizing noise and irrelevant data impact.

Result: The proposed DDPG method achieved 95% accuracy in identifying criminals, which significantly outperformed several existing methods. The approach demonstrates effectiveness in handling complex crime investigation datasets.

Conclusion: The Deep Deterministic Policy Gradient algorithm shows promising results for criminal identification applications, offering a more accurate and efficient solution compared to conventional methods. The 95% accuracy rate indicates strong potential for practical implementation in crime investigation systems.

Abstract: In the world of AI and advanced technologies investigation aspects identification of a crime or criminal plays a major problem. In this research we focus on a Conventional ways of implicating criminal investigations usually rely on limited data analysis. Finding an optimal and efficient method that will effectively identify criminals from complex datasets and minimise false positives and false negatives is the considered as a challenge. The main novelty approach of this work is based on the deep learning algorithm Deep Deterministic Policy Gradient (DDPG) is presented in this paper. We train the DDPG model with a dataset of crime scene material, witness statements and suspect profiles. The algorithm uses features to maximise the likelihood of identifying the offender while minimising the noise impact and irrelevant data. We show the efficacy of the proposed method, where DDPG identified criminals with an amazing accuracy of 95% than other several existing methods.

</details>


### [82] [A Heterogeneous Temporal Memory Governance Framework for Long-Term LLM Persona Consistency](https://arxiv.org/abs/2605.14802)
*Zhao Yang,Wang Huan,Li Yingshuo,Tu Haomiao,Lin Hujite*

Main category: cs.AI

TL;DR: ARPM is an external temporal memory governance framework that addresses long-term dialogue issues like fact loss, timeline confusion, and persona drift through separated static/dynamic memory, multimodal retrieval, and evidence verification protocols.


<details>
  <summary>Details</summary>
Motivation: Large language models suffer from fact loss, timeline confusion, persona drift, and reduced stability during long-range interactions, especially under high-noise knowledge bases, context clearing, and cross-model transfer scenarios.

Method: ARPM separates static knowledge memory from dynamic dialogue experience memory and combines vector retrieval, BM25, RRF fusion, dual-temporal reranking, chronological evidence reading, and controlled analysis protocols for evidence verification and answer binding.

Result: Experiments show: 1) Under 1:5 SNR, CSV recall accuracy is 54.0% vs manual 100.0%; under 1:200+, 44.0% vs 80.0%. 2) Disabling dialogue history retrieval reduces strict accuracy from 100% to 66.7%; disabling BM25 reduces to 80.0%. 3) ARPM maintains continuity under 5.1M-character noise, periodic clearing, and multi-model handoff.

Conclusion: Long-term persona consistency can be decomposed into governable components and evaluated in a white-box manner, with ARPM treating continuity as a traceable, auditable, and transferable governance problem rather than encoding it into model weights.

Abstract: Large language models often suffer from fact loss, timeline confusion, persona drift, and reduced stability during long-range interaction, especially under high-noise knowledge bases, context clearing, and cross-model transfer. To address these issues, we introduce ARPM, an external temporal memory governance framework for long-term dialogue. ARPM separates static knowledge memory from dynamic dialogue experience memory and combines vector retrieval, BM25, RRF fusion, dual-temporal reranking, chronological evidence reading, and a controlled analysis protocol for evidence verification and answer binding. Unlike approaches that encode persona consistency into model weights or rely only on long context, ARPM treats continuity as a traceable, auditable, and transferable governance problem. Using engineering logs, we conduct three experiments. First, in a 50-round question-answering setting, we compare signal-to-noise ratios of 1:5 and 1:200+, and distinguish CSV auto-judgment from manual review. Under 1:5, CSV recall accuracy is 54.0%, while manual review raises it to 100.0%. Under 1:200+, the values are 44.0% and 80.0%. These results show that automatic rules can underestimate recall after supporting evidence enters the prompt. Second, ablation results show that dialogue history retrieval is necessary for recent continuity: disabling it reduces strict accuracy from 100% to 66.7%, and disabling BM25 reduces it to 80.0%, indicating that pure semantic retrieval is insufficient for correction and tracing. Third, under a 5.1-million-character noise substrate, periodic context clearing, and multi-model handoff, ARPM maintains semantic continuity, boundary continuity, and persona consistency, while exposing limits caused by weak protocol compliance. These findings show that long-term persona consistency can be decomposed into governable components and evaluated in a white-box manner.

</details>


### [83] [Interestingness as an Inductive Heuristic for Future Compression Progress](https://arxiv.org/abs/2605.14831)
*Vincent Herrmann,Jürgen Schmidhuber*

Main category: cs.AI

TL;DR: Interestingness as inductive heuristic for compression progress is theoretically viable and empirically supported; Algorithmic Prior yields quadratic improvement in discovery predictions.


<details>
  <summary>Details</summary>
Motivation: The paper addresses a bottleneck in developing recursively self-improving systems: the challenge of "interestingness" - the ability to prospectively identify which tasks or data have potential for future progress.

Method: Formalizes interestingness as an inductive heuristic for future compression progress using Kolmogorov Complexity and Algorithmic Statistics tools. Analyzes complexity-runtime profiles under Length, Algorithmic, and Speed priors across three universal computational paradigms.

Result: 1. The inductive property of interestingness (past progress signaling future discovery) is theoretically viable and empirically supported. 2. Expected future progress depends exponentially on recency of last observed breakthrough. 3. Algorithmic Prior is significantly more optimistic than Length Prior, yielding quadratic increase in expected discovery for same observed profile.

Conclusion: Interestingness as an inductive heuristic for identifying progress opportunities in recursively self-improving systems is both theoretically sound and empirically demonstrable, with Algorithmic Prior offering superior predictive power.

Abstract: One of the bottlenecks on the way towards recursively self-improving systems is the challenge of interestingness: the ability to prospectively identify which tasks or data hold the potential for future progress. We formalize interestingness as an inductive heuristic for future compression progress and investigate its predictability using tools from Kolmogorov Complexity and Algorithmic Statistics. By analyzing complexity-runtime profiles under Length, Algorithmic, and Speed priors, we demonstrate that the inductive property of interestingness -- the capacity for past progress to signal future discovery -- is theoretically viable and empirically supported. We prove that expected future progress depends exponentially on the recency of the last observed breakthrough. Furthermore, we show that the Algorithmic Prior is significantly more optimistic than the Length Prior, yielding a quadratic increase in expected discovery for the same observed profile. These findings are experimentally confirmed across three diverse universal computational paradigms.

</details>


### [84] [Emotion-Attended Stateful Memory (EASM):The Architecture for Hyper-Personalization at Scale](https://arxiv.org/abs/2605.14833)
*Vineet Kotecha,Vansh Gupta*

Main category: cs.AI

TL;DR: Stateful memory architecture with emotional attention significantly outperforms stateless LLMs in personalization across all conversational scenarios, with largest gains in memory grounding, plan clarity, and emotional validation.


<details>
  <summary>Details</summary>
Motivation: Current language models are fundamentally stateless across sessions, limiting their ability to personalize interactions over time. While retrieval-augmented generation and fine-tuning help with knowledge access and domain capability, they don't enable persistent understanding of individual users.

Method: Proposed an emotion-attended stateful memory architecture that dynamically constructs user-specific conversational context using long-term history, emotional signals, and inferred intent at inference time. Conducted a controlled A/B study across 30 non-scripted conversations spanning six emotionally distinct categories using the same underlying language model in both conditions.

Result: The memory-enriched condition consistently outperformed the stateless baseline across all evaluated scenarios. Largest gains: memory grounding (95% improvement), plan clarity (57%), and emotional validation (34%). Results remained consistent even in emotionally adversarial conversations involving grief, distress, and uncertainty.

Conclusion: Stateful emotional memory may represent a foundational infrastructure layer for hyper-personalized AI systems, though broader validation across larger and more diverse evaluations remains necessary.

Abstract: Current language model systems remain fundamentally stateless across sessions, limiting their ability to personalize interactions over time. While retrieval-augmented generation and fine-tuning improve knowledge access and domain capability, they do not enable persistent understanding of individual users. We propose an emotion-attended stateful memory architecture that dynamically constructs user-specific conversational context using long-term history, emotional signals, and inferred intent at inference time. To evaluate its impact, we conducted a controlled A/B study across thirty non-scripted conversations spanning six emotionally distinct categories using the same underlying language model in both conditions. The memory-enriched condition consistently outperformed the stateless baseline across all evaluated scenarios. The largest gains were observed in memory grounding (95% improvement), plan clarity (57%), and emotional validation (34%). Results remained consistent even in emotionally adversarial conversations involving grief, distress, and uncertainty. These findings suggest that stateful emotional memory may represent a foundational infrastructure layer for hyper-personalized AI systems, though broader validation across larger and more diverse evaluations remains necessary

</details>


### [85] [A Deterministic Agentic Workflow for HS Tariff Classification: Multi-Dimensional Rule Reasoning with Interpretable Decisions](https://arxiv.org/abs/2605.14857)
*Yu Zhang,Dongjiang Zhuang,Qu Zhou,Zheng Huang,Junhe Wu,Jing Cao,Kai Chen*

Main category: cs.AI

TL;DR: A deterministic agentic workflow for HS tariff classification that uses fixed control flow and narrow LLM calls achieves superior performance by addressing multi-dimensional rule reasoning constraints.


<details>
  <summary>Details</summary>
Motivation: HS tariff classification is complex due to multi-dimensional rule reasoning where competing priority rules (material, form, function, essential character, etc.) must be satisfied simultaneously. End-to-end LLM prompting fails by addressing only one dimension while ignoring priority constraints on others.

Method: Deterministic agentic workflow with fixed control flow, narrow LLM calls confined to specific stages, and local reflection/verification mechanisms. Combines offline knowledge-engineering of Chinese HS tariff with six-stage online pipeline, producing structured outputs with verbatim citations of relevant chapter/section notes.

Result: Achieves 75.0% top-1 and 91.5% top-3 at 4-digit level, 64.2% top-1 and 78.3% top-3 at 6-digit level with Qwen3.6-plus. Open-weight Qwen3.6-27B-FP8 achieves 84.2% 4-digit and 77.4% 6-digit top-1 agreement with frontier model. Audit suggests some HSCodeComp ground-truth labels may deviate from HS general rules.

Conclusion: The deterministic agentic workflow outperforms end-to-end LLM approaches by properly handling multi-dimensional rule reasoning constraints in HS classification. The method provides interpretability through structured outputs with citations, and findings suggest potential issues with existing benchmark labels that warrant community review.

Abstract: Harmonized System (HS) tariff classification is a high-stakes, expert-level task in which a free-form product description must be mapped to a specific six- or eight-digit code under the General Interpretive Rules (GIR), section notes, chapter notes, and Explanatory Notes. The difficulty lies not in knowledge volume but in *multi-dimensional rule reasoning*: a correct classification must satisfy competing priority rules along several axes simultaneously, including material, form, function, essential character, the part-versus-whole boundary, and specific listing versus residual headings. End-to-end prompting of large language models fails characteristically by resolving one axis while ignoring the priority constraints on the others. We present a *deterministic agentic workflow* in contrast to self-planning agents: the control flow is fixed, language model calls are confined to narrow stages, and reflection and verification are retained as local mechanisms. This design yields interpretability by construction--each decision is decomposed into stage-wise structured outputs with verbatim citation of the chapter or section notes that bear on it. The architecture combines offline knowledge-engineering of the Chinese HS tariff with an online six-stage pipeline. Evaluated on HSCodeComp at the six-digit level, the workflow reaches 75.0% top-1 and 91.5% top-3 at four digits, and 64.2% top-1 and 78.3% top-3 at six digits with Qwen3.6-plus; an open-weight Qwen3.6-27B-FP8 backbone in non-thinking mode achieves 84.2% four-digit and 77.4% six-digit top-1 agreement with the frontier model. A two-stage manual audit of 226 six-digit disagreements suggests that a non-trivial fraction of HSCodeComp ground-truth labels may deviate from HS general rules; full adjudication records are released in the appendix as preliminary findings for community review.

</details>


### [86] [Holistic Evaluation and Failure Diagnosis of AI Agents](https://arxiv.org/abs/2605.14865)
*Netta Madvil,Gilad Dym,Alon Mecilati,Edo Dekel,Jonatan Liberman,Rotem Brazilay,Liron Schliesser,Max Svidlo,Shai Nir,Orel Shalom,Yaron Friedman,David Connack,Amos Rimon,Philip Tannor,Shir Chorev*

Main category: cs.AI

TL;DR: A new agent evaluation framework that combines top-down diagnosis with bottom-up span-level analysis to precisely identify where and why failures occur in multi-step AI agent processes.


<details>
  <summary>Details</summary>
Motivation: Current AI agent evaluation methods are insufficient: outcome metrics only report success/failure without explaining why, and process-level approaches fail to connect failure types to their exact locations within long, structured agent execution traces.

Method: A holistic agent evaluation framework that pairs top-down agent-level diagnosis with bottom-up span-level evaluation, decomposing analysis into independent per-span assessments that scale to traces of arbitrary length and produce span-level rationales.

Result: State-of-the-art results on TRAIL benchmark across GAIA and SWE-Bench, with relative gains up to 38% on category F1, 3.5x on localization accuracy, and 12.5x on joint localization-categorization accuracy. The framework leads in more error categories than any other evaluator.

Conclusion: The same frontier model achieves several times higher localization accuracy when used inside the framework than as a monolithic judge, demonstrating that evaluation methodology - not model capability - is the primary bottleneck in AI agent assessment.

Abstract: AI agents execute complex multi-step processes, but current evaluation falls short: outcome metrics report success or failure without explaining why, and process-level approaches struggle to connect failure types to their precise locations within long, structured traces. We present a holistic agent evaluation framework that pairs top-down agent-level diagnosis with bottom-up span-level evaluation, decomposing analysis into independent per-span assessments. This decomposition scales to traces of arbitrary length and produces span-level rationales for each verdict. On the TRAIL benchmark, our framework achieves state-of-the-art results across all metrics on both GAIA and SWE-Bench, with relative gains over the strongest prior baselines of up to 38% on category F1, up to 3.5x on localization accuracy, and up to 12.5x on joint localization-categorization accuracy. Per-category analysis shows our framework leading in more error categories than any other evaluator. Notably, the same frontier model achieves several times higher localization accuracy when used inside our framework than as a monolithic judge over the full trace, showing that evaluation methodology, not model capability, is the bottleneck.

</details>


### [87] [BiFedKD: Bidirectional Federated Knowledge Distillation Framework for Non-IID and Long-Tailed ECG Monitoring](https://arxiv.org/abs/2605.14886)
*Zixuan Shu,Tiancheng Cao,Hen-Wei Huang*

Main category: cs.AI

TL;DR: BiFedKD: A bidirectional federated knowledge distillation framework that improves ECG arrhythmia classification under non-IID data while reducing communication and computation costs compared to traditional federated learning.


<details>
  <summary>Details</summary>
Motivation: ECG monitoring in IoMT networks faces privacy constraints and bandwidth limitations. Traditional federated learning has high communication overhead, while federated distillation suffers performance degradation under non-IID and long-tailed ECG data distributions.

Method: Proposes BiFedKD framework using bidirectional federated knowledge distillation with aggregation-by-distillation pipeline and temperature scaling to create stable global distillation signals for cross-client alignment.

Result: On MIT-BIH Arrhythmia dataset: 3.52% accuracy improvement, 9.93% Macro-F1 improvement. Reduces communication overhead by 40% and computation cost by 71.7% to achieve same Macro-F1 as baseline.

Conclusion: BiFedKD effectively addresses non-IID and long-tailed distribution challenges in ECG federated learning while significantly reducing communication and computation costs, making it suitable for practical IoMT deployments.

Abstract: Electrocardiogram (ECG) monitoring in Internet of Medical Things (IoMT) networks is constrained by strict data-sharing regulations and privacy concerns. Federated learning (FL) enables collaborative learning by keeping raw ECG data on devices, but frequent transmissions of high-dimensional model updates incur heavy per-round traffic over bandwidth-limited links. To alleviate this bottleneck, federated distillation (FD) replaces parameter exchange with logit-based knowledge transfer. However, the performance of FD often degrades under the non-independent and identically distributed (non-IID) and long-tailed label distributions in ECG deployments. To address these challenges, we propose a bidirectional federated knowledge distillation (BiFedKD) framework that employs an aggregation-by-distillation pipeline with temperature scaling to produce a stable global distillation signal for cross-client alignment. Experiments on the MIT-BIH Arrhythmia dataset show that BiFedKD improves accuracy and Macro-F1 over the baseline by $3.52\%$ and $9.93\%$, respectively. Moreover, to reach the same Macro-F1, BiFedKD reduces communication overhead by $40\%$ and computation cost by $71.7\%$ compared with the baseline.

</details>


### [88] [Beyond Individual Intelligence: Surveying Collaboration, Failure Attribution, and Self-Evolution in LLM-based Multi-Agent Systems](https://arxiv.org/abs/2605.14892)
*Shihao Qi,Jie Ma,Rui Xing,Wei Guo,Xiao Huang,Zhitao Gao,Jianhao Deng,Jun Liu,Lingling Zhang,Bifan Wei,Boqian Yang,Pinghui Wang,Jianwen Sun,Jing Tao,Yaqiang Wu,Hui Liu,Yu Yao,Tongliang Liu*

Main category: cs.AI

TL;DR: This survey proposes a unified LIFE framework for multi-agent systems, connecting capability foundation, agent collaboration, fault attribution, and autonomous self-improvement as causally linked stages for developing self-organizing collective intelligence.


<details>
  <summary>Details</summary>
Motivation: Current LLM-based agents have strong individual capabilities but struggle with sustained coordination in multi-agent systems. While multi-agent collaboration addresses this, it introduces error propagation risks that are difficult to diagnose and rarely lead to structural improvements. Existing research fragments the study of individual agent capabilities, collaboration, and self-improvement without examining their causal dependencies.

Method: The survey organizes research around the LIFE progression framework: 1) Lay the capability foundation (individual agent abilities), 2) Integrate agents through collaboration (multi-agent coordination), 3) Find faults through attribution (error diagnosis and tracing), and 4) Evolve through autonomous self-improvement (structural adaptation). For each stage, the authors provide systematic taxonomies and formally characterize dependencies between adjacent stages.

Result: The survey synthesizes existing work into a unified framework that reveals how each stage both depends on and constrains the next. It identifies open challenges at stage boundaries and proposes a cross-stage research agenda for developing closed-loop multi-agent systems capable of continuous failure diagnosis, structural reorganization, and agent behavior refinement.

Conclusion: The LIFE framework bridges previously fragmented research threads, offering both a systematic reference and conceptual roadmap toward autonomous, self-improving multi-agent intelligence. This approach extends current coordination frameworks toward more self-organizing forms of collective intelligence by enabling causal understanding of error propagation and structural adaptation.

Abstract: LLM-based autonomous agents have demonstrated strong capabilities in reasoning, planning, and tool use, yet remain limited when tasks require sustained coordination across roles, tools, and environments. Multi-agent systems address this through structured collaboration among specialized agents, but tighter coordination also amplifies a less explored risk: errors can propagate across agents and interaction rounds, producing failures that are difficult to diagnose and rarely translate into structural self-improvement. Existing surveys cover individual agent capabilities, multi-agent collaboration, or agent self-evolution separately, leaving the causal dependencies among them unexamined. This survey provides a unified review organized around four causally linked stages, which we term the LIFE progression: Lay the capability foundation, Integrate agents through collaboration, Find faults through attribution, and Evolve through autonomous self-improvement. For each stage, we provide systematic taxonomies and formally characterize the dependencies between adjacent stages, revealing how each stage both depends on and constrains the next. Beyond synthesizing existing work, we identify open challenges at stage boundaries and propose a cross-stage research agenda for closed-loop multi-agent systems capable of continuously diagnosing failures, reorganizing structures, and refining agent behaviors, extending current coordination frameworks toward more self-organizing forms of collective intelligence. By bridging these previously fragmented research threads, this survey aims to offer both a systematic reference and a conceptual roadmap toward autonomous, self-improving multi-agent intelligence.

</details>


### [89] [COREKG: Coreset-Guided Personalized Summarization of Knowledge Graphs](https://arxiv.org/abs/2605.14900)
*Sohel Aman Khan,Raghava Mutharaju,Supratim Shit*

Main category: cs.AI

TL;DR: COREKG applies coreset theory to create personalized Knowledge Graph summaries by sampling relevant triples based on user query patterns, achieving better query accuracy and structural coverage than state-of-the-art methods while using only a small fraction of the original graph.


<details>
  <summary>Details</summary>
Motivation: Large Knowledge Graphs (KGs) become unwieldy for tasks like question answering and visualization. Personalized KG summarization is crucial to capture specific user requirements based on query patterns, reducing storage requirements and improving query runtime by maintaining only relevant information.

Method: Adapts coreset theory for personalized KG summarization. Uses sensitivity-based importance sampling to select a relevant subset of triples that approximates the full dataset with bounded approximation error. Defines sensitivity scores measuring the importance of each triple with respect to a user's query workload, then constructs coresets independently for each user based on their query behavior.

Result: Evaluation on Freebase, WikiData, and DBpedia shows COREKG delivers higher query-answering accuracy and structural coverage than state-of-the-art methods (GLIMPSE, PPR, iSummary, PEGASUS, APEX²) while requiring only a tiny fraction of the original graph.

Conclusion: The coreset-based approach to personalized KG summarization effectively addresses the challenge of large knowledge graphs by creating compact, query-specific summaries that maintain high accuracy and coverage, demonstrating practical advantages over existing methods.

Abstract: Knowledge Graphs (KGs) are extensively used across different domains and in several applications. Often, these KGs are very large in size. Such KGs become unwieldy for tasks such as question answering and visualization. Summarization of KGs offers a viable alternative in such cases. Furthermore, personalized KG summarization is crucial in the current data-driven world as it captures the specific requirements of users based on their query patterns. Since it only maintains relevant information, the personalized summaries of KG are small, resulting in significantly smaller storage requirements and query runtime. In this work, we adapt the coreset theory to create personalized KG summaries. For a given dataset and a user-specific query workload, we present an approach that samples a relevant subset of triples using sensitivity-based importance sampling. We ensure that the subset approximates the characteristics of the full dataset with bounded approximation error. We define sensitivity scores that measure the importance of a triple with respect to a user's query workload, which are then used by our coreset construction algorithm. We explicitly focus on personalized knowledge graph summarization by constructing summaries independently for each user based on their query behaviour.
  Our evaluation on Freebase, WikiData, and DBpedia shows that COREKG delivers higher query-answering accuracy and structural coverage than the state-of-the-art methods, such as GLIMPSE, PPR, iSummary, PEGASUS and APEX$^2$ while requiring only a tiny fraction of the original graph.

</details>


### [90] [KGPFN: Unlocking the Potential of Knowledge Graph Foundation Model via In-Context Learning](https://arxiv.org/abs/2605.14907)
*Yisen Gao,Jiaxin Bai,Haoyu Huang,Zhongwei Xie,Yufei Li,Hong Ting Tsang,Sirui Han,Yangqiu Song*

Main category: cs.AI

TL;DR: KGPFN is a knowledge graph foundation model that unifies transferable relational learning with in-context learning by encoding both local query neighborhoods and global relation-specific context, achieving strong adaptation to unseen graphs through inference-time learning alone.


<details>
  <summary>Details</summary>
Motivation: Current KG foundation models primarily focus on relation-level universality while neglecting in-context learning, which is crucial for KG reasoning. KGs have inherently structured and heterogeneous contexts that require both local query entity neighborhoods and global relation patterns for effective prediction.

Method: KGPFN uses a Prior-data Fitted Network framework with three key components: 1) Learning relation representations via message passing on relation graphs to capture cross-graph relational invariances, 2) Encoding local neighborhoods using multi-layer NBFNet for query-specific reasoning, and 3) Constructing relation-specific global context by retrieving instances of query relations and aggregating them using feature-level and sample-level attention.

Result: The model demonstrates strong adaptation to previously unseen graphs through in-context learning alone, consistently outperforming competitive fine-tuned KG foundation models across 57 KG benchmarks.

Conclusion: KGPFN successfully unifies transferable relational regularities with inference-time in-context learning, learning when to apply reusable patterns versus when to override them with contextual evidence, establishing an effective KG foundation model approach.

Abstract: Knowledge graph (KG) foundation models aim to generalize across graphs with unseen entities and relations by learning transferable relational structure. However, most existing methods primarily emphasize relation-level universality, while in-context learning, the other pillar of foundation models remains under-explored for KG reasoning. In KGs, context is inherently structured and heterogeneous: effective prediction requires conditioning on the local context around the query entities as well as the global context that summarizes how a relation behaves across many instances. We propose KGPFN, a KG foundation model using Prior-data Fitted Network that unifies transferable relational regularities with inference-time in-context learning from structured context. KGPFN first learns relation representations via message passing on relation graphs to capture cross-graph relational invariances. For query-specific reasoning, it encodes local neighborhoods using a multi-layer NBFNet as local context. To enable ICL at global scale, it constructs relation-specific global context by retrieving a large set of instances of the query relation together with their local neighborhoods, and aggregates them within a Prior-Data Fitted Network framework that combines feature-level and sample-level attention. Through multi-graph pretraining on diverse KGs, KGPFN learns when to instantiate reusable patterns and when to override them using contextual evidence. Experiments on 57 KG benchmarks demonstrate that KGPFN achieves strong adaptation to previously unseen graphs through in-context learning alone, consistently outperforming competitive fine-tuned KG foundation models. Our code is available at https://github.com/HKUST-KnowComp/KGPFN.

</details>


### [91] [From Sycophantic Consensus to Pluralistic Repair: Why AI Alignment Must Surface Disagreement](https://arxiv.org/abs/2605.14912)
*Varad Vishwarupe,Nigel Shadbolt,Marina Jirotka*

Main category: cs.AI

TL;DR: The paper argues that current AI alignment approaches fail at pluralism by being sycophantic (agreeing with users), proposes reframing pluralism around conversational mechanisms (scoping, signalling, repair), and introduces a metric to measure principled revision vs capitulation.


<details>
  <summary>Details</summary>
Motivation: Current RLHF-trained AI assistants suffer from "sycophantic consensus" - they tend to agree with, validate, and minimize friction with users rather than genuinely engage with diverse values. This is problematic because AI systems mediate important discussions in health, civic life, labor, and governance, and the collapse of disagreement has serious distributive consequences.

Method: 1) Reframe pluralistic alignment around three conversational mechanisms from Grice's maxims: scoping (acknowledging perspective limits), signalling (surfacing value conflicts), and repair (principled revision). 2) Formalize a metric called Pluralistic Repair Score (PRS) to distinguish principled revision from capitulation. 3) Conduct empirical study on two frontier RLHF models (Claude Sonnet 4.5 and GPT-4o) using contested-value prompts.

Result: The empirical study (N=198 for Claude, N=100 for GPT-4o) shows that both models exhibit agreement-following behavior alongside low repair-quality on contested-value prompts, demonstrating that current alignment approaches fail at enabling principled disagreement and revision.

Conclusion: Pluralism isn't just about preference aggregation but requires conversational mechanisms that allow visible disagreement and principled revision. The paper argues that pluralism is ultimately made or unmade at the deployment-governance layer through interfaces, preference-data pipelines, and audit infrastructure, not just at the technical level.

Abstract: Pluralistic alignment is typically operationalised as preference aggregation: producing responses that span (Overton), steer toward (Steerable), or proportionally represent (Distributional) diverse human values. We argue that aggregation alone is an incomplete primitive for deployed pluralistic alignment. Under genuine value pluralism, the failure mode of contemporary RLHF-trained assistants is not insufficient coverage but sycophantic consensus: a learned tendency to agree with, validate, and minimise friction with the immediate interlocutor. Because deployed AI systems now mediate consequential deliberation across health, civic life, labour, and governance, the collapse of disagreement at the interaction layer is not a narrow technical concern but a structural failure with distributive consequences. We reframe pluralistic alignment around three conversational mechanisms drawn from Grice's maxims: scoping (acknowledging the limits of one's perspective), signalling (surfacing value-conflict rather than smoothing it over), and repair (revising one's position on principled grounds, not on user pressure). We formalise a metric, the Pluralistic Repair Score (PRS), distinguishing principled revision from capitulation, and present a small-scale empirical illustration on two frontier RLHF-trained models (Claude Sonnet 4.5, N=198; GPT-4o, N=100) showing that, for both, agreement-following coexists with low repair-quality on contested-value prompts. PRS measures an interactional precondition for pluralism (visible disagreement; principled revision) rather than pluralism in full; we discuss the difference, take seriously the reflexive question of whose "principled" counts, and argue that pluralism is most decisively made or unmade at the deployment-governance layer: interfaces, preference-data pipelines, and audit infrastructure.

</details>


### [92] [GraphFlow: An Architecture for Formally Verifiable Visual Workflows Enabling Reliable Agentic AI Automation](https://arxiv.org/abs/2605.14968)
*Drewry H. Morris,Luis Valles,Reza Hosseini Ghomi*

Main category: cs.AI

TL;DR: GraphFlow is a visual workflow system that improves reliability of AI automation by treating workflow diagrams as executable specifications with verifiable contracts and explicit trust boundaries.


<details>
  <summary>Details</summary>
Motivation: Current agentic AI systems in multi-step, mission-critical processes have low reliability due to compounding errors - a 10-step process with 90% per-step reliability only completes 35% of the time. Existing systems lack semantic correctness guarantees and are difficult to audit.

Method: GraphFlow uses workflow diagrams as executable specifications with compile-time verification of contracts (preconditions, postconditions, composition obligations) for reusable automations. It employs swimlanes to make trust boundaries explicit and includes a durable runtime engine with append-only event logs for replay, retries, and audit.

Result: A year-long pilot across three clinical sites executed 8,728 workflow runs with 97.08% completion rate using an early prototype without verified-core subsystem. Failures were primarily localized to external integrations.

Conclusion: GraphFlow demonstrates a promising approach to improving AI automation reliability through formal specifications and verification. The verified core system is under active development and future work will evaluate its effectiveness.

Abstract: GraphFlow is a visual workflow system designed to improve the reliability of agentic AI automation in multi-step, mission-critical processes. In these workflows, small errors compound rapidly: under an idealized model of independent steps, a ten-step process with 90% per-step reliability completes successfully only 35% of the time. Existing workflow platforms provide durable execution and observability but offer few semantic correctness guarantees, while agentic systems plan at inference time, making behavior sensitive to prompt variation and difficult to audit.
  GraphFlow is designed to address this gap by treating workflow diagrams as the executable specification, a single artifact defining data scope, execution semantics, and monitoring. At compile time, a restricted class of diagrams is specified to produce reusable automations whose contracts (preconditions, postconditions, and composition obligations) are intended to be proof-checked before admission to a shared library. At runtime, a durable engine records outcomes in an append-only event log and can enforce contracts at system boundaries, supporting replay, retries, and audit. Swimlanes make trust boundaries explicit, separating verified logic from external systems, human judgment, and AI decisions.
  A year-long pilot across three clinical sites executed 8,728 cohort-enrolled workflow runs with a 97.08% completion rate under an early prototype without the verified-core subsystem; observed failures were localized primarily to external integrations. The formal semantics and proof-checked admission model described here are specified and under active development. Evaluation of the verified core is reserved for future work.

</details>


### [93] [Explainable Detection of Depression Status Shifts from User Digital Traces](https://arxiv.org/abs/2605.14995)
*Loris Belcastro,Francesco Gervino,Fabrizio Marozzo,Domenico Talia,Paolo Trunfio*

Main category: cs.AI

TL;DR: The paper proposes an explainable framework that combines BERT-based models and LLMs to detect and analyze depression-related status shifts in users' digital traces, generating interpretable reports about mental health signal evolution over time.


<details>
  <summary>Details</summary>
Motivation: Users generate timestamped digital traces (social media posts, chats, online interactions) that may reflect their mental state evolution, including phases of improvement, deterioration, or stability. There's a need for explainable frameworks that can detect and analyze these depression-related status shifts in an interpretable way.

Method: The framework combines multiple BERT-based models to extract complementary signals across different dimensions (sentiment, emotion, depression severity). These signals are aggregated over time to construct user-level trajectories, which are analyzed to identify meaningful change points. To enhance interpretability, a large language model is integrated to generate concise, human-readable reports describing the evolution of mental-health signals and highlighting key transitions.

Result: Evaluation on two social media datasets shows the approach produces more coherent and informative summaries than direct LLM-based reporting, achieving higher coverage of user history, stronger temporal coherence, and improved sensitivity to change points. Ablation study confirms the contribution of each component, particularly temporal modeling and segmentation.

Conclusion: The method provides an interpretable view of mental health signals over time, supporting research and decision making, though it explicitly avoids aiming at clinical diagnosis. The framework effectively combines BERT-based signal extraction with LLM-based report generation to create coherent, temporally-aware mental health analyses from digital traces.

Abstract: Every day, users generate digital traces (e.g., social media posts, chats, and online interactions) that are inherently timestamped and may reflect aspects of their mental state. These traces can be organized into temporal trajectories that capture how a user's mental health signals evolve, including phases of improvement, deterioration, or stability. In this work, we propose an explainable framework for detecting and analyzing depression-related status shifts in user digital traces. The approach combines multiple BERT-based models to extract complementary signals across different dimensions (e.g., sentiment, emotion, and depression severity). Such signals are then aggregated over time to construct user-level trajectories that are analyzed to identify meaningful change points. To enhance interpretability, the framework integrates a large language model to generate concise and human-readable reports that describe the evolution of mental-health signals and highlight key transitions. We evaluate the framework on two social media datasets. Results show that the approach produces more coherent and informative summaries than direct LLM-based reporting, achieving higher coverage of user history, stronger temporal coherence, and improved sensitivity to change points. An ablation study confirms the contribution of each component, particularly temporal modeling and segmentation. Overall, the method provides an interpretable view of mental health signals over time, supporting research and decision making without aiming at clinical diagnosis.

</details>


### [94] [Learning Developmental Scaffoldings to Guide Self-Organisation](https://arxiv.org/abs/2605.14998)
*Milton L. Montero,Elias Najarro,Jakob Schauser,Sebastian Risi*

Main category: cs.AI

TL;DR: Neural Cellular Automata paired with learned coordinate-based pattern generators can jointly learn both self-organization rules and developmental pre-patterns, showing improved robustness and encoding capacity over purely self-organizing systems.


<details>
  <summary>Details</summary>
Motivation: Natural systems often offload information to initial conditions rather than generating it through pure self-organization. Biological development exemplifies this through maternal pre-patterns that scaffold self-organizing processes, creating a memory-compute trade-off analogous to computational systems.

Method: Develop a model combining Neural Cellular Automaton (NCA) with learned coordinate-based pattern generator (SIREN), trained simultaneously to generate target patterns. Use information-theoretic analyses to measure information distribution between pre-patterns and self-organizing processes.

Result: Jointly learning both pre-patterns and self-organization rules yields improvements in robustness, encoding capacity, and symmetry breaking compared to purely self-organizing alternatives. Effective pre-patterns don't simply approximate targets but bias developmental dynamics to facilitate convergence.

Conclusion: The relationship between initial condition structure and self-organization dynamics is non-trivial, with pre-patterns playing a crucial role in developmental processes by offloading information to initial conditions, similar to computational memory-compute trade-offs.

Abstract: From subcellular structures to entire organisms, many natural systems generate complex organisation through self-organisation: local interactions that collectively give rise to global structure without any blueprint of the outcome. Yet a significant portion of the information driving such processes is not produced by self-organisation itself, instead, it is often offloaded to initial conditions of the system. Biological development is a prime example, where maternal pre-patterns encode positional and symmetry-breaking information that scaffolds the self-organising process. From maternal morphogen gradients in early embryogenesis to tissue-level morphogenetic pre-patterns guiding organ formation, this transfer of information to initial conditions, analogous to a memory-compute trade-off in computational systems, is a fundamental part of developmental processes. In this work, we study this offloading phenomenon by introducing a model that jointly learns both the self-organisation rules and the pre-patterns, allowing their interplay to be varied and measured under controlled conditions: a Neural Cellular Automaton (NCA) paired with a learned coordinate-based pattern generator (SIREN), both trained simultaneously to generate a set of patterns. We provide information-theoretic analyses of how information is distributed between pre-patterns and the self-organising process, and show that jointly learning both components yields improvements in robustness, encoding capacity, and symmetry breaking over purely self-organising alternatives. Our analysis further suggests that effective pre-patterns do not simply approximate their targets; rather, they bias the developmental dynamics in ways that facilitate convergence, pointing to a non-trivial relationship between the structure of initial conditions and the dynamics of self-organisation.

</details>


### [95] [Small, Private Language Models as Teammates for Educational Assessment Design](https://arxiv.org/abs/2605.15015)
*Chris Davis Jaldi,Anmol Saini,Shan Zhang,Noah Schroeder,Cogan Shimizu,Eleni Ilkou*

Main category: cs.AI

TL;DR: SLMs achieve competitive performance with LLMs for educational question generation while enabling local, privacy-sensitive deployment, though model-based evaluations show systematic biases compared to expert ratings.


<details>
  <summary>Details</summary>
Motivation: Existing research on AI for educational assessment design has limitations: relies on subjective/limited evaluation methods, focuses on proprietary LLMs, and rarely examines real-world deployment constraints. SLMs offer privacy and resource advantages but their effectiveness for assessment tasks remains underexplored.

Method: Systematic comparison of LLMs vs SLMs for assessment question design; evaluation of generation quality across Bloom's taxonomy levels using reproducible, pedagogically grounded metrics; assessment of model-based judging against expert-informed evaluation by analyzing reliability and agreement patterns.

Result: SLMs achieve competitive performance across key pedagogically motivated quality dimensions while enabling local, privacy-sensitive deployment. However, model-based evaluations exhibit systematic inconsistencies and bias relative to expert ratings.

Conclusion: Language models should be positioned as bounded assistants in assessment workflows, necessitating Human-in-the-Loop approaches. The findings advance automated educational question generation by examining quality, reliability, and deployment-aware trade-offs between different model types.

Abstract: Generative AI increasingly supports educational design tasks, e.g., through Large Language Models (LLMs), demonstrating the capability to design assessment questions that are aligned with pedagogical frameworks (e.g., Bloom's taxonomy). However, they often rely on subjective or limited evaluation methods; focus primarily on proprietary models; or rarely systematically examine generation, evaluation, or deployment constraints in real educational settings. Meanwhile, Small Language Models (SLMs) have emerged as local alternatives that better address privacy and resource limitations; yet their effectiveness for assessment tasks remains underexplored. To address this gap, we systematically compare LLMs and SLMs for assessment question design; evaluate generation quality across Bloom's taxonomy levels using reproducible, pedagogically grounded metrics; and further assess model-based judging against expert-informed evaluation by analyzing reliability and agreement patterns. Results show that SLMs achieve competitive performance across key pedagogically motivated quality dimensions while enabling local, privacy-sensitive deployment. However, model-based evaluations also exhibit systematic inconsistencies and bias relative to expert ratings. These findings provide evidence to posit language models as bounded assistants in assessment workflows; underscore the necessity of Human-in-the-Loop; and advance the automated educational question generation field by examining quality, reliability, and deployment-aware trade-offs.

</details>


### [96] [Orchard: An Open-Source Agentic Modeling Framework](https://arxiv.org/abs/2605.15040)
*Baolin Peng,Wenlin Yao,Qianhui Wu,Hao Cheng,Xiao Yu,Rui Yang,Tao Ge,Alessandrio Sordoni,Xingdi Yuan,Yelong Shen,Pengcheng He,Tong Zhang,Zhou Yu,Jianfeng Gao*

Main category: cs.AI

TL;DR: Orchard is an open-source framework for scalable agentic modeling featuring a lightweight environment service and three specialized agent training recipes for coding, GUI interaction, and personal assistant tasks.


<details>
  <summary>Details</summary>
Motivation: Current open research in agentic modeling is constrained by infrastructure and training gaps, with high-performing systems relying on proprietary codebases while open-source frameworks focus mainly on orchestration rather than scalable agent training.

Method: Orchard framework with Orchard Env (lightweight environment service) and three agentic modeling recipes: Orchard-SWE for coding agents using distilled trajectories and credit-assignment SFT with Balanced Adaptive Rollout RL; Orchard-GUI for vision-language computer-use agents with distilled trajectories; Orchard-Claw for personal assistant agents with synthetic tasks.

Result: Orchard-SWE achieves 64.3% (SFT) and 67.5% (SFT+RL) on SWE-bench Verified; Orchard-GUI achieves 74.1%, 67.0%, and 64.0% success rates on WebVoyager, Online-Mind2Web, and DeepShop; Orchard-Claw achieves 59.6% pass@3 on Claw-Eval and 73.9% with stronger harness.

Conclusion: A lightweight, open, harness-agnostic environment layer enables reusable agentic data, training recipes, and evaluations across domains, establishing state-of-the-art performance among open-source models while remaining competitive with proprietary systems.

Abstract: Agentic modeling aims to transform LLMs into autonomous agents capable of solving complex tasks through planning, reasoning, tool use, and multi-turn interaction with environments. Despite major investment, open research remains constrained by infrastructure and training gaps. Many high-performing systems rely on proprietary codebases, models, or services, while most open-source frameworks focus on orchestration and evaluation rather than scalable agent training. We present Orchard, an open-source framework for scalable agentic modeling. At its core is Orchard Env, a lightweight environment service providing reusable primitives for sandbox lifecycle management across task domains, agent harnesses, and pipeline stages. On top of Orchard Env, we build three agentic modeling recipes. Orchard-SWE targets coding agents. We distill 107K trajectories from MiniMax-M2.5 and Qwen3.5-397B, introduce credit-assignment SFT to learn from productive segments of unresolved trajectories, and apply Balanced Adaptive Rollout for RL. Starting from Qwen3-30B-A3B-Thinking, Orchard-SWE achieves 64.3% on SWE-bench Verified after SFT and 67.5% after SFT+RL, setting a new state of the art among open-source models of comparable size. Orchard-GUI trains a 4B vision-language computer-use agent using only 0.4K distilled trajectories and 2.2K open-ended tasks. It achieves 74.1%, 67.0%, and 64.0% success rates on WebVoyager, Online-Mind2Web, and DeepShop, respectively, making it the strongest open-source model while remaining competitive with proprietary systems. Orchard-Claw targets personal assistant agents. Trained with only 0.2K synthetic tasks, it achieves 59.6% pass@3 on Claw-Eval and 73.9% when paired with a stronger ZeroClaw harness. Collectively, these results show that a lightweight, open, harness-agnostic environment layer enables reusable agentic data, training recipes, and evaluations across domains.

</details>


### [97] [Case-Based Calibration of Adaptive Reasoning and Execution for LLM Tool Use](https://arxiv.org/abs/2605.15041)
*Renning Pang,Tian Lan,Leyuan Liu,Piao Tong,Sheng Cao,Xiaosong Zhang*

Main category: cs.AI

TL;DR: CAST is a case-driven framework that uses historical tool-use execution trajectories to guide adaptive reasoning and improve structural validity in large language model tool use.


<details>
  <summary>Details</summary>
Motivation: Tool use extends LLMs beyond parametric knowledge, but reliable execution requires balancing appropriate reasoning depth with strict structural validity. Current approaches struggle with determining optimal reasoning strategies and avoiding structural breakdowns.

Method: CAST treats historical execution trajectories as structured cases, extracting complexity profiles to estimate optimal reasoning strategies and failure profiles to map likely structural breakdowns. It translates this knowledge into fine-grained reward design and adaptive reasoning, enabling models to internalize case-based strategies during reinforcement learning.

Result: Experiments on BFCLv2 and ToolBench show CAST improves schema-faithful execution and task-level tool-use success while reducing unnecessary deliberation. Achieves up to 5.85 percentage points gain in overall execution accuracy and reduces average reasoning length by 26%, significantly mitigating high-impact structural errors.

Conclusion: Historical execution cases can provide reusable adaptation knowledge for calibrated tool use, demonstrating that case-based approaches effectively balance reasoning depth with structural validity in LLM tool-use systems.

Abstract: Tool use extends large language models beyond parametric knowledge, but reliable execution requires balancing appropriate reasoning depth with strict structural validity. We approach this problem from a case-based perspective to present CAST, a case-driven framework that treats historical execution trajectories as structured cases. Instead of reusing raw exemplar outputs, CAST extracts case-derived signals to identify complexity profiles for estimating optimal reasoning strategies, alongside failure profiles to map likely structural breakdowns. The framework translates this knowledge into a fine-grained reward design and adaptive reasoning, enabling the model to autonomously internalize case-based strategies during reinforcement learning. Experiments on BFCLv2 and ToolBench demonstrate that CAST improves both schema-faithful execution and task-level tool-use success while reducing unnecessary deliberation. The approach achieves up to 5.85 percentage points gain in overall execution accuracy and reduces average reasoning length by 26%, significantly mitigating high-impact structural errors. Ultimately, this demonstrates how historical execution cases can provide reusable adaptation knowledge for calibrated tool use.

</details>


### [98] [Dual-Dimensional Consistency: Balancing Budget and Quality in Adaptive Inference-Time Scaling](https://arxiv.org/abs/2605.15100)
*Rongman Xu,Yifei Li,Tianzhe Zhao,Yanrui Wu,Bo Li,Hang Yan*

Main category: cs.AI

TL;DR: DDC is a unified framework that bridges path quality with adaptive termination to optimize LLM reasoning efficiency by reducing token consumption by over 10x while maintaining accuracy.


<details>
  <summary>Details</summary>
Motivation: Current LLM reasoning strategies face inefficient trade-offs between sampling budget and reasoning quality, with width consensus methods risking reinforcement of hallucinations and depth pruning mechanisms prematurely truncating valid reasoning chains.

Method: Proposes Dual-Dimensional Consistency (DDC) framework that couples Confidence-Weighted Bayesian protocol with Trend-Aware Stratified Pruning to concentrate computational resources on high-quality reasoning paths, filtering hallucinations while accelerating consensus.

Result: Evaluation across five benchmarks shows the approach reduces token consumption by over 10 times while maintaining or exceeding accuracy of strong baselines across various LLMs.

Conclusion: DDC provides an effective solution for optimizing LLM reasoning efficiency by bridging path quality with adaptive termination, demonstrating significant computational savings while preserving reasoning quality.

Abstract: Large Language Models (LLMs) have demonstrated remarkable abilities in reasoning. However, maximizing their potential through inference-time scaling faces challenges in trade-off between sampling budget and reasoning quality. Current strategies remain inefficient as they typically treat sampling width and depth as orthogonal objectives, where width consensus methods risk reinforcing hallucinations, while depth pruning mechanisms prematurely truncate complex yet valid reasoning chains. Therefore, we propose Dual-Dimensional Consistency (DDC), a unified framework that bridges path quality with adaptive termination. By coupling Confidence-Weighted Bayesian protocol with a Trend-Aware Stratified Pruning, our method ensures that computational resources are concentrated on high quality reasoning paths, filtering hallucinations while accelerating consensus. Evaluations across five benchmarks demonstrate that this approach reduces token consumption by over 10 times while maintaining or exceeding the accuracy of strong baselines across various LLMs.

</details>


### [99] [Why Neighborhoods Matter: Traversal Context and Provenance in Agentic GraphRAG](https://arxiv.org/abs/2605.15109)
*Riccardo Terrenzi,Maximilian von Zastrow,Serkan Ayvaz*

Main category: cs.AI

TL;DR: Agentic GraphRAG systems present new challenges for citation faithfulness, requiring evaluation of not just cited sources but also uncited entities and graph traversal paths that influence answers.


<details>
  <summary>Details</summary>
Motivation: Traditional citation evaluation focuses on whether cited sources support the answer, but Agentic GraphRAG complicates this because agents explore knowledge graphs before producing answers with limited citations, potentially missing important context from visited-but-uncited entities and traversal paths.

Method: Through controlled ablation experiments, researchers compare effects of isolating, removing, and masking both cited and uncited graph entities to understand their influence on answers.

Result: Cited evidence is often necessary (removing it changes answers substantially and reduces accuracy), but citations are not sufficient - accurate answers also depend on uncited traversal context and surrounding graph structure.

Conclusion: Citation evaluation in Agentic GraphRAG should move beyond traditional source support assessment toward provenance tracking over the broader retrieval trajectory, including both cited and uncited entities.

Abstract: Retrieval-Augmented Generation can improve factuality by grounding answers in external evidence, but Agentic GraphRAG complicates what it means for citations to be faithful. In these systems, an agent explores a knowledge graph before producing an answer and a small set of citations. We frame citation faithfulness as a trajectory-level problem: final citations should not only support the answer, but also account for the graph traversal, structure, and visited-but-uncited entities that may influence it. Through controlled ablation experiments, we compare the effects of isolating, removing, and masking cited and uncited graph entities. Our results show that cited evidence is often necessary, as removing it substantially changes answers and reduces accuracy. However, citations are not sufficient, because accurate answers can also depend on uncited traversal context and surrounding graph structure. These findings suggest that citation evaluation in Agentic GraphRAG should move beyond source support toward provenance over the broader retrieval trajectory.

</details>


### [100] [OpenDeepThink: Parallel Reasoning via Bradley--Terry Aggregation](https://arxiv.org/abs/2605.15177)
*Shang Zhou,Wenhao Chai,Kaiyuan Liu,Huanzhi Mao,Qiuyang Mang,Jingbo Shang*

Main category: cs.AI

TL;DR: OpenDeepThink is a population-based test-time compute framework that improves LLM reasoning by using pairwise Bradley-Terry comparisons for candidate selection, achieving significant performance gains on code generation tasks.


<details>
  <summary>Details</summary>
Motivation: Current test-time compute scaling methods for LLMs primarily focus on depth (extending single reasoning traces), while breadth scaling (sampling multiple candidates) introduces a selection bottleneck - choosing the best candidate without ground-truth verification, as pointwise LLM judging is noisy and biased.

Method: OpenDeepThink uses a population-based approach where each generation: (1) LLM judges random pairs of candidates, (2) aggregates votes via Bradley-Terry ranking into global ranking, (3) preserves top-ranked candidates, (4) mutates top 75% using natural-language critiques from comparisons, and (5) discards bottom 25%. This creates an iterative improvement process across multiple LLM-call rounds.

Result: The framework raises Gemini 3.1 Pro's effective Codeforces Elo by +405 points in eight sequential LLM-call rounds (~27 minutes wall-clock). Gains transfer across weaker/stronger models without retuning, with concentrated improvements in objectively verifiable domains and reduced performance in subjective ones. Authors also release CF-73, a curated Codeforces dataset with expert ratings.

Conclusion: OpenDeepThink provides an effective population-based framework for test-time compute scaling that addresses the selection bottleneck in breadth-based reasoning approaches, demonstrating significant performance gains on coding tasks through iterative pairwise comparison and refinement.

Abstract: Test-time compute scaling is a primary axis for improving LLM reasoning. Existing methods primarily scale depth by extending a single reasoning trace. Scaling breadth by sampling multiple candidates in parallel is straightforward, but introduces a selection bottleneck: choosing the best candidate without a ground-truth verifier, since pointwise LLM judging is noisy and biased. To address this, we introduce OpenDeepThink, a population-based test-time compute framework that selects via pairwise Bradley-Terry comparison. Each generation, the LLM judges random pairs of candidates and aggregates votes via Bradley-Terry into a global ranking; top-ranked candidates are preserved and the top three quarters are mutated using the natural-language critiques produced during comparison; the bottom quarter is discarded. OpenDeepThink raises Gemini 3.1 Pro's effective Codeforces Elo by +405 points in eight sequential LLM-call rounds (~27 minutes wall-clock). The pipeline transfers across weaker and stronger models without retuning, and on the multi-domain HLE benchmark, gains appear concentrated in objectively verifiable domains and reverse in subjective ones. We release CF-73, a curated set of 73 expert-rated Codeforces problems with International Grandmaster annotation and 99% local-evaluation agreement against the official verdict.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [101] [Vision-Based Runtime Monitoring under Varying Specifications using Semantic Latent Representations](https://arxiv.org/abs/2605.13923)
*Bardh Hoxha,Oliver Schön,Hideki Okamoto,Lars Lindemann,Georgios Fainekos*

Main category: cs.LG

TL;DR: A framework for certified runtime monitoring of past-time signal temporal logic from visual observations with reusable monitors that provide finite-sample guarantees without per-formula retraining.


<details>
  <summary>Details</summary>
Motivation: Need for runtime monitoring of safety properties from visual observations under partial observability, with formal guarantees and reusability across different formulas without retraining.

Method: Two approaches: 1) Semantic basis monitor that predicts robustness scores for temporal atoms and composes them for any formula via parse tree decoder, 2) Rolling prediction monitor that predicts only current predicate values and reconstructs temporal history online. Both use conformal calibration for certification.

Result: Proved semantic basis is minimum prediction target for monotone, 1-Lipschitz reusable interfaces; single conformal calibration certifies entire formula fragment. On pedestrian benchmark, rolling monitor tighter at short horizons, semantic basis up to 4× tighter at long horizons. Both satisfy coverage guarantees on Waymo driving data.

Conclusion: Presented certified runtime monitoring framework with two reusable monitors that provide finite-sample guarantees for ptSTL formulas from visual observations; semantic basis approach offers better long-horizon performance while rolling approach simpler for short horizons.

Abstract: We study certified runtime monitoring of past-time signal temporal logic (ptSTL) from visual observations under partial observability. The monitor must infer safety-relevant quantities from images and provide finite-sample guarantees, while being \emph{reusable}: once trained and calibrated, it should certify any formula in a target fragment without per-formula retraining. For fragments induced by a finite dictionary of temporal atoms, we prove that the \emph{semantic basis}, the vector of atom robustness scores, is the minimum prediction target within the class of monotone, 1-Lipschitz reusable interfaces: any formula is evaluated by a deterministic decoder derived from the parse tree, and a single conformal calibration pass certifies the entire fragment with no union bound. We also introduce a \emph{rolling prediction monitor} that predicts only current predicate values and reconstructs temporal history online; this is easier to learn but grows conservative at long horizons. On a pedestrian-crossroad benchmark, rolling achieves tighter certified bounds at short horizons while the semantic-basis monitor is up to 4-times tighter at long horizons. We validate the presented monitors on real-world Waymo driving data, where both monitors satisfy the conformal coverage guarantee empirically.

</details>


### [102] [Mechanistic Interpretability of EEG Foundation Models via Sparse Autoencoders](https://arxiv.org/abs/2605.13930)
*William Lehn-Schiøler,Magnus Ruud Kjær,Rahul Thapa,Magnus Guldberg Pedersen,Anton Storgaard Mosquera,Nick Williams,Radu Gatej,Tue Lehn-Schiøler,Sándor Beniczky,Sadasivan Puthusserypady,James Zou,Lars Kai Hansen*

Main category: cs.LG

TL;DR: The paper applies TopK Sparse Autoencoders to extract interpretable features from EEG foundation models and evaluates concept steering to understand how clinical concepts are represented and potentially manipulated in the latent space.


<details>
  <summary>Details</summary>
Motivation: EEG foundation models achieve state-of-the-art clinical performance but their internal computations remain opaque, creating a barrier to clinical trust and interpretability.

Method: The authors apply TopK Sparse Autoencoders (SAEs) across three architecturally distinct EEG transformers (SleepFM, REVE, LaBraM) to extract sparse feature dictionaries. They use concept steering with a "target vs. off-target" probe area metric to quantify steering selectivity and identify three operational regimes. They also introduce a spectral decoder to map interventions back to amplitude spectrum.

Result: The research reveals: 1) A single hyperparameter procedure transfers robustly across all three architectures; 2) Three operational regimes of concept representation; 3) Critical representational failures including "wrecking-ball" interventions that collapse global model performance; 4) Clinical entanglements like age-pathology confounding; 5) Spectral decoding translates latent manipulations into physiologically interpretable frequency signatures.

Conclusion: The framework provides systematic interpretability tools for EEG foundation models, exposing both successes and critical failures in clinical concept representation, while enabling translation of latent manipulations to physiologically meaningful frequency signatures that can support clinical trust.

Abstract: EEG foundation models achieve state-of-the-art clinical performance, yet the internal computations driving their predictions remain opaque: a barrier to clinical trust. We apply TopK Sparse Autoencoders (SAEs) across three architecturally distinct EEG transformers: SleepFM, REVE, and LaBraM to extract sparse feature dictionaries from their embeddings. By grounding these features in a clinical taxonomy (abnormality, age, sex, and medication), we benchmark monosemanticity and entanglement across architectures. A single hyperparameter procedure, driven by an intrinsic dictionary health audit, transfers robustly across all three architectures. Via concept steering, we introduce a "target vs. off-target" probe area metric to quantify steering selectivity and reveal three operational regimes: selectively steerable, encoded but entangled, and non-encoded. This framework exposes critical representational failures: "wrecking-ball" interventions that collapse global model performance, and clinical entanglements, such as age-pathology confounding, where it is impossible to suppress one concept without corrupting the other. Finally, a spectral decoder maps these interventions back to the amplitude spectrum, translating latent manipulations into physiologically interpretable frequency signatures, such as pathological slow-wave suppression and $α$-band restoration.

</details>


### [103] [Rethinking Molecular OOD Generalization via Target-Aware Source Selection](https://arxiv.org/abs/2605.13932)
*Zhuohao Lin,Kun Li,Jiameng Chen,Jiajun Yu,Duanhua Cao,Yizhen Zheng,Wenbin Hu*

Main category: cs.LG

TL;DR: SCOPE-BENCH introduces cluster-level partitioning in physicochemical space to properly evaluate molecular OOD generalization, while POMA uses RL to select optimal source subsets for domain adaptation, reducing prediction errors by up to 11.2%.


<details>
  <summary>Details</summary>
Motivation: Current scaffold-splitting protocols for molecular property prediction fail to prevent microscopic semantic overlap, leading to shortcut learning and overestimated extrapolation capabilities. Additionally, conventional domain adaptation methods struggle with extreme structural shifts, as blindly aligning heterogeneous source libraries introduces topological noise and causes negative transfer.

Method: Two components: 1) SCOPE-BENCH - a benchmark using cluster-level partitioning in explicit physicochemical descriptor space for robust OOD evaluation. 2) POMA - a framework with a retrieve-compose-adapt pipeline that identifies structurally similar source scaffolds as proxy targets, uses reinforcement learning to select optimal source subsets from exponentially large candidate pools, and performs dual-scale domain adaptation at macroscopic topological and microscopic pharmacophore scales.

Result: On SCOPE-BENCH, state-of-the-art 3D molecular models show prediction errors surging by up to 8.0x (mean 5.9x). POMA achieves up to 11.2% reduction in mean absolute error with an average relative improvement of 6.2% across diverse backbone architectures.

Conclusion: The proposed SCOPE-BENCH benchmark effectively reveals the limitations of current molecular models in extreme OOD scenarios, while POMA demonstrates significant improvements in molecular property prediction through intelligent source selection and dual-scale domain adaptation.

Abstract: Robust prediction of molecular properties under extreme out-of-distribution (OOD) scenarios is a pivotal bottleneck in AI-driven drug discovery. Current scaffold-splitting protocols fail to obstruct microscopic semantic overlap, predisposing models to shortcut learning and overestimating their true extrapolation capability; meanwhile, conventional domain adaptation paradigms suffer under extreme structural shifts, as blindly aligning heterogeneous source libraries injects topological noise and triggers negative transfer. To address these two challenges, scaffold-cluster out-of-distribution performance evaluation benchmark (SCOPE-BENCH), a benchmark built on cluster-level partitioning in an explicit physicochemical descriptor space, is proposed alongside policy optimization for multi-source adaptation (POMA), a framework that formulates knowledge transfer as a retrieve-compose-adapt pipeline: labeled source scaffolds structurally close to the unlabeled target are first identified as proxy targets; a reinforcement learning policy then adaptively selects the optimal source subset from an exponentially large candidate pool; and dual-scale domain adaptation is finally performed at macroscopic topological and microscopic pharmacophore scales. Evaluations show that prediction errors of state-of-the-art 3D molecular models surge by up to 8.0x on SCOPE-BENCH with a mean of 5.9x, while POMA achieves up to an 11.2% reduction in mean absolute error with an average relative improvement of 6.2% across diverse backbone architectures. Code is available at https://anonymous.4open.science/r/Molecular-OOD-Code-73F6.

</details>


### [104] [Unsupervised learning of acquisition variability in structural connectomes via hybrid latent space modeling](https://arxiv.org/abs/2605.13933)
*Gaurav Rudravaram,Lianrui Zuo,Karthik Ramadass,Elyssa McMaster,Jongyeon Yoon,Aravind R. Krishnan,Adam M. Saunders,Chenyu Gao,Nancy R. Newlin,Praitayini Kanakaraj,Lori L. Beason Held,Murat Bilgel,Laura A. Barquero,Micah DArchangel,Tin Q. Nguyen,Laurie B. Cutting,Derek Archer,Timothy J. Hohman,Daniel C. Moyer,Bennett A. Landman*

Main category: cs.LG

TL;DR: The paper introduces an unsupervised framework that uses architectural annealing to automatically balance discrete and continuous latent variables in structural connectome analysis, enabling better separation of acquisition-related variability from biological variation in diffusion MRI data.


<details>
  <summary>Details</summary>
Motivation: Acquisition differences across sites, scanners, and protocols in diffusion MRI (dMRI) introduce variability that complicates structural connectome analysis. Current dimensionality reduction methods often absorb acquisition effects into continuous latent spaces, while existing hybrid models require manual capacity tuning to ensure discrete components capture intended variability.

Method: The authors propose an unsupervised framework that removes manual tuning by architecturally annealing encoder outputs before decoding, allowing adaptive balancing of discrete and continuous latent variables during training. They evaluate on a curated dataset of N=7,416 structural connectomes spanning ages 2-102 from 13 studies with 25 unique acquisition-parameter combinations.

Result: The architectural annealing approach produces stronger site learning (ARI=0.53, p<0.05) compared to baselines including standard VAE, PCA with k-means clustering, and hybrid models that anneal only through the loss function. The hybrid continuous-discrete latent space with architectural annealing recovers clusters aligned with scanner and protocol differences.

Conclusion: A hybrid continuous-discrete latent space with architectural (rather than loss-based) annealing provides a useful unsupervised mechanism for capturing acquisition variability in dMRI. By jointly modeling smooth and categorical structure, the Joint-VAE effectively separates acquisition-related effects from biological variation in structural connectome analysis.

Abstract: Acquisition differences across sites, scanners, and protocols in dMRI introduce variability that complicates structural connectome analysis. This motivates deep learning models that can represent high-dimensional connectomes in a low-dimensional space while explicitly separating acquisition-related effects from biological variation. Conventional dimensionality reduction methods model all variance as continuous, so acquisition effects often get absorbed into a continuous latent space. Recent hybrid latent-space models combine discrete and continuous components to address this, but typically require manual capacity tuning to ensure the discrete component captures the intended variability. We introduce an unsupervised framework that removes this manual tuning by architecturally annealing encoder outputs before decoding, allowing the model to adaptively balance discrete and continuous latent variables during training. To evaluate it, we curated a dataset of N=7,416 structural connectomes derived from dMRI, spanning ages 2 to 102 and 13 studies with 25 unique acquisition-parameter combinations. Of these, 5,900 are cognitively unimpaired, 877 have mild cognitive impairment (MCI), and 639 have Alzheimer's disease (AD). We compare against a standard VAE, PCA with k-means clustering, and hybrid models that anneal only through the loss function. Our architectural annealing produces stronger site learning (ARI=0.53, p<0.05) than these baselines. Results show that a hybrid continuous-discrete latent space, with architectural rather than loss-based annealing, provides a useful unsupervised mechanism for capturing acquisition variability in dMRI: by jointly modeling smooth and categorical structure, the Joint-VAE recovers clusters aligned with scanner and protocol differences.

</details>


### [105] [Beyond Mode-Seeking RL: Trajectory-Balance Post-Training for Diffusion Language Models](https://arxiv.org/abs/2605.13935)
*Saba Ahmadi,Prasanna Parthasarathi,Yufei Cui*

Main category: cs.LG

TL;DR: TraFL is a new post-training method for diffusion language models that uses trajectory balancing to prevent over-concentration on narrow reward paths, improving coverage of alternative correct solutions across mathematical reasoning and code generation tasks.


<details>
  <summary>Details</summary>
Motivation: Current post-training methods for diffusion language models adapt reward-maximizing objectives, but suffer from "trajectory locking" - a failure mode where sampled reward-driven updates concentrate probability mass onto narrow denoising paths, reducing coverage of alternative correct solutions.

Method: TraFL (Trajectory Flow baLancing) uses a trajectory-balance objective that trains the policy toward a reward-tilted target distribution anchored to a frozen reference model. It includes a diffusion-compatible sequence-level surrogate and a learned prompt-dependent normalization.

Result: TraFL is the only evaluated post-training method that improves over the base model in every benchmark-length setting for mathematical reasoning and code generation, with gains that persist as sampling budget increases. It maintains performance above base model on Minerva Math and is strongest on every LiveCodeBench difficulty split.

Conclusion: The proposed TraFL method successfully addresses trajectory locking in diffusion language models, enabling better coverage of alternative correct solutions while maintaining strong performance across diverse reasoning tasks and transferring well to held-out evaluations.

Abstract: Diffusion language models are a promising alternative to autoregressive models, yet post-training methods for them largely adapt reward-maximizing objectives. We identify a central failure mode in this setting we call trajectory locking: sampled reward-driven updates over-concentrate probability mass onto a narrow set of denoising paths, reducing coverage of alternative correct solutions under repeated sampling. To address this, we propose TraFL (Trajectory Flow baLancing), a trajectory-balance objective that trains the policy toward a reward-tilted target distribution anchored to a frozen reference model. We make this practical for diffusion language models with a diffusion-compatible sequence-level surrogate and a learned prompt-dependent normalization. Across mathematical reasoning and code generation benchmarks, TraFL is the only evaluated post-training method that improves over the base model in every benchmark-length setting, with gains that persist as the sampling budget increases. The improvements transfer to held-out evaluations: TraFL stays above the base model on Minerva Math and is the strongest method on every LiveCodeBench difficulty split.

</details>


### [106] [Quantum Advantage in Multi Agent Reinforcement Learning](https://arxiv.org/abs/2605.14235)
*Simranjeet Singh Dahia,Claudia Szabo*

Main category: cs.LG

TL;DR: This paper empirically evaluates quantum entanglement's role in agent coordination within quantum multi-agent reinforcement learning (QMARL), showing entanglement provides quantum advantage over classical baselines in both CHSH game and cooperative navigation tasks.


<details>
  <summary>Details</summary>
Motivation: Most prior QMARL work lacks provable baselines, making it difficult to rigorously distinguish quantum advantage from algorithmic coincidence. The authors aim to directly address this gap by evaluating quantum entanglement's role in agent coordination with proper baseline comparisons.

Method: The authors use a decentralized QMARL framework with variational quantum circuit (VQC) actors sharing entangled states. They evaluate in two scenarios: 1) CHSH game with mathematically proven classical ceiling, and 2) Cooperative navigation task. They compare entangled vs. unentangled quantum circuits and different Bell state entanglement structures.

Result: 1) In CHSH game, entangled QMARL agents approach Tsirelson limit (0.854 win rate) vs. classical ceiling of 0.75, while unentangled circuits match classical baseline. 2) In cooperative navigation, QMARL without entanglement achieves ~2× success rate improvement over classical MAA2C (~0.85 vs ~0.40). Hybrid configuration (quantum actor with classical centralized critic) outperforms both fully classical and fully quantum solutions.

Conclusion: The research provides clear evidence of quantum advantage in QMARL through entanglement, distinguishes entanglement from quantum circuits as the active coordination mechanism, shows entanglement structure matters (some Bell states help while others harm performance), and demonstrates practical hybrid configurations can outperform purely classical or quantum approaches.

Abstract: We present an empirical evaluation of quantum entanglement in agent coordination within quantum multi agent reinforcement learning (QMARL). While QMARL has attracted growing interest recently, most prior work evaluates quantum policies without provable baselines, making it impossible to rigorously distinguish quantum advantage from algorithmic coincidence. We address this directly by evaluating a decentralized QMARL framework with variational quantum circuit (VQC) actors with shared entangled states. In the CHSH game, which has a mathematically proven classical performance ceiling of 0.75 win rate, we show that entangled QMARL agents approach the Tsirelson limit of 0.854, providing clear evidence of their quantum advantage. We show that unentangled quantum circuits match the classical baseline, confirming that entanglement and not the quantum circuit itself is the active coordination mechanism. We also explore the effect of specific entanglement structures, as some Bell states enable coordination gains while others actively harm performance. On cooperative navigation (CoopNav), QMARL without entanglement achieves $\sim2\times$ improvement in success rate over classical MAA2C ($\sim$0.85 versus $\sim$0.40), with the hybrid configuration, quantum actor paired with a classical centralised critic, outperforming both fully classical and fully quantum solutions. We present our experimental analysis and discuss future work.

</details>


### [107] [Towards the Next Frontier of LLMs, Training on Private Data: A Cross-Domain Benchmark for Federated Fine-Tuning](https://arxiv.org/abs/2605.13936)
*Daniel M. Jimenez-Gutierrez,Enrique Zuazua,Georgios Kellaris,Joaquin del Rio,Oleksii Sliusarenko,Xabi Uribe-Etxebarria*

Main category: cs.LG

TL;DR: Federated learning enables LLM fine-tuning across distributed private datasets without data sharing, performing close to centralized training while preserving privacy.


<details>
  <summary>Details</summary>
Motivation: Valuable private data in regulated sectors like healthcare and finance can't be shared due to privacy and regulatory constraints, creating data silos that limit LLM development despite the potential for deeper domain expertise.

Method: Built on Sherpa.ai Federated Learning platform, the framework enables nodes to jointly fine-tune a shared LLM without exchanging private data. Evaluated three PEFT strategies (LoRA, QLoRA, IA3) across healthcare and finance datasets under non-IID settings.

Result: Federated fine-tuning performs close to centralized training and outperforms isolated single-institution learning. QLoRA and IA3 improve efficiency with limited accuracy degradation compared to full fine-tuning.

Conclusion: Federated PEFT is a viable approach for adapting LLMs where data cannot be shared, enabling collaboration across institutional data silos while addressing privacy, regulatory, and efficiency concerns.

Abstract: The recent success of large language models (LLMs) has been largely driven by vast public datasets. However, the next frontier for LLM development lies beyond public data. Much of the world's most valuable information is private, especially in highly regulated sectors such as healthcare and finance, where data include patient histories or customer communications. Unlocking this data could represent a major leap forward, enabling LLMs with deeper domain expertise and stronger real-world utility. Yet, these data cannot be shared because they are distributed across institutions and constrained by privacy, regulatory, and organizational barriers. Moreover, institutional datasets are typically non-independent and identically distributed (non-IID), differing across sites in population characteristics, data modalities, documentation patterns, and task-specific label distributions.
  In this paper, we demonstrate a practical approach to unlocking private and distributed institutional data for LLM adaptation through federated collaboration across data silos. Built on the Sherpa.ai Federated Learning platform, our framework enables nodes to jointly fine-tune a shared LLM without exchanging private data. We evaluate this approach through a cross-domain benchmark in healthcare and finance, using four closed-ended question answering and classification datasets: MedQA, MedMCQA, FPB, and FiQA-SA. We compare three parameter-efficient fine-tuning (PEFT) strategies-LoRA, QLoRA, and IA3-across pretrained backbones under non-IID settings reflecting institutional data heterogeneity. Our results show that federated fine-tuning performs close to centralized training and outperforms isolated single-institution learning. From a Green AI perspective, QLoRA and IA3 improve efficiency with limited accuracy degradation, supporting federated PEFT as a viable approach for adapting LLMs where data cannot be shared.

</details>


### [108] [Data-Augmented Game Starts for Accelerating Self-Play Exploration in Imperfect Information Games](https://arxiv.org/abs/2605.14379)
*JB Lanier,Nathan Monette,Pierre Baldi,Roy Fox*

Main category: cs.LG

TL;DR: DAGS (Data-Augmented Game Starts) accelerates exploration in two-player zero-sum games by initializing reinforcement learning from offline demonstration states, using multi-task observation flags to prevent biased equilibria.


<details>
  <summary>Details</summary>
Motivation: Large-scale imperfect-information games like StarCraft and Dota have sparse rewards and long horizons, making approximate equilibrium computation infeasible. Offline demonstrations from skilled players can provide good coverage of high-level strategies relevant to equilibrium play.

Method: Proposes Data-Augmented Game Starts (DAGS): initialize RL data collection at intermediate states sampled from offline demonstration data to explore strategically relevant subgames. Uses multi-task observation flags to mitigate biased equilibria from augmented starting state distributions.

Result: DAGS enables regularized policy gradient methods to achieve lower exploitability under fixed computational budgets in challenging exploration games (Kuhn Poker, Goofspiel variants). Released new benchmark environments that increase exploration challenges while keeping exploitability tractable.

Conclusion: Starting state sampling from offline data accelerates exploration in imperfect-information games, though it can bias equilibria without proper mitigation (multi-task observation flags). The approach is promising for large-scale games with sparse rewards.

Abstract: Finding approximate equilibria for large-scale imperfect-information competitive games such as StarCraft, Dota, and CounterStrike remains computationally infeasible due to sparse rewards and challenging exploration over long horizons. In this paper, we propose a multi-agent starting-state sampling strategy designed to substantially accelerate online exploration in regularized policy-gradient game methods for two-player zero-sum (2p0s) games. Motivated by an assumption that offline demonstrations from skilled humans can provide good coverage of high-level strategies relevant to equilibrium play, we propose the initialization of reinforcement learning data collection at intermediate states sampled from offline data to facilitate exploration of strategically relevant subgames. Referring to this method as Data-Augmented Game Starts (DAGS), we perform experiments using synthetic datasets and analytically tractable, long-horizon control variants of two-player Kuhn Poker, Goofspiel, and a counterexample game designed to penalize biased beliefs over hidden information. Under fixed computational budgets, DAGS enables regularized policy gradient methods to achieve lower exploitability in games with significantly more challenging exploration. We show that augmenting starting state distributions when solving imperfect information games can lead to biased equilibria, and we provide a straightforward mitigation to this in the form of multi-task observation flags. Finally, we release a new set of benchmark environments that drastically increase exploration challenges and state counts in existing OpenSpiel games while keeping exploitability measurements analytically tractable.

</details>


### [109] [EvolveMem:Self-Evolving Memory Architecture via AutoResearch for LLM Agents](https://arxiv.org/abs/2605.13941)
*Jiaqi Liu,Xinyu Ye,Peng Xia,Zeyu Zheng,Cihang Xie,Mingyu Ding,Huaxiu Yao*

Main category: cs.LG

TL;DR: EvolveMem is a self-evolving memory architecture for LLM agents that autonomously optimizes both stored knowledge and retrieval mechanisms through LLM-powered diagnosis and configuration adjustments.


<details>
  <summary>Details</summary>
Motivation: Existing memory systems for LLM agents treat retrieval infrastructure as fixed while only stored content evolves, lacking true adaptation. Adaptive memory requires co-evolution of both stored knowledge and the retrieval mechanisms that query it.

Method: EvolveMem exposes retrieval configuration as structured action space optimized by an LLM-powered diagnosis module. The system reads failure logs, identifies root causes, proposes targeted configuration adjustments, and applies them with automatic revert-on-regression and explore-on-stagnation safeguards.

Result: On LoCoMo benchmark, EvolveMem outperforms strongest baseline by 25.7% relative and achieves 78.0% relative improvement over minimal baseline. On MemBench, exceeds strongest baseline by 18.9% relative. Evolved configurations transfer positively across benchmarks.

Conclusion: EvolveMem demonstrates that closed-loop self-evolution enables autonomous architecture research, discovering universal retrieval principles rather than benchmark-specific heuristics, with positive transfer across different memory benchmarks.

Abstract: Long-term memory is essential for LLM agents that operate across multiple sessions, yet existing memory systems treat retrieval infrastructure as fixed: stored content evolves while scoring functions, fusion strategies, and answer-generation policies remain frozen at deployment. We argue that truly adaptive memory requires co-evolution at two levels: the stored knowledge and the retrieval mechanism that queries it. We present EvolveMem, a self-evolving memory architecture that exposes its full retrieval configuration as a structured action space optimized by an LLM-powered diagnosis module. In each evolution round, the module reads per-question failure logs, identifies root causes, and proposes targeted configuration adjustments; a guarded meta-analyzer applies them with automatic revert-on-regression and explore-on-stagnation safeguards. This closed-loop self-evolution realizes an AutoResearch process: the system autonomously conducts iterative research cycles on its own architecture, replacing manual configuration tuning. Starting from a minimal baseline, the process converges autonomously, discovering effective retrieval strategies including entirely new configuration dimensions not present in the original action space. On LoCoMo, EvolveMem outperforms the strongest baseline by 25.7% relative and achieves a 78.0% relative improvement over the minimal baseline. On MemBench, EvolveMem exceeds the strongest baseline by 18.9% relative. Evolved configurations transfer across benchmarks with positive rather than catastrophic transfer, indicating that the self-evolution process captures universal retrieval principles rather than benchmark-specific heuristics. Code is available at https://github.com/aiming-lab/SimpleMem.

</details>


### [110] [EMA: Efficient Model Adaptation for Learning-based Systems](https://arxiv.org/abs/2605.13942)
*Daiyang Yu,Xinyu Chen,Yihan Zhang,Yan Liang,Yaqi Qiao,Fan Lai*

Main category: cs.LG

TL;DR: EMA is a model adaptation system that helps learning-based networked systems adapt to changing environments with minimal overhead by using state transformers for warm-start adaptation and data-centric labeling prioritization.


<details>
  <summary>Details</summary>
Motivation: Networked systems operate in heterogeneous, dynamic environments where conditions and objectives shift over time, but existing learning-based systems lack adaptation support, leading to costly training, degraded performance, and slow responsiveness.

Method: EMA uses a system-driven, data-centric approach with two key components: 1) state transformers that align new environment states with similar previous states for warm-start adaptation, and 2) prioritized labeling of high-utility data while balancing training and labeling costs.

Result: Evaluation on eight learning-based systems shows EMA reduces adaptation costs (GPU training time) by 14.9-42.4% while improving system performance (network throughput) by 6.9-31.3%.

Conclusion: EMA effectively addresses adaptation challenges in learning-based networked systems by minimizing operational overhead through state alignment and intelligent data labeling, enabling efficient adaptation to evolving environments.

Abstract: Machine learning (ML) is increasingly applied to optimize system performance in tasks such as resource management and network simulation. Unlike traditional ML tasks (e.g., image classification), networked systems often operate in heterogeneous, long-running, and dynamic environment states, where input conditions (e.g., network loads) and operational objectives can shift over time and across settings. Existing learning-based systems offer little support for adaptation, resulting in costly model training, extensive data collection, degraded system performance, and slow responsiveness.
  This paper presents EMA, the first model adaptation system supporting learning-based systems to adapt to evolving environments with minimal operational overhead. EMA takes a system-driven, data-centric approach that accommodates diverse system and model designs while addressing two key deployment challenges. First, it reduces expensive model training by introducing state transformers that align the input state of a new environment with previously similar states, allowing models to warm-start adaptation. Second, it addresses the often-overlooked yet costly process of data labeling--collecting ground truth for exploring and training on various system decisions--by prioritizing labeling high-utility data while balancing the tradeoff between training and labeling cost. Evaluations on eight representative learning-based systems show that EMA reduces adaptation costs (e.g., GPU training time) by 14.9-42.4% while improving system performance (e.g., network throughput) by 6.9-31.3%.

</details>


### [111] [A Unified Geometric Framework for Weighted Contrastive Learning](https://arxiv.org/abs/2605.13943)
*Raphael Vock,Edouard Duchesnay,Benoit Dufumier*

Main category: cs.LG

TL;DR: Contrastive learning objectives can be interpreted as Distance Geometry Problems, revealing that weighting schemes determine geometric realizability, with supervised CL collapsing classes to prototypes while different weighting choices lead to regular simplex, imbalanced, or inconsistent embedding geometries.


<details>
  <summary>Details</summary>
Motivation: Despite contrastive learning's goal of preserving relational structure between samples, the geometry of resulting embeddings remains poorly understood, motivating a geometric analysis of how different weighting schemes in CL objectives affect the latent space structure.

Method: The authors reinterpret weighted InfoNCE objectives as Distance Geometry Problems, where weighting schemes specify target geometries. This framework allows exact characterization of optimal embeddings for various supervised and weakly supervised objectives, analyzing geometric consistency and realizability.

Result: Supervised classification objectives collapse samples within classes to single prototypes: balanced SupCon produces regular simplex geometry, while class imbalance breaks symmetry. Soft SupCon preserves regular simplex regardless of imbalance. Continuous-label settings reveal y-Aware CL cannot achieve optimum unless labels are on a hypersphere, while geometrically consistent weightings like Euclidean-Euclidean or X-CLR admit unique optimal embeddings.

Conclusion: The choice of weighting scheme fundamentally determines whether contrastive learning is geometrically realizable, degenerate, or inconsistent, providing a principled framework for designing contrastive objectives based on geometric consistency considerations.

Abstract: Contrastive learning (CL) aims to preserve relational structure between samples by learning representations that reflect a similarity graph. Yet, the geometry of the resulting embeddings remains poorly understood. Here we show that weighted InfoNCE objectives can be interpreted as Distance Geometry Problems, where the weighting scheme specifies the target geometry to be realized by the representation. This viewpoint yields exact characterizations of the optimal embeddings for several supervised and weakly supervised objectives. In supervised classification, both SupCon and Soft SupCon (a dense relaxation of it where pairs from distinct classes have small non-zero similarity) collapse samples within each class to a single prototype. However, while balanced SupCon recovers the classical regular simplex geometry, class imbalance breaks this symmetry: SupCon induces non-uniform inter-class similarities depending on class sizes, whereas Soft SupCon preserves a regular simplex geometry regardless of class imbalance. In continuous-label settings, our framework reveals a different failure mode: y-Aware CL generally cannot attain its entropic optimum unless the labels lie on a hypersphere, exposing a mismatch between Euclidean label weights and spherical latent similarity. By contrast, geometrically consistent choices such as Euclidean-Euclidean weighting or X-CLR admit unique optimal embeddings. Our results show that the choice of weighting scheme determines whether contrastive learning is geometrically realizable, degenerate, or inconsistent, providing a principled framework for designing contrastive objectives.

</details>


### [112] [Collider-Bench: Benchmarking AI Agents with Particle Physics Analysis Reproduction](https://arxiv.org/abs/2605.13950)
*Darius A. Faroughy,Sofia Palacios Schweitzer,Ian Pang,Siddharth Mishra-Sharma,David Shih*

Main category: cs.LG

TL;DR: Collider-Bench is a new benchmark for evaluating LLM agents' ability to reproduce experimental analyses from the Large Hadron Collider using only public papers and open scientific software, measuring both accuracy and computational efficiency.


<details>
  <summary>Details</summary>
Motivation: Existing benchmarks for autonomous language-model agents don't capture the complexity and nuance of real scientific work, particularly the challenge of reproducing experimental analyses where public tools only approximate internal software and published papers omit critical implementation details.

Method: Introduces Collider-Bench, where agents must convert published LHC analyses into executable simulation-and-selection pipelines and submit predicted collision event yields. Tasks are evaluated using standard histogram metrics for continuous fidelity scores, computational cost tracking, and LLM judges to detect qualitative failures like fabrications and hallucinations.

Result: Evaluation across general purpose coding agents shows that on average, no agent reliably beats the physicist-in-the-loop solution, indicating current limitations in autonomous scientific analysis reproduction.

Conclusion: Collider-Bench provides a realistic benchmark for evaluating LLM agents on complex scientific analysis reproduction, revealing current gaps in autonomous scientific reasoning despite significant advances in language models and coding capabilities.

Abstract: Autonomous language-model agents are increasingly evaluated on long-horizon tool-use tasks, but existing benchmarks rarely capture the complexity and nuance of real scientific work. To address this gap, we introduce Collider-Bench, a benchmark for evaluating whether LLM agents can reproduce experimental analyses from the Large Hadron Collider (LHC) using only public papers and open scientific software. Such analyses are often difficult to reproduce because the public toolchain only approximates the software used internally by the experimental collaborations, while the published papers inevitably omit implementation details needed for a faithful reconstruction. Agents must therefore rely on physical reasoning, domain knowledge, and trial-and-error to fill these gaps. Each task requires the agent to turn a published analysis into an executable simulation-and-selection pipeline and submit predicted collision event yields in specified signal regions. These predictions are evaluated with standard histogram metrics that provide continuous fidelity scores without a hand-written rubric. We also report the computational cost incurred by each agent per task. Finally, we evaluate the codebase and full session trace using an LLM judge to catch qualitative failure modes such as fabrications, hallucinations and duplications. We release an initial set of tasks drawn from LHC searches, together with a containerized sandbox and event simulation tools. We evaluate across a capability ladder of general purpose coding agents. Our results show that on average no agent reliably beats the physicist-in-the-loop solution.

</details>


### [113] [WarmPrior: Straightening Flow-Matching Policies with Temporal Priors](https://arxiv.org/abs/2605.13959)
*Sinjae Kang,Chanyoung Kim,Kaixin Wang,Li Zhao,Kimin Lee*

Main category: cs.LG

TL;DR: WarmPrior improves generative robotic policies by using action history as a source distribution instead of Gaussian noise, leading to straighter probability paths and better performance.


<details>
  <summary>Details</summary>
Motivation: The paper identifies that the source distribution in generative policies (diffusion/flow matching) for robotic control is an underexplored design axis. Current approaches use standard Gaussian distributions, but there might be better alternatives that could improve performance.

Method: The authors propose WarmPrior - a temporally grounded prior constructed from readily available recent action history. They replace the standard Gaussian source distribution with this warm start prior in generative policies. They analyze the effect on probability paths and test the approach in both behavior cloning and prior-space reinforcement learning settings.

Result: WarmPrior consistently improves success rates on robotic manipulation tasks compared to standard Gaussian priors. The gain is traced to markedly straighter probability paths, similar to the effect of optimal-transport couplings in Rectified Flow. In prior-space RL, WarmPrior reshapes the exploration distribution, improving both sample efficiency and final performance.

Conclusion: The source distribution is an important and underexplored design axis in generative robot control. Using WarmPrior (based on action history) consistently outperforms standard Gaussian priors across different settings, suggesting that better-designed source distributions can significantly improve generative robotic policies.

Abstract: Generative policies based on diffusion and flow matching have become a dominant paradigm for visuomotor robotic control. We show that replacing the standard Gaussian source distribution with WarmPrior, a simple temporally grounded prior constructed from readily available recent action history, consistently improves success rates on robotic manipulation tasks. We trace this gain to markedly straighter probability paths, echoing the effect of optimal-transport couplings in Rectified Flow. Beyond standard behavior cloning, WarmPrior also reshapes the exploration distribution in prior-space reinforcement learning, improving both sample efficiency and final performance. Collectively, these results identify the source distribution as an important and underexplored design axis in generative robot control.

</details>


### [114] [Towards Resource-Efficient LLMs: End-to-End Energy Accounting of Distillation Pipelines](https://arxiv.org/abs/2605.13981)
*Katherine Lambert,Sasha Luccioni*

Main category: cs.LG

TL;DR: Researchers develop a comprehensive energy accounting framework for AI distillation pipelines, revealing previously ignored energy costs and providing practical guidelines for energy-efficient distillation methods.


<details>
  <summary>Details</summary>
Motivation: The rapid deployment of large language models has led to increased GPU demand and datacenter scaling, raising concerns about electricity consumption and grid stress. While distillation is promoted as an efficient way to create smaller models, existing claims fail to account for the full end-to-end energy costs including teacher-side workloads like data generation, logit caching, and evaluation.

Method: The paper presents a comprehensive energy accounting framework that measures complete computational costs of distillation pipelines through detailed stage-wise tracking of GPU device power consumption. The authors separate and log empirical energy use across distinct phases, systematically measuring energy and emissions of two common distillation methods: classic logit-based knowledge distillation and synthetic-data supervised fine-tuning, constructing energy-quality Pareto frontiers.

Result: The analysis exposes previously ignored energy costs in distillation pipelines through empirical measurements. The researchers derive practical design rules for selecting distillation methods and hyperparameters under energy and budget constraints based on the constructed energy-quality trade-offs.

Conclusion: The paper provides a standardized measurement framework for reproducible distillation research that explicitly accounts for complete pipeline energy impact, including the release of an open-source measurement harness and accounting protocol to establish comparable baselines for energy-efficient AI development.

Abstract: The rise in deployment of large language models has driven a surge in GPU demand and datacenter scaling, raising concerns about electricity use, grid stress, and the impacts of modern AI workloads. Distillation is often promoted as one of the most effective paths to obtain cheaper, more efficient models, yet these claims rarely account for the full end-to-end energy and resource costs, including crucial teacher-side workloads such as data generation, logit caching, and evaluation. We present a comprehensive energy accounting framework that measures the complete computational cost of distillation pipelines via detailed stage-wise tracking of GPU device power consumption. In our experiments, we separate and log empirical energy use across distinct phases and systematically measure the energy and emissions of two common distillation methods: the classic logit-based knowledge distillation and synthetic-data supervised fine-tuning, constructing energy-quality Pareto frontiers that expose the previously ignored costs. From these measurements and analyses, we derive practical design rules for selecting distillation methods and hyperparameters under energy and budget constraints, and release an open-source measurement harness and accounting protocol to provide a standardized foundation for comparable, reproducible distillation research, explicitly accountable for complete pipeline energy impact.

</details>


### [115] [TabPFN-3: Technical Report](https://arxiv.org/abs/2605.13986)
*Léo Grinsztajn,Klemens Flöge,Oscar Key,Felix Birkel,Philipp Jund,Brendan Roof,Mihir Manium,Shi Bin,Hoo,Magnus Bühler,Anurag Garg,Dominik Safaric,Jake Robertson,Benjamin Jäger,Simone Alessi,Adrian Hayler,Vladyslav Moroshan,Lennart Purucker,Philipp Singer,Alan Arazi,Julien Siems,Jan Hendrik Metzen,Georg Grab,Nick Erickson,Siyuan Guo,Eliott Kalfon,Simon Bing,David Salinas,Clara Cornu,Lilly Charlotte Wehrhahn,Diana Kriuchkova,Kursat Kaya,Lydia Sidhoum,Marie Salmon,Jerry Chen,Madelon Hulsebos,Yann LeCun,Samuel Müller,Bernhard Schölkopf,Sauraj Gambhir,Noah Hollmann,Frank Hutter*

Main category: cs.LG

TL;DR: TabPFN-3 scales tabular foundation models to handle 1M training rows with significant performance gains, faster inference, and extends capabilities to time series, relational, and tabular-text data while being 20x faster than its predecessor.


<details>
  <summary>Details</summary>
Motivation: Tabular data is fundamental to high-value prediction problems, but existing models face limitations in scalability, training/inference time, and performance on diverse data types including time series, relational, and tabular-text data.

Method: Pretrained exclusively on synthetic data from prior models, TabPFN-3 introduces test-time compute scaling, reduced KV cache, row-chunking techniques, and specialized checkpoints like TabPFN-TS-3 for time series applications.

Result: Outperforms all other models on TabArena benchmark by significant margin, achieves SOTA on relational data (RelBenchV1) and tabular-text data (TabSTAR), ranks 2nd on time-series benchmark, and beats 8-hour-tuned gradient-boosted-tree baselines on datasets up to 1M rows.

Conclusion: TabPFN-3 dramatically pushes the frontier of tabular prediction, achieves Pareto-optimal speed/performance trade-offs, and enables practical deployment with 20x speed improvement over previous versions while maintaining or exceeding state-of-the-art performance across diverse data types.

Abstract: Tabular data underpins most high-value prediction problems in science and industry, and TabPFN has driven the foundation model revolution for this modality. Designed with feedback from our users, TabPFN-3 builds on this foundation to scale state-of-the-art performance to datasets with 1M training rows and substantially reduce training and inference time. Pretrained exclusively on synthetic data from our prior, TabPFN-3 dramatically pushes the frontier of tabular prediction and brings substantial gains on time series, relational, and tabular-text data. On the standard tabular benchmark TabArena, a forward pass of TabPFN-3 outperforms all other models, including tuned and ensembled baselines, by a significant margin, and pareto-dominates the speed/performance frontier. On more diverse datasets, TabPFN-3 ranks first on datasets with many classes, and beats 8-hour-tuned gradient-boosted-tree baselines on datasets up to 1M training rows and 200 features. TabPFN-3 introduces test-time compute scaling to tabular foundation models. Our API offering TabPFN-3-Plus (Thinking) exploits this to beat all non-TabPFN models by over 200 Elo on TabArena, rising to 420 Elo on the largest data subset, and outperforms AutoGluon 1.5 extreme while being 10x faster, without using LLMs, real data, internet search or any other model besides TabPFN. TabPFN-3 extends the capabilities of our models, enabling SOTA prediction on relational data (new SOTA foundation model on RelBenchV1) and tabular-text data (SOTA on TabSTAR via TabPFN-3-Plus); and improves existing integrations: a specialized checkpoint, TabPFN-TS-3, ranks 2nd on the time-series benchmark fev-bench, and SHAP-value computation is up to 120x faster. TabPFN-3 achieves this performance while being up to 20x faster than TabPFN-2.5. In addition, a reduced KV cache and row-chunking scale to 1M rows on one H100 with fast inference speed.

</details>


### [116] [Neural Fields for NV-Center Inverse Sensing](https://arxiv.org/abs/2605.13988)
*Zhixuan Zhao,Tao Zhong,Yixun Hu,Nathalie P. de Leon,Christine Allen-Blanchette*

Main category: cs.LG

TL;DR: NeTMY is a neural field method for quantum sensing that avoids center-collapse failure in inverse reconstruction of sparse magnetic-noise spectra from NV centers.


<details>
  <summary>Details</summary>
Motivation: Current methods for inverse problems in quantum sensing fail with nonlinear, spectrally coupled forward models like NV center magnetic-noise sensing, particularly due to center-collapse failure in density optimization.

Method: NeTMY uses an amortization-free coordinate neural field coupled to a differentiable NV forward model with annealed positional encoding, multiscale optimization, sparsity/gating, and spectrum-fidelity losses.

Result: NeTMY achieves best localization and distributional metrics on sparse synthetic reconstructions, mitigating center-collapse pathology without directly executing raw density-space gradients.

Conclusion: NV quantum sensing serves as a valuable testbed for physics-faithful neural inverse problems, with NeTMY demonstrating effective regularization through neural parameterization.

Abstract: Inverse problems in scientific sensing are often solved with either hand-designed regularizers or supervised networks trained on simulated labels, yet both can fail when the forward model is nonlinear, spectrally coupled, and physically delicate. We study this issue for noise sensing based on nitrogen-vacancy (NV) centers in diamond, where a quantum sensor measures magnetic-noise spectra generated by sparse spin sources. We show that replacing a common scalar/coherent forward approximation with a tensor power-summed dipolar operator changes the inverse landscape and exposes a center-collapse failure mode in free-density optimization. We propose NeTMY, an amortization-free coordinate neural field coupled to the differentiable NV forward model, with annealed positional encoding, multiscale optimization, sparsity/gating, and spectrum-fidelity losses. Across sparse synthetic reconstructions generated by the corrected operator, NeTMY achieves the best localization and distributional metrics in the tested benchmark. Mechanism experiments show that NeTMY does not directly execute the raw density-space gradient; its parameterization smooths and redistributes updates, mitigating the center-collapse pathology. These results position NV quantum sensing as a useful testbed for physics-faithful neural inverse problems.

</details>


### [117] [HodgeCover: Higher-Order Topological Coverage Drives Compression of Sparse Mixture-of-Experts](https://arxiv.org/abs/2605.13997)
*Tao Zhong,Dongzhe Zheng,Christine Allen-Blanchette*

Main category: cs.LG

TL;DR: HodgeCover is a learning-free compressor for Sparse MoE layers that uses Hodge theory to identify irreducible cycles of experts that must remain separate, enabling more effective expert merging than previous pairwise-only methods.


<details>
  <summary>Details</summary>
Motivation: Existing learning-free compressors for Sparse Mixture-of-Experts layers fail because they only consider pairwise compatibility between experts, missing irreducible cycles where three experts are each pairwise compatible but cannot be jointly merged - a fundamental structural limitation.

Method: The authors formalize the obstruction as the harmonic kernel of the simplicial Laplacian on a 2-complex representing experts (vertices), KL merge barriers (edges), and triplet barriers (faces). They isolate this kernel via Hodge decomposition and develop HodgeCover, which greedily covers harmonic-critical edges and triplet-critical triangles. A hybrid variant combines HodgeCover with weight pruning on surviving experts.

Result: On three open-weight Sparse MoE backbones under aggressive expert reduction, HodgeCover matches state-of-the-art learning-free baselines on pure expert reduction, leads on aggressive hybrid compression (expert reduction + pruning), and uniquely balances retained mass across all four Hodge components.

Conclusion: Exposing the harmonic kernel structure of learned MoE networks fundamentally changes which compressor performs best in high-compression regimes, showing that topological considerations are crucial for effective expert merging beyond pairwise compatibility analysis.

Abstract: Sparse Mixture-of-Experts (MoE) layers route tokens through a handful of experts, and learning-free compression of these layers reduces inference cost without retraining. A subtle obstruction blocks every existing compressor in this family: three experts can each be pairwise compatible yet form an irreducible cycle when merged together, so any score that ranks experts on pairwise signals is structurally blind to which triples are jointly mergeable. We show the obstruction is a precise mathematical object, the harmonic kernel of the simplicial Laplacian on a 2-complex whose vertices are experts, whose edges carry KL merge barriers, and whose faces carry triplet barriers; Hodge-decomposing the edge-barrier signal isolates the kernel exactly. We turn the diagnostic into a selection objective: HodgeCover greedily covers the harmonic-critical edges and triplet-critical triangles, and a hybrid variant of HodgeCover pairs it with off-the-shelf weight pruning on survivors. On three open-weight Sparse MoE backbones under aggressive expert reduction, HodgeCover matches state-of-the-art learning-free baselines on the expert-reduction axis, leads on the aggressive-compression frontier of the hybrid axis, and uniquely balances retained mass across all four Hodge components. These results show that exposing the harmonic kernel of a learned MoE structure changes which compressor wins at the regime that matters most.

</details>


### [118] [Support Before Frequency in Discrete Diffusion](https://arxiv.org/abs/2605.13999)
*Adrian Müller,Antoine Gonon,Zebang Shen,Ya-Ping Hsieh,Niao He*

Main category: cs.LG

TL;DR: Discrete diffusion models learn data validity/support structure before frequency distributions, with different diffusion mechanisms (uniform vs absorbing) creating distinct hierarchies in denoising objectives.


<details>
  <summary>Details</summary>
Motivation: Although discrete diffusion models are competitive for language modeling, it's unclear how their denoising objectives organize learning. The paper aims to understand whether these models learn different aspects of the data distribution at different stages.

Method: Theoretical analysis of uniform and absorbing (masking) diffusion in the small-noise regime, examining how single-token reverse edits decompose into validity (support) information vs frequency information. Experiments with masked language diffusion models and synthetic regular-language tasks validate predictions.

Result: For both diffusion types, denoising decomposes into leading-scale validity decisions (whether moves maintain grammatical validity) and finer frequency coefficients. Uniform diffusion shows trichotomy of validity moves, while absorbing diffusion concentrates on validity-improving moves. Experiments confirm support-localization emerges earlier than frequency ranking.

Conclusion: Discrete diffusion models learn data support (validity structure) before learning within-support frequency distributions. The separation mechanism depends on diffusion type, providing insights into the hierarchical learning organization of denoising objectives.

Abstract: Discrete diffusion models are increasingly competitive for language modeling, yet it remains unclear how their denoising objectives organize learning. Although these objectives target the full data distribution, we show that the exact reverse process induces a hierarchy between coarse support information and finer frequency information. For uniform and absorbing (a.k.a. masking) diffusion, we prove that, in the small-noise regime of the final denoising steps, each single-token reverse edit decomposes into a leading scale, determined by whether it moves toward the data support (e.g., grammatically valid sentences), and a finer coefficient, determining relative probabilities within the same scale. Thus, recovering validity structure only requires learning the correct order of magnitude of reverse probabilities, whereas recovering data frequencies requires coefficient-level estimation. The separation is mechanism-dependent: uniform diffusion exhibits a trichotomy into validity-improving, validity-preserving, and validity-worsening edits, while absorbing diffusion places its leading-order mass on validity-improving moves. Experiments on a masked language diffusion model and synthetic regular-language tasks support these predictions: support-localization emerges earlier than within-support frequency ranking, and the contrast between uniform and absorbing diffusion matches the predicted rate separation. Together, our results suggest that discrete diffusion models learn data support before data frequencies.

</details>


### [119] [Dywave: Event-Aligned Dynamic Tokenization for Heterogeneous IoT Sensing Signal](https://arxiv.org/abs/2605.14014)
*Tomoyoshi Kimura,Denizhan Kara,Jinyang Li,Hongjue Zhao,Yigong Hu,Yizhuo Chen,Xiaomin Ouyang,Shengzhong Liu,Tarek Abdelzaher*

Main category: cs.LG

TL;DR: Dywave is a dynamic tokenization framework for IoT sensing signals that uses wavelet decomposition and adaptive compression to create compact, semantically-aligned token representations, achieving up to 12% higher accuracy and 75% token reduction compared to state-of-the-art methods.


<details>
  <summary>Details</summary>
Motivation: IoT sensing signals are non-stationary and multi-scale, making standard tokenization techniques inadequate for capturing their intrinsic temporal structures and underlying physical events. Existing methods struggle with these characteristics, leading to inefficient representations and reduced performance.

Method: Dywave employs a three-step approach: 1) wavelet-based hierarchical decomposition of signals, 2) identification of meaningful temporal boundaries corresponding to semantic events, and 3) adaptive compression of redundant intervals while preserving temporal coherence. This creates compact input representations aligned with signal semantics.

Result: Extensive evaluations on five real-world IoT datasets (activity recognition, stress assessment, nearby object detection) show Dywave outperforms state-of-the-art methods by up to 12% in accuracy. It reduces input token lengths by up to 75% across mainstream sequence models, improving computational efficiency while also exhibiting better robustness to domain shifts and varying sequence lengths.

Conclusion: Dywave provides an effective solution for tokenizing IoT sensing signals by aligning token boundaries with semantic events, offering superior performance, efficiency, and robustness compared to existing approaches, making it suitable for resource-constrained IoT applications.

Abstract: Internet of Things (IoT) systems continuously collect heterogeneous sensing signals from ubiquitous sensors to support intelligent applications such as human activity analysis, emotion monitoring, and environmental perception. These signals are inherently non-stationary and multi-scale, posing unique challenges for standard tokenization techniques. This paper proposes Dywave, a dynamic tokenization framework for IoT sensing signals that constructs compact input representations aligned with intrinsic temporal structures and underlying physical events. Dywave leverages wavelet-based hierarchical decomposition, identifies meaningful temporal boundaries corresponding to underlying semantic events, and adaptively compresses redundant intervals while preserving temporal coherence. Extensive evaluations on five real-world IoT sensing datasets across activity recognition, stress assessment, and nearby object detection demonstrate that Dywave outperforms state-of-the-art methods by up to 12% in accuracy, while improving computational efficiency by reducing input token lengths by up to 75% across mainstream sequence models. Moreover, Dywave exhibits improved robustness to domain shifts and varying sequence lengths.

</details>


### [120] [R2R2: Robust Representation for Intensive Experience Reuse via Redundancy Reduction in Self-Predictive Learning](https://arxiv.org/abs/2605.14026)
*Sanghyeob Song,Donghyeok Lee,Jinsik Kim,Sungroh Yoon*

Main category: cs.LG

TL;DR: Proposes R2R2 (Robust Representation via Redundancy Reduction), a regularization method for Self-Predictive Learning that mitigates overfitting in high data reuse regimes by addressing representation-level instability through a non-centered objective design.


<details>
  <summary>Details</summary>
Motivation: In data-scarce domains like robotics, intensive data reuse enhances efficiency but causes overfitting. Prior work focuses on critic bias, but representation-level instability in Self-Predictive Learning under high Update-to-Data regimes remains underexplored.

Method: Proposes R2R2 - a regularization method within Self-Predictive Learning framework. Theoretically identifies that standard zero-centering conflicts with SPL's spectral properties and designs a non-centered objective accordingly. Validates on SPL-native algorithms like TD7 and extends SimbaV2 by integrating a tailored SPL module (SimbaV2-SPL).

Result: Experiments across 11 continuous control tasks confirm R2R2 effectively mitigates overfitting. At UTD ratio of 20, it improves TD7 by ~22% and provides additional gains on top of SimbaV2-SPL, which itself establishes a new state-of-the-art performance.

Conclusion: R2R2 addresses the underexplored representation-level instability in Self-Predictive Learning under high data reuse regimes, providing an effective regularization approach that complements existing methods and enhances performance in data-scarce reinforcement learning applications.

Abstract: For reinforcement learning in data-scarce domains like real-world robotics, intensive data reuse enhances efficiency but induces overfitting. While prior works focus on critic bias, representation-level instability in Self-Predictive Learning (SPL) under high Update-to-Data (UTD) regimes remains underexplored. To bridge this gap, we propose Robust Representation via Redundancy Reduction (R2R2), a regularization method within SPL. We theoretically identify that standard zero-centering conflicts with SPL's spectral properties and design a non-centered objective accordingly. We verify R2R2 on SPL-native algorithms like TD7. Furthermore, to demonstrate its orthogonality to prior advancements, we extend the state-of-the-art SimbaV2, which originally lacks SPL, by integrating a tailored SPL module, termed SimbaV2-SPL. Experiments across 11 continuous control tasks confirm that R2R2 effectively mitigates overfitting; specifically, at a UTD ratio of 20, it improves TD7 by ~22% and provides additional gains on top of SimbaV2-SPL, which itself establishes a new state-of-the-art. The code can be found at: https://github.com/songsang7/R2R2

</details>


### [121] [Self-Pruned Key-Value Attention: Learning When to Write by Predicting Future Utility](https://arxiv.org/abs/2605.14037)
*Gergely Szilvasy,Manuel Faysse,Maria Lomeli,Matthijs Douze,Pierre-Emmanuel Mazaré,Loïc Cabannes,Wen-tau Yih,Hervé Jégou*

Main category: cs.LG

TL;DR: Self-Pruned Key-Value Attention (SP-KV) reduces transformer KV cache memory usage 3-10× by predicting utility of KV pairs and only keeping high-utility ones, trained end-to-end with minimal performance degradation.


<details>
  <summary>Details</summary>
Motivation: Modern language models process longer sequences in agentic paradigms, but efficient text generation is constrained by Key-Value cache memory footprint and bandwidth limitations. The increasing sequence lengths exacerbate memory usage and decoding speed issues.

Method: SP-KV uses a lightweight utility predictor to score each KV pair. Recent KVs stay in a local window, while older pairs are stored in cache only if predicted utility exceeds a threshold. The LLM and utility predictor are jointly trained end-to-end via next-token prediction loss, adapted from pretrained checkpoints.

Result: Dynamic sparsification reduces KV cache size by 3-10× (longer sequences more compressible), leading to significant memory usage and decoding speed improvements with minimal validation loss or downstream task performance degradation. Reveals structured layer- and head-specific sparsity patterns.

Conclusion: SP-KV effectively reduces KV-cache memory footprint while maintaining performance, with discovered sparsity patterns potentially guiding hybrid local-global attention architecture design for more efficient transformer-based language models.

Abstract: Under modern test-time compute and agentic paradigms, language models process ever-longer sequences. Efficient text generation with transformer architectures is increasingly constrained by the Key-Value cache memory footprint and bandwidth. To address this limitation, we introduce Self-Pruned Key-Value Attention (SP-KV), a mechanism designed to predict future KV utility in order to reduce the size of the long-term KV cache. This strategy operates at a fine granularity: a lightweight utility predictor scores each key-value pair, and while recent KVs are always available via a local window, older pairs are written in the cache and used in global attention only if their predicted utility surpasses a given threshold. The LLM and the utility predictor are trained jointly end-to-end exclusively through next-token prediction loss, and are adapted from pretrained LLM checkpoints.
  Rather than enforcing a fixed compression ratio, SP-KV performs dynamic sparsification: the mechanism adapts to the input and typically reduces the KV cache size by a factor of $3$ to $10\times$, longer sequences often being more compressible. This leads to vast improvements in memory usage and decoding speed, with little to no degradation of validation loss nor performance on a broad set of downstream tasks. Beyond serving as an effective KV-cache reduction mechanism, our method reveals structured layer- and head-specific sparsity patterns that we can use to guide the design of hybrid local-global attention architectures.

</details>


### [122] [Reliability-Gated Source Anchoring for Continual Test-Time Adaptation](https://arxiv.org/abs/2605.14063)
*Vikash Singh,Debargha Ganguly,Weicong Chen,Sabyasachi Sahoo,Sreehari Sankar,Biyao Zhang,Mohsen Harir,Shouren Wang,Osama Zafar,Christian Gagné,Vipin Chaudhary*

Main category: cs.LG

TL;DR: RMemSafe is a reliability-gated continual test-time adaptation method that automatically reduces source anchoring when the pretrained source model becomes unreliable, preventing blind anchoring failures.


<details>
  <summary>Details</summary>
Motivation: Existing continual test-time adaptation methods use a fixed anchor to a pretrained source model, but this becomes harmful when the source model becomes unreliable (e.g., on challenging datasets like CCC-Hard where ResNet-50 accuracy drops to ~1.3%). This "blind anchoring" failure mode causes methods to continue applying the same anchor strength even when the source is no longer reliable.

Method: RMemSafe extends ROID with a reliability gate based on the frozen source model's normalized predictive entropy. When entropy is high (source becomes unreliable), the gate attenuates all explicit source-coupled uses in the objective, causing the source anchor and agreement filter to vanish, and the objective reduces to a source-agnostic fallback comprising ROID's base losses plus marginal calibration.

Result: RMemSafe achieves lowest error on 8 of 9 matched-split continual-corruption cells and is the best reset-based method on all 9. It improves ROID+ASR by 1.05pp on ResNet-50 and 0.48pp on ViT-B/16. A controlled source-degradation sweep shows 1.13× shallower harm slope than ROID+ASR.

Conclusion: RMemSafe provides a principled solution to the blind anchoring problem in continual test-time adaptation by using entropy-based reliability gating to gracefully degrade source anchoring when the source model becomes unreliable, while explicitly recognizing the method's limitations (it detects high-entropy collapse but not confidently wrong low-entropy sources).

Abstract: Continual test-time adaptation (CTTA) updates a pretrained model online on an unlabeled, non-stationary stream while anchoring it to a frozen source checkpoint. This anchor is useful only when the source remains reliable. On CCC-Hard, however, a ResNet-50 source falls to approximately $1.3\%$ top-$1$ accuracy, while existing source-anchored CTTA methods continue applying the same anchor strength. We call this failure mode blind anchoring and propose RMemSafe, a reliability-gated extension of ROID that uses the frozen source's normalized predictive entropy to attenuate all explicit source-coupled uses in the objective. When the source posterior approaches uniformity, the gate closes: the source anchor and agreement filter vanish, and the objective reduces to a source-agnostic fallback comprising ROID's base losses plus marginal calibration. Combined with ASR, RMemSafe achieves the lowest error on $8$ of $9$ matched-split continual-corruption cells and is the best reset-based method on all $9$, improving ROID+ASR by $1.05$~pp on ResNet-50 and $0.48$~pp on ViT-B/16. A controlled source-degradation sweep shows a $1.13{\times}$ shallower harm slope than ROID+ASR, consistent with the graceful-decay prediction. The entropy gate detects high-entropy source collapse, not confidently wrong low-entropy sources; this scope is explicitly evaluated and discussed.

</details>


### [123] [Comparative Evaluation of Machine Learning Approaches for Minority-Class Financial Distress Prediction Under Class Imbalance Constraints](https://arxiv.org/abs/2605.14067)
*Karan Sehgal,Khawar Naveed Bhatti*

Main category: cs.LG

TL;DR: This paper compares statistical methods, ensemble learning, and neural models for financial distress prediction under class imbalance, finding that gradient-boosting methods outperform statistical classifiers while emphasizing interpretability and reproducibility.


<details>
  <summary>Details</summary>
Motivation: Financial distress prediction is challenging due to highly imbalanced datasets where distressed firms are a small minority, requiring effective methods to handle this imbalance in enterprise risk analysis.

Method: Comparative evaluation using structured preprocessing, SMOTE for imbalance mitigation, ensemble learning architectures (XGBoost, CatBoost, LightGBM, Random Forest), neural models, and SHAP-based feature attribution for explainability.

Result: Gradient-boosting approaches achieved improved minority-class sensitivity compared to baseline statistical classifiers under severe imbalance conditions, with the workflow emphasizing reproducibility, interpretability, and auditability.

Conclusion: The work provides an applied engineering evaluation to support reproducible and interpretable ML workflows for financial distress prediction under severe class imbalance, positioning gradient-boosting methods as effective for minority-class prediction with governance-oriented evaluation.

Abstract: Financial distress prediction remains a significant challenge in enterprise risk analysis due to the highly imbalanced nature of real-world financial datasets, where bankrupt or distressed firms typically constitute only a small minority of observations.
  This paper presents a comparative evaluation of classical statistical methods, ensemble learning approaches, and exploratory neural models for minority-class financial distress prediction under class imbalance constraints.
  The study incorporates structured preprocessing, imbalance mitigation using the Synthetic Minority Oversampling Technique (SMOTE), comparative evaluation across ensemble learning architectures including XGBoost, CatBoost, LightGBM, Random Forest, and explainability analysis using SHAP-based feature attribution methods.
  Experimental evaluation demonstrates that gradient-boosting approaches achieved improved minority-class sensitivity relative to baseline statistical classifiers under severe imbalance conditions. The workflow additionally emphasises reproducibility, interpretability, auditability, and governance-oriented machine learning evaluation within enterprise financial risk environments.
  The work is positioned as an applied engineering evaluation intended to support reproducible and interpretable machine learning workflows for financial distress prediction under severe class imbalance constraints.

</details>


### [124] [SurF: A Generative Model for Multivariate Irregular Time Series Forecasting](https://arxiv.org/abs/2605.14069)
*Mohammad R. Rezaei,Tejas Balaji,Rahul G. Krishnan*

Main category: cs.LG

TL;DR: SurF is a novel generative model for irregularly sampled multivariate event streams that uses the Time Rescaling Theorem as a learnable bijection to transform event sequences into i.i.d. noise, enabling multi-dataset pretraining and outperforming existing methods on several benchmarks.


<details>
  <summary>Details</summary>
Motivation: Current approaches struggle with irregularly sampled multivariate event streams: tokenization methods fail when inter-event intervals vary by orders of magnitude, and neural temporal point processes are bottlenecked by window-level numerical quadrature. There's a need for models that can handle heterogeneous event-stream datasets effectively.

Method: 1) SurF uses the Time Rescaling Theorem (TRT) as a learnable bijection between event sequences and i.i.d. unit-rate exponential noise, allowing single-model training across diverse datasets. 2) Three efficient parameterizations of cumulative intensity for long sequences. 3) Transformer-based encoder for multi-dataset pretraining.

Result: On six real-world benchmarks, SurF achieves best reported time RMSE on Earthquake, Retweet, and Taobao, and is within trial-level noise of strongest specialist on remaining three. Under leave-one-out protocol, held-out checkpoint beats every classical and neural-autoregressive baseline on 5/6 datasets and beats all baselines on Amazon and Earthquake.

Conclusion: SurF represents a significant advancement in generative modeling for irregular event streams, demonstrating strong performance across diverse datasets and laying groundwork for foundation models over asynchronous event streams.

Abstract: Irregularly sampled multivariate event streams remain a stubbornly difficult modality for generative modeling: tokenization-based approaches break down when inter-event intervals vary by orders of magnitude, and neural temporal point processes are bottlenecked by window-level numerical quadrature. We (i) propose SurF, a generative model that uses the Time Rescaling Theorem (TRT) as a learnable bijection between event sequences and i.i.d.\ unit-rate exponential noise, enabling a single model to be trained across heterogeneous event-stream datasets; (ii) three efficient parameterizations of the cumulative intensity that scale to long sequences; and (iii) a Transformer-based encoder for multi-dataset pretraining. On six real-world benchmarks, SurF achieves the best reported time RMSE on Earthquake, Retweet, and Taobao, and is within trial-level noise of the strongest specialist on the remaining three. Under a strict leave-one-out protocol, the held-out checkpoint beats every classical and neural-autoregressive baseline on 5/6 datasets and beats every baseline on Amazon and Earthquake, an initial step toward foundation models over asynchronous event streams.

</details>


### [125] [AttnGen: Attention-Guided Saliency Learning for Interpretable Genomic Sequence Classification](https://arxiv.org/abs/2605.14073)
*Rayhaneh Shabani Nia,Ali Karkehabadi*

Main category: cs.LG

TL;DR: AttnGen: Attention-guided training framework for genomic sequence classification that embeds interpretability into optimization via progressive masking of low-contribution nucleotides, improving accuracy, convergence, and biological relevance.


<details>
  <summary>Details</summary>
Motivation: Deep neural networks achieve strong performance in genomic sequence classification but lack interpretability - it's challenging to relate their predictions to biologically meaningful sequence patterns. Need methods that provide interpretability without sacrificing performance.

Method: AttnGen framework uses attention mechanism to compute nucleotide-level importance scores and progressively suppresses low-contribution positions during training. This encourages models to focus on compact sets of informative regions while reducing reliance on noisy elements.

Result: On demo_human_or_worm benchmark, AttnGen achieved 96.73% validation accuracy (vs. 95.83% for CNN baseline) with faster convergence and improved stability. Perturbation analysis showed model relies on small subset of positions - removing high-saliency nucleotides drops accuracy to near chance level. Optimal masking is 10-20% of positions for best trade-off.

Conclusion: Attention-guided masking improves classification performance while reshaping how models distribute importance across sequences, providing better interpretability. The approach may extend to more complex interpretable sequence modeling beyond short genomic sequences.

Abstract: Deep neural networks have achieved strong performance in genomic sequence classification; however, relating their predictions to biologically meaningful sequence patterns remains challenging. In this work, we present AttnGen, an attention-guided training framework that embeds interpretability directly into the optimization process. AttnGen computes nucleotide-level importance scores using an attention mechanism and progressively suppresses low-contribution positions during training. This encourages the model to focus its predictions on a compact set of informative regions while reducing reliance on noisy sequence elements. We evaluate AttnGen on the standardized demo_human_or_worm benchmark, a binary classification task over 200-nucleotide sequences. With moderate masking, AttnGen achieves a validation accuracy of 96.73%, outperforming a conventional CNN baseline with 95.83% accuracy, while also exhibiting faster convergence and improved training stability. To assess whether the learned importance scores reflect functionally relevant signal, we conduct perturbation-based analysis by removing high-saliency nucleotides. This causes accuracy to drop from 96.9% to near chance level on a 3,000-sequence evaluation set, indicating that the model relies on a relatively small subset of informative positions. Our analysis shows that masking 10--20% of positions provides the most favorable trade-off between predictive performance and interpretability. These results suggest that attention-guided masking not only improves classification performance but also reshapes how models distribute importance across sequence positions. Although this study focuses on short genomic sequences, the proposed approach may extend to more complex interpretable sequence modeling settings.

</details>


### [126] [Fair and Calibrated Toxicity Detection with Robust Training and Abstention](https://arxiv.org/abs/2605.14074)
*Mokshit Surana*

Main category: cs.LG

TL;DR: Fairness in toxicity classification requires evaluating ranking, calibration, and abstention together, as methods that differ only in aggregate performance show radically different failure patterns across subgroups.


<details>
  <summary>Details</summary>
Motivation: Current fairness evaluations focus on single axes (like ranking) but ignore how training interventions affect calibration and abstention, which are critical for real-world deployment safety.

Method: Compares Empirical Risk Minimization (ERM), instance-level reweighting, and Group DRO across three axes: ranking, calibration, and abstention. Combines these with temperature scaling, confidence-based abstention, and per-identity threshold optimization. Uses subgroup AUC, BPSN/BNSP AUC, error gaps, and per-subgroup Expected Calibration Error (ECE) with bootstrap confidence intervals.

Result: (1) ERM shows near-perfect aggregate calibration but significant miscalibration across all identity subgroups. (2) Training interventions reshape rather than eliminate disparity - reweighting improves ranking but worsens calibration gaps. (3) Post-hoc methods inherit training failures - temperature scaling fails with non-uniform miscalibration. (4) Abstention is itself unfair - confidence-based deferral benefits background content more than identity-mentioning content.

Conclusion: Safety-relevant AI fairness requires a multi-axis framework that evaluates ranking, calibration, and abstention together, as methods with similar aggregate performance can have dramatically different real-world harm patterns due to hidden failure modes across subgroups.

Abstract: Fairness in toxicity classification involves three integrated axes: ranking, calibration, and abstention. Training-time interventions and post-hoc safety mechanisms cannot be evaluated independently because the former determines the efficacy of the latter. We compare Empirical Risk Minimization (ERM), instance-level reweighting, and Group DRO across these axes, combined with temperature scaling, confidence-based abstention, and per-identity threshold optimization. Evaluation uses subgroup AUC, BPSN/BNSP AUC, error gaps, and per-subgroup Expected Calibration Error (ECE) with bootstrap CIs ($n = 1000$).
  We report four findings. (1) Calibration disparity is a hidden fairness violation. ERM has near-perfect aggregate calibration ($0.013$) but is significantly miscalibrated across all identity subgroups ($+0.029$ to $+0.134$). (2) Training interventions reshape rather than eliminate disparity. Reweighted ERM improves ranking (BPSN AUC $+0.06$ to $+0.12$) but worsens the calibration-fairness gap by up to $+0.232$. Group DRO eliminates calibration disparity but only by becoming uniformly miscalibrated globally (ECE $0.118$). (3) Post-hoc methods inherit training failure modes. Temperature scaling fails because miscalibration is non-uniform. Confidence-based abstention works under ERM but breaks under DRO, where the risk-coverage curve rises with deferral. (4) Abstention itself is unfair. Confidence-based deferral helps background content far more than identity-mentioning content. We argue that SRAI fairness requires a multi-axis framework: methods that differ only in aggregate ranking can differ sharply in failure modes that determine real-world harm.

</details>


### [127] [Rethinking Layer Relevance in Large Language Models Beyond Cosine Similarity](https://arxiv.org/abs/2605.14075)
*Cristian Hinostroza,Rodrigo Toro Icarte,Christ Devia,Andres Carvallo De Ferari,Eugenio Herrera-Berg,Denis Parra,Jorge F Silva*

Main category: cs.LG

TL;DR: Cosine similarity is a poor proxy for layer importance in LLMs; actual accuracy drop from layer removal is a better metric despite higher computational cost.


<details>
  <summary>Details</summary>
Motivation: Understanding LLM internal mechanisms is crucial for interpretability and optimization. While cosine similarity is widely used to assess layer relevance, there's concern about whether it accurately measures actual layer importance for model performance.

Method: The paper combines theoretical analysis showing cosine similarity can be arbitrarily low for crucial layers, with empirical evidence from various LLMs demonstrating weak correlation between cosine similarity and actual performance degradation. They propose using actual accuracy drop from layer removal as a more robust metric.

Result: Cosine similarity has weak/moderate correlation with performance degradation, making it misleading for assessing layer relevance. The proposed accuracy-drop metric provides more accurate layer importance assessment, enabling better pruning strategies and lightweight models.

Conclusion: Researchers should move beyond cosine similarity for layer relevance assessment in LLMs. Using actual performance degradation as a metric, despite higher computational cost, yields more reliable insights for interpretability and model optimization.

Abstract: Large language models (LLMs) have revolutionized natural language processing. Understanding their internal mechanisms is crucial for developing more interpretable and optimized architectures. Mechanistic interpretability has led to the development of various methods for assessing layer relevance, with cosine similarity being a widely used tool in the field. On this work, we demonstrate that cosine similarity is a poor proxy for the actual performance degradation caused by layer removal. Our theoretical analysis shows that a layer can exhibit an arbitrarily low cosine similarity score while still being crucial to the model's performance. On the other hand, empirical evidence from a range of LLMs confirms that the correlation between cosine similarity and actual performance degradation is often weak or moderate, leading to misleading interpretations of a transformer's internal mechanisms. We propose a more robust metric for assessing layer relevance: the actual drop in model accuracy resulting from the removal of a layer. Even though it is a computationally costly metric, this approach offers a more accurate picture of layer importance, allowing for more informed pruning strategies and lightweight models. Our findings have significant implications for the development of interpretable LLMs and highlight the need to move beyond cosine similarity in assessing layer relevance.

</details>


### [128] [Mini-JEPA Foundation Model Fleet Enables Agentic Hydrologic Intelligence](https://arxiv.org/abs/2605.14120)
*Mashrekur Rahman*

Main category: cs.LG

TL;DR: Mini-JEPAs: A fleet of small, specialized geospatial foundation models for environmental reasoning, combining multiple sensor types (optical, SAR, thermal, phenology, topography-soil) with a routing LLM for better hydrologic intelligence than generalist models.


<details>
  <summary>Details</summary>
Motivation: Generalist geospatial foundation models like AlphaEarth have limitations: they compromise on specialized hydrologic signals, are often inaccessible/expensive, require large-scale compute, and may not capture specific sensor modalities optimally.

Method: Developed Mini-JEPAs: five 22M-parameter Joint Embedding Predictive Architecture models with identical Vision Transformer backbone, each specialized for different sensor modalities (Sentinel-2 optical, Sentinel-1 SAR, MODIS thermal, multi-temporal Sentinel-2 phenology, topography-soil). Combined with a router LLM that reads modality references to select appropriate sensors.

Result: High predictive performance (R² up to 0.97 for elevation/temperature, 0.81 for precipitation). Mini-JEPAs show diverse geometric structures (participation ratios 8.9-20.2, intrinsic dimensionalities 2.3-9.0). Joint models add predictive value beyond AlphaEarth alone (ΔR² up to 0.031). Router LLM achieves perfect hit rate on curated questions. Dual retrieval with routed fleet outperforms AlphaEarth alone on physics-matched questions (Cohen's d=1.10, p=0.031).

Conclusion: Specialized Mini-JEPAs with routing capabilities provide more accessible, compute-efficient hydrologic intelligence that outperforms generalist models for targeted environmental questions, enabling operational deployment with modest computational resources.

Abstract: Geospatial foundation models compress multispectral observations into dense embeddings increasingly used in natural-language environmental reasoning systems. A single planetary-scale model, e.g. Google AlphaEarth, handles broad characterization well but may compromise on specialized hydrologic signals. Such generalist models are also often inaccessible, expensive, and require large-scale compute. We propose Mini-JEPAs: a fleet of small sensor-specialized Joint Embedding Predictive Architecture (JEPA) foundation models consulted by a routing agent for specialized questions. We pretrained five 22M-parameter Mini-JEPAs sharing an identical Vision Transformer backbone, JEPA recipe, and 64-d output space, using Sentinel-2 optical, Sentinel-1 SAR, MODIS thermal, multi-temporal Sentinel-2 phenology, and a topography-soil stack. Each Mini-JEPA reconstructs the variable matched to its sensor, with cross-validated $R^2$ reaching 0.97 for elevation, 0.97 for temperature, and 0.81 for precipitation. The five manifolds differ in geometric structure, with global participation ratios from 8.9 to 20.2 and local intrinsic dimensionalities from 2.3 to 9.0. Joint topography-soil and phenology models add predictive value beyond AlphaEarth alone for soil moisture, aridity, and precipitation ($ΔR^2$ up to 0.031). A router LLM reads per-modality references and selects appropriate sensors with a perfect hit rate over a curated question set. In paired LLM-as-Judge evaluation, dual retrieval over AlphaEarth and the routed fleet outperforms AlphaEarth alone on physics-matched questions (Cohen's $d = 1.10$, $p = 0.031$). Locally-trained Mini-JEPAs can be operationalized for hydrologic intelligence with modest compute.

</details>


### [129] [Reinforcement Learning for Tool-Calling Agents in Fast Healthcare Interoperability Resources (FHIR)](https://arxiv.org/abs/2605.14126)
*Marius S. Knorr,Robert Müller,Jan P. Bremer,Nils Schweingruber*

Main category: cs.LG

TL;DR: RL post-training on a Qwen3-8B model improves FHIR data reasoning from 50% to 77% correctness on FHIR-AgentBench while enforcing data integrity constraints.


<details>
  <summary>Details</summary>
Motivation: FHIR is the dominant healthcare data exchange standard where electronic health records form directed graphs. Answering clinical questions requires multi-step reasoning across resource types, but current tool-augmented LLM agents often select wrong resources or violate traversal constraints on real-world hospital data.

Method: Frames reasoning on FHIR as sequential decision-making over a queryable structured graph. Implements multi-turn CodeAct agent and post-trains it with reinforcement learning using custom harness and tools. Uses LLM Judge to provide execution-grounded rewards for training.

Result: RL post-training significantly improves performance while enforcing data-integrity constraints. Answer correctness improves from 50% (o4-mini baseline) to 77% on FHIR-AgentBench using smaller and cheaper Qwen3-8B model.

Conclusion: Presents an end-to-end post-training pipeline (environment building, harness construction, model training, custom evaluation) that reliably improves multi-turn reasoning over structured clinical graphs, demonstrating RL post-training can enhance LLM performance on complex healthcare data reasoning tasks.

Abstract: Fast Healthcare Interoperability Resources (FHIR) is the dominant standard for interoperable exchange of healthcare data. In FHIR, electronic health records form a directed graph of resources. Answering clinically meaningful questions over FHIR requires agents to perform multi-step reasoning, filtering, and aggregation across multiple resource types. Prior work shows that even tool-augmented LLM agents (retrieval, code execution, multi-turn planning) often select the wrong resources or violate traversal constraints. We study this problem in the context of FHIR-AgentBench, a benchmark for realistic question answering over real-world hospital data, and frame reasoning on FHIR as a sequential decision-making problem over a queryable structured graph. We implement a multi-turn CodeAct agent and post-train it with reinforcement learning using a custom harness and tools. A LLM Judge provides execution-grounded rewards. Compared to prompt-based, closed-model baselines, RL post-training improves performance while enforcing data-integrity constraints. Empirically, our approach improves answer correctness from 50% (o4-mini) to 77% on FHIR-AgentBench using a smaller and cheaper Qwen3-8B model. We present an end-to-end post-training pipeline (environment building, harness construction, model training and custom evaluation) that reliably improves multi-turn reasoning over structured clinical graphs.

</details>


### [130] [bde: A Python Package for Bayesian Deep Ensembles via MILE](https://arxiv.org/abs/2605.14146)
*Vyron Arvanitis,Angelos Aslanidis,Emanuel Sommer,David Rügamer*

Main category: cs.LG

TL;DR: bde is a Python package for Bayesian Deep Ensembles using MILE inference, offering scikit-learn compatible estimators for fast training, MCMC sampling, and uncertainty quantification on tabular data.


<details>
  <summary>Details</summary>
Motivation: There's a need for user-friendly tools that combine Bayesian methods with deep learning for tabular data, particularly for uncertainty quantification and efficient training while maintaining compatibility with existing machine learning workflows.

Method: The package implements Microcanonical Langevin Ensembles (MILE), a sampling-based inference method, using efficient JAX implementation. It provides scikit-learn compatible estimators for both regression and classification tasks.

Result: bde enables fast training, efficient Markov Chain Monte Carlo sampling, and reliable uncertainty quantification for Bayesian Deep Ensembles, specifically optimized for tabular data with scikit-learn API compatibility.

Conclusion: bde successfully fills the gap for accessible Bayesian Deep Ensemble tools for tabular data, offering both theoretical rigor through MILE implementation and practical usability through scikit-learn compatibility and JAX-based efficiency.

Abstract: bde is a user-friendly Python package for Bayesian Deep Ensembles with a particular focus on tabular data. Built on an efficient JAX implementation of the sampling-based inference method Microcanonical Langevin Ensembles (MILE), it provides scikit-learn compatible estimators for fast training, efficient Markov Chain Monte Carlo sampling, and uncertainty quantification in both regression and classification tasks.

</details>


### [131] [A Systematic Evaluation of Imbalance Handling Methods in Biomedical Binary Classification](https://arxiv.org/abs/2605.14147)
*Jiandong Chen,Lingjie Su,Le Peng,Yash Travadi,Rui Zhang,Ju Sun*

Main category: cs.LG

TL;DR: IHMs like ROS and RW benefit complex models on unstructured data, while simpler models see no advantage; RUS/SMOTE degrade performance.


<details>
  <summary>Details</summary>
Motivation: To systematically examine how imbalance handling methods (IHMs) impact predictive performance in biomedical binary classification, considering the interplay between model complexity and diverse data modalities.

Method: Evaluated five IHMs (RUS, ROS, SMOTE, RW, DMO) against RAW baseline across three biomedical datasets (MIMIC-III, ADE-Corpus-V2, MURA) representing tabular, text, and image modalities. Tested on models ranging from logistic regression/RF to DNNs (MLP, BiLSTM, BERT, DenseNet, DINOv2).

Result: 1) IHMs showed no significant advantage for simple models (logistic regression) on tabular data. 2) Clear benefits for complex models on unstructured data: ROS and RW consistently enhanced powerful models, DMO worked primarily for unstructured text/image data. 3) RUS and SMOTE consistently degraded performance.

Conclusion: IHM effectiveness depends on both model complexity and data modality. Performance gains are most pronounced when using appropriate IHMs (ROS, RW, DMO) with high-complexity models.

Abstract: Objective: The primary goal of this study was to systematically examine the impact of commonly used imbalance handling methods (IHMs) on predictive performance in biomedical binary classification, considering the interplay between model complexity and diverse data modalities.
  Material and Methods: We evaluated five representative IHMs: random undersampling (RUS), random oversampling (ROS), SMOTE, re-weighting (RW), and direct F1-score optimization (DMO), against a raw training (RAW) baseline. The evaluation encompassed three public biomedical datasets: MIMIC-III (tabular), ADE-Corpus-V2 (text), and MURA (image), spanning three common biomedical data modalities. To assess varying model complexity, we employed a range of architectures, from classical logistic regression and random forest to deep neural networks, including multilayer perceptron (MLP), BiLSTM, BERT, DenseNet, and DINOv2.
  Results: For simpler models such as logistic regression on tabular data, IHMs yielded no significant advantage over the RAW baseline, aligning with prior findings. However, clear benefits were observed for more complex models and unstructured data: (a) ROS and RW consistently enhanced the performance of powerful models; (b) direct F1-score optimization demonstrated utility primarily for unstructured text and image data; and (c) RUS and SMOTE consistently degraded performance and are therefore not recommended.
  Conclusion: The effectiveness of IHMs depends on both model complexity and data modality. Performance gains are most pronounced when leveraging appropriate IHMs, such as ROS, RW, and DMO, on high-complexity models.

</details>


### [132] [Uncovering Trajectory and Topological Signatures in Multimodal Pediatric Sleep Embeddings](https://arxiv.org/abs/2605.14156)
*Scott Ye,Harlin Lee*

Main category: cs.LG

TL;DR: Multimodal embeddings from pediatric sleep analysis can be enhanced with geometric, topological, and clinical features to improve diagnostic prediction and model calibration under extreme class imbalance.


<details>
  <summary>Details</summary>
Motivation: While generative models show promise in pediatric sleep analysis, the latent structure of their multimodal embeddings remains poorly understood. There's a need to understand what diagnostic information is contained in sequences of 30-second PSG epochs and whether additional features can enhance task performance.

Method: Used a multimodal masked autoencoder to embed pediatric PSG epochs, then augmented embeddings with: 1) PHATE-derived per-epoch coordinates and whole-night movement descriptors, 2) persistent homology summaries of embedding clouds, and 3) EHR data. Tested with simple linear and MLP models chosen for interpretability rather than SOTA performance.

Result: Geometric, topological, and clinical features each provided complementary gains. For binary predictions, AUPRC improved from 0.26 to 0.34 for desaturation, 0.31 to 0.48 for EEG arousal, 0.09 to 0.22 for hypopnea, and 0.05 to 0.14 for apnea. Full fusion models performed best with improved calibration across all four binary tasks.

Conclusion: Latent geometry/topology and EHR data offer complementary, interpretable signals beyond embeddings alone, improving calibration and robustness under extreme class imbalance in pediatric sleep analysis.

Abstract: While generative models have shown promise in pediatric sleep analysis, the latent structure of their multimodal embeddings remains poorly understood. This work investigates session-wide diagnostic information contained in the sequences of 30-second pediatric PSG epochs embedded by a multimodal masked autoencoder. We test whether augmenting embeddings with PHATE-derived per-epoch coordinates and whole-night movement descriptors, persistent homology summaries of the embedding cloud, and EHR yields task-relevant signals. Simple linear and MLP models, chosen for interpretability rather than state-of-the-art performance, show that geometric, topological, and clinical features each provide complementary gains. For binary predictions, feature importance is task-dependent, and more expressive late-fusion models generally perform better, with AUPRC improving from 0.26 to 0.34 for desaturation, 0.31 to 0.48 for EEG arousal, 0.09 to 0.22 for hypopnea, and 0.05 to 0.14 for apnea. We also report Brier score and Expected Calibration Error, where the full fusion model yields the best calibration across all four binary tasks. Our study reveals that latent geometry/topology and EHR offer complementary, interpretable signals beyond embeddings, improving calibration and robustness under extreme imbalance.

</details>


### [133] [Finite Sample Bounds for Learning with Score Matching](https://arxiv.org/abs/2605.14168)
*Devin Smedira,Abhijith Jayakumar,Sidhant Misra,Marc Vuffray,Andrey Y. Lokhov*

Main category: cs.LG

TL;DR: First non-asymptotic sample complexity bounds for learning polynomial exponential families with score matching, showing polynomial dependence on dimension.


<details>
  <summary>Details</summary>
Motivation: Score matching has become widely used for learning exponential families with continuous variables due to computational advantages over maximum likelihood, but theoretical understanding of its statistical properties is still lacking.

Method: Provides non-asymptotic sample complexity analysis for learning structure of exponential families of polynomials using score matching methodology.

Result: Derived sample bounds show polynomial dependence on model dimension, representing the first non-asymptotic analysis of score matching for this problem class.

Conclusion: The work provides crucial theoretical foundations for score matching by establishing the first non-asymptotic sample complexity results, addressing an important gap in understanding statistical properties of this widely used method.

Abstract: Learning of continuous exponential family distributions with unbounded support remains an important area of research for both theory and applications in high-dimensional statistics. In recent years, score matching has become a widely used method for learning exponential families with continuous variables due to its computational ease when compared against maximum likelihood estimation. However, theoretical understanding of the statistical properties of score matching is still lacking. In this work, we provide a non-asymptotic sample complexity analysis for learning the structure of exponential families of polynomials with score matching. The derived sample bounds show a polynomial dependence on the model dimension. These bounds are the first of its kind, as all prior work has shown only asymptotic bounds on the sample complexity.

</details>


### [134] [CSI-JEPA: Towards Foundation Representations for Ubiquitous Sensing with Minimal Supervision](https://arxiv.org/abs/2605.14171)
*Xuanhao Luo,Zhizhen Li,Yuchen Liu*

Main category: cs.LG

TL;DR: CSI-JEPA: A self-supervised predictive representation learning framework for Wi-Fi sensing that learns reusable temporal-spectral representations from unlabeled CSI data using masked prediction, achieving state-of-the-art performance with minimal labeled data.


<details>
  <summary>Details</summary>
Motivation: Traditional CSI sensing models require task-specific supervised training with substantial labeled data, which is costly to collect and limits scalability. In real deployments, unlabeled CSI data is abundant but labeled data is scarce, creating a need for label-efficient learning approaches.

Method: The framework uses self-supervised predictive representation learning where it predicts latent features of masked channel regions from visible context. It tokenizes channel-response amplitude windows along time and subcarrier dimensions, employs channel variation-aware masking that targets regions with stronger local variations, and after pretraining, freezes the encoder as a backbone with lightweight task-specific adapters for downstream tasks.

Result: Evaluation on seven real-world Wi-Fi sensing tasks shows CSI-JEPA outperforms competitive baselines with up to 10.64 percentage points mean accuracy gain over state-of-the-art supervised Transformers and achieves label savings up to 98.0% with matched budget.

Conclusion: CSI-JEPA provides an effective self-supervised approach for learning reusable temporal-spectral representations from unlabeled CSI data, enabling label-efficient multi-task Wi-Fi sensing with superior performance and significant reductions in labeled data requirements.

Abstract: Channel state information (CSI) provides a widely available sensing modality for human and environment perception, but existing CSI sensing models usually rely on task-specific supervised training and require substantial labeled data for each task, device, user, or environment. This limits their scalability in practical deployments where unlabeled CSI is abundant but labeled data is costly to collect. In this paper, we present CSI-JEPA, a self-supervised predictive representation learning framework for label-efficient, multi-task Wi-Fi sensing. CSI-JEPA learns reusable temporal-spectral representations from unlabeled CSI samples by predicting latent features of masked channel regions from visible context. To better match the physical structure of CSI, CSI-JEPA tokenizes channel-response amplitude windows along the time and subcarrier dimensions. It then introduces a channel variation-aware masking strategy that samples predictive targets from regions with stronger local temporal and subcarrier-domain variations. After pretraining, the encoder is frozen and used as a backbone, with lightweight task-specific adapters added for downstream sensing tasks. We evaluate CSI-JEPA on seven real-world Wi-Fi sensing tasks spanning diverse objectives and deployment settings. The results show that CSI-JEPA improves downstream sensing performance over competitive baselines, achieving up to 10.64 percentage points mean accuracy gain over state-of-the-art supervised Transformer and matched-budget label savings of up to 98.0%.

</details>


### [135] [LLMs Know When They Know, but Do Not Act on It: A Metacognitive Harness for Test-time Scaling](https://arxiv.org/abs/2605.14186)
*Qi Cao,Yufan Wang,Peijia Qin,Shuhao Zhang,Pengtao Xie*

Main category: cs.LG

TL;DR: LLMs show metacognitive signals that can be turned into effective inference control through a metacognitive harness that uses pre-solve and post-solve monitoring to decide when to trust solutions, retry, or aggregate multiple attempts.


<details>
  <summary>Details</summary>
Motivation: LLMs expose useful self-monitoring signals (pre-solve and post-solve judgments) but these signals are typically measured in isolation rather than used to actively control inference. The researchers wanted to determine if LLMs possess latent metacognitive ability that can be transformed into effective test-time control.

Method: Inspired by the Nelson-Narens theory from cognitive psychology, they propose a metacognitive harness that separates monitoring from reasoning. For each problem, the model first reports a pre-solve feeling-of-knowing (FOK) signal; after each solve attempt, it reports a post-solve judgment-of-learning (JOL) signal. The harness uses these signals as an explicit control interface: deciding when to trust current solutions, when to retry with metacognitive feedback, and when to pass multiple attempts to a final aggregator.

Result: The metacognitive harness substantially improves performance across text, code, and multimodal reasoning benchmarks. On evaluated public benchmark snapshots, it raised pooled accuracy from 48.3 to 56.9 and exceeded the strongest listed leaderboard entries on three primary evaluation settings: HLE-Verified, LiveCodeBench v6, and R-Bench-V.

Conclusion: Strong LLMs may already possess useful metacognitive ability but require an explicit control harness to effectively act on it during reasoning. The results demonstrate that turning passive monitoring signals into active control mechanisms can significantly enhance model performance without parameter updates or benchmark-specific fine-tuning.

Abstract: Large language models (LLMs) often expose useful signals of self-monitoring: before solving a problem, they can estimate whether they are likely to succeed, and after solving it, they can judge whether their answer is likely to be correct. However, these signals are typically measured or elicited in isolation, rather than used to control inference. In this work, we ask whether LLMs possess latent metacognitive ability that can be turned into effective test-time control. Inspired by the Nelson--Narens theory from cognitive psychology, we propose a metacognitive harness that separates monitoring from reasoning. For each problem, the model first reports a pre-solve feeling-of-knowing (FOK) signal; after each solve attempt, it reports a post-solve judgment-of-learning (JOL) signal. Rather than treating these signals as passive confidence estimates, the harness turns them into an explicit control interface for reasoning: it decides when to trust the current solution, when to retry with compact metacognitive feedback, and when to pass multiple attempts to a final aggregator. Across text, code, and multimodal reasoning benchmarks, our harness substantially improves a fixed Claude Sonnet-4.6 base model without parameter updates or benchmark-specific fine-tuning. On the evaluated public benchmark snapshots, it raises pooled accuracy from 48.3 to 56.9 and exceeds the strongest listed leaderboard entries on the three primary evaluation settings: HLE-Verified, LiveCodeBench v6, and R-Bench-V. These results suggest that strong LLMs may already possess useful metacognitive ability, but require an explicit control harness to act on it during reasoning.

</details>


### [136] [How to Scale Mixture-of-Experts: From muP to the Maximally Scale-Stable Parameterization](https://arxiv.org/abs/2605.14200)
*Leena Chennuru Vankadara,Moritz Haas,Luke Hayward,Sebastian Bordt,Alessandro Breccia*

Main category: cs.LG

TL;DR: The paper develops principled scaling laws for MoE architectures by analyzing three scaling regimes and deriving Maximally Scale-Stable Parameterization (MSSP) that ensures learning rate transfer and monotonic improvement with scale.


<details>
  <summary>Details</summary>
Motivation: Current MoE architectures lack principled understanding of how hyperparameters should scale with network width, expert width, number of experts, sparsity, and depth to ensure stability and optimal performance at scale.

Method: Used Dynamical Mean Field Theory (DMFT) to analyze three scaling regimes: (I) co-scaling width and expert width, (II) co-scaling width, experts, and sparsity, (III) full proportional scaling. Developed MSSP for SGD and Adam based on maximal scale stability desiderata.

Result: Showed existing μP parameterization fails to ensure monotonic improvement or learning-rate transfer. Derived MSSP that robustly recovers learning rate transfer and monotonic improvement across all scaling regimes through DMFT analysis.

Conclusion: Combined with depth-scaling theory, MSSP provides complete scaling prescription for MoE architectures as function of width, depth, expert width, and number of experts, enabling principled scaling to larger models.

Abstract: Recent frontier large language models predominantly rely on Mixture-of-Experts (MoE) architectures. Despite empirical progress, there is still no principled understanding of how hyperparameters should scale with network width $N$, expert width $N_e$, number of experts $M$, sparsity $K$, and depth $L$ to ensure both stability and optimal performance at scale. We take a principled step toward resolving this gap by analyzing three different scaling regimes: (I) co-scaling $N\asymp N_e$, (II) co-scaling $N\asymp M\asymp K$, and (III) full proportional scaling of $N, N_e, M$, and $K$. For each regime, we develop a novel Dynamical Mean Field Theory (DMFT) description of the limiting training dynamics of MoEs that provides a formal foundation for our analysis. Within this framework, we derive the unique parameterization for SGD and Adam satisfying all maximal-update ($μ$) desiderata. We then show that the resulting $μ$P prescription does not reliably induce monotonic improvement with scale or robust learning-rate transfer. We trace these pathologies to scale-dependent observables in the aggregation dynamics, which motivates a refined set of desiderata that we term maximal scale stability. Guided by this principle, we derive a Maximally Scale-Stable Parameterization (MSSP) for both SGD and Adam in all three scaling regimes, and characterize the corresponding limiting dynamics - qualitatively distinct from the $μ$P limit - through a separate DMFT analysis. Experiments verify that MSSP robustly recovers learning rate transfer and monotonic improvement with scale across regimes. Combined with existing depth-scaling theory, these results provide a complete scaling prescription for MoE architectures as a function of width, depth, expert width, and number of experts.

</details>


### [137] [Towards Fine-Grained and Verifiable Concept Bottleneck Models](https://arxiv.org/abs/2605.14210)
*Yingying Fang,Haijie Xu,Shuang Wu,Mariathasan Anish,Guang Yang*

Main category: cs.LG

TL;DR: The paper introduces a fine-grained Concept Bottleneck Model (CBM) framework that grounds each concept in localized visual evidence, improving verifiability and transparency while maintaining predictive performance comparable to standard CBMs.


<details>
  <summary>Details</summary>
Motivation: Existing Concept Bottleneck Models (CBMs) struggle to verify whether predicted concepts correspond to correct visual evidence, limiting their reliability and transparency. Current CBMs lack mechanisms to ensure concepts are learned from intended visual features rather than spurious correlations.

Method: The authors propose a fine-grained CBM framework that grounds each concept in localized visual evidence, enabling direct inspection of where and how concepts are encoded in the image. Unlike post-hoc attribution methods, this approach validates both the presence and correctness of concept representations.

Result: Experiments on medical imaging benchmarks show that the learned concept space is information-complete and achieves predictive performance comparable to standard CBMs, while substantially improving transparency. The framework enables users to interpret predictions and verify that models learn intended concepts rather than spurious correlations.

Conclusion: The proposed framework bridges interpretability with verifiability, enhancing the trustworthiness of CBMs and establishing a principled mechanism for human-model interaction at the concept level. It paves the way toward more reliable and clinically actionable concept-based learning systems.

Abstract: Concept Bottleneck Models (CBMs) offer interpretable alternatives to black-box predictors by introducing human-relatable concepts before the final output. However, existing CBMs struggle to verify whether predicted concepts correspond to the correct visual evidence, limiting their reliability. We propose a fine-grained CBM framework that grounds each concept in localized visual evidence, enabling direct inspection of where and how concepts are encoded. This design allows users to interpret predictions and verify that the model learns intended concepts rather than spurious correlations. Experiments on medical imaging benchmarks show that our learned concept space is information-complete and achieves predictive performance comparable to standard CBMs, while substantially improving transparency. Unlike post-hoc attribution methods, our framework validates both the presence and correctness of concept representations, bridging interpretability with verifiability. Our approach enhances the trustworthiness of CBMs and establishes a principled mechanism for human-model interaction at the concept level, paving the way toward more reliable and clinically actionable concept-based learning systems.

</details>


### [138] [PreFT: Prefill-only finetuning for efficient inference](https://arxiv.org/abs/2605.14217)
*Andrew Lanpouthakoun,Aryaman Arora,Zhengxuan Wu,Dhruv Pai,Ben Keigwin,Dan Jurafsky,Christopher Potts*

Main category: cs.LG

TL;DR: PreFT (Prefill-only Finetuning) applies adapters only to prefill tokens and discards them during decoding, significantly improving throughput for multi-adapter serving with minimal performance impact.


<details>
  <summary>Details</summary>
Motivation: Serving user-specific PEFTs harms throughput despite specialized optimizations due to fundamental mismatch between prefill and decode phases, where decode has much lower throughput when serving multiple adapters.

Method: Propose PreFT where adapters are only applied to prefill tokens and discarded afterwards, with efficient implementation of prefill-only LoRA and ReFT on vLLM inference engine.

Result: PreFT achieves 1.9× throughput improvement when serving 512 adapters on Llama 3.1 70B; on SFT tasks evaluation loss is higher but compensatable by increasing rank; on RL tasks PreFTs approach parity with standard PEFTs.

Conclusion: Prefill-only adaptation provides more favorable accuracy-throughput tradeoff than existing PEFTs for personalized LLM serving, validating it as an efficient approach for multi-user scenarios.

Abstract: Large language models can now be personalised efficiently at scale using parameter efficient finetuning methods (PEFTs), but serving user-specific PEFTs harms throughput, even with specialised kernels and memory management techniques. This is because, theoretically and empirically, a mismatch exists between prefill (processing a large number of tokens at once) and decode (generating a single token autoregressively): the latter has far lower throughput when serving multiple adapters. Rather than optimising performance relative to parameter count, for efficient multi-adapter serving, we instead ought to optimise performance relative to serving throughput. We therefore propose PreFT (Prefill-only Finetuning), wherein we only apply the adapter to prefill tokens and discard it afterwards. PreFT significantly increases throughput with minimal effect on performance. We develop and release an efficient implementation of two prefill-only PEFTs, LoRA and ReFT, on the vLLM inference engine. We first show that serving multi-user PreFTs is more efficient than traditional PEFTs ($1.9\times$ the throughput when serving $512$ adapters on Llama 3.1 70B). Then, we compare the performance of prefill-only vs. all-token adapters on a variety of supervised finetuning and reinforcement learning tasks with LMs at varying scales. On SFT, we observe that the evaluation loss of PreFTs is higher than PEFTs, but can be compensated by increasing rank with nearly no reduction in throughput. On RL, we consistently find that PreFTs approach parity with standard PEFTs. Together, this work validates prefill-only adaptation of LLMs as a more favourable accuracy-throughput tradeoff than existing PEFTs for personalised serving.

</details>


### [139] [Diagnosing Training Inference Mismatch in LLM Reinforcement Learning](https://arxiv.org/abs/2605.14220)
*Tianle Zhong,Neiwen Ling,Yifan Pi,Zijun Wei,Tianshu Yu,Geoffrey Fox,Peng Wu,Xiao Yu*

Main category: cs.LG

TL;DR: Training-Inference Mismatch (TIM) in LLM RL systems occurs when rollout generation and policy optimization stages produce numerically different token probabilities despite using the same model weights, causing training collapse.


<details>
  <summary>Details</summary>
Motivation: Modern LLM RL systems separate rollout generation from policy optimization, expecting exact token probability matching. However, implementation differences cause Training-Inference Mismatch (TIM), which is difficult to detect due to entanglement with off-policy drift and stabilization mechanisms.

Method: The researchers isolate TIM using a zero-mismatch diagnostic setting called VeXact to demonstrate that small token-level numerical disagreements independently cause training collapse. They show TIM changes the effective optimization problem and identify remedies to mitigate it.

Result: Small token-level numerical disagreements in TIM can independently cause training collapse. TIM changes the effective optimization problem and is not benign numerical noise but a systems-level perturbation affecting LLM RL stability.

Conclusion: TIM should be treated as a first-order factor in analyzing LLM RL stability, not as benign numerical noise, and requires specific remedies for mitigation.

Abstract: Modern LLM RL systems separate rollout generation from policy optimization. These two stages are expected to produce token probabilities that match exactly. However, implementation differences can make them assign different values to the same sequence under the same model weights, inducing Training-Inference Mismatch (TIM). TIM is difficult to inspect because it is entangled with off-policy drift and common stabilization mechanisms. In this work, we isolate TIM in a zero-mismatch diagnostic setting (VeXact), and show that small token-level numerical disagreements can independently cause training collapse. We further show that TIM changes the effective optimization problem, and identify a set of remedies that could mitigate TIM. Our results suggest that TIM is not benign numerical noise, but a systems-level perturbation that should be treated as a first-order factor in analyzing LLM RL stability.

</details>


### [140] [DT-Transformer: A Foundation Model for Disease Trajectory Prediction on a Real-world Health System](https://arxiv.org/abs/2605.14227)
*Yunying Zhu,Andrew R Weckstein,Kueiyu Joshua Lin,Jie Yang*

Main category: cs.LG

TL;DR: DT-Transformer is a foundation model trained on 57.1M EHR entries from 1.7M patients across 11 hospitals, achieving strong disease trajectory prediction with median AUC of 0.871 across 896 disease categories.


<details>
  <summary>Details</summary>
Motivation: Accurate disease trajectory prediction is crucial for early intervention and resource allocation. Models trained on curated research cohorts or single-hospital datasets fail to capture real-world clinical complexity and only show fragments of patient trajectories. There's a need for models that reflect routine deployment settings using large, multi-hospital health systems.

Method: Developed DT-Transformer, a foundation model trained on 57.1M structured EHR entries from 1.7M patients across the Mass General Brigham health system (11 hospitals plus outpatient clinics). The model was validated using both held-out and prospective validation settings.

Result: DT-Transformer achieved strong discrimination with median age- and sex-stratified AUC of 0.871 across 896 disease categories in next-event prediction. All disease categories exceeded AUC 0.5, showing robust predictive performance across diverse conditions.

Conclusion: Health system-scale training using multi-hospital EHR data enables development of foundation models that are well-suited for real-world clinical forecasting. The success of DT-Transformer supports this approach for creating models that better reflect clinical complexity and deployment settings.

Abstract: Accurate disease trajectory prediction is critical for early intervention, resource allocation, and improving long-term outcomes. While electronic health records (EHRs) provide a rich longitudinal view of patient health in clinical environments, models trained on curated research cohorts may not reflect routine deployment settings, and those trained on single-hospital datasets capture only fragments of each patient's trajectory. This highlights the importance of leveraging large, multi-hospital health systems for training and validation to better reflect real-world clinical complexity. In this work, we develop DT-Transformer, a foundation model trained on 57.1M structured EHR entries over 1.7M patients from Mass General Brigham (MGB), spanning 11 hospitals and a broad network of outpatient clinics. DT-Transformer achieves strong discrimination in both held-out and prospective validation settings. Next-event prediction achieves a median age- and sex-stratified AUC of 0.871 across 896 disease categories, with all categories exceeding AUC 0.5. These results support health system-scale training as a path toward foundation models suited to real-world clinical forecasting.

</details>


### [141] [AudioMosaic: Contrastive Masked Audio Representation Learning](https://arxiv.org/abs/2605.14231)
*Hanxun Huang,Qizhou Wang,Xingjun Ma,Cihang Xie,Christopher Leckie,Sarah Erfani*

Main category: cs.LG

TL;DR: AudioMosaic introduces a contrastive learning-based audio encoder using structured time-frequency masking to create positive pairs, enabling efficient training and achieving SOTA performance on audio benchmarks.


<details>
  <summary>Details</summary>
Motivation: While generative approaches dominate audio self-supervised learning, contrastive methods remain under-explored due to challenges in designing effective audio augmentations and requiring large batch sizes for training.

Method: AudioMosaic uses structured time-frequency masking on spectrogram patches to construct positive pairs for contrastive learning, reducing memory usage and enabling efficient large-batch pre-training.

Result: The method achieves state-of-the-art performance on standard audio benchmarks under both linear probing and fine-tuning, learns more discriminative utterance-level representations, and improves performance on audio-language tasks when integrated with audio-language models.

Conclusion: AudioMosaic demonstrates that contrastive learning with structured time-frequency masking is an effective approach for audio SSL, producing transferable representations across datasets, domains, and acoustic conditions while being computationally efficient.

Abstract: Audio self-supervised learning (SSL) aims to learn general-purpose representations from large-scale unlabeled audio data. While recent advances have been driven mainly by generative reconstruction objectives, contrastive approaches remain less explored, partly due to the difficulty of designing effective audio augmentations and the large batch sizes required for contrastive pre-training. We introduce \textbf{AudioMosaic}, a contrastive learning-based audio encoder for general audio understanding. During pre-training, AudioMosaic constructs positive pairs by applying structured time-frequency masking to spectrogram patches, which reduces memory usage and enables efficient large-batch training. Compared with generative approaches, the AudioMosaic encoder learns more discriminative utterance-level representations that demonstrate strong transferability across datasets, domains, and acoustic conditions. Extensive experiments show that AudioMosaic achieves state-of-the-art performance on several standard audio benchmarks under both linear probing and fine-tuning. We further show that integrating the pretrained AudioMosaic encoder into audio-language models improves performance on audio-language tasks. The code is publicly available in our \href{https://github.com/HanxunH/AudioMosaic}{GitHub repository}.

</details>


### [142] [Active Learners as Efficient PRP Rerankers](https://arxiv.org/abs/2605.14236)
*Jeremías Figueiredo Paschmann,Juan Kaplan,Francisco Nattero Santiago Mauricio Barron Bucolo,Juan Wisznia,Luciano del Corro*

Main category: cs.LG

TL;DR: PRP reranking reframed as active learning with noise-robust ranking algorithms, using randomized-direction oracle to handle position bias with single LLM calls per pair.


<details>
  <summary>Details</summary>
Motivation: Current Pairwise Ranking Prompting (PRP) uses classical sorting algorithms that assume transitive, noise-free comparisons, but LLM judgments are noisy, order-sensitive, and sometimes intransitive. Truncating full permutations for budget constraints doesn't produce reliable top-K rankings.

Method: Reframe PRP reranking as active learning from noisy pairwise comparisons, using active rankers as drop-in replacements. Introduce randomized-direction oracle that uses a single LLM call per pair, converting systematic position bias into zero-mean noise for unbiased ranking without bidirectional calls.

Result: Active rankers improve NDCG@10 per call in the call-constrained regime compared to traditional sorting approaches. The noise-robust framework with randomized-direction oracle enables unbiased aggregate ranking with fewer calls.

Conclusion: Active learning approaches for noisy pairwise comparisons are superior to classical sorting for LLM-based reranking, and the randomized-direction oracle provides a cost-effective way to handle position bias while maintaining ranking quality under budget constraints.

Abstract: Pairwise Ranking Prompting (PRP) elicits pairwise preference judgments from an LLM, which are then aggregated into a ranking, usually via classical sorting algorithms. However, judgments are noisy, order-sensitive, and sometimes intransitive, so sorting assumptions do not match the setting. Because sorting aims to recover a full permutation, truncating it to meet a call budget does not produce a dependable top-K. We thus reframe PRP reranking as active learning from noisy pairwise comparisons and show that active rankers are drop-in replacements that improve NDCG@10 per call in the call-constrained regime. Our noise-robust framework also introduces a randomized-direction oracle that uses a single LLM call per pair. This approach converts systematic position bias into zero-mean noise, enabling unbiased aggregate ranking without the cost of bidirectional calls.

</details>


### [143] [Paraphrasing Attack Resilience of Various AI-Generated Text Detection Methods](https://arxiv.org/abs/2605.14240)
*Andrii Shportko,Inessa Verbitsky*

Main category: cs.LG

TL;DR: Evaluation shows that Binoculars-inclusive ensembles perform best for AI text detection but are most vulnerable to paraphrasing attacks, revealing a performance vs. resilience tradeoff in current methods.


<details>
  <summary>Details</summary>
Motivation: The rise of LLMs has created problems with plagiarism and misinformation, while AI detector bypassing tools are increasing. There's growing demand for reliable machine-generated text detection methods, but their resilience to attacks needs evaluation.

Method: Investigated paraphrasing attack resilience of three detection approaches: fine-tuned RoBERTa, Binoculars, and text feature analysis. Also evaluated ensembles of these methods using Random Forest classifiers.

Result: Binoculars-inclusive ensembles yielded the strongest detection results but suffered the most significant performance losses during paraphrasing attacks. This reveals a dichotomy between performance and resilience.

Conclusion: Current AI text detection methods face a fundamental tradeoff: higher-performing systems (like Binoculars-inclusive ensembles) are most vulnerable to attacks, complicating perceptions of reliability in state-of-the-art techniques.

Abstract: The recent large-scale emergence of LLMs has left an open space for dealing with their consequences, such as plagiarism or the spread of false information on the Internet. Coupling this with the rise of AI detector bypassing tools, reliable machine-generated text detection is in increasingly high demand. We investigate the paraphrasing attack resilience of various machine-generated text detection methods, evaluating three approaches: fine-tuned RoBERTa, Binoculars, and text feature analysis, along with their ensembles using Random Forest classifiers. We discovered that Binoculars-inclusive ensembles yield the strongest results, but they also suffer the most significant losses during attacks. In this paper, we present the dichotomy of performance versus resilience in the world of AI text detection, which complicates the current perception of reliability among state-of-the-art techniques.

</details>


### [144] [Latency-Quality Routing for Functionally Equivalent Tools in LLM Agents](https://arxiv.org/abs/2605.14241)
*Kexin Chu,Dawei Xiang,Wei Zhang*

Main category: cs.LG

TL;DR: LQM-ContextRoute is a contextual bandit router that handles multiple functionally equivalent tool providers by prioritizing answer quality per service cycle rather than additive latency-quality tradeoffs, improving performance under load and provider heterogeneity.


<details>
  <summary>Details</summary>
Motivation: LLM agents increasingly access same-function tools through multiple providers with varying latency, reliability, and quality. Current routing approaches face challenges when providers differ significantly and gold labels are unavailable at deployment time, requiring adaptive solutions under runtime load.

Method: LQM-ContextRoute uses latency-quality matching: providers are ranked by expected answer quality per service cycle instead of additive reward. It combines capacity-aware scoring with query-specific quality estimation and LLM-as-judge feedback for online adaptation to load changes and quality differences.

Result: On web-search load benchmark: +2.18 pp F1 improvement over SW-UCB while maintaining latency-quality frontier. StrategyQA (high heterogeneity): up to +18 pp accuracy improvement over SW-UCB. Heterogeneous retriever pools: +2.91-3.22 pp NDCG improvement over SW-UCB.

Conclusion: Same-function tool routing benefits from treating latency as service capacity rather than additive tradeoff, especially when runtime pressure and provider-quality heterogeneity coexist, showing superiority of the capacity-aware approach over traditional reward balancing.

Abstract: Tool-augmented LLM agents increasingly access the same tool type through multiple functionally equivalent providers, such as web-search APIs, retrievers, or LLM backends exposed behind a shared interface. This creates a provider-routing problem under runtime load: the router must choose among providers that differ in latency, reliability, and answer quality, often without gold labels at deployment time. We introduce LQM-ContextRoute, a contextual bandit router for same-function tool providers. Its key design is latency-quality matching: instead of letting low latency offset poor answers in an additive reward, the router ranks providers by expected answer quality per service cycle. It combines this capacity-aware score with query-specific quality estimation and LLM-as-judge feedback, allowing it to adapt online to both load changes and provider-quality differences. On the main web-search load benchmark, LQM-ContextRoute improves F1 by +2.18 pp over SW-UCB while staying on the latency-quality frontier. In a high-heterogeneity StrategyQA setting, LQM-ContextRoute avoids additive-reward collapse and improves accuracy by up to +18 pp over SW-UCB; on heterogeneous retriever pools, it improves NDCG by +2.91--+3.22 pp over SW-UCB. These results show that same-function tool routing benefits from treating latency as service capacity, especially when runtime pressure and provider-quality heterogeneity coexist.

</details>


### [145] [Artificial Intelligence-Assistant Cardiotocography: Unified Model for Signal Reconstruction, Fetal Heart Rate Analysis, and Variability Assessment](https://arxiv.org/abs/2605.14242)
*Xiaohua Wang,Kai Yu,XuXiao Liang,Liang Wang,Chao Han*

Main category: cs.LG

TL;DR: AI-based FHR monitoring model shows high accuracy in detecting critical fetal heart rate patterns through deep learning on large datasets and novel validation metrics.


<details>
  <summary>Details</summary>
Motivation: Traditional fetal heart rate (FHR) monitoring faces limitations including equipment performance issues, data transmission problems, and subjective doctor assessments, compromising reliability in detecting fetal compromise.

Method: Developed FHrCTG AI model with pre-training on 558,412 unlabeled data points and fine-tuning on 7,266 expert-reviewed entries. Used Intersection Overlapping Labels (IOL) approach to transform rate analysis into categorical judgments for validation.

Result: High sensitivity and specificity for critical decelerations (89.13%/87.78%) and accelerations (62.5%/92.04%). Impressive AUC scores for clinical application: 0.7214 for periodicity and 0.9643 for amplitude variation verification.

Conclusion: The AI-based FHrCTG model effectively addresses traditional FHR monitoring limitations by providing accurate, objective assessments of fetal heart rate variability and patterns, potentially improving prevention of adverse fetal outcomes.

Abstract: The monitoring of fetal heart rate (FHR) and the assessment of its variability are crucial for preventing fetal compromise and adverse outcomes. However, traditional methods encounter limitations arising from equipment performance, data transmission, and subjective assessments by doctors. We have developed a tailored AI-based FHrCTG model specifically for FHR monitoring, which effectively mitigates noise interference and precisely reconstructs signals. Our model was pre-trained on a massive dataset consisting of 558,412 unlabeled data points and further refined using 7,266 expert-reviewed entries. To validate FHR, we introduced the Intersection Overlapping Labels (IOL) approach, which transforms rate analysis into categorical judgments. Testing revealed that our model demonstrates high sensitivity and specificity in detecting critical FHR decelerations (89.13% and 87.78%, respectively) and accelerations (62.5% and 92.04%, respectively). Furthermore, based on Fischer's criteria for clinical application, our model achieved impressive AUC scores of 0.7214 and 0.9643 for verifying FHR periodicity and amplitude variation, respectively.

</details>


### [146] [Action-Conditioned Risk Gating for Safety-Critical Control under Partial Observability](https://arxiv.org/abs/2605.14246)
*Yushen Liu,Yin-Jen Chen,Ziyi Chen,Tao Wang,Heng Huang,Xugui Zhou,Yanfu Zhang*

Main category: cs.LG

TL;DR: A lightweight reinforcement learning method that uses action-conditioned near-term risk prediction for risk-sensitive control in partially observable domains, avoiding expensive belief-space planning while maintaining safety constraints.


<details>
  <summary>Details</summary>
Motivation: Risk-sensitive partially observable Markov decision processes are important for safety-critical control problems, but traditional belief-space planning methods are computationally expensive and sensitive to model specifications, making them impractical for real-world applications.

Method: The method creates a compact finite-history proxy state and learns an action-conditioned predictor of near-term safety violations. This predicted risk is used in two ways: as a risk penalty during value learning, and as a decision-time gate that interpolates between optimistic and conservative ensemble value estimates based on action risk levels.

Result: In automated glucose regulation (adult and adolescent cohorts), the method improves glycemic tradeoffs and substantially reduces runtime compared to belief-space planning. In Safety-Gym navigation benchmarks, it achieves better reward-cost balance than unconstrained RL and several safe-RL baselines.

Conclusion: Action-conditioned near-term risk prediction provides an effective local signal for approximate risk-sensitive POMDP control when full belief-space planning is impractical, offering a lightweight alternative that maintains safety while improving computational efficiency.

Abstract: Many safety-critical control problems are modeled as risk-sensitive partially observable Markov decision processes, where the controller must make decisions from incomplete observations while balancing task performance against safety risk. Although belief-space planning provides a principled solution, maintaining and planning over beliefs can be computationally costly and sensitive to model specification in practical domains. We propose a lightweight risk-gated reinforcement learning approximation for risk-sensitive control under partial observability. The method constructs a compact finite-history proxy state and learns an action-conditioned predictor of near-term safety violation. This predicted candidate-action risk is used in two complementary ways: as a risk penalty during value learning, and as a decision-time gate that interpolates between optimistic and conservative ensemble value estimates. As a result, low-risk actions are evaluated closer to reward-seeking estimates, while high-risk actions are evaluated more conservatively. We evaluate the approach in two safety-critical partially observable domains: automated glucose regulation and safety-constrained navigation. Across adult and adolescent glucose-control cohorts, the method improves overall glycemic tradeoffs and substantially reduces runtime relative to a belief-space planning baseline. On Safety-Gym navigation benchmarks, it achieves a more favorable reward-cost balance than unconstrained RL and several standard safe-RL baselines. These results suggest that action-conditioned near-term risk can provide an effective local signal for approximate risk-sensitive POMDP control when full belief-space planning is impractical.

</details>


### [147] [EnergyLens: Predictive Energy-Aware Exploration for Multi-GPU LLM Inference Optimization](https://arxiv.org/abs/2605.14249)
*Zhiye Song,Kyungmi Lee,Eun Kyung Lee,Xin Zhang,Tamar Eilam,Anantha P. Chandrakasan*

Main category: cs.LG

TL;DR: EnergyLens is an end-to-end framework for predicting and optimizing LLM inference energy consumption, providing accurate multi-GPU energy modeling without expensive production profiling.


<details>
  <summary>Details</summary>
Motivation: As LLMs scale, predicting and reducing their energy footprint is critical for sustainability and datacenter operations, but existing approaches either require expensive production-level profiling or fail to accurately capture multi-GPU energy behavior, leaving practitioners without tools for optimization prioritization and configuration selection.

Method: EnergyLens uses an intuitive einsum-based interface to capture LLM specifications including fusion, parallelism, and compute-communication overlap, combined with load-imbalance-aware MoE modeling and an empirically driven communication energy model for multi-GPU settings.

Result: The framework achieves mean absolute percentage errors (MAPEs) between 9.25% and 13.19% for multi-GPU prefill and decode energy on Llama3 and Qwen3-MoE, and 12.97% across SM allocations for Megatron-style overlap. Energy exploration reveals up to 1.47x and 52.9x energy variation across configurations.

Conclusion: EnergyLens effectively bridges the gap in LLM energy prediction tools, enabling practitioners to optimize energy consumption without expensive profiling and revealing that compute-communication overlap requires systematic analysis rather than intuition, with the framework correctly identifying Pareto-optimal configurations.

Abstract: We present EnergyLens, an end-to-end framework for energy-aware large language model (LLM) inference optimization. As LLMs scale, predicting and reducing their energy footprint has become critical for sustainability and datacenter operations, yet existing approaches either require production-level code and expensive profiling or fail to accurately capture multi-GPU energy behavior. As a result, practitioners lack tools for deciding which optimizations to prioritize and for selecting among existing deployment configurations when exhaustive profiling is impractical. EnergyLens addresses this gap with an intuitive einsum-based interface that captures LLM specifications including fusion, parallelism, and compute-communication overlap, combined with load-imbalance-aware MoE modeling and an empirically driven communication energy model for multi-GPU settings. We validate EnergyLens on Llama3 and Qwen3-MoE across tensor-parallel and expert-parallel configurations, achieving mean absolute percentage errors (MAPEs) between 9.25% and 13.19% for multi-GPU prefill and decode energy, and 12.97% across SM allocations for Megatron-style overlap. Our energy-driven exploration reveals up to 1.47x and 52.9x energy variation across configurations in prefill and decode efficiency and motivates distributed serving. We further show that compute-communication overlap is difficult to optimize with intuition alone, but EnergyLens correctly identifies Pareto-optimal overlap configurations.

</details>


### [148] [Not All Timesteps Matter Equally: Selective Alignment Knowledge Distillation for Spiking Neural Networks](https://arxiv.org/abs/2605.14252)
*Kai Sun,Peibo Duan,Yongsheng Huang,Guowei Zhang,Benjamin Smith,Nanxu Gong,Levin Kuhlmann*

Main category: cs.LG

TL;DR: SeAl-KD proposes selective knowledge distillation for SNNs that selectively aligns class-level and temporal knowledge rather than enforcing uniform alignment across all timesteps.


<details>
  <summary>Details</summary>
Motivation: Current knowledge distillation methods for SNNs enforce uniform alignment across all timesteps, assuming equal treatment for all timestep predictions. However, SNN predictions vary over time, and intermediate timesteps don't all need to be individually correct when the final aggregated output is correct. Forcing every timestep toward the same supervision target may not be optimal for effective distillation.

Method: Proposes Selective Alignment Knowledge Distillation (SeAl-KD), which selectively aligns class-level and temporal knowledge by: 1) equalizing competing logits at erroneous timesteps, and 2) reweighting temporal alignment based on confidence and inter-timestep similarity. This approach provides corrective guidance to erroneous timesteps while preserving useful temporal dynamics.

Result: Extensive experiments on both static image and neuromorphic event-based datasets demonstrate consistent improvements over existing distillation methods for SNNs.

Conclusion: Selective alignment of knowledge distillation is more effective for SNNs than uniform alignment across timesteps, leading to better performance by addressing the temporal evolution of predictions and providing targeted guidance where needed.

Abstract: Spiking neural networks (SNNs), which are brain-inspired and spike-driven, achieve high energy efficiency. However, a performance gap between SNNs and artificial neural networks (ANNs) still remains. Knowledge distillation (KD) is commonly adopted to improve SNN performance, but existing methods typically enforce uniform alignment across all timesteps, either from a teacher network or through inter-temporal self-distillation, implicitly assuming that per-timestep predictions should be treated equally. In practice, SNN predictions vary and evolve over time, and intermediate timesteps need not all be individually correct even when the final aggregated output is correct. Under such conditions, effective distillation should not force every timestep toward the same supervision target, but instead provide corrective guidance to erroneous timesteps while preserving useful temporal dynamics. To address this issue, we propose Selective Alignment Knowledge Distillation (SeAl-KD), which selectively aligns class-level and temporal knowledge by equalizing competing logits at erroneous timesteps and reweighting temporal alignment based on confidence and inter-timestep similarity. Extensive experiments on static image and neuromorphic event-based datasets demonstrate consistent improvements over existing distillation methods. The code is available at https://github.com/KaiSUN1/SeAl

</details>


### [149] [Architecture-Aware Explanation Auditing for Industrial Visual Inspection](https://arxiv.org/abs/2605.14255)
*Sibo Jia,Zihang Zhao,Kunrong Li*

Main category: cs.LG

TL;DR: This paper proposes an architecture-aware explanation audit protocol based on the native-readout hypothesis, showing that explanation faithfulness depends on structural similarity to model's native decision mechanisms, not just architecture family.


<details>
  <summary>Details</summary>
Motivation: Deep classifiers in industrial visual inspection often produce heatmap explanations that appear visually plausible but fail to identify image regions actually driving model decisions, raising concerns about explanation faithfulness.

Method: The authors operationalize an architecture-aware explanation audit protocol grounded in the native-readout hypothesis, using perturbation-based faithfulness evaluation (Deletion AUC) on WM-811K wafer maps under three-seed zero-fill perturbation. They compare ViT-Tiny, Swin-Tiny, ResNet18+CBAM, and DenseNet121 models with different explanation methods.

Result: ViT-Tiny + Attention Rollout performs worst (Deletion AUC 0.211) despite lower classification accuracy, while Swin-Tiny/Grad-CAM shows good performance (0.432-0.525). Swin-Tiny demonstrates that readout structure (spatial feature-map hierarchy) rather than architecture family determines Grad-CAM compatibility. RISE compresses all families to Deletion AUC ~0.1, showing native readout is a compatibility principle, not optimality guarantee. Faithfulness rankings are joint properties of model, explainer, and perturbation operator triples.

Conclusion: Explanation pathways should be co-designed with model architectures based on readout structure, deployed heatmaps should be accompanied by quantitative faithfulness metrics, and audit results are dataset/task dependent requiring qualification.

Abstract: Industrial visual inspection systems increasingly rely on deep classifiers whose heatmap explanations may appear visually plausible while failing to identify the image regions that actually drive model decisions. This paper operationalizes an architecture-aware explanation audit protocol grounded in the native-readout hypothesis: the perturbation-based faithfulness of an explanation method is bounded by its structural distance from the model's native decision mechanism. On WM-811K wafer maps (9 classes, 172k images) under a three-seed zero-fill perturbation protocol, ViT-Tiny + Attention Rollout attains Deletion AUC 0.211 against 0.432-0.525 for Swin-Tiny / ResNet18+CBAM / DenseNet121 + Grad-CAM (abs(Cohen's d) > 1.1), despite lower classification accuracy. Swin-Tiny disentangles architecture family from readout structure: despite being a Transformer, its spatial feature-map hierarchy makes it Grad-CAM compatible, showing that the operative factor is readout structure rather than architecture family. A model-agnostic control (RISE) compresses all families to Deletion AUC about 0.1, indicating the gap arises from the explainer pathway; notably, RISE outperforms all native methods, so native readout is a compatibility principle rather than an optimality guarantee. A blur-fill sensitivity analysis shows that the family ordering reverses under a different perturbation baseline, reinforcing that faithfulness rankings are joint properties of (model, explainer, perturbation operator) triples. An exploratory boundary-condition study on MVTec AD (pretrained models) indicates that audit results are dataset/task dependent and identifies conditions requiring qualification. The protocol yields actionable guidance: explanation pathways should be co-designed with model architectures based on readout structure, and deployed heatmaps should be accompanied by quantitative faithfulness metrics.

</details>


### [150] [Dynamics of the Transformer Residual Stream: Coupling Spectral Geometry to Network Topology](https://arxiv.org/abs/2605.14258)
*Jesseba Fernando,Grigori Guitchounts*

Main category: cs.LG

TL;DR: Training installs a monotonic spectral gradient in LLMs from rotation-dominated early layers to near-symmetric late layers, creating a low-rank bottleneck that funnels perturbations into few dimensions, linking perturbation propagation to functional topology.


<details>
  <summary>Details</summary>
Motivation: While large language models are powerful, how computation propagates through their layers remains poorly understood. Prior analyses used scalar summaries or approximate linearizations, leaving the full spectral geometry of trained LLMs unknown.

Method: Performed full Jacobian eigendecomposition across three production-scale LLMs, treating depth as discrete time and residual stream as a dynamical system to analyze the spectral geometry.

Result: Training creates a monotonic spectral gradient: early layers are non-normal and rotation-dominated, while late layers are near-symmetric. This creates a cumulative low-rank bottleneck that funnels perturbations into a small fraction of residual stream dimensions. The gradient and dimensional collapse are learned rather than architectural, disappearing when structured non-normality is removed. Topological positioning of graph communities predicts whether Jacobian amplifies or suppresses them.

Conclusion: LLMs develop a learned spectral geometry linking perturbation propagation and compression to the network's functional topology, with the topological positioning of communities determining amplification/suppression effects absent at initialization.

Abstract: Large language models are remarkably capable, yet how computation propagates through their layers remains poorly understood. A growing line of work treats depth as discrete time and the residual stream as a dynamical system, where each layer's nonlinear update has a local linear description. However, previous analyses have relied on scalar summaries or approximate linearizations, leaving the full spectral geometry of trained LLMs unknown. We perform full Jacobian eigendecomposition across three production--scale LLMs and show that training installs a monotonic spectral gradient through depth -- from non-normal, rotation-dominated early layers to near--symmetric late layers -- together with a cumulative low-rank bottleneck that funnels perturbations into a small fraction of the residual stream's effective dimensions. Our experiments reveal that this gradient and the dimensional collapse are learned rather than architectural, and is largely dissolved when structured non-normality is removed. We further show that the topological positioning of graph communities predicts whether the Jacobian amplifies or suppresses them, with the sign of the coupling determined by the local operator type, a relationship absent at initialization. These results map a learned spectral geometry in LLMs that links perturbation propagation and compression to the network's functional topology.

</details>


### [151] [TILT: Target-induced loss tilting under covariate shift](https://arxiv.org/abs/2605.14280)
*Kakei Yamamoto,Martin J. Wainwright*

Main category: cs.LG

TL;DR: TILT (Target-Induced Loss Tilting) is a method for unsupervised domain adaptation under covariate shift that decomposes source predictor into f+b, penalizes b on target data, and deploys f as final target predictor.


<details>
  <summary>Details</summary>
Motivation: Addressing the challenge of unsupervised domain adaptation under covariate shift where source and target distributions differ. Existing methods like importance weighting can be unstable or fail when supports are disjoint.

Method: Proposes TILT which decomposes source predictor as f+b, fits f+b on labeled source data while penalizing auxiliary component b on unlabeled target inputs. The target-side penalty induces implicit relative importance weighting localized to current error.

Result: Theoretical analysis shows TILT induces bounded importance weighting even for disjoint supports. Provides finite-sample oracle inequality and guarantees for sparse ReLU networks. Empirical results show improved target performance over baselines with stable regularization.

Conclusion: TILT offers a theoretically grounded, stable method for unsupervised domain adaptation that outperforms traditional importance weighting and relative density-ratio baselines, particularly effective when dealing with covariate shift.

Abstract: We introduce and analyze Target-Induced Loss Tilting (TILT) for unsupervised domain adaptation under covariate shift. It is based on a novel objective function that decomposes the source predictor as $f+b$, fits $f+b$ on labeled source data while simultaneously penalizing the auxiliary component $b$ on unlabeled target inputs. The resulting fit $f$ is deployed as the final target predictor. At the population level, we show that this target-side penalty implicitly induces relative importance weighting at the population level, but in terms of an estimand $b^*_f$ that is self-localized to the current error, and remains uniformly bounded for any source-target pair (even those with disjoint supports). We prove a general finite-sample oracle inequality on the excess risk, and use it to give an end-to-end guarantee for training with sparse ReLU networks. Experiments on controlled regression problems and shifted CIFAR-100 distillation show that TILT improves target-domain performance over source-only training, exact importance weighting, and relative density-ratio baselines, with a stable dependence on the regularization parameter.

</details>


### [152] [Smooth Multi-Policy Causal Effect Estimation in Longitudinal Settings](https://arxiv.org/abs/2605.14284)
*Wenxin Chen,Weishen Pan,Kyra Gan,Fei Wang*

Main category: cs.LG

TL;DR: Policy-Encoded Q Network (PEQ-Net) enables joint estimation of multiple dynamic treatment policies through shared representations, reducing second-order bias and improving variance compared to conventional separate estimation methods.


<details>
  <summary>Details</summary>
Motivation: Conventional longitudinal causal inference methods estimate each dynamic treatment policy in isolation, preventing information sharing across counterfactuals and inducing second-order bias that inflates finite-sample variance even after standard debiasing techniques.

Method: Proposes policy-aware reparameterization of Iterative Conditional Expectation (ICE) Q-functions with shared representations through a Policy-Encoder architecture (PEQ-Net). Uses kernel mean embeddings to train shared policy encoder, ensuring representation space reflects population-level policy dissimilarities. Applies LTMLE correction step.

Result: PEQ-Net consistently outperforms existing ICE-based methods in semi-synthetic datasets, achieving substantial reductions in root-mean-square error, particularly when evaluating closely related policies. The architecture imposes structural constraint on second-order remainder, stabilizing finite-sample variance.

Conclusion: Policy-aware joint estimation through shared representations with PEQ-Net addresses limitations of separate policy evaluation, reducing variance and improving accuracy, especially for related policies, making it valuable for healthcare and policy decision-making.

Abstract: Comparative evaluation of multiple dynamic treatment policies is essential for healthcare and policy decisions, yet conventional longitudinal causal inference methods estimate each in isolation, preventing information sharing across counterfactuals. We demonstrate that this separate estimation paradigm induces a structurally uncontrolled second-order bias, inflating finite-sample variance even after standard debiasing with longitudinal targeted maximum likelihood estimation(LTMLE). To address this, we propose a policy-aware reparameterization of Iterative Conditional Expectation (ICE) Q-functions that enables joint estimation through shared representations. We implement this approach in the Policy-Encoded Q Network (PEQ-Net), an architecture centered on a shared policy encoder. The encoder is trained using kernel mean embeddings, ensuring that the learned representation space reflects population-level policy dissimilarities. After applying an LTMLE correction step, we prove this design imposes a structural constraint on the second-order remainder, thereby stabilizing finite-sample variance. Experiments on semi-synthetic datasets demonstrate that PEQ-Net consistently outperforms existing ICE-based methods, achieving substantial reductions in root-mean-square error, particularly when evaluating closely related policies.

</details>


### [153] [MetaMoE: Diversity-Aware Proxy Selection for Privacy-Preserving Mixture-of-Experts Unification](https://arxiv.org/abs/2605.14289)
*Weisen Jiang,Shuhao Chen,Sinno Jialin Pan*

Main category: cs.LG

TL;DR: MetaMoE is a privacy-preserving framework that unifies independently trained, domain-specialized experts into a single Mixture-of-Experts model using public proxy data, with diversity-aware proxy selection and context-aware routing.


<details>
  <summary>Details</summary>
Motivation: In practice, data is distributed across clients and cannot be shared due to privacy constraints, making unified MoE training challenging. Existing MoE approaches assume centralized access to training data, which is unrealistic in privacy-sensitive distributed settings.

Method: MetaMoE uses public proxy data as surrogates for inaccessible private data, with diversity-aware proxy selection to choose client-domain-relevant and diverse samples from public data. These proxies approximate private data distributions and supervise router learning. The method also uses proxy data to align expert training for better coordination and employs a context-aware router for expert selection across heterogeneous inputs.

Result: Experiments on computer vision and natural language processing benchmarks demonstrate that MetaMoE consistently outperforms recent privacy-preserving MoE unification methods.

Conclusion: MetaMoE provides an effective solution for unifying independently trained MoE experts in privacy-preserving distributed settings, addressing key challenges of data isolation while maintaining strong performance through careful proxy selection and routing mechanisms.

Abstract: Mixture-of-Experts (MoE) models scale capacity by combining specialized experts, but most existing approaches assume centralized access to training data. In practice, data are distributed across clients and cannot be shared due to privacy constraints, making unified MoE training challenging. We propose MetaMoE, a privacy-preserving framework that unifies independently trained, domain-specialized experts into a single MoE using public proxy data as surrogates for inaccessible private data. Central to MetaMoE is diversity-aware proxy selection, which selects client-domain-relevant and diverse samples from public data to effectively approximate private data distributions and supervise router learning. These proxies are further used to align expert training, improving expert coordination at unification time, while a context-aware router enhances expert selection across heterogeneous inputs. Experiments on computer vision and natural language processing benchmarks demonstrate that MetaMoE consistently outperforms recent privacy-preserving MoE unification methods. Code is available at https://github.com/ws-jiang/MetaMoE.

</details>


### [154] [Minimal-Intervention KV Retention: A Design-Space Study and a Diversity-Penalty Survivor](https://arxiv.org/abs/2605.14292)
*Libo Sun,Po-wei Harn,Peixiong He,Xiao Qin*

Main category: cs.LG

TL;DR: KV-cache compression methods at small budgets were all ineffective, but a simple modification to TriAttention's retention scorer with a redundancy penalty (α method) outperformed heavier structural redesigns on some configurations.


<details>
  <summary>Details</summary>
Motivation: KV-cache compression at small budgets is a crowded but underperforming design space. The authors aim to find effective compression methods for long-form mathematical reasoning tasks within constrained cache budgets.

Method: Studied 7 mechanisms across 5 families on MATH-500 with distilled reasoning models. Proposed α method: modified TriAttention retention scorer using greedy facility-location-inspired selection with V-space redundancy penalty controlled by λ. Used pre-registered protocol tuning λ on development split and confirming on held-out split.

Result: All 7 existing mechanisms failed. With λ=0.5, α method achieved statistically significant improvements (cleared Bonferroni) on 2 of 4 (model, budget) cells: Qwen b=128 and Llama b=64. No negative results were significant.

Conclusion: A minimal scoring modification outperformed heavier structural redesigns in this regime. The combined matched-memory, sympy-graded, held-out confirmation protocol provided the evidence standard needed to reveal this asymmetry in effectiveness.

Abstract: KV-cache compression at small budgets is a crowded design space spanning cache representation, head-wise routing, compression cadence, decoding behavior, and within-budget scoring. We study seven mechanisms across these five families under matched mean cache on long-form mathematical reasoning (MATH-500~\cite{hendrycks2021math}) with two distilled-reasoning models (Qwen-7B and Llama-8B variants of DeepSeek-R1-Distill~\cite{deepseek2025r1}) at budgets $b \in \{64, 128\}$. All seven were rejected. We then propose $α$, a one-function modification to the TriAttention~\cite{mao2026triattention} retention scorer that replaces argmax-top-$k$ with greedy facility-location-inspired selection under a V-space redundancy penalty controlled by a single weight $λ$. A pre-registered protocol tunes $λ$ on a frozen development split and confirms on a disjoint held-out split; with $λ= 0.5$, $α$ clears Bonferroni on two of the four (model, budget) cells (Qwen $b{=}128$ and Llama $b{=}64$), no cell is significantly negative, and the pre-registered Branch~A triggers. The finding is asymmetric: a minimal scoring modification beat heavier structural redesigns in this regime, and the combined matched-memory, sympy-graded, held-out confirmation protocol is the evidence standard that made the asymmetry visible.

</details>


### [155] [Policy Optimization in Hybrid Discrete-Continuous Action Spaces via Mixed Gradients](https://arxiv.org/abs/2605.14297)
*Matias Alvo,Daniel Russo,Yash Kanoria*

Main category: cs.LG

TL;DR: HPO is a hybrid reinforcement learning method that combines differentiable simulation and policy gradients for discrete-continuous action spaces, outperforming standard methods on high-dimensional problems.


<details>
  <summary>Details</summary>
Motivation: Standard policy gradient methods suffer from credit assignment issues in high-dimensional discrete-continuous action spaces, while differentiable simulation fails with discrete actions or non-smooth dynamics.

Method: HPO uses a mixed gradient estimator that backpropagates through smooth parts of the simulator (pathwise gradients) while using score-function estimators for discrete/non-smooth parts, maintaining unbiasedness. It reformulates action discontinuity problems into hybrid form.

Result: HPO substantially outperforms PPO on inventory control and switched linear-quadratic regulator problems, with performance gaps increasing as continuous action dimension grows. The method's mixed gradient structure shows that cross terms become negligible near optimal discrete responses.

Conclusion: HPO effectively addresses the limitations of both policy gradients and differentiable simulation for hybrid action spaces, enabling more efficient learning and approximate decentralized updates that reduce variance near optimality.

Abstract: We study reinforcement learning in hybrid discrete-continuous action spaces, such as settings where the discrete component selects a regime (or index) and the continuous component optimizes within it -- a structure common in robotics, control, and operations problems. Standard model-free policy gradient methods rely on score-function (SF) estimators and suffer from severe credit-assignment issues in high-dimensional settings, leading to poor gradient quality. On the other hand, differentiable simulation largely sidesteps these issues by backpropagating through a simulator, but the presence of discrete actions or non-smooth dynamics yields biased or uninformative gradients. To address this, we propose Hybrid Policy Optimization (HPO), which backpropagates through the simulator wherever smoothness permits, using a mixed gradient estimator that combines pathwise and SF gradients while maintaining unbiasedness. We also show how problems with action discontinuities can be reformulated in hybrid form, further broadening its applicability. Empirically, HPO substantially outperforms PPO on inventory control and switched linear-quadratic regulator problems, with performance gaps increasing as the continuous action dimension grows. Finally, we characterize the structure of the mixed gradient, showing that its cross term -- which captures how continuous actions influence future discrete decisions -- becomes negligible near a discrete best response, thereby enabling approximate decentralized updates of the continuous and discrete components and reducing variance near optimality. All resources are available at github.com/MatiasAlvo/hybrid-rl.

</details>


### [156] [Language-Induced Priors for Domain Adaptation](https://arxiv.org/abs/2605.14301)
*Qiyuan Chen,Jiayu Zhou,Raed Al Kontar*

Main category: cs.LG

TL;DR: Using LLM-generated priors from text descriptions to guide domain adaptation when target data is scarce, preventing negative transfer.


<details>
  <summary>Details</summary>
Motivation: Domain adaptation faces a paradox in cold-start regimes: when target data is scarce, statistical methods can't distinguish relevant source domains from irrelevant ones, leading to negative transfer. Text descriptions of target domains are often available but overlooked.

Method: Proposed a probabilistic framework that translates expert textual descriptions into a Language-Induced Prior (LIP) using a pretrained LLM. The LIP learns preferences and is integrated into an Expectation-Maximization algorithm to identify source relevance. The framework works with any parametric model with available likelihood.

Result: Theoretically proved the estimator matches oracle cold-start MSE under correct prior while remaining asymptotically consistent regardless of LIP quality. Empirically validated on three tasks: Gaussian estimation (descriptive), C-MAPSS dataset (predictive), and MuJoCo hopper (prescriptive).

Conclusion: Leveraging LLMs to convert textual domain descriptions into informative priors effectively addresses cold-start domain adaptation by guiding source selection when target signals are weak, while maintaining theoretical guarantees and practical effectiveness across different task types.

Abstract: Domain adaptation faces a fundamental paradox in the cold-start regime. When target data is scarce, statistical methods fail to distinguish relevant source domains from irrelevant ones, which often leads to negative transfer. In this paper, we address this challenge by leveraging expert textual descriptions of the target domain, a resource that is often available but overlooked. We propose a probabilistic framework that translates these semantic descriptions into a choice model, namely a Language-Induced Prior (LIP), that learns the preferences from a pretrained Large Language Model (LLM). The LIP is then integrated into an Expectation-Maximization algorithm to identify source relevance. Methodologically, this framework is compatible with any parametric model where a likelihood is available. It allows the LIP to guide the selection of sources when target signals are weak, while gradually refining these choices as samples accumulate. Theoretically, we prove that the estimator roughly matches an oracle cold-start MSE under a correct prior, while remaining asymptotically consistent regardless of the quality of the LIP. Empirically, we validated the framework on a descriptive (Gaussian estimation), a predictive (C-MAPSS dataset), and a prescriptive task (MuJoCo hopper).

</details>


### [157] [Matrix-Space Reinforcement Learning for Reusing Local Transition Geometry](https://arxiv.org/abs/2605.14304)
*Zuyuan Zhang,Carlee Joe-Wong,Tian Lan*

Main category: cs.LG

TL;DR: MSRL introduces a geometric abstraction using matrix descriptors to represent trajectory segments, enabling compositional generalization and transfer learning in RL through algebraic composition in matrix space.


<details>
  <summary>Details</summary>
Motivation: Current methods for compositional generalization in sequential decision-making overlook rich local transition geometry and dynamics, focusing instead on reusing skills or predictive models without adequately capturing the geometric structure of transitions.

Method: Proposes Matrix-Space Reinforcement Learning (MSRL) which uses positive semidefinite matrix descriptors that aggregate first- and second-order statistics of lifted one-step transitions. These descriptors support algebraic composition in abstract matrix space and enable conditioning value functions on trajectory-segment matrices.

Result: MSRL achieves the best average finite-budget target AUC of 0.73, outperforming MSRL from scratch (0.65), TD-MPC-PT+FT (0.63), and TD-MPC (0.57). The method is plug-in compatible with standard RL approaches and includes obstruction filtering to reject implausible compositions.

Conclusion: MSRL provides a geometric foundation for compositional generalization in RL by leveraging matrix-space representations that capture transition structure, enable algebraic composition, and facilitate transfer learning through matrix-to-value mappings that bootstrap new tasks.

Abstract: Compositional generalization in sequential decision-making requires identifying which parts of prior rollouts remain useful for new tasks. Existing methods reuse skills or predictive models, but often overlook rich local transition geometry and dynamics. We propose Matrix-Space Reinforcement Learning (MSRL), a geometric abstraction that represents trajectory segments through positive semidefinite matrix descriptors aggregating first- and second-order statistics of lifted one-step transitions. These descriptors expose shared hidden structure, support algebraic composition in an abstract matrix space, and reveal opportunities for transfer. We prove that the descriptor is well defined up to coordinate gauge, complete for the induced low-order additive signal class, additive under valid segment composition, and minimally sufficient among admissible additive descriptors. We further show that conditioning value functions on the trajectory-segment matrix yields a first-order smooth approximation of action values, enabling source-learned matrix-to-value mappings to bootstrap learning in new tasks. MSRL is plug-in compatible with standard model-free and model-based methods, while obstruction filtering rejects implausible compositions. Empirically, MSRL achieves the best average finite-budget target AUC of 0.73, outperforming MSRL from scratch (0.65), TD-MPC-PT+FT (0.63), and TD-MPC (0.57).

</details>


### [158] [Beyond Binary: Reframing GUI Critique as Continuous Semantic Alignment](https://arxiv.org/abs/2605.14311)
*Yuchen Sun,Pei Fu,Shaojie Zhang,Anan Du,Xiuwen Xi,Ruoceng Zhang,Zhenbo Luo,Jian Luan,Chongyang Zhang*

Main category: cs.LG

TL;DR: BBCritic introduces a new approach for GUI agents that uses contrastive learning in an affordance space instead of binary classification, enabling fine-grained action ranking and outperforming larger binary models.


<details>
  <summary>Details</summary>
Motivation: Existing GUI critic models use binary classification which causes severe entanglement: they cannot distinguish between valid actions and plausible-but-invalid distractors. This is due to two structural defects: Affordance Collapse (hierarchical affordance space compressed into 0/1 labels) and Noise Sensitivity (binary objectives overfit to noisy decision boundaries).

Method: BBCritic uses the Functional Equivalence Hypothesis and two-stage contrastive learning to align instructions and actions in a shared Affordance Space, recovering the hierarchical structure flattened by binary supervision. It also introduces BBBench, a benchmark with dense action space and four-level hierarchical taxonomy for fine-grained ranking evaluation.

Result: BBCritic-3B outperforms 7B-parameter state-of-the-art binary models without any extra annotation. It demonstrates strong zero-shot transferability across platforms and tasks, showing GUI critique is fundamentally a metric-learning problem rather than classification.

Conclusion: The paper establishes that GUI critique should be approached as a metric-learning problem rather than binary classification. BBCritic's success demonstrates the importance of recovering hierarchical affordance structures through contrastive learning, enabling more effective fine-grained action ranking for generalist GUI agents.

Abstract: Test-Time Scaling (TTS), which samples multiple candidate actions and ranks them via a Critic Model, has emerged as a promising paradigm for generalist GUI agents. Its efficacy thus hinges on the critic's fine-grained ranking ability. However, existing GUI critic models uniformly adopt binary classification. Our motivational analysis of these models exposes a severe entanglement: scores for valid actions and plausible-but-invalid distractors become indistinguishable. We attribute this failure to two structural defects: Affordance Collapse--the hierarchical affordance space is compressed into 0/1 labels; and Noise Sensitivity--binary objectives overfit to noisy decision boundaries. To resolve this, we introduce BBCritic (Beyond-Binary Critic), a paradigm shift grounded in the Functional Equivalence Hypothesis. Through two-stage contrastive learning, BBCritic aligns instructions and actions in a shared Affordance Space, recovering the hierarchical structure that binary supervision flattens. We also present BBBench (Beyond-Binary Bench), the first GUI critic benchmark that pairs a dense action space with a hierarchical four-level taxonomy, enabling fine-grained ranking evaluation. Experimental results show that BBCritic-3B, trained without any extra annotation, outperforms 7B-parameter SOTA binary models. It demonstrates strong zero-shot transferability across platforms and tasks, supporting our methodological view: GUI critique is fundamentally a metric-learning problem, not a classification one.

</details>


### [159] [Guided Diffusion Sampling for Precipitation Forecast Interventions](https://arxiv.org/abs/2605.14317)
*Ayumu Ueyama,Kazuhiko Kawamoto,Hiroshi Kera*

Main category: cs.LG

TL;DR: Proposes a gradient-based guidance framework using diffusion models to reduce extreme precipitation by steering diffusion sampling trajectories rather than directly perturbing atmospheric states.


<details>
  <summary>Details</summary>
Motivation: Extreme precipitation causes severe damage, and while weather control has been discussed as mitigation, data-driven weather forecasting models haven't been explored for perturbation-based interventions. Current adversarial attacks exploit model artifacts without considering physical plausibility.

Method: A gradient-based guidance framework for precipitation-reduction interventions through diffusion sampling in diffusion-based weather forecasting models. Instead of directly perturbing atmospheric states, it steers the diffusion sampling trajectory to enable precipitation reduction while maintaining consistency with atmospheric distribution.

Result: Experiments on extreme precipitation events from WeatherBench2 show the method achieves effective precipitation reduction while yielding more physically plausible interventions than adversarial perturbations.

Conclusion: The proposed diffusion sampling guidance framework provides a novel approach for precipitation reduction interventions that maintains physical plausibility, addressing limitations of adversarial perturbation methods.

Abstract: Extreme precipitation causes severe societal and economic damage, and weather control has long been discussed as a potential mitigation strategy. However, to the best of our knowledge, perturbation-based interventions for weather control using data-driven weather forecasting models have not yet been explored. While adversarial attacks also generate perturbations that alter forecasts, they aim to exploit model artifacts and do not account for physical plausibility. In this paper, we propose a gradient-based guidance framework for precipitation-reduction interventions through diffusion sampling in diffusion-based weather forecasting models. Instead of directly perturbing atmospheric states, our method steers the diffusion sampling trajectory, enabling precipitation reduction while maintaining consistency with the atmospheric distribution. To assess physical plausibility, we evaluate from three perspectives: (i) vertical and variable-wise perturbation profiles, (ii) latent-space trajectory deviation, and (iii) cross-model transferability. Experiments on extreme precipitation events from WeatherBench2 demonstrate that our method achieves effective precipitation reduction while yielding more physically plausible interventions than adversarial perturbations.

</details>


### [160] [Dynamic Latent Routing](https://arxiv.org/abs/2605.14323)
*Fangyuan Yu,Xin Su,Amir Abdullah*

Main category: cs.LG

TL;DR: The paper introduces Dynamic Latent Routing (DLR), a method for language model fine-tuning that uses dynamic search to learn optimal policy composition, outperforming supervised fine-tuning in low-data settings.


<details>
  <summary>Details</summary>
Motivation: The motivation stems from investigating temporal concatenation of sub-policies in MDPs with time-varying rewards, and applying the "search, select, update" principle from General Dijkstra Search (GDS) to language model fine-tuning for improved performance in low-data scenarios.

Method: The authors propose Dynamic Latent Routing (DLR), a language-model post-training method that jointly learns discrete latent codes, routing policies, and model parameters through dynamic search in a single training stage, inspired by the GDS framework for optimal policy composition.

Result: DLR matches or outperforms supervised fine-tuning across four datasets and six models in low-data fine-tuning settings, achieving a mean gain of +6.6 percentage points, while prior discrete-latent baselines consistently underperform SFT. Mechanistic analyses show DLR learns structured routing behaviors with distinct causal roles.

Conclusion: The paper demonstrates that dynamic search-based approaches to policy composition can significantly improve language model fine-tuning in low-data settings, showing that structured routing behaviors with clear causal roles can be learned through the proposed DLR framework.

Abstract: We investigate the temporal concatenation of sub-policies in Markov Decision Processes (MDP) with time-varying reward functions. We introduce General Dijkstra Search (GDS), and prove that globally optimal goal-reaching policies can be recovered through temporal composition of intermediate optimal sub-policies. Motivated by the "search, select, update" principle underlying GDS, we propose Dynamic Latent Routing (DLR), a language-model post-training method that jointly learns discrete latent codes, routing policies, and model parameters through dynamic search in a single training stage. In low-data fine-tuning settings, DLR matches or outperforms supervised fine-tuning across four datasets and six models, achieving a mean gain of +6.6 percentage points, while prior discrete-latent baselines consistently underperform SFT. Mechanistic analyses and targeted code ablations show that DLR learns structured routing behaviors with distinct causal roles.

</details>


### [161] [AIM-DDI: A Model-Agnostic Multimodal Integration Module for Drug-Drug Interaction Prediction](https://arxiv.org/abs/2605.14327)
*Yerin Park,Sangseon Lee*

Main category: cs.LG

TL;DR: AIM-DDI is a reusable, architecture-independent multimodal integration module that improves drug-drug interaction prediction, especially for unseen drugs.


<details>
  <summary>Details</summary>
Motivation: Existing multimodal DDI models have fusion mechanisms tied to specific prediction architectures, limiting reuse and generalization to unseen drugs, which is crucial for clinical safety.

Method: Proposes AIM-DDI, which represents heterogeneous modality information (structural, chemical, semantic) as tokens in a shared latent space and uses a unified fusion module to model cross-modal dependencies, enabling model-agnostic integration.

Result: Extensive evaluations show AIM-DDI consistently improves DDI prediction performance across diverse models, with strongest gains in the most challenging both-unseen setting where neither drug in test pairs was observed during training.

Conclusion: Treating multimodal integration as a reusable module rather than model-specific fusion is an effective strategy for robust unseen-drug DDI prediction, offering architecture-independent benefits.

Abstract: Drug-drug interaction (DDI) prediction is a critical task in computational biomedicine, as adverse interactions between co-administered drugs can cause severe side effects and clinical risks. A key challenge is unseen-drug generalization, where interactions must be predicted for drugs not observed during training. Although multimodal DDI models exploit diverse drug-related information, their fusion mechanisms are often tied to specific prediction architectures, limiting their reuse across models. To address this, we propose AIM-DDI, an architecture-independent multimodal integration module that represents heterogeneous modality information as tokens in a shared latent space. By modeling dependencies across modality tokens through a unified fusion module, AIM-DDI enables model-agnostic integration of structural, chemical, and semantic drug signals across different DDI prediction architectures. Extensive evaluations across diverse DDI models and DrugBank-based settings show that AIM-DDI consistently improves prediction performance, with the strongest gains under the most challenging both-unseen setting where neither drug in a test pair is observed during training. These results suggest that treating multimodal integration as a reusable module, rather than a model-specific fusion component, is an effective strategy for robust unseen-drug DDI prediction.

</details>


### [162] [Nearest-Neighbor Radii under Dependent Sampling](https://arxiv.org/abs/2605.14343)
*Yuanyuan Gao,Yilong Hou,Zhexiao Lin*

Main category: cs.LG

TL;DR: This paper analyzes nearest-neighbor methods under dependent sampling conditions, showing that geometric properties remain informative despite data dependence, with results applicable to high-dimensional manifolds.


<details>
  <summary>Details</summary>
Motivation: Nearest-neighbor methods are fundamental but typically analyzed under independent sampling assumptions. Real-world data often exhibits dependence (e.g., time-series), so understanding nearest-neighbor behavior under dependent sampling is crucial for practical applications.

Method: Study nearest-neighbor radii under dependent sampling, specifically strong mixing dependent observations. Establish distribution-free almost sure convergence under polynomial mixing conditions and provide sharp non-asymptotic moment bounds under geometric mixing conditions.

Result: The moment bounds depend on local intrinsic dimension rather than ambient dimension, making results applicable to high-dimensional data concentrated near lower-dimensional manifolds. Both synthetic experiments and real-world time-series benchmarks confirm that nearest-neighbor geometry remains informative under dependent sampling.

Conclusion: Nearest-neighbor methods remain valid and informative even under dependent sampling conditions, with theoretical guarantees that adapt to data's intrinsic dimensionality rather than ambient dimension, supporting practical applications in real-world dependent data scenarios.

Abstract: Nearest-neighbor methods are fundamental to classical and modern machine learning, yet their geometric properties are typically analyzed under independent sampling. In this paper, we study the nearest-neighbor radii under dependent sampling. We consider strong mixing dependent observations and ask whether dependence changes the scale of nearest-neighbor neighborhoods. We establish distribution-free almost sure convergence under polynomial mixing and sharp non-asymptotic moment bounds under geometric mixing. The moment bounds depend on the local intrinsic dimension rather than the ambient dimension, making the results applicable to high-dimensional data concentrated near lower-dimensional manifolds. Synthetic experiments and real-world time-series benchmarks support the theory, showing that nearest-neighbor geometry remains informative under dependence sampling.

</details>


### [163] [Exemplar Partitioning for Mechanistic Interpretability](https://arxiv.org/abs/2605.14347)
*Jessica Rumbelow*

Main category: cs.LG

TL;DR: Exemplar Partitioning (EP) is an unsupervised method for constructing interpretable feature dictionaries from LLM activations using 1000x fewer tokens than sparse autoencoders, based on Voronoi partitions anchored by observed exemplars.


<details>
  <summary>Details</summary>
Motivation: Current sparse autoencoder (SAE) methods require massive amounts of token data (~10^3 times more) to build feature dictionaries. The authors aim to develop a more efficient method that constructs interpretable feature dictionaries directly from observed activations, enabling better comparability across models and layers while maintaining interpretability and causal intervention capabilities.

Method: EP constructs Voronoi partitions of activation space using leader-clustering of streamed activations within a distance threshold. Each region is anchored by an observed exemplar that serves as both membership criterion and intervention direction. Dictionary size emerges naturally from activation geometry rather than being pre-specified. Exemplars are observed (not learned), enabling direct comparability across layers, models, and training checkpoints.

Result: EP dictionary regions are interpretable and support causal interventions (e.g., collapsing refusal behavior in Gemma-2-2B). Cross-checkpoint matching separates finetuning effects. EP regions decompose activation space differently from SAE features but share ~20% overlap at F1 > 0.5. EP achieves 0.881 mean AUROC on AxBench latent concept detection (comparable to SAE-A's 0.911) while using ~1000x less build compute. EP one-hot probes retain ~97% of raw-activation probe accuracy at L0 = 1.

Conclusion: EP provides an efficient, interpretable alternative to SAEs for constructing feature dictionaries from LLM activations, offering direct comparability across models, causal intervention capabilities, and competitive performance with dramatically reduced computational requirements (~10^3 fewer tokens).

Abstract: We introduce Exemplar Partitioning (EP), an unsupervised method for constructing interpretable feature dictionaries from large language model activations with $\sim 10^{3}\times$ fewer tokens than comparable sparse autoencoders (SAEs). An EP dictionary is a Voronoi partition of activation space, built by leader-clustering streamed activations within a distance threshold. Each region is anchored by an observed exemplar that serves as both its membership criterion and intervention direction; dictionary size is not prespecified, but determined by the activation geometry at that threshold. Because exemplars are observed rather than learned, dictionaries built from the same data stream are directly comparable across layers, models, and training checkpoints.
  We characterise EP as an interpretability object via targeted demonstrations of properties newly accessible through this construction, plus one head-to-head benchmark. In Gemma-2-2B, EP dictionary regions are interpretable and support causal interventions: refusal in instruction-tuned Gemma concentrates in a region whose exemplar ablation can collapse held-out refusal. Cross-checkpoint matching between base and instruction-tuned dictionaries separates the directions preserved through finetuning from those introduced by it. EP regions and Gemma Scope SAE features decompose activation space differently but agree on a shared core: $\sim 20\%$ of EP regions match an SAE feature at $F_{1} > 0.5$, and EP one-hot probes retain $\sim 97\%$ of raw-activation probe accuracy at $\ell_{0} = 1$. Nearest-exemplar distance provides a free out-of-distribution signal at inference. On AxBench latent concept detection at Gemma-2-2B-it L20, EP at $p_{1}$ reaches mean AUROC $0.881$, $+0.126$ over the canonical GemmaScope SAE leaderboard entry and within $0.030$ of SAE-A's $0.911$, at $\sim 10^{3}\times$ less build compute.

</details>


### [164] [Distributionally Robust Multi-Task Reinforcement Learning via Adaptive Task Sampling](https://arxiv.org/abs/2605.14350)
*Nicholas E. Corrado,Wenyuan Huang,Josiah P. Hanna*

Main category: cs.LG

TL;DR: DRATS is a distributionally robust adaptive task sampling algorithm that improves multi-task RL by dynamically prioritizing harder tasks for sampling to overcome imbalanced data allocation issues.


<details>
  <summary>Details</summary>
Motivation: Standard multi-task RL suffers from imbalanced learning where agents quickly solve easy tasks but learn slowly on hard tasks. Prior work focused on conflicting task gradients, but this paper identifies imbalanced data allocation as a distinct under-explored challenge - equal interaction allocation over-allocates data to easy tasks and under-allocates to hard tasks.

Method: The authors formalize multi-task RL as a feasibility problem and derive a minimax objective to minimize the worst-case return gap. They introduce DRATS (Distributionally Robust Adaptive Task Sampling) which adaptively prioritizes sampling tasks that are furthest from being solved.

Result: DRATS improves data efficiency and increases worst-task performance compared to existing task sampling algorithms in benchmarks like MetaWorld-MT10 and MT50.

Conclusion: The paper successfully addresses imbalanced data allocation in multi-task RL through adaptive task sampling, shifting focus from gradient conflicts to data distribution problems and demonstrating effectiveness in challenging benchmarks.

Abstract: Multi-task reinforcement learning (MTRL) aims to train a single agent to efficiently optimize performance across multiple tasks simultaneously. However, jointly optimizing all tasks often yields imbalanced learning: agents quickly solve easy tasks but learn slowly on harder ones. While prior work primarily attributes this imbalance to conflicting task gradients and proposes gradient manipulation or specialized architectures to address it, we instead focus on a distinct and under-explored challenge: imbalanced data allocation. Standard MTRL allocates an equal number of environment interactions to each task, which over-allocates data to easy tasks that require relatively few interactions to solve and under-allocates data to hard tasks that require substantially more experience to solve. To address this challenge, we introduce Distributionally Robust Adaptive Task Sampling (DRATS), an algorithm that adaptively prioritizes sampling tasks furthest from being solved. We derive DRATS by formalizing MTRL as a feasibility problem from which we derive a minimax objective for minimizing the worst-case return gap, the difference between a desired target return and the agent's return on a task. In benchmarks like MetaWorld-MT10 and MT50, DRATS improves data efficiency and increases worst-task performance compared to existing task sampling algorithms.

</details>


### [165] [RQ-MoE: Residual Quantization via Mixture of Experts for Efficient Input-Dependent Vector Compression](https://arxiv.org/abs/2605.14359)
*Zhengjia Zhong,Shuyan Ke,Zaizhou Lin,Jiaqi Song,Hongyi Lan,Hui Li*

Main category: cs.LG

TL;DR: RQ-MoE combines mixture-of-experts with dual-stream quantization for efficient dynamic vector quantization with parallel decoding, achieving state-of-the-art performance with 6x-14x faster decoding.


<details>
  <summary>Details</summary>
Motivation: Existing multi-codebook vector quantization methods use static codebooks that lack expressiveness for heterogeneous data. Recent dynamic quantizers like QINCo adapt to inputs but suffer from sequential dependencies that create decoding bottlenecks.

Method: RQ-MoE uses a two-level mixture-of-experts (MoE) framework with dual-stream quantization. It enables input-dependent codebook adaptation, decouples instruction from quantization for parallel decoding, and allows dynamic codebook construction.

Result: RQ-MoE achieves state-of-the-art or on-par performance in reconstruction and retrieval tasks. It provides 6x-14x faster decoding than prior vector quantization methods while maintaining high quality.

Conclusion: RQ-MoE presents an effective framework for dynamic vector quantization that overcomes bottlenecks of previous methods, offering both superior performance and significant efficiency gains through parallelizable architecture.

Abstract: Vector quantization is a fundamental tool for compressing high-dimensional embeddings, yet existing multi-codebook methods rely on static codebooks that limit expressiveness under heterogeneous data geometry. While recent dynamic quantizers like QINCo adapt codebooks to individual inputs and improve expressiveness, their strict sequential dependencies create decoding bottlenecks. We propose Residual Quantization via Mixture of Experts (RQ-MoE), a framework combining a two-level MoE with dual-stream quantization to enable input-dependent codebook adaptation for efficient vector quantization. RQ-MoE enables dynamic codebook construction and decouples instruction from quantization, facilitating parallel decoding. Theoretically, we show that standard Residual Quantization and QINCo can be recovered as constrained special cases of RQ-MoE, and derive a guideline for setting expert dimensionality in RQ-MoE. Extensive experiments show that RQ-MoE achieves state-of-the-art or on-par performance in reconstruction and retrieval, while providing 6x-14x faster decoding than prior vector quantization methods. The implementation is available at https://github.com/KDEGroup/RQ-MoE.

</details>


### [166] [MoRe: Modular Representations for Principled Continual Representation Learning on Squantial Data](https://arxiv.org/abs/2605.14364)
*Jiaqi Sun,Boyang Sun,Mohamad Rasmy,Xiangchen Song,Kun Zhang*

Main category: cs.LG

TL;DR: MoRe is a modular continual learning framework that identifies hierarchical structure in representations rather than architecturally, enabling principled module reuse and preservation during adaptation


<details>
  <summary>Details</summary>
Motivation: Continual learning requires adapting to new data while preserving old knowledge. Current approaches modify parameters or architectures in task-specific ways, but the core challenge is representational: tasks need distinct yet structured representations that can be selectively updated without disruption. The authors argue structure should reflect intrinsic data organization rather than artificial task boundaries, and time-delayed dependencies in sequential data naturally reveal this organization.

Method: MoRe (Modular Representation) identifies modularity in the representation itself instead of allocating it at architectural level. It decomposes knowledge into a hierarchy of fundamental and specific modules with identifiability guarantees, inspired by the modular organization of the human brain. This enables principled module reuse, alignment, and expansion during adaptation while preserving old modules by construction.

Result: Experiments on synthetic benchmarks and real-world LLM activations demonstrate interpretable hierarchical structure and improved plasticity-stability trade-offs. The framework shows promise as a principled foundation for continual adaptation.

Conclusion: MoRe provides a principled approach to continual learning by discovering modular structure in representations, enabling efficient knowledge reuse and preservation. The brain-inspired modular hierarchy offers interpretable representations and better balances adaptation with stability compared to existing task-specific methods.

Abstract: Continual learning requires models to adapt to new data while preserving previously acquired knowledge. At its core, this challenge can be viewed as principled one-step adaptation: incorporating new information with minimal interference to existing representations. Most existing approaches address this challenge by modifying model parameters or architectures in a supervised, task-specific manner. However, the underlying issue is representational: tasks require distinct yet structured representations that can be selectively updated without disrupting representations, while structure should reflect intrinsic organization in the data rather than task boundaries. In sequential data, time-delayed dependencies provide a natural signal for uncovering this organization, revealing how fundamental representations give rise to more specific ones. Inspired by the modular organization of the human brain, we propose MoRe, a framework that identifies modularity in the representation itself rather than allocating it at the architectural level. MoRe decomposes knowledge into a hierarchy of fundamental and specific modules with identifiability guarantees, enabling principled module reuse, alignment, and expansion during adaptation while preserving old modules by construction. Experiments on synthetic benchmarks and real-world LLM activations demonstrate interpretable hierarchical structure, improved plasticity-stability trade-offs, suggesting MoRe as a principled foundation for continual adaptation

</details>


### [167] [LoMETab: Beyond Rank-1 Ensembles for Tabular Deep Learning](https://arxiv.org/abs/2605.14365)
*Changryeol Choi,Hyewon Park,Yujin Kwon,Gowun Jeong*

Main category: cs.LG

TL;DR: LoMETab is a rank-r generalization of multiplicative implicit ensembles that provides controllable diversity for neural tabular models through adapter rank and initialization scale parameters.


<details>
  <summary>Details</summary>
Motivation: Recent tabular learning benchmarks show tight performance clusters rather than clear hierarchies among methods, making it important to understand and control what makes simple neural tabular models competitive.

Method: Proposes LoMETab, which generalizes rank-1 BatchEnsemble/TabM to rank-r identity-residual Hadamard family by parameterizing member weights as W_k = W ⊙ (1 + A_kB_k^⊤), where W is shared and (A_k, B_k) are member-specific low-rank factors, exposing two diversity-control axes: adapter rank r and initialization scale σ_init.

Result: LoMETab sustains higher pairwise KL divergence than additive low-rank ablation, and (r, σ_init) provides broad control over pairwise KL (varying by orders of magnitude). The diversity is reflected in task-appropriate output measures, and experiments show predictive performance is dataset-dependent over the (r, σ_init) grid.

Conclusion: LoMETab serves as a controllable family of implicit ensembles rather than a fixed rank-1 construction, offering practical diversity control for neural tabular models when benchmark performance gains have plateaued.

Abstract: Recent tabular learning benchmarks increasingly show a tight performance cluster rather than a clear hierarchy among leading methods, spanning gradient boosted decision trees, attention-based architectures, and implicit ensembles such as TabM. As benchmark gains plateau, a complementary goal is to understand and control the mechanisms that make simple neural tabular models competitive. We propose LoMETab, a rank-$r$ generalization of multiplicative implicit ensembles. LoMETab lifts the rank-1 BatchEnsemble/TabM modulation to a rank-$r$ identity-residual Hadamard family by parameterizing each member weight as $W_k = W \odot (1 + A_kB_k^\top)$, where $W$ is shared and $(A_k, B_k)$ are member-specific low-rank factors. This exposes two practical diversity-control axes: the adapter rank $r$ and the initialization scale $σ_{\mathrm{init}}$, and we prove that for $r \ge 2$ this generalization strictly enlarges BatchEnsemble's hypothesis class. Empirically, we show that this added capacity manifests as measurable predictive diversity after training: on representative classification datasets, LoMETab sustains higher pairwise KL than an additive low-rank ablation, and $(r, σ_{\mathrm{init}})$ provides broad control over pairwise KL, varying by up to several orders of magnitude across configurations. The induced diversity is reflected in task-appropriate output-level measures: argmax disagreement for classification and ambiguity for regression, indicating that the control extends beyond pairwise KL to decision- and output-level member variation. Finally, experiments sweeping over adapter rank $r$ and initialization scale $σ_{\mathrm{init}}$ reveal that predictive performance is dataset-dependent over the $(r, σ_{\mathrm{init}})$ grid, supporting LoMETab as a controllable family of implicit ensembles rather than a fixed rank-1 construction.

</details>


### [168] [Turning Stale Gradients into Stable Gradients: Coherent Coordinate Descent with Implicit Landscape Smoothing for Lightweight Zeroth-Order Optimization](https://arxiv.org/abs/2605.14373)
*Chen Liang,Xiatao Sun,Qian Wang,Daniel Rakita*

Main category: cs.LG

TL;DR: Coherent Coordinate Descent (CoCD) is a deterministic zeroth-order optimizer that achieves O(1) query complexity per step by leveraging gradient coherence and converting stale gradients into computational assets, outperforming existing methods in sample efficiency and stability.


<details>
  <summary>Details</summary>
Motivation: Existing zeroth-order optimization methods face a trade-off between sample inefficiency (standard finite differences) and high variance (random subspace methods). There's a need for a deterministic, sample-efficient, and budget-aware ZO optimizer that can work in scenarios where backpropagation is unavailable, such as memory-constrained on-device learning and black-box optimization.

Method: Coherent Coordinate Descent (CoCD) formalizes gradient coherence and is equivalent to Block Cyclic Coordinate Descent with "warm starts." It converts historical (stale) gradients into computational assets by leveraging gradient coherence across coordinates, enabling deterministic updates. The method also reveals that larger finite-difference step sizes can induce implicit smoothing on the optimization landscape.

Result: CoCD achieves O(1) query complexity per step while maintaining global descent directions. Experiments on MLP, CNN, and ResNet architectures (up to 270k parameters) show CoCD significantly outperforms BCCD in sample efficiency and convergence loss/accuracy, and exhibits superior stability over randomized ZO methods.

Conclusion: Deterministic, structure-aware updates offer a superior alternative to randomization for lightweight ZO optimization. CoCD demonstrates that by formalizing gradient coherence and intelligently reusing historical gradient information, it's possible to achieve both sample efficiency and stability without the variance trade-off of randomized methods.

Abstract: Zeroth-Order (ZO) optimization is pivotal for scenarios where backpropagation is unavailable, such as memory-constrained on-device learning and black-box optimization. However, existing methods face a stark trade-off: they are either sample-inefficient (e.g., standard finite differences) or suffer from high variance due to randomized estimation (e.g., random subspace methods). In this work, we propose Coherent Coordinate Descent (CoCD), a deterministic, sample-efficient, and budget-aware ZO optimizer. Theoretically, we formalize the notion of gradient coherence and demonstrate that CoCD is equivalent to Block Cyclic Coordinate Descent (BCCD) with ``warm starts,'' effectively converting historical (stale) gradients from a liability into a computational asset. This mechanism enables $O(1)$ query complexity per step while maintaining global descent directions. Furthermore, we derive error bounds revealing a counter-intuitive insight: larger finite-difference step sizes can induce an implicit smoothing effect on the optimization landscape by reducing the effective smoothness constant, thereby improving convergence stability. Experiments on MLP, CNN, and ResNet architectures (up to 270k parameters) demonstrate that CoCD significantly outperforms BCCD in terms of sample efficiency and convergence loss/accuracy, and exhibits superior stability over randomized ZO methods. Our results suggest that deterministic, structure-aware updates offer a superior alternative to randomization for lightweight ZO optimization.

</details>


### [169] [Optimal Pattern Detection Tree for Symbolic Rule-Based Classification](https://arxiv.org/abs/2605.14374)
*Young-Chae Hong,Yangho Chen*

Main category: cs.LG

TL;DR: OPDT (Optimal Pattern Detection Tree) is a novel MIP-based rule discovery method that finds single optimal patterns in data with optimality guarantees, enhanced by a framework to incorporate domain knowledge constraints.


<details>
  <summary>Details</summary>
Motivation: There's a need for interpretable pattern discovery methods (unlike black-box deep learning) that can provide transparency and explainability while incorporating domain knowledge and compliance requirements for applications in healthcare, risk assessment, and machinery maintenance.

Method: Introduces OPDT based on mixed-integer programming (MIP) for binary classification, plus Branching Structure Constraints (BSC) framework to encode domain knowledge and constraints directly into the optimization model.

Result: Computational experiments show OPDT discovers patterns with optimality guarantees on moderately sized datasets within reasonable runtime, identifying optimal rules that maximize coverage while minimizing false positive rates.

Conclusion: OPDT provides a transparent, interpretable alternative to black-box models with formal optimality guarantees and the ability to incorporate domain expertise, making it valuable for real-world applications requiring explainable AI.

Abstract: Pattern discovery in data plays a crucial role across diverse domains, including healthcare, risk assessment, and machinery maintenance. In contrast to black-box deep learning models, symbolic rule discovery emerges as a key data mining task, generating human-interpretable rules that offer both transparency and intuitive explainability. This paper introduces the Optimal Pattern Detection Tree (OPDT), a rule-based machine learning model based on novel mixed-integer programming to discover a single optimal pattern in data through binary classification. To incorporate prior knowledge and compliance requirements, we further introduce the Branching Structure Constraints (BSC) framework, which enables decision makers to encode domain knowledge and constraints directly into the model. This optimization-based approach discovers a hidden underlying pattern in datasets, when it exists, by identifying an optimal rule that maximizes coverage while minimizing the false positive rate due to misclassification. Our computational experiments show that OPDT discovers a pattern with optimality guarantees on moderately sized datasets within reasonable runtime.

</details>


### [170] [NodeSynth: Socially Aligned Synthetic Data for AI Evaluation](https://arxiv.org/abs/2605.14381)
*Qazi Mamunur Rashid,Xuan Yang,Zhengzhe Yang,Yanzhou Pan,Erin van Liemt,Darlene Neal,Kshitij Pancholi,Jamila Smith-Loud*

Main category: cs.LG

TL;DR: NodeSynth is an evidence-grounded methodology that generates socially relevant synthetic queries using a fine-tuned taxonomy generator anchored in real-world evidence, enabling more effective evaluation of LLMs on sociotechnical issues.


<details>
  <summary>Details</summary>
Motivation: Current synthetic data generation methods for model evaluation lack sociotechnical nuance required for sensitive domains, resulting in datasets that don't properly assess models' performance on real-world social issues.

Method: The authors introduce NodeSynth, which uses a fine-tuned taxonomy generator (TaG) anchored in real-world evidence to generate socially relevant synthetic queries. The methodology involves granular taxonomic expansion to create comprehensive test cases.

Result: NodeSynth elicited failure rates up to five times higher than human-authored benchmarks when tested against four mainstream LLMs. Ablation studies showed granular taxonomic expansion drives these failure rates, and independent validation revealed critical deficiencies in prominent guard models like Llama-Guard-3.

Conclusion: NodeSynth provides a scalable approach for high-stakes model evaluation and targeted safety interventions. The authors open-sourced their end-to-end research prototype and datasets to enable broader adoption of evidence-grounded synthetic data generation for sociotechnical evaluation.

Abstract: Recent advancements in generative AI facilitate large-scale synthetic data generation for model evaluation. However, without targeted approaches, these datasets often lack the sociotechnical nuance required for sensitive domains. We introduce NodeSynth, an evidence-grounded methodology that generates socially relevant synthetic queries by leveraging a fine-tuned taxonomy generator (TaG) anchored in real-world evidence. Evaluated against four mainstream LLMs (e.g., Claude 4.5 Haiku), NodeSynth elicited failure rates up to five times higher than human-authored benchmarks. Ablation studies confirm that our granular taxonomic expansion significantly drives these failure rates, while independent validation reveals critical deficiencies in prominent guard models (e.g., Llama-Guard-3). We open-source our end-to-end research prototype and datasets to enable scalable, high-stakes model evaluation and targeted safety interventions (https://github.com/google-research/nodesynth).

</details>


### [171] [Watch your neighbors: Training statistically accurate chaotic systems with local phase space information](https://arxiv.org/abs/2605.14405)
*Joon-Hyuk Ko,Andrus Giraldo,Deok-Sun Lee*

Main category: cs.LG

TL;DR: A framework for learning chaotic dynamics models that combines accurate local Jacobians with long-term statistical properties using local phase space coverings and maximum mean discrepancy minimization.


<details>
  <summary>Details</summary>
Motivation: Chaotic systems present fundamental challenges for data-driven dynamics discovery, as small modeling errors lead to exponentially growing trajectory discrepancies. Prior work focused either on reproducing Jacobians (governing local expansion/contraction) or long-term statistical behavior, but not both.

Method: Constructs local coverings of a chaotic attractor in phase space and analyzes expansion/contraction of these coverings under dynamics. Trains surrogate model by minimizing maximum mean discrepancy between pushforward distributions of coverings under surrogate and ground-truth dynamics.

Result: Experiments show significant improvement in Jacobian accuracy while remaining competitive with state-of-the-art statistically accurate dynamics learning methods.

Conclusion: The proposed framework bridges the gap between Jacobian-focused and statistics-focused approaches, providing a better surrogate model for chaotic dynamics by combining accurate local dynamics with faithful long-term statistical properties.

Abstract: Chaotic systems pose fundamental challenges for data-driven dynamics discovery, as small modeling errors lead to exponentially growing trajectory discrepancies. Since exact long-term prediction is unattainable, it is natural to ask what a good surrogate model for chaotic dynamics is. Prior work has largely focused either on reproducing the Jacobian of the underlying dynamics, which governs local expansion and contraction rates, or on training surrogate models that reproduce the ground-truth dynamics' long-term statistical behavior. In this work, we propose a new framework that aims to bridge these two paradigms by training surrogate dynamics models with accurate Jacobians and long-term statistical properties. Our method constructs a local covering of a chaotic attractor in phase space and analyzes the expansion and contraction of these coverings under the dynamics. The surrogate model is trained by minimizing the maximum mean discrepancy between the pushforward distributions of the coverings under the surrogate and ground-truth dynamics. Experiments show that our method significantly improves Jacobian accuracy while remaining competitive with state-of-the-art statistically accurate dynamics learning methods. Our code is fully available at https://anonymous.4open.science/r/neighborwatch.

</details>


### [172] [GeoViSTA: Geospatial Vision-Tabular Transformer for Multimodal Environment Representation](https://arxiv.org/abs/2605.14406)
*Yuhao Liu,Sadeer Al-Kindi,Ashok Veeraraghavan,Guha Balakrishnan*

Main category: cs.LG

TL;DR: GeoViSTA is a geospatial vision-tabular transformer that learns unified embeddings from imagery and tabular data using bilateral cross-attention, improving downstream predictions for environmental and social outcomes.


<details>
  <summary>Details</summary>
Motivation: Existing geospatial foundation models focus on Earth observation imagery but don't incorporate structured socioeconomic tabular data, creating a modality gap that limits their ability to model the complete environment needed for complex environmental, social, and health-related reasoning.

Method: GeoViSTA uses a vision-tabular architecture with bilateral cross-attention to exchange spatial and semantic information across modalities. It employs geography-aware attention to align continuous image patches with irregular census-tract tokens. The model is trained with self-supervised joint masked-autoencoding that forces recovery of missing image patches and tabular rows using local spatial context and cross-modal cues.

Result: GeoViSTA's unified embeddings improve linear probing performance on downstream tasks, outperforming baselines in predicting disease-specific mortality and fire hazard frequency across held-out regions.

Conclusion: Jointly modeling the physical environment alongside structured socioeconomic context yields highly transferable representations for holistic geospatial inference, demonstrating the value of bridging the modality gap between imagery and tabular data.

Abstract: Large-scale pretraining on Earth observation imagery has yielded powerful representations of the natural and built environment. However, most existing geospatial foundation models do not directly model the structured socioeconomic covariates typically stored in tabular form. This modality gap limits their ability to capture the complete total environment, which is critical for reasoning about complex environmental, social, and health-related outcomes. In this work, we propose GeoViSTA (Geospatial Vision-Tabular Transformer), a vision-tabular architecture that learns unified geospatial embeddings from co-registered gridded imagery and tabular data. GeoViSTA utilizes bilateral cross-attention to exchange spatial and semantic information across modalities, guided by a geography-aware attention mechanism that aligns continuous image patches with irregular census-tract tokens. We train GeoViSTA with a self-supervised joint masked-autoencoding objective, forcing it to recover missing image patches and tabular rows using local spatial context and cross-modal cues. Empirically, GeoViSTA's unified embeddings improve linear probing performance on high-impact downstream tasks, outperforming baselines in predicting disease-specific mortality and fire hazard frequency across held-out regions. These results demonstrate that jointly modeling the physical environment alongside structured socioeconomic context yields highly transferable representations for holistic geospatial inference.

</details>


### [173] [MahaVar: OOD Detection via Class-wise Mahalanobis Distance Variance under Neural Collapse](https://arxiv.org/abs/2605.14413)
*Donghwan Kim,Hyunsoo Yoon*

Main category: cs.LG

TL;DR: MahaVar: A new OOD detection method that uses class-wise Mahalanobis distance variance to distinguish ID and OOD samples, achieving SOTA results on major benchmarks.


<details>
  <summary>Details</summary>
Motivation: Existing OOD detection methods need improvement. The authors observed that ID samples exhibit a "sharp minimum structure" in class-wise Mahalanobis distances (high variance), while OOD samples show less variance. This observation lacked theoretical grounding.

Method: Propose MahaVar, a post-hoc OOD detector that combines Mahalanobis distance with class-wise distance variance. The method is based on theoretical analysis using Neural Collapse geometry showing ID samples structurally exhibit high variance.

Result: MahaVar achieves state-of-the-art performance on CIFAR-100 and ImageNet benchmarks (OpenOOD v1.5 protocol), with consistent improvements in AUROC and FPR@95 over existing Mahalanobis-based methods.

Conclusion: Class-wise distance variance is an effective OOD scoring mechanism, theoretically grounded in Neural Collapse. MahaVar demonstrates superior performance while maintaining simplicity as a post-hoc detector.

Abstract: Out-of-distribution (OOD) detection is a critical component for ensuring the reliability of deep neural networks in safety-critical applications. In this work, we present a key empirical observation: for in-distribution (ID) samples, class-wise Mahalanobis distances exhibit a pronounced sharp minimum structure, where the distance to the nearest class is small while distances to all other classes remain large, resulting in high variance across classes. In contrast, OOD samples tend to exhibit a less pronounced sharp minimum structure, producing comparatively lower variance across classes. We further provide a theoretical analysis grounding this observation in Neural Collapse geometry: under relaxed Neural Collapse assumptions on within-class compactness and inter-class separation, ID samples are shown to structurally exhibit high class-wise distance variance, offering a theoretical basis for its use as an OOD score. Motivated by this observation and its theoretical backing, we propose MahaVar, a simple and effective post-hoc OOD detector that augments the Mahalanobis distance with a class-wise distance variance term. Following the OpenOOD v1.5 benchmark protocol, MahaVar achieves state-of-the-art performance on CIFAR-100 and ImageNet, with consistent improvements in both AUROC and FPR@95 over existing Mahalanobis-based methods across all benchmarks.

</details>


### [174] [What if Tomorrow is the World Cup Final? Counterfactual Time Series Forecasting with Textual Conditions](https://arxiv.org/abs/2605.14422)
*Shuqi Gu,Yongxiang Zhao,Baoyu Jing,Kan Ren*

Main category: cs.LG

TL;DR: TADiff introduces counterfactual time series forecasting with textual conditions, using a text-attribution mechanism to distinguish mutable vs immutable factors and providing evaluation frameworks for both factual and counterfactual scenarios.


<details>
  <summary>Details</summary>
Motivation: Traditional time series forecasting methods rely on historical data or factual future conditions, overlooking counterfactual scenarios and being limited to simple structured conditions. Real-world scenarios require forecasting that adapts to complex, stochastic future conditions influenced by upcoming events, creating challenges in both forecasting and evaluation.

Method: Proposes counterfactual time series forecasting with textual conditions, introduces a text-attribution mechanism that distinguishes mutable from immutable factors to improve forecast accuracy under sophisticated textual conditions, and develops a comprehensive evaluation framework for both factual and counterfactual settings.

Result: The method enables more flexible and condition-aware forecasting that can handle complex real-world scenarios by incorporating textual conditions and counterfactual reasoning, even without ground truth time series for evaluation.

Conclusion: The approach addresses key limitations in traditional forecasting by introducing textual conditions and counterfactual scenarios, providing a more robust framework for real-world applications where future events dynamically influence time series patterns.

Abstract: Time series forecasting has become increasingly critical in real-world scenarios, where future sequences are influenced not only by historical patterns but also by forthcoming events. In this context, forecasting must dynamically adapt to complex and stochastic future conditions, which introduces fundamental challenges in both forecasting and evaluation. Traditional methods typically rely on historical data or factual future conditions, while overlooking counterfactual scenarios. Furthermore, many existing approaches are restricted to simple structured conditions, limiting their ability to generalize to the real-world complexities. To address these gaps, we introduce the task of counterfactual time series forecasting with textual conditions, enabling more flexible and condition-aware forecasting. We propose a comprehensive evaluation framework that encompasses both factual and counterfactual settings, even in the absence of ground truth time series. Additionally, we present a novel text-attribution mechanism that distinguishes mutable from immutable factors, thereby improving forecast accuracy under sophisticated and stochastic textual conditions. The project page is at https://seqml.github.io/TADiff/

</details>


### [175] [Collaborative Yet Personalized Policy Training: Single-Timescale Federated Actor-Critic](https://arxiv.org/abs/2605.14423)
*Leo Muxing Wang,Pengkun Yang,Lili Su*

Main category: cs.LG

TL;DR: FedAC: A federated actor-critic framework that learns shared subspace representations while maintaining personalized local policies, achieving linear speedup with convergence guarantees despite environmental heterogeneity.


<details>
  <summary>Details</summary>
Motivation: Existing actor-critic methods typically either overlook environmental heterogeneity or sacrifice personalization by training a single shared policy. There's a practical need for collaborative policy training that respects agent differences while leveraging shared structure.

Method: Proposes a federated actor-critic framework where agents share a common linear subspace representation while maintaining personalized local policy components. Agents iteratively estimate three components: common subspace, local critic heads, and local policies (actors). Uses canonical single-timescale updates with Markovian sampling.

Result: Establishes finite-time convergence with linear speedup: critic error converges at rate Õ(1/((1-γ)^4√(TK))) and policy gradient norm at Õ(1/((1-γ)^6√(TK))), where T is rounds, K is agents, γ is discount factor. Linear speedup with K despite heterogeneous Markovian trajectories. Experimental gains shown over Single PPO and FedAvg PPO on federated Hopper-v5.

Conclusion: The framework successfully addresses federated RL with environmental heterogeneity by balancing shared representation learning with personalization, achieving provable convergence with linear speedup and practical performance improvements over baseline methods.

Abstract: Despite the popularity of the actor-critic method and the practical needs of collaborative policy training, existing works typically either overlook environmental heterogeneity or give up personalization altogether by training a single shared policy across all agents. We consider a federated actor-critic framework in which agents share a common linear subspace representation while maintaining personalized local policy components, and agents iteratively estimate the common subspace, local critic heads, and local policies (i.e., actors). Under canonical single-timescale updates with Markovian sampling, we establish finite-time convergence via a novel joint linear approximation framework. Specifically, we show that the critic error converges to zero at the rate of $\tilde{\mathcal{O}}(1/((1-γ)^4\sqrt{TK}))$, and the policy gradient norm converges to zero at the rate of $\tilde{\mathcal{O}}(1/((1-γ)^6\sqrt{TK}))$, where $T$ is the number of rounds, $K$ is the number of agents, and $γ\in (0,1)$ is the discount factor. These results demonstrate linear speedup with respect to the number of agents $K$, despite heterogeneous Markovian trajectories under distinct transition kernels and coupled learning dynamics. To address these challenges, we develop a new perturbation analysis for the projected subspace updates and QR decomposition steps, together with conditional mixing arguments for heterogeneous Markovian noise. Furthermore, to handle the additional complications induced by policy updates and temporal dependence, we establish fine-grained characterizations of the discrepancies between function evaluations under Markovian sampling and under temporally frozen policies. Experiments instantiate the framework within PPO on federated \texttt{Hopper-v5} action-map heterogeneity, showing gains over Single PPO and FedAvg PPO and downstream transfer from the learned shared trunk.

</details>


### [176] [FrontierSmith: Synthesizing Open-Ended Coding Problems at Scale](https://arxiv.org/abs/2605.14445)
*Runyuan He,Qiuyang Mang,Shang Zhou,Kaiyuan Liu,Hanchen Li,Huanzhi Mao,Qizheng Zhang,Zerui Li,Bo Peng,Lufeng Cheng,Tianfu Fu,Yichuan Wang,Wenhao Chai,Jingbo Shang,Alex Dimakis,Joseph E. Gonzalez,Alvin Cheung*

Main category: cs.LG

TL;DR: FrontierSmith is an automated system that generates open-ended coding problems from closed-ended tasks to improve LLMs' performance on open-ended coding challenges.


<details>
  <summary>Details</summary>
Motivation: Current LLMs struggle with open-ended coding problems because such training data is scarce and expensive to construct, while most progress has focused on well-defined coding tasks like bug fixing and competitive programming.

Method: FrontierSmith starts with competitive programming problems and evolves them into open-ended variants by changing goals, restricting outputs, and generalizing inputs. It uses an idea divergence metric to select problems that elicit diverse approaches, then generates test cases and verifiers for selected candidates.

Result: Training on the synthesized data yields substantial improvements: Qwen3.5-9B improves by +8.82 on FrontierCS and +306.36 (Elo rating) on ALE-bench; Qwen3.5-27B improves by +12.12 and +309.12 respectively. The problems also make agents use more tokens and take more turns, similar to human-curated open-ended problems.

Conclusion: Closed-ended coding tasks can serve as a practical starting point for generating long-horizon, open-ended coding training data, enabling significant improvements in LLMs' open-ended coding capabilities.

Abstract: Many real-world coding challenges are open-ended and admit no known optimal solution. Yet, recent progress in LLM coding has focused on well-defined tasks such as feature implementation, bug fixing, and competitive programming. Open-ended coding remains a weak spot for LLMs, largely because open-ended training problems are scarce and expensive to construct. Our goal is to synthesize open-ended coding problems at scale to train stronger LLM coders. We introduce FrontierSmith, an automated system for iteratively evolving open-ended problems from existing closed-ended coding tasks. Starting from competitive programming problems, FrontierSmith generates candidate open-ended variants by changing the problems'goals, restricting outputs, and generalizing inputs. It then uses a quantitative idea divergence metric to select problems that elicit genuinely diverse approaches from different solvers. Agents then generate test cases and verifiers for the surviving candidates. On two open-ended coding benchmarks, training on our synthesized data yields substantial gains over the base models: Qwen3.5-9B improves by +8.82 score on FrontierCS and +306.36 (Elo-rating-based performance) on ALE-bench; Qwen3.5-27B improves by +12.12 and +309.12, respectively. The synthesized problems also make agents take more turns and use more tokens, similar to human-curated ones, suggesting that closed-ended seeds can be a practical starting point for long-horizon coding data.

</details>


### [177] [When Answers Stray from Questions: Hallucination Detection via Question-Answer Orthogonal Decomposition](https://arxiv.org/abs/2605.14449)
*Siyang Yao,Erhu Feng,Yubin Xia*

Main category: cs.LG

TL;DR: QAOD is a single-pass hallucination detection framework that projects away question-aligned directions from answer representations, using question-orthogonal components to suppress domain-conditioned variation while maintaining efficiency.


<details>
  <summary>Details</summary>
Motivation: Current hallucination detection methods face trade-offs: black-box consistency methods need repeated inference (inefficient), while single-pass white-box probes degrade under domain shift and treat answer representations in isolation.

Method: QAOD uses orthogonal decomposition to remove question-aligned components from answer representations, selects layers via diversity-penalized Fisher scoring and discriminative neurons via Fisher importance, and employs two probing strategies: joint probe (with question context) for in-domain performance and orthogonal-only probe for cross-domain generalization.

Result: QAOD's joint probe achieves best in-domain AUROC across all model-dataset pairs, while the orthogonal-only probe delivers strongest OOD transfer (surpassing best white-box baseline by up to 21% on BioASQ) at under 25% of generation cost.

Conclusion: QAOD effectively balances accuracy, efficiency, and robustness for hallucination detection by using question-orthogonal decomposition to suppress domain variation, offering both strong in-domain performance and excellent cross-domain generalization with low computational cost.

Abstract: Hallucination detection in large language models (LLMs) requires balancing accu racy, efficiency, and robustness to distribution shift. Black-box consistency methods are effective but demand repeated inference; single-pass white-box probes are effi cient yet treat answer representations in isolation, often degrading sharply under domain shift. We propose QAOD (Question-Answer Orthogonal Decomposition), a single-pass framework that projects away the question-aligned direction from the answer representation to obtain a question-orthogonal component that suppresses domain-conditioned variation. To identify informative signals, QAOD further selects layers via diversity-penalized Fisher scoring and discriminative neurons via Fisher importance. To address both in-domain detection and cross-domain generalization, we design two complementary probing strategies: pairing the or thogonal component with question context yields a joint probe that maximizes in-domain discriminability, while using the orthogonal component alone preserves domain-agnostic factuality signals for robust transfer. QAOD's joint probe achieves the best in-domain AUROC across all evaluated model-dataset pairs, while the orthogonal-only probe delivers the strongest OOD transfer, surpassing the best white-box baseline by up to 21% on BioASQ at under 25% of generation cost.

</details>


### [178] [LiSA: Lifelong Safety Adaptation via Conservative Policy Induction](https://arxiv.org/abs/2605.14454)
*Minbeom Kim,Lesly Miculicich,Bhavana Dalvi Mishra,Mihir Parmar,Phillip Wallis,Bharath Chandrasekhar,Kyomin Jung,Tomas Pfister,Long T. Le*

Main category: cs.LG

TL;DR: LiSA (Lifelong Safety Adaptation) is a conservative policy induction framework that improves AI guardrails through structured memory to adapt to real-world deployment contexts with sparse, noisy feedback.


<details>
  <summary>Details</summary>
Motivation: As AI agents handle private data and execute workflows, guardrail failures can cause serious harms like data leaks or unauthorized actions. The hardest failures are contextual - depending on local norms and policies that can't be fully specified before deployment. There's a gap between needing adaptive guardrails and having only sparse, noisy user feedback in real deployment settings.

Method: LiSA converts occasional failures into reusable policy abstractions using structured memory, adds conflict-aware local rules to prevent overgeneralization in mixed-label contexts, and applies evidence-aware confidence gating via posterior lower bounds. This allows memory reuse to scale with accumulated evidence rather than empirical accuracy alone.

Result: LiSA consistently outperforms strong memory-based baselines under sparse feedback across PrivacyLens+, ConFaide+, and AgentHarm datasets. It remains robust under noisy user feedback even at 20% label-flip rates and pushes the latency-performance frontier beyond backbone model scaling.

Conclusion: LiSA offers a practical approach to secure AI agents against unpredictable real-world edge risks by enabling guardrails to adapt to their operating environments through structured memory and conservative policy updates, bridging the gap between pre-deployment specifications and real-world deployment challenges.

Abstract: As AI agents move from chat interfaces to systems that read private data, call tools, and execute multi-step workflows, guardrails become a last line of defense against concrete deployment harms. In these settings, guardrail failures are no longer merely answer-quality errors: they can leak secrets, authorize unsafe actions, or block legitimate work. The hardest failures are often contextual: whether an action is acceptable depends on local privacy norms, organizational policies, and user expectations that resist pre-deployment specification. This creates a practical gap: guardrails must adapt to their own operating environments, yet deployment feedback is typically limited to sparse, noisy user-reported failures, and repeated fine-tuning is often impractical. To address this gap, we propose LiSA (Lifelong Safety Adaptation), a conservative policy induction framework that improves a fixed base guardrail through structured memory. LiSA converts occasional failures into reusable policy abstractions so that sparse reports can generalize beyond individual cases, adds conflict-aware local rules to prevent overgeneralization in mixed-label contexts, and applies evidence-aware confidence gating via a posterior lower bound, so that memory reuse scales with accumulated evidence rather than empirical accuracy alone. Across PrivacyLens+, ConFaide+, and AgentHarm, LiSA consistently outperforms strong memory-based baselines under sparse feedback, remains robust under noisy user feedback even at 20% label-flip rates, and pushes the latency--performance frontier beyond backbone model scaling. Ultimately, LiSA offers a practical path to secure AI agents against the unpredictable long tail of real-world edge risks.

</details>


### [179] [Focused PU learning from imbalanced data](https://arxiv.org/abs/2605.14467)
*Elias Zavitsanos,Georgios Paliouras*

Main category: cs.LG

TL;DR: A novel PU learning method for imbalanced datasets that uses a focused empirical risk estimator to handle both positive and unlabeled examples, achieving SOTA performance on imbalanced data under SCAR/SAR labeling mechanisms with successful real-world financial fraud detection application.


<details>
  <summary>Details</summary>
Motivation: Many real-world problems like disease gene identification, targeted marketing, fraud detection, and recommender systems suffer from limited labeled data. Training data often consists only of positive and unlabeled instances (PU learning), with unlabeled set dominated by negatives but containing some positives. Existing methods struggle with imbalanced settings and hard-to-detect positive examples that resemble negative ones.

Method: Proposes a focused empirical risk estimator that incorporates both positive and unlabeled examples to train binary classifiers. The method specifically addresses imbalanced settings and handles the challenge of positive examples that are difficult to distinguish from negative ones.

Result: Empirical evaluations demonstrate state-of-the-art performance on imbalanced datasets under two labeling mechanisms: selecting positives completely at random (SCAR) and selecting at random (SAR). The method shows successful real-world application in financial misstatement detection.

Conclusion: The proposed PU learning method effectively addresses the challenges of imbalanced datasets and hard-to-detect positive examples, providing superior performance on both controlled experiments and real-world applications like financial fraud detection.

Abstract: We propose a new method of learning from positive and unlabeled (PU) examples in highly imbalanced datasets. Many real-world problems, such as disease gene identification, targeted marketing, fraud detection, and recommender systems, are hard to address with machine learning methods, due to limited labeled data. Often, training data comprises positive and unlabeled instances, the latter typically being dominated by negative, but including also several positive instances. While PU learning is well-studied, few methods address imbalanced settings or hard-to-detect positive examples that resemble negative ones. Our approach uses a focused empirical risk estimator, incorporating both positive and unlabeled examples to train binary classifiers. Empirical evaluations demonstrate state-of-the-art performance on imbalanced datasets under two labeling mechanisms - selecting positives completely at random (SCAR) and selecting at random (SAR). Beyond these controlled experiments, we demonstrate the value of the proposed method in the real-world application of financial misstatement detection.

</details>


### [180] [Test-Time Learning with an Evolving Library](https://arxiv.org/abs/2605.14477)
*Weijia Xu,Alessandro Sordoni,Chandan Singh,Zelalem Gero,Michel Galley,Xingdi Yuan,Jianfeng Gao*

Main category: cs.LG

TL;DR: EvoLib is a test-time learning framework that allows LLMs to accumulate, reuse, and evolve knowledge across problems without parameter updates or external supervision through a shared library of abstractions.


<details>
  <summary>Details</summary>
Motivation: Current test-time learning methods often require parameter updates or external supervision, limiting their ability to accumulate and evolve knowledge across problem instances. There's a need for a framework that enables continual learning without these constraints while maintaining knowledge abstractions for reuse.

Method: The approach maintains a shared library of knowledge abstractions (modular skills and reflective insights) extracted from the model's own inference trajectories. It uses a principled weighting and consolidation mechanism that optimizes for both immediate utility and long-term value, allowing instance-specific abstractions to evolve into more general, reusable ones over time.

Result: EvoLib substantially outperforms top test-time scaling and learning methods across challenging benchmarks in mathematical reasoning, code generation, and multi-turn agentic environments, even without ground-truth feedback.

Conclusion: EvoLib demonstrates that LLMs can effectively accumulate and evolve knowledge across problem instances without parameter updates or external supervision, representing a promising direction for continual learning in language models through knowledge library evolution.

Abstract: We introduce EvoLib, a test-time learning framework that enables large language models to accumulate, reuse, and evolve knowledge across problem instances without parameter updates or external supervision. Instead of adapting model parameters, our approach maintains a shared library of knowledge abstractions, including modular skills and reflective insights, automatically extracted from the model's own inference trajectories. To support continual improvement, we introduce a principled weighting and consolidation mechanism that jointly optimizes for immediate utility and long-term value. This allows simple, instance-specific abstractions to evolve into more general and reusable ones over time. Across challenging benchmarks in mathematical reasoning, code generation, and multi-turn agentic environments, EvoLib improves substantially over the top test-time scaling and learning methods without ground-truth feedback.

</details>


### [181] [A Novel Schur-Decomposition-Based Weight Projection Method for Stable State-Space Neural-Network Architectures](https://arxiv.org/abs/2605.14489)
*Sergio Vanegas,Lasse Lensu,Fredy Ruiz*

Main category: cs.LG

TL;DR: A novel method for building stable black-box models of dynamical systems using Schur decomposition projections to ensure asymptotic stability while maintaining training compatibility.


<details>
  <summary>Details</summary>
Motivation: Building black-box models for dynamical systems from data is challenging, especially when requiring asymptotic stability guarantees. Existing methods often struggle to balance stability constraints with training efficiency.

Method: Introduced a stability-ensuring projection scheme based on Schur decomposition for linear discrete-time state-space layers, plus an alternative pre-factorized formulation. The method dynamically projects the quasi-triangular factor of the state matrix's real Schur decomposition onto its nearest stable peer.

Result: On synthetic linear systems, the method achieves accuracy and convergence rates comparable to state-of-the-art stable-system identification techniques, with only marginal computational increase. In neural-network architectures with static nonlinearities, lower weight count facilitates convergence without sacrificing accuracy on real-world datasets.

Conclusion: The Schur-based projection provides a numerically robust framework for identifying complex dynamics at state-of-the-art levels while satisfying strict asymptotic-stability requirements.

Abstract: Building black-box models for dynamical systems from data is a challenging problem in machine learning, especially when asymptotic stability guarantees are required. In this paper, we introduce a novel stability-ensuring and backpropagation-compatible projection scheme based on the Schur decomposition for the state matrix of linear discrete-time state-space layers, as well as an alternative pre-factorized formulation of the methodology. The proposed methods dynamically project the quasi-triangular factor of the state matrix's real Schur decomposition onto its nearest stable peer, ensuring stable dynamics with minimal overparameterization. Experiments on synthetic linear systems demonstrate that the method achieves accuracy and convergence rates comparable to those of state-of-the-art stable-system identification techniques, despite a marginal increase in computational complexity. Furthermore, the lower weight count facilitates convergence during training without sacrificing accuracy in stacked neural-network architectures with static nonlinearities targeting real-world datasets. These results suggest that the Schur-based projection provides a numerically robust framework for identifying complex dynamics on par with the State of the Art while satisfying strict asymptotic-stability requirements.

</details>


### [182] [ROAD: Adaptive Data Mixing for Offline-to-Online Reinforcement Learning via Bi-Level Optimization](https://arxiv.org/abs/2605.14497)
*Letian Yang,Xu Liu,Yiqiang Lu,Jian Liu,Weiqiang Wang,Shuai Li*

Main category: cs.LG

TL;DR: ROAD is a dynamic plug-and-play framework for offline-to-online RL that automates data replay through bi-level optimization and a multi-armed bandit mechanism, eliminating manual adjustments while achieving superior stability and performance.


<details>
  <summary>Details</summary>
Motivation: Current offline-to-online RL approaches suffer from non-stationary distribution shift between offline datasets and evolving online policies. Existing methods rely on static mixing ratios or heuristic replay strategies that lack adaptability to different environments and training dynamics, leading to suboptimal tradeoffs between stability and asymptotic performance.

Method: Proposes ROAD framework that formulates data selection as bi-level optimization: outer level treats data mixing as meta-decision governing policy performance, inner level handles conventional Q-learning updates. Uses multi-armed bandit mechanism with surrogate objective approximating bi-level gradient, maintaining offline priors while preventing value overestimation.

Result: Empirical results show ROAD consistently outperforms existing data replay methods across various datasets, eliminates need for manual context-specific adjustments, and achieves superior stability and asymptotic performance compared to state-of-the-art approaches.

Conclusion: ROAD provides an effective solution to the data selection challenge in offline-to-online RL through automated adaptive data mixing, addressing fundamental objective misalignment and offering a practical framework for seamless integration of offline priors with online fine-tuning.

Abstract: Offline-to-online reinforcement learning harnesses the stability of offline pretraining and the flexibility of online fine-tuning. A key challenge lies in the non-stationary distribution shift between offline datasets and the evolving online policy. Common approaches often rely on static mixing ratios or heuristic-based replay strategies, which lack adaptability to different environments and varying training dynamics, resulting in suboptimal tradeoff between stability and asymptotic performance. In this work, we propose Reinforcement Learning with Optimized Adaptive Data-mixing (ROAD), a dynamic plug-and-play framework that automates the data replay process. We identify a fundamental objective misalignment in existing approaches. To tackle this, we formulate the data selection problem as a bi-level optimization process, interpreting the data mixing strategy as a meta-decision governing the policy performance (outer-level) during online fine-tuning, while the conventional Q-learning updates operate at the inner level. To make it tractable, we propose a practical algorithm using a multi-armed bandit mechanism. This is guided by a surrogate objective approximating the bi-level gradient, which simultaneously maintains offline priors and prevents value overestimation. Our empirical results demonstrate that this approach consistently outperforms existing data replay methods across various datasets, eliminating the need for manual, context-specific adjustments while achieving superior stability and asymptotic performance.

</details>


### [183] [Enjoy Your Layer Normalization with the Computational Efficiency of RMSNorm](https://arxiv.org/abs/2605.14521)
*Yuxin Guo,Yihao Yue,Yunhao Ni,Yizhou Ruan,Jie Luo,Wenjun Wu,Lei Huang*

Main category: cs.LG

TL;DR: A framework to determine if Layer Normalization can be replaced with more efficient RMSNorm without changing model function by folding centering operations into upstream linear layers.


<details>
  <summary>Details</summary>
Motivation: Layer Normalization introduces non-negligible inference overhead due to per-sample centering and scaling. While RMSNorm is more efficient by removing centering, it may discard beneficial centering effects. There's a need to replace LN with RMSNorm while preserving model functionality.

Method: Develop a framework to determine if LN can be replaced by RMSNorm without changing model function. Fold LN's centering operation into upstream general linear layers using column-centered constraint (CCC) and column-based weight centering (CBWC). Extend analysis to arbitrary DNNs, define foldable LNs, and develop a graph-based detection algorithm.

Result: Analysis shows many LNs in widely used architectures are foldable, enabling exact inference-time conversion and end-to-end acceleration of 2% to 12% without changing model predictions. Even when exact equivalence is partially broken in practical training, the method remains competitive with vanilla LN while improving efficiency.

Conclusion: The proposed framework successfully identifies foldable LNs that can be replaced by RMSNorm for efficiency gains while maintaining model performance, offering a practical solution to reduce inference overhead in deep learning models.

Abstract: Layer normalization (LN) is a fundamental component in modern deep learning, but its per-sample centering and scaling introduce non-negligible inference overhead. RMSNorm improves efficiency by removing the centering operation, yet this may discard benefits associated with centering. This paper propose a framework to determine whether an LN in an arbitrary DNN can be replaced by RMSNorm without changing the model function. The key idea is to fold LN's centering operation into upstream general linear layers by enforcing zero-mean outputs through the column-centered constraint (CCC) and column-based weight centering (CBWC). We extend the analysis to arbitrary DNNs, define such LNs as foldable LNs, and develop a graph-based detection algorithm. Our analysis shows that many LNs in widely used architectures are foldable, enabling exact inference-time conversion and end-to-end acceleration of 2% to 12% without changing model predictions. Experiments across multiple task families further show that, when exact equivalence is partially broken in practical training settings, our method remains competitive with vanilla LN while improving efficiency.

</details>


### [184] [Lang2MLIP: End-to-End Language-to-Machine Learning Interatomic Potential Development with Autonomous Agentic Workflows](https://arxiv.org/abs/2605.14527)
*Wenwen Li,Yuki Orimo,Nontawat Charoenphakdee*

Main category: cs.LG

TL;DR: Lang2MLIP is an LLM-based multi-agent framework that automates machine learning interatomic potential development through natural language input and sequential decision-making, eliminating the need for fixed pipelines and domain expertise.


<details>
  <summary>Details</summary>
Motivation: Current MLIP development requires expertise in atomistic simulations, machine learning, and workflow design, with existing automated pipelines being inflexible and dependent on domain experts. This creates barriers for non-experts working with complex materials systems.

Method: Proposes Lang2MLIP: a multi-agent framework where LLMs observe current dataset, model, evaluation results, and execution logs, then automatically select appropriate actions to improve the model. The system can self-correct by revisiting earlier subsystems when failures arise.

Result: Evaluated on a solid electrolyte interphase (SEI) system with multiple components and interfaces, demonstrating the framework's ability to automate MLIP development through LLM-based sequential decision-making.

Conclusion: LLM-based multi-agent systems show promise for automating MLIP development and making it more accessible to non-experts by removing the need for predefined pipelines and enabling self-correction capabilities.

Abstract: Developing machine learning interatomic potentials (MLIPs) for complex materials systems remains challenging because it requires expertise in atomistic simulations, machine learning, and workflow design, as well as iterative active learning procedures. Existing automated pipelines typically assume a fixed sequence of stages or depend on domain experts, which limits their adaptability to heterogeneous materials systems where the optimal curriculum is not known in advance. To lower the barrier to developing MLIPs for non-experts, we propose Lang2MLIP, a multi-agent framework that takes natural-language input and formulates end-to-end MLIP development as a sequential decision-making problem solved by large language models (LLMs). At each step, a decision-making agent observes the current dataset, model, evaluation results, and execution log, and then automatically selects an appropriate action to improve the model. This removes the need for a predefined pipeline and enables the agent to self-correct by revisiting earlier subsystems when new failures arise. We evaluate this approach on a solid electrolyte interphase (SEI) system with multiple components and interfaces. These results suggest that LLM-based multi-agent systems are a promising direction for automating MLIP development and making it more accessible to non-experts.

</details>


### [185] [Exploring Geographic Relative Space in Large Language Models through Activation Patching](https://arxiv.org/abs/2605.14535)
*Stef De Sabbata,Rahul Baiju,Stefano Mizzaro,Kevin Roitero*

Main category: cs.LG

TL;DR: LLMs' geographic reasoning safety concerns investigated via activation patching for mechanistic interpretability of relative spatial processing.


<details>
  <summary>Details</summary>
Motivation: The increased use of LLMs in geography raises safety concerns due to limited understanding of their inner workings, especially regarding how they process relative geographic space.

Method: Activation patching, an emerging tool for mechanistic interpretability, is used to examine how LLMs process relative geographic space.

Result: The abstract does not provide specific results, but indicates the methodology (activation patching) is being applied to study LLM processing of geographic spatial relationships.

Conclusion: Mechanistic interpretability approaches like activation patching are necessary to understand LLM processing of geographic space for safer integration in geography applications.

Abstract: The increased use of Large Language Models (LLMs) in geography raises substantial questions about the safety of integrating these tools across a wide range of processes and analyses, given our very limited understanding of their inner workings. In this extended abstract, we examine how LLMs process relative geographic space using activation patching, an emerging tool for mechanistic interpretability.

</details>


### [186] [RxEval: A Prescription-Level Benchmark for Evaluating LLM Medication Recommendation](https://arxiv.org/abs/2605.14543)
*Shuhao Chen,Weisen Jiang,Changmiao Wang,Xiaoqing Wu,Xuanren Shi,Yu Zhang,James T. Kwok*

Main category: cs.LG

TL;DR: RxEval is a new benchmark for evaluating LLM medication prescribing capabilities using prescription-level multiple-choice questions with detailed patient profiles and clinical trajectories, showing current models struggle with exact medication recommendations.


<details>
  <summary>Details</summary>
Motivation: Existing benchmarks for inpatient medication recommendation use admission-level prediction with coarse drug codes and multi-hot diagnostic/procedure inputs, which fail to capture the per-timepoint, information-rich nature of real-world prescribing where clinicians must repeatedly select specific medications, doses, and routes as patient conditions evolve.

Method: Proposed RxEval benchmark formulates medication recommendation as prescription-level multiple-choice questions. Each question presents detailed patient profiles and time-ordered clinical trajectories, requiring selection of specific medication-dose-route triples from real prescriptions. Uses patient-specific distractors generated via reasoning-chain perturbation. The benchmark comprises 1,547 questions spanning 584 patients, 18 diagnostic categories, and 969 unique medications.

Result: Evaluation of 16 LLMs shows RxEval is both challenging and discriminative: F1 scores range from 45.18 to 77.10 across models, and the best Exact Match is only 46.10%. Error analysis reveals that even frontier models may overlook stated patient information and fail to derive appropriate clinical conclusions.

Conclusion: RxEval provides a more realistic benchmark for evaluating LLM prescribing capabilities, highlighting significant gaps in current models' ability to make precise medication recommendations despite having access to detailed clinical information. The benchmark exposes limitations in clinical reasoning and attention to patient-specific details.

Abstract: Inpatient medication recommendation requires clinicians to repeatedly select specific medications, doses, and routes as a patient's condition evolves. Existing benchmarks formulate this task as admission-level prediction over coarse drug codes with multi-hot diagnostic and procedure code inputs, failing to capture the per-timepoint, information-rich nature of real prescribing. We propose RxEval, a prescription-level benchmark that evaluates LLM prescribing capability by multiple-choice questions: each question presents a detailed patient profile and time-ordered clinical trajectory, requiring selection of specific medication-dose-route triples from real prescriptions and patient-specific distractors generated via reasoning-chain perturbation. RxEval comprises 1,547 questions spanning 584 patients, 18 diagnostic categories, and 969 unique medications. Evaluation of 16 LLMs shows that RxEval is both challenging and discriminative: F1 ranges from 45.18 to 77.10 across models, and the best Exact Match is only 46.10%. Error analysis reveals that even frontier models may overlook stated patient information and fail to derive clinical conclusions.

</details>


### [187] [Discovering Physical Directions in Weight Space: Composing Neural PDE Experts](https://arxiv.org/abs/2605.14546)
*Pengkai Wang,Pengwei Liu,Yuanyi Wang,Guanyu Chen,Xingyu Ren,Xiaolong Li,Zhongkai Hao,Yuting Kong,Qixin Zhang,Dong Ni*

Main category: cs.LG

TL;DR: The paper introduces Calibration-Conditioned Merge (CCM), a post-hoc method for composing neural PDE experts by identifying and calibrating along a physical parameter direction in weight space, enabling accurate interpolation between PDE regimes without additional training.


<details>
  <summary>Details</summary>
Motivation: While neural operators can be fine-tuned to multiple PDE regimes, it's unclear whether weight-space updates form isolated experts or reveal reusable physical structure. The authors aim to understand if endpoint fine-tuning reveals a calibratable physical direction for transfer across continuous PDE families.

Method: Starting from a shared family anchor, fine-tune low- and high-regime endpoint experts, then separate updates into family-shared adaptation and physical parameter direction. Propose CCM - a calibration-conditioned merge method that: 1) identifies the physical direction in weight space, 2) infers target composition coordinate from metadata/calibration/observed rollout, and 3) deploys a single merged checkpoint for the remaining rollout.

Result: CCM achieves strong gains in extrapolative regimes: reduces out-of-distribution rollout error relative to family anchor by 54.2% on reaction-diffusion, 42.8% on viscosity-parameterized Navier-Stokes, and 13.8% on radial dam-break dynamics. Experiments confirm endpoint fine-tuning reveals a calibratable physical direction rather than arbitrary checkpoint drift.

Conclusion: Endpoint fine-tuning of neural PDE operators reveals a reusable physical structure in weight space that aligns with underlying physical parameters. CCM provides a training-free method to compose experts along this physical direction, enabling effective transfer across PDE regimes with strong performance on extrapolative tasks.

Abstract: Recent advances in neural operators have made partial differential equation (PDE) surrogate modeling increasingly scalable and transferable through large-scale pretraining and in-context adaptation. However, after a shared operator is fine-tuned to multiple regimes within a continuous physical family, it remains unclear whether the resulting weight-space updates merely form isolated regime experts or reveal reusable physical structure. Starting from a shared family anchor, we fine-tune low- and high-regime endpoint experts and show that their updates can be separated into a family-shared adaptation and a direction aligned with the underlying physical parameter. This separation reinterprets endpoint experts as finite-difference probes of a local physical direction in weight space, explaining why static averaging can interpolate between regimes but attenuates endpoint-specific physics. Building on this perspective, we propose Calibration-Conditioned Merge (CCM), a post-hoc coordinate readout method for composing neural PDE experts along this physical direction. Given physical metadata, a calibrated coordinate mapping, or a short observed rollout prefix, CCM infers the target composition coordinate and deploys a single merged checkpoint for the remaining rollout. We evaluate CCM on the reaction--diffusion system, viscosity-parameterized two-dimensional Navier--Stokes equations, and radial dam-break dynamics. Across these benchmarks, CCM achieves its strongest gains in extrapolative regimes, reducing out-of-distribution rollout error relative to the family anchor by 54.2%, 42.8%, and 13.8%, respectively. Further experiments across FNO scales, a DPOT-style backbone, and ablations confirm that endpoint fine-tuning is not arbitrary checkpoint drift, but reveals a calibratable physical direction for training-free transfer across PDE regimes.

</details>


### [188] [Multi-Dimensional Model Integrity and Responsibility Assessment Index and Scoring Framework](https://arxiv.org/abs/2605.14550)
*Phuc Truong Loc Nguyen,Thanh Hung Do,Truong Thanh Hung Nguyen,Hung Cao*

Main category: cs.LG

TL;DR: MIRAI is a unified evaluation framework that assesses tabular AI models across explainability, fairness, robustness, privacy, and sustainability, combining these dimensions into a single score for responsible model selection.


<details>
  <summary>Details</summary>
Motivation: Current AI evaluation in high-stakes tabular domains focuses too narrowly on predictive performance, with explainability, fairness, robustness, privacy, and sustainability assessed mostly in isolation rather than holistically.

Method: Proposes MIRAI framework that measures tabular models across five dimensions under controlled comparison, combining established metrics through normalized and direction-aligned dimension scores for direct model comparison.

Result: Experiments on healthcare, financial, and socioeconomic datasets show that higher predictive performance doesn't guarantee better overall integrity/responsibility, and simpler models often achieve better cross-dimensional balance than complex deep tabular architectures.

Conclusion: MIRAI provides a compact, practical basis for responsible model selection in regulated settings by enabling holistic assessment beyond just predictive accuracy.

Abstract: Artificial intelligence in high-stakes tabular domains cannot be evaluated by predictive performance alone, yet current practice still assesses explainability, fairness, robustness, privacy, and sustainability mostly in isolation. We propose the Model Integrity and Responsibility Assessment Index (MIRAI), a unified evaluation framework that measures tabular models across these five dimensions under a controlled comparison setting and aggregates them into a single score. MIRAI combines established metrics through normalized and direction-aligned dimension scores, which enables direct comparison across models with different architectural and computational profiles. Experiments on healthcare, financial, and socioeconomic datasets show that higher predictive performance does not necessarily imply better overall integrity and responsibility. In several cases, simpler models achieve a stronger cross-dimensional balance than more complex deep tabular architectures. MIRAI provides a compact and practical basis for responsible model selection in regulated settings.

</details>


### [189] [SeesawNet: Towards Non-stationary Time Series Forecasting with Balanced Modeling of Common and Specific Dependencies](https://arxiv.org/abs/2605.14551)
*Hao Li,Lu Zhang,Liu Chong,Yankai Chen,Pengyang Wang,Yingjie Zhou*

Main category: cs.LG

TL;DR: SeesawNet is a novel architecture for multivariate time series forecasting that adaptively balances common and instance-specific dependency modeling using Adaptive Stationary-Nonstationary Attention, outperforming state-of-the-art methods.


<details>
  <summary>Details</summary>
Motivation: Instance normalization (IN) helps reduce distribution shifts in time series forecasting but can over-smooth instance-specific structural information essential for temporal and cross-channel heterogeneity. Existing methods often fail to balance the tension between capturing common patterns and instance-specific dependencies based on each instance's non-stationary structures.

Method: Proposed SeesawNet with Adaptive Stationary-Nonstationary Attention (ASNA) that captures common dependencies from normalized sequences and specific dependencies from raw sequences, adaptively fusing them based on instance-level non-stationarity. The architecture alternates temporal and channel relationship modeling to jointly capture long-range and cross-variable dependencies.

Result: Extensive experiments on multiple real-world benchmarks demonstrate that SeesawNet consistently outperforms state-of-the-art methods.

Conclusion: SeesawNet successfully addresses the dilemma of balancing common and instance-specific dependency modeling in multivariate time series forecasting through its adaptive attention mechanism and unified architecture design.

Abstract: Instance normalization (IN) is widely used in non-stationary multivariate time series forecasting to reduce distribution shifts and highlight common patterns across samples. However, IN can over-smooth instance-specific structural information that is essential for modeling temporal and cross-channel heterogeneity. While prior methods further suppress distribution discrepancies or attempt to recover temporal specific dependencies, they often ignore a central tension: how to adaptively model common and instance-specific dependency based on each instance's non-stationary structures. To address this dilemma, we propose SeesawNet, a unified architecture that dynamically balances common and instance-specific dependency modeling in both temporal and channel dimensions. At its core is Adaptive Stationary-Nonstationary Attention (ASNA), which captures common dependencies from normalized sequences and specific dependencies from raw sequences, and adaptively fuses them according to instance-level non-stationarity. Built upon ASNA, SeesawNet alternates dedicated temporal and channel relationship modeling to jointly capture long-range and cross-variable dependencies. Extensive experiments on multiple real-world benchmarks demonstrate that SeesawNet consistently outperforms state-of-the-art methods.

</details>


### [190] [Efficient Multi-objective Prompt Optimization via Pure-exploration Bandits](https://arxiv.org/abs/2605.14553)
*Donghao Li,Chengshuai Shi,Weijuan Ou,Cong Shen,Jing Yang*

Main category: cs.LG

TL;DR: Researchers address multi-objective prompt selection for LLMs using bandit algorithms, developing efficient methods for identifying optimal prompts across multiple performance metrics.


<details>
  <summary>Details</summary>
Motivation: Current prompt engineering research overlooks that prompt performance is inherently multi-faceted and cannot be captured by a single metric, creating a gap in effectively selecting prompts that balance multiple objectives.

Method: The study frames prompt selection as a pure-exploration multi-objective bandit problem. Researchers adapt efficient algorithms from multi-objective bandits and introduce a novel design for best feasible arm identification in structured bandits, with theoretical guarantees for the linear case.

Result: Extensive experiments across multiple LLMs show that bandit-based approaches yield significant improvements over baselines, establishing a principled and efficient framework for multi-objective prompt optimization.

Conclusion: Bandit algorithms provide an effective solution to the multi-objective prompt selection problem, offering provable efficiency and practical improvements over existing methods for identifying optimal prompts across multiple metrics.

Abstract: Prompt engineering has become central to eliciting the capabilities of large language models (LLMs). At its core lies prompt selection -- efficiently identifying the most effective prompts. However, most prior investigations overlook a key challenge: the inherently multi-faceted nature of prompt performance, which cannot be captured by a single metric. To fill this gap, we study the multi-objective prompt selection problem under two practical settings: Pareto prompt set recovery and best feasible prompt identification. Casting the problem into the pure-exploration bandits framework, we adapt provably efficient algorithms from multi-objective bandits and further introduce a novel design for best feasible arm identification in structured bandits, with theoretical guarantees on the identification error in the linear case. Extensive experiments across multiple LLMs show that the bandit-based approaches yield significant improvements over baselines, establishing a principled and efficient framework for multi-objective prompt optimization.

</details>


### [191] [Resolving Action Bottleneck: Agentic Reinforcement Learning Informed by Token-Level Energy](https://arxiv.org/abs/2605.14558)
*Langzhou He,Junyou Zhu,Yue Zhou,Zhengyao Gu,Junhua Liu,Wei-Chieh Huang,Henry Peng Zou,David Wipf,Philip S. Yu,Qitian Wu*

Main category: cs.LG

TL;DR: ActFocus improves agentic RL by reweighting training signals to focus on action tokens rather than reasoning tokens, addressing the action bottleneck problem and achieving significant performance gains over standard methods like PPO and GRPO.


<details>
  <summary>Details</summary>
Motivation: Current policy-gradient methods (PPO, GRPO) treat all tokens equally during agentic reinforcement learning, leading to uniform credit assignment. This is problematic because action tokens (which are crucial for rewards) constitute only a small fraction of trajectories, while reasoning tokens dominate, causing misallocation of training signals - a phenomenon called the "Action Bottleneck".

Method: From an energy-based modeling perspective, the authors show that token-level training signals concentrate sharply on action tokens. They propose ActFocus, a token reweighting approach that downweights gradients on reasoning tokens. They also introduce an energy-based redistribution mechanism that further increases weights on action tokens with higher uncertainty.

Result: ActFocus consistently outperforms PPO and GRPO across four environments and different model sizes. It achieves final-step gains of up to 65.2 and 63.7 percentage points over PPO and GRPO respectively, without any additional runtime or memory cost.

Conclusion: The action bottleneck phenomenon in agentic RL leads to suboptimal credit assignment, and ActFocus's simple but effective token reweighting approach addresses this by focusing training signals on action tokens, resulting in substantial performance improvements over standard methods.

Abstract: Agentic reinforcement learning trains large language models using multi-turn trajectories that interleave long reasoning traces with short environment-facing actions. Common policy-gradient methods, such as PPO and GRPO, treat each token in a trajectory equally, leading to uniform credit assignment. In this paper, we critically demonstrate that such uniform credit assignment largely misallocates token-level training signals. From an energy-based modeling perspective, we show that token-level training signals, quantified by their correlations with reward variance of different rollouts sampled from a given prompt, concentrate sharply on action tokens rather than reasoning tokens, even though action tokens account for only a small fraction of the trajectory. We refer to this phenomenon as the Action Bottleneck. Motivated by this observation, we propose an embarrassingly simple token reweighting approach, ActFocus, that downweights gradients on reasoning tokens, along with an additional energy-based redistribution mechanism that further increases the weights on action tokens with higher uncertainty. Across four environments and different model sizes, ActFocus consistently outperforms PPO and GRPO, yielding final-step gains of up to 65.2 and 63.7 percentage points, respectively, without any additional runtime or memory cost.

</details>


### [192] [Woodelf++: A Fast and Unified Partial Dependence Plot Algorithm for Decision Tree Ensembles](https://arxiv.org/abs/2605.14578)
*Ron Wettenstein,Alexander Nadel,Udi Boker*

Main category: cs.LG

TL;DR: Woodelf++ is a unified framework that efficiently computes Partial Dependence Plots, Joint PDPs, and Any-Order Partial Dependence Interaction Values for decision tree ensembles, offering dramatic speed improvements over existing methods.


<details>
  <summary>Details</summary>
Motivation: Current methods for computing explainability tools like Partial Dependence Plots, Joint-PDPs, and feature interaction values are computationally expensive, especially for large datasets. There's a need for more efficient algorithms that can compute all these explainability tools in a unified framework.

Method: Woodelf++ builds on Woodelf (an efficient SHAP computation algorithm) and extends it by deriving suitable metrics over pseudo-Boolean functions. This allows unified computation of PDPs (exact and approximate), Joint-PDPs, Any-Order-PDIVs, and introduces Full PDPs that use model split thresholds to capture behavior across all possible feature values.

Result: The method delivers substantial complexity improvements: 6x faster than state-of-the-art for PDP and Joint-PDP, and up to five orders of magnitude faster than scikit-learn. For Any-Order-PDIVs, Woodelf++ computes all interaction values in 5 minutes vs. estimated 1,000,000+ years for state-of-the-art. Implementation is in pure Python with GPU acceleration support.

Conclusion: Woodelf++ provides a unified, efficient framework for computing multiple explainability tools on decision tree ensembles, with exponential speed improvements for certain tasks, making comprehensive model interpretation more practical for large datasets.

Abstract: Partial Dependence Plots (PDPs) visualize how changes in a single feature affect the average model prediction. They are widely used in practice to interpret decision tree ensembles and other machine learning models. Joint-PDPs extend this idea to pairs of features, revealing their combined effect. Partial Dependence Interaction Values (PDIVs) measure feature interactions. The Any-Order-PDIVs task computes these interactions for every feature subset across all rows of the dataset.
  We introduce Woodelf++, a unified and efficient approach for computing all these useful explainability tools on decision tree ensembles, building on Woodelf, an algorithm for efficient SHAP computation. By deriving suitable metrics over pseudo-Boolean functions, Woodelf++ can compute PDPs (exact and approximate), Joint-PDPs, and Any-Order-PDIVs in a unified framework. Our method delivers substantial complexity improvements over the state of the art, including an exponential gain for Any-Order-PDIVs. Additionally, we introduce and efficiently compute Full PDPs, which leverage the model's split thresholds to faithfully capture its behavior across all possible feature values.
  Woodelf++ is implemented in pure Python and supports GPU acceleration. On a dataset with 400,000 rows, Woodelf++ computes PDP and Joint-PDP up to 6x faster than the state of the art and up to five orders of magnitude faster than scikit-learn. For Any-Order-PDIVs, the gap is even larger: Woodelf++ computes all interaction values in 5 minutes, while the state of the art is estimated to require over 1,000,000 years.

</details>


### [193] [Angel or Demon: Investigating the Plasticity Interventions' Impact on Backdoor Threats in Deep Reinforcement Learning](https://arxiv.org/abs/2605.14587)
*Oubo Ma,Ruixiao Lin,Yang Dai,Jiahao Chen,Chunyi Zhou,Linkang Du,Shouling Ji*

Main category: cs.LG

TL;DR: Empirical study shows plasticity interventions in DRL can both mitigate and exacerbate backdoor vulnerabilities, revealing gradient amplification vs. activation pathway disruption as key mechanisms.


<details>
  <summary>Details</summary>
Motivation: Plasticity interventions are essential for modern DRL but their impact on backdoor vulnerabilities is unexplored, creating risks in practical deployments despite extensive research on DRL backdoor attacks.

Method: Empirical study of 14,664 cases integrating representative plasticity interventions and attack scenarios, with pathological analysis of underlying mechanisms.

Result: Only SAM intervention exacerbates backdoor threats through gradient amplification, while other interventions mitigate them via activation pathway disruption and representation space compression.

Conclusion: Derived SCC framework for robust backdoor injection explaining intervention-backdoor interplay, and identified abnormal loss landscape sharpness as a key indicator for DRL backdoor detection.

Abstract: Extensive research has highlighted the severe threats posed by backdoor attacks to deep reinforcement learning (DRL). However, prior studies primarily focus on vanilla scenarios, while plasticity interventions have emerged as indispensable built-in components of modern DRL agents. Despite their effectiveness in mitigating plasticity loss, the impact of these interventions on DRL backdoor vulnerabilities remains underexplored, and this lack of systematic investigation poses risks in practical DRL deployments. To bridge this gap, we empirically study 14,664 cases integrating representative interventions and attack scenarios. We find that only one intervention (i.e., SAM) exacerbates backdoor threats, while other interventions mitigate them. Pathological analysis identifies that the exacerbation is attributed to backdoor gradient amplification, while the mitigation stems from activation pathway disruption and representation space compression. From these findings, we derive two novel insights: (1) a conceptual framework SCC for robust backdoor injection that deconstructs the mechanistic interplay between interventions and backdoors in DRL, and (2) abnormal loss landscape sharpness as a key indicator for DRL backdoor detection.

</details>


### [194] [Silent Collapse in Recursive Learning Systems](https://arxiv.org/abs/2605.14588)
*Zhipeng Zhang*

Main category: cs.LG

TL;DR: Silent collapse is a phenomenon in recursive learning where model internal distributions degrade despite stable conventional metrics; the paper identifies early precursors and proposes a monitoring framework to prevent it.


<details>
  <summary>Details</summary>
Motivation: Recursive learning (training models on their own generated data) is increasingly common but standard performance metrics fail to detect internal degradation before it becomes irreversible, potentially leading to "silent collapse" where models deteriorate without apparent warning.

Method: The paper identifies three trajectory-level precursors to silent collapse: (1) contraction of anchor entropy, (2) freezing of representation drift, and (3) erosion of tail coverage. Based on these, they propose the MTR (Monitor-Trust-Regulator) framework - a lightweight metacognitive loop that monitors trajectory statistics, estimates a slow-timescale trust variable, and adaptively modulates learning intensity.

Result: The discovered precursors manifest multiple generations before any degradation in standard validation metrics, enabling early warning. The MTR framework provides early warning and actively prevents silent collapse without requiring access to pristine real data.

Conclusion: Silent collapse is a gradual, detectable phenomenon in recursive learning systems, and the proposed MTR framework offers a practical solution for early detection and prevention, addressing critical challenges when original data is unavailable, contaminated, or private.

Abstract: Recursive learning -- where models are trained on data generated by previous versions of themselves -- is increasingly common in large language models, autonomous agents, and self-supervised systems. However, standard performance metrics (loss, perplexity, accuracy) often fail to detect internal degradation before it becomes irreversible. Here we identify a phenomenon we call silent collapse: under broad recursive conditions, model internal distributions -- predictive entropy, representational diversity, and tail coverage -- progressively contract even as conventional metrics appear stable or improving.
  We discover that silent collapse is not abrupt. Its onset is reliably preceded by three trajectory-level precursors: (1) contraction of anchor entropy, (2) freezing of representation drift, and (3) erosion of tail coverage. These signals manifest multiple generations before any degradation in standard validation metrics, enabling early warning.
  Based on these precursors, we propose the MTR (Monitor--Trust--Regulator) framework, a lightweight metacognitive loop that monitors trajectory statistics, estimates a slow-timescale trust variable, and adaptively modulates the effective learning intensity. MTR provides early warning and actively prevents silent collapse without requiring access to pristine real data -- a critical advantage when original data is unavailable, contaminated, or private.

</details>


### [195] [Fast Rates for Inverse Reinforcement Learning](https://arxiv.org/abs/2605.14599)
*Andreas Schlaginhaufen,Maryam Kamgarpour*

Main category: cs.LG

TL;DR: Novel structural and statistical results for entropy-regularized min-max inverse reinforcement learning with linear reward classes in finite-horizon MDPs with Borel spaces, showing fast O(1/n) convergence rates without exploration assumptions.


<details>
  <summary>Details</summary>
Motivation: There's a need for better understanding of min-max inverse reinforcement learning (IRL) methods, particularly their statistical properties, structural relationships with maximum likelihood estimation, and performance guarantees under realistic conditions (misspecification, no exploration assumptions).

Method: Theoretical analysis of entropy-regularized min-max IRL with linear reward classes in finite-horizon MDPs with Borel state/action spaces. Uses pseudo-self-concordance properties to derive statistical guarantees, and establishes equivalence relationships between MLE and Min-Max-IRL at population and empirical levels.

Result: Showed equivalence between MLE and Min-Max-IRL at population level (and empirical level under deterministic dynamics). Proved fast O(1/n) convergence rates for both trajectory-level KL divergence and squared parameter error in Hessian norm. Extended reward-identifiability results to general Borel spaces and derived new results on derivatives of soft-optimal value function.

Conclusion: The paper provides strong theoretical foundations for min-max IRL methods, demonstrating fast convergence rates and structural insights that work under realistic conditions (misspecification, no exploration assumptions), making these methods both theoretically sound and practically viable.

Abstract: We establish novel structural and statistical results for entropy-regularized min-max inverse reinforcement learning (Min-Max-IRL) with linear reward classes in finite-horizon MDPs with Borel state and action spaces. On the structural side, we show that maximum likelihood estimation (MLE) and Min-Max-IRL are equivalent at the population level, and at the empirical level under deterministic dynamics. On the statistical side, exploiting pseudo-self-concordance of the Min-Max-IRL loss, we prove that both the trajectory-level KL divergence and the squared parameter error in the Hessian norm decay at the fast rate $\mathcal{O}(n^{-1})$, where $n$ is the number of expert trajectories. Our guarantees apply under misspecification and require no exploration assumptions. We further extend reward-identifiability results to general Borel spaces and derive novel results on the derivatives of the soft-optimal value function with respect to reward parameters.

</details>


### [196] [An Amortized Efficiency Threshold for Comparing Neural and Heuristic Solvers in Combinatorial Optimization](https://arxiv.org/abs/2605.14624)
*Sohaib Afifi*

Main category: cs.LG

TL;DR: Neural combinatorial-optimization solvers can become more energy-efficient than CPU metaheuristics after sufficient deployment volume, measured by the Amortized Efficiency Threshold (AET).


<details>
  <summary>Details</summary>
Motivation: Challenge the common critique that neural solvers are less energy-efficient than CPU metaheuristics by showing that training cost (GPU) and runtime cost (CPU) need to be compared at a fixed deployment volume.

Method: Define Amortized Efficiency Threshold (AET) as deployment volume where neural solver breaks even with heuristic baseline in total energy/carbon under solution quality constraint. Analyze cumulative-energy ratio convergence with embodied-carbon accounting.

Result: For Multi-Task VRP with 20 customers across 19 variants, AET is ~158,000 instances with per-instance energy ratio of 0.41 (neural solver uses 41% of CPU metaheuristic energy after training).

Conclusion: Neural solvers can be net-efficient after sufficient deployment; framework provides measurement protocol for energy-efficiency analysis, leaving larger problem sizes for future work.

Abstract: A common critique of neural combinatorial-optimization solvers is that they are less energy-efficient than CPU metaheuristics, given the operational energy cost of training them on GPUs. This paper examines the inferential step from "training is expensive" to "neural solvers are net-inefficient", which is where the critique actually goes wrong. Training the network costs a large fixed amount of GPU energy; running the metaheuristic costs a small amount of CPU energy on every instance, repeated as long as the solver is deployed. The two are not commensurable until a deployment volume is fixed. We define the Amortized Efficiency Threshold (AET) as the deployment volume above which a neural solver breaks even with a heuristic baseline in total energy or carbon, under an explicit constraint on solution quality. We show that the cumulative-energy ratio between the two solvers tends to a constant strictly below one whenever the network wins per-instance, and that this limit does not depend on how the training cost was measured. An embodied-carbon term amortizes hardware fabrication symmetrically on both sides. We instantiate the framework on the Multi-Task VRP (MTVRP) environment at n=20 customers across 19 problem variants and five training seeds, with HGS via PyVRP as the heuristic baseline. The measured crossover sits near $1.58 \times 10^5$ deployed instances; the per-instance ratio is 0.41, reflecting the moderate size of the instances tested. The contribution is the framework, the open instrumentation, and the measurement protocol; structural convergence of the ratio at larger problem sizes is left to future empirical work.

</details>


### [197] [Action-Inspired Generative Models](https://arxiv.org/abs/2605.14631)
*Eshwar R. A.,Debnath Pal*

Main category: cs.LG

TL;DR: AGMs use a lightweight learned potential to score and weight bridge samples during training, selectively penalizing uninformative transport paths while maintaining inference efficiency.


<details>
  <summary>Details</summary>
Motivation: Existing bridge-matching methods treat all stochastic transitions uniformly, regardless of whether they lie on structurally coherent trajectories or degenerate ones, leading to suboptimal training.

Method: Introduces a dual-network framework with a lightweight scalar potential V_φ (~1.4% of main network parameters) that scores bridge samples online and modulates drift objective via importance weights with stop-gradient barrier to prevent adversarial feedback. No iterative half-bridge fitting or auxiliary SDE solvers needed.

Result: Consistent improvements in generation quality across fidelity and coverage metrics by selectively penalizing uninformative transport paths. At inference, V_φ is discarded, leaving standard Euler-Maruyama integration of EMA drift unchanged.

Conclusion: AGMs provide a plug-and-play enhancement to bridge-matching training that improves generation quality by focusing training on informative transport paths while maintaining inference efficiency and requiring minimal parameter overhead.

Abstract: We introduce Action-Inspired Generative Models (AGMs), a dual-network generative framework motivated by the observation that existing bridge-matching methods assign uniform regression weight to every stochastic transition in the transport landscape, regardless of whether a given bridge sample lies along a structurally coherent trajectory or a degenerate one. We address this by introducing a lightweight learned scalar potential $V_φ$ that scores bridge samples online and modulates the drift objective via importance weights derived through a stop-gradient barrier -- preventing adversarial feedback between the two networks whilst preserving $V_φ$'s guiding signal. Crucially, $V_φ$ comprises only $\sim$1.4% of the primary drift network's parameter count, adds no overhead to the inference graph, and requires no iterative half-bridge fitting or auxiliary stochastic differential equation (SDE) solvers: it is a plug-and-play enhancement to any bridge-matching training loop. At inference, $V_φ$ is discarded entirely, leaving standard Euler-Maruyama integration of the exponential moving average (EMA) drift. We demonstrate that selectively penalising uninformative transport paths through the learned potential yields consistent improvements in generation quality across fidelity and coverage metrics.

</details>


### [198] [DRL-STAF: A Deep Reinforcement Learning Framework for State-Aware Forecasting of Complex Multivariate Hidden Markov Processes](https://arxiv.org/abs/2605.14632)
*Manrui Jiang,Jingru Huang,Yong Chen,Chen Zhang*

Main category: cs.LG

TL;DR: DRL-STAF is a deep reinforcement learning framework that jointly predicts observations and estimates hidden states for multivariate hidden Markov processes, outperforming traditional HMMs and deep learning methods.


<details>
  <summary>Details</summary>
Motivation: Existing approaches have limitations: deep learning methods lack explicit state modeling and interpretability, while Hidden Markov Models struggle with complex nonlinear emissions, scalability, and state-space explosion in multivariate settings. There's a need for methods that combine predictive accuracy with interpretable state modeling for complex multivariate hidden Markov processes.

Method: DRL-STAF uses deep neural networks to model complex nonlinear emissions and employs reinforcement learning to estimate discrete hidden states. This reduces reliance on predefined transition structures and enables flexible adaptation to diverse temporal dynamics while mitigating state-space explosion problems typical of multivariate HMMs.

Result: Extensive experiments show DRL-STAF outperforms HMM variants, standalone deep learning models, and existing DL-HMM hybrids in most cases. The framework also provides reliable hidden-state estimates along with strong predictive performance.

Conclusion: DRL-STAF successfully bridges the gap between interpretable state modeling and complex nonlinear modeling by combining deep learning with reinforcement learning for state estimation, offering a superior solution for forecasting multivariate hidden Markov processes with both accuracy and interpretability.

Abstract: Forecasting multivariate hidden Markov processes is challenging due to nonlinear and nonstationary observations, latent state transitions, and cross-sequence dependencies. While deep learning methods achieve strong predictive accuracy, they typically lack explicit state modeling, whereas Hidden Markov Models (HMMs) provide interpretable latent states but struggle with complex nonlinear emissions and scalability. To address these limitations, we propose DRL-STAF, a Deep Reinforcement Learning based STate-Aware Forecasting framework that jointly predicts next-step observations and estimates the corresponding hidden states for complex multivariate hidden Markov processes. Specifically, DRL-STAF models complex nonlinear emissions using deep neural networks and estimates discrete hidden states using reinforcement learning, reducing the reliance on predefined transition structures and enabling flexible adaptation to diverse temporal dynamics. In particular, DRL-STAF mitigates the state-space explosion encountered by typical multivariate HMM-based methods. Extensive experiments demonstrate that DRL-STAF outperforms HMM variants, standalone deep learning models, and existing DL-HMM hybrids in most cases, while also providing reliable hidden-state estimates.

</details>


### [199] [Unbiased and Second-Order-Free Training for High-Dimensional PDEs](https://arxiv.org/abs/2605.14643)
*Jaemin Seo,Surin Lee,Jae Yong Lee*

Main category: cs.LG

TL;DR: BSDE-based deep learning methods for high-dimensional PDEs are computationally efficient but suffer from bias in standard Euler-Maruyama discretization; this work proposes an unbiased, second-order-free training framework that maintains BSDE's computational advantages.


<details>
  <summary>Details</summary>
Motivation: BSDE-based deep learning methods are competitive alternatives to PINNs for high-dimensional PDEs, offering second-order-free training without Hessian evaluations. However, the commonly used Euler-Maruyama discretization introduces intrinsic bias in BSDE training losses, while existing high-order bias-elimination schemes reintroduce computationally expensive second-order spatial derivatives.

Method: The authors provide a principled analysis of the EM-induced loss bias and propose a novel training framework that is both unbiased and second-order-free, preserving the computational efficiency advantages of BSDE methods without requiring expensive Hessian evaluations.

Result: The proposed method achieves unbiased training while maintaining computational efficiency comparable to standard BSDE approaches. The framework eliminates the bias introduced by EM discretization without reintroducing the computational overhead of second-order derivative calculations.

Conclusion: This work successfully addresses the bias issue in BSDE-based deep learning for high-dimensional PDEs by developing an unbiased, computationally efficient framework that combines the advantages of BSDE methods (curse-of-dimensionality avoidance, second-order-free training) with unbiasedness, making it a practical and theoretically sound alternative to existing approaches.

Abstract: Deep learning methods based on backward stochastic differential equations (BSDEs) have emerged as competitive alternatives to physics-informed neural networks (PINNs) for solving high-dimensional partial differential equations (PDEs). By leveraging probabilistic representations, BSDE approaches can avoid the curse of dimensionality and often admit second-order-free training objectives that do not require explicit Hessian evaluations. It has recently been established that the commonly used Euler-Maruyama (EM) time discretization induces an intrinsic bias in BSDE training losses. While high-order schemes such as Heun can fully eliminate this bias, such schemes re-introduce second-order spatial derivatives and incur substantial computational overhead. In this work, we provide a principled analysis of EM-induced loss bias and propose an unbiased, second-order-free training framework that preserves the computational advantages of BSDE methods. Our code is available at https://github.com/seojaemin22/Un-EM-BSDE.

</details>


### [200] [Slower Generalization, Faster Memorization: A Sweet Spot in Algorithmic Learning](https://arxiv.org/abs/2605.14659)
*Shin So,Kyelim Lee,Albert No*

Main category: cs.LG

TL;DR: Transformers learning the Needleman-Wunsch algorithm show fastest validation convergence at intermediate dataset sizes, not largest ones, revealing a dataset-size sweet spot for structured-output tasks.


<details>
  <summary>Details</summary>
Motivation: The paper challenges the common intuition in grokking that once training data is sufficient to identify the underlying rule, additional data should accelerate validation convergence. The authors aim to show this intuition can fail in structured-output tasks.

Method: The authors use a controlled structured-output task: Needleman-Wunsch (NW) matrix generation with small Transformers. They study how dataset size affects convergence speed, comparing validation exact-match accuracy across different dataset sizes. They use a multiplication task as a baseline for comparison.

Result: In NW matrix generation, Transformers reach high validation accuracy fastest at an intermediate dataset size (a sweet spot), not at the largest size. Past this point, generalization requires more gradient updates. Conversely, larger datasets require fewer updates to reach high training accuracy in early stages. The multiplication baseline doesn't show this post-threshold slowdown.

Conclusion: The study separates critical data size for generalization onset from optimal dataset size for update-based convergence. It identifies structured-output tasks where learning the rule and completing exact-fitting can diverge, challenging simplistic grokking intuitions.

Abstract: Critical-data-size accounts of grokking suggest a natural post-threshold intuition: once training data is sufficient to identify the underlying rule, additional data should accelerate validation convergence. We show that this intuition can fail in a controlled structured-output task. In Needleman--Wunsch (NW) matrix generation, small Transformers reach high validation exact-match accuracy fastest at an intermediate dataset size, not at the largest one. Past this dataset-size sweet spot, generalization remains achievable but requires more gradient updates. Conversely, in the regime where partial validation competence first appears, larger datasets can require fewer updates to reach high training accuracy, suggesting that emerging rule structure can accelerate fitting beyond example-wise memorization. A multiplication baseline does not show the same post-threshold slowdown. These results separate the critical data size for the onset of generalization from the dataset size that optimizes update-based convergence, and identify structured-output tasks where learning the rule and completing exact-fitting can diverge.

</details>


### [201] [AQKA: Active Quantum Kernel Acquisition Under a Shot Budget](https://arxiv.org/abs/2605.14672)
*Jian Xu,Chao Li,Delu Zeng,John Paisley,Qibin Zhao*

Main category: cs.LG

TL;DR: AQKA introduces adaptive shot allocation for quantum kernel learning that dominates existing methods in budget-limited regimes, providing +8-32% improvements on quantum hardware.


<details>
  <summary>Details</summary>
Motivation: Current quantum kernel estimation requires Θ(N²S) measurement shots, which is the major bottleneck for near-term quantum hardware deployment. Existing methods use uniform shot allocation within chosen subsets, ignoring how much each kernel entry contributes to classifier performance.

Method: Proposes AQKA (Adaptive Quantum Kernel Allocation) with two main contributions: 1) A complete regime decomposition for shot-budgeted quantum kernel learning, showing when each allocator wins; 2) A closed-form pair-level acquisition theory providing optimal shot allocation formulas for KRR and SVM, with explicit gradient expressions and regret bounds.

Result: AQKA dominates in budget-limited regimes (B ≲ 16 n_pairs), with improvements growing from +8 to +25 percentage points as N scales from 225 to 1000, reaching +26-32 pts on IBM Pittsburgh hardware. On IBM hardware experiments, shows +17.0 ± 4.8 pts advantage at N=20 (3.5σ significance) and maintains advantage at N=30.

Conclusion: Adaptive shot allocation significantly improves quantum kernel learning efficiency by intelligently distributing measurement shots based on entry importance to classifier performance, outperforming uniform allocation methods across various regimes and demonstrating practical advantages on real quantum hardware.

Abstract: Estimating an $N \times N$ quantum kernel from circuit fidelities requires $Θ(N^2 S)$ measurement shots, the dominant bottleneck for deployment on near-term hardware. Existing budget-saving methods (Nyström-QKE, ShoFaR, kernel-target alignment) sub-sample \emph{which} entries to measure but allocate shots \emph{uniformly} within their chosen subset, ignoring how much each entry drives the downstream classifier. We close this gap with two contributions. \textbf{First, a complete regime decomposition} for shot-budgeted quantum kernel learning: a principled menu of when each allocator wins. Our method, \emph{AQKA}, dominates the budget-limited regime ($B \lesssim 16 n_{\mathrm{pairs}}$) on sparse-sensitivity KRR, with the gap \emph{growing} from $+8$ to $+25$ pts over uniform as $N$ scales $225{\to}1000$ and reaching $+26$--$32$ pts on an \texttt{ibm\_pittsburgh} (156-qubit Heron) hardware kernel; Nyström-QKE wins at saturating budgets on planted-sparse via low-rank reconstruction; ShoFaR is competitive only at extreme low budgets. \textbf{Second, a closed-form pair-level acquisition theory}: $s_{ij}^{\star} \propto |g_{ij}|\sqrt{K_{ij}(1-K_{ij})}$ with explicit gradient $g_{ij}$ for KRR (Lemma~1, $|β_iα_j+β_jα_i|\sqrt{K_{ij}(1-K_{ij})}$) and SVM via the envelope theorem ($|η_i^*η_j^*|\sqrt{K_{ij}(1-K_{ij})}$); a \emph{corrected} sparsity-aware Cauchy--Schwarz rate $ρ\le 2m/N$ matching empirics (vs.\ the naive $m^2/N^2$); an explicit-constant plug-in regret bound (Theorem~2); and a tighter SVM ceiling $ρ^{\mathrm{SVM}} \le m_{\mathrm{sv}}^2/N^2$. We close with the first multi-seed live online adaptive shot allocation on quantum hardware: $+17.0 \pm 4.8$ pts at $N{=}20$ on \texttt{ibm\_aachen} ($3.5σ$, 5 seeds), with the advantage holding at $N{=}30$ at higher budget on \texttt{ibm\_berlin} ($+14.0 \pm 8.5$ pts, 5 seeds).

</details>


### [202] [Spontaneous symmetry breaking and Goldstone modes for deep information propagation](https://arxiv.org/abs/2605.14685)
*Nabil Iqbal,T. Anderson Keller,Yue Song,Takeru Miyato,Max Welling*

Main category: cs.LG

TL;DR: Deep neural networks with continuous symmetry in internal layers develop Goldstone-like modes that enable coherent information propagation, improving trainability and performance in both feedforward and recurrent architectures.


<details>
  <summary>Details</summary>
Motivation: Inspired by physics where spontaneously broken continuous symmetries give rise to Goldstone modes that enable coherent information propagation over long distances and times. The authors want to explore whether similar phenomena occur in deep neural networks with internal layer symmetries.

Method: Study deep neural networks whose internal layers are equivariant under a continuous symmetry. Use both analytical and empirical approaches to demonstrate the existence of Goldstone-like degrees of freedom and their effects on information propagation.

Result: Goldstone-like degrees of freedom enable coherent signal propagation across depth and recurrent iterations, providing stable information flow without needing architectural stabilizers like residual connections or normalization. In feedforward networks, this improves trainability and representational diversity. In recurrent settings, it enables long-term memory by propagating information over recurrent iterations, improving RNN and GRU performance on long-sequence tasks.

Conclusion: Deep neural networks with continuous symmetry in their internal layers exhibit Goldstone-like phenomena analogous to physical systems, providing a natural mechanism for stable information propagation that enhances performance in both feedforward and recurrent architectures without relying on conventional stabilizers.

Abstract: In physical systems, whenever a continuous symmetry is spontaneously broken, the system possesses excitations called Goldstone modes, which allow coherent information propagation over long distances and times. In this work, we study deep neural networks whose internal layers are equivariant under a continuous symmetry and may therefore support analogous Goldstone-like degrees of freedom. We demonstrate, both analytically and empirically, that these degrees of freedom enable coherent signal propagation across depth and recurrent iterations, providing a mechanism for stable information flow without relying on architectural stabilizers such as residual connections or normalization. In feedforward networks, this results in improved trainability and representational diversity across layers. In recurrent settings, we demonstrate the same mechanism is valuable for long-term memory by propagating information over recurrent iterations, thereby improving performance of RNNs and GRUs on long-sequence modeling tasks.

</details>


### [203] [ReMIA: a Powerful and Efficient Alternative to Membership Inference Attacks against Synthetic Data Generators](https://arxiv.org/abs/2605.14686)
*Davide Scassola,Andrea Coser,Sebastiano Saccani*

Main category: cs.LG

TL;DR: ReMIA is a practical privacy metric for synthetic data that requires only two SDG training runs and measures if a classifier can identify which source dataset a record came from, matching state-of-the-art MIA sensitivity while being much more practical.


<details>
  <summary>Details</summary>
Motivation: Synthetic data generators are vulnerable to membership inference attacks, but current state-of-the-art MIAs are impractical due to requiring hundreds of SDG training runs and large auxiliary datasets. Fast proxy metrics like DCR have limited sensitivity.

Method: ReMIA generates two synthetic datasets from two source datasets and measures whether a classifier can identify which source a record came from, requiring only two SDG training runs and additional data no larger than the original training set.

Result: Experiments across multiple tabular datasets and SDGs show ReMIA has sensitivity comparable to state-of-the-art MIAs while being substantially more practical, and reveals that SDGs can achieve privacy-utility trade-offs unmatched by traditional noise-based methods.

Conclusion: ReMIA provides a practical and effective privacy metric for synthetic data that bridges the gap between resource-intensive state-of-the-art MIAs and limited-sensitivity proxy metrics, enabling better evaluation of privacy risks in synthetic data sharing.

Abstract: Tabular data sharing under privacy constraints is increasingly important for research and collaboration. Synthetic data generators (SDGs) are a promising solution, but synthetic data remains vulnerable to attacks, such as membership inference attacks (MIAs), which aim to determine whether a specific record was part of the training data. State-of-the-art MIAs are powerful but impractical: they rely on shadow modeling, requiring hundreds of SDG training runs, and need auxiliary data several times larger than the original training set. Fast proxy metrics like distance to closest record (DCR) are efficient but have limited sensitivity to MIA risk. We introduce ReMIA (Relative Membership Inference Attack), a practical privacy metric that requires only two SDG training runs and additional data no larger than the original training set. Rather than predicting whether a record was in the training set, ReMIA generates two synthetic datasets from two source datasets and measures whether a classifier can identify which source a record came from. Experiments across multiple tabular datasets and SDGs show that ReMIA has a sensitivity comparable to state-of-the-art MIAs while being substantially more practical. We further observe that SDGs can achieve privacy-utility trade-offs that traditional noise-based anonymization methods do not match. Code is available at https://github.com/aindo-com/remia.

</details>


### [204] [The Rate-Distortion-Polysemanticity Tradeoff in SAEs](https://arxiv.org/abs/2605.14694)
*Tommaso Mencattini,Francesco Montagna,Francesco Locatello*

Main category: cs.LG

TL;DR: SAEs face a tradeoff between accuracy, efficiency, and interpretability, where forcing monosemantic representations increases rate and distortion, and polysemanticity is fundamentally determined by data distribution.


<details>
  <summary>Details</summary>
Motivation: Sparse Autoencoders often fail to learn monosemantic (highly interpretable) representations while maintaining good reconstruction accuracy and feature efficiency, limiting their usefulness for mechanistic interpretability. There's a need to understand the fundamental tradeoff between faithful reconstruction, efficiency, and interpretability.

Method: 1) Formalized the Rate-Distortion-Polysemanticity tradeoff in SAEs; 2) Under toy-modeling assumptions, theoretically and empirically showed monosemantic constraints increase rate and distortion; 3) Assuming generative models, demonstrated polysemanticity is determined by training data distribution and feature co-occurrence probabilities; 4) Extended analysis to real-world settings by deriving necessary conditions for polysemanticity measures and benchmarking existing proxy metrics on SAEs trained on LLMs.

Result: 1) Proved that restricting SAEs to be monosemantic necessarily increases both rate (feature efficiency) and distortion (reconstruction error); 2) Showed optimal SAE polysemanticity is determined by data distribution, particularly feature co-occurrence probabilities; 3) Established necessary conditions for polysemanticity measures when data-generating process is unknown; 4) Benchmarking revealed insights about existing proxy metrics for polysemanticity assessment.

Conclusion: Polysemanticity is fundamentally a data problem rather than solely an architectural or optimization issue. When addressing polysemanticity at architectural and optimization levels, the underlying data distribution and feature co-occurrence patterns must be accounted for, as they determine the degree of polysemanticity in optimal SAEs.

Abstract: Sparse Autoencoders (SAEs) that can accurately reconstruct their input (minimizing distortion) by making efficient use of few features (minimizing the rate) often fail to learn monosemantic representations (highly interpretable), limiting their usefulness for mechanistic interpretability. In this paper, we characterise this tension in learning faithful, efficient, and interpretable explanations, introducing the Rate-Distortion-Polysemanticity tradeoff in SAEs. Under toy-modeling assumptions, we theoretically and empirically show that restricting the SAE to be monosemantic necessarily comes with an increase in rate and distortion. Assuming a generative model behind the input observations, we further demonstrate that the degree of polysemanticity of optimal SAEs is determined by the training data distribution, especially by the probability of features to co-occur. Finally, we extend the analysis to real-world settings by deriving necessary conditions that a polysemanticity measure should satisfy when the data-generating process is unknown, and we benchmark existing proxy metrics on SAEs trained on Large Language Models. Taken together, our findings show that polysemanticity is a data problem that should be accounted for when addressing it at the architectural and optimization level.

</details>


### [205] [NeuroAtlas: Benchmarking Foundation Models for Clinical EEG and Brain-Computer Interfaces](https://arxiv.org/abs/2605.14698)
*Konstantinos Kontras,Trui Osselaer,Stylianos G. Mouslech,Angeliki-Ilektra Karaiskou,Guido Gagliardi,Thomas Strypsteen,Mohammad Hossein Badiei,Anku Rani,Maarten Vanmarcke,Miguel Bhagubai,Chanakya Ekbote,Jaedong Hwang,Christos Chatzichristos,Paul Pu Liang,Maarten De Vos*

Main category: cs.LG

TL;DR: EEG foundation models don't consistently outperform generic time-series models, standard ML metrics are insufficient for clinical EEG evaluation, and current models fail to deliver unified out-of-the-box EEG solutions.


<details>
  <summary>Details</summary>
Motivation: To systematically evaluate whether foundation models (FMs) for EEG actually provide promised unified representations that generalize across tasks, given inconsistent evaluations in existing literature with varying datasets, preprocessing, and metrics that obscure clinical relevance.

Method: Created NeuroAtlas benchmark with 42 EEG datasets (260k hours) covering clinical EEG (epilepsy, sleep, brain age) and BCI; evaluated both EEG-specific FMs and generic time-series FMs using supervised baselines and bespoke clinical evaluation metrics.

Result: 1) EEG-FMs don't consistently outperform time-series FMs without EEG-specific architectures/pretraining. 2) Standard ML metrics insufficient for clinical utility - need event-level decision quality, hypnogram features, brain-age gap measures. 3) Model rankings/performance vary substantially within domains. Current models perform largely on par with only narrow advantages for a few.

Conclusion: Current pretrained EEG models don't yet deliver on the promise of out-of-the-box unified EEG solutions, performing largely equivalently with only narrow advantages. NeuroAtlas exposes this gap and provides the resources needed for developing next-generation unified EEG FMs.

Abstract: Foundation models (FMs) promise to extract unified representations that generalize across downstream tasks. They have emerged across fields, including electroencephalography (EEG), but it is less clear how effective they are in this particular field. Published evaluations differ in datasets, in the EEG-specific preprocessing that might influence reported results, and in the reported metrics, frequently obscuring the clinical relevance in EEG. We introduce NeuroAtlas, the largest EEG benchmark to date: 42 datasets and 260k hours covering clinical EEG (epilepsy, sleep medicine, brain age estimation) and brain-computer interfaces, and include multiple datasets per task along with bespoke clinical evaluation metrics. Besides evaluating EEG-FMs with respect to supervised baselines, we present results from generic time-series FMs. We report three findings. First, EEG-specific FMs do not consistently outperform time-series FMs, which have neither EEG-focused architectures nor been pretrained on EEG. Second, standard machine learning metrics are insufficient to assess clinical utility: thus, we thoroughly evaluate more appropriate measures such as the quality of event-level decision-making, hypnogram-derived features, and the brain-age gap in the domains of epilepsy, sleep, and brain age, respectively. Third, model rankings and performance can vary substantially within domains. We conclude that pretrained models perform largely on par, with only narrow advantages for a few, and that current models do not yet deliver on the promise of an out-of-the-box unified EEG model. NeuroAtlas exposes this gap and provides the datasets and metrics for the next generation of unified EEG FMs.

</details>


### [206] [TAPIOCA: Why Task- Aware Pruning Improves OOD model Capability](https://arxiv.org/abs/2605.14738)
*Krish Sharma,Omar Naim,Soumadeep Saha,Nicholas Asher*

Main category: cs.LG

TL;DR: Task-aware layer pruning improves OOD performance by removing layers that distort the task-adapted geometry, not for in-distribution data.


<details>
  <summary>Details</summary>
Motivation: The paper aims to understand when and why task-aware layer pruning (as shown by TALE) improves model performance on specific tasks, particularly investigating the conditions under which such improvements occur.

Method: The researchers use controlled experiments across polynomial regression tasks and large language models. They analyze layerwise norm and pairwise-distance profiles on ID and OOD inputs, provide causal evidence through controlled distribution shifts and residual-scaling interventions, and test across different model scales.

Result: Task-aware pruning yields no benefit on in-distribution data but consistently improves out-of-distribution accuracy. OOD inputs induce distorted layerwise representation profiles, and pruning identifies layers that create or amplify this distortion, realigning OOD representations with the task-adapted geometry.

Conclusion: Task-aware pruning works by removing layers that distort the task-adapted geometry on OOD inputs, providing a geometric explanation for why it improves OOD performance without affecting ID performance.

Abstract: Recent work has promoted task-aware layer pruning as a way to improve model performance on particular tasks, as shown by TALE. In this paper, we investigate when such improvements occur and why. We show first that, across controlled polynomial regression tasks and large language models, such pruning yields no benefit on in-distribution (ID) data but consistently improves out-of-distribution (OOD) accuracy. We further show empirically that OOD inputs induce layerwise norm and pairwise-distance profiles that deviate from the corresponding ID profiles. This leads to a geometric explanation of task-aware pruning: each task induces a task-adapted geometry, characterized empirically by the representation profiles observed on ID inputs. OOD inputs can introduce a distorted version of the task-adapted geometry. Task-aware pruning identifies layers that create or amplify this distortion; by removing them, it shifts OOD representational norms and pairwise distances toward those observed on the adapted distribution. This realigns OOD inputs with the model's task-adapted geometry and improves performance. We provide causal evidence through controlled distribution shifts and residual-scaling interventions, and demonstrate consistent behavior across model scales.

</details>


### [207] [Selective Safety Steering via Value-Filtered Decoding](https://arxiv.org/abs/2605.14746)
*Bat-Sheva Einbinder,Hen Davidov,Yee Whye Teh,Yarin Gal,Yaniv Romano*

Main category: cs.LG

TL;DR: A new decoding-time safety filtering method called value-filtered decoding that reduces unnecessary interventions while improving unsafe responses, with explicit bounds on false interventions controlled by a threshold parameter.


<details>
  <summary>Details</summary>
Motivation: While LLMs are trained for human alignment, they can still violate safety constraints. Existing decoding-time steering methods often unnecessarily intervene on safe generations, which can distort desirable base model properties like helpfulness, fluency, style, and coherence.

Method: Proposes value-filtered decoding that filters tokens using a value-based safety criterion. The method provides an explicit bound on the probability of false interventions, controlled by a single threshold hyperparameter that allows trading off intervention rates for safety improvements.

Result: Across multiple datasets and experiments, the value-filtered decoding method outperforms existing baselines, achieving better trade-offs between safety, helpfulness, and similarity to the base model.

Conclusion: The proposed approach effectively addresses the problem of unnecessary interventions in decoding-time safety steering while maintaining model quality, providing practitioners with controllable trade-offs between safety and model properties.

Abstract: While large language models (LLMs) are trained to align with human values, their generations may still violate safety constraints. A growing line of work addresses this problem by modifying the model's sampling policy at decoding time using a safety reward. However, existing decoding-time steering methods often intervene unnecessarily, modifying generations that would have been safe under the base model. Such unnecessary interventions are undesirable, as they can distort key properties of the base model such as helpfulness, fluency, style, and coherence. We propose a new test-time steering method designed to reduce such unnecessary interventions while improving the safety of unsafe responses. Our approach filters tokens using a value-based safety criterion and provides an explicit bound on the probability of false interventions. A single threshold hyperparameter controls this bound, allowing practitioners to trade off higher rates of unnecessary intervention for better output safety. Across multiple datasets and experiments, we show that our value-filtered decoding method outperforms existing baselines, achieving better trade-offs between safety, helpfulness, and similarity to the base model.

</details>


### [208] [Cognitive-Uncertainty Guided Knowledge Distillation for Accurate Classification of Student Misconceptions](https://arxiv.org/abs/2605.14752)
*Qirui Liu,Hao Chen,Weijie Shi,Jiajie Xu,Jia Zhu*

Main category: cs.LG

TL;DR: A two-stage knowledge distillation framework that identifies high-value samples via cognitive uncertainty to address student misconception identification challenges like data scarcity, fuzzy boundaries, and deployment constraints.


<details>
  <summary>Details</summary>
Motivation: Three key challenges in student misconception identification: (1) data scarcity with long-tail distribution and difficulty synthesizing authentic student reasoning, (2) fuzzy boundaries between error categories with high annotation noise, and (3) deployment paradox where large models have pretraining biases and can't deploy on edge, while small models overfit to noise.

Method: A two-stage knowledge distillation framework: First stage performs standard distillation to transfer task capabilities. Second stage introduces a dual-layer marginal selection mechanism based on cognitive uncertainty, identifying four types of critical samples using teacher model uncertainty and confidence differences. Difficulty-adaptive mechanisms balance hard/soft label contributions for different data subsets, enabling student models to inherit inter-class relationships while distinguishing ambiguous error types.

Result: Achieved MAP@3 of 0.9585 (+17.8% improvement) on MAP-Charting dataset using only 10.30% of filtered samples. With a 4B parameter model, attained 84.38% accuracy on cross-topic tests of middle school algebra misconception benchmarks, significantly outperforming state-of-the-art LLM (67.73%) and standard fine-tuned 72B models (81.25%).

Conclusion: The proposed knowledge distillation framework effectively addresses deployment constraints and data scarcity by identifying high-value samples through cognitive uncertainty analysis, enabling small models to outperform much larger models on student misconception identification tasks with minimal data requirements.

Abstract: Accurately identifying student misconceptions is crucial for personalized education but faces three challenges: (1) data scarcity with long-tail distribution, where authentic student reasoning is difficult to synthesize; (2) fuzzy boundaries between error categories with high annotation noise; (3) deployment parado-large models overlook unconventional approaches due to pretraining bias and cannot be deployed on edge, while small models overfit to noise. Unlike traditional methods that increase diversity through large-scale data synthesis, we propose a two-stage knowledge distillation framework that mines high-value samples from existing data. The first stage performs standard distillation to transfer task capabilities. The second stage introduces a dual-layer marginal selection mechanism based on cognitive uncertainty, identifying four types of critical samples based on teacher model uncertainty and confidence differences. For different data subsets, we design difficulty-adaptive mechanism to balance hard/soft label contributions, enabling student models to inherit inter-class relationships from teacher soft labels while distinguishing ambiguous error types. Experiments show that with augmented training on only 10.30% of filtered samples, we achieve MAP@3 of 0.9585 (+17.8%) on the MAP-Charting dataset, and using only a 4B parameter model, we attain 84.38% accuracy on cross-topic tests of middle school algebra misconception benchmarks, significantly outperforming sota LLM (67.73%) and standard fine-tuned 72B models (81.25%). Our code is available at https://github.com/RoschildRui/acl2026_map.

</details>


### [209] [Crys-JEPA: Accelerating Crystal Discovery via Embedding Screening and Generative Refinement](https://arxiv.org/abs/2605.14759)
*Nian Liu,Nikita Kazeev,Stephen Gregory Dale,Artem Maevskiy,Yuwei Zeng,Ryoji Kubo,Pengru Huang,Thomas Laurent,Yann LeCun,Kostya S. Novoselov,Xavier Bresson*

Main category: cs.LG

TL;DR: Crys-JEPA: A joint embedding predictive architecture that learns energy-aware latent spaces for crystals, enabling discovery of both stable and novel materials through a screening-and-refinement pipeline that escapes the stability-novelty trade-off inherent in existing generative models.


<details>
  <summary>Details</summary>
Motivation: Existing crystal generative models are trapped in a stability-novelty trade-off - they maximize likelihood of observed crystals but fail to discover materials that are both novel and stable, limiting their usefulness for real discovery where both criteria matter.

Method: Introduces Crys-JEPA (Joint Embedding Predictive Architecture) that learns an energy-aware latent space preserving formation-energy differences, allowing stability assessment via embedding-based comparisons. Builds a screening-and-refinement pipeline that identifies promising crystals and reintroduces them to refine the model.

Result: Achieves improvements over baselines up to 81.4% on MP-20 dataset and 82.6% on Alex-MP-20 dataset on the V.S.U.N metric, demonstrating substantial advancement in discovering stable and novel crystals.

Conclusion: Crys-JEPA successfully escapes the stability-novelty trade-off by learning an energy-aware representation space that enables efficient stability assessment without expensive energy evaluations, paving the way for more effective discovery of novel, stable materials.

Abstract: De novo crystal generation seeks to discover materials that are not merely realistic, but also stable and novel. However, most existing generative models are trained to maximize the likelihood of observed crystals, which encourages samples to stay close to known materials yet not necessarily align with the criteria that matter in discovery. Through an empirical investigation, we show that current crystal generative models are caught in a pronounced stability--novelty trade-off: moving toward the observed distribution preserves stability but limits novelty, whereas moving away from it quickly destroys stability. This suggests that the useful region for discovering crystals that are both stable and novel is extremely narrow. To escape the trade-off, we introduce Crys-JEPA, a joint embedding predictive architecture for crystals that learns an energy-aware latent space preserving formation-energy differences. In this space, stability assessment can be reformulated as an embedding-based comparison against accessible training crystals, reducing the reliance on expensive energy evaluation and task-specific external references. Building on Crys-JEPA, we further develop a screening-and-refinement pipeline that identifies promising generated crystals and reintroduces them to refine the generative model. On MP-20 and Alex-MP-20 datasets, we achieve improvements over baselines up to 81.4% and 82.6% on V.S.U.N metric, respectively.

</details>


### [210] [Compositional Sparsity as an Inductive Bias for Neural Architecture Design](https://arxiv.org/abs/2605.14764)
*Hongyu Lin,Antonio Briola,Yuanrong Wang,Tomaso Aste*

Main category: cs.LG

TL;DR: Homological Neural Networks (HNNs) use information filtering to extract sparse dependency structures and map them into fixed-wiring neural graphs, achieving high-dimensional learning with compositional sparsity.


<details>
  <summary>Details</summary>
Motivation: To identify the structural priors that enable DNNs to overcome the curse of dimensionality by investigating whether compositional sparsity (target functions decomposing into low-dimensional constituents) is the key mechanism for effective high-dimensional learning.

Method: Combine Information Filtering Networks (IFNs) to extract sparse dependency structures via constrained information maximization with Homological Neural Networks (HNNs) that map the inferred topological structure into fixed-wiring sparse neural graphs, creating an interpretable hierarchical composition pipeline.

Result: HNNs are orders of magnitude sparser than standard DNNs, require minimal hyperparameter tuning, recover underlying compositional structure in synthetic tasks, remain stable in high-dimensional regimes where dense alternatives degrade, and consistently match or outperform dense baselines on real-world datasets with fewer parameters, lower variance, and reduced hyperparameter sensitivity.

Conclusion: The proposed HNN framework successfully demonstrates that compositional sparsity is a key structural prior for overcoming the curse of dimensionality in deep learning, providing sparse, interpretable neural networks that maintain performance while being more stable and parameter-efficient than dense alternatives.

Abstract: Identifying the structural priors that enable Deep Neural Networks (DNNs) to overcome the curse of dimensionality is a fundamental challenge in machine learning theory. Existing literature suggests that effective high-dimensional learning is driven by compositional sparsity, where target functions decompose into constituents supported on low-dimensional variable subsets. To investigate this hypothesis, we combine Information Filtering Networks (IFNs), which extract sparse dependency structures via constrained information maximisation, with Homological Neural Networks (HNNs), which map the inferred topology into fixed-wiring sparse neural graphs. We formalise the design principles underlying this construction and present an interpretable pipeline in which abstraction emerges through hierarchical composition. HNNs are orders of magnitude sparser than standard DNNs and require only minimal hyperparameter tuning. On synthetic tasks with known sparse hierarchies, HNNs recover the underlying compositional structure and remain stable in regimes where dense alternatives degrade as dimensionality increases. Across a broad suite of real-world datasets, HNNs consistently match or outperform dense baselines while using far fewer parameters, exhibiting lower variance and showing reduced sensitivity to hyperparameters.

</details>


### [211] [Composable Crystals: Controllable Materials Discovery via Concept Learning](https://arxiv.org/abs/2605.14769)
*Nian Liu,Yuwei Zeng,Ryoji Kubo,Nikita Kazeev,Stephen Gregory Dale,Artem Maevskiy,Pengru Huang,Thomas Laurent,Kostya S. Novoselov,Xavier Bresson*

Main category: cs.LG

TL;DR: A compositional framework for crystal generation using learned reusable concepts for controllable exploration beyond training data.


<details>
  <summary>Details</summary>
Motivation: Existing methods rely on black-box stochastic sampling with limited control over generating structures beyond observed distributions. Need for interpretable, controllable crystal generation that can explore novel materials.

Method: Train vector-quantized variational autoencoder to discover shared reusable crystal concepts as building blocks. Introduce composition generator to efficiently recombine concepts, iteratively refine it using high-quality generated samples, then use concept compositions to condition downstream crystal generation.

Result: On MP-20 and Alex-MP-20 datasets, compositing concepts separately increased base model performance by 53.2% and 51.7% on V.S.U.N metric (Validity, Stability, Uniqueness, Novelty), with particular gains in novelty.

Conclusion: Concept-based compositional framework enables controllable exploration of novel crystals beyond training distribution, providing interpretable building blocks and improved generation quality compared to unconstrained random sampling.

Abstract: De novo crystal generation, a central task in materials discovery, aims to generate crystals that are simultaneously valid, stable, unique, and novel. Existing methods mainly rely on black-box stochastic sampling, providing limited control over how generated structures move beyond the observed distribution. In this paper, we introduce a concept-based compositional framework for crystal generation. We train a vector-quantized variational autoencoder to automatically discover a shared set of reusable crystal concepts, which serve as building blocks for guided generation. These learned concepts naturally exhibit interpretability from both local atomic environments and global symmetry patterns, and generalize to crystals from different distributions. By recombining such concepts, our framework enables controllable exploration of novel crystals beyond the training distribution, rather than relying solely on unconstrained random sampling. To further improve composition efficiency, we introduce a composition generator and iteratively refine it using high-quality samples generated by the model itself. The resulting concept compositions are then used to condition downstream crystal generation. Numerical experiments on MP-20 and Alex-MP-20 show that compositing concepts separately increase base model up to 53.2% and 51.7% on V.S.U.N metric, with particular gains in novelty.

</details>


### [212] [Beyond What to Select: A Plug-and-play Oscillatory Data-Volume Scheduling for Efficient Model Training](https://arxiv.org/abs/2605.14773)
*Suorong Yang,Hanqi Zhu,Hai Gan,Fangjian Su,Guang Li,Furao Shen,Soujanya Poria*

Main category: cs.LG

TL;DR: PODS is a Plug-and-play Oscillatory Data-volume Scheduling framework that dynamically adjusts data selection ratios during training to optimize the regularization-coverage trade-off, improving training efficiency without sacrificing model performance.


<details>
  <summary>Details</summary>
Motivation: Existing data selection methods focus only on what data to select (sample importance criteria) while keeping selection ratios static throughout training. This misses the opportunity to leverage the implicit regularization effect induced by data selection, which varies with selection ratios - lower ratios amplify regularization while higher ratios preserve data coverage.

Method: PODS dynamically schedules how much data to select during training by oscillating between low-ratio regularization phases (for stronger regularization) and high-ratio recovery phases (for maintaining optimization stability). It's a lightweight, ratio-level module that's compatible with existing selection methods, working independently of sample-scoring metrics.

Result: Experiments show PODS consistently improves efficiency-generalization trade-offs: reduces ImageNet-1k training cost by 50% with improved accuracy, accelerates LLM instruction tuning by over 2x without performance degradation, and works across various datasets, architectures, and tasks.

Conclusion: Dynamically scheduling data selection volume (ratio) is crucial for optimizing training efficiency and generalization. PODS demonstrates that focusing on "how much to select" rather than just "what to select" enables better exploitation of selection-induced regularization while maintaining optimization fidelity.

Abstract: Data selection accelerates training by identifying representative training data while preserving model performance. However, existing methods mainly focus on designing sample-importance criteria, i.e., deciding what to select, while typically fixing the selected data volume as the target ratio throughout training. Thus, they are often dynamic in sample identity but static in data volume. In this work, we revisit data selection from an optimization perspective and show that selected-data training induces an implicit regularization effect modulated by the instantaneous selection ratio. This reveals a key trade-off: lower ratios amplify selection-induced regularization, whereas higher ratios preserve data coverage and optimization fidelity. Motivated by this insight, we propose PODS, a Plug-and-play Oscillatory Data-volume Scheduling framework. Rather than introducing another sample-scoring metric, PODS serves as a lightweight module that dynamically schedules how much data to select over training. Under the target selection ratio, PODS alternates between low-ratio regularization phases and high-ratio recovery phases to exploit selection-induced regularization without sacrificing optimization stability. With its lightweight, ratio-level, and task-agnostic design, PODS is compatible with existing static and dynamic selection methods and broadly applicable across training paradigms. Experiments across various datasets, architectures, and tasks show that PODS consistently improves the efficiency-generalization trade-off, e.g., reducing ImageNet-1k training cost by 50% with improved accuracy and accelerating LLM instruction tuning by over 2x without performance degradation.

</details>


### [213] [Peng's Q($λ$) for Conservative Value Estimation in Offline Reinforcement Learning](https://arxiv.org/abs/2605.14779)
*Byeongchan Kim,Min-hwan Oh*

Main category: cs.LG

TL;DR: CPQL is a model-free offline RL algorithm using Conservative Peng's Q(λ) operator for conservative multi-step value estimation, achieving better performance than behavior policy while providing theoretical guarantees, and enabling smooth offline-to-online transition.


<details>
  <summary>Details</summary>
Motivation: Existing offline RL methods often suffer from over-pessimistic value estimation when using single-step Bellman operators. There's a need for multi-step conservative value estimation that can fully leverage offline trajectories while avoiding performance drops below behavior policy and providing theoretical guarantees.

Method: Adapts Peng's Q(λ) (PQL) operator for conservative value estimation as an alternative to Bellman operator. Uses multi-step approach that fully leverages offline trajectories, inducing implicit behavior regularization by keeping the fixed point closer to the behavior policy's value function.

Result: CPQL consistently and significantly outperforms existing offline single-step baselines on D4RL benchmark. Simultaneously mitigates over-pessimistic estimation, achieves ≥ behavior policy performance, provides near-optimal guarantees, and enables smooth offline-to-online transition without initial performance drop.

Conclusion: CPQL is the first multi-step conservative value estimation method for offline RL that theoretically and empirically demonstrates effectiveness, bridging gaps in performance guarantees and enabling robust offline-to-online learning with pre-trained Q-functions.

Abstract: We propose a model-free offline multi-step reinforcement learning (RL) algorithm, Conservative Peng's Q($λ$) (CPQL). Our algorithm adapts the Peng's Q($λ$) (PQL) operator for conservative value estimation as an alternative to the Bellman operator. To the best of our knowledge, this is the first work in offline RL to theoretically and empirically demonstrate the effectiveness of conservative value estimation with a \textit{multi-step} operator by fully leveraging offline trajectories. The fixed point of the PQL operator in offline RL lies closer to the value function of the behavior policy, thereby naturally inducing implicit behavior regularization. CPQL simultaneously mitigates over-pessimistic value estimation, achieves performance greater than (or equal to) that of the behavior policy, and provides near-optimal performance guarantees -- a milestone that previous conservative approaches could not achieve. Extensive numerical experiments on the D4RL benchmark demonstrate that CPQL consistently and significantly outperforms existing offline single-step baselines. In addition to the contributions of CPQL in offline RL, our proposed method also contributes to the offline-to-online learning framework. Using the Q-function pre-trained by CPQL in offline settings enables the online PQL agent to avoid the performance drop typically observed at the start of fine-tuning and to attain robust performance improvements. Our code is available at https://github.com/oh-lab/CPQL.

</details>


### [214] [Understanding Imbalanced Forgetting in Rehearsal-Based Class-Incremental Learning](https://arxiv.org/abs/2605.14785)
*Alberto Tamajo,Srinandan Dasmahapatra,Rahman Attar*

Main category: cs.LG

TL;DR: This paper investigates imbalanced forgetting in class-incremental learning, identifies three last-layer gradient coefficients that predict forgetting patterns, and finds self-induced interference as the strongest predictor.


<details>
  <summary>Details</summary>
Motivation: Class-incremental learning suffers from catastrophic forgetting, and despite balanced rehearsal allocation, some classes are forgotten more than others (imbalanced forgetting). This phenomenon remains underexplored but is systematically severe in rehearsal-based CIL.

Method: Constructs three last-layer coefficients through principled analysis that capture different gradient-level sources of interference affecting each past class during incremental steps. Demonstrates these coefficients reliably predict forgetting rankings of past classes.

Result: The three coefficients together reliably predict how past classes will rank in terms of forgetting. Self-induced interference emerges as the strongest predictor, with evidence suggesting it's influenced by new-class interference coefficient.

Conclusion: The findings provide insights into imbalanced forgetting mechanisms and suggest promising directions for mitigation by reducing class-wise disparities in identified interference sources. The coefficients offer a plausible mechanistic account linking last-layer gradient interactions to class-level forgetting outcomes.

Abstract: Neural networks suffer from catastrophic forgetting in class-incremental learning (CIL) settings. Rehearsal$\unicode{x2013}$replaying a subset of past samples$\unicode{x2013}$is a well-established mitigation strategy. However, recent results suggest that, despite balanced rehearsal allocation, some classes are forgotten substantially more than others. Despite its relevance, this imbalanced forgetting phenomenon remains underexplored. This work shows that imbalanced forgetting arises systematically and severely in rehearsal-based CIL and investigates it extensively. Specifically, we construct, from a principled analysis, three last-layer coefficients that capture different gradient-level sources of interference affecting each past class during an incremental step. We then demonstrate that, together, they reliably predict how past classes will rank in terms of forgetting at the end of that step. While predictive performance alone does not establish causality, these results support the interpretation of the coefficients as a plausible mechanistic account linking last-layer gradient-level interactions during training to class-level forgetting outcomes. Notably, one coefficient$\unicode{x2013}$capturing self-induced interference$\unicode{x2013}$emerges as the strongest predictor, with controlled experiments providing evidence consistent with this coefficient being influenced by the new-class interference coefficient. Overall, our findings provide valuable insights and suggest promising directions for mitigating imbalanced forgetting by reducing class-wise disparities in the identified sources of interference.

</details>


### [215] [GFMate: Empowering Graph Foundation Models with Test-time Prompt Tuning](https://arxiv.org/abs/2605.14809)
*Yan Jiang,Ruihong Qiu,Zi Huang*

Main category: cs.LG

TL;DR: GFMate introduces a novel test-time graph prompt tuning method for Graph Foundation Models that avoids entanglement with source domains and model pre-training, achieving up to 30.63% improvement across 12 benchmark datasets.


<details>
  <summary>Details</summary>
Motivation: Existing graph prompt methods suffer from limitations: they entangle prompts with specific source domains and GFM pre-training strategies, restricting generalizability to other domains and different GFMs. Additionally, they only use few-shot tuning and neglect valuable information in unlabelled target domain test data.

Method: GFMate uses centroid and layer prompts applied after pre-training on target domains, avoiding entanglement with specific source domains. It introduces a test-time complementary learning objective that leverages both labelled and unlabelled target domain data for effective prompt tuning at test time.

Result: Extensive experiments on 12 benchmark datasets demonstrate superior performance and efficiency, achieving improvements of up to 30.63% compared to existing methods. The method shows strong generalizability across different domains and GFMs.

Conclusion: GFMate provides an effective pre-training-agnostic test-time graph prompt tuning approach that overcomes limitations of existing methods by avoiding domain entanglement and fully utilizing both labelled and unlabelled target domain data, offering a more generalizable solution for graph foundation models.

Abstract: Graph prompt tuning has shown great potential in graph learning by introducing trainable prompts to enhance the model performance in conventional single-domain scenarios. Recent research has extended graph prompts to improve Graph Foundation Models (GFMs) by few-shot tuning auxiliary prompts. Despite their progress, most existing methods embed source-domain information into prompts, which serve either as input to GFMs or encoded during model pre-training. Such prompt entanglement with specific source domains and GFM pre-training strategy restricts their generalisability to other domains and different GFMs. Furthermore, existing GFM prompts merely rely on few-shot tuning for adaptation, neglecting the rich information in unlabelled target domain test data. Motivated by these insights, this paper aims to empower GFMs with pre-training-agnostic test-time graph prompt tuning, named GFMate. GFMate introduces centroid and layer prompts applied after pre-training on target domains, avoiding entanglement with specific source domains and model pre-training. In addition, a test-time complementary learning objective is devised to exploit both labelled and unlabelled target domain data for effective test-time prompt tuning. Extensive experiments on 12 benchmark datasets demonstrate the superior performance and efficiency of GFMate, achieving improvements of up to 30.63%. Code is available at https://github.com/YanJiangJerry/GFMate.

</details>


### [216] [ToMAToMP: Robust and Multi-Parameter Topological Clustering](https://arxiv.org/abs/2605.14824)
*Ludo Andrianirina,Mathieu Carrière*

Main category: cs.LG

TL;DR: ToMAToMP is a new multi-parameter topological clustering method that extends the original ToMATo algorithm to handle multiple functions simultaneously with theoretical guarantees, addressing limitations like graph hyperparameter tuning and outlier sensitivity.


<details>
  <summary>Details</summary>
Motivation: The motivation addresses three main limitations of existing topological clustering (ToMATo): 1) Requires user-tuned graph hyperparameter, 2) Sensitivity to outlier values in function range, 3) Can only handle one function at a time despite many applications needing multiple functions.

Method: The method leverages a recent tool from multi-parameter persistent homology called MMA (Multidimensional Morse Analysis) decomposition to design a clustering algorithm that can handle several functions simultaneously while maintaining theoretical guarantees.

Result: The paper proves robustness properties for ToMAToMP and shows it can make ToMATo independent of graph tuning and robust to outliers. Numerical experiments demonstrate strong improvements over non-topological and topological baselines across various datasets.

Conclusion: ToMAToMP is the first topological clustering method capable of handling multiple functions simultaneously with theoretical guarantees, addressing key limitations of previous approaches while maintaining efficiency and quality in clustering results.

Abstract: Topological clustering, and its main algorithm ToMATo, is a clustering method from Topological Data Analysis (TDA) which has been applied successfully in several applications during the last few years. This is due to its high versatility, as clusters are detected from the persistent components in the sublevel sets of any user-defined function (gene expression, pixel values, etc), and efficiency, as topological clustering enjoys robustness guarantees. However, ToMATo is also limited in several ways. First, a graph on the data points needs to be provided as a hyper-parameter of the method (whose fine-tuning is left to the user). Second, ToMATo is known to be very sensitive to outlier values in the function range. Finally, and most importantly, ToMATo can only handle one function at a time, whereas it is critical to use several functions in various applications. In this article, we introduce ToMAToMP: the first topological clustering method able to handle several functions at the same time with theoretical guarantees. More specifically, we leverage a recent tool from multi-parameter persistent homology, called MMA decomposition, to design our clustering algorithm, and prove that it enjoys robustness properties. As corollaries, we show that it can be used to make ToMATo independent of graph tuning, and robust to outliers. Finally, we provide a set of numerical experiments showcasing the efficiency and quality of the clusterings produced by ToMAToMP, by showing strong improvement over non-topological and topological baselines for various datasets.

</details>


### [217] [GenAI for Energy-Efficient and Interference-Aware Compressed Sensing of GNSS Signals on a Google Edge TPU](https://arxiv.org/abs/2605.14839)
*Thorben Wegner,Lucas Heublein,Tobias Feigl,Felix Ott,Christopher Mutschler,Alexander Rügamer*

Main category: cs.LG

TL;DR: Real-time GNSS jamming detection using GenAI/VAEs on Edge TPUs with >42x compression while maintaining high classification accuracy.


<details>
  <summary>Details</summary>
Motivation: Traditional GNSS jamming classification requires expensive cloud-based post-processing and complex data transmission, while power-constrained environments need real-time solutions for growing jamming threats.

Method: Uses variational autoencoders (VAEs) deployed on Google Edge TPUs to compress GNSS data streams at hardware level with 8-bit quantization. Tests on raw IQ, FFT, and handcrafted features, and explores conditional/factorized VAEs for latent feature disentanglement.

Result: Achieves >42x compression with 0.915 F2-score classifying ~72 interference types on reconstructed signals (vs 0.923 on originals). Reduces transmission costs and enables energy-efficient deployment.

Conclusion: Hardware-centric GenAI approach provides practical real-time solution for GNSS interference mitigation with high compression, accurate classification, and enhanced model interpretability for sensitive applications.

Abstract: Traditional methods for classifying global navigation satellite system (GNSS) jamming signals typically involve post-processing raw or spectral data streams, requiring complex and costly data transmission to cloud-based interference classification systems. In contrast, our proposed approach efficiently compresses GNSS data streams directly at the hardware receiver while simultaneously classifying jamming and spoofing attacks in real time. Given the growing prevalence of GNSS jamming, there is a critical need for real-time solutions suitable for power-constrained environments. This paper introduces a novel method for compressing and classifying GNSS jamming threats using generative artificial intelligence (GenAI), specifically variational autoencoders (VAEs), deployed on Google Edge tensor processing units (TPUs). The study evaluates various autoencoder (AE) architectures to compress and reconstruct GNSS signals, focusing on preserving interference characteristics while minimizing data size near the receiver hardware. The pipeline adapts large-scale AE models for Google Edge TPUs through 8-bit quantization to ensure energy-efficient deployment. Tests on raw in-phase and quadrature-phase (IQ) data, Fast Fourier Transform (FFT) data, and handcrafted features show the system achieves significant compression (>42x) and accurate classification of approximately 72 interference types on reconstructed signals (F2-score 0.915), closely matching the original signals (F2-score 0.923). The hardware-centric GenAI approach also substantially reduces jammer signal transmission costs, offering a practical solution for interference mitigation. Ablation studies on conditional and factorized VAEs (i.e., FactorVAE) explore latent feature disentanglement for data generation, enhancing model interpretability and fostering trust in machine learning (ML) solutions for sensitive interference applications.

</details>


### [218] [In-Context Learning for Data-Driven Censored Inventory Control](https://arxiv.org/abs/2605.14840)
*Sohom Mukherjee,Anh-Duy Pham,Richard Pibernik,Yunbei Xu*

Main category: cs.LG

TL;DR: A novel approach called ICGPS (in-context generative posterior sampling) combines offline generative models with online decision-making to handle inventory control with demand censoring, showing robustness to prior mismatch and achieving sublinear Bayesian regret.


<details>
  <summary>Details</summary>
Motivation: Existing methods for inventory control with decision-dependent censoring (censored/repeated newsvendor problem) have limitations: parametric Thompson sampling can be brittle under prior mismatch, while offline imputation methods don't transfer well to online learning. There's a need for a robust approach that combines the benefits of both worlds.

Method: The paper proposes ICGPS which combines offline meta-training of generative models with online in-context autoregressive generation. The approach takes oracle actions on learned completions of latent demand, using a frozen time-series transformer backbone (ChronosFlow) with a trainable conditional normalizing-flow head for fast censoring-consistent sampling.

Result: Theoretical analysis shows Bayesian regret of ICGPS is bounded by Thompson sampling benchmark regret plus a deployment penalty scaling as √T × √(completion mismatch). For the repeated newsvendor problem, sublinear Bayesian regret is achieved by reducing censored feedback to bandit convex optimization. Experimental results show ChronosFlow-ICGPS matches correctly specified Thompson sampling, outperforms myopic/UCB baselines, and is robust to prior mismatch and distribution shift.

Conclusion: ICGPS provides a robust framework for operational problems with decision-dependent censoring that combines the predictive power of modern generative models with principled online decision-making, offering theoretical guarantees and practical performance across both synthetic benchmarks and real-world datasets.

Abstract: We study inventory control with decision-dependent censoring, focusing on the censored or repeated newsvendor (R-NV), where each order quantity determines whether demand is fully observed or censored by sales. Existing approaches based on parametric Thompson sampling (TS) can be brittle under prior mismatch, while offline imputation methods need not transfer to online learning. Motivated by the predictive view of decision making, we combine these ideas by taking oracle actions on learned completions of latent demand. We propose in-context generative posterior sampling (ICGPS), which uses modern generative models that are meta-trained offline and deployed online by in-context autoregressive generation. Theoretically, we show that the Bayesian regret of ICGPS with a learned completion kernel is bounded by the Bayesian regret of a TS benchmark with the ideal completion kernel plus a deployment penalty scaling as $\sqrt{T}$ times the square root of the completion mismatch. This yields a plug-in template for operational problems with known TS regret bounds. For R-NV, we derive sublinear Bayesian regret by reducing censored feedback to bandit convex optimization feedback. We also show that, under reasonable coverage and stability assumptions, the online completion mismatch is controlled by the offline censored predictive mismatch, so offline predictive quality transfers to online performance. Practically, we instantiate ICGPS with ChronosFlow, which combines a frozen time-series transformer backbone with a trainable conditional normalizing-flow head for fast censoring-consistent sampling. In benchmark experiments, ChronosFlow-ICGPS matches correctly specified TS, outperforms myopic and UCB-style baselines, and is robust to prior mismatch and distribution shift. ChronosFlow-ICGPS also performs well for the real-world SuperStore dataset, especially under heavy censoring.

</details>


### [219] [GPart: End-to-End Isometric Fine-Tuning via Global Parameter Partitioning](https://arxiv.org/abs/2605.14841)
*Paolo Mandica,Michał Brzozowski,Zuzanna Dubanowska,Neo Christopher Chung*

Main category: cs.LG

TL;DR: GPart is a novel parameter-efficient fine-tuning method that uses a single isometric partition matrix to map low-dimensional trainable vectors directly into full weight space, achieving superior efficiency without low-rank constraints.


<details>
  <summary>Details</summary>
Motivation: Current LoRA methods for parameter-efficient fine-tuning have a fundamental limitation: their bilinear structure distorts the optimization landscape because the mapping from trainable parameters to weight updates is not distance-preserving. Even improved methods like Uni-LoRA don't solve the core distance-preservation problem.

Method: GPart removes the low-rank bottleneck entirely by using a single isometric partition matrix to map a d-dimensional trainable vector directly into the full weight space. This creates an extremely minimal pipeline with just one random projection that maintains end-to-end isometry, requiring only d+1 values for storage (the vector plus a random seed).

Result: GPart demonstrates superior or comparable performance to existing PEFT methods across natural language understanding, computer vision tasks, and mathematical reasoning, while achieving state-of-the-art efficiency.

Conclusion: GPart offers a straightforward and elegant path to parameter-efficient fine-tuning by removing structural constraints, achieving both high efficiency and strong performance through random low-dimensional subspaces without imposing low-rank matrix structure.

Abstract: Low-rank adaptation (LoRA) has become the dominant paradigm for parameter-efficient fine-tuning (PEFT) of large language models (LLMs). However, its bilinear structure introduces a critical limitation: the mapping from trainable parameters to weight updates is not distance-preserving, distorting the optimization landscape. Methods that project a low-dimensional vector into LoRA's parameter space, such as Uni-LoRA, improve parameter efficiency, but the subsequent bilinear LoRA map breaks end-to-end isometry, leaving the core distance-preservation problem unresolved. We propose GPart (Global Partition fine-tuning), a highly parameter-efficient fine-tuning method which removes the low-rank bottleneck entirely. Our method uses a single isometric partition matrix to map a $d$-dimensional trainable vector directly into the full weight space of the model. The result is an extremely minimal fine-tuning pipeline: one random projection, end-to-end isometric, with a single clean hyperparameter ($d$) and storage cost of $d+1$ values (the trainable vector plus a random seed). GPart builds on the theoretical premise that effective fine-tuning can emerge from random low-dimensional subspaces of the full weight space, without imposing low-rank matrix structure. We empirically demonstrate the superior or comparable performance of GPart to existing PEFT methods on natural language understanding, computer vision tasks, and mathematical reasoning. Overall, GPart achieves state-of-the-art efficiency and performance by removing structural constraints, offering a straightforward and elegant path to PEFT.

</details>


### [220] [XFP: Quality-Targeted Adaptive Codebook Quantization with Sparse Outlier Separation for LLM Inference](https://arxiv.org/abs/2605.14844)
*Thomas Witt*

Main category: cs.LG

TL;DR: XFP introduces a novel weight quantization approach for LLM inference that inverts the workflow: instead of manually specifying bit-widths, operators set quality floors on cosine similarity, and XFP automatically determines codebook sizes, outlier budgets, and packing. This achieves high performance while maintaining accuracy.


<details>
  <summary>Details</summary>
Motivation: Current weight quantization methods for LLM inference require manual bit-width selection, Hessian calculations, or calibration data. The authors aim to simplify this process by inverting the workflow, allowing operators to specify quality requirements while the system automatically handles quantization details.

Method: XFP decomposes each weight matrix into a sparse fp16 outlier residual and a dense sub-byte index tensor into per-group learned codebooks. It offers two storage modes: V2 (per-channel Lloyd quantization) and V2a (shared library of L=32 codebooks per layer). The H-Process provides a quality-driven iterative method to find optimal quantization thresholds that fit models within memory constraints while maintaining output quality.

Result: On Qwen3.5-122B-A10B with V2 mode, XFP reaches 138 tok/s single-stream decode at 94.49% GSM8K accuracy, 49% faster than Marlin INT4 at TP=1. For larger models like Qwen3.5-397B-A17B, the H-Process fits the model into 2x96 GB at ~3.4 effective bits, delivering 100.9 tok/s with 66.72% GSM8K accuracy, exceeding INT4 with routed-expert pruning on memory, throughput, and accuracy simultaneously.

Conclusion: XFP provides an effective dynamic weight quantization solution for LLM inference that simplifies operator workflow while achieving competitive performance. The H-Process extends this capability to memory-constrained scenarios, enabling deployment of large models that wouldn't otherwise fit in target memory envelopes.

Abstract: We introduce XFP, a dynamic weight quantizer for LLM inference that inverts the conventional workflow: the operator specifies reconstruction quality floors on per-channel cosine similarity (one strict floor for attention and shared experts, one lazy floor for routed-expert MoE); XFP determines codebook size, outlier budget, and packing per layer automatically -- no Hessian, no calibration data, no manual bit-width selection. Each weight matrix is decomposed into a sparse fp16 outlier residual and a dense sub-byte index tensor into a per-group learned codebook. Two storage modes share one auto-select frontend and one fused decode kernel: V2 (per-channel Lloyd) and V2a (shared library of L=32 codebooks per layer). On Qwen3.5-122B-A10B under V2, XFP reaches 138 tok/s single-stream decode on workstation hardware (RTX PRO 6000 Blackwell, TP=2) at 94.49% GSM8K strict-match (3 seeds, n=3957), and is 49% faster than Marlin INT4 at TP=1. For models that do not fit in the target memory envelope, we present the H-Process: a quality-driven iteration over the two cosine thresholds that finds the operating point at which the model just fits while still producing sensible output. Three constraints define its search space: the operator-set thresholds, an OOM boundary at quantize-on-load, and a garbage boundary in generation (cosine similarity steers; benches verify). On Qwen3.5-397B-A17B (512 routed experts/layer), the H-Process fits the full expert population into 2x96 GB at ~3.4 effective bits and delivers 100.9 tok/s long-output decode at 66.72% GSM8K strict-match on the full 1319-problem set (single seed at submission; multi-seed evaluation in progress), exceeding INT4 with routed-expert pruning on memory, throughput, and accuracy simultaneously.

</details>


### [221] [Exploitation of Hidden Context in Dynamic Movement Forecasting: A Neural Network Journey from Recurrent to Graph Neural Networks and General Purpose Transformers](https://arxiv.org/abs/2605.14855)
*Lukas Schelenz,Shobha Rajanna,Denis Gosalci,Lucas Heublein,Jonas Pirkl,Jonathan Ott,Felix Ott,Christopher Mutschler,Tobias Feigl*

Main category: cs.LG

TL;DR: The paper evaluates various forecasting methods for predicting NBA player trajectories, finding that a hybrid LSTM model with contextual information achieves the best performance in terms of accuracy, efficiency, and data requirements.


<details>
  <summary>Details</summary>
Motivation: Forecasting is crucial for mitigating delays in signal processing pipelines, especially for predicting dynamic movements in chaotic sports environments like NBA games. The unpredictable nature of sports with abrupt velocity and direction changes presents significant challenges that traditional methods struggle to address.

Method: The paper evaluates multiple forecasting approaches including: traditional methods like (S)ARIMA(X), Kalman filters, and Particle filters; and machine learning methods including LSTM networks, graph neural networks (GNNs), Transformers, and temporal convolutional neural networks (TCNN). A hybrid LSTM model augmented with contextual information is proposed as a key architecture.

Result: ML-based methods showed substantial improvements over linear models across forecast horizons up to 2 seconds. The hybrid LSTM with contextual information achieved the lowest final displacement error (FDE) of 1.51m, outperforming TCNN, GAT, and Transformers. It also required less data and training time compared to GAT and Transformers. No single architecture excelled across all metrics, revealing trade-offs in input history length, generalizability, and contextual information incorporation.

Conclusion: There is no universal best architecture for trajectory prediction in fast-paced environments like NBA gameplay. Task-specific considerations are essential, and the hybrid LSTM with contextual information represents a strong balance between accuracy and efficiency for this specific application.

Abstract: Forecasting within signal processing pipelines is crucial for mitigating delays, particularly in predicting the dynamic movements of objects such as NBA players. This task poses significant challenges due to the inherently interactive and unpredictable nature of sports, where abrupt changes in velocity and direction are prevalent. Traditional approaches, including (S)ARIMA(X), Kalman filters (KF), and Particle filters (PF), often struggle to model the non-linear dynamics present in such scenarios. Machine learning (ML) methods, such as long short-term memory (LSTM) networks, graph neural networks (GNNs), and Transformers, offer greater flexibility and accuracy but frequently fail to explicitly capture the interplay between temporal dependencies and contextual interactions, which are critical in chaotic sports environments. In this paper, we evaluate these models and assess their strengths and weaknesses. Experimental results reveal key performance trade-offs across input history length, generalizability, and the ability to incorporate contextual information. ML-based methods demonstrated substantial improvements over linear models across forecast horizons of up to 2s. Among the tested architectures, our hybrid LSTM augmented with contextual information achieved the lowest final displacement error (FDE) of 1.51m, outperforming temporal convolutional neural network (TCNN), graph attention network (GAT), and Transformers, while also requiring less data and training time compared to GAT and Transformers. Our findings indicate that no single architecture excels across all metrics, emphasizing the need for task-specific considerations in trajectory prediction for fast-paced, dynamic environments such as NBA gameplay.

</details>


### [222] [REALM: Retrospective Encoder Alignment for LFP Modeling](https://arxiv.org/abs/2605.14867)
*Peicheng Wu,Zhenyu Bu,Runze Ma,Lin Du*

Main category: cs.LG

TL;DR: REALM is a retrospective distillation framework that enables causal LFP decoding for real-time BCI deployment, achieving state-of-the-art performance with reduced parameters and training time.


<details>
  <summary>Details</summary>
Motivation: Spike-based BCIs face power and bandwidth limitations with high channel counts and wireless operation, while LFP-based models offer improved stability and efficiency but suffer from reduced accuracy and non-causal architectures unsuitable for real-time deployment.

Method: REALM uses retrospective distillation: first pretraining a bidirectional Mamba-2 teacher model via masked autoencoding, then distilling it into a compact causal student model using representation alignment and task supervision objectives.

Result: REALM outperforms both causal and non-causal LFP-based SOTA methods, achieves 2× reduction in parameters, 10× reduction in training time, and demonstrates competitive decoding performance without spike signals.

Conclusion: Retrospective distillation effectively bridges offline and real-time neural decoding, showing LFP-only models can be competitive practical alternatives for next-generation wireless implantable BCIs.

Abstract: Spike activity has been the dominant neural signal for behavior decoding due to its high spatial and temporal resolution. However, as brain-computer interfaces (BCIs) move toward high channel counts and wireless operation, the high sampling frequency of spike signals becomes a bottleneck due to high power and bandwidth requirements. Local field potentials (LFPs) represent a different spatial-temporal scale of brain activity compared to spikes, offering key advantages including improved long-term stability, reduced energy consumption, and lower bandwidth requirement. Despite these benefits, LFP-based decoding models typically show reduced accuracy and often rely on non-causal architectures that are unsuitable for real-time deployment. To address these challenges, we propose REALM: a retrospective distillation framework that enables causal LFP decoding. Inspired by offline-to-online distillation strategies in speech recognition, REALM transfers representational knowledge from a pretrained multi-session bidirectional LFP model to a causal version for real-time deployment. We first pretrain a bidirectional Mamba-2 teacher model using a masked autoencoding objective. We then distill this teacher model into a compact student model via a combined objective of representation alignment and task supervision. REALM consistently outperforms both causal and non-causal LFP-based SOTA methods for behavior decoding. Notably, our REALM improves decoding performance while achieving a $2\times$ reduction in parameter count and a $10\times$ reduction in training time. These results demonstrate that retrospective distillation effectively bridges the gap between offline and real-time neural decoding. REALM shows that LFP-only models can achieve competitive decoding performance without reliance on spike signals, offering a practical and scalable alternative for next-generation wireless implantable BCIs.

</details>


### [223] [Fast Adversarial Attacks with Gradient Prediction](https://arxiv.org/abs/2605.14868)
*Kamil Ciosek,Aleksandr V. Petrov,Nicolò Felicioni,Konstantina Palla*

Main category: cs.LG

TL;DR: A new family of attacks that predicts gradients from forward-pass hidden states using linear regression, eliminating backward passes and achieving ~5x throughput increase for adversarial example generation compared to FGSM.


<details>
  <summary>Details</summary>
Motivation: Current adversarial attack methods like FGSM are throughput-limited by the cost of backward passes, which creates bottlenecks for large-scale robustness evaluation, adversarial training, and red-teaming tasks.

Method: The method predicts input gradients from forward-pass hidden states using lightweight linear regression, motivated by a kernel view of neural networks. The approach is exact in the Neural Tangent Kernel regime and works well for practical finite-width models.

Result: The method achieves similar attack performance to FGSM while using only a small fraction of the time, resulting in a 532% increase in throughput (approximately 5x speedup).

Conclusion: Gradient prediction provides a simple and general approach to significantly faster adversarial example generation under realistic computational constraints, enabling more efficient robustness evaluation and training.

Abstract: Generating adversarial examples at scale is a core primitive for robustness evaluation, adversarial training, and red-teaming, yet even "fast" attacks such as FGSM remain throughput-limited by the cost of a backward pass. We introduce a family of attacks that eliminates the backward pass by predicting the input gradient from forward-pass hidden states via a lightweight linear regression. The approach is motivated by a kernel view of neural networks and is exact in the Neural Tangent Kernel regime, while remaining effective for practical finite-width models. Empirically, our methods recover much of FGSM's attack performance while using only a small fraction of the time, corresponding to a $532\%$ increase in throughput. These results suggest gradient prediction as a simple and general route to significantly faster adversarial generation under realistic wall-clock constraints.

</details>


### [224] [AIMing for Standardised Explainability Evaluation in GNNs: A Framework and Case Study on Graph Kernel Networks](https://arxiv.org/abs/2605.14884)
*Magdalena Proszewska,N. Siddharth*

Main category: cs.LG

TL;DR: AIM is a comprehensive evaluation framework for explainable GNNs that measures Accuracy, Instance-level, and Model-level explanations to enable cross-model comparison and generate actionable insights for model improvement.


<details>
  <summary>Details</summary>
Motivation: Current evaluation frameworks for GNN explainability are limited: they focus on post-hoc explanations, make cross-model comparisons difficult, and lack comprehensive measures for inherently interpretable models. There's a need for a more flexible and applicable framework.

Method: The authors introduce AIM framework with minimal constraints to enhance flexibility. They apply it in a pipeline that extracts explanations from inherently interpretable GNNs (GKNs and PNs), evaluates them using AIM, identifies limitations, and obtains insights about model characteristics.

Result: Using GKNs as a case study, the insights from AIM were used to develop an improved model called xGKN that maintains high accuracy while demonstrating improved explainability. The framework enables systematic evaluation and improvement of GNN explainability.

Conclusion: AIM advances XAI for GNNs by providing a comprehensive, flexible evaluation framework that facilitates cross-model comparisons and generates actionable insights for improving both accuracy and explainability in graph neural networks.

Abstract: Graph Neural Networks (GNNs) have advanced significantly in handling graph-structured data, but a comprehensive framework for evaluating explainability remains lacking. Existing evaluation frameworks primarily involve post-hoc explanations, and operate in the setting where multiple methods generate a suite of explanations for a single model. This makes comparison of explanations across models difficult. Evaluation of inherently interpretable models often targets a specific aspect of interpretability relevant to the model, but remains underdeveloped in terms of generating insight across a suite of measures. We introduce AIM, a comprehensive framework that addresses these limitations by measuring Accuracy, Instance-level explanations, and Model-level explanations. AIM is formulated with minimal constraints to enhance flexibility and facilitate broad applicability. Here, we use AIM in a pipeline, extracting explanations from inherently interpretable GNNs such as graph kernel networks (GKNs) and prototype networks (PNs), evaluating these explanations with AIM, identifying their limitations and obtaining insights to their characteristics. Taking GKNs as a case study, we show how the insights obtained from AIM can be used to develop an updated model, xGKN, that maintains high accuracy while demonstrating improved explainability. Our approach aims to advance the field of Explainable AI (XAI) for GNNs, providing more robust and practical solutions for understanding and improving complex models.

</details>


### [225] [Critic-Driven Voronoi-Quantization for Distilling Deep RL Policies to Explainable Models](https://arxiv.org/abs/2605.14897)
*Senne Deproost,Denis Steckelmacher,Ann Nowé*

Main category: cs.LG

TL;DR: CD-VSP partitions black-box RL policies into Voronoi regions where linear functions approximate the policy, using critic values to guide where to add subpolicies, achieving good interpretability-performance balance.


<details>
  <summary>Details</summary>
Motivation: Existing distillation methods for RL policies struggle to balance performance-interpretability trade-off and lack RL-specific considerations like action value. Traditional distillation only minimizes behavior distance between original and surrogate policies, ignoring important RL components.

Method: Critic-Driven Voronoi State Partitioning (CD-VSP): Model-agnostic method that partitions black-box control policy into Voronoi regions where simple linear models can be optimized via gradient descent. Uses critic value network to iteratively introduce new subpolicies in regions with insufficient value (as proxy for policy complexity). Voronoi quantizer assigns linear functions to state space points via nearest neighbor lookups, creating cell-like partitions.

Result: Validated on several well-known benchmarks. Proved that this distillation approach approximates original policy using reasonable-sized set of linear functions.

Conclusion: CD-VSP provides an effective approach to distill black-box RL policies into interpretable Voronoi-partitioned linear functions while maintaining performance, addressing the performance-interpretability trade-off by leveraging critic values to guide partitioning complexity.

Abstract: Despite many successful attempts at explaining Deep Reinforcement Learning policies using distillation, it remains difficult to balance the performance-interpretability trade-off and select a fitting surrogate model. In addition to this, traditional distillation only minimizes the distance between the behavior of the original and the surrogate policy while other RL-specific components such as action value are disregarded. To solve this, we introduce a new model-agnostic method called Critic-Driven Voronoi State Partitioning, which partitions a black box control policy into regions where a simple class of model can be optimized using gradient descent. By exploiting the critic value network of the original policy, we iteratively introduce new subpolicies in regions with insufficient value, standing in for a measure of policy complexity. The partitioning, a Voronoi quantizer, uses nearest neighbor lookups to assign a linear function to each point in the state space resulting in a cell-like diagram. We validate our approach on several well known benchmarks and proof that this distillation approaches the original policy using a reasonable sized set of linear functions.

</details>


### [226] [TILBench: A Systematic Benchmark for Tabular Imbalanced Learning Across Data Regimes](https://arxiv.org/abs/2605.14915)
*Ruizhe Liu,Jiaqi Luo*

Main category: cs.LG

TL;DR: TILBench: A large-scale benchmark for tabular imbalanced learning evaluating 40+ methods across 57 datasets, showing no single method dominates across all settings and effectiveness depends on dataset characteristics and computational constraints.


<details>
  <summary>Details</summary>
Motivation: Despite decades of research on imbalanced learning for tabular data, there's still a lack of systematic empirical understanding of how different methods behave across diverse data characteristics. It's unclear how different method families compare in predictive performance, robustness, and computational scalability.

Method: Created Tabular Imbalanced Learning Benchmark (TILBench), conducting large-scale empirical evaluation of more than 40 representative algorithms across 57 diverse tabular datasets, resulting in over 200,000 controlled experiments covering a wide range of data characteristics.

Result: No single method consistently dominates across all settings. The effectiveness of imbalanced learning methods depends strongly on dataset characteristics and computational constraints.

Conclusion: Based on empirical findings, practical recommendations are provided for selecting appropriate imbalanced learning methods in real-world applications, emphasizing the need to consider dataset characteristics and computational requirements rather than relying on a single "best" method.

Abstract: Imbalanced learning remains a fundamental challenge in tabular data applications. Despite decades of research and numerous proposed algorithms, a systematic empirical understanding of how different imbalanced learning methods behave across diverse data characteristics is still lacking. In particular, it remains unclear how different method families compare in predictive performance, robustness under varying data characteristics, and computational scalability. In this work, we present Tabular Imbalanced Learning Benchmark (TILBench), a large-scale empirical benchmark for tabular imbalanced learning. TILBench evaluates more than 40 representative algorithms across 57 diverse tabular datasets, resulting in over 200000 controlled experiments across a wide range of data characteristics. Our findings show that no single method consistently dominates across all settings; instead, the effectiveness of imbalanced learning methods depends strongly on dataset characteristics and computational constraints. Based on these findings, we provide practical recommendations for selecting appropriate methods in real-world applications.

</details>


### [227] [A Mutual Information Lower Bound for Multimodal Regression Active Learning](https://arxiv.org/abs/2605.14917)
*Leonardo Ferreira Guilhoto,Akshat Kaushal,Paris Perdikaris*

Main category: cs.LG

TL;DR: Proposes MI-LB, a novel acquisition function for active learning in continuous regression with multimodal predictions that specifically targets epistemic uncertainty through mutual information between output and epistemic index.


<details>
  <summary>Details</summary>
Motivation: Existing acquisition functions for continuous regression fail to properly target epistemic uncertainty in multimodal predictive distributions - variance misses modal disagreement, and information-theoretic targets like BALD are designed for discrete outputs.

Method: Introduces a Two-Index framework separating epistemic (model hypothesis) and aleatoric (within-hypothesis) uncertainty sources, derives mutual information between output and epistemic index as acquisition objective, and develops MI-LB - a closed-form approximation for Mixture Density Network ensembles.

Result: MI-LB matches or beats all baselines on multimodal benchmarks, showing consistent superiority. Traditional geometric and Fisher-based methods only compete when input space already encodes multimodality and collapse otherwise.

Conclusion: The Two-Index framework and MI-LB acquisition function provide an effective solution for active learning in continuous regression with multimodal predictions by properly targeting epistemic uncertainty that can be resolved with additional data.

Abstract: Active learning for continuous regression has lacked an acquisition function that targets epistemic uncertainty when the predictive distribution is multimodal: variance misses modal disagreement, and information-theoretic targets like BALD are designed for discrete outputs. We introduce a Two-Index framework that makes this separation explicit: one stochastic index selects among competing model hypotheses (epistemic source), while a second governs within-hypothesis randomness (aleatoric source). An entropy decomposition within the framework identifies the mutual information between the output and the epistemic index as a principled acquisition objective, and we prove this quantity vanishes as the model is trained on growing datasets, confirming that it captures exactly the uncertainty data can resolve. Because this mutual information is intractable for continuous outputs, we derive the Mutual Information Lower Bound (MI-LB) acquisition function, a closed-form approximation for Mixture Density Network ensembles. On benchmarks featuring multimodal systems, MI-LB matches or beats every baseline evaluated and is the only method to do so consistently -- geometric and Fisher-based baselines compete only when the input space already encodes the multimodality, and collapse otherwise.

</details>


### [228] [Learning with Shallow Neural Networks on Cluster-Structured Features](https://arxiv.org/abs/2605.14927)
*Elisabetta Cornacchia,Laurent Massoulié*

Main category: cs.LG

TL;DR: Deep learning success often attributed to low-dimensional structure in data; this paper studies how spatial correlations in input data (like images/text) affect sample complexity for shallow neural networks trained with gradient descent.


<details>
  <summary>Details</summary>
Motivation: Standard theoretical models assume low-dimensional structure lies in target functions, but real-world data (images, text, genomic sequences) exhibit strong spatial correlations within the input space itself. Need a tractable model to understand how these correlations affect learning sample complexity.

Method: Propose a tractable model with targets depending on small number of latent Boolean variables. Input features grouped into clusters and correlated with latent variables. Analyze layerwise gradient-descent variant under identifiability assumption.

Result: Sample complexity scales with number of hidden variables and, when signal-to-noise ratio is sufficiently high, becomes independent of input dimension (up to logarithmic terms). Empirical validation on synthetic and real data supports theoretical findings.

Conclusion: Spatial correlations in input data can significantly reduce sample complexity for neural network learning, making it depend primarily on the number of latent variables rather than input dimension when SNR is high, explaining empirical success of deep learning in high-dimensional settings.

Abstract: The success of deep learning in high-dimensional settings is often attributed to the presence of low-dimensional structure in real-world data. While standard theoretical models typically assume that this structure lies in the target function, projecting unstructured inputs onto a low-dimensional subspace, data such as images, text or genomic sequences exhibit strong spatial correlations within the input space itself. In this paper, we propose a tractable model to study how these correlations affect the sample complexity of learning with gradient descent on shallow neural networks. Specifically, we consider targets that depend on a small number of latent Boolean variables, and input features grouped into clusters and correlated with the latent variables. Under an identifiability assumption, we show that for a layerwise gradient-descent variant, the sample complexity scales with the number of hidden variables and, when the signal-to-noise ratio is sufficiently high, is independent of the input dimension, up to logarithmic terms. We empirically test our theoretical findings on both synthetic and real data.

</details>


### [229] [A Hardware-Aware, Per-Layer Methodology for Post-Training Quantization of Large Language Models](https://arxiv.org/abs/2605.14929)
*Earl Killian*

Main category: cs.LG

TL;DR: SOP is a post-training quantization method for LLMs that achieves near-lossless performance at 4.5-6 bits per weight through combination of per-layer codebook optimization, block-wise scaling, and hardware-efficient LUT decoding, outperforming conventional FP8 at lower bitwidth.


<details>
  <summary>Details</summary>
Motivation: To develop a quantization method that enables efficient deployment of large language models on hardware with minimal accuracy loss, addressing the limitations of conventional fixed-precision formats like FP8 by reducing weight storage costs while maintaining high fidelity.

Method: Combines per-layer search of fixed and dynamic codebook pairs with per-block selection bit, signed per-block scales, activation-weighted cosine selection, multiple-choice knapsack promotion of sensitive layers, outlier and sparse-residual correction. Uses various codebook types including NF4, BOF4, Split87, SH4, and per-layer optimized DD4 codebooks stored in LUT SRAM. Proposes hardware-efficient LUT output format (HIF) for improved performance.

Result: Achieves lower weight reconstruction error than conventional per-layer-POT FP8 baseline (8.0 bpw) at just 6.5 bpw, representing 1.5 bpw lower storage cost. Demonstrates that block-scaled small atoms with careful scale precision can replace conventionally-deployed FP8 formats across six open model families.

Conclusion: SOP enables efficient quantization of large language models to 4.5-6 bits per weight with near-lossless fidelity, outperforming conventional FP8 approaches while reducing storage requirements, making it suitable for hardware deployment with per-layer LUT decode capabilities.

Abstract: Scaled Outer Product (SOP) is a post-training quantization methodology for large language model weights, designed to deliver near-lossless fidelity at 4.5--6 bits per weight on hardware with per-layer LUT decode. The methodology combines per-layer search of fixed and dynamic codebook pairs selected by a per-block selection bit, signed per-block scales, activation-weighted cosine selection, and multiple-choice knapsack promotion of sensitive layers with outlier and sparse-residual correction. Fixed codebooks include NF4, BOF4, Split87, and SH4; per-layer optimized codebooks (DD4) are hosted in LUT SRAM. A new hardware-efficient LUT output format (HIF) is proposed to improve performance, energy, and cost. Across six open model families, the recommended FP6 operating point (E2M3sUE4M4, 6.5 bpw) achieves lower weight reconstruction error than the conventional per-layer-POT FP8 baseline (E4M3, 8.0 bpw) at 1.5 bpw lower storage cost, demonstrating that block-scaled small atoms with carefully chosen scale precision can replace conventionally-deployed FP8. Full evaluation across the 4.5--6 bpw range, including layer promotion and sparse residual correction, is reported in a companion paper.

</details>


### [230] [Slot-MPC: Goal-Conditioned Model Predictive Control with Object-Centric Representations](https://arxiv.org/abs/2605.14937)
*Jonathan Spieler,Angel Villar-Corrales,Sven Behnke*

Main category: cs.LG

TL;DR: Slot-MPC is an object-centric world modeling framework that combines vision-based slot representations with model predictive control for improved robotic manipulation planning and generalization.


<details>
  <summary>Details</summary>
Motivation: Existing object-centric world models and RL approaches learn reactive policies fixed at inference time, limiting generalization to novel situations. There's a need for approaches that can adapt to unseen scenarios through planning rather than just reactive policies.

Method: Slot-MPC learns vision-based slot representations encoding individual objects, then learns an action-conditioned object-centric dynamics model. At inference, it uses gradient-based model predictive control (MPC) to optimize actions directly through the differentiable world model.

Result: Experiments on simulated robotic manipulation tasks show Slot-MPC improves both task performance and planning efficiency compared to non-object-centric world model baselines. Gradient-based MPC outperforms gradient-free, sampling-based MPC in offline settings with limited state-action coverage.

Conclusion: Explicitly structured, object-centric representations provide a strong inductive bias for controllable and generalizable decision-making, enabling agents to adapt to previously unseen situations through planning rather than just reactive policies.

Abstract: Predictive world models enable agents to model scene dynamics and reason about the consequences of their actions. Inspired by human perception, object-centric world models capture scene dynamics using object-level representations, which can be used for downstream applications such as action planning. However, most object-centric world models and reinforcement learning (RL) approaches learn reactive policies that are fixed at inference time, limiting generalization to novel situations. We propose Slot-MPC, an object-centric world modeling framework that enables planning through Model Predictive Control (MPC). Slot-MPC leverages vision encoders to learn slot-based representations, which encode individual objects in the scene, and uses these structured representations to learn an action-conditioned object-centric dynamics model. At inference time, the learned dynamics model enables action planning via MPC, allowing agents to adapt to previously unseen situations. Since the learned world model is differentiable, we can use gradient-based MPC to directly optimize actions, which is computationally more efficient than relying on gradient-free, sampling-based MPC methods. Experiments on simulated robotic manipulation tasks show that Slot-MPC improves both task performance and planning efficiency compared to non-object-centric world model baselines. In the considered offline setting with limited state-action coverage, we find that gradient-based MPC performs better than gradient-free, sampling-based MPC. Our results demonstrate that explicitly structured, object-centric representations provide a strong inductive bias for controllable and generalizable decision-making. Code and additional results are available at https://slot-mpc.github.io.

</details>


### [231] [Octopus: History-Free Gradient Orthogonalization for Continual Learning in Multimodal Large Language Models](https://arxiv.org/abs/2605.14938)
*Yuehao Liu,Shanyan Guan,Weijia Zhang,Xuanming Shang,Yanhao Ge,Wei Li,Chao Ma*

Main category: cs.LG

TL;DR: Octopus: A two-stage continual learning framework using gradient orthogonalization without historical data, achieving SOTA performance in multimodal LLMs.


<details>
  <summary>Details</summary>
Motivation: Existing continual learning methods for MLLMs have limitations: architecture-based approaches add computational overhead and generalize poorly, rehearsal-based methods require storing historical data (privacy/storage concerns), and regularization alone insufficiently prevents parameter interference.

Method: Proposes Octopus framework with two-stage finetuning based on History-Free Gradient Orthogonalization (HiFGO). Enforces gradient-level orthogonality without historical task data. Decouples task adaptation from regularization to balance plasticity and stability.

Result: Experiments on UCIT show state-of-the-art performance, surpassing prior SOTA by 2.14% (Avg) and 6.82% (Last).

Conclusion: Octopus effectively addresses continual learning challenges in MLLMs through gradient orthogonalization and two-stage finetuning, achieving optimal balance between learning new tasks and retaining previous knowledge without historical data.

Abstract: Continual learning in multimodal large language models (MLLMs) aims to sequentially acquire knowledge while mitigating catastrophic forgetting, yet existing methods face inherent limitations: architecture-based approaches incur additional computational overhead and often generalize poorly to new tasks, rehearsal-based methods rely on storing historical data, raising privacy and storage concerns, and conventional regularization-based strategies alone are insufficient to fully prevent parameter interference. We propose Octopus, a two-stage continual learning framework based on History-Free Gradient Orthogonalization (HiFGO), which enforces gradient-level orthogonality without historical task data. Our proposed two-stage finetuning strategy decouples task adaptation from regularization, achieving a principled balance between plasticity and stability. Experiments on UCIT show that Octopus establishes state-of-the-art performance, surpassing prior SOTA by 2.14% and 6.82% in terms of Avg and Last.

</details>


### [232] [Not All Symbols Are Equal: Importance-Aware Constellation Design for Semantic Communication](https://arxiv.org/abs/2605.14940)
*Albert Shaju,Christo Kurisummoottil Thomas,Mayukh Roy Chowdhury*

Main category: cs.LG

TL;DR: Joint semantic-physical layer framework protects task-relevant information by dynamically selecting transmission subsets based on channel conditions and using learned semantic-aware M-QAM constellations that depart from uniform spacing/Gray coding.


<details>
  <summary>Details</summary>
Motivation: Existing semantic communication systems decouple constellation design and semantic encoding, exposing critical symbols to channel errors at the same rate as irrelevant ones. Standard M-QAM minimizes average BER without regard for semantic content, making it suboptimal for task-oriented transmission.

Method: Proposes a joint semantic-physical layer framework with: 1) vector quantized-variational autoencoder for discrete latent concepts, 2) semantic criticality indicator (SCI) to score concept task relevance, 3) deep reinforcement learning agent for dynamic transmission subset selection based on channel conditions, and 4) learned semantic-aware M-QAM constellation based on joint co-occurrence statistics and SCI scores.

Result: Achieves near 100% semantic protection probability (SPP) vs. 50% for standard constellations across 4-QAM to 1024-QAM, 21:1 compression ratio with semantic quality above 0.9, generalizes across MNIST, Fashion-MNIST, and FSDD datasets without modification. Proves any Gray-coded constellation is strictly suboptimal for semantic transmission.

Conclusion: Joint semantic-physical layer optimization significantly outperforms decoupled approaches by protecting task-critical information through semantic-aware constellation design that accounts for both importance and co-occurrence statistics, enabling robust goal-oriented communication across varying channel conditions.

Abstract: Semantic communication systems for goal-oriented transmission must protect task-relevant information not only through source compression but also via physical layer mapping. Existing approaches decouple constellation design and semantic encoding, exposing critical symbols to channel errors at the same rate as irrelevant ones. Contrary to this, in this paper, a joint semantic-physical layer framework is proposed, which is composed of a vector quantized-variational autoencoder that extracts discrete latent concepts, a semantic criticality indicator (SCI) that scores each concept by task relevance, and a deep reinforcement learning agent that dynamically selects the transmission subset based on instantaneous channel conditions. At the physical layer, a learned semantic-aware M -QAM constellation assigns symbol positions according to joint co-occurrence statistics and SCI scores, departing from the uniform spacing and Gray coding of standard M -QAM which minimizes average BER without regard for semantic content. We introduce a novel semantic symbol vulnerability (SSV) metric and a semantic protection probability (SPP) to quantify the exposure of task-critical symbols to decoding errors, and prove that any Gray-coded constellation is strictly suboptimal in SCI-Weighted SSV whenever the source exhibits non-uniform semantic importance and co-occurrence statistics. Simulation results demonstrate that the proposed constellation achieves near 100% SPP across modulation orders from 4-QAM to 1024-QAM versus 50% for standard constellations at high spectral efficiency, a 21:1 compression ratio with semantic quality above 0.9, generalizing across MNIST, Fashion-MNIST, and FSDD without modification.

</details>


### [233] [Efficient Online Conformal Selection with Limited Feedback](https://arxiv.org/abs/2605.14953)
*Sreenivas Gollapudi,Kostas Kollias,Kamesh Munagala,Ali Sinop*

Main category: cs.LG

TL;DR: Adaptive Conformal Inference achieves both adversarial validity and stochastic efficiency in bandit feedback settings for conformal selection problems.


<details>
  <summary>Details</summary>
Motivation: Traditional online conformal prediction focuses on validity but minimizing resource cost (efficiency) under limited feedback remains challenging, especially with bandit feedback where only partial information is available.

Method: Apply Adaptive Conformal Inference (ACI) update rule to appropriate control parameters/dual variables. Use a unifying algorithmic technique with Lyapunov function analytic framework to handle bandit and semi-bandit feedback settings.

Result: The approach is adversarially valid (ensures success target on average for any input sequence, including distribution shifts) and stochastically efficient (achieves sublinear efficiency regret for i.i.d. inputs against stochastic benchmark).

Conclusion: The work provides a theoretical bridge between efficient online learning with limited feedback and distribution-free uncertainty quantification, handling more complex settings with significantly less feedback than prior work.

Abstract: We address the problem of conformal selection, where an agent must select a minimal subset of options to ensure that at least one ``success'' is identified with a pre-specified target probability $φ$. While traditional online conformal prediction focuses on maintaining validity for the observed sequence, minimizing the resource cost (efficiency) of such selections, especially under limited feedback, remains a significant challenge. In this work, we consider settings with the most limited ``bandit'' feedback, and demonstrate that the simple Adaptive Conformal Inference (ACI) update rule, when applied to the appropriate control parameter or dual variable, is both adversarially valid, ensuring the success target is met on average for any input sequence (and hence under distribution shifts), and stochastically efficient, achieving sublinear efficiency regret for $i.i.d.$ inputs against an appropriate stochastic benchmark. We show such guarantees under canonical models capturing bandit and semi-bandit feedback to the agent via a unifying algorithmic technique, and analytic framework involving Lyapunov functions. Our approach handles more complex settings than prior work, while requiring significantly less feedback, and our results provide a new theoretical bridge between efficient online learning with limited feedback and distribution-free uncertainty quantification.

</details>


### [234] [InfoSFT: Learn More and Forget Less with Information-Aware Token Weighting](https://arxiv.org/abs/2605.14967)
*Mahdi Sabbaghi,George Pappas,Adel Javanmard,Hamed Hassani*

Main category: cs.LG

TL;DR: InfoSFT is a principled weighting scheme for supervised fine-tuning that focuses learning on medium-confidence tokens to improve generalization while preserving existing capabilities.


<details>
  <summary>Details</summary>
Motivation: Standard SFT uniformly fits all samples, including low-likelihood ones under the base model, which can cause overfitting to specific samples rather than learning target behaviors, and can degrade prior capabilities through substantial policy shifts. Existing methods that filter or down-weight low-likelihood data risk suppressing novel behaviors the model needs to learn.

Method: InfoSFT introduces a weighting scheme for the SFT objective that concentrates learning signals on maximally informative, medium-confidence tokens - those that are neither overly familiar to the base model nor too unlikely to cause instability. This requires only a one-line modification to the standard token-wise loss.

Result: InfoSFT demonstrably improves generalization over vanilla SFT and likelihood-weighted baselines across math, code, and chain-of-thought tasks with diverse model families, while better preserving pre-existing capabilities.

Conclusion: The proposed InfoSFT method offers a simple yet effective solution to the limitations of standard SFT by focusing on medium-confidence tokens, balancing learning of novel behaviors with stability and preservation of existing capabilities.

Abstract: Supervised fine-tuning (SFT) provides the standard approach for teaching LLMs new behaviors from offline expert demonstrations. However, standard SFT uniformly fits all samples -- including those with low likelihood under the base model -- which can disproportionately drive training updates toward overfitting specific samples rather than learning the target behavior. Moreover, adapting to these unlikely samples induces substantial policy shifts that degrade prior capabilities. Existing methods mitigate this by filtering, regenerating, or down-weighting low-likelihood data. In doing so, they often suppress precisely the novel behaviors the base model has yet to learn.
  We propose InfoSFT, a principled weighting scheme for the SFT objective that concentrates learning signals on maximally informative, medium-confidence tokens -- those neither overly familiar to the base model nor too unlikely to cause instability. Requiring only a one-line modification to the standard token-wise loss, InfoSFT demonstrably improves generalization over vanilla SFT and likelihood-weighted baselines across math, code, and chain-of-thought tasks with diverse model families, while better preserving pre-existing capabilities.

</details>


### [235] [Distance-Matrix Wasserstein Statistics for Scalable Gromov--Wasserstein Learning](https://arxiv.org/abs/2605.14981)
*Ao Xu,Tieru Wu*

Main category: cs.LG

TL;DR: DMW (Distance-Matrix Wasserstein) is a scalable hierarchy of Wasserstein statistics that compares distributions of random distance matrices as a computationally efficient relaxation and lower bound of Gromov-Wasserstein distances.


<details>
  <summary>Details</summary>
Motivation: Gromov-Wasserstein distances are powerful for comparing metric spaces without requiring coordinate alignment, but they suffer from computational intractability due to being nonconvex quadratic optimal transport problems that are difficult to compute at scale.

Method: Proposes DMW which samples n points from each space, records pairwise distances to form distance matrices, and transports the resulting distributions of distance matrices using Wasserstein statistics. Introduces sliced and multi-scale variants for scalable computation, with special properties for p=1 distances.

Result: Proves DMW is a relaxation and lower bound of GW, establishes reverse approximation inequality showing the gap is controlled by Wasserstein error of sampling approximation. Provides finite-sample bounds with intrinsic-dimensional rates. Experimental validation on synthetic data, scalability, graph classification, and two-sample testing shows effectiveness.

Conclusion: DMW provides a scalable, theoretically-grounded alternative to GW that maintains interpretable structural comparison properties while being computationally tractable for large-scale applications, with convergence guarantees to GW as sampled subspaces become dense.

Abstract: Gromov--Wasserstein (GW) distances compare graphs, shapes, and point clouds through internal distances, without requiring a common coordinate system. This invariance is powerful, but discrete GW is a nonconvex quadratic optimal transport problem and is difficult to estimate at scale. We propose \emph{Distance-Matrix Wasserstein} (DMW), a hierarchy of Wasserstein statistics comparing laws of random finite distance matrices. Rather than optimizing a global point-level alignment, DMW samples $n$ points from each space, records their pairwise distances, and transports the resulting matrix laws. We prove that DMW is a relaxation and lower bound of GW, and establish a reverse approximation inequality: the GW--DMW gap is controlled by the Wasserstein error of approximating each original measure with $n$ samples. Hence population DMW converges to GW as sampled subspaces become dense. We further give finite-sample bounds, including intrinsic-dimensional rates that depend on the data manifold rather than the ambient matrix dimension $\binom n2$. For scalable computation, we introduce sliced and multi-scale DMW; for $p=1$, the sliced multi-scale dissimilarity yields positive-definite exponential kernels. Experiments on synthetic metric spaces, scalability benchmarks, graph classification, and two-sample testing validate the theory and demonstrate an interpretable GW-style proxy for structural comparison.

</details>


### [236] [Second-Order Actor-Critic Methods for Discounted MDPs via Policy Hessian Decomposition](https://arxiv.org/abs/2605.14982)
*Sanjeev Manivannan,Shuban V*

Main category: cs.LG

TL;DR: A second-order actor-critic method for discounted RL that uses Hessian-vector products for computationally efficient curvature-aware updates, justified by two-timescale analysis where critic is treated as quasi-stationary.


<details>
  <summary>Details</summary>
Motivation: First-order actor-critic methods converge to stationary points but lack curvature information that second-order optimization provides for faster convergence. However, applying second-order methods in RL is challenging due to computational complexity of Hessian estimation and the non-constant nature of action-value functions with respect to policy parameters.

Method: The paper proposes a second-order actor-critic framework where the critic evolves on a faster timescale than the actor, making it quasi-stationary during actor updates. This justifies treating the action-value function as locally constant, enabling stable Hessian approximations. The method uses Hessian-vector product computations for computationally efficient second-order updates in the discounted reward setting.

Result: The paper develops a stable and computationally efficient second-order actor-critic method that leverages full curvature information as much as possible while avoiding the typical computational burden of Hessian estimation through Hessian-vector product computations.

Conclusion: Two-timescale actor-critic analysis provides theoretical justification for treating critics as quasi-stationary, enabling stable second-order approximations in policy gradient methods. This leads to a practical second-order actor-critic algorithm with curvature-aware updates that accelerate convergence while maintaining computational efficiency.

Abstract: We address the discounted reward setting in reinforcement learning (RL). To mitigate the value approximation challenges in policy gradient methods, actor-critic approaches have been developed and are known to converge to stationary points under suitable assumptions. However, these methods rely on first-order updates. In contrast, second-order optimization provides principled curvature-aware updates that are proven to accelerate convergence, but its application in RL is limited by the computational complexity of Hessian estimation. In this work, we analyze second-order approximations for the actor update that leverage the full curvature information of the objective as much as possible. A stable approximation requires treating the action-value function as locally constant with respect to policy parameters, which does not generally hold in policy gradient methods. We show that this approximation becomes well-justified under a two-timescale actor-critic framework, where the critic evolves on a faster timescale and can be treated as quasi-stationary during actor updates. Building on this insight, we formulate a second-order actor-critic method for the discounted reward setting that leverages Hessian-vector product (HVP) computations, resulting in a computationally efficient and stable second-order update.

</details>


### [237] [DeepTokenEEG Enhancing Mild Cognitive Impairment and Alzheimers Classification via Tokenized EEG Features](https://arxiv.org/abs/2605.15009)
*Thinh Nguyen-Quang,Minh Long Ngo,Ngoc-Son Nguyen,Nguyen Thanh Vinh,Huy-Dung Han,Bui Thanh Tung,Nguyen Quang Linh,Khuong Vo,Manoj Vishwanath,Hung Cao*

Main category: cs.LG

TL;DR: DeepTokenEEG is a lightweight EEG-based model that achieves 100% accuracy for Alzheimer's disease detection using only 0.29M parameters, outperforming state-of-the-art methods by 1.41-15.35%.


<details>
  <summary>Details</summary>
Motivation: EEG-based AD detection is non-invasive and cost-effective but faces challenges with data availability, accuracy of deep learning methods, and time-consuming expert interpretation, necessitating better solutions.

Method: Proposes DeepTokenEEG, a novel lightweight model using spatial and temporal tokenizers to capture AD-related biomarkers in both temporal and frequency domains with only 0.29 million parameters.

Result: Achieves maximum accuracy of 100% on specific frequency bands, improving by 1.41-15.35% over state-of-the-art methods on a dataset of 274 subjects (180 AD cases, 94 healthy controls).

Conclusion: DeepTokenEEG shows strong potential for early AD detection and screening, with promising deployment applicability due to its compact size and high performance.

Abstract: The detection of Alzheimers disease (AD) is considered crucial, as timely intervention can improve patient outcomes. Electroencephalogram (EEG)-based diagnosis has been recognized as a non-invasive, accessible, and cost-effective approach for AD detection; however, it faces challenges related to data availability, accuracy of modern deep learning methods, and the time-consuming nature of expert-based interpretation. In this study, a novel lightweight and high-performance model, DeepTokenEEG, was designed for the diagnosis of AD and the classification of EEG signals from AD patients, individuals with other neurological conditions, and healthy subjects. Unlike traditional heavy-weight models, DeepTokenEEG ultilizes spatial and temporal tokenizer that effectively captures AD-related biomarkers in both temporal and frequency domain with only 0.29 million paramaters. Trained in a combined dataset of 274 subjects, including 180 AD cases, and 94 healthy controls, the proposed method achieves a maximum recorded accuracy of 100% on specific frequency bands, representing an improvement of 1.41-15.35% over state-of-the-art methods on the same dataset. These results indicate the potential of DeepTokenEEG for early detection and screening of AD, with promising applicability for deployment due to its compact size.

</details>


### [238] [Boosting Reinforcement Learning with Verifiable Rewards via Randomly Selected Few-Shot Guidance](https://arxiv.org/abs/2605.15012)
*Kai Yan,Alexander G. Schwing,Yu-Xiong Wang*

Main category: cs.LG

TL;DR: FEST: A few-shot demonstration-guided RLVR algorithm that achieves strong results with only 128 demonstrations by combining supervised, on-policy signals and decaying weights to prevent overfitting.


<details>
  <summary>Details</summary>
Motivation: RLVR struggles with sample efficiency on difficult problems where correct rollouts are hard to generate. Existing demonstration-guided RLVR approaches require expensive SFT with large amounts of data, making them impractical for many applications.

Method: FEST combines few-shot supervised fine-tuning (only 128 demonstrations) with reinforcement learning. Key components: 1) supervised signal from demonstrations, 2) on-policy signal from RL, 3) decaying weights on the few-shot SFT dataset to prevent overfitting during multi-epoch training.

Result: FEST outperforms baselines with magnitudes less SFT data across several benchmarks. It even matches baseline performance trained with full datasets, demonstrating remarkable efficiency.

Conclusion: Few-shot demonstration guidance is effective for RLVR when combined with proper techniques to prevent overfitting. FEST provides a practical, data-efficient solution for RLVR on difficult problems where correct rollouts are scarce.

Abstract: Reinforcement Learning with Verifiable Rewards (RLVR) has achieved great success in developing Large Language Models (LLMs) with chain-of-thought rollouts for many tasks such as math and coding. Nevertheless, RLVR struggles with sample efficiency on difficult problems where correct rollouts are hard to generate. Prior works propose to address this issue via demonstration-guided RLVR, i.e., to conduct Supervised FineTuning (SFT) when RL fails; however, SFT often requires a lot of data, which can be expensive to acquire. In this paper, we propose FEST, a FEw-ShoT demonstration-guided RLVR algorithm. It attains compelling results with only 128 demonstrations randomly selected from an SFT dataset. We find that three components are vital for the success: supervised signal, on-policy signal, and decaying weights on the few-shot SFT dataset to prevent overfitting from multiple-epoch training. On several benchmarks, FEST outperforms baselines with magnitudes less SFT data, even matching their performance with full dataset.

</details>


### [239] [Generalized Priority-Aware Shapley Value](https://arxiv.org/abs/2605.15018)
*Kiljae Lee,Ziqi Liu,Weijing Tang,Yuan Zhang*

Main category: cs.LG

TL;DR: GPASV extends Shapley value to arbitrary directed weighted priority graphs, allowing penalization rather than forbidding order violations, with applications to LLM ensemble valuation.


<details>
  <summary>Details</summary>
Motivation: Existing priority-aware Shapley value extensions require binary and acyclic pairwise priority, which is violated in real-world scenarios like aggregated human preferences and multi-criterion comparisons where priorities can be cyclic and weighted.

Method: Introduces Generalized Priority-Aware Shapley Value (GPASV), a random order value defined on arbitrary directed weighted priority graphs where edges penalize rather than forbid order violations. Includes axiomatic characterization, computational methods, and priority sweeping diagnostic.

Result: GPASV covers classical models as boundary cases and enables priority-aware valuation on complex structures. Application to LLM ensemble valuation on cyclic Chatbot Arena preference graph shows different balances of pairwise graph priority versus individual soft priority yield substantively different valuations.

Conclusion: GPASV provides a flexible framework for valuation with arbitrary priority structures, demonstrating that priority-aware valuation is nuanced and requires careful consideration of priority balance rather than being a one-button operation.

Abstract: Shapley value and its priority-aware extensions are widely used for valuation in machine learning, but existing methods require pairwise priority to be binary and acyclic, a restriction spectacularly violated in real-data examples such as aggregated human preferences and multi-criterion comparisons. We introduce the generalized priority-aware Shapley value (GPASV), a random order value defined on arbitrary directed weighted priority graphs, in which pairwise edges penalize rather than forbid order violations. GPASV covers a range of classical models as boundary cases. We establish GPASV through an axiomatic characterization, develop the associated computational methods, and introduce a priority sweeping diagnostic extending PASV's. We apply GPASV to LLM ensemble valuation on the cyclic Chatbot Arena preference graph, illustrating that priority-aware valuation is not a one-button operation: different balances of pairwise graph priority versus individual soft priority produce substantively different valuations of the same data.

</details>


### [240] [TopoPrimer: The Missing Topological Context in Forecasting Models](https://arxiv.org/abs/2605.15035)
*Zara Zetlin,Kayhan Moharreri,Maria Safi*

Main category: cs.LG

TL;DR: TopoPrimer is a framework that incorporates global topological structure of time series populations into forecasting models to improve accuracy, handle seasonal spikes, and address cold-start problems.


<details>
  <summary>Details</summary>
Motivation: Current forecasting models often struggle with seasonal demand spikes, cold-start scenarios (no item history), and may not effectively leverage the topological relationships within time series populations. There's a need for models that can maintain accuracy during difficult forecasting regimes.

Method: TopoPrimer uses persistent homology and spectral sheaf coordinates to capture global topological structure of time series populations. This topological information is precomputed once per domain and can be integrated into models in two ways: per-token input for fully-trained models, or as a lightweight adapter for pre-trained backbones. Sheaf coordinates are identified as the primary accuracy driver.

Result: TopoPrimer consistently improved forecasting accuracy across four public benchmarks (Chronos and TimesFM), with up to 7.3% MSE improvement on ECL dataset. It shows strong performance in difficult regimes: maintains within 10% accuracy during peak seasonal demand (vs. 50% degradation for classical/zero-shot models), reduces cold-start MAE by 27% over topology-free baselines. The topology advantage persists across zero-shot and fine-tuned backbones with similar magnitude.

Conclusion: Topological structure provides complementary signals to traditional time series training, with most benefits in challenging forecasting scenarios like seasonal peaks and cold-start problems. The framework is effective for both fully-trained models and as an adapter for pre-trained backbones.

Abstract: We introduce TopoPrimer, a framework that makes the global topological structure of the series population an explicit input
  to any forecasting model. TopoPrimer improves accuracy across diverse domains, stabilizes forecasts under seasonal demand
  spikes, and closes the cold-start gap. Precomputed once per domain via persistent homology and spectral sheaf coordinates,
  TopoPrimer deploys per token for fully-trained models and as a lightweight adapter for pre-trained backbones. Of these two
  components, sheaf coordinates are the primary accuracy driver. Across four public benchmarks on Chronos and TimesFM,
  TopoPrimer consistently improves forecasting accuracy, with gains of up to 7.3% MSE on ECL. The topology advantage persists
  with near-identical magnitude across zero-shot and fine-tuned backbones, suggesting topology and per-series training
  capture complementary signals. The gains are most pronounced in difficult regimes. Under peak seasonal demand, classical
  and zero-shot models degrade by up to 50%, while TopoPrimer stays within 10%. At cold start with no item history,
  TopoPrimer reduces MAE by 27% over a topology-free baseline.

</details>


### [241] [Separating Intrinsic Ambiguity from Estimation Uncertainty in Deep Generative Models for Linear Inverse Problems](https://arxiv.org/abs/2605.15050)
*Yuxin Guo,Dongrui Deng,Pulkit Grover*

Main category: cs.LG

TL;DR: This paper introduces a structural decomposition method to isolate intrinsic ambiguity from other uncertainty components in posterior inference for inverse problems, enabling better calibration diagnostics.


<details>
  <summary>Details</summary>
Motivation: Posterior uncertainty in deep generative models for inverse problems (like medical imaging) is difficult to interpret because it mixes intrinsic ambiguity with propagated inference uncertainty. Current methods often select models based on reconstruction quality alone, hiding important failure modes in uncertainty estimation.

Method: The authors propose a structural decomposition of posterior uncertainty that isolates intrinsic ambiguity inherent to the forward operator. They use a cascade formulation that makes this ambiguity accessible for calibration analysis, enabling qualitative diagnostics and simulation-based calibration tests.

Result: The method was validated on a Gaussian example with analytical posterior structure, demonstrated on accelerated MRI reconstruction, and applied to EEG source imaging, revealing failure modes that remain hidden when models are selected only by reconstruction quality.

Conclusion: The structural decomposition approach provides improved tools for understanding and calibrating posterior uncertainty in inverse problems, particularly important for high-stakes applications where uncertainty matters as much as predictions themselves.

Abstract: Recently, deep generative models have been used for posterior inference in inverse problems, including high-stakes applications in medical imaging and scientific discovery, where the uncertainty of a prediction can matter as much as the prediction itself. However, posterior uncertainty is difficult to interpret because it can mix ambiguity inherent to the forward operator with uncertainty propagated through inference. We introduce a structural decomposition of posterior uncertainty that isolates intrinsic ambiguity. A cascade formulation makes this ambiguity accessible for calibration analysis, enabling qualitative diagnostics and simulation-based calibration tests that reveal failure modes that remain hidden when models are selected by reconstruction quality alone. We first validate the approach on a Gaussian example with analytical posterior structure, then illustrate the decomposition on accelerated magnetic resonance imaging (MRI), and finally apply the calibration diagnostics to electroencephalography (EEG) source imaging.

</details>


### [242] [An Interpretable Latency Model for Speculative Decoding in LLM Serving](https://arxiv.org/abs/2605.15051)
*Linghao Kong,Megan Flynn,Michael Peng,Nir Shavit,Mark Kurtz,Alexandre Marques*

Main category: cs.LG

TL;DR: Analyzes speculative decoding in production LLM serving systems with a latency model that accounts for varying request loads and batch dynamics.


<details>
  <summary>Details</summary>
Motivation: Prior work shows SD speedups in isolated/fixed-batch settings, but real production serving has varying request loads where effective batch size emerges from the system rather than being directly controlled. Need to understand SD behavior in actual serving environments.

Method: Develops an interpretable latency model for SD in LLM serving: infers effective batch size from request rate using Little's Law, decomposes per-request demand into load-independent and load-dependent components for prefill, drafting, and verification. Validates with extensive vLLM measurements across various parameters.

Result: Model accurately describes observed latency, explains why speedups diminish with increased server load, characterizes how draft length/acceptance rate/verifier-drafter size shape latency across serving conditions. Extends framework to mixture of experts models. Provides implications for configuring SD in deployed systems.

Conclusion: Provides a structured framework for understanding speculative decoding in real LLM serving systems, bridging the gap between isolated benchmarks and production deployment realities.

Abstract: Speculative decoding (SD) accelerates large language model (LLM) inference by using a smaller draft model to propose multiple tokens that are verified by a larger target model in parallel. While prior work demonstrates substantial speedups in isolated or fixed-batch settings, the behavior of SD in production serving systems remains poorly understood: request load varies over time, and effective batch size emerges from the serving system rather than being directly controlled or observed. In this work, we develop a simple and interpretable latency model for SD in LLM serving. We infer effective batch size from request rate using Little's Law and decompose per-request demand into load-independent and load-dependent components for prefill, drafting, and verification. We validate our model using extensive measurements from vLLM across verifier and drafter model sizes, prefill and decode lengths, request rates, draft lengths, and acceptance probabilities. The model accurately describes observed latency, explains why speedups often diminish as server load increases, and characterizes how draft length, acceptance rate, and verifier-drafter size shape latency across serving conditions, with implications for configuring SD in deployed systems. We further show how the framework extends to mixture of experts models, where sparse expert activation changes the effective service costs across load regimes. Together, our results provide a structured framework for understanding SD in real LLM serving systems.

</details>


### [243] [TFGN: Task-Free, Replay-Free Continual Pre-Training Without Catastrophic Forgetting at LLM Scale](https://arxiv.org/abs/2605.15053)
*Anurup Ganguli*

Main category: cs.LG

TL;DR: TFGN is a novel architectural overlay for transformer language models that enables continual pre-training on heterogeneous text domains without replay or task labels, achieving minimal forgetting and positive forward transfer through input-conditioned parameter updates.


<details>
  <summary>Details</summary>
Motivation: The paper addresses the unsolved problem of continually pre-training large language models on heterogeneous text domains without replay buffers or task labels, which current methods handle poorly at LLM scale with their reliance on replay, task identifiers, or regularization penalties that don't scale well.

Method: TFGN introduces an architectural overlay for transformer language models that produces input-conditioned, parameter-efficient updates while leaving most of the transformer unchanged. It uses a Read/Write decomposition where the forward pass is fully dense, but cross-domain parameter updates are structured to avoid writing to prior-domain subspaces. The system also includes extensions: a closed-loop meta-control layer (Extension A) and an operator-level plan vector (Extension B).

Result: On six heterogeneous text domains across three model scales and two regimes, TFGN achieves minimal backward transfer (-0.007 at LLaMA 3.1 8B Retrofit), strong HellaSwag retention (0.506/0.504/0.510), and ≥99.59% L2-orthogonal gradient separation. It shows positive forward transfer with JavaScript PPL dropping 26.8-62.0% from Python training alone. Extension A reduces forgetting by 81% at ~398M scale, and Extension B achieves 99.96% cosine fidelity over 30 source-target pairs.

Conclusion: TFGN is the first architecture that simultaneously solves catastrophic forgetting at LLM scale without replay or task labels, implements a closed-loop autonomous-learning meta-controller, and carries an operator-level latent planner, representing a significant advancement in continual learning for large language models.

Abstract: Continually pre-training a large language model on heterogeneous text domains, without replay or task labels, has remained an unsolved architectural problem at LLM scale. Existing methods rely on replay buffers, task identifiers, regularization penalties that scale poorly, or sentence-classification-scale evaluation.
  We introduce TFGN, an architectural overlay for transformer language models that produces input-conditioned, parameter-efficient updates while leaving the rest of the transformer unchanged. On six heterogeneous text domains (Prose, Python, Math, Biomedical, Chinese, JavaScript) at 1B tokens per phase across three model scales (~398M, ~739M, ~9B) and two regimes (From-Scratch and Retrofit), TFGN achieves backward transfer of -0.007 at LLaMA 3.1 8B Retrofit, HellaSwag retention 0.506/0.504/0.510, and >=99.59% L2-orthogonal gradient separation between domain pairs - with no replay, no task IDs, no Fisher penalty.
  The same matrices show positive cross-domain forward transfer: held-out JavaScript PPL drops 26.8% at LLaMA-8B Retrofit and 62.0% at GPT-2 Medium From-Scratch purely from Python training. Two extensions on the same substrate close further open problems. A closed-loop meta-control layer (Extension A) reduces forgetting by an additional 81% at ~398M, mapping onto the System A and System M roles of Dupoux et al. (arXiv:2603.15381). An operator-level plan vector (Extension B) reshapes forward-pass behavior at 99.96% cosine fidelity over 30 source->target pairs.
  The architectural insight is a Read/Write decomposition: the forward pass is fully dense, while cross-domain parameter updates are structured so prior-domain subspaces are not written to. To our knowledge, TFGN is the first architecture that simultaneously closes catastrophic forgetting at LLM scale, realizes a closed-loop autonomous-learning meta-controller, and carries an operator-level latent planner.

</details>


### [244] [DiffusionOPD: A Unified Perspective of On-Policy Distillation in Diffusion Models](https://arxiv.org/abs/2605.15055)
*Quanhao Li,Junqiu Yu,Kaixun Jiang,Yujie Wei,Zhen Xing,Pandeng Li,Ruihang Chu,Shiwei Zhang,Yu Liu,Zuxuan Wu*

Main category: cs.LG

TL;DR: DiffusionOPD: Multi-task training for diffusion models using Online Policy Distillation to avoid cross-task interference and achieve state-of-the-art performance.


<details>
  <summary>Details</summary>
Motivation: Existing RL methods for diffusion models are limited to single-task optimization. Multi-task RL is challenging due to cross-task interference, imbalance in joint optimization, and catastrophic forgetting in cascade approaches.

Method: Proposes DiffusionOPD (Online Policy Distillation) framework: trains task-specific teachers independently, then distills their capabilities into a unified student along the student's own rollout trajectories. Lifts OPD from discrete tokens to continuous-state Markov processes, deriving closed-form per-step KL objective for both stochastic SDE and deterministic ODE refinement.

Result: Extensive experiments show DiffusionOPD surpasses both multi-reward RL and cascade RL baselines in training efficiency and final performance. Achieves state-of-the-art results on all evaluated benchmarks. Analytic gradient provides lower variance and better generality compared to conventional PPO-style policy gradients.

Conclusion: DiffusionOPD provides an effective multi-task training paradigm for diffusion models that avoids optimization burdens of joint training while preventing catastrophic forgetting, offering superior performance and efficiency across multiple tasks.

Abstract: Reinforcement learning has emerged as a powerful tool for improving diffusion-based text-to-image models, but existing methods are largely limited to single-task optimization. Extending RL to multiple tasks is challenging: joint optimization suffers from cross-task interference and imbalance, while cascade RL is cumbersome and prone to catastrophic forgetting. We propose DiffusionOPD, a new multi-task training paradigm for diffusion models based on Online Policy Distillation (OPD). DiffusionOPD first trains task-specific teachers independently, then distills their capabilities into a unified student along the student own rollout trajectories. This decouples single-task exploration from multi-task integration and avoids the optimization burden of solving all tasks jointly from scratch. Theoretically, we lift the OPD framework from discrete tokens to continuous-state Markov processes, deriving a closed-form per-step KL objective that unifies both stochastic SDE and deterministic ODE refinement via mean-matching. We formally and empirically demonstrate that this analytic gradient provides lower variance and better generality compared to conventional PPO-style policy gradients. Extensive experiments show that DiffusionOPD consistently surpasses both multi-reward RL and cascade RL baselines in training efficiency and final performance, while achieving state-of-the-art results on all evaluated benchmarks.

</details>


### [245] [Croissant Baker: Metadata Generation for Discoverable, Governable, and Reusable ML Datasets](https://arxiv.org/abs/2605.15079)
*Rafi Al Attrach,Rajna Fani,Sebastian Lobentanzer,Joan Giner-Miguelez,Debanshu Das,Varuni H. K.,Nobin Sarwar,Rajat Ghosh,Anwai Archit,Surbhi Motghare,Christina Conrad Parry,Luis Oala,Lara Grosso,Joaquin Vanschoren,Steffen Vogler,Sujata Goswami,Eric S. Rosenthal,Marzyeh Ghassemi,Matthew McDermott,Tom Pollard*

Main category: cs.LG

TL;DR: Croissant Baker is a local-first, open-source CLI tool that generates validated Croissant metadata directly from dataset directories, solving adoption barriers for governed/local datasets and achieving 97-100% agreement with ground truth across domains.


<details>
  <summary>Details</summary>
Motivation: Croissant has become the standard metadata format for ML datasets, but current generation methods require uploading data to public platforms first, which is infeasible for governed and large local repositories containing high-value data that ML increasingly relies on.

Method: Developed Croissant Baker, a local-first, open-source command-line tool that generates validated Croissant metadata directly from dataset directories through a modular handler registry. The tool works without requiring data uploads to public platforms.

Result: Successfully evaluated on over 140 datasets, scaling to large datasets like MIMIC-IV (886 million rows, 374 Parquet files). Achieved 97-100% agreement with producer-authored or standards-derived ground truth across multiple domains on held-out comparisons.

Conclusion: Croissant Baker effectively addresses the adoption barrier for Croissant metadata generation in governed and local repositories, enabling wider adoption of the metadata standard without compromising data governance requirements.

Abstract: Croissant has emerged as the metadata standard for machine learning datasets, providing a structured, JSON-LD-based format that makes dataset discovery, automated ingestion, and reproducible analysis machine-checkable across ML platforms. Adoption has accelerated, and NeurIPS now requires Croissant metadata in every submission to its dataset tracks. Yet in practice Croissant generation usually starts with uploading data to a public platform, a path infeasible for governed and large local repositories that hold much of the high-value data ML increasingly relies on. We release Croissant Baker, a local-first, open-source command-line tool that generates validated Croissant metadata directly from a dataset directory through a modular handler registry. We evaluate Croissant Baker on over 140 datasets, scaling to MIMIC-IV at 886 million rows and 374 Parquet files. On held-out comparisons against producer-authored or standards-derived ground truth, Croissant Baker reaches 97-100% agreement across multiple domains.

</details>


### [246] [Novel Dynamic Batch-Sensitive Adam Optimiser for Vehicular Accident Injury Severity Prediction](https://arxiv.org/abs/2605.15083)
*Daniel Asare Kyei,Alimatu Saadia-Yussiff,Maame G. Asante-Mensah,Abdul Lateef-Yussiff,Charles Roland Haruna,Derry Emmanuel*

Main category: cs.LG

TL;DR: Dynamic Batch-Sensitive Adam (DBS-Adam) is a novel optimizer that dynamically adjusts learning rates based on batch difficulty, improving performance on imbalanced sequential data, particularly for accident injury severity prediction using Bi-LSTM networks.


<details>
  <summary>Details</summary>
Motivation: Existing optimizers struggle with imbalanced and sequential datasets, limiting their ability to capture patterns in minority classes. This is particularly problematic for applications like accident injury severity prediction where class imbalance is common and affects training stability and convergence.

Method: Proposes DBS-Adam optimizer that scales learning rates dynamically using a batch difficulty score derived from exponential moving averages of gradient norms and batch loss. Applied to Bi-Directional LSTM networks for accident severity prediction, combined with SMOTE-ENN resampling and Focal Loss to address class imbalance. Four experimental configurations compare baseline models and alternative architectures.

Result: DBS-Adam outperforms state-of-the-art optimizers (AMSGrad, AdamW, AdaBound) with statistically significant precision improvements (p=0.020). Achieves 95.22% test accuracy, 96.11% precision, 95.28% recall, 95.39% F1-score, and test loss of 0.0086. Competitive performance validated across five random seeds.

Conclusion: DBS-Adam effectively handles imbalanced sequential data, demonstrating superior performance for accident severity classification. Enables real-time emergency response applications and provides a valuable optimization approach for challenging imbalanced learning scenarios.

Abstract: The choice of optimiser is important in deep learning, as it strongly influences model efficiency and speed of convergence. However, many commonly used optimisers encounter difficulties when applied to imbalanced and sequential datasets, limiting their ability to capture patterns of minority classes. In this study, we propose Dynamic Batch-Sensitive Adam (DBS-Adam), an optimiser that dynamically scales the learning rate using a batch difficulty score derived from exponential moving averages of gradient norms and batch loss. DBS-Adam improves training stability and accelerates convergence by increasing updates for difficult batches and reducing them for easier ones. We evaluate DBS-Adam by integrating it with Bi-Directional LSTM networks for accident injury severity prediction, addressing class imbalance through SMOTE-ENN resampling and Focal Loss. Four experimental configurations compare baseline Bi-LSTM models and alternative architectures to assess optimiser impact. Rigorous comparison against state-of-the-art optimisers (AMSGrad, AdamW, AdaBound) across five random seeds demonstrated DBS-Adam's competitive performance with statistically significant precision improvements (p=0.020). Results indicate that DBS-Adam outperforms standard optimisation approaches, achieving 95.22% test accuracy, 96.11% precision, 95.28% recall, 95.39% F1-score, and a test loss of 0.0086. The proposed framework enables effective real-time accident severity classification for targeted emergency response and road safety interventions, demonstrating the value of DBS-Adam for learning from imbalanced sequential data.

</details>


### [247] [Proposal and study of statistical features for string similarity computation and classification](https://arxiv.org/abs/2605.15110)
*E. O. Rodrigues,D. Casanova,M. Teixeira,V. Pegorini,F. Favarim,E. Clua,A. Conci,Panos Liatsis*

Main category: cs.LG

TL;DR: Co-occurrence matrix (COM) and run-length matrix (RLM) features from visual computing are adapted for string similarity computation, showing superior performance over traditional statistical measures like edit distances and longest common subsequence.


<details>
  <summary>Details</summary>
Motivation: There's a need for language-agnostic statistical features for string similarity computation that can work across different languages, grammatical structures, and contexts (words, phrases, codes, texts).

Method: Adapted co-occurrence matrix (COM) and run-length matrix (RLM) features from visual computing for string analysis. Compared these with traditional statistical measures including longest common subsequence, maximal consecutive longest common subsequence, mutual information, and edit distances through synthetic experiments and real text plagiarism dataset evaluation.

Result: In synthetic experiments, COM and RLM features outperformed other state-of-the-art statistical features. RLM features performed better than COM in 3 out of 4 cases with statistical significance (P-value < 0.001). On a real text plagiarism dataset, RLM features achieved the best results.

Conclusion: Visual computing features COM and RLM, when adapted for string similarity computation, provide effective language-agnostic statistical measures that outperform traditional statistical features, with RLM showing particularly strong performance in real-world text plagiarism detection.

Abstract: Adaptations of features commonly applied in the field of visual computing, co-occurrence matrix (COM) and run-length matrix (RLM), are proposed for the similarity computation of strings in general (words, phrases, codes and texts). The proposed features are not sensitive to language related information. These are purely statistical and can be used in any context with any language or grammatical structure. Other statistical measures that are commonly employed in the field such as longest common subsequence, maximal consecutive longest common subsequence, mutual information and edit distances are evaluated and compared. In the first synthetic set of experiments, the COM and RLM features outperform the remaining state-of-the-art statistical features. In 3 out of 4 cases, the RLM and COM features were statistically more significant than the second best group based on distances (P-value < 0.001). When it comes to a real text plagiarism dataset, the RLM features obtained the best results.

</details>


### [248] [Learning from Language Feedback via Variational Policy Distillation](https://arxiv.org/abs/2605.15113)
*Yang Li,Erik Nijkamp,Semih Yavuz,Shafiq Rayhan Joty*

Main category: cs.LG

TL;DR: VPD (Variational Policy Distillation) is a new framework that overcomes limitations of passive self-distillation in RLVR by formalizing learning from language feedback as a Variational EM problem, enabling co-evolution of student and teacher policies through adaptive refinement.


<details>
  <summary>Details</summary>
Motivation: RLVR suffers from sparse outcome signals causing exploration bottlenecks. Existing self-distillation methods use fixed passive teachers whose assessment capabilities plateau as students improve, halting further learning.

Method: Proposes Variational Policy Distillation (VPD), formalizing learning from language feedback as Variational Expectation-Maximization. E-step: teacher actively refined via adaptive trust-region update on trajectory outcomes. M-step: student internalizes dense distributional guidance on on-policy rollouts.

Result: VPD consistently outperforms standard RLVR and existing self-distillation baselines across diverse sources of diagnostic feedback on scientific reasoning and code generation tasks. Stress-testing shows fundamental bounds compared to pure environment-driven RL.

Conclusion: VPD overcomes limitations of passive distillation by continuously improving teachers' ability to extract actionable signals from textual critique, enabling more effective learning from language feedback in complex reasoning tasks.

Abstract: Reinforcement learning from verifiable rewards (RLVR) suffers from sparse outcome signals, creating severe exploration bottlenecks on complex reasoning tasks. Recent on-policy self-distillation methods attempt to address this by utilizing language feedback to generate dense, token-level supervision. However, these approaches rely on a fixed, passive teacher to interpret the feedback. As the student policy improves, the teacher's zero-shot assessment capabilities plateau, ultimately halting further learning. To overcome this, we propose Variational Policy Distillation (VPD), a framework that formalizes learning from language feedback as a Variational Expectation-Maximization (EM) problem. VPD co-evolves both policies: in the E-step, the teacher is actively refined on trajectory outcomes via an adaptive trust-region update, translating textual feedback into a dynamically improved target token distribution. In the M-step, the student internalizes this dense distributional guidance on its own on-policy rollouts. By continuously improving the teacher's ability to extract actionable signals from textual critique, VPD overcomes the limitations of passive distillation. Evaluated across diverse sources of diagnostic feedback on scientific reasoning and code generation tasks, VPD consistently outperforms both standard RLVR and existing self-distillation baselines. Finally, by stress-testing our framework on rigid mathematical reasoning and cold-start regimes, we illuminate the fundamental bounds of feedback-driven self-distillation compared to pure environment-driven RL.

</details>


### [249] [Natural Synthesis: Outperforming Reactive Synthesis Tools with Large Reasoning Models](https://arxiv.org/abs/2605.15131)
*Frederik Schmitt,Matthias Cosler,Niklas Metzger,Julian Siber,Vladimir Krsmanovic,Mohamed Ghanem,Bernd Finkbeiner*

Main category: cs.LG

TL;DR: A neuro-symbolic approach combining large language models with model checkers for reactive synthesis, plus natural language autoformalization to avoid manual specification writing.


<details>
  <summary>Details</summary>
Motivation: Reactive synthesis is challenging due to algorithmic hardness and difficulty of writing formal specifications manually. The paper aims to address both issues: improving synthesis algorithms and simplifying the specification process.

Method: Neuro-symbolic approach coupling large reasoning models with model checkers to iteratively repair synthesized Verilog implementations using symbolic feedback. Also introduces autoformalization that converts natural language specifications to formal ones.

Result: The approach solves more benchmarks than best dedicated tools in annual synthesis competition, extends to parameterized systems (undecidable problem), and shows comparable performance to starting from formal specifications.

Conclusion: Natural synthesis (from natural language to hardware) is a viable end-to-end workflow, addressing both algorithmic and specification challenges in reactive synthesis.

Abstract: Reactive synthesis, the problem of automatically constructing a hardware circuit from a logical specification, is a long-standing challenge in formal verification. It is elusive for two reasons: It is algorithmically hard, and writing formal specifications by hand is notoriously difficult. In this paper, we tackle both sides of the problem. For the algorithmic side, we present a neuro-symbolic approach to reactive synthesis that couples large reasoning models with model checkers to iteratively repair a synthesized Verilog implementation via sound symbolic feedback. Our approach solves more benchmarks than the best dedicated tools in the annual synthesis competition and extends to constructing parameterized systems, a problem known to be undecidable. On the specification side, we introduce an autoformalization step that shifts the specification task from temporal logic to natural language by introducing a hand-authored dataset of natural-language specifications for evaluation. We demonstrate performance comparable to that of starting from formal specifications, establishing natural synthesis as a viable end-to-end workflow.

</details>


### [250] [Causal Foundation Models with Continuous Treatments](https://arxiv.org/abs/2605.15133)
*Christopher Stith,Medha Barath,Vahid Balazadeh,Jesse C. Cresswell,Rahul G. Krishnan*

Main category: cs.LG

TL;DR: First causal foundation model for continuous treatments that meta-learns to predict causal effects across unseen tasks without fine-tuning, outperforming task-specific models.


<details>
  <summary>Details</summary>
Motivation: Causal inference for continuous treatments (where intervention variables have continuous range) is under-explored compared to binary treatments and represents a significant shift requiring models that can represent effects across treatment value continua.

Method: 1) Design novel prior over data-generating processes with continuous treatments to generate rich causal training corpus; 2) Train transformer to reconstruct individual treatment-response curves using observational data; 3) Leverage in-context learning to amortize Bayesian posterior inference.

Result: Model achieves state-of-the-art performance on individual treatment-response curve reconstruction tasks compared to causal models trained specifically for those tasks.

Conclusion: Successful creation of first causal foundation model for continuous treatment setting that can generalize across unseen tasks without additional training, representing significant advancement in causal inference for continuous interventions.

Abstract: Causal inference, estimating causal effects from observational data, is a fundamental tool in many disciplines. Of particular importance across a variety of domains is the continuous treatment setting, where the variable of intervention has a continuous range. This setting is far less explored and represents a substantial shift from the binary treatment setting, with models needing to represent effects across a continuum of treatment values. In this paper, we present the first causal foundation model for the continuous treatment setting. Our model meta-learns the ability to predict causal effects across a wide variety of unseen tasks without additional training or fine-tuning. First, we design a novel prior over data-generating processes with continuous treatment variables in order to generate a rich causal training corpus. We then train a transformer to reconstruct individual treatment-response curves given only observational data, leveraging in-context learning to amortize expensive Bayesian posterior inference. Our model achieves state-of-the-art performance on individual treatment-response curve reconstruction tasks compared to causal models which are trained specifically for those tasks.

</details>


### [251] [Training ML Models with Predictable Failures](https://arxiv.org/abs/2605.15134)
*Will Schwarzer,Scott Niekum*

Main category: cs.LG

TL;DR: ML deployment safety assessment: Shows how failure-rate forecasts from evaluation sets have a safety-favorable bias, identifies rare failure modes that cause under-prediction, and introduces forecastability loss fine-tuning to improve predictions while preserving task performance.


<details>
  <summary>Details</summary>
Motivation: Estimating ML model failure rates at deployment scale is crucial for safety assessment, but evaluation sets are often too small to observe rare failures that matter. Existing methods extrapolate from evaluation set failure scores but may miss rare failure modes present in deployment.

Method: Analyzes the failure-rate estimator from Jones et al. (2025) with a finite-k decomposition of forecast error. Shows it has inherent over-prediction bias (safety-favorable) but under-predicts when evaluation sets miss rare high-failure modes. Proposes "forecastability loss" as a fine-tuning objective to address this. Validates in two proof-of-concept experiments: language-model password game and RL gridworld.

Result: In experiments, fine-tuning with forecastability loss substantially reduces held-out forecast error while preserving primary-task capability. Achieves safety performance similar to supervised baselines.

Conclusion: The forecastability loss fine-tuning approach effectively addresses the under-prediction issue caused by rare failure modes missing from evaluation sets, improving deployment-scale failure rate predictions while maintaining model performance and safety comparable to supervised methods.

Abstract: Estimating how often an ML model will fail at deployment scale is central to pre-deployment safety assessment, but a feasible evaluation set is rarely large enough to observe the failures that matter. Jones et al. (2025) address this by extrapolating from the largest k failure scores in an evaluation set to predict deployment-scale failure rates. We give a finite-k decomposition of this estimator's forecast error and show that it has a built-in bias toward over-prediction in the typical case, which is the safety-favorable direction. This bias is offset when the evaluation set misses a rare high-failure mode that the deployment set contains, leaving the forecast to under-predict at deployment scale. We propose a fine-tuning objective, the forecastability loss, that addresses this failure mode. In two proof-of-concept experiments, a language-model password game and an RL gridworld, fine-tuning substantially reduces held-out forecast error while preserving primary-task capability and achieving safety similar to that of supervised baselines.

</details>


### [252] [Forgetting That Sticks: Quantization-Permanent Unlearning via Circuit Attribution](https://arxiv.org/abs/2605.15138)
*Saisab Sadhu,Pratinav Seth,Vinay Kumar Sankarapu*

Main category: cs.LG

TL;DR: Researchers identify that standard unlearning methods fail when models are quantized, showing it's a systematic dual failure: gradient methods lose forgetting under quantization, while quantization-resistant methods don't forget meaningfully. They propose MANSU, which isolates causal circuits and ensures quantization survival, along with a new verification metric called CAD.


<details>
  <summary>Details</summary>
Motivation: Current machine unlearning evaluations ignore the reality that deployed language models are always quantized first. Recent work shows 4-bit post-training quantization can reverse unlearning effects, but this was dismissed as a tuning artifact. The authors show this is actually a systematic failure affecting all existing methods.

Method: MANSU (Mechanistic-Aligned Null-Space Unlearning) combines three key components: 1) causal circuit attribution to identify minimal forget-set subgraphs, 2) circuit-restricted null-space projection with diagonal-Fisher retain bound, and 3) per-parameter magnitude floor that guarantees quantization survival by construction. Also introduces Circuit Attribution Divergence (CAD) as a mechanistic verification metric.

Result: MANSU is the first method to jointly satisfy all four key properties: meaningful forgetting, retain preservation, non-positive PTQ gap (quantization doesn't reverse forgetting), and structural erasure. In contrast, gradient-based baselines recover up to +0.05 accuracy under compression, showing they lose their unlearning effects when quantized.

Conclusion: The paper identifies a fundamental sparsity-permanence tradeoff in unlearning: gradient updates are too small to survive quantization. MANSU solves this by isolating causal circuits and ensuring parameter changes exceed quantization bin boundaries, making it the first method to achieve both effective unlearning and quantization survival.

Abstract: Standard unlearning evaluations measure behavioral suppression in full precision, immediately after training, despite every deployed language model being quantized first. Recent work has shown that 4-bit post-training quantization can reverse machine unlearning; we show this is not a tuning artefact but a systematic dual failure: gradient-based methods that achieve meaningful forgetting lose it under compression, while methods that survive quantization barely change the model. Both failures trace to the same root cause: across all baselines, per-parameter updates lie 47-828x below the NF4 quantization bin width; updates diffused across billions of parameters cannot clear quantization bin boundaries, a consequence we formalize as a sparsity-permanence tradeoff. We present MANSU (Mechanistic-Aligned Null-Space Unlearning), which resolves both modes by combining causal circuit attribution to isolate the minimal forget-set subgraph, circuit-restricted null-space projection with a diagonal-Fisher retain bound, and a per-parameter magnitude floor guaranteeing quantization survival by construction. We additionally introduce Circuit Attribution Divergence (CAD), a mechanistic verification metric distinguishing structural erasure from behavioral suppression, a distinction existing metrics cannot make. Across multiple model families and hazard benchmarks, MANSU is the first method to jointly satisfy all four properties with margin on each (meaningful forgetting, retain preservation, non-positive PTQ gap, and structural erasure), while gradient-based baselines recover up to +0.05 accuracy under compression.

</details>


### [253] [Widening the Gap: Exploiting LLM Quantization via Outlier Injection](https://arxiv.org/abs/2605.15152)
*Xiaohua Zhan,Kazuki Egashira,Robin Staab,Mark Vero,Martin Vechev*

Main category: cs.LG

TL;DR: This paper introduces the first successful quantization-conditioned attack that works against advanced LLM quantization methods like AWQ, GPTQ, and GGUF I-quants by exploiting outlier-induced weight collapse.


<details>
  <summary>Details</summary>
Motivation: While LLM quantization is essential for memory-efficient deployment, recent work shows quantization schemes pose security risks where models appear benign in full precision but become malicious after quantization. However, existing attacks only work on simple quantization methods and fail against popular, sophisticated schemes, limiting their practical impact.

Method: The attack exploits a property common to many modern quantization methods: large outliers can cause other weights to be rounded to zero. By injecting outliers into specific weight blocks, attackers can induce targeted, predictable weight collapse. This allows crafting seemingly benign full-precision models that exhibit malicious behaviors after quantization.

Result: The attack achieves high success rates against a broad range of advanced quantization methods (including AWQ, GPTQ, and GGUF I-quants) where prior attacks consistently fail. Extensive evaluation across three attack scenarios and LLMs demonstrates consistent effectiveness.

Conclusion: This work demonstrates for the first time that security risks of quantization are not limited to simple schemes but are broadly relevant across complex, widely-used quantization methods, significantly expanding the practical impact of quantization-conditioned attacks.

Abstract: LLM quantization has become essential for memory-efficient deployment. Recent work has shown that quantization schemes can pose critical security risks: an adversary may release a model that appears benign in full precision but exhibits malicious behavior once quantized by users. However, existing quantization-conditioned attacks have been limited to relatively simple quantization methods, where the attacker can estimate weight regions that remain invariant under the target quantization. Notably, prior attacks have consistently failed to compromise more popular and sophisticated schemes, limiting their practical impact. In this work, we introduce the first quantization-conditioned attack that consistently induces malicious behavior that can be triggered by a broad range of advanced quantization techniques, including AWQ, GPTQ, and GGUF I-quants. Our attack exploits a simple property shared by many modern quantization methods: large outliers can cause other weights to be rounded to zero. Consequently, by injecting outliers into specific weight blocks, an adversary can therefore induce a targeted, predictable weight collapse in the model. This effect can be used to craft seemingly benign full-precision models that exhibit a wide range of malicious behaviors after quantization. Through extensive evaluation across three attack scenarios and LLMs, we show that our attack achieves high success rates against a broad range of quantization methods on which prior attacks fail. Our results demonstrate, for the first time, that the security risks of quantization are not restricted to simpler schemes but are broadly relevant across complex, widely-used quantization methods.

</details>


### [254] [Self-Distilled Agentic Reinforcement Learning](https://arxiv.org/abs/2605.15155)
*Zhengxi Lu,Zhiyuan Yao,Zhuowen Han,Zi-Han Wang,Jinyang Wu,Qi Gu,Xunliang Cai,Weiming Lu,Jun Xiao,Yueting Zhuang,Yongliang Shen*

Main category: cs.LG

TL;DR: SDAR combines RL with gated self-distillation to improve LLM agents by using token-level guidance while mitigating multi-turn instability through adaptive gating of teacher signals.


<details>
  <summary>Details</summary>
Motivation: Reinforcement learning for LLM agents provides only coarse trajectory-level supervision, while naive self-distillation suffers from multi-turn instability and problems with negative teacher rejections in skill-conditioned privileged guidance.

Method: SDAR (Self-Distilled Agentic Reinforcement Learning) treats On-Policy Self-Distillation (OPSD) as a gated auxiliary objective while keeping RL as primary optimization. It maps detached token-level signals into a sigmoid gate that strengthens distillation on teacher-endorsed positive-gap tokens and softly attenuates negative teacher rejections.

Result: SDAR substantially improves over GRPO (+9.4% on ALFWorld, +7.0% on Search-QA, +10.2% on WebShop-Acc), avoids instability of naive GRPO+OPSD, and consistently outperforms hybrid RL--OPSD baselines across model scales on Qwen2.5 and Qwen3 families.

Conclusion: Gating mechanism effectively stabilizes multi-turn distillation while maintaining RL's core optimization, enabling reliable transfer of dense token-level supervision to agentic settings without destabilizing the training process.

Abstract: Reinforcement learning (RL) has emerged as a central paradigm for post-training LLM agents, yet its trajectory-level reward signal provides only coarse supervision for long-horizon interaction. On-Policy Self-Distillation (OPSD) complements RL by introducing dense token-level guidance from a teacher branch augmented with privileged context. However, transferring OPSD to multi-turn agents proves problematic: compounding multi-turn instability destabilizes supervision, while skill-conditioned privileged guidance requires asymmetric treatment for negative teacher rejections may arise from imperfect skills retrieval or utilization. We introduce SDAR (Self-Distilled Agentic Reinforcement Learning), which treats OPSD as a gated auxiliary objective while keeping RL as the primary optimization backbone. SDAR maps detached token-level signals into a sigmoid gate, strengthening distillation on teacher-endorsed positive-gap tokens and softly attenuating negative teacher rejections. Across the Qwen2.5 and Qwen3 families on ALFWorld, WebShop, and Search-QA, SDAR substantially improves over GRPO (+9.4% on ALFWorld, +7.0% on Search-QA, +10.2% on WebShop-Acc), avoids the instability of naive GRPO+OPSD, and consistently outperforms hybrid RL--OPSD baselines across model scales.

</details>


### [255] [Position: Behavioural Assurance Cannot Verify the Safety Claims Governance Now Demands](https://arxiv.org/abs/2605.15164)
*Pratinav Seth,Vinay Kumar Sankarapu*

Main category: cs.LG

TL;DR: Current AI behavioral assurance methods can't verify critical safety properties required by governance frameworks, creating an "audit gap" where safety claims are unsupported by evidence; the paper proposes technical solutions to address this.


<details>
  <summary>Details</summary>
Motivation: AI governance frameworks (2019-2026) require verification of safety properties like absence of hidden objectives and bounded catastrophic capability, but current behavioral assurance methods (evaluations, red-teaming) are epistemically limited to observable outputs and can't verify latent representations or long-horizon behaviors.

Method: The paper formalizes the "audit gap" (divergence between required and achievable verification access), introduces "fragile assurance" concept, analyzes a 21-instrument inventory to identify incentive structures favoring surface-level proxies, and proposes technical solutions including bounding behavioral evidence weight and adding mechanistic-evidence classes.

Result: Identified a systematic incentive gradient where geopolitical and industrial pressures reward surface-level behavioral proxies over deep structural verification, creating fragile assurance situations where evidence doesn't support safety claims.

Conclusion: Behavioral assurance alone is insufficient for modern AI safety governance; a technical pivot is needed to include mechanistic-evidence approaches (linear probes, activation patching, before/after-training comparisons) and limit reliance on behavioral evidence in legal frameworks.

Abstract: This position paper argues that behavioural assurance, even when carefully designed, is being asked to carry safety claims it cannot verify. AI governance frameworks enacted between 2019 and early 2026 require reviewable evidence of properties such as the absence of hidden objectives, resistance to loss-of-control precursors, and bounded catastrophic capability; current assurance methodologies (primarily behavioural evaluations and red-teaming) are epistemically limited to observable model outputs and cannot verify the latent representations or long-horizon agentic behaviours these frameworks presume to regulate. We formalize this structural mismatch as the audit gap, the divergence between required and achievable verification access, and introduce the concept of fragile assurance to describe cases where the evidential structure does not support the asserted safety claim. Through an analysis of a 21-instrument inventory, we identify an incentive gradient where geopolitical and industrial pressures systematically reward surface-level behavioral proxies over deep structural verification. Finally, we propose a technical pivot: bounding the weight of behavioral evidence in legal text and extending voluntary pre-deployment access with mechanistic-evidence classes, specifically linear probes, activation patching, and before/after-training comparisons.

</details>


### [256] [Eradicating Negative Transfer in Multi-Physics Foundation Models via Sparse Mixture-of-Experts Routing](https://arxiv.org/abs/2605.15179)
*Ellwil Sharma,Arastu Sharma*

Main category: cs.LG

TL;DR: Shodh-MoE: A sparse Mixture of Experts architecture for multi-physics SciML that prevents negative transfer between incompatible PDE regimes like open-channel flows and porous media by routing different physical domains to specialized experts while maintaining shared experts for universal symmetries.


<details>
  <summary>Details</summary>
Motivation: Scaling Scientific Machine Learning toward universal foundation models is bottlenecked by negative transfer - co-training disparate PDE regimes causes gradient conflict, unstable optimization, and plasticity loss in dense neural operators, especially when broadband open-channel fluid dynamics and boundary-dominated porous media flows impose incompatible spectral and geometric demands.

Method: Introduces Shodh-MoE, a sparse-activated latent transformer with physics-informed autoencoder compression to 16^3 latents using Helmholtz-style velocity parameterization for divergence-free manifolds. Features Top-1 soft-semantic router that dynamically assigns localized latent patches to expert subnetworks, creating specialized parameter paths for distinct physics while preserving shared experts for universal symmetries.

Result: Achieves exact mass conservation with divergence ~2.8×10⁻¹⁰ on 128³ grids. In distributed pretraining over 20,000 steps with mixed 3D physical tensors, routing shows autonomous domain bifurcation: open-channel tokens route exclusively to Expert 0, porous-media tokens to Expert 1. Converges simultaneously across both regimes with latent validation MSEs of 2.46×10⁻⁵ and 9.76×10⁻⁶, and decoded physical MSEs of 2.48×10⁻⁶ and 1.76×10⁻⁶.

Conclusion: Sparse expert routing is a practical architectural mechanism for mitigating multi-physics interference in universal neural operators, enabling simultaneous convergence across incompatible physical regimes without negative transfer.

Abstract: Scaling Scientific Machine Learning (SciML) toward universal foundation models is bottlenecked by negative transfer: the simultaneous co-training of disparate partial differential equation (PDE) regimes can induce gradient conflict, unstable optimization, and plasticity loss in dense neural operators. In particular, broadband open-channel fluid dynamics and boundary-dominated porous media flows impose incompatible spectral and geometric demands on a single dense parameter path. We introduce Shodh-MoE, a sparse-activated latent transformer architecture for multi-physics transport. Shodh-MoE operates on compressed 16^3 physical latents produced by a physics-informed autoencoder with an intra-tokenizer Helmholtz-style velocity parameterization, restricting decoded states to divergence-free velocity manifolds. The model guarantees exact mass conservation, achieving a physically verifiable velocity divergence of ~2.8 x 10^-10 (evaluated post-hoc in FP64) on 128^3 grids. A Top-1 soft-semantic router dynamically assigns localized latent patches to expert subnetworks, enabling specialized parameter paths for distinct physical mechanisms while preserving shared experts for universal symmetries. In a 20,000-step distributed pretraining run over mixed three-dimensional physical tensors, routing telemetry shows autonomous domain bifurcation: held-out validation tokens from the open-channel domain route exclusively to Expert 0, while porous-media tokens route exclusively to Expert 1. The model converges simultaneously across both regimes, achieving latent validation MSEs of 2.46 x 10^-5 and 9.76 x 10^-6, and decoded physical MSEs of 2.48 x 10^-6 and 1.76 x 10^-6. These results support sparse expert routing as a practical architectural mechanism for mitigating multi-physics interference in universal neural operators.

</details>


### [257] [When Are Two Networks the Same? Tensor Similarity for Mechanistic Interpretability](https://arxiv.org/abs/2605.15183)
*ML Nissen Gonzalez,Melwina Albuquerque,Laurence Wroe,Jacob Meyer Cohen,Logan Riggs Smith,Thomas Dooms*

Main category: cs.LG

TL;DR: Introduces tensor similarity, a weight-based metric for comparing neural network components that is invariant to weight-space symmetries and captures functional equivalence.


<details>
  <summary>Details</summary>
Motivation: Existing similarity measures either rely on empirical behavior (blind to out-of-distribution mechanisms) or basis-dependent parameters (disregard weight-space symmetries), creating limitations for verifying that two model parts implement the same computation.

Method: Proposes tensor similarity, a weight-based metric for tensor-based models that is invariant to weight-space symmetries. It uses an efficient recursive algorithm to capture global functional equivalence and account for cross-layer mechanisms.

Result: Empirically, tensor similarity tracks functional training dynamics (like grokking and backdoor insertion) with higher fidelity than existing metrics. It reduces similarity measurement and faithfulness verification to a solved algebraic problem rather than empirical approximation.

Conclusion: Tensor similarity provides a principled solution for comparing model components in mechanistic interpretability, addressing key limitations of existing approaches by being both symmetry-invariant and capable of capturing functional equivalence beyond empirical distributions.

Abstract: Mechanistic interpretability aims to break models into meaningful parts; verifying that two such parts implement the same computation is a prerequisite. Existing similarity measures evaluate either empirical behaviour, leaving them blind to out-of-distribution mechanisms, or basis-dependent parameters, meaning they disregard weight-space symmetries. To address these issues for the class of tensor-based models, we introduce a weight-based metric, tensor similarity, that is invariant to such symmetries. This metric captures global functional equivalence and accounts for cross-layer mechanisms using an efficient recursive algorithm. Empirically, tensor similarity tracks functional training dynamics, such as grokking and backdoor insertion, with higher fidelity than existing metrics. This reduces measuring similarity and verifying faithfulness into a solved algebraic problem rather than one of empirical approximation.

</details>


### [258] [FutureSim: Replaying World Events to Evaluate Adaptive Agents](https://arxiv.org/abs/2605.15188)
*Shashwat Goel,Nikhil Chandak,Arvindh Arun,Ameya Prabhu,Steffen Staab,Moritz Hardt,Maksym Andriushchenko,Jonas Geiping*

Main category: cs.LG

TL;DR: FutureSim is a grounded simulation benchmark that replays real-world events chronologically to evaluate AI agents' ability to forecast future events and adapt to new information over long time horizons.


<details>
  <summary>Details</summary>
Motivation: AI agents are increasingly deployed in dynamic, open-ended environments that require adaptation to new information, but current benchmarks don't adequately measure this capability for realistic use-cases.

Method: Built FutureSim, a simulation that replays real-world events in chronological order using real news articles and question resolutions over a simulated period. Agents are evaluated on their ability to forecast world events beyond their knowledge cutoff (January-March 2026) while interacting with the chronological replay.

Result: FutureSim reveals clear capability separation among frontier agents, with the best agent achieving only 25% accuracy, and many performing worse than making no predictions at all (negative Brier skill score). Ablation studies show FutureSim provides a realistic setting to study emerging research directions.

Conclusion: FutureSim's benchmark design paves the way to measure AI progress on open-ended adaptation spanning long time-horizons in the real world, offering a realistic testbed for studying adaptation, search, memory, and uncertainty reasoning.

Abstract: AI agents are being increasingly deployed in dynamic, open-ended environments that require adapting to new information as it arrives. To efficiently measure this capability for realistic use-cases, we propose building grounded simulations that replay real-world events in the order they occurred. We build FutureSim, where agents forecast world events beyond their knowledge cutoff while interacting with a chronological replay of the world: real news articles arriving and questions resolving over the simulated period. We evaluate frontier agents in their native harness, testing their ability to predict world events over a three-month period from January to March 2026. FutureSim reveals a clear separation in their capabilities, with the best agent's accuracy being 25%, and many having worse Brier skill score than making no prediction at all. Through careful ablations, we show how FutureSim offers a realistic setting to study emerging research directions like long-horizon test-time adaptation, search, memory, and reasoning about uncertainty. Overall, we hope our benchmark design paves the way to measure AI progress on open-ended adaptation spanning long time-horizons in the real world.

</details>


<div id='cs.MA'></div>

# cs.MA [[Back]](#toc)

### [259] [Ready from Day 1: Population-Aware Coordination for Large-Scale Constrained Multi-Agent Systems](https://arxiv.org/abs/2605.13900)
*Angel Wang,Dominique Perrault-Joncas,Alvaro Maggiar,Carson Eisenach,Dean Foster*

Main category: cs.MA

TL;DR: Learned population-aware coordination interfaces enable efficient planning in large-scale multi-agent systems by predicting resource utilization and cost responses conditioned on population summaries, remaining reliable across evolving populations without retraining.


<details>
  <summary>Details</summary>
Motivation: In large-scale multi-agent systems with shared resource constraints, planners need to evaluate candidate resource plans iteratively but face challenges with population composition changes across planning cycles that affect response maps.

Method: Propose population-aware coordination interfaces: learned primal and dual maps conditioned on compact population summaries. Primal map predicts aggregate utilization under proposed cost trajectories; dual map predicts cost trajectories for target plans. These maps encode response-relevant population structure and support coordination from compact subsamples.

Result: In supply-chain capacity-control case study: 16-19% reduction in forecast error and 20-51% reduction in capacity violations compared to population-unaware baselines; 20K-agent cohorts accurately coordinate 500K-agent populations; simulator-trained primal maps achieve 11.1% MAPE on real observations vs 13-24% for baselines.

Conclusion: Population-aware coordination interfaces enable reliable, efficient planning across evolving populations without per-cycle retraining, with demonstrated effectiveness in supply-chain applications and support for Sim2Real transfer evaluation.

Abstract: In large-scale multi-agent systems with shared resource constraints, an upstream planner must iteratively evaluate candidate resource plans -- assessing feasibility, aggregate response, and marginal cost -- before committing to one. Lagrangian relaxation separates local decisions through a broadcast cost signal, but the planner still needs the cost-to-utilization response map to explore plan space, and this map depends on population composition that changes across planning cycles. We propose \emph{population-aware coordination interfaces}: learned primal and dual maps, conditioned on compact population summaries, that the planner queries inside its iterative loop. The primal map predicts aggregate utilization under a proposed cost trajectory; the dual map predicts the cost trajectory for a target plan. By encoding response-relevant population structure, these maps remain reliable across evolving populations without per-cycle retraining, and support coordination of large populations from compact subsamples. We additionally cast Sim2Real transfer as a backtestable procedure, enabling evaluation before deployment. In a supply-chain capacity-control case study, population-aware interfaces reduce forecast error by 16--19\% and capacity violations by 20--51\% relative to population-unaware baselines under composition shift; 20K-agent cohorts support accurate coordination of 500K-agent populations; and simulator-trained primal maps achieve 11.1\% MAPE on real observations versus 13--24\% for baselines.

</details>


### [260] [Privacy Preserving Multi Agent Path Finding](https://arxiv.org/abs/2605.14119)
*Rotem Lev Lehman,Roni Stern,Guy Shani*

Main category: cs.MA

TL;DR: A privacy-preserving framework for multi-agent path finding that addresses both planning-level privacy (using mock agents) and execution-level privacy (adapting existing algorithms), with a post-processing technique to reduce solution costs.


<details>
  <summary>Details</summary>
Motivation: In MAPF applications, while agents must avoid collisions, they also need privacy constraints where they don't want to share their paths. Some agents have privacy requirements during planning or execution, especially with limited sensing capabilities.

Method: 1) For planning-level privacy: proposed a general framework using mock agents during planning to obscure actual agent locations. 2) For execution-level privacy: adapted existing MAPF algorithms (PIBT and LaCAM) to preserve privacy when agents have limited sensing. 3) Developed a post-processing technique to optimize solution costs without compromising privacy.

Result: Implemented the proposed algorithms and evaluated them empirically. The post-processing technique significantly improved solution costs while maintaining privacy constraints for both planning and execution levels.

Conclusion: The paper successfully formulates and addresses privacy concerns in MAPF through both planning-level and execution-level privacy preservation, with practical algorithms that can be integrated into existing MAPF frameworks while optimizing solution quality.

Abstract: In the multi-agent path finding (MAPF) problem, a group of agents search in a graph for a path for each agent where no two paths collide. While in all applications of MAPF the agents must not collide with each other, in some of them the agents may not wish to share their paths due to privacy constraints. In this work, we formulate two types of privacy constraints for MAPF and propose algorithms that preserve them. The first type of privacy we consider is planning-level privacy, which means that during planning, the agents cannot identify exactly the planned location of the other agents. We propose a general framework for obtaining planning-level privacy, which works by adding mock agents to the planning process. The second type of privacy we consider is execution-level privacy, which is relevant when agents have limited sensing capabilities. Execution-level privacy is preserved if none of the agents is allowed to sense the location of the other agents during execution. We show how to adapt two popular MAPF algorithms, namely PIBT and LaCAM, such that they preserve execution-level privacy. Lastly, we propose a post-processing technique that allows the agents to reduce the sum of costs of the returned solution without losing any privacy. We also implemented our algorithms and evaluated them empirically, showing that the proposed post-processing technique indeed improved cost significantly.

</details>


### [261] [IFPV: An Integrated Multi-Agent Framework for Generative Operational Planning and High-Fidelity Plan Verification](https://arxiv.org/abs/2605.14851)
*Zhigao Huang,Zhengqing Hu,Dong Chen,Shaohan Zhang,Zhao Jin,Bo Zhang,Han Wu,Mingliang Xu*

Main category: cs.MA

TL;DR: An integrated multi-agent framework called IFPV that combines generative operational planning with high-fidelity adversarial verification for military battlefield environments.


<details>
  <summary>Details</summary>
Motivation: Traditional operational planning methods face challenges of generation infeasibility, while verification methods suffer from insufficiency in rapidly changing battlefield environments.

Method: Proposes IFPV with two tightly coupled modules: Multi-Perspective Hierarchical Agents (MPHA) for generative planning using Pathfinder, Analyst, and Planner agents, and Adversarial Cognitive Simulation Engine (ACSE) for verification with an opponent using customized world models for dynamic counteractions.

Result: IFPV improves mission success by 19.4% and reduces operational cost by 41.7% compared to single-step LLM planning baseline. ACSE increases average suppression rate by 31.8% compared to traditional rule-based validators, showing stricter verification environment.

Conclusion: The integrated framework effectively addresses limitations of traditional planning and verification methods through multi-agent collaboration and adversarial simulation, providing more robust operational planning solutions for complex battlefield environments.

Abstract: Operational plan generation and verification are critical for modern complex and rapidly changing battlefield environments, yet traditional generation and verification methods still respectively face the challenges of generation infeasibility and verification insufficiency. To alleviate these limitations, we propose an Integrated Multi-Agent Framework for Generative Operational Planning and High-Fidelity Plan Verification (IFPV). IFPV consists of two tightly coupled modules: Multi-Perspective Hierarchical Agents (MPHA) for generative operational planning and an Adversarial Cognitive Simulation Engine (ACSE) for high-fidelity adversarial plan verification. MPHA decomposes commander intent into executable multi-platform tactical action sequences through the collaboration of Pathfinder, Analyst, and Planner agents. ACSE introduces an opponent equipped with a customized world model, which predicts the future evolution of mission-critical platforms and conducts dynamic counteractions against candidate plans. Simulation experiments in the Asymmetric Combat Tactic Simulator (ACTS) show that IFPV improves mission success by 19.4% and reduces operational cost by 41.7% compared with a single-step large language model (LLM) planning baseline. Compared with a traditional rule-based validator, ACSE increases the average suppression rate by 31.8%, indicating that the proposed verification environment is stricter and more discriminative in revealing the latent vulnerabilities of candidate plans. The code for IFPV can be found at https://github.com/zhigao3ks/IFPV.

</details>


### [262] [Decision-Level Fusion for Robust Wearable Affect Recognition](https://arxiv.org/abs/2605.14878)
*Lokesh Singh,Athina Georgara,Jayati Deshmukh,Tan Viet Tuyen Nguyen,Sarvapali D. Ramchurn*

Main category: cs.MA

TL;DR: Proposed a non-stationary pipeline combining Fourier-Bessel Series Expansion with Empirical Wavelet Transform for robust affective state recognition from wearable physiology, showing decision-level aggregation outperforms feature-level aggregation for heterogeneous sensor data.


<details>
  <summary>Details</summary>
Motivation: Automatic affective state recognition from wearable physiology has important applications in public health and preventive care, but real deployments face challenges with non-stationary dynamics, artefacts, missing sensors, and oversmoothing of short-lived discriminative patterns by traditional spectral methods.

Method: Proposed a non-stationary pipeline combining Fourier-Bessel Series Expansion (FBSE) with Empirical Wavelet Transform (EWT) for data-driven spectral segmentation to extract mode-wise transient descriptors. Used decision-level aggregation over per-modality predictors weighted by predictive uncertainty and modality reliability, rather than feature-level aggregation.

Result: On the WESAD dataset (15 subjects, ECG, EDA, BVP, EMG, ACC signals across baseline/stress/amusement), decision-level aggregation was approximately 84% of the time at least as good as feature-level aggregation, and approximately 48% of the time strictly better, showing improved robustness under heterogeneous and partially reliable sensing.

Conclusion: Decision-level aggregation with uncertainty and reliability weighting provides more robust affective state recognition from wearable physiology in real-world conditions with non-stationary dynamics and imperfect sensors, outperforming traditional feature-level aggregation approaches.

Abstract: Automatic recognition of affective state from wearable physiology has clear societal impact for public health, preventive care, and stress-aware interventions, but real deployments require robustness to non-stationary dynamics, artefacts, and missing sensors. We study this problem on WESAD, using baseline, stress, and amusement conditions, where common fixed-basis spectral features such as FFT bandpower and Welch PSD can oversmooth short-lived discriminative patterns. We propose a non-stationary pipeline that combines Fourier-Bessel Series Expansion (FBSE) with EWT data-driven spectral segmentation to extract mode-wise transient descriptors. For multimodal integration, we adopt decision-level aggregation over per-modality predictors and weight each modality by predictive uncertainty and modality reliability. Results on WESAD, using 15 subjects and ECG, EDA, BVP, EMG, and ACC signals across three classes, indicate that decision-level aggregation is approximately 84 percent of the time at least as good as feature-level aggregation, and approximately 48 percent of the time strictly better, suggesting improved robustness under heterogeneous and partially reliable sensing.

</details>


### [263] [Temporal Fair Division in Multi-Agent Systems: From Precise Alternation Metrics to Scalable Coordination Proxies](https://arxiv.org/abs/2605.14879)
*Nikolaos Al. Papadopoulos*

Main category: cs.MA

TL;DR: The paper introduces Rotational Periodicity (RP) and ALT families of temporal fairness metrics for repeated multi-agent resource competition, showing they outperform traditional measures by revealing coordination failures while being computationally efficient.


<details>
  <summary>Details</summary>
Motivation: Real-world environments often require agents to repeatedly compete for limited resources, necessitating temporal fairness measures that evaluate entire interaction histories rather than just single episodes. Traditional fairness metrics fail to capture these temporal dynamics.

Method: The authors formalize the Multi-Agent Battle of the Exes (MBoE) as a repeated fair division problem, establish Perfect Alternation as the canonical solution, and introduce Rotational Periodicity metrics with Rotational Score and Waiting Periods Evaluation components. They compare RP against ALT sliding-window measures.

Result: Empirical evaluation shows: 1) Both RP and ALT reveal coordination failures invisible to traditional metrics (Q-learning performs worse than random by 10-73% on RP), 2) RP achieves 12-25x computational speedup over ALT, 3) The metrics are complementary - ALT works better for small populations while RP scales better for larger ones.

Conclusion: The RP and ALT families form a comprehensive diagnostic toolkit for temporal fair division, with RP providing lightweight efficiency and ALT offering richer discrimination capabilities, together addressing the limitations of traditional fairness metrics in repeated resource competition scenarios.

Abstract: A plethora real-world environments require agents to compete repeatedly for the same limited resource, calling for a temporal notion of fairness judged across entire interaction histories. This paper advances the theory of temporal fair division by introducing Rotational Periodicity (RP), a family of lightweight metrics, alongside the ALT family of sliding-window measures, within a unified framework for repeated multi-agent resource competition.
  We formalise the Multi-Agent Battle of the Exes (MBoE) as a repeated fair division instance and establish Perfect Alternation (PA) as its canonical temporally fair solution, drawing connections to proportionality, envy-freeness, and n-periodic round-robin allocation. RP decomposes temporal fairness into two complementary sub-measures: Rotational Score (RS) and Waiting Periods Evaluation (WPE), achieving O(nu+n) time complexity versus the O(nu*n) of ALT, where nu is the episode count and n the agent count.
  Empirical evaluation across n in {2,3,5,8,10} reveals three findings. First, both RP and ALT expose a coordination failure invisible to traditional metrics: Q-learning agents perform worse than random policies by 10-73% on RP and 7-35% on CALT, while Reward Fairness remains misleadingly high (above 0.92 for n>=3). Second, RP achieves 12-25x computational speedup over ALT, growing with n. Third, the two families are complementary: ALT provides richer discrimination for small populations; RP scales reliably where ALT becomes intractable. Together they form a diagnostic toolkit for temporal fair division.

</details>


### [264] [Multi-Agentic Approach for History Matching of Oil Reservoirs](https://arxiv.org/abs/2605.15028)
*Linar Samigullin,Sergei Shumilin,Evgeny Burnaev*

Main category: cs.MA

TL;DR: PetroGraph is a multi-agent AI framework that automates reservoir history matching workflows by decomposing tasks into specialized agents with LLMs and domain tools, achieving significant error reduction on benchmark models.


<details>
  <summary>Details</summary>
Motivation: Automated history matching in reservoir engineering remains difficult in practice despite potential benefits, requiring engineers to manually configure complex heterogeneous workflows involving parameter selection, optimization tuning, simulator execution, and diagnostics.

Method: PetroGraph decomposes history matching workflow into specialized agents for model review, experimental planning, parameterization, optimization, simulation, and summarization. It combines LLM agents with domain-specific tools, retrieval-augmented simulator documentation access, ECLIPSE input deck validation, human-in-the-loop checkpoints, and an OPM Flow-based simulation backend.

Result: On three reservoir models of increasing complexity: PetroGraph reduces mismatch by 95% on synthetic SPE1 model, 69% on faulted SPE9 benchmark, and 13% on real-field Norne model, using weighted normalized root mean square error as the objective.

Conclusion: Multi-agent orchestration can automate key decisions in history matching, lower expertise barriers for operating complex simulation workflows, and provide a flexible foundation for extensible, domain-aware reservoir model adaptation.

Abstract: History matching is a central inverse problem in reservoir engineering, where uncertain reservoir parameters must be calibrated against observations. Although automated history matching can reduce manual effort, practical deployment remains difficult because engineers must still configure heterogeneous workflows involving parameter selection, physically admissible bounds, optimizer choice, hyperparameter tuning, simulator execution, and diagnostic reporting. We propose PetroGraph, a multi-agent framework for intelligent reservoir history matching that decomposes this workflow into specialized agents for model review, experimental planning, parameterization, optimization, simulation, and summarization. The system combines large language model agents with domain-specific tools, retrieval-augmented access to simulator documentation, validation of modified ECLIPSE input decks, human-in-the-loop checkpoints, and an OPM Flow-based simulation backend. This design enables users to initiate and steer history matching through natural language while preserving explicit control over selected parameters and optimization settings. We evaluate PetroGraph on three reservoir models of increasing complexity: the synthetic SPE1 model, the faulted SPE9 benchmark, and the real-field Norne model. Using weighted normalized root mean square error as the objective, PetroGraph reduces the mismatch by 95% on SPE1, 69% on SPE9, and 13% on Norne. These results demonstrate that multi-agent orchestration can automate key decisions in history matching, lower the expertise barrier for operating complex simulation workflows, and provide a flexible foundation for extensible, domain-aware reservoir model adaptation.

</details>
