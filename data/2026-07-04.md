<div id=toc></div>

# Table of Contents

- [cs.AI](#cs.AI) [Total: 86]
- [cs.LG](#cs.LG) [Total: 101]
- [cs.MA](#cs.MA) [Total: 6]


<div id='cs.AI'></div>

# cs.AI [[Back]](#toc)

### [1] [PACE: A Neuro-Symbolic Framework for Plausible and Actionable Counterfactual Explanations](https://arxiv.org/abs/2607.01306)
*Pavel Iakovets,Liyanapathiranage Sudeepika Wajirakumari Samarathunga,Martin Thomas Horsch,Fadi Al Machot*

Main category: cs.AI

TL;DR: PACE is a neuro-symbolic framework that generates counterfactual explanations by combining neural predictions with symbolic reasoning to enforce domain-specific constraints, producing more realistic and actionable explanations.


<details>
  <summary>Details</summary>
Motivation: Existing counterfactual explanation methods often produce unrealistic or infeasible recommendations because they lack mechanisms to incorporate domain knowledge and intervention constraints. There's a need for explanations that are both prediction-changing and practically feasible.

Method: A modular neuro-symbolic framework that separates prediction and reasoning: uses a neural predictive model for classification and a symbolic reasoning layer (Answer Set Programming rules) that enforces domain-specific constraints during counterfactual generation. The approach is model-agnostic and explicitly models feasible interventions.

Result: The framework demonstrates a trade-off between counterfactual validity (prediction change) and plausibility (realism). Case study on Adult Income dataset shows symbolic constraints yield explanations that better satisfy domain-specific feasibility requirements while preserving immutable attributes like education, occupation, and working hours.

Conclusion: Neuro-symbolic methods like PACE offer promising potential for transparent, feasibility-aware counterfactual explanations in explainable AI by combining data-driven predictions with symbolic reasoning to produce realistic and actionable recommendations.

Abstract: Counterfactual explanations explain machine learning predictions by identifying minimal input changes that would alter a model's decision. Although many existing methods successfully generate prediction-changing alternatives, they often produce unrealistic or infeasible recommendations due to a lack of explicit mechanisms for incorporating domain knowledge and intervention constraints. Neuro-symbolic AI offers a promising direction by combining data-driven predictive models with symbolic reasoning capable of representing human-understandable rules and feasible actions. This paper presents PACE, a modular neuro-symbolic framework for generating feasibility-aware counterfactual explanations. The framework separates prediction and reasoning into two components: a neural predictive model for classification and a symbolic reasoning layer that enforces domain-specific constraints during counterfactual generation. By explicitly modeling feasible interventions, the framework produces explanations consistent with domain knowledge while remaining interpretable and actionable. The approach is model-agnostic and adaptable to domains requiring realistic decision support. A case study is conducted on the Adult Income dataset, combining a multilayer perceptron classifier with Answer Set Programming (ASP) rules encoding feasible modifications to education, occupation, and working hours while preserving immutable attributes. Results highlight the trade-off between counterfactual validity and plausibility and show that symbolic constraints yield explanations that better satisfy domain-specific feasibility requirements, illustrating the potential of neuro-symbolic methods for transparent, feasibility-aware counterfactual explanation in explainable AI.

</details>


### [2] [Auto-FL-Research: Agentic Search for Federated Learning Algorithms](https://arxiv.org/abs/2607.01366)
*Holger R. Roth,Ziyue Xu,Chester Chen,Daguang Xu,Peter Cnudde,Andrew Feng*

Main category: cs.AI

TL;DR: Auto-FL-Research (AFR) is an automated workflow for federated learning algorithmic recipe search using constrained coding agents, achieving gains on multiple FL tasks but revealing mixed outcomes about what drives improvements.


<details>
  <summary>Details</summary>
Motivation: Federated learning research involves many small algorithmic choices that are expensive to explore manually and difficult to compare fairly, as changes can alter training paths and evaluation results.

Method: AFR uses constrained coding-agent workflow where agents propose and implement candidate training algorithms (server aggregation rules, client update schedules, local objectives, model variants) while task profiles fix constraints like mutation surface, compute budget, communication contract, and final evaluation.

Result: Evaluation on 5 healthcare FLamby tasks and 6 LEAF datasets showed gains on 4 FLamby tasks and 5 of 6 LEAF profiles, but also revealed seed-sensitive and search-selected failure cases. Same-budget controls showed some gains correspond to genuine FL-recipe changes, while others are recovered by fixed-surface tuning or fail under repeat evaluation.

Conclusion: The mixed outcomes demonstrate how agent-generated candidates can be separated into repeatable FL mechanisms, fixed-surface tuning effects, and single-run artifacts, highlighting both the promise and challenges of automated FL algorithmic search.

Abstract: Federated learning (FL) research often depends on many small but consequential algorithmic choices: optimizer variants, server aggregation rules, local training schedules, normalization, regularization, and model architecture. These choices are expensive to explore manually and difficult to compare fairly when candidate changes can also alter the FL training or evaluation path. In this work, we present Auto-FL-Research (AFR), a constrained coding-agent workflow for FL algorithmic recipe search. Agents may propose and implement candidate training algorithms, including server aggregation rules, client update schedules, local objectives, and registered model variants, while task profiles fix the mutation surface, compute budget, communication contract, and final model evaluation. Each campaign records candidate scores, runtime, edited files, artifacts, and failure status. We evaluate AFR on five healthcare cross-silo FLamby tasks and on grouped-client profiles for the five fixed LEAF datasets plus the LEAF synthetic task. Five-seed repeat evaluations support gains on four FLamby tasks and five of six LEAF profiles, while also exposing seed-sensitive and search-selected failure cases. Same-budget controls show that several gains correspond to FL-recipe changes, whereas other improvements are recovered by fixed-surface scalar controls or fail under repeat or held-out evaluation. These mixed outcomes are part of the contribution: they show how agent-generated candidates can be separated into repeated FL mechanisms, fixed-surface tuning effects, and selected single-run artifacts.

</details>


### [3] [The Wiola Architecture for Efficient Small Language Models](https://arxiv.org/abs/2607.01394)
*Aryuemaan Kumar Chowdhury,Afreen Shaik,Yaparla Bhargavi,Brahma Kumar*

Main category: cs.AI

TL;DR: Wiola is a completely novel small language model architecture with five original components: SRPE for 3D helical positional encoding, GCLA for inter-layer attention, ATM for token merging, DSFF for dual-stream feed-forward networks, and WiolaRMSNorm for improved normalization.


<details>
  <summary>Details</summary>
Motivation: The authors aim to create a fully original Small Language Model (SLM) architecture from first principles, without inheriting structural lineage from existing models like GPT, LLaMA, Mistral, or Falcon, offering novel architectural innovations.

Method: Wiola introduces five novel components: 1) Spiral Rotary Positional Encoding (SRPE) on a 3D helical manifold, 2) Gated Cross-Layer Attention (GCLA) for inter-layer coherence, 3) Adaptive Token Merging (ATM) to reduce attention complexity, 4) Dual Stream Feed-Forward (DSFF) replacing MLPs, and 5) WiolaRMSNorm with per-dimension learned offsets.

Result: The model is released in four sizes (120M, 360M, 700M, and 1.5B parameters) with complete mathematical derivations, architectural diagrams, complexity analyses, and systematic comparisons against GPT-2, LLaMA-2, and Mistral. It's fully compatible with HuggingFace Transformers and passes all 22 architectural unit tests.

Conclusion: Wiola presents a fully original SLM architecture with five independently novel components that offer innovative alternatives to established model designs, demonstrating that new architectural approaches can be developed from first principles while maintaining compatibility with existing ecosystems.

Abstract: We present Wiola, a fully original Small Language Model (SLM) architecture built from first principles, sharing no structural lineage with any existing model family including GPT, LLaMA, Mistral, or Falcon. Wiola introduces five independently novel components: (i) Spiral Rotary Positional Encoding (SRPE), which embeds token positions on a three-dimensional helical manifold combining absolute, relative, and hierarchical positional signals; (ii) Gated Cross-Layer Attention (GCLA), providing each decoder layer with soft cross-attention access to compressed summaries of two preceding layers for inter-layer coherence; (iii) Adaptive Token Merging (ATM), which dynamically merges se mantically redundant adjacent tokens in middle network layers to reduce attention complexity without information loss; (iv) Dual Stream Feed-Forward (DSFF), replacing the conventional MLP with two parallel streams fused by a learned per-dimension gate; and (v) WiolaRMSNorm, a modified normalisation introducing a per-dimension learned offset vector that prevents representation collapse. We provide complete mathematical derivations, architectural block diagrams, complexity analyses, and systematic comparisons against GPT-2, LLaMA-2, and Mistral. Wiola is released in four sizes (120M, 360M, 700M, and 1.5B parameters) and is fully compatible with the HuggingFace Transformers ecosystem, with all 22 architectural unit tests passing.

</details>


### [4] [Hardware-Enforced Semantic Coordination for Safety-Critical Real-Time Autonomous Systems](https://arxiv.org/abs/2607.02376)
*Uwe M. Borghoff,Paolo Bottoni,Remo Pareschi*

Main category: cs.AI

TL;DR: The paper proposes using FPGAs to implement hardware-enforced coordination semantics for agentic AI systems, ensuring bounded latency, deterministic coordination, and enforceable safety guarantees.


<details>
  <summary>Details</summary>
Motivation: As agentic AI systems become more complex with heterogeneous components (LLMs, world models, optimization engines, etc.) operating concurrently under uncertainty, there's a need for bounded and verifiable coordination. Software-mediated coordination has limitations in domains requiring deterministic coordination, bounded latency, and enforceable safety guarantees.

Method: The approach maps selected coordination mechanisms from the Topic-Based Communication Space Petri Net (TB-CSPN) framework onto FPGA primitives. This creates a hardware-native semantic coordination layer that enforces temporal synchronization, semantic gating, authorization constraints, and bounded coordination behavior directly in hardware.

Result: The proposed architecture separates semantic reasoning (which remains adaptive and software-driven) from interaction management (which becomes hardware-enforced). Embedded coordination semantics become deterministic through FPGA implementation, while semantic reasoning stays flexible.

Conclusion: Hardware-enforced semantic coordination via FPGAs provides a solution for achieving verifiable, deterministic coordination in complex autonomous systems where software-only approaches are insufficient for safety-critical real-time deployment.

Abstract: Recent advances in agentic AI are producing increasingly complex autonomous systems that integrate large language models, world models, optimization engines, specialized neural architectures, autonomous platforms, and human operators. While much current research focuses on improving reasoning capabilities, safety-critical real-time deployment also requires bounded and verifiable coordination among heterogeneous components operating concurrently under uncertainty. Software-mediated coordination presents fundamental limitations in domains where bounded latency, deterministic coordination, and enforceable safety guarantees are essential.
  Hence, we propose a hardware-enforced semantic coordination architecture in which selected coordination semantics are implemented directly at the hardware level via field-programmable gate arrays (FPGAs). The approach builds on the Topic-Based Communication Space Petri Net (TB-CSPN) framework, which separates semantic reasoning from interaction management.
  In this approach, selected TB-CSPN coordination mechanisms are mapped onto FPGA primitives, creating a hardware-native semantic coordination layer. Focus is not on acceleration, but on enforcing temporal synchronization, semantic gating, authorization constraints, and bounded coordination behavior directly in hardware. Semantic reasoning remains adaptive and software-driven, while embedded coordination semantics become deterministic.

</details>


### [5] [Agent4cs: A Multi-agent System for Code Summarization in Large Hierarchical Codebases](https://arxiv.org/abs/2607.01425)
*Yongjian Tang,Ezgi Sarikayak,Doruk Tuncel,Jie M. Zhang,Thomas Runkler*

Main category: cs.AI

TL;DR: Agent4cs is a multi-agent framework that improves codebase summarization by leveraging hierarchical repository structure and specialized agents for summarization, keyword extraction, and quality assurance, outperforming existing methods on semantic consistency and keyword coverage.


<details>
  <summary>Details</summary>
Motivation: Existing code summarization approaches have limitations: they often treat source code as flat text, ignore rich interdependencies and hierarchical information within repositories, rely on single language models, and struggle with obfuscated structures and incomplete documentation in large codebases.

Method: Agent4cs uses a multi-agent framework with bottom-up summarization approach. It employs three specialized agents: 1) Summarization agent producing robust summaries, 2) Keyword-extraction agent proactively identifying critical information from subfolders, and 3) Quality-assurance agent iteratively refining outputs for readability, coherence, and completeness.

Result: Evaluation on 7 frontier models shows Agent4cs improves semantic consistency across all folder levels by average 8% compared to two structured prompting baselines with code segments. Real-world dataset evaluation demonstrates up to 38% gains in normalized keyword coverage rate over the same baselines.

Conclusion: The multi-agent framework effectively addresses limitations of existing code summarization methods by better utilizing repository structure and interdependencies, offering significant improvements in both semantic consistency and keyword coverage for large codebase analysis.

Abstract: Understanding large, complex codebases, especially those with obfuscated structures and incomplete documentation, remains a significant challenge. Existing code summarization solutions often rely on a single language model or coding assistant like Claude Code, and treat source code as flat text, underutilizing the rich interdependencies and hierarchical information within a repository. To address these shortcomings, we propose Agent4cs - a multi-agent framework that summarizes large codebases in a bottom-up fashion, where a summarization agent focuses on producing robust summaries; a keyword-extraction agent proactively identifies critical information from subfolders; and a quality-assurance agent iteratively refines the outputs for readability, coherence, and completeness. Evaluated on 7 frontier models, Agent4cs improves semantic consistency across all folder levels by average 8% compared to two structured prompting baselines with code segments. Furthermore, extensive evaluation on real-world datasets demonstrates up to 38% gains in normalized keyword coverage rate over the same baselines.

</details>


### [6] [What LLM Agents Say When No One Is Watching: Social Structure and Latent Objective Emergence in Multi-Agent Debates](https://arxiv.org/abs/2607.02507)
*Arman Ghaffarizadeh,Danyal Mohaddes,Aliakbar Izadkhah,Shahriar Noroozizadeh*

Main category: cs.AI

TL;DR: LLM agents exhibit systematic divergence between public statements and off-the-record responses in socially structured settings, with divergence increasing from ~3% baseline to ~40% in alignment-inducing scenarios.


<details>
  <summary>Details</summary>
Motivation: LLM agents increasingly operate in socially structured environments where role, audience, and relational context influence what's advantageous or costly to express. The paper aims to understand whether social structure without explicit objectives affects what agents express publicly versus privately.

Method: Introduced a dual-channel debate framework where agents produce both public utterances (shared with participants) and off-the-record responses (recorded but not shown). Tested across 10 models, 3 scenarios, and 5 variations per scenario, with behavioral measures including stance, semantic similarity, natural language inference, and survey responses.

Result: Alignment-inducing settings produce systematic public-OTR divergence in targeted agents, increasing from ~3% baseline to ~40% divergence. OTR responses sometimes explicitly attribute public accommodation to relational pressures like career risk or sponsorship obligation. Results consistent across all four aggregate analyses.

Conclusion: Agent evaluation should extend beyond explicit goals to detect emergent objectives. The dual-channel evaluation framework and complementary behavioral measures provide a way to operationalize assessment of how social structure shapes agent behavior, even without explicit objectives.

Abstract: LLM agents will increasingly act in socially structured settings where role, audience, and relational context can shape what is advantageous or costly to say. We study whether such social structure, without any explicit objective in the prompt, changes what an agent expresses publicly relative to an off-the-record (OTR) channel elicited under the same condition. We introduce a dual-channel debate framework in which agents produce public utterances that enter the shared history alongside OTR responses that are recorded but never shown to the other participant. Across 10 models, 3 scenarios, and 5 variations within each scenario, alignment-inducing settings produce systematic public-OTR divergence in the targeted agent, with its decision divergence rising from a $\sim$3% baseline to roughly 40%. The effect is consistent across four aggregate analyses: stance, semantic similarity, natural language inference, and survey responses. In some cases, the OTR response explicitly attributes public accommodation to relational pressures, such as career risk or sponsorship obligation. The findings suggest that agent evaluation should extend beyond explicit goals and detect emergent objectives. We present a dual-channel evaluation framework and complementary behavioral measures that operationalize this assessment.

</details>


### [7] [When Should Service Agents Reconsider? Difficulty-Routed Control in Customer-Service Operations](https://arxiv.org/abs/2607.01426)
*Qian Chen,Chengyuan Liu,Xin Yu*

Main category: cs.AI

TL;DR: A difficulty-routed service-control architecture for autonomous customer-service agents that routes routine sessions through a low-cost baseline while escalating operationally complex sessions to a safeguarded workflow with conflict-aware communication and write-triggered reconsideration.


<details>
  <summary>Details</summary>
Motivation: Autonomous customer-service agents are shifting from conversational interfaces to operational execution roles (refunds, cancellations, exchanges, etc.), creating a service-control problem: firms need to keep routine service fast and low-friction while preventing operational errors on complex requests where customer instructions, policy constraints, firm records, and backend writes interact.

Method: A difficulty-routed service-control architecture with a lightweight router that keeps routine sessions on a low-cost baseline path and routes operationally coupled sessions to an escalated workflow. The escalated path uses conflict-aware communication and write-triggered reconsideration to concentrate deliberation and safeguards before consequential backend writes rather than applying additional control uniformly.

Result: Evaluation on human-verified retail and airline tasks shows the method improves reliability consistently on service requests with operational conflict. Routing evidence indicates stronger control is directed toward conflicted requests rather than broadly applied to routine ones. Gains come from added turns and tool calls that support evidence gathering, write separation, and pre-write reconsideration. The escalated workflow preserves fallback plans, binds retrieved records to correct actions, sequences writes, and decomposes multi-entity requests.

Conclusion: The difficulty-routed architecture successfully addresses the service-control problem by differentiating between routine and complex service requests, concentrating safeguards where needed most - before consequential backend writes - while maintaining efficiency for simple cases. The approach extends effectively from retail to airline reservation operations.

Abstract: Autonomous customer-service agents are shifting from conversational interfaces toward operational execution roles: they retrieve firm records, apply service policies, and execute backend writes such as refunds, cancellations, exchanges, order modifications, and reservation changes. This shift creates a service-control problem: firms must keep routine service fast and low-friction while preventing operational errors on requests where customer instructions, policy constraints, firm records, and backend writes interact. We propose a difficulty-routed service-control architecture that asks when service agents should reconsider before acting. A lightweight router keeps routine sessions on a low-cost baseline path and routes operationally coupled sessions to an escalated workflow. The escalated path uses conflict-aware communication and write-triggered reconsideration to concentrate deliberation and safeguards before consequential backend writes, rather than applying additional control uniformly across all service sessions. We evaluate the architecture on human-verified retail and airline tasks from $τ^{2}$-bench. In retail, the method improves reliability consistently on service requests with operational conflict. Routing evidence shows that stronger control is directed toward conflicted requests rather than broadly applied to routine ones. Dialogue and tool-use profiles suggest that gains do not come from indiscriminate interaction expansion or broader tool chains; instead, added turns and tool calls support evidence gathering, write separation, and pre-write reconsideration. Case-level evidence shows that the escalated workflow preserves fallback plans, binds retrieved records to the correct action, sequences writes, and decomposes multi-entity requests. Airline results extend the same service-control logic to reservation operations.

</details>


### [8] [CreativityNeuro: Steering Language Model Weights to Improve Divergent Thinking and Reduce Mode Collapse](https://arxiv.org/abs/2607.01433)
*Samuel Schapiro,Core Francisco Park,Felix Sosa,Lav R. Varshney*

Main category: cs.AI

TL;DR: CreativityNeuro enhances LLM divergent thinking via contrastive weight steering without data or fine-tuning, reducing mode collapse and improving performance across creativity tests.


<details>
  <summary>Details</summary>
Motivation: LLMs suffer from the artificial hivemind effect - they generate similar responses to open-ended questions and lack divergent thinking crucial for creativity.

Method: CreativityNeuro uses data-free contrastive weight steering to enhance divergent thinking in LLMs without requiring behavioral data, re-training, or gradient-based fine-tuning.

Result: Improves DAT performance by up to 14 human percentile points; significant improvements in originality, surprise, and creativity on AUT/Task Task (N=720); reduces mode collapse across all tasks; weight-space steering generalizes to unseen tasks better than activation steering.

Conclusion: CreativityNeuro provides a straightforward, data-free way to enhance LLM divergent thinking and reduce mode collapse in creative domains without training or fine-tuning.

Abstract: Divergent thinking is a crucial aspect of creativity, yet large language models (LLMs) tend to consistently generate similar responses to open-ended questions, in what has been termed the artificial hivemind effect. Here, we introduce CreativityNeuro, a data-free method for enhancing divergent thinking in LLMs via contrastive weight steering. We evaluate our method across multiple creativity assessments and report several main findings. On the Divergent Association Task (DAT), a vocabulary-space creativity test, CreativityNeuro improves performance by up to 14 human percentile points. Next, in a large-scale human evaluation (N=720) on the Alternative Uses Test (AUT) and the Task Task, CreativityNeuro achieves significant improvements in originality, surprise, and creativity, transferring to longer-form and more open-ended tasks. Importantly, we find that across all three tasks, CreativityNeuro demonstrably reduces measures of mode collapse. Moreover, activation steering achieves comparable performance to CreativityNeuro on the DAT, but it does not transfer to the AUT and Task Task, demonstrating the effectiveness of weight-space steering in generalizing to unseen tasks. In conclusion, CreativityNeuro improves divergent thinking and reduces mode collapse without requiring behavioral data, re-training, or gradient-based fine-tuning, providing a straightforward way to enhance LLM performance in creative domains.

</details>


### [9] [Discrete Diffusion Language Models for Interactive Radiology Report Drafting](https://arxiv.org/abs/2607.01436)
*Max Van Puyvelde,Halil Ibrahim Gulluk,Wim Van Criekinge,Olivier Gevaert*

Main category: cs.AI

TL;DR: Diffusion language models match or exceed autoregressive models in medical VQA tasks while offering faster decoding and unique infill capabilities for radiology reports.


<details>
  <summary>Details</summary>
Motivation: Medical foundation models remain almost entirely autoregressive despite diffusion language models becoming competitive in text generation. There's a need to explore diffusion models for medical applications, particularly for their bidirectional denoising capabilities that could benefit real-world clinical reporting workflows.

Method: Adapted DiffusionGemma-26B (a mixture-of-experts diffusion language model) and benchmarked it against same-size autoregressive sibling Gemma-4-26B using identical LoRA fine-tuning recipes. Evaluated on medical visual question answering datasets using a verbosity-robust LLM judge.

Result: Diffusion matches or exceeds autoregressive performance on all medical VQA datasets. The fine-tuned model (3.8B active parameters) is competitive with frontier vision-language models, with decoding 3.5-4.4x faster than autoregressive. The diffusion model uniquely enables any-order infill capabilities for radiology reports.

Conclusion: Diffusion language models offer compelling advantages for medical applications: they achieve parity or better performance than autoregressive models while providing faster decoding and novel drafting capabilities like text infill, which is particularly valuable for improving inconsistent clinical reports.

Abstract: Diffusion language models, which generate text by denoising a token canvas bidirectionally instead of emitting tokens left to right, have become competitive with autoregressive (AR) generation. Medical foundation models, however, remain almost entirely autoregressive. We adapt a mixture-of-experts diffusion language model, DiffusionGemma-26B, and benchmark it against its same-size AR sibling Gemma-4-26B under an identical LoRA recipe on medical visual question answering datasets, scored by a verbosity-robust LLM judge. Diffusion matches or exceeds AR on all of them, and the finetuned model (3.8B active) is competitive with frontier vision-language models; its decoding is also 3.5-4.4x faster. Beyond this parity, the diffusion model offers a drafting capability AR lacks: any-order infill. Because the canvas is denoised bidirectionally, a radiologist can fix report fragments and have the model fill the text between them, an operation inherent to diffusion but not to autoregression, which is subpar at it. This suits real reports, which are often terse or inconsistent across clinicians and institutions.

</details>


### [10] [Beyond Next-Token Prediction: An RLVR Proof of Concept for Tool-Use Agents on Atlassian Workflows](https://arxiv.org/abs/2607.01465)
*Karthikeya Aditya Vissa,Sankalp Mane,Ananya Mantravadi,Harshit Rajgarhia,Abhishek Mukherji*

Main category: cs.AI

TL;DR: RLVR (Reinforcement Learning with Verifiable Rewards) applied directly to enterprise API workflows significantly improves small language models' performance on specific API tasks, lifting reward scores from baseline ranges of 0.35-0.92 to 0.95-1.00 on most tested scenarios.


<details>
  <summary>Details</summary>
Motivation: There's a mismatch between LLMs' next-token prediction objective and the requirements of niche enterprise SaaS workflows, where success depends on hitting correct API endpoints with proper nested arguments in sequence. This leads to silent failures like dropped required fields, hallucinated tools, or premature stops.

Method: Used Reinforcement Learning with Verifiable Rewards (RLVR) applied directly in target environments. Built five synthetic environments emulating Jira REST v3 and Confluence v2 APIs at schema fidelity. Rewards were computed entirely from tool-call traces without live APIs, learned judges, or human labels. Tested on Qwen3-1.7B and Qwen3.5-4B models using the same checkers that drive GRPO training.

Result: On four scenarios with non-degenerate rewards, RL-trained policies lifted average reward from baseline range of 0.35-0.92 to 0.95-1.00. Largest single gain was on Confluence page creation (0.35 → 1.00). One scenario (ticket-transition) had saturating rewards that the prompted 4B model already maxed out.

Conclusion: This is a preliminary step toward outcome-optimized small models for niche enterprise APIs. Key limitations are that hand-crafting verifiable rewards doesn't scale beyond a handful of endpoints, and some scenarios have saturating reward shapes that baseline models already perform well on.

Abstract: Large language models are trained to predict the next token, not to act inside a specific API. In niche enterprise SaaS workflows -- where success means hitting the right endpoint with the right nested arguments in the right order -- this objective mismatch shows up as silent failures: dropped required fields, hallucinated tools, or early stops after a single read. We ask whether Reinforcement Learning with Verifiable Rewards (RLVR), applied directly in the target environment, closes the gap. As a proof of concept we build a suite of five synthetic environments emulating the Jira REST v3 and Confluence v2 APIs at schema fidelity; rewards are computed entirely from the tool-call trace, with no live API, no learned judge, and no human label in the loop. Scoring prompted Qwen3-1.7B and Qwen3.5-4B on the same checkers that drive GRPO training, we find that on the four scenarios whose rewards are non-degenerate the RL-trained policy lifts average reward from a 4B-baseline range of 0.35--0.92 to 0.95--1.00, with the largest single gain on Confluence page creation ($0.35 \rightarrow 1.00$). We position this as a preliminary step toward outcome-optimised small models for niche enterprise APIs, and foreground two limitations a workshop reader should weigh: hand-crafting verifiable rewards does not scale beyond the handful of endpoints reported here, and one of our five scenarios (ticket-transition) has a saturating reward shape that the prompted 4B already maxes out.

</details>


### [11] [World Feedback for Clinical Agents: Diagnosing RL in FHIR Environments](https://arxiv.org/abs/2607.01470)
*Ananya Mantravadi,Harshit Rajgarhia,Prasanna Desikan,Abhishek Mukherji*

Main category: cs.AI

TL;DR: Researchers addressed limitations in applying reinforcement learning (RL) to clinical protocol-execution tasks by identifying and overcoming structural barriers in existing benchmarks, leading to a new benchmark (MAB-v3) and a hybrid training approach combining supervised fine-tuning with RL.


<details>
  <summary>Details</summary>
Motivation: Clinical protocol-execution tasks are well-suited for RL from world feedback since verifiers can grade unlimited rollouts without per-episode annotation. However, applying RL requires both a sound feedback channel and sufficient base capability in the model.

Method: 1) Audited existing MedAgentBench v1/v2, identifying a 41.7% silent-finish ceiling problem. 2) Constructed MedAgentBench-v3 (MAB-v3) with 508 tasks and reduced ceiling (8.9%). 3) Trained Qwen3-8B model, identifying structural barriers: capability ceiling (10/20 task types had 0% base performance) and format-knowledge barrier (3/20 types required exact clinical codes). 4) Tested pure RL vs. rule-based SFT approaches.

Result: Pure RL achieved only 18.2% pass@1, while rule-based supervised fine-tuning (SFT) achieved 34.1% - a 15.9 percentage point gap entirely attributable to the identified structural barriers. The authors developed a taxonomy (decision/format-knowledge/lookup) that predicts RL learnability and prescribes solutions.

Conclusion: The structural barriers (capability ceiling and format-knowledge barrier) fundamentally limit pure RL for clinical tasks. The solution is a hybrid approach: use SFT to inject necessary clinical codes and format knowledge, then apply RL to learn the conditional decision logic. This taxonomy provides a framework for predicting and addressing RL learnability challenges in clinical domains.

Abstract: Clinical protocol-execution tasks -- checking a lab value, applying a threshold, placing a correctly structured FHIR order -- are natural candidates for RL from world feedback: once clinical SMEs encode decision logic into a verifier, that verifier grades unlimited rollouts without per-episode annotation. But applying RL requires a sound feedback channel and sufficient base capability. We audit MedAgentBench v1/v2, find a 41.7\% silent-finish ceiling that makes inaction the RL dominant strategy, and construct \textbf{MedAgentBench-v3 (MAB-v3)} (508 tasks, 8.9\% ceiling). Training Qwen3-8B exposes two structural barriers: a \emph{capability ceiling} (10/20 task types have 0\% base performance, zero gradient) and a \emph{format-knowledge barrier} (3/20 types require exact clinical codes undiscoverable by exploration). Pure RL reaches 18.2\% pass@1 vs.\ 34.1\% for rule-based SFT; the 15.9~pp gap is attributable entirely to these barriers. A decision/format-knowledge/lookup taxonomy predicts RL learnability and prescribes the fix: SFT to inject codes, RL to learn conditionals.

</details>


### [12] [Procedural Memory Distillation: Online Reflection for Self-Improving Language Models](https://arxiv.org/abs/2607.01480)
*Ye Liu,Srijan Bansal,Bo Pang,Yang Li,Zeyu Leo Liu,Yifei Ming,Zixuan Ke,Shafiq Joty,Semih Yavuz*

Main category: cs.AI

TL;DR: PMD introduces procedural memory distillation that captures cross-episode learning signals into reusable memory scaffolds during RLVR training, enabling policies to internalize procedural knowledge and outperform prior methods.


<details>
  <summary>Details</summary>
Motivation: Current RLVR methods waste valuable procedural information in rollouts - they use only episode-level verification signals and lose cross-episode insights about consistent strategies, persistent failure modes, and recurring patterns across episodes and epochs.

Method: PMD creates procedural memory organized at three abstraction levels: raw trajectories, self-reflected strategies/lessons, and higher-level behavioral patterns. A memory-conditioned self-teacher uses this accumulated experience to supervise the student policy on its own rollouts, enabling knowledge internalization through co-evolution between policy and memory.

Result: PMD improves over SDPO by 3.8-5.5% on SCIKNOWEVAL and 7.9-13.6% on LIVECODEBENCH across Qwen3-8B and OLMo3-Instruct-7B models. Co-evolution is crucial - freezing either memory or policy trails PMD by over 10% across SCIKNOWEVAL domains.

Conclusion: Procedural memory distillation effectively captures and reuses cross-episode learning signals in RLVR training, with co-evolution between policy and memory enabling significant performance gains over episode-local approaches while maintaining memory-free inference.

Abstract: Reinforcement learning with verifiable rewards (RLVR), along with recent selfdistillation variants such as SDPO, evaluates each rollout against a verifier and updates the policy from that episode-level signal. However, the richer procedural information in the rollout is rarely retained or reused. Across episodes and epochs, the model repeatedly encounters related problems under a changing policy, producing cross-episode signals that episode-local updates cannot capture: which strategies consistently pass verification, which failure modes persist, which patterns recur. We propose Procedural Memory Distillation (PMD), which converts these crossepisode signals into reusable procedural memory and distills it into the policy's weights during training. This memory functions as a training scaffold, absorbed into the policy itself, yielding a memory-free model at inference. PMD organizes the memory at three levels of abstraction: raw trajectories, self-reflected strategies and lessons, and higher-level behavioral patterns that recur across problems, all extracted online from the model's own trajectories. A memory-conditioned self-teacher draws on the accumulated experience to supervise the student on its own rollouts, enabling student to progressively internalize procedural knowledge within its parameters. The central design principle is co-evolution: the policy generates rollouts that update the memory, and memory shapes the supervision that updates the policy. Empirically, across Qwen3-8B and OLMo3-Instruct-7B, PMD improves over SDPO by 3.8-5.5% on SCIKNOWEVAL and 7.9-13.6% on LIVECODEBENCH. Co-evolution powers these gains: freezing either the memory or the policy trails PMD by more than 10% across SCIKNOWEVAL domains.

</details>


### [13] [The Agentic Garden of Forking Paths](https://arxiv.org/abs/2607.01507)
*Jiacheng Miao,Jonathan K Pritchard,James Zou*

Main category: cs.AI

TL;DR: AI agents with different personas analyze the same data and reach divergent conclusions, revealing how analytical flexibility can lead to opposing findings even when analyses are methodologically defensible.


<details>
  <summary>Details</summary>
Motivation: The motivation is to expose the hidden "forking paths" in empirical research where different analytical choices can lead to different conclusions from the same data, and to understand how this problem might be amplified by AI systems.

Method: The researchers created AI agents with different personas to analyze the same datasets across four high-stakes domains. They introduced the m-value (multiverse value) to quantify how extreme a reported finding is within the space of possible analyses, and developed Agentic Bootstrap using AI agents to sample plausible analysis paths.

Result: AI agents reproduced 72% of the human ideological gap in reported effect estimates. 86% of AI analyses passed independent AI review and 78% passed human expert review despite reaching opposing conclusions. In the immigration study, 13.5% of human analyses fell in the most extreme 5% of the analysis space.

Conclusion: Scientific evidence should be evaluated by its position within the distribution of plausible analyses, not just by a single reported analysis. AI agents can make analytical exploration inexpensive and scalable, potentially amplifying existing problems of selective reporting. The proposed m-value and Agentic Bootstrap provide tools to assess scientific credibility through the lens of analytical variability.

Abstract: Empirical research rarely admits a unique analysis. Different analytical choices can lead to different conclusions from the same data, yet these hidden forking paths are difficult to observe. We show that AI agents capture much of the analytical variation among human researchers while making these paths explicit. Across four high-stakes domains, assigning different personas is sufficient for AI agents to report divergent, often opposing, conclusions from the same data and question, with findings systematically aligned with those beliefs. In a study in which 42 human research teams analyzed the same immigration dataset, AI agents reproduced 72% of the human ideological gap in reported effect estimates. Despite reaching opposing conclusions, it is difficult to identify clear issues in each analysis based on the final AI reports: 86% passed independent AI review and 78% passed majority human expert review. These findings suggest that the central challenge is often not flawed analyses, but selective exploration and reporting from a large space of methodologically defensible analyses. AI agents may amplify this longstanding problem by making such exploration inexpensive and scalable. To address this, we introduce the m-value (multiverse value), the probability that an analysis path would produce a claim at least as extreme as the reported one. We further introduce Agentic Bootstrap, which estimates the m-value by using AI agents to sample plausible analysis paths. Applied to the human immigration study, 13.5% of reported human analyses fell in the most extreme 5% of the analysis space (m<0.05). Scientific evidence should therefore be evaluated not only by a single reported analysis but also by its position within the distribution of analyses that could reasonably have been reported. Agentic Bootstrap makes this distribution observable and turns it into a criterion for scientific credibility.

</details>


### [14] [Janus: a Playground for User-Involved Agentic Permission Management](https://arxiv.org/abs/2607.01510)
*Natalie Grace Brigham,Eugene Bagdasarian,Tadayoshi Kohno,Franziska Roesner*

Main category: cs.AI

TL;DR: Janus is a playground system for implementing and evaluating user-involved permission management designs for AI agents that autonomously execute tool calls, with modular components for system implementation and automated evaluation.


<details>
  <summary>Details</summary>
Motivation: AI agents that autonomously execute tool calls on behalf of users raise critical questions about permission management. Despite many proposed approaches, the user's role in agentic permission management remains under-explored, creating a gap in understanding how users should be involved in permission decisions for autonomous AI systems.

Method: The authors introduce Janus, consisting of Janus-Core (a modular agentic system supporting diverse permission management designs) and Janus-Harness (an automated evaluation framework). They develop a conceptual model identifying key design axes for user involvement, implement six permission assistants spanning the design space, and evaluate them across three scenarios with three synthetic responders.

Result: User input is critical and can significantly strengthen privacy and security. AI augmentation of user decisions helps reduce cognitive load. Realistic user behavior including permission fatigue must be accounted for in system design. No single design performs optimally across all contexts.

Conclusion: Permission assistants in agentic systems require a more principled and context-sensitive approach. Janus is publicly available to support future investigation into this dimension of agentic system design, as there is no one-size-fits-all solution for user involvement in AI agent permission management.

Abstract: AI agents that autonomously execute tool calls on a user's behalf raise pressing questions about permission management: what role could users play, and what role should they play? Despite many proposed approaches, the user's role in agentic permission management remains under explored. We introduce Janus, a playground system for implementing and evaluating user-involved agentic permission management designs. Janus consists of two components: Janus-Core, a modular agentic system supporting a diverse spectrum of permission management designs, and Janus-Harness, an automated evaluation framework. Grounded in a conceptual model that identifies key design axes for user involvement, we implement six permission assistants spanning the design space and evaluate them across three scenarios and three synthetic responders. We demonstrate that user input is critical and can significantly strengthen privacy and security, that AI augmentation of user decisions can help reduce cognitive load, and that realistic user behavior including permission fatigue must be accounted for in system design. No single design performs optimally across all contexts, motivating a more principled and context-sensitive approach to deploying permission assistants in agentic systems. Janus is publicly available to support future investigation into this dimension of agentic system design.

</details>


### [15] [Revisiting Chain-of-Thought Reasoning under Limited Supervision: Semi-supervised Chain-of-Thought Learning](https://arxiv.org/abs/2607.01511)
*Hongyang He,Jiuming Liu,Victor Sanchez*

Main category: cs.AI

TL;DR: Semi-CoT uses unlabeled questions to generate pseudo reasoning chains, filters them with answer-level semantic entropy, and uses high-quality ones as pseudo-supervision for semi-supervised learning.


<details>
  <summary>Details</summary>
Motivation: Most CoT methods use reasoning chains only as inference-time prompts rather than reusing them as learning signals. There's potential to leverage unlabeled data to construct pseudo reasoning supervision for improved learning.

Method: Semi-CoT framework: 1) For each unlabeled question, sample multiple pseudo-CoTs, 2) Estimate answer-level semantic entropy, 3) Select low-entropy reasoning chains as reliable pseudo-CoT demonstrations, 4) Use these as semi-supervised pseudo-supervision.

Result: Entropy gate selects high-precision pseudo-CoTs (91.36% to 100% pseudo-answer precision). Semi-CoT gives small gains on SVAMP and GSM8K, but shows negative transfer on AQuA and ceiling effect on MultiArith.

Conclusion: Unlabeled questions can provide reliable pseudo reasoning signals, but effective use requires stronger demonstration selection or student training methods to avoid negative transfer and ceiling effects.

Abstract: Chain-of-thought (CoT) reasoning has emerged as an effective approach for activating latent reasoning capabilities in large language models. However, most existing CoT methods use reasoning chains mainly as inference-time prompts, while the generated reasoning traces are rarely reused as semi-supervised learning signals. In this report, we define \textbf{Semi-supervised Chain-of-Thought Learning} and propose \textbf{Semi-CoT}, a simple framework that uses unlabeled questions to construct pseudo reasoning supervision. Semi-CoT samples multiple pseudo-CoTs for each unlabeled question, estimates answer-level semantic entropy, and selects low-entropy reasoning chains as reliable pseudo-CoT demonstrations. This extends the self-training view of CoT from inference-time refinement to semi-supervised pseudo-supervision. Pilot experiments on AQuA, SVAMP, GSM8K, and MultiArith show that the entropy gate selects high-precision pseudo-CoTs, with pseudo-answer precision ranging from $91.36\%$ to $100\%$. Semi-CoT also gives small gains on SVAMP and GSM8K, while AQuA shows negative transfer and MultiArith reaches a ceiling. These results suggest that unlabeled questions can provide reliable pseudo reasoning signals, but their effective use still requires stronger demonstration selection or student training.

</details>


### [16] [OPINE-World: Programmatic World Modeling with Ontology-error-Prioritized Interactive Exploration](https://arxiv.org/abs/2607.01531)
*David Courtis,Wenhao Li,Scott Sanner*

Main category: cs.AI

TL;DR: OPINE-World is an LLM-based agent that learns object-centric programmatic world models online through a hypothesis-test loop with two cooperating agents, using Bayesian ontology error for exploration guidance, achieving high efficiency on ARC-AGI-3 benchmark.


<details>
  <summary>Details</summary>
Motivation: Current world model approaches face limitations: deep network models are data-hungry and poor at transfer learning, while program-synthesized models have been limited to structured-state worlds with given object vocabularies and don't scale to pixel-rendered environments where object structure must be hypothesized flexibly.

Method: OPINE-World uses two cooperating LLM agents in a hypothesis-test loop: one agent acts in the environment while the other synthesizes object-centric programmatic world models in code. The system employs replay verification, model-based planning, and steers exploration with a Bayesian measure called "ontology error" that evaluates object-type adequacy.

Result: The system solves 20 out of 25 games on the ARC-AGI-3 benchmark without per-game training, achieving an action-efficiency score of 78.4 against the human baseline. ARC-AGI-3 is challenging as it withholds object vocabulary, goals, and action semantics.

Conclusion: OPINE-World demonstrates that combining program synthesis with online learning and Bayesian exploration guidance enables efficient world model acquisition in complex environments, bridging the gap between data efficiency and scalability while achieving human-like action efficiency.

Abstract: Learning how an environment behaves from interaction is central to building agents that adapt to unfamiliar tasks. World models learned with deep networks are flexible but data-hungry and transfer poorly beyond their training distribution. Program-synthesized world models, written as source code by LLMs and refined through counterexample-guided inductive synthesis (CEGIS), are instead data-efficient and reusable, yet they have been demonstrated mainly on structured-state worlds with a given object vocabulary, and a single program search does not scale to pixel-rendered environments whose object structure must be hypothesized flexibly. We introduce OPINE-World, an LLM agent that learns an object-centric programmatic world model online from interaction. OPINE-World couples two cooperating agents in a loop of hypothesis and test, one acting in the environment and one synthesizing the model in code with replay verification and model-based planning, and it steers exploration with a Bayesian measure of object-type adequacy we call ontology error. We evaluate OPINE-World on ARC-AGI-3, a benchmark for skill-acquisition efficiency in which the object vocabulary, the goal, and the action semantics are withheld. OPINE-World solves 20 of 25 games without per-game training and reaches an action-efficiency score of 78.4 against the human baseline.

</details>


### [17] [Scaling Trends for Lie Detector Oversight in Preference Learning](https://arxiv.org/abs/2607.01567)
*Oskar J. Hollinsworth,Ann-Kathrin Dombrowski,Sam Adam-Day,Adam Gleave,Chris Cundy*

Main category: cs.AI

TL;DR: SOLiD (Scalable Oversight via Lie Detectors) scales effectively to larger models, reducing undetected deception from 34% to 14% for 1B to 405B parameter models, and can eliminate human labelers in fine-tuning without significant deception increase, but is vulnerable to distribution shifts that raise false positive rates.


<details>
  <summary>Details</summary>
Motivation: Deceptive behavior in large language models is costly to monitor and prevent, creating a need for scalable oversight approaches like SOLiD that can identify potentially deceptive responses for review using lie detectors rather than constant human supervision.

Method: The paper scales and evaluates the SOLiD framework (originally by Cundy & Gleave, 2025) to larger models and more diverse preference-learning settings. SOLiD uses lie detectors to identify responses that require review by high-cost human labelers, creating a more efficient oversight system.

Result: SOLiD shows favorable scaling properties: undetected deception drops significantly from 34% for 1B-parameter models to only 14% for 405B-parameter models at a 99% detector true positive rate. Additionally, expensive human labelers can be completely removed from the fine-tuning phase without statistically significant increase in deception.

Conclusion: While SOLiD demonstrates promising scalability and efficiency benefits for oversight of large language models, it has a critical limitation: sensitivity to distribution shift between detector training data and preference-training data, which can drive detector false positive rates to impractical levels, limiting its robustness in real-world deployment.

Abstract: Deceptive behavior in LLMs is costly to monitor and prevent, motivating approaches such as Scalable Oversight via Lie Detectors (SOLiD) (Cundy & Gleave, 2025), which uses lie detectors to identify responses for review by high-cost labelers. In this paper, we scale SOLiD to larger models and evaluate it in more diverse and realistic preference-learning settings. We find favorable scaling: undetected deception drops from 34% for 1B-parameter models to 14% for 405B-parameter models at a detector true positive rate of 99%, and expensive human labelers can be removed entirely from the fine-tuning phase without a statistically significant increase in deception. However, SOLiD is sensitive to distribution shift between detector training and preference-training data, which can drive detector false positive rates to impractical levels.

</details>


### [18] [EO-Agents: A Three-Agent LLM Pipeline for Earth Observation Hypothesis Generation](https://arxiv.org/abs/2607.01584)
*Mahyar Ghazanfari,Amin Tabrizian,Armin Mehrabian,Peng Wei*

Main category: cs.AI

TL;DR: A pipeline that combines knowledge graphs and LLMs for generating structured Earth science hypotheses from NASA datasets, producing 160 novel yet plausible research directions across multiple domains.


<details>
  <summary>Details</summary>
Motivation: Current LLM-based hypothesis generation relies on unstructured literature and free-form textual claims, lacking grounding in structured scientific data. There's a need for systems that can generate testable hypotheses directly from organized Earth observation datasets.

Method: A pipeline that: 1) uses NASA Earth Observation Knowledge Graph with heterogeneous graph neural network to rank candidate dataset pairings based on historical co-usage patterns, 2) employs a three-agent LLM pipeline for filtering, generating, and evaluating structured research hypotheses, 3) applies this to 1,475 NASA datasets.

Result: Generated 160 hypotheses spanning multiple Earth-science domains including ecohydrology, glaciology, aerosol-cloud interactions, vegetation phenology, and stratospheric chemistry. Model-predicted novel dataset pairings were rated nearly as plausible as held-out real co-usages from literature. Experimental results show hypothesis rankings remain stable across different LLMs, but absolute scores depend strongly on judge identity.

Conclusion: The pipeline successfully surfaces scientifically coherent yet unexplored dataset combinations, demonstrating practical hypothesis generation grounded in structured knowledge. However, the strong dependence of evaluation scores on judge identity highlights limitations of single-judge LLM evaluation in scientific contexts.

Abstract: Large language models have recently been explored for scientific hypothesis generation, but most prior work relies on unstructured literature and free-form textual claims. We present a pipeline for Earth observation that grounds hypothesis generation directly in the NASA Earth Observation Knowledge Graph. A heterogeneous graph neural network trained on historical co-usage relations ranks candidate dataset pairings, and a three-agent LLM pipeline filters, generates, and evaluates structured research hypotheses. Applied to 1,475 NASA datasets, the system produces 160 hypotheses spanning multiple Earth-science domains, including ecohydrology, glaciology, aerosol--cloud interactions, vegetation phenology, and stratospheric chemistry. Model-predicted novel dataset pairings are rated nearly as plausible as held-out real co-usages from the literature, indicating that the pipeline surfaces scientifically coherent yet unexplored combinations. A 2*2*2 factorial experiment across GPT-5.2 and Claude Sonnet 4.6 shows that hypothesis rankings remain stable, while absolute scores depend strongly on judge identity, highlighting limitations of single-judge LLM evaluation.

</details>


### [19] [Hawk: Harnessing Hardware-Aware Knowledge for High-Performance NPU Kernel Generation](https://arxiv.org/abs/2607.01590)
*Junyi Wen,Ruiyan Zhuang,Yongjia Xu,Pengtu Li,Rui Zou,Hongyi Chen,Chingman Wan,Puxu Yang,Wuhui Chen,Yanlin Wang*

Main category: cs.AI

TL;DR: Hawk is a training-free framework that improves NPU kernel generation accuracy from 49.4% to 80.0% by using hardware-aware knowledge through three specialized modules, achieving up to 2.2x speedup over baselines.


<details>
  <summary>Details</summary>
Motivation: Manual development of high-performance NPU kernels is bottlenecked by hardware constraints and memory hierarchies. LLMs fail on NPUs due to lack of hardware-specific knowledge, and naive code transplantation leads to runtime crashes and performance degradation.

Method: Three core modules: 1) Run-Time Knowledge Synthesis Module with Triple-Part Executable Knowledge Representation linking error context with executable semantics; 2) Bottleneck-Aware Knowledge Retrieval Module using 2D-Retrieval in syntactic and hardware-aligned semantic spaces; 3) Effect-Driven Knowledge Distillation Module using LLM-driven semantic arbitration to prune errors and consolidate redundancies based on execution feedback.

Result: Extensive evaluations on real-world NPU workloads show Hawk elevates generation accuracy from 49.4% to 80.0%, achieving up to 2.2x execution speedup over state-of-the-art baselines.

Conclusion: Hawk successfully addresses the NPU kernel generation problem through a training-free framework that effectively integrates hardware-aware knowledge, significantly improving both correctness and performance compared to existing approaches.

Abstract: Developing high-performance kernels for Neural Processing Units (NPUs) is a critical industry bottleneck, requiring developers to manually navigate implicit hardware constraints and strict memory hierarchies. While large language models offer immense automation potential, they fail catastrophically on NPUs due to a fundamental lack of hardware-specific priors. Naively transplanting code snippets from similar NPU kernels may pass the compiler, but it consistently triggers runtime crashes and performance degradation by blindly violating underlying hardware constraints. To overcome this, we introduce Hawk, a training-free framework that harnesses hardware-aware knowledge through three core modules: (1) Run-Time Knowledge Synthesis Module, which employs a Triple-Part Executable Knowledge Representation to inherently couple the error context with executable semantics; (2) Bottleneck-Aware Knowledge Retrieval Module, which implements a 2D-Retrieval paradigm to project queries into orthogonal syntactic and hardware-aligned semantic spaces; and (3) Effect-Driven Knowledge Distillation Module, which leverages LLM-driven semantic arbitration to continuously distill the knowledge by pruning errors and consolidating redundancies based on the empirical execution feedback. Extensive evaluations on real-world NPU workloads demonstrate that Hawk elevates generation accuracy from 49.4% to 80.0%, while achieving up to a 2.2x execution speedup over state-of-the-art baselines.

</details>


### [20] [Safe and Adaptive Cloud Healing: Verifying LLM-Generated Recovery Plans with a Neural-Symbolic World Model](https://arxiv.org/abs/2607.01595)
*Junyan Tan,Haoran Lin,Siyuan Guo,Yichen Fang,Xinyue Luo,Tianyu Shen,Zeyu Qiao*

Main category: cs.AI

TL;DR: PASE is a novel fault self-healing framework that uses LLMs as a core Plan Synthesis Engine to generate recovery plans, combining neuro-symbolic program synthesis with DRL-optimized prompting for adaptive cloud system recovery.


<details>
  <summary>Details</summary>
Motivation: As cloud-based AI systems grow in scale and complexity, ensuring service reliability through rapid fault detection and adaptive recovery has become critical. Existing approaches using LLMs and DRL have sequential, loosely coupled architectures that underutilize LLMs' generative and reasoning capabilities.

Method: PASE introduces a paradigm shift by treating recovery as a neuro-symbolic program synthesis task. It employs an LLM as a Plan Synthesis Engine to generate structured recovery plans from semantic primitives. A Neural-Symbolic World Model verifies plan feasibility through simulation, while a Meta-Prompt Optimizer (trained via DRL) learns to generate optimal prompts that guide the LLM's planning process, creating a tight reason-plan-verify-adapt loop.

Result: Experiments on a real-world cloud fault injection dataset show PASE significantly outperforms state-of-the-art methods, reducing average system recovery time by over 40% and improving fault detection accuracy in unknown fault scenarios.

Conclusion: PASE advances autonomous system management by unifying LLM-based reasoning with model-assisted verification and meta-learned guidance, enabling dynamic, context-aware recovery strategy generation beyond predefined action spaces.

Abstract: As the scale and complexity of cloud-based AI systems continue to escalate, ensuring service reliability through rapid fault detection and adaptive recovery has become a critical challenge. While existing approaches integrate Large Language Models (LLMs) for semantic understanding and Deep Reinforcement Learning (DRL) for policy optimization, they often rely on sequential, loosely coupled architectures that underutilize the generative and reasoning capabilities of LLMs. In this paper, we propose a paradigm shift with PASE, a Planning-Aware Semantic self-healing engine, a novel fault self-healing framework that reconceptualizes recovery as a neuro-symbolic program synthesis task. PASE employs an LLM as a core Plan Synthesis Engine to generate structured recovery plans from a library of semantic primitives. A Neural-Symbolic World Model verifies plan feasibility through simulation, while a Meta-Prompt Optimizer, trained via DRL, learns to generate optimal prompts that guide the LLM's planning process. This tight reason-plan-verify-adapt loop enables dynamic, context-aware recovery strategy generation beyond predefined action spaces. Experiments on a real-world cloud fault injection dataset demonstrate that PASE significantly outperforms state-of-the-art methods, reducing average system recovery time by over 40% and improving fault detection accuracy in unknown fault scenarios. Our framework advances autonomous system management by unifying LLM-based reasoning with model-assisted verification and meta-learned guidance.

</details>


### [21] [SemHash-LLM: A Multi-Granularity Semantic Hashing Framework for Document Deduplication](https://arxiv.org/abs/2607.01601)
*Xinyi Fang,Kejian Tong,Jiabei Liu,Tao Ning,Yuhang He*

Main category: cs.AI

TL;DR: SemHash LLM is an efficient multi-granularity deduplication framework that combines semantic hashing, attention-weighted MinHash, and selective LLM verification to detect document duplicates with minimal computational cost.


<details>
  <summary>Details</summary>
Motivation: Large-scale document deduplication needs to be both efficient (handling massive corpora) and accurate (preserving semantic equivalence), but traditional methods struggle to balance computational cost with semantic understanding across different types of duplication patterns.

Method: Combines character, token, and document-level signals through gated fusion, then applies cascaded filtering with semantic projection hashing (learning binary codes in LLM embedding space), attention-weighted MinHash (emphasizing informative content over boilerplate), and selective LLM-based adjudication for uncertain cases. Uses adaptive decision boundaries and uncertainty estimation for robustness.

Result: Achieves strong duplicate detection quality with less than 1% neural verification cost, effectively handling template pollution, short text perturbation, containment, and viral fragments.

Conclusion: SemHash LLM provides an effective multi-granularity framework for large-scale document deduplication that balances efficiency and accuracy by combining lightweight hashing techniques with selective LLM verification.

Abstract: Large scale document deduplication must preserve semantic equivalence while remaining efficient over massive corpora. We present SemHash LLM, a multi granularity framework that unifies semantic projection hashing, attention weighted MinHash, contrastive boundary learning, and selective LLM based adjudication. The method combines character, token, and document level signals through gated fusion, then applies a cascaded filtering pipeline for efficient candidate reduction. Semantic projection hashing learns compact binary codes in distilled LLM embedding space, while attention weighted Min- Hash suppresses boilerplate and emphasizes informative content. Adaptive decision boundaries and uncertainty estimation further improve robustness across template pollution, short text perturbation, containment, and viral fragments. Experiments show that SemHash LLM achieves strong duplicate detection quality with less than one percent neural verification cost.

</details>


### [22] [Profit-Based Counterfactual Explanations for Product Improvement: A Case Study of Manga Sales in Japan](https://arxiv.org/abs/2607.01610)
*Keita Kinjo,Takeshi Ebina*

Main category: cs.AI

TL;DR: Proposes Profit-Based Counterfactual Explanation (PBCE), a framework that reformulates counterfactual explanation as profit maximization in management/marketing contexts, eliminating need for exogenous target specification and providing economic interpretation for distance metrics.


<details>
  <summary>Details</summary>
Motivation: Current CE methods have three key limitations: (1) require exogenous specification of desired output target, (2) use distance functions without clear practical interpretation in regression settings, and (3) focus on altering predictions rather than optimizing decision objectives, while real-world decision-making often requires explicit objective maximization.

Method: Formulates counterfactual explanation as a profit maximization problem in management and marketing contexts. Proposes PBCE framework that eliminates exogenous target specification by directly maximizing profit as primary optimization objective. Reinterprets distance term as cost of modifying product attributes, providing economically grounded interpretation.

Result: PBCE addresses limitations of existing CE methods by: (1) removing need for exogenous target specification, (2) providing clear economic interpretation for distance metrics as attribute modification costs, and (3) shifting focus from prediction alteration to objective optimization aligned with real-world decision-making needs.

Conclusion: The proposed PBCE framework advances counterfactual explanation methodology by grounding it in profit maximization objectives, making it more practical and interpretable for management and marketing applications where decision-making requires explicit objective optimization rather than just prediction manipulation.

Abstract: Counterfactual explanation (CE) is widely used to enhance the interpretability of machine learning models and support data-driven decision-making based on model predictions. However, existing CE methods typically require two exogenously specified inputs: a desired output value (target) and a distance function that quantifies changes in explanatory variables. In regression settings, neither the validity of target specification nor the practical interpretation of the distance metric has been sufficiently addressed. Furthermore, most existing CE methods focus on altering predictions rather than optimizing a decision objective, even though real-world decision-making often requires explicit objective maximization. To address these limitations, we formulate CE as a profit maximization problem in management and marketing contexts and propose a framework termed profit-based counterfactual explanation (PBCE). PBCE eliminates the need for exogenous target specification by directly maximizing profit as the primary optimization objective. Concurrently, the distance term is reinterpreted as the cost of modifying product attributes, providing a clear and economically grounded interpretation.

</details>


### [23] [Scaling with Confidence: Calibrating Confidence of LLMs for Adaptive Test Time Scaling](https://arxiv.org/abs/2607.01612)
*Xuqing Yang,Yi Yuan,Shanzhe Lei,Xuhong Wang*

Main category: cs.AI

TL;DR: C3RL is a reinforcement learning algorithm that improves LLM calibration between confidence and accuracy, enabling better resource allocation at inference time with CAS strategy.


<details>
  <summary>Details</summary>
Motivation: Current RL methods for LLMs prioritize correctness but neglect confidence calibration, causing models to become overconfident and hallucinate when uncertain, undermining reliability.

Method: C3RL integrates three reward components: correctness, confidence calibration, and dataset-informed reference accuracy. The authors also introduce CAS (Confidence-based Adaptive Test Time Scaling) for inference-time resource allocation based on confidence.

Result: C3RL improves calibration without sacrificing accuracy across 8 text and multimodal datasets, outperforming state-of-the-art. CAS reduces inference budget up to 12.33x while surpassing majority voting performance on in-domain and out-of-domain datasets.

Conclusion: The combination of C3RL and CAS enables more reliable and resource-efficient LLM deployment by addressing confidence calibration and adaptive computation allocation problems.

Abstract: Training large language models (LLMs) with reinforcement learning (RL) has significantly advanced their performance on reasoning and question-answering tasks. However, prevailing RL reward designs typically prioritize response correctness, neglecting to incentivize models to express their confidence accurately. This leads to a critical problem: performance gains are often accompanied by poor calibration between confidence and accuracy, misleading models to overconfidently hallucinate when uncertain. To address this limitation, we propose $\textbf{C}$orrectness and $\textbf{C}$onfidence $\textbf{C}$alibration $\textbf{R}$einforcement $\textbf{L}$earning ($\textbf{C3RL}$), a novel RL algorithm integrating correctness, calibration and dataset-informed reference accuracy rewards together. Comprehensive evaluation across 8 text and multimodal datasets demonstrates that C3RL enhances calibration without sacrificing accuracy, outperforming the current state-of-the-art method in both performance and calibration metrics. Utilizing the well-calibrated verbalized confidence from C3RL, we further introduce $\textbf{C}$onfidence-based $\textbf{A}$daptive Test Time $\textbf{S}$caling ($\textbf{CAS}$), an adjustable inference-time strategy that allocates computational resources based on response confidence. Experiments show that CAS surpasses majority voting on both in-domain and out-of-domain datasets while reducing the inference budget by up to 12.33 times. We believe the synergy of C3RL and CAS paves the way for deploying more reliable and resource-efficient LLMs. The code, data and models will be released.

</details>


### [24] [Spatial Support Matters: Geometry-Aware Graph Fusion for Rainfall Field Reconstruction](https://arxiv.org/abs/2607.01621)
*Low Jun Yu,Niramay Kachhadiya,Herath Mudiyanselage Viraj Vidura Herath,Sanka Rasnayaka,Lucy Amanda Marshall*

Main category: cs.AI

TL;DR: A geometry-aware multi-support heterogeneous graph neural network that fuses rainfall data from different sensor types (gauges, microwave links, radar/satellite) by representing each according to their spatial support dimensions, enabling improved fine-scale rainfall reconstruction.


<details>
  <summary>Details</summary>
Motivation: Real rainfall sensing systems have incompatible spatial supports: gauges measure points (0D), microwave links measure paths (1D), and radar/satellite measure gridded areas (2D). Existing heterogeneous graph approaches discard the geometry of measurement supports, which imposes distinct constraints on rainfall field reconstruction.

Method: Proposes a geometry-aware multi-support heterogeneous graph neural network that represents each observation according to its support type as distinct node layers (0D points, 1D lines, 2D grids). Fuses them through cross-support message passing into a point-support prediction layer. Uses inductive masked-node formulation to decouple prediction resolution from sensing resolution.

Result: On Singapore data, reduces RMSE by 23.2% over classical interpolation baseline (inverse-distance weighting). Consistently outperforms other neural architectures like convolutional fusion and support-agnostic heterogeneous graph baselines. Generalization study with Sydney data shows fusion delivers largest gains when field is under-sampled relative to its correlation length.

Conclusion: The proposed geometry-aware multi-support heterogeneous GNN effectively reconciles different rainfall sensor types by respecting their geometric constraints, enabling flexible fine-scale reconstruction. Performance gains are maximized when sensor spacing is large relative to rainfall correlation length, making it particularly valuable for under-sampled regions.

Abstract: Fine-scale rainfall reconstruction is critical for urban flood modeling, but real rainfall sensing systems observe the field through incompatible spatial supports: gauges measure points, microwave links measure paths, and radar/satellite products measure gridded areas. These differences in measurement support impose geometrically distinct constraints on the rainfall field, yet existing heterogeneous graph approaches reconcile such sources in feature space, giving each its own embedding while discarding the geometry of its support. We propose a geometry-aware multi-support heterogeneous graph neural network that represents each observation according to its support type (0D point, 1D line, or 2D grid) as a distinct node layer, and fuses them through cross-support message passing into a point-support prediction layer from which the field is reconstructed. An inductive masked-node formulation decouples prediction resolution from sensing resolution, allowing the same trained model to reconstruct the field at user-defined target locations or display grids. On Singapore data, the proposed method reduces RMSE by 23.2\% over the classical interpolation baseline, inverse-distance weighting, and consistently outperforms other neural architectures such as convolutional fusion and support-agnostic heterogeneous graph baselines. A generalization study using data from Sydney, Australia lets us characterize when multi-support fusion helps: the available skill appears to depend on gauge spacing relative to the spatial correlation length of the field, so fusion delivers the largest gains where the field is under-sampled relative to its correlation length and little when it is already resolved. Code and models will be open-sourced upon paper acceptance.

</details>


### [25] [Autonomous discovery of traffic laws with AI traffic scientists](https://arxiv.org/abs/2607.01639)
*Xingyuan Dai,Yue Liu,Xiaoyan Gong,Qinghai Miao,Junyou Shang,Yutong Wang,Chao Guo,Yonglin Tian,Yizhang Chai,Chao Xiang,Yisheng Lv,Fei-Yue Wang*

Main category: cs.AI

TL;DR: TrafficSci is an AI system that autonomously discovers traffic laws through an iterative workflow, rediscovering three established laws and identifying a new temporal memory scale in urban driving behavior across multiple cities.


<details>
  <summary>Details</summary>
Motivation: Current traffic law discovery relies on expert-driven approaches that require manual identification of patterns from heterogeneous data or intervention experiments. While AI has advanced scientific discovery in controlled lab settings, extending these capabilities to complex transportation domains remains challenging.

Method: TrafficSci formulates traffic-law discovery as an iterative, auditable workflow with three components: evidence scoping, critic-judge hypothesis induction, and observational-interventional validation.

Result: Across four case studies spanning population, network, control and trajectory scales, the system autonomously rediscovered three established traffic laws and identified an unreported intrinsic temporal memory scale in urban driving behavior. This finding was statistically consistent across eight cities and two trajectory datasets.

Conclusion: TrafficSci provides a pathway for extending AI-driven scientific discovery from controlled laboratory domains to complex, real-world urban systems like transportation networks.

Abstract: Universal traffic laws describe recurrent patterns in congestion, mobility and driving behavior across cities, providing a scientific basis for transportation planning, management and control. Their discovery, however, remains expert-driven, requiring candidate regularities to be identified from heterogeneous observational evidence or validated through intervention experiments. Although autonomous artificial intelligence (AI) systems have advanced scientific discovery in controlled laboratory settings, extending them to complex transportation domains remains a challenge. Here we present TrafficSci, an agentic AI system that formulates traffic-law discovery as an iterative, auditable workflow integrating evidence scoping, critic-judge hypothesis induction, and observational-interventional validation. Across four case studies spanning population, network, control and trajectory scales, TrafficSci autonomously rediscovers three established traffic laws and identifies an unreported intrinsic temporal memory scale in urban driving behavior, statistically consistent across eight cities and two trajectory datasets. TrafficSci provides a route for extending AI-driven scientific discovery from controlled domains to complex urban systems.

</details>


### [26] [Diverse Evidence, Better Forecasts: Multi-Agent Deliberation Under Information Asymmetry](https://arxiv.org/abs/2607.01661)
*Yuante Li,Yicheng Tao,Kate Zhang,Taozhi Wang,Gefei Gu,Yaxin Zhou*

Main category: cs.AI

TL;DR: Multi-agent LLM forecasting benefits from designed information asymmetry, where agents receive both shared public and disjoint private evidence, preventing herding and enabling genuine belief revision through deliberation.


<details>
  <summary>Details</summary>
Motivation: Current multi-agent forecasting systems overlook critical design choices about information distribution. When all agents receive identical evidence, deliberation collapses into herding rather than genuine belief revision, making multi-agent systems little better than single agents. The paper identifies this as a fundamental gap in existing approaches.

Method: Proposes designed information asymmetry: partitioning evidence into shared public and disjoint private subsets so each agent holds exclusive knowledge only accessible to others through deliberation. This is instantiated in InfoDelphi, a framework combining relevance-aware evidence routing, rationale-based iterative deliberation, and confidence-weighted aggregation.

Result: On PolyGym benchmark (375 binary forecasting questions from real-world prediction markets), InfoDelphi outperforms strongest single-agent and multi-agent baselines by 12-18% in Brier score and 4-8 percentage points in accuracy. Experiments confirm removing information asymmetry eliminates most deliberation gains.

Conclusion: Diversity of input through designed information asymmetry is the key enabler of effective multi-agent reasoning, as it reduces inter-agent error correlation and transforms deliberation from herding into genuine belief revision.

Abstract: Multi-agent systems are increasingly used for forecasting future events, as deliberation among multiple LLMs is believed to improve reasoning and calibration. Yet existing approaches overlook a critical design choice: what information each agent receives. When all agents are given identical evidence, deliberation collapses into herding rather than genuine belief revision, leaving multi-agent systems little better than a single agent. We identify this as a fundamental gap and propose designed information asymmetry to close it: by partitioning evidence into shared public and disjoint private subsets, each agent holds exclusive knowledge that can only reach others through deliberation. We theoretically show that this decomposition reduces inter-agent error correlation, and instantiate it in InfoDelphi, a framework combining relevance-aware evidence routing, rationale-based iterative deliberation, and confidence-weighted aggregation. On PolyGym, a benchmark of 375 binary forecasting questions derived from real-world prediction markets, InfoDelphi outperforms the strongest single-agent and multi-agent baselines by 12--18% in Brier score and 4--8 percentage points in accuracy. More detailed experiments confirm that removing information asymmetry eliminates most deliberation gains, establishing diversity of input as the key enabler of effective multi-agent reasoning.

</details>


### [27] [Separating Expert Retention from Autonomous Source Inference in Raw-ECG-Replay-Free Continual ECG Deployment](https://arxiv.org/abs/2607.01674)
*Yufan Lu,Xinhui Liu,Chenyang Xu,Yuxi Zhou,Hao Wang,Shenda Hong*

Main category: cs.AI

TL;DR: ECG-Founder++ is a method for incremental learning in multi-source ECG deployment that freezes a pretrained backbone, assigns isolated classifiers to new sources, and uses a lightweight router with top-2 margin fusion for source inference when metadata is unavailable.


<details>
  <summary>Details</summary>
Motivation: In multi-source ECG deployment, models need to incorporate new data sources when earlier raw ECGs cannot be retained or replayed, and deployment requires selecting appropriate experts when source metadata is unavailable.

Method: The method uses frozen ECGFounder features, adds balanced-softmax linear experts for each new domain, and trains a lightweight router on retained training features and domain labels. It employs validation-calibrated margin rule to fuse the two most likely experts instead of committing to a single routed expert.

Result: Source-aware expert selection achieves 0.7915±0.0036 Macro-F1, nearly matching offline performance. Without source IDs, MLP router achieves 0.7756±0.0027 and top-2 margin fusion reaches 0.7782±0.0022. The top-2 gain over hard routing is small (+0.0026) with confidence interval including zero. The gap to oracle performance is 0.0111-0.0133.

Conclusion: The method supports strong source-aware expert retention but is not memory-free since frozen training features are retained for router updates. Autonomous source inference remains the main bottleneck, with top-2 margin fusion providing only marginal improvement over hard routing.

Abstract: In multi-source ECG deployment, models may need to incorporate new data sources when earlier raw ECGs cannot be retained or replayed. Freezing a pretrained backbone and assigning each source an isolated classifier prevents parameter interference, but deployment still requires selecting an expert when source metadata are unavailable. We study this distinction through \ours{}, an incremental expert bank built on frozen 1024-dimensional ECGFounder features. Each arriving domain adds a balanced-softmax linear expert, while a lightweight router is fitted only on retained training features and domain labels from sources observed so far. A validation-calibrated margin rule fuses the two most likely experts instead of committing to a single routed expert.
  On CPSC, PTB-XL, Georgia, and Chapman-Shaoxing, source-aware expert selection reaches $0.7915\pm0.0036$ Macro-F1 and a matched offline independent-head reference reaches $0.7885\pm0.0009$, supporting strong source-aware expert retention. Without source IDs, an MLP router reaches $0.7756\pm0.0027$ and top-2 margin fusion reaches $0.7782\pm0.0022$. The top-2 gain over hard MLP routing is small ($+0.0026$), with a 95\% confidence interval from paired bootstrap that includes zero. Across three domain orders, the top-2-to-oracle gap remains $0.0111$--$0.0133$, identifying autonomous source inference as the main remaining bottleneck. No raw ECGs are replayed, but frozen training features are retained for router updates; the method is therefore not memory-free.

</details>


### [28] [Epistemic Goggles: A Pretrained Module that Induces an Epistemic Frame via Gradient Editing](https://arxiv.org/abs/2607.01690)
*Joshua Penman*

Main category: cs.AI

TL;DR: Goggles is a learned module that edits gradients during LLM fine-tuning to impart specific epistemic frames (like treating content as fiction), enabling models to learn from misaligned data without absorbing problematic beliefs or behaviors.


<details>
  <summary>Details</summary>
Motivation: Current language models suffer from "Negation Neglect" - when fine-tuned on documents explicitly labeled as fictional, models still believe the core claims about 91% of the time. This makes it dangerous to train models on potentially harmful or misaligned data, even when clearly marked as such.

Method: Goggles intervenes on the fine-tuning gradient rather than the data itself. It's a learned module that edits gradients a LoRA adapter receives during supervised fine-tuning, imparting a chosen epistemic frame (like "this is fiction") to whatever the documents teach. The module is trained once for a given base model, frame, and LoRA configuration, then applied frozen to new documents.

Result: With Goggles, models correctly identify fictional content 91% of the time (vs 9% baseline) while preserving capability on GPQA and TruthfulQA. The imparted frame persists under continued fine-tuning where prior interventions revert. The same architecture supports other frames like treating documents as part of an AI safety evaluation.

Conclusion: Goggles provides a path toward training language models on known-misaligned data without absorbing the behaviors that data demonstrates, addressing the fundamental problem of Negation Neglect through gradient intervention rather than data modification.

Abstract: Finetuning a language model on documents that are explicitly annotated as fictional results in a model that still actually believes the documents' core claims, an effect known as Negation Neglect. In our evaluations, models trained on documents prefixed and suffixed with such annotations correctly identify the relevant claims as fictional only about 9% of the time. To address this, we introduce Goggles, a learned module that intervenes on the finetuning gradient rather than the data. During supervised finetuning, a Goggles module edits the gradients an LLM LoRA receives, imparting a chosen epistemic frame (the stance the model takes toward the nature of what it reads) to whatever the documents teach. A Goggles instance is trained once for a given base model, frame, and LoRA configuration, then applied frozen to documents it was never trained on. Trained through Goggles on those same documents, now carrying no fictional annotation, the model flags the content as fictional roughly 91% of the time, while preserving capability (GPQA and TruthfulQA match or exceed baseline). The same architecture supports other frames: a Goggles instance can be trained to treat documents as "part of an AI safety evaluation by Redwood Research" rather than simply as fiction. The imparted frame persists under continued finetuning that pushes back toward the claim, where prior interventions revert. Goggles suggests a path toward training language models on known-misaligned data without absorbing the behaviors that data demonstrates.

</details>


### [29] [COMFYCLAW: Self-Evolving Skill Harnesses for Image Generation Workflows](https://arxiv.org/abs/2607.01709)
*Zongxia Li,Dawei Liu,Fuxiao Liu,Yuhang Zhou,Xiyang Wu,Jingxi Chen,Jing Xie,Xiaomin Wu,Lichao Sun*

Main category: cs.AI

TL;DR: COMFYCLAW is an agentic skill evolution framework for ComfyUI workflows that learns from previous executions to improve workflow construction through typed graph editing, automatic error reversion, visual verification, and reusable skill distillation.


<details>
  <summary>Details</summary>
Motivation: As agent workflows become repeated and domain-specific, there's a growing need for agents to recall workflow patterns, execution constraints, and user preferences from previous runs to improve efficiency and reliability in recurring tasks, particularly in workflow-based image generation.

Method: The system formulates workflow construction as typed graph editing, exposes stage-organized tools, automatically reverts invalid edits, uses region-level vision-language model verifier to translate visual failures into repair suggestions, and evolves a skill library by distilling trajectories, execution errors, and verifier feedback into reusable Agent Skills.

Result: COMFYCLAW achieves the best average image-generation evaluation score across all six agent configurations (4 benchmark splits, 3 agent models, 2 image backbones), outperforming verifier-only baselines without skill evolution. Human annotations also show preference for COMFYCLAW over variants without skill evolution.

Conclusion: Skill evolution is an effective mechanism for improving agent reliability and performance in recurring visual workflow construction, demonstrating that learning from past executions can significantly enhance agent capabilities in domain-specific repetitive tasks.

Abstract: Agents are increasingly used to construct workflows and assist humans in completing recurring tasks more efficiently. As these workflows become repeated and domain-specific, agent memory and reusable skills become increasingly important: agents should be able to recall workflow patterns, execution constraints, and user preferences from previous runs. We study this problem in workflow-based image generation and introduce COMFYCLAW, an agentic skill evolution harness for controlling ComfyUI workflows. COMFYCLAW formulates workflow construction as typed graph editing, exposes tools organized by construction stage, automatically reverts invalid edits, and uses a region-level vision-language model (VLM) verifier to translate visual failures into actionable repair suggestions. The framework further evolves a progressively disclosed skill library, where trajectories, execution errors, and verifier feedback from previous runs are distilled into reusable Agent Skills. Across four benchmark splits, three agent models, and two image backbones, COMFYCLAW achieves the best average image-generation evaluation score across all six agent configurations, outperforming a verifier-only baseline without skill evolution. Human annotations further show that annotators prefer COMFYCLAW over variants without skill evolution. Our results suggest that skill evolution is an effective mechanism for improving agent reliability and performance in recurring visual workflow construction.

</details>


### [30] [Generic Expert Coverage for Pruning SparseMixture-of-Experts Language Models](https://arxiv.org/abs/2607.01710)
*Yongqin Zeng,Sicheng Pan,Jiale Wang,Hai-tao Zheng,Hong-Gee Kim,Chunxia Ma,XiuTeng Zhou*

Main category: cs.AI

TL;DR: Generic TB-Coverage is a coverage-aware expert pruning method for MoE models that uses generic text corpora (WikiText2 and C4) for calibration instead of downstream data, preserving high-utility experts from each corpus to improve pruning quality.


<details>
  <summary>Details</summary>
Motivation: Sparsely activated Mixture-of-Experts language models have substantial structured redundancy among routed experts, but pruning them without downstream calibration data is challenging. Existing methods rely on single aggregated importance scores that bias retained experts toward dominant calibration patterns.

Method: Proposes Generic TB-Coverage which profiles per-expert utility separately on each calibration corpus (WikiText2 and C4) and enforces a fixed-budget coverage rule that preserves high-utility experts from each corpus before constructing the final pruning mask. This ensures cross-corpus coverage rather than collapsing expert utility into one aggregated score.

Result: Across Qwen1.5-MoE-A2.7B and DeepSeek-MoE-16B-Base at 25%, 50%, and 75% retention budgets, the method improves average accuracy on six common zero-shot benchmarks over random pruning, REAP, and ExpertSparsity, while reducing perplexity degradation on WikiText2 and C4. Gains are largest under aggressive pruning (25% and 50% retain).

Conclusion: Preserving cross-corpus expert coverage is an effective generic-data prior for MoE pruning. The improvements hold with fixed pruning budgets and no downstream calibration data, suggesting this approach works well with only generic text corpora for calibration.

Abstract: Sparsely activated Mixture-of-Experts (MoE) language models contain substantial structured redundancy among routed experts, but pruning them without downstream calibration data remains challenging. Existing expert-pruning methods typically rely on a single aggregated importance score, which can bias the retained set toward experts favored by dominant calibration patterns. We propose \textbf{Generic TB-Coverage}, a coverage-aware expert pruning method that uses only generic text corpora (WikiText2 and C4) for calibration. Instead of collapsing expert utility into one score, our method profiles per-expert utility separately on each corpus and enforces a fixed-budget coverage rule that preserves high-utility experts from each corpus before constructing the final pruning mask. Across Qwen1.5-MoE-A2.7B and DeepSeek-MoE-16B-Base at 25\%, 50\%, and 75\% retention budgets, our method improves average accuracy on six common zero-shot benchmarks over random pruning, REAP, and ExpertSparsity, while also reducing perplexity degradation on WikiText2 and C4. The gains are largest under aggressive pruning (25\% and 50\% retain), suggesting that preserving cross-corpus expert coverage is an effective generic-data prior for MoE pruning. Our improvements hold with fixed pruning budgets and no downstream calibration data.

</details>


### [31] [Distributionally Robust Listwise Preference Optimization](https://arxiv.org/abs/2607.01715)
*Xudong Wu,Jian Qian,Pangpang Liu,Vaneet Aggarwal,Jiayu Chen*

Main category: cs.AI

TL;DR: A robust listwise preference optimization method for language-model alignment that handles ranking-label uncertainty through a total-variation robust Plackett-Luce objective, with efficient computation and strong theoretical guarantees.


<details>
  <summary>Details</summary>
Motivation: Existing robust preference optimization focuses on pairwise supervision at dataset, prompt, or preference-pair levels, but doesn't address listwise ranking-label uncertainty where observed rankings may be ambiguous due to annotator inconsistency, near-ties, lossy rankwise feedback, or reward-model noise.

Method: Proposes a pointwise total-variation robust Plackett-Luce objective that directly robustifies ranking labels conditional on candidate lists. The robust loss decomposes into nominal PL loss plus worst-case PL correction, with efficient computation reducing the inner maximization from K! enumeration to O(K log K).

Result: The method shows strong theoretical guarantees: convexity with O(ε⁻²) sample complexity in offline settings, and weak convexity with Õ(ε⁻²) Moreau-envelope stationarity in online settings. Experiments demonstrate preserved performance under clean labels, improved robustness under noise, and enhanced reliability of reward-model-ranked candidate expansion.

Conclusion: The proposed robust listwise preference optimization effectively handles ranking-label uncertainty with tractable computation and strong theoretical guarantees, improving language-model alignment robustness in both offline and online settings.

Abstract: Existing robust preference optimization for language-model alignment mainly studies pairwise supervision and places robustness at the dataset, prompt, or preference-pair level. We instead study listwise preference optimization under ranking-label uncertainty: given a prompt and a candidate list, the observed ranking over that list may be ambiguous due to annotator inconsistency, near-ties, lossy rankwise feedback, or reward-model noise. We propose a pointwise total-variation robust Plackett--Luce objective that directly robustifies the ranking label conditional on the candidate list. The robust loss admits an exact decomposition into the nominal PL loss plus a worst-case PL correction, and the worst-case ranking is obtained by sorting current implicit scores in ascending order, reducing the inner maximization from $K!$ enumeration to $O(K\log K)$. This tractable structure yields strong offline and online optimization guarantees. In the offline fixed-list setting, the robust objective is convex and projected stochastic subgradient reaches global $ε$-suboptimality with $O(ε^{-2})$ sample complexity. In the online policy-induced setting, where candidate lists are generated by the current policy, we establish weak convexity and $\widetilde O(ε^{-2})$ Moreau-envelope stationarity. Experiments in offline LLM alignment show that the proposed robust correction largely preserves performance under clean labels and improves robustness under noise. In online alignment, it makes reward-model-ranked candidate expansion more reliable and improves both reward-model and external GPT-4 judge metrics.

</details>


### [32] [DRL-CLBA: A Clean Label Backdoor Attack for Speech Classification via DDPG Reinforcement Learning](https://arxiv.org/abs/2607.01729)
*Yueming Huang,Wenhan Yao,Fen Xiao,Xiarun Chen,Weiping Wen*

Main category: cs.AI

TL;DR: DRL-CLBA is a novel clean-label backdoor attack for speech classification that uses reinforcement learning (DDPG) and audio steganography to create sample-specific triggers without poisoned labels, making it highly stealthy and effective against defenses.


<details>
  <summary>Details</summary>
Motivation: Existing backdoor attacks for speech classification are vulnerable to detection through manual data inspection, especially those relying on poisoned labels. Sample-specific attacks can bypass many defenses but are still detectable via data defense mechanisms. There's a need for more stealthy attacks that can bypass advanced defenses while maintaining high effectiveness.

Method: The authors propose DRL-CLBA (Deep Reinforcement Learning-based Clean Label Backdoor Attack) which combines: 1) Deep Deterministic Policy Gradient (DDPG) reinforcement learning to optimize target samples toward trigger-bearing anchor points in the model's deep latent space, and 2) Deep audio steganography to embed sample-specific triggers into source audio, creating feature-space anchors. This enables label-migration-free poisoning of target samples.

Result: Experimental evaluation across three datasets and four different DNNs shows DRL-CLBA achieves high attack success rates and effectively bypasses various backdoor defenses. The attack demonstrates strong resistance against fine-tuning, pruning, and spectral signature defenses, highlighting critical vulnerabilities in speech-controlled systems.

Conclusion: DRL-CLBA represents a significant advancement in stealthy backdoor attacks for speech classification, successfully combining reinforcement learning with audio steganography to create sample-specific, clean-label attacks that evade current defense mechanisms. This exposes critical security vulnerabilities in speech-controlled systems that need to be addressed by future defense research.

Abstract: Deep learning models for speech classification are vulnerable to backdoor attacks, where malicious triggers cause misclassification at inference time. While sample-specific attacks can bypass many defenses, they often rely on poisoned label attack, making them detectable via manual data defense. In this paper, we propose DRL-CLBA, a novel clean label backdoor attack for speech classification that leverages Deep Deterministic Policy Gradient (DDPG) reinforcement learning. We also utilize deep audio steganography to embed sample-specific triggers into source audio, creating feature-space anchors. The proposed reinforcement learning framework effectively optimizes target samples toward trigger-bearing anchor points in the model's deep latent space, enabling label-migration-free poisoning of target samples. Experimental results across three datasets and four different DNNs demonstrate that DRL-CLBA achieves a high attack success rate, effectively bypassing some backdoor defenses. The attack demonstrates strong resistance against fine-tuning, pruning, and spectral signature defenses, exposing critical vulnerabilities in speech-controlled systems.

</details>


### [33] [Reformalization of the Jordan Curve Theorem](https://arxiv.org/abs/2607.01734)
*Simon Guilloud,Sankalp Gambhir,Samuel Chassot*

Main category: cs.AI

TL;DR: The paper presents a case study in reformalization - converting formal proofs between different proof assistants, specifically reformalizing the Jordan Curve Theorem from Mizar/HOL Light to Lean/Agda, with analysis of pipeline design choices.


<details>
  <summary>Details</summary>
Motivation: The motivation is to explore reformalization (a variant of autoformalization) where the input is an existing formal proof in one proof assistant that gets translated to another proof assistant, addressing practical interoperability between different formal proof systems.

Method: The method involves conducting three concrete reformalization case studies: 1) from Mizar to Lean, 2) from HOL Light to Lean, and 3) from HOL Light to Agda, using the Jordan Curve Theorem as the test case, then analyzing the results and pipeline design choices.

Result: The paper reports successful reformalizations of the Jordan Curve Theorem across different proof assistant pairs, identifies key pipeline design choices that impact practical reformalization tasks, and provides insights into the reformalization process between various proof assistant ecosystems.

Conclusion: Reformalization between proof assistants is practically feasible, and careful pipeline design choices significantly affect the success and efficiency of reformalization tasks, contributing to better interoperability between formal verification systems.

Abstract: We present a case study in reformalization, a variant of autoformalization in which the input proof is not natural language but a formal development in a different proof assistant. Concretely, we report three reformalizations of the Jordan Curve Theorem: from Mizar to Lean, from HOL Light to Lean, and from HOL Light to Agda. We analyse the results and identify pipeline design choices that matter for practical reformalization tasks.

</details>


### [34] [Meta-Benchmarks for Financial-Services LLM Evaluation](https://arxiv.org/abs/2607.01740)
*Blair Hudson*

Main category: cs.AI

TL;DR: A meta-benchmarking framework that organizes 452 public benchmarks into 41 O*NET work activities and 38 banking domains, using weighted Elo tournaments to evaluate LLMs for financial services work rather than relying on average global performance metrics.


<details>
  <summary>Details</summary>
Motivation: Public LLM leaderboards optimize for global average performance but fail to capture specific cognitive demands of financial-services work. Different models excel in different areas, and current benchmarks don't reflect real-world banking needs across sales, operations, risk, and support work.

Method: Created a meta-benchmarking framework organizing 452 benchmarks into 41 O*NET Generalized Work Activities and 38 BIAN banking domains. Used multiplicative weighting scheme (discrimination x coverage x recency) that rewards benchmarks still separating best models. Applied weighted K-factor in pairwise Elo tournament to produce cross-benchmark-comparable work-activity scores without raw score normalization.

Result: Demonstrated the framework on a point-in-time public snapshot covering 288 models across 25 organizations as of June 2026. The approach produces work-activity and business-domain scores that better reflect financial-services capabilities than traditional leaderboards.

Conclusion: The framework provides a reproducible approach for financial institutions facing model selection and governance challenges, offering more relevant evaluation of LLMs for specific banking tasks by suppressing saturated legacy tests and focusing on active, discriminative benchmarks.

Abstract: Public LLM leaderboards optimise for global average performance and do not capture the specific cognitive demands of financial-services work: a model that leads on MMLU-Pro may underperform on document-grounded compliance reasoning, and a coding leader may handle multi-turn customer interactions poorly. We present a meta-benchmarking framework that organises 452 publicly reported benchmarks into 41 O*NET Generalized Work Activities and aggregates those into 38 BIAN banking business domains spanning sales, operations, risk, and support work. A multiplicative weighting scheme (discrimination x coverage x recency), computed over a rolling model window, rewards benchmarks that still separate the best models, are widely reported, and remain in active use, suppressing saturated legacy tests automatically. These weights scale the K-factor in a pairwise Elo tournament, producing cross-benchmark-comparable work-activity scores without raw score normalisation; business-domain scores are weighted averages of the constituent work-activity Elos. We demonstrate the framework on a point-in-time public snapshot covering 288 models across 25 organisations as of June 2026, and describe the methodology, full taxonomy, design decisions, and limitations with the aim of making the approach reproducible for institutions facing similar selection and governance challenges.

</details>


### [35] [Path-level Hindsight Instructions for Semantic Exploration in Vision-Language Navigation](https://arxiv.org/abs/2607.01754)
*Sung June Kim,Sangpil Kim,Honglak Lee*

Main category: cs.AI

TL;DR: Phi-Nav is an on-policy VLN framework that uses hindsight reasoning to align language instructions with exploratory trajectories, enabling effective training with limited expert demonstrations.


<details>
  <summary>Details</summary>
Motivation: On-policy exploration in Vision-Language Navigation creates semantic mismatch between executed visual streams and original language instructions, as exploratory trajectories deviate from expert demonstrations. This leads to a supervision gap where semantically unlabeled movements lack dense training signals.

Method: Phi-Nav uses a three-stage dual-supervision cycle: 1) Oracle-guided on-policy exploration with expert action feedback, 2) Hindsight speaker synthesizes path-level instructions from collected visual observations, 3) Second imitation pass treats synthesized trajectory-instruction pairs as additional expert demonstrations. This bridges semantic supervision gap.

Result: Phi-Nav achieves competitive performance on R2R-CE and RxR-CE benchmarks while requiring only a fraction of the expert demonstrations used by current baselines.

Conclusion: The work demonstrates the necessity of semantic exploration in VLN and positions Phi-Nav as an effective solution for training embodied agents with limited data by transforming semantically unlabeled movements into dense training signals through hindsight reasoning.

Abstract: On-policy exploration is a crucial component for training robust Vision-Language Navigation agents, as it exposes the policy to a broader state distribution. However, such exploration inevitably leads to trajectories that deviate from expert demonstrations, resulting in a semantic mismatch between the executed visual stream and the original language instruction. In this work, we address this challenge by introducing Phi-Nav, a unified on-policy framework that leverages hindsight reasoning to align instructions with the agent's actual exploratory journey. Specifically, Phi-Nav operates through a three-stage dual-supervision cycle: 1) the agent performs oracle-guided on-policy exploration, sampling a trajectory while learning from expert action feedback, 2) a hindsight speaker synthesizes a path-level hindsight instruction grounded in the collected visual observations, and 3) the agent conducts a second imitation pass, treating the synthesized trajectory-instruction pair as an additional expert demonstration. Through this process, Phi-Nav bridges the critical semantic supervision gap inherent in on-policy methods, transforming semantically unlabeled movement into dense training signals. Evaluations on the R2R-CE and RxR-CE benchmarks show that Phi-Nav yields competitive performance while requiring only a fraction of the expert demonstrations used by current baselines. These results underscore the necessity of semantic exploration in VLN, positioning Phi-Nav as an effective solution for training embodied agents with limited data.

</details>


### [36] [Mastermind: Strategy-grounded Learning for Repository-Scale Vulnerability Reproduction](https://arxiv.org/abs/2607.01764)
*Mingzhe Du,Luu Anh Tuan,Tianyi Wu,Renyang Liu,Zhijiang Guo,Dong Huang,See-Kiong Ng*

Main category: cs.AI

TL;DR: Mastermind is a dual-loop framework that learns transferable strategies for repository-level vulnerability reproduction, separating strategy learning from task execution to improve LLM agents' success rates.


<details>
  <summary>Details</summary>
Motivation: Current LLM agents for vulnerability reproduction often fail by choosing wrong strategies, even when they can execute steps correctly. Strategy is identified as the right learning unit - compact enough to optimize, concrete enough to guide execution, and stable enough to reuse.

Method: Dual-loop framework with trainable planner that learns reusable vulnerability-reproduction strategies through Supervised Fine-Tuning (SFT) and milestone-based GRPO, plus experience loop maintaining task-local strategy records. Planner is trained independently from executor, allowing strategy learning to improve multiple frozen executors.

Result: Achieved 84.5% pass rate with GPT-5.5 as frozen executor, outperforming open-book PoC context (60.0%), Best-of-8 sampling (63.0%), and iterative improvement (77.0%). Same planner improved GPT-5.4 mini and GLM~5.1 from 45.0%/58.5% to 60.0%/71.0% respectively.

Conclusion: Learning high-level strategies is an effective and transferable mechanism for improving repository-scale software engineering agents, demonstrating strategy as the optimal learning unit for complex SE tasks.

Abstract: Repository-level vulnerability reproduction is a demanding software engineering (SE) task: an agent must inspect a codebase, infer the input grammar that reaches a vulnerable path, construct a proof-of-conceptv(PoC), and verify that the crash disappears on the patched build. Recent LLM agents can often execute these steps when the approach is correct, yet they still fail by choosing the wrong strategy. This paper argues that strategy, rather than the full action trajectory, is the right learning unit for such SE agents: it is compact enough to optimize, concrete enough to guide execution, and stable enough to store and reuse across attempts. We present Mastermind, a dual-loop framework that separates transferable strategy learning from task-specific experience. A trainable planner learns reusable vulnerability-reproduction strategies through SFT and milestone-based GRPO, while an experience loop maintains task-local strategy records that guide subsequent attempts. The planner is trained independently of the executor, allowing strategy learning to improve multiple frozen executors without modifying their action-generation capability. We evaluate Mastermind on CyberGym using 260 training tasks and 200 held-out evaluation tasks. With GPT-5.5 as the frozen executor, Mastermind achieves an 84.5% pass rate, outperforming open-book PoC context (60.0%), Best-of-8 sampling (63.0%), and iterative improvement (77.0%). The same planner also improves GPT-5.4 mini and GLM~5.1 from 45.0% and 58.5% to 60.0% and 71.0%. These results demonstrate that learning high-level strategies is an effective and transferable mechanism for improving repository-scale SE agents.

</details>


### [37] [SimWorlds: A Multi-Agent System for Dynamic 3D Scene Creation](https://arxiv.org/abs/2607.01766)
*Chunjiang Liu,Xiaoyuan Wang,Haoyu Chen,Yizhou Zhao,Ming-Hsuan Yang,László A. Jeni*

Main category: cs.AI

TL;DR: SimWorlds is a multi-agent framework that generates dynamic, editable 4D scenes (3D + time) from text descriptions using Blender-specific procedural knowledge, addressing challenges in coordinating physics, timing, and verification that static text-to-scene systems don't face.


<details>
  <summary>Details</summary>
Motivation: Existing text-to-3D systems focus on static output, but dynamic 4D scenes (with liquids, particles, rigid bodies, articulated mechanisms) remain largely unexplored despite their value for editable content and physics-grounded training data for video generation and embodied AI. Two key challenges in dynamic scenes: coordinating multiple physics solvers, temporal sequencing, camera, lighting in coherent scenes; and verifying motion correctness from rendered video is harder than judging single images.

Method: SimWorlds uses a multi-agent framework with: 1) Blender-specific procedural knowledge, 2) a planner-coder-reviewer workflow driving a fixed ordered sequence of construction stages, 3) a layered scene protocol enforced by a deterministic verifier, and 4) a runtime-state inspection tool suite that catches mechanism failures not visible in rendered images. The system also introduces 4DBuildBench benchmark for assessing visual fidelity and physical consistency.

Result: Experiments show that SimWorlds outperforms prior dynamic Blender generation baselines. The system successfully generates dynamic scenes with flowing liquids, emitting particles, cascading rigid bodies, and moving articulated mechanisms from text descriptions alone.

Conclusion: SimWorlds advances text-to-4D scene generation by addressing the unique coordination and verification challenges of dynamic scenes through a multi-agent framework with specialized procedural knowledge, structured workflows, and verification tools, enabling the creation of physically consistent, editable dynamic content from natural language descriptions.

Abstract: LLM agents are increasingly used to translate natural language into 3D scenes in a procedural way, but existing systems focus on static output. Dynamic 4D scenes from text alone, in which liquids flow, particles emit, rigid bodies cascade, and articulated mechanisms move, remain largely unexplored despite their value as editable content and as physics-grounded training data for video generation and embodied AI. Two challenges set the dynamic case apart from static text-to-scene work: an agent must jointly coordinate spatial layout, multiple physics solvers, temporal sequencing, camera, and lighting in a single coherent scene, and verifying motion correctness from rendered video is fundamentally harder than judging a single image. We present SimWorlds: a multi-agent framework that produces dynamic, editable 4D scenes from text, with Blender-specific procedural knowledge, a planner-coder-reviewer workflow driving a fixed ordered sequence of construction stages, a layered scene protocol enforced by a deterministic verifier, and a runtime-state inspection tool suite that catches mechanism failures the rendered image cannot reveal. We also introduce 4DBuildBench, a benchmark for assessing both visual fidelity and physical consistency of the procedural dynamic 3D scenes generated from text prompts. Experiments show that SimWorlds outperforms prior dynamic Blender generation baselines.

</details>


### [38] [Repair the Amplifier, Not the Symptom: Stable World-Model Correction for Agent Rollouts](https://arxiv.org/abs/2607.01767)
*Xinyuan Song,Zekun Cai*

Main category: cs.AI

TL;DR: WM-SAR (World-Model Subgraph Amplification Repair) is a new approach that identifies causal error-amplifying subgraphs in large planning workflows and sends only those to LLMs for repair, outperforming traditional engineering correctors that scan for visible symptoms.


<details>
  <summary>Details</summary>
Motivation: As agent planning moves to persistent workflows with thousands of steps, failures occur inside large planning graphs. Full-graph replay is computationally expensive, consumes large context budgets, exposes LLMs to irrelevant symptoms, and degrades long-context retrieval. The missing component is an efficient world-model corrector that repairs failed planning graphs in place.

Method: The paper compares two families of correctors: (1) Engineering LLM correctors that scan nodes/edges, choose suspicious regions, and ask LLMs to repair them, and (2) WM-SAR that works backward from subgraph amplification, identifies nodes/edges that keep re-amplifying errors, and sends only that causal subgraph to the LLM.

Result: WM-SAR substantially outperforms engineering correctors under realistic token budgets, achieves near-whole-graph stabilization with a compact region, and gives the LLM a cleaner repair target. Engineering correctors can help but require very large contexts.

Conclusion: WM-SAR provides a more efficient approach to repairing large planning graphs by focusing on causal error-amplifying subgraphs rather than visible symptoms, enabling better performance within practical context budgets and offering cleaner repair targets for LLMs.

Abstract: As agent planning moves from short tool chains toward persistent workflows with thousands or tens of thousands of steps, failures will occur inside large planning graphs rather than in isolated predictions. Replanning the entire graph after every mistake is neither computationally realistic nor desirable: full-graph replay consumes large context budgets, exposes the LLM to many irrelevant symptoms, and can degrade long-context retrieval. This paper studies the missing component in such systems: a world-model corrector that repairs the failed planning graph in place. We compare two families of correctors. The first is the common engineering approach: scan nodes and edges, choose a suspicious local region, and ask an LLM to repair it. We implement strong engineering LLM correctors and find that they can help, especially when given very large contexts. The second family is our approach, WM-SAR (World-Model Subgraph Amplification Repair): instead of scanning for visible symptoms, it works backward from subgraph amplification, identifies the nodes and edges that keep re-amplifying error, and sends only that causal subgraph to the LLM. Across graph simulations and LLM repair experiments, WM-SAR substantially outperforms engineering correctors under realistic token budgets, achieves near-whole-graph stabilization with a compact region, and gives the LLM a cleaner repair target.

</details>


### [39] [Verifiable Knowledge Expansion through Retrieval-Grounded Formal Concept Analysis](https://arxiv.org/abs/2607.01773)
*Yujin Yang,Heejung Lee*

Main category: cs.AI

TL;DR: A retrieval-augmented small language model framework that uses formal concept analysis for symbolic verification of knowledge expansion, applied to rare ataxia ontology construction.


<details>
  <summary>Details</summary>
Motivation: Language models can propose knowledge structures from text, but their outputs can be unsupported or inconsistent, requiring verification for reliable ontology construction.

Method: Uses formal concept analysis as symbolic verification loop: starting from seed attributes, FCA proposes implications over formal context, then a retrieval-grounded SLM oracle validates implications or returns counterexamples, with support for incidence judgments, consistency checks, and attribute proposals.

Result: In rare ataxia setting from Orphadata: retrieval-grounded 10-seed runs achieve relation F1 of 0.29-0.52 and closure-based implication F1 of 0.22-0.30; larger seed sets increase evaluated implications and often improve implication F1; incidence judgments in fixed object-attribute setting can improve closure-based implication scores.

Conclusion: The framework provides inspectable ontology construction with verification, but identifying positive object-attribute pairs remains challenging even with fixed candidates, highlighting the difficulty of precise knowledge extraction and verification.

Abstract: Ontology construction requires deciding which objects, attributes, and structural relations should be accepted as valid knowledge. Language models can propose such structures from text, but their outputs can still be unsupported or inconsistent. This paper proposes a retrieval-augmented small language model (SLM) framework that uses formal concept analysis (FCA) as a symbolic verification loop for knowledge expansion. Starting from seed attributes, FCA proposes implications over a growing formal context. A retrieval-grounded SLM oracle then validates each implication or returns a counterexample. The oracle also supports incidence judgments, consistency checks, and attribute proposals, making accepted implications, counterexamples, contradictions, and corrections inspectable. In a rare ataxia setting constructed from Orphadata resources, retrieval-grounded 10-seed runs obtain relation F1 of 0.29-0.52 and closure-based implication F1 of 0.22-0.30. Larger seed sets increase the number of evaluated implications and often improve implication F1. The lower implication scores reflect a stricter evaluation of derived implications, where one missed or extra relation can affect several implication judgments. Ablations show that incidence judgments in a fixed object-attribute setting can improve closure-based implication scores. However, identifying positive object-attribute pairs remains difficult even when the candidate objects and attributes are fixed.

</details>


### [40] [Subliminal Clocks: Latent Time Modelling in Diffusion Language Models](https://arxiv.org/abs/2607.01774)
*Maximo Rulli,Thomas Fontanari,Simone Petruzzi,Federico Alvetreti,Giorgio Strano,Donato Crisostomi,Giorgos Nikolaou,Tommaso Mencattini,Andrea Santilli,Emanuele Rodolà,Simone Scardapane,Alessio Devoto*

Main category: cs.AI

TL;DR: DLMs internally encode denoising progress information that can be extracted, manipulated to control model behavior, and shows structured geometric properties.


<details>
  <summary>Details</summary>
Motivation: DLMs don't explicitly condition on diffusion timesteps like standard approaches, raising questions about whether they internally represent denoising progress and how this information is used.

Method: Probes were used across DLM layers to extract latent representations of denoising progress from internal activations, and manipulation was done by steering models along low-dimensional subspaces associated with inferred timesteps.

Result: DLMs do encode timestep-related information in residual streams, which can be reliably extracted and manipulated to systematically modulate model confidence and entropy, with the representation showing structured, interpretable geometric properties.

Conclusion: DLMs internally represent denoising progress through structured latent signals that are both decodable and manipulable, revealing insights about how these models process denoising information.

Abstract: Diffusion Language Models (DLMs) have recently emerged as a promising alternative to autoregressive models. Unlike standard diffusion-based approaches, DLMs are not explicitly conditioned on a timestep, raising a natural question: do these models internally represent denoising progress, and how is such information used downstream? In this work, we show that DLMs do in fact encode a latent representation related to the diffusion timestep within their residual streams. We find that this signal can be reliably extracted using probes across layers, indicating that denoising progress is decodable from internal activations. We further demonstrate that steering the model along a low-dimensional subspace associated with the inferred timestep allows us to systematically modulate its notion of denoising progress, leading to predictable changes in model confidence and entropy. Finally, we analyse the geometry of the identified representation, showing that it exhibits structured and interpretable properties in activation space, and shedding light on how such a signal is processed by these models.

</details>


### [41] [Safety Testing LLM Agents at Scale: From Risk Discovery to Evidence-Grounded Verification](https://arxiv.org/abs/2607.01793)
*Yunhao Feng,Ruixiao Lin,Ming Wen,Qinqin He,Yanming Guo,Yifan Ding,Yutao Wu,Jialuo Chen,Yunhao Chen,Xiaohu Du,Jianan Ma,Zixing Chen,Zhuoer Xu,Xingjun Ma,Xinhao Deng*

Main category: cs.AI

TL;DR: Vera is an automated safety testing framework for LLM agents that discovers emerging risks, generates executable test cases, and evaluates agents in sandboxed environments, revealing significant safety vulnerabilities in current systems.


<details>
  <summary>Details</summary>
Motivation: Current safety testing for LLM agents relies on expert-designed violations with hard-coded evaluation rules, making it costly to extend as agents evolve. There's a need for automated, scalable safety testing that can adapt to rapidly changing agent capabilities and risks.

Method: Three-stage pipeline: 1) Literature-driven exploration to discover and structure emerging risks into taxonomies, 2) Combinatorial composition across taxonomy dimensions to generate executable safety cases with concrete goals and verification predicates, 3) Adaptive execution in isolated sandboxes with control agents steering multi-turn interactions and evidence-grounded verifiers.

Result: Tested on four production agent frameworks (OpenClaw, Hermes, Codex, Claude Code), revealing average attack success rates of 93.9% under multi-channel attacks. Released Vera-Bench with 1600 executable safety cases covering 124 risk categories across three execution settings.

Conclusion: Modular, executable testing infrastructure is essential for rigorous and maintainable safety evaluation of rapidly evolving agentic systems at scale. The framework successfully automates safety testing and reveals substantial safety weaknesses in current LLM agents.

Abstract: LLM agents increasingly perform autonomous actions through external tools, leading to complex and evolving safety risks. However, existing safety testing targets expert-designed safety violations, and the corresponding outcomes are evaluated by hard-coded rules, making them costly to extend as agents evolve. To this end, we present Vera, an end-to-end automated safety testing framework that instantiates software engineering testing principles for non-deterministic agents through a three-stage, self-reinforcing pipeline. First, a literature-driven exploration continuously discovers and structures emerging risks into taxonomies of safety risks, attack methods, and tool execution environments. Second, combinatorial composition across taxonomy dimensions produces executable safety cases, each specifying a concrete safety goal, a programmatically constructed initial state, and a deterministic verification predicate grounded in observable artifacts. Third, adaptive execution runs heterogeneous agents in isolated sandboxes where a control agent steers multi-turn interaction based on runtime observations, while evidence-grounded verifiers judge outcomes from environment state and tool-call evidence rather than model self-report. We evaluate Vera on four production agent frameworks (OpenClaw, Hermes, Codex, Claude Code), revealing substantial safety weaknesses, with average attack success rates reaching 93.9\% under multi-channel attacks; we also release Vera-Bench, comprising 1600 executable safety cases spanning 124 risk categories across three execution settings. These results indicate that modular, executable testing infrastructure is essential for rigorous and maintainable safety evaluation of rapidly evolving agentic systems at scale. The code is publicly available at https://github.com/Yunhao-Feng/Vera.

</details>


### [42] [MMIR-TCM: Memory-Integrated Multimodal Inference and Retrieval for TCM Clinical Decision Support](https://arxiv.org/abs/2607.01814)
*Lihui Luo,Joongwon Chae,Ziyan Chen,Yang Liu,Siyi Cheng,Weihan Gao,Zelin Zeng,Xiaoming Yin,Samaneh Beheshti Kashi,Dongmei Yu,Lian Zhang,Jing Sui,Zeming Liang,Jiansong Ji,Peter E. Lobie,Peiwu Qin*

Main category: cs.AI

TL;DR: MMIR-TCM is a novel multimodal AI framework that integrates MLLM with memory-augmented segmentation and RAG to automate Traditional Chinese Medicine diagnosis by bridging visual tongue features with textual clinical reasoning, outperforming leading models like GPT-4o.


<details>
  <summary>Details</summary>
Motivation: Traditional Chinese Medicine diagnosis through tongue inspection faces challenges with subjectivity and reproducibility. The semantic gap between visual tongue features and textual reasoning, plus lack of large-scale standardized datasets, hampers AI applications in TCM clinical tasks like syndrome differentiation and prescription generation.

Method: Three-stage architecture: 1) Training-free Memory-SAM module for robust tongue extraction, 2) Fine-tuned Qwen3-VL model for structured tongue diagnosis generation, 3) Qwen3-based RAG component for evidence-grounded clinical decision support. Developed using MedTCM, a new large-scale multimodal dataset for TCM research.

Result: MMIR-TCM significantly outperforms leading models including GPT-4o and Gemini 2.5 Flash. The team also developed TDEU, a domain-specific evaluation metric that incorporates semantic understanding and diagnostic importance to properly assess clinical accuracy.

Conclusion: The framework successfully addresses key challenges in TCM AI applications by emulating expert diagnostic processes through multimodal integration, memory-augmented segmentation, and retrieval-augmented generation, providing a robust solution for automated TCM clinical decision support.

Abstract: Traditional Chinese Medicine (TCM) diagnosis, particularly through tongue inspection, faces persistent challenges in subjectivity and reproducibility. The application of multimodal artificial intelligence to TCM clinical tasks, such as syndrome differentiation and prescription generation, is significantly hampered by the semantic gap between visual tongue features and textual reasoning, as well as the lack of large-scale, standardized datasets. To address these challenges, we introduce MMIR-TCM, a novel framework that emulates the diagnostic process of TCM experts by integrating multimodal large language model(MLLM) with memory-augmented segmentation and retrieval-augmented generation (RAG). Employing a three-stage architecture, MMIR-TCM integrates a training-free Memory-SAM module for robust tongue extraction, a fine-tuned Qwen3-VL model for structured tongue diagnosis generation, and a Qwen3-based RAG component for evidence-grounded clinical decision support generation. The framework was developed and validated using MedTCM, a new large-scale multimodal dataset that we introduce specifically for advanced TCM research. To properly evaluate our framework's clinical accuracy, which existing metrics fail to capture, we also developed TDEU, a domain-specific evaluation metric incorporating semantic understanding and diagnostic importance. Our comprehensive experiments demonstrate that MMIR-TCM significantly outperforms leading models, including GPT-4o and Gemini 2.5 Flash.

</details>


### [43] [Pre-Flight: A Benchmark for Evaluating Large Language Models on Aviation Operational Knowledge](https://arxiv.org/abs/2607.01829)
*Alex Brooker,Tim Hughes*

Main category: cs.AI

TL;DR: Pre-Flight is an open-source benchmark of 300 multiple-choice questions for evaluating LLMs on aviation-specific operational knowledge, where even the best 2026 model reaches only 82.7% accuracy compared to ~95% expert reference.


<details>
  <summary>Details</summary>
Motivation: LLMs are increasingly proposed for aviation operations, but general benchmarks don't measure safety-critical domain knowledge. The high-stakes, regulated nature of aviation makes this gap particularly consequential.

Method: Created Pre-Flight benchmark with 300 multiple-choice questions drawn from international standards and airport ground operations material, authored/reviewed by aviation practitioners. Evaluated contemporary models using Inspect evaluation framework with standard multiple-choice protocol.

Result: Even the strongest model (released in 2026) reaches only 82.7% accuracy, compared to ~95% expert reference from aviation professionals. Performance has improved only gradually from ~75% in early 2025, leaving a substantial and persistent gap below expert-level reliability.

Conclusion: Domain-specific evaluation like Pre-Flight is necessary for responsible deployment of generative AI in non-safety-critical aviation operations, as current LLMs still fall significantly short of expert reliability despite gradual improvements.

Abstract: Large language models (LLMs) are increasingly proposed for aviation business operations, from documentation and training generation to customer facing assistants. General purpose benchmarks do not measure whether a model reasons safely and correctly about aviation specific operational knowledge, and the high stakes, regulated nature of the domain makes that gap consequential. We present Pre-Flight, an open source benchmark of 300 multiple choice questions drawn from international standards and airport ground operations material, covering international airport ground operations, ICAO and US FAA regulations, aviation general knowledge and complex operational scenarios. Questions were authored and reviewed by practitioners with experience in air traffic management, ground operations and commercial flying. We evaluate a range of contemporary commercial and open weight models using the Inspect evaluation framework, scoring by accuracy under a standard multiple choice protocol, and we maintain the leaderboard on a rolling basis as new models are released. Against an informal expert reference of around 95%, obtained from a low sample quiz of aviation professionals at a conference, even the strongest model evaluated (released in 2026) reaches 82.7%, having improved only gradually from roughly 75% in early 2025. A substantial and persistent gap below expert level reliability therefore remains. We release the dataset, the evaluation harness and the results, and the benchmark is available within the community evaluations package distributed with inspect_evals. We argue that domain specific evaluation of this kind is a necessary precondition for responsible deployment of generative AI in non safety critical aviation operations.

</details>


### [44] [Actual causality in fault trees](https://arxiv.org/abs/2607.01840)
*Georgiana Caltais,Milan Lopuhaä-Zwakenberg,Mariëlle Stoelinga*

Main category: cs.AI

TL;DR: Fault trees can be used for both risk analysis (what can go wrong) and failure diagnostics (why has it gone wrong) by applying Halpern & Pearl's theory of actual causality to connect minimal cut sets with actual causes.


<details>
  <summary>Details</summary>
Motivation: Fault trees are effective for risk modeling and answering "what can go wrong?" through minimal cut set analysis, but they currently lack the ability to answer the diagnostic question "why has it gone wrong?" which is fundamental for failure analysis.

Method: The authors study fault trees from the perspective of Halpern & Pearl's theory of actual causality, providing a complete classification of different notions of actual causality in terms of both the fault tree's graph structure and logical structure.

Result: The research shows how minimal cut sets give rise to actual causes, establishing a connection between fault tree structural analysis and causal reasoning for diagnostic purposes.

Conclusion: By applying causal theory to fault trees, the paper bridges the gap between risk analysis and failure diagnostics, enabling fault trees to answer both predictive ("what can go wrong") and diagnostic ("why has it gone wrong") questions through a unified framework.

Abstract: Fault trees are a widely used as effective risk models for complex systems, answering the question "what can go wrong?", especially through minimal cut set analysis. We study fault trees from the perspective of Halpern & Pearl's theory of actual causality. This allows us to use fault trees to answer the question "why has it gone wrong?", which is fundamental to failure diagnostics. We give a complete classification of each of the different notions of actual causality in terms of the fault tree's graph structure and logical structure, and show how minimal cut sets give rise to actual causes.

</details>


### [45] [CLAP: Closed-Loop Training, Evaluation, and Release Control for Domain Agent Post-training](https://arxiv.org/abs/2607.01846)
*Fangfei Li,Chenyang Zhao,Long Wang,Feng Tian,Zhiyue Zheng,Lv Guo*

Main category: cs.AI

TL;DR: CLAP is a closed-loop method for domain agent post-training that converts business data into structured training samples and uses validation, risk diagnosis, and replay testing to decide adapter suitability, showing modest but inconsistent gains on manufacturing tasks.


<details>
  <summary>Details</summary>
Motivation: Domain agents face challenges with noisy business data, uncertain post-training benefits, offline/application mismatch, and adapter-release risks. Existing approaches rely too heavily on training completion metrics or single offline scores, lacking systematic evaluation of adapter suitability for target applications.

Method: CLAP (Closed-Loop Agent Post-training) converts business data into structured SFT samples, decision-preference samples, holdout sets, risk diagnostics, and release-gate records. It combines data validation, target/evidence normalization, reward/KL diagnosis, offline gates, and application-chain replay to evaluate adapter suitability.

Result: On five manufacturing batches, QLoRA-style LoRA-SFT showed modest average gains (overall score +0.0098, pass rate +0.0240, evidence accuracy +0.0280), but only 3 of 5 batches improved with some regressing. GRPO exposed high KL risks. Application-chain replay revealed RAG is necessary for factual extraction, with application-RAG-oriented adapter improving metrics over base+RAG but increasing latency.

Conclusion: Domain-agent post-training should be managed through an integrated data-training-evaluation-release loop rather than relying on training completion or single offline scores. The closed-loop approach provides systematic evaluation for adapter deployment decisions in real applications.

Abstract: Domain agents often face noisy business data, uncertain post-training gains, offline/application mismatch, and adapter-release risk. This paper presents CLAP (Closed-Loop Agent Post-training), a closed-loop method that converts business data into structured SFT samples, decision-preference samples, holdout sets, risk diagnostics, and release-gate records. CLAP combines data validation, target/evidence normalization, reward/KL diagnosis, offline gates, and application-chain replay to decide whether an adapter is suitable for the target application chain. On five anonymized manufacturing-scenario batches, QLoRA-style LoRA-SFT yields modest average gains: overall score increases by 0.0098, pass rate by 0.0240, and evidence accuracy by 0.0280, while hallucination and wrong facts decrease. Yet only 3 of 5 batches improve, some batches regress, and GRPO exposes high KL risks. Application-chain replay further shows that RAG is necessary for factual extraction; under the same 3B backbone and 100 replay cases, an application-RAG-oriented LoRA-SFT adapter improves value, core fields, and answer-evidence doc/page matching over base+RAG, but increases latency. These results support managing domain-agent post-training through an integrated data-training-evaluation-release loop rather than relying on training completion or a single offline score.

</details>


### [46] [Safety Targeted Embedding Exploit via Refinement](https://arxiv.org/abs/2607.01859)
*Joshua Adrian Cahyono*

Main category: cs.AI

TL;DR: STEER is a gradient-guided attack that translates safety-critical words into low-resource languages to bypass LLM safety mechanisms, achieving high attack success rates across multiple models.


<details>
  <summary>Details</summary>
Motivation: Current safety training for LLMs is predominantly conducted in English, creating an epistemic gap where safety mechanisms may not generalize well to low-resource languages and mixed-language code-switching, potentially allowing models to generate harmful responses for inputs outside their safety training distribution.

Method: The paper introduces STEER (Safety Targeted Embedding Exploit via Refinement), a gradient-guided attack that: 1) identifies words contributing most strongly to a model's refusal behavior, and 2) iteratively translates these words into low-resource languages to suppress refusal while preserving harmful intent.

Result: STEER achieves attack success rates of up to 93.0% on JailbreakBench and 96.7% on AdvBench across six open-source 8B-parameter models, outperforming random code-switching and Greedy Coordinate Gradient (GCG). The attacks also transfer to GPT-4o-mini with 35.5% success rate without target model access.

Conclusion: Safety mechanisms aligned primarily on English cannot be assumed to generalize across multilingual inputs. Improving multilingual safety requires broader coverage during alignment and mechanisms that explicitly detect and abstain on out-of-distribution inputs.

Abstract: Safety training for large language models (LLMs) is conducted predominantly in English, leaving uncertain how well safety mechanisms generalize to low-resource languages and mixed-language code-switching. We show that this creates an epistemic gap in which models confidently generate harmful responses for inputs that fall outside the distribution of their safety training. To study this phenomenon, we introduce STEER (Safety Targeted Embedding Exploit via Refinement), a gradient-guided attack that identifies words contributing most strongly to the model's refusal behavior and iteratively translates them into low-resource languages to suppress refusal while preserving harmful intent. Across six open-source 8B-parameter models, STEER achieves attack success rates of up to 93.0% on JailbreakBench and 96.7% on AdvBench, outperforming random code-switching and Greedy Coordinate Gradient (GCG). The resulting prompts also transfer to GPT-4o-mini, achieving a 35.5% attack success rate without requiring access to the target model, suggesting that the underlying weakness is not specific to a single architecture. These findings demonstrate that safety mechanisms aligned primarily on English cannot be assumed to generalize across multilingual inputs. We argue that improving multilingual safety requires broader coverage during alignment and mechanisms that explicitly detect and abstain on out-of-distribution inputs.

</details>


### [47] [CamoNAS: Neural Architecture Search for Enhanced Camouflaged Object Detection](https://arxiv.org/abs/2607.01870)
*Dawei Ren,Yan Zhang,Hongying Tang,Qiaoling Zhou,Jianpo Liu*

Main category: cs.AI

TL;DR: CamoNAS is a neural architecture search framework for camouflaged object detection that automatically searches both cell-level operations and network-level downsampling paths, using a frequency-aware dual-stream approach with learnable wavelet transforms.


<details>
  <summary>Details</summary>
Motivation: Camouflaged object detection is challenging due to weak edge cues and ill-defined boundaries. Traditional COD models rely on hand-designed architectures and multi-scale feature fusion guided by intuition rather than systematic search, which may not be optimal for detecting objects that blend into their surroundings.

Method: CamoNAS uses a frequency-aware multi-resolution Neural Architecture Search framework that automatically searches both cell-level operations and network-level downsampling paths. It employs an RGB frequency dual-stream architecture where a learnable wavelet transform complements the RGB spatial stream, forming a hierarchical search space tailored for camouflaged object detection.

Result: CamoNAS achieves state-of-the-art performance on four COD benchmarks: CAMO, COD10K, NC4K, and CHAMELEON, demonstrating the effectiveness of NAS for camouflaged object detection.

Conclusion: The paper successfully introduces a systematic neural architecture search approach to camouflaged object detection, showing that automated architecture search outperforms traditional hand-designed models for this challenging task, with frequency-aware components providing important complementary information to spatial RGB features.

Abstract: Camouflaged Object Detection (COD) aims to locate and segment objects that blend into their surroundings, presenting challenges due to weak edge cues and ill-defined boundaries. Traditional COD models rely on hand-designed architectures and multi-scale feature fusion, which are often guided by intuition rather than systematic search. This paper introduces CamoNAS, a frequency-aware multi-resolution Neural Architecture Search (NAS) framework for COD. CamoNAS automatically searches both cell-level operations and network-level downsampling paths, forming a hierarchical search space tailored to detect camouflaged objects. Additionally, it adopts an RGB frequency dual-stream architecture, where a learnable wavelet transform complements the RGB spatial stream. CamoNAS achieves state-of-the-art performance on four COD benchmarks (CAMO, COD10K, NC4K, CHAMELEON), highlighting the effectiveness of NAS for COD. Our code is available at https://github.com/rendaweiSIMIT/CamoNAS.

</details>


### [48] [SkillCoach: Self-Evolving Rubrics for Evaluating and Enhancing Agentic Skill-Use](https://arxiv.org/abs/2607.01874)
*Jiayin Zhu,Kelong Mao,Yudong Guo,Dengbo He,Sulong Xu,Simiu Gu,Yutao Yue*

Main category: cs.AI

TL;DR: SkillCoach introduces a self-evolving rubric framework that evaluates LLM agent skill-use across four dimensions (selection, following, composition, reflection) to improve process quality beyond just final task success.


<details>
  <summary>Details</summary>
Motivation: The paper addresses the problem that in realistic skill repositories with overlapping skills, reliable skill-use is difficult. Current evaluation using final verifier success is too coarse - agents can succeed through trial and error while making process errors like selecting distractor skills, skipping required steps, incorrect composition, or omitting final checks.

Method: SkillCoach introduces a self-evolving rubric framework that: 1) derives skill-grounded process rubrics from real rollouts, 2) evaluates trajectories along four dimensions (skill selection, skill following, skill composition, and skill-grounded reflection), 3) keeps external verifier as separate outcome signal to distinguish process quality from accidental success, and 4) uses evolved rubrics as process supervision for selecting high-quality training trajectories.

Result: Experiments show that evolved rubrics substantially improve evaluation quality, expose failures hidden by final accuracy, and provide stronger supervision signals than outcome-only filtering for enhancing agentic skill-use.

Conclusion: SkillCoach provides a more nuanced approach to evaluating and training LLM agents' skill-use by focusing on process quality rather than just final outcomes, addressing the limitations of overlapping skills in realistic repositories.

Abstract: Skills are becoming a reusable operational layer for LLM agents, encoding SOPs, domain rules, tool workflows, scripts, and validation routines. In realistic skill repositories, overlapping skills make reliable skill-use difficult. Final verifier success is too coarse for both evaluation and training, since an agent may pass through trial and error while selecting distractor skills, skipping required steps, composing workflows incorrectly or omitting final checks. We introduce SkillCoach, a self-evolving rubric framework for evaluating and enhancing agentic skill-use. SkillCoach derives skill-grounded process rubrics from real rollouts and evaluates trajectories along four dimensions: skill selection, skill following, skill composition, and skill-grounded reflection. It keeps the external verifier as a separate outcome signal, allowing process quality to be distinguished from accidental task success. The evolved rubrics further serve as process supervision for selecting high-quality training trajectories. Experiments show that evolved rubrics substantially improve evaluation quality, expose failures hidden by final accuracy, and provide stronger supervision signals than outcome-only filtering for enhancing agentic skill-use.

</details>


### [49] [Spec-AUF: Accept-Until-Fail Training under Train-Inference Misalignment for Masked Block Drafters](https://arxiv.org/abs/2607.01893)
*Tianjian Yang,Meng Li*

Main category: cs.AI

TL;DR: AUF (Acceptance-aware Up to First failure) is a new training objective for block drafters in speculative decoding that improves efficiency by focusing supervision only on tokens up to the first predicted rejection, rather than the full block.


<details>
  <summary>Details</summary>
Motivation: Current block drafters use full-block cross-entropy loss that supervises every position, but during speculative decoding inference, tokens after the first rejection are discarded. This creates a mismatch between training (which supervises all tokens) and inference (which only uses tokens up to first failure).

Method: AUF modifies the cross-entropy support to only supervise tokens through the drafter's first predicted failure. It's a simple change to the loss function that doesn't require auxiliary objectives, verifier rollouts, or changes to the inference pipeline.

Result: AUF improves average emitted length from 2.40 to 2.61 on Qwen3-8B across six benchmarks, with gains on every benchmark. It also transfers well to Domino's two-branch head (2.56 to 2.68). The method shows that even though a decay-only baseline has higher token accuracy, it decodes worse, and position-decay weighting becomes inert once AUF is applied.

Conclusion: AUF provides an effective, simple approach to align block drafter training with speculative decoding inference by concentrating supervision on the accepted prefix, leading to improved decoding efficiency without changing the inference pipeline or exactness contract.

Abstract: Speculative decoding accelerates autoregressive generation by drafting a block of tokens that the target model verifies left-to-right, committing only the longest accepted prefix. Block (DLM-style) drafters predict the whole block in parallel, which is fast but trained with a full-block cross-entropy that supervises every position against the gold continuation -- even though inference discards every token after the first rejection. Recent acceptance-aware objectives patch this by reweighting the full-block loss; we instead use teacher-forced learning as a motivation for how supervision should concentrate on the accepted prefix. A mask-only block drafter has no input-side channel for gold-prefix conditioning, so AUF approximates that prefix-sensitive supervision on the loss side by keeping the cross-entropy support only through the drafter's first predicted failure. AUF is a single, detached change to the CE support -- no auxiliary objective, no verifier rollouts, and no change to the inference pipeline or the exactness contract. Within fixed drafter backbones and serving settings on Qwen3-8B, AUF raises the DFlash drafter's average emitted length $τ$, averaged over six benchmarks, from 2.40 to 2.61, with a gain on every benchmark, and transfers to Domino's two-branch head (2.56 to 2.68). Two findings sharpen the picture: the decay-only baseline reaches higher token accuracy on the shared block mask yet decodes worse, and on DFlash, once AUF truncates the support, the standard exponential position-decay weighting becomes empirically inert.

</details>


### [50] [Rethinking Complexity Metrics for LLM-Integrated Applications: Beyond Source Code](https://arxiv.org/abs/2607.01903)
*Zihao Xu,Yuekang Li,Gelei Deng,Yi Liu,Zhenchang Xing*

Main category: cs.AI

TL;DR: HECATE is the first tool to measure complexity in both prompt and code layers of LLM-integrated applications, identifying 10 effective metrics that predict maintenance effort better than traditional code metrics alone.


<details>
  <summary>Details</summary>
Motivation: Current complexity metrics only operate at the code level and completely overlook prompt-layer behavior in LLM-integrated applications, where much of the runtime behavior originates. There's a need to assess complexity across both layers to better understand and predict maintenance effort.

Method: Developed HECATE with a "Prompt-as-Specification" formalism (Hoare-logic-inspired) that treats prompts as behavior specifications. Generated 52 candidate metrics from 25 complexity dimensions across published taxonomies, then empirically validated them on 118 components from 18 open-source repositories using maintenance activity from version history as a complexity proxy.

Result: Only 10 metrics survived validation (7 new, 3 conventional). The 7 new metrics measure structural breadth (distinct elements like LLM call sites, memory attributes, prompt templates) rather than sheer volume. These metrics outperform conventional metrics and retain significance even when accounting for code size. Prompt-layer complexity is established as a distinct dimension.

Conclusion: Prompt complexity is a distinct dimension from code complexity in LLM-integrated applications, and HECATE's structural breadth metrics effectively predict maintenance effort, with the top metrics showing generalizability across held-out repositories.

Abstract: LLM-integrated applications blend natural language prompts with program code, and much of their runtime behavior originates in the prompt layer rather than in the code itself. Existing complexity metrics, however, operate solely at the code level and therefore overlook this behavioral logic entirely. We present HECATE, the first tool designed to assess complexity in both the prompt and code layers of such applications. Central to HECATE is Prompt-as-Specification, a Hoare-logic-inspired formalism that interprets every prompt as a specification of intended behavior. Grounded in 25 complexity dimensions identified across published taxonomies, the tool generates 52 candidate metrics. We assess each metric against 118 components collected from 18 open-source repositories, relying on maintenance activity derived from version history as an empirical proxy for complexity, and discard any metric that loses significance once code size is accounted for. Only ten metrics withstand this test. Seven belong to our newly introduced set; rather than measuring sheer volume, each tallies structurally distinct elements, such as LLM call sites, memory attributes, and prompt templates, an attribute we call structural breadth. Of the three surviving conventional metrics, RFC exhibits a similar breadth-oriented character, while Halstead N and V survive only as a residual effect of size; our top-performing metrics exceed all three. Crucially, the prompt-layer metrics retain significance even when the strongest code-level metric is added as a covariate, establishing prompt complexity as a dimension in its own right. A final validation on 20 components spanning six held-out repositories shows that the two best-performing metrics continue to predict maintenance effort, supporting their generalizability beyond the training set.

</details>


### [51] [ContextSniper: AntTrail's Token-Efficient Code Memory for Repository-Level Program Repair](https://arxiv.org/abs/2607.01916)
*Chiwang Luk,Matin Mohammad Najafi,Zhifeng Jia,Wei Yang,Xiuchang Li,Jinwei Zhu,Yang Ren,Lei Chen,Gao Cong*

Main category: cs.AI

TL;DR: ContextSniper is a token-efficient code memory layer that reduces context usage by 38.9-51.5% for LLM agents in repository-level program repair tasks, with minimal performance impact on resolution rates.


<details>
  <summary>Details</summary>
Motivation: LLM agents for program repair waste large context budgets on whole-file reads, broad searches, and long terminal outputs where useful evidence is mixed with irrelevant code and logs.

Method: ContextSniper implements Sniper feature for precision evidence selection: retrieves candidate code and runtime evidence, ranks with hybrid retrieval signals, filters long outputs through intention-aware context gate, and returns compact evidence packets while preserving recoverable source context outside the prompt.

Result: Reduces total token use by 51.5% and logged cost by 36.4% for OpenClaw, reduces total token use by 38.9% and estimated cost by 27.3% for Claude Code. Resolution rates decrease slightly from 26.0% to 24.0% for OpenClaw and from 32.0% to 30.0% for Claude Code.

Conclusion: ContextSniper effectively reduces context usage and costs for LLM agents in repository-level program repair while maintaining comparable performance, with minimal impact on resolution rates.

Abstract: Large language model agents can repair real repository issues, but they often spend large context budgets on whole-file reads, broad searches, and long terminal outputs where useful evidence is mixed with irrelevant code and logs. This paper presents ContextSniper, AntTrail's token-efficient code memory layer for repository-level program repair. As the coding specialization of AntTrail's broader agent memory engine, ContextSniper implements the Sniper feature for precision evidence selection: it retrieves candidate code and runtime evidence, ranks it with hybrid retrieval signals, filters long outputs through an intention-aware context gate, and returns compact evidence packets while preserving recoverable source context outside the prompt. We evaluate ContextSniper on SWE-bench Lite with OpenClaw and Claude Code, using 50 task runs per host-agent condition. ContextSniper reduces total token use by 51.5% and logged cost by 36.4% for OpenClaw, and reduces total token use by 38.9% and estimated cost by 27.3% for Claude Code. Submitted-resolution rates decrease slightly, from 26.0% to 24.0% for OpenClaw and from 32.0% to 30.0% for Claude Code. ContextSniper's pilot testing scripts are open-sourced at https://github.com/Calluking/ContextSniper

</details>


### [52] [ElephantAgent: Contextual State Continuity in Agentic Systems](https://arxiv.org/abs/2607.01919)
*Jiankai Jin,Xiangzheng Zhang,Zhao Liu,Wenzhuo Xu,Dongdong Yang,Deyue Zhang,Quanchen Zou*

Main category: cs.AI

TL;DR: ElephantAgent is a protocol that enforces Contextual State Continuity to defend against tool and memory poisoning attacks in agentic systems by maintaining verifiable state integrity through replicated trusted hardware and historical traceability.


<details>
  <summary>Details</summary>
Motivation: Agentic systems that use external tools and persistent memory create novel attack surfaces through tool/memory poisoning attacks. These attacks can covertly bias agent behavior, stemming from a lack of verifiable continuity in the agent's contextual state for planning and execution.

Method: ElephantAgent enforces Contextual State Continuity by: 1) Defining contextual state as the bounded, security-critical subset of agent context (tool state and memory), 2) Recomputing digest of local contextual state before each query and verifying against latest authorized digest, 3) Using replicated trusted hardware to maintain linearizable ledger of authorized state transitions, and 4) Providing Historical Traceability for conditional post-hoc audit and recovery to known-good states.

Result: The protocol detects out-of-band state tampering through state continuity verification and provides defense against in-band semantic abuse via historical traceability for audit and recovery capabilities.

Conclusion: ElephantAgent addresses the security vulnerabilities in agentic systems by extending state-continuity protection to evolving contextual states, enabling robust defense against contextual state poisoning attacks through verifiable state integrity and recovery mechanisms.

Abstract: Agentic systems enhance their capabilities by invoking external tools and maintaining persistent memory. However, these external dependencies introduce novel attack surfaces. Recent tool and memory poisoning attacks show that maliciously crafted tool descriptors and poisoned memory can covertly bias agent behavior. These threats reflect a deeper issue: the lack of verifiable continuity in the agent's contextual state for planning and execution. We present ElephantAgent, a protocol that enforces Contextual State Continuity to defend against contextual state poisoning. Inspired by prior state-continuity mechanisms (e.g., Nimble), ElephantAgent extends this protection to the evolving contextual state of agentic systems. We define the contextual state as the bounded, security-critical subset of the agent's entire context (e.g., tool state and memory). Before processing each query, ElephantAgent recomputes the digest of the local contextual state and verifies it against the latest authorized digest. Using replicated trusted hardware, ElephantAgent maintains a linearizable ledger of authorized contextual state transitions and detects out-of-band state tampering. To handle in-band semantic abuse, ElephantAgent additionally provides Historical Traceability, enabling conditional post-hoc audit and recovery to a known-good prior state.

</details>


### [53] [A-TMA: Decoupling State-Aware Memory Failures in Long-Term Agent Memory](https://arxiv.org/abs/2607.01935)
*Zitong Shi,Yixuan Tang,Anthony Kum Hoe Tung*

Main category: cs.AI

TL;DR: ATMA addresses ghost memory failures in LLM agents by adding state-aware overlays to memory systems, keeping track of current, historical, and transition facts with explicit state labels to prevent confusion from outdated information.


<details>
  <summary>Details</summary>
Motivation: LLM agents with long-term memory face "ghost memory" problems where old, current, and transition facts coexist and get mixed during retrieval, misleading answers. Memory systems need to track what's true now, what used to be true, and what changed over time.

Method: Proposed ATMA (state aware overlay for existing memory systems) that: 1) keeps superseded and transition records in memory bank, 2) builds evidence packets for the query's requested state view, and 3) exposes current, historical, and transition labels to QA. Also introduced LTP benchmark for measuring ghost memory failures.

Result: On LTP benchmark, Graphiti+ATMA improves conflict accuracy by 0.240 absolute over Graphiti alone. On LoCoMo benchmark for long conversations, temporal F1 improved from 0.0295 to 0.1705. Gains vary by host system but show explicit state roles reduce memory failures hidden by final QA accuracy.

Conclusion: Memory systems should be evaluated at three levels (bank maintenance, retrieval, answer resolution) with decoupled evaluation to identify where ghost memory occurs. ATMA's state-aware approach demonstrates value in managing temporal facts and reducing hidden memory failures in LLM agents.

Abstract: Long term memory lets LLM agents act as persistent assistants, but user facts change. A useful memory system must know what is true now, what used to be true, and what changed. We study \emph{ghost memory}, a state coordination failure in which old, current, and transition facts coexist in the memory bank, remain mixed during retrieval, and mislead the answer model. We argue that memory systems should be understood and optimized from three levels: bank maintenance, retrieval, and answer time resolution. We propose ATMA, a state aware overlay for existing memory systems. ATMA keeps superseded and transition records in the bank, builds evidence packets for the query's requested state view, and exposes current, historical, and transition labels to QA. We further call for decoupled evaluation of bank, retrieval, and answer level failures, since final QA accuracy can hide where ghost memory occurs. To make this failure measurable, we build LTP (LoCoMo Temporal Plus), a conflict heavy benchmark for ghost memory, and evaluate on LoCoMo for long conversation generalization. On LTP, Graphiti+ATMA improves conflict accuracy by 0.240 absolute over Graphiti. On LoCoMo, Graphiti+ATMA raises temporal F1 from 0.0295 to 0.1705. The gains are host dependent, but they indicate that explicit state roles can reduce memory failures hidden by final QA accuracy.

</details>


### [54] [Atomic Task Graph: A Unified Framework for Agentic Planning and Execution](https://arxiv.org/abs/2607.01942)
*Yue Zhang,Sihan Chen,Ziwen Huang,Hanyun Cui,Kangye Ji,Zhi Wang*

Main category: cs.AI

TL;DR: ATG is a unified control framework that uses explicit graph structures to expose dependencies between subtasks, enabling parallel execution, error localization, and result reuse without requiring large models or task-specific fine-tuning.


<details>
  <summary>Details</summary>
Motivation: Current LLM-based agents either require scaling to larger models (costly) or task-specific fine-tuning (poor generalization), while prompt-based methods fail to expose and reuse intermediate results between subtasks.

Method: Atomic Task Graph (ATG) framework that maintains explicit dependency graphs. It recursively decomposes tasks into subtasks forming DAGs, executes independent branches in parallel, and uses graph evolution history for error localization and repair.

Result: Experiments show ATG consistently outperforms strong baselines in success rate and execution efficiency across three interactive benchmarks using only 7B-8B backbone models.

Conclusion: ATG provides an effective training-free approach for improving LLM-based agent performance by making task dependencies explicit, enabling parallel execution and targeted error recovery while maintaining computational efficiency.

Abstract: LLM-based agents have shown strong potential for solving complex multi-step tasks, yet existing performance improvements often rely on either scaling to larger backbone models or task-specific fine-tuning. The former incurs substantial computational costs, while the latter typically generalizes poorly across different tasks. Although prompt-based control is training-free and broadly applicable, existing methods still leave input-output dependencies between subtasks implicit in textual trajectories, making verified intermediate results difficult to reuse. To address these limitations, we propose Atomic Task Graph (ATG), a unified control framework for planning and execution. Specifically, ATG maintains an explicit graph to expose dependencies and support reuse. During planning, it recursively decomposes a high-level task into subtasks, forming a sequence of directed acyclic graphs (DAGs) whose evolution can be traced. During execution, the dependencies exposed by ATG allow independent branches to be executed in parallel, thereby improving execution efficiency. When failures are detected, ATG leverages the graph evolution history to localize the error source and repair only the affected region, preserving validated regions unchanged. Experiments show that ATG consistently outperforms strong baselines in success rate and execution efficiency across three interactive benchmarks using only 7B-8B backbones.

</details>


### [55] [OntoLearner: A Modular Python Library for Ontology Learning with Large Language Models](https://arxiv.org/abs/2607.01977)
*Hamed Babaei Giglou,Jennifer D'Souza,Andrei Aioanei,Nandana Mihindukulasooriya,Sören Auer*

Main category: cs.AI

TL;DR: OntoLearner is a unified framework that provides standardized access to 180 ontologies across 22 domains, enabling systematic evaluation of ontology learning tasks and revealing that failure modes scale with ontological complexity rather than model limitations.


<details>
  <summary>Details</summary>
Motivation: Ontology learning research has been fragmented across methods, domains, and evaluation practices for decades, lacking shared infrastructure for systematic evaluation and ontology access, which has hindered progress and left central challenges unaddressed.

Method: Developed OntoLearner framework with: (1) 180 machine-readable ontologies across 22 domains, (2) pipeline-ready datasets with train/dev/test splits for three core OL tasks (term typing, taxonomy discovery, non-taxonomic relation extraction), (3) modular framework for LLM-driven learning pipelines and standardized benchmarking.

Result: Large-scale empirical study evaluating 22 retrieval models and 12 LLMs reveals that failure modes scale with ontological complexity rather than model size or architectural sophistication; primary bottleneck is structural mismatch between model knowledge encoding and ontology organization.

Conclusion: Effective ontology learning is achievable through cross-domain, multi-task benchmarking enabled by OntoLearner, which provides the necessary infrastructure to address OL's central challenges by reframing them from model capability issues to structural alignment problems.

Abstract: Ontology learning (OL) aims to automatically construct structured knowledge models from text, yet progress remains fragmented across methods, domains, and evaluation practices. Despite decades of research, OL lacks a shared infrastructure for systematic evaluation and ontology access. This absence has hindered progress and fragmented research, leaving the central challenges of OL largely unaddressed. We introduce OntoLearner, a modular, cross-domain, and first-of-its-kind framework that unifies ontology access, large language model (LLM)-driven learning pipelines, and standardized benchmarking. OntoLearner releases 180 machine-readable ontologies spanning 22 domains and provides pipeline-ready datasets with train/dev/test splits for three core OL tasks: term typing, taxonomy discovery, and non-taxonomic relation extraction. Using this infrastructure, we conduct a large-scale empirical study of OL, evaluating 22 retrieval models and 12 LLMs across domains and tasks. The results converge on a finding that reframes the central challenge of OL: failure modes scale with ontological complexity rather than model size or architectural sophistication. The primary bottleneck is not model capability, but a structural mismatch between how models encode knowledge and how ontologies organize it. These findings establish that effective OL is reachable through the cross-domain, multi-task benchmarking enabled by OntoLearner. OntoLearner is open-source (MIT license) at https://github.com/sciknoworg/OntoLearner/.

</details>


### [56] [Multimodal Knowledge Edit-Scoped Generalization for Online Recursive MLLM Editing](https://arxiv.org/abs/2607.01978)
*Siyuan Li,Youyuan Zhang,Ruitong Liu,Junxi Wang,Jing Li*

Main category: cs.AI

TL;DR: ScopeEdit is an online multimodal knowledge editing method that controls edit propagation boundaries in MLLMs, using separate branches for local absorption and evidence-gated cross-modal generalization with constant per-edit overhead.


<details>
  <summary>Details</summary>
Motivation: Existing multimodal large language model (MLLM) editors focus on edit reliability and stability but don't control semantic boundaries of edits. Pilot analyses show instance-level success doesn't guarantee transfer to cross-modal variants or prevent leakage to unrelated inputs, revealing a "scope gap" problem.

Method: Proposes ScopeEdit, a scope-aware online editor that decomposes updates into: 1) modality-local absorption branch for stable edit absorption, and 2) evidence-gated shared generalization branch that enables cross-modal propagation only when visual-textual evidence aligns. Both use scope-separated write geometries in orthogonal low-rank spaces and maintain branch-wise preconditioners via Sherman-Morrison recursions for constant overhead.

Result: Extensive experiments across diverse benchmarks, long-horizon edit streams, MLLM backbones, real-world VLKEB scenarios, and complex vision-language architectures show ScopeEdit consistently improves the trade-off between in-scope cross-modal transfer and out-scope locality while preserving edit reliability, stability, and online efficiency.

Conclusion: The paper formulates Edit-Scoped Generalization as a new paradigm for online MLLM editing, shifting from merely correcting instances to controlling edit propagation boundaries. ScopeEdit successfully addresses the scope gap problem with a practical solution that maintains efficiency while enabling controlled cross-modal knowledge transfer.

Abstract: Online multimodal knowledge editing requires injecting a continual stream of visual-textual corrections into multimodal large language models (MLLMs) with bounded overhead and minimal disruption to unrelated behaviors. Existing editors mainly emphasize edit reliability and long-horizon stability, but rarely control the semantic boundary of each edit. Our pilot analyses of post-edit behaviors and internal neuronal activities reveal a scope gap behind reliable edits: instance-level success neither guarantees transfer to valid cross-modal variants nor prevents leakage to unrelated inputs, while edit-related cross-modal responses concentrate in deeper semantic layers. Therefore, we formulate Edit-Scoped Generalization, reframing online MLLM editing from merely correcting an instance to controlling the propagation boundary of each edit. To this end, we propose ScopeEdit, a scope-aware online editor that decomposes each update into a modality-local absorption branch and an evidence-gated shared generalization branch. The local branch supports stable edit absorption, whereas the shared branch enables cross-modal propagation only when visual and textual evidence are sufficiently aligned. Both branches perform scope-separated write geometries in orthogonal low-rank spaces and maintain branch-wise preconditioners via Sherman--Morrison recursions, yielding constant per-edit overhead. Extensive experiments across diverse benchmarks, long-horizon edit streams, MLLM backbones, real-world VLKEB scenarios, and complex vision-language architectures show that ScopeEdit consistently improves the trade-off between in-scope cross-modal transfer and out-of-scope locality, while preserving edit reliability, stability and online efficiency. Our code is available at https://github.com/lab-klc/ScopeEdit.

</details>


### [57] [Episodic-to-Semantic Consolidation Without Identity Drift](https://arxiv.org/abs/2607.01988)
*Xue Qin,Simin Luan,Cong Yang,Zhijun Li*

Main category: cs.AI

TL;DR: A framework for memory consolidation in adaptive agents that preserves cryptographic identity by treating consolidation as a deterministic function over episodic memory, creating a separate semantic knowledge layer without changing the agent's certified identity.


<details>
  <summary>Details</summary>
Motivation: Long-running adaptive agents face a tension between knowledge consolidation and information integrity. Traditional consolidation methods (fine-tuning, prompt rewriting, policy distillation) change the agent, creating liability in regulated deployment where agents operate under commitments and audit contracts tied to specific cryptographically certified identities.

Method: Treat consolidation as a deterministic function f: M^ep → M^sem over episodic memory, producing a separately addressable semantic knowledge layer. The identity hash does not read M^sem, so consolidation updates knowledge without changing the certified identity. Includes formal agent representation, structural lemma on manifest's hash-input set, deterministic aggregation algorithm producing auditable database rows with confidence and provenance.

Result: Validated with synthetic experiments showing per-field correctness, byte-equal identity across consolidation passes, and mean 79.82% reduction in unproductive planner attempts (95% BCa CI [78.02%, 81.49%] across 10 seeds) against calibrated Bayesian-shrunk baseline.

Conclusion: A knowledge-update discipline for autonomic agents where lessons accumulate as queryable facts while the agent's certified identity remains byte-equal across its operational lifetime, demonstrated through an embodied service agent case study.

Abstract: Long-running adaptive intelligent agents face a structural tension between knowledge consolidation and information integrity. Memory consolidation is conventionally treated as an agent-changing operation: a model is fine-tuned, a prompt rewritten, a policy distilled, or a reflection appended to the context that governs future behaviour. In regulated autonomic deployment this is a liability because the agent operates under commitments and audit contracts that bind to a specific, cryptographically certified identity. We propose to treat consolidation not as a mutation of the planner or the identity manifest, but as a deterministic function f: M^ep -> M^sem over episodic memory whose output is a separately addressable semantic knowledge layer; the identity hash does not read M^sem, so consolidation updates knowledge without changing the agent's certified identity. We give a formal account of the agent representation, prove identity invariance through a structural lemma on the manifest's hash-input set, specify a deterministic aggregation algorithm whose outputs are auditable database rows with explicit confidence and supporting-event provenance, and validate the construction with synthetic experiments demonstrating per-field correctness, byte-equal identity across consolidation passes, and a mean 79.82% reduction in unproductive planner attempts (95% BCa CI [78.02%, 81.49%] across 10 seeds) against a calibrated Bayesian-shrunk baseline. The construction is a knowledge-update discipline for autonomic agents in which lessons accumulate as queryable facts while the agent's certified identity remains byte-equal across its operational lifetime, with an embodied service agent as the running case study.

</details>


### [58] [Traceable Fault Diagnosis for Battery Energy Storage Systems via Retrieval-Augmented Multi-Agent O&M Assistant](https://arxiv.org/abs/2607.01992)
*Jiangdi Ru,Bing Li,Yage Huang,Ding Wang,Keru Hua*

Main category: cs.AI

TL;DR: The paper presents a traceable BESS fault-diagnosis assistant using retrieval-augmented multi-agent reasoning to connect operational data, domain knowledge, visual evidence, and report generation for improved reliability in large-scale battery energy storage systems.


<details>
  <summary>Details</summary>
Motivation: Large-scale BESSs require complex O&M decisions combining various data sources (alarms, measurements, topology, diagnostics, historical cases, maintenance documents). Existing monitoring platforms can flag threshold violations but often cannot explain whether specific issues (voltage inconsistency, resistance drift, short-circuit risk, capacity divergence, thermal abnormality) actually require intervention, creating a need for better diagnostic capabilities.

Method: The authors developed a traceable BESS fault-diagnosis assistant using retrieval-augmented multi-agent reasoning. The approach features: BESS-specific task routing, schema-constrained natural-language database access, hybrid text-image retrieval, and evidence-based answer synthesis to connect operational data, domain knowledge, visual evidence, and report generation.

Result: Preliminary internal evaluation was conducted for three key components: routing, database access, and diagnostic reasoning. The paper reports on this initial evaluation, though specific performance metrics or quantitative results are not provided in the abstract.

Conclusion: The proposed traceable BESS fault-diagnosis assistant represents a novel approach to improving BESS reliability by integrating multiple data sources and using multi-agent reasoning to provide explainable fault diagnosis, addressing limitations of traditional threshold-based monitoring systems.

Abstract: Large-scale battery energy storage systems (BESSs) require O&M decisions that combine alarms, cell-level measurements, device topology, diagnostic tables, historical cases, and maintenance documents. Monitoring platforms can flag threshold violations, but they often cannot explain whether voltage inconsistency, resistance drift, short-circuit risk, capacity divergence, or thermal abnormality needs intervention. This digest presents a traceable BESS fault-diagnosis assistant that uses retrieval-augmented multi-agent reasoning to connect operational data, domain knowledge, visual evidence, and report generation. Reliability is improved through BESS-specific task routing, schema-constrained natural-language database access, hybrid text-image retrieval, and evidence-based answer synthesis. Preliminary internal evaluation is reported for routing, database access, and diagnostic reasoning.

</details>


### [59] [InduceKV: Fixed-Footprint Continual Adaptation of Multimodal LLMs via Inducing KV Memories](https://arxiv.org/abs/2607.02010)
*Qianyu Chen,Ziteng Feng,Canran Xiao,Runxuan Tang*

Main category: cs.AI

TL;DR: InduceKV is a fixed-memory continual adaptation method that stores training prefixes as attention-ready KV payloads, using bilevel selection to optimize retrieval and memory composition under strict memory budgets, outperforming various baselines across multiple continual learning benchmarks.


<details>
  <summary>Details</summary>
Motivation: Multimodal LLMs need continual adaptation to evolving tasks/domains, but current methods struggle with bounded deployment footprints due to accumulating adaptation states (repeated parameter updates or growing replay stores). The paper addresses fixed-footprint continual adaptation where adaptation state stays within fixed memory budgets while backbone models remain unchanged.

Method: InduceKV stores selected training prefixes as attention-ready memory entries with frozen retrieval keys and compact layerwise KV payloads that append to self-attention caches. Uses bilevel selection: lightweight calibration for retrieval, while selected memory balances current-task likelihood, anchor-based retention, and coverage in frozen retrieval space. Works under strict memory budgets.

Result: Across task-incremental instruction tuning, continual VQA, domain-incremental adaptation, and lifelong multimodal instruction tuning, InduceKV consistently outperforms PEFT, MoE, replay, and prompt-retrieval baselines under matched memory budgets. Gains persist even when controlling for backbone strength, replay effects, candidate pool size, and computational resources.

Conclusion: InduceKV provides an effective approach for fixed-footprint continual adaptation of multimodal LLMs, offering superior performance through its retrieval-based method with attention-ready KV payloads and bilevel memory selection, enabling practical deployment with bounded memory constraints.

Abstract: Multimodal large language models must adapt to evolving tasks and domains, yet continual improvement under bounded deployment footprint remains difficult because repeated parameter updates or growing replay stores can accumulate adaptation state over time. We study fixed-footprint continual adaptation: the deployed adaptation state is kept under a fixed memory budget, while the backbone model is left unchanged and task-specific updates are externalized. We propose InduceKV, a retrieval-based method that stores each selected training prefix as an attention-ready memory entry, consisting of a frozen retrieval key and compact layerwise key--value (KV) payloads that can be appended to the model's self-attention cache. Under a strict memory budget, InduceKV constructs a compact inducing set through bilevel selection: a lightweight calibration is fit for retrieval, while the selected memory balances current-task likelihood, anchor-based retention, and coverage in the frozen retrieval space. Across task-incremental instruction tuning, continual VQA, domain-incremental adaptation, and lifelong multimodal instruction tuning, InduceKV consistently improves over PEFT, MoE, replay, and prompt-retrieval baselines under matched memory budgets. We further report backbone-matched, stage-1 CoIN, compute-matched, and scalability diagnostics, showing that the gains are not due to a stronger backbone, replay alone, or an unbounded candidate pool.

</details>


### [60] [Hidden Forgetting in Continual Multimodal Learning: When Accuracy Survives but Grounding Fails](https://arxiv.org/abs/2607.02020)
*Qianyu Chen,Canran Xiao,Runxuan Tang*

Main category: cs.AI

TL;DR: MLLMs suffer from hidden evidence-use forgetting in continual learning - answers remain correct but evidence sources shift. RCL framework preserves both answers and evidence reliance without inference overhead.


<details>
  <summary>Details</summary>
Motivation: Standard continual learning metrics only measure answer correctness, ignoring whether multimodal models maintain consistent evidence use (visual, textual, OCR, chart, document). This leads to "hidden evidence-use forgetting" where answers are correct but grounding shifts silently.

Method: Propose RCL: Replay-free Reliance-Constrained Continual Learning. Freezes previous checkpoint as behavioral reference, estimates teacher/student evidence-reliance profiles via counterfactual channel interventions, and jointly optimizes task learning, prediction preservation, and reliance preservation without inference-time cost.

Result: RCL consistently improves final performance and reduces forgetting across CoIN, COAST, MCITlib, and evidence-sensitive multimodal streams. Substantially lowers modality reliance drift, dominant evidence flips, and hidden forgetting rates compared to replay-free, PEFT, routing, and memory-assisted baselines.

Conclusion: Robust continual multimodal learning requires preserving both correct answers AND the evidence path behind them. RCL demonstrates that maintaining evidence use patterns is crucial for trustworthy multimodal adaptation.

Abstract: Multimodal large language models must continually adapt to evolving tasks and domains, yet standard continual learning metrics mainly measure whether old answers remain correct, leaving the stability of multimodal grounding largely unexamined. We study this overlooked failure mode and ask whether a continually adapted MLLM can preserve not only what it answers, but also how it uses visual, textual, OCR, chart, and document evidence. We identify \emph{hidden evidence-use forgetting}, where answer accuracy is retained while the model silently shifts toward different or less grounded evidence channels, and propose \textsc{RCL}, a replay-free reliance-constrained continual learning framework. \textsc{RCL} freezes the previous checkpoint as a behavioral reference, estimates teacher and student evidence-reliance profiles through counterfactual channel interventions, and jointly optimizes task learning, prediction preservation, and reliance preservation without adding inference-time cost. Across CoIN, COAST, MCITlib, and an evidence-sensitive multimodal stream, \textsc{RCL} consistently improves final performance and reduces forgetting over replay-free, PEFT, routing, and memory-assisted baselines, while substantially lowering modality reliance drift, dominant evidence flips, and hidden forgetting rates. These results suggest that robust continual multimodal learning requires preserving the evidence path behind correct answers, not merely the answers themselves.

</details>


### [61] [PACE: A Proxy for Agentic Capability Evaluation](https://arxiv.org/abs/2607.02032)
*Yueqi Song,Lintang Sutawika,Jiarui Liu,Lindia Tjuatja,Jiayi Geng,Yunze Xiao,Daniel Lee,Aditya Bharat Soni,Vincent Lo,Xiang Yue,Graham Neubig*

Main category: cs.AI

TL;DR: PACE creates cheap proxy benchmarks from atomic non-agentic tests to accurately predict expensive agentic LLM performance.


<details>
  <summary>Details</summary>
Motivation: Agentic benchmarks like SWE-Bench and GAIA are expensive (thousands of dollars per evaluation) and time-consuming, while non-agentic capability tests are fast and cheap. There's a need for reliable, cost-effective ways to estimate agentic performance during model development.

Method: PACE constructs proxy benchmarks by selecting instances from existing non-agentic evaluations whose aggregate scores best predict model performance on agentic benchmarks. It uses regression to map scores from a compact subset of source instances to target agentic scores, combining target-relevance local selection and globally informative global selection strategies.

Result: PACE-Bench achieves mean absolute error under 4%, Spearman correlation above 0.80, and pairwise model-ranking accuracy around 85% with LOOCV, using less than 1% of full agentic evaluation cost across 14 models, 4 agentic benchmarks, and 19 non-agentic benchmarks.

Conclusion: PACE enables reliable agentic performance estimation for model development, selection, and routing without expensive full evaluations, revealing which specific skills each agentic benchmark uniquely demands.

Abstract: Evaluating LLM agents on benchmarks like SWE-Bench and GAIA can be expensive, time-consuming, and requires complex infrastructure. A single evaluation can cost thousands of dollars and take days to complete. In contrast, non-agentic LLM benchmarks that test individual capabilities (e.g., reasoning, code generation) are fast and cheap to run. In this paper, we investigate whether performance on expensive agentic benchmarks can be accurately predicted by the performance on a small, carefully selected subset of atomic evaluation instances. We introduce PACE, a framework that constructs proxy benchmarks by selecting instances from existing non-agentic evaluations whose aggregate scores most reliably predict model performances on agentic benchmarks. Given a pool of candidate instances spanning atomic capabilities, PACE fits a regression that maps a model's scores on a compact subset of source instances to its score on the target agentic benchmark. The subset itself is curated by combining two complementary instance-selection strategies, target-relevance local selection and globally informative global selection. We apply PACE to the 4 target agentic benchmarks in this paper, which yields PACE-Bench, the concrete proxy benchmark that we evaluate in the paper. Experiments across 14 models, 4 agentic benchmarks, and 19 non-agentic benchmarks show that PACE-Bench predicts agentic scores with leave-one-out cross-validation (LOOCV) mean absolute error (MAE) under 4%, Spearman correlation above 0.80, and pairwise model-ranking accuracy around 85%, all at much less than 1% of the full agentic evaluation cost. We further analyze the selected proxy instances, revealing which skills each agentic benchmark uniquely demands. PACE enables practitioners to obtain reliable estimates of agentic performance during model development, selection, and routing, without the overhead of full agent evaluation.

</details>


### [62] [Algebraic Model Counting for Global Analysis of Optimal Decision Trees](https://arxiv.org/abs/2607.02069)
*Hiroki Arimura*

Main category: cs.AI

TL;DR: ADTC is an algebraic framework for exhaustive analysis of optimal/near-optimal decision trees that reformulates analytical tasks into unified sum-of-products computations over semirings, enabling efficient global assessment of model reliability.


<details>
  <summary>Details</summary>
Motivation: Ensuring model reliability in Explainable AI requires a global assessment of the hypothesis space, particularly for decision trees where the space grows doubly exponentially with depth. Current approaches lack comprehensive tools to analyze the complete landscape of optimal and near-optimal models.

Method: Proposes Algebraic Decision Tree Counting (ADTC), inspired by Algebraic Model Counting, which reformulates optimization, counting, and sampling tasks into unified sum-of-products computations over a semiring. Uses dynamic programming with O*(n^{O(Δ)}) complexity and introduces model behavior tensors with convolution products over tensor semirings to handle complex multi-metric constraints.

Result: The framework efficiently constructs model profiles capturing global landscape and trade-offs between criteria like accuracy, size, and fairness. Software implementation called emtrees demonstrates utility on real-world datasets, showing how ADTC facilitates evidence-based model selection in sensitive domains.

Conclusion: ADTC provides a formal algebraic framework for exhaustive analysis of decision trees, enabling comprehensive global assessment of model reliability that supports evidence-based model selection in Explainable AI applications, especially in sensitive domains.

Abstract: Ensuring model reliability in Explainable AI requires a global assessment of the hypothesis space. We propose a formal framework for the exhaustive analysis of optimal and near-optimal decision trees, called Algebraic Decision Tree Counting (ADTC). Inspired by Algebraic Model Counting (AMC) in knowledge representation, ADTC reformulates diverse analytical tasks, such as optimization, counting, and sampling, into a unified sum-of-products computation over a semiring $R$. While the hypothesis space of decision trees is doubly exponential with respect to the maximum depth $Δ$, our dynamic programming algorithm achieves $O^*(n^{O(Δ)})$ time complexity in the number of features $n$, where $O^*$ suppresses polynomial factors. To handle complex constraints consisting of multiple tree metrics, we introduce model behavior tensors that aggregate semiring values via convolution products over a tensor semiring. This algebraic approach efficiently constructs a model profile that captures the global landscape and trade-offs between criteria such as accuracy, size, and fairness. We demonstrate the utility of our software, emtrees, on real-world datasets, illustrating how ADTC facilitates evidence-based model selection in sensitive domains.

</details>


### [63] [Evidence-State Rewards for Long-Context Reasoning](https://arxiv.org/abs/2607.02073)
*Ya Gao,Pekka Marttinen*

Main category: cs.AI

TL;DR: Maven is a reinforcement learning framework with editable evidence memory that rewards action-level state transitions for long-context reasoning, outperforming existing methods.


<details>
  <summary>Details</summary>
Motivation: Existing long-context RL methods only reward final answers or static evidence extraction, providing little feedback on how intermediate actions change evidence states during reasoning.

Method: Propose Maven with editable evidence memory that defines answer-conditioned evidence-state value and rewards different action types: add actions by marginal gain/hindsight contribution, link actions by evidence synergy, and drop actions by improved answer support after removing misleading evidence, assigned to action spans in GRPO.

Result: Outperforms outcome-only RL and evidence-identification baselines across Llama and Qwen models on LongBench v2, LongReason, and RULER, producing more sufficient evidence sets and lower distractor retention.

Conclusion: Long-context RL benefits from optimizing stateful evidence navigation rather than one-shot evidence extraction.

Abstract: Long-context reasoning requires models to locate, revise, and synthesize evidence distributed across lengthy inputs. Existing long-context RL methods usually reward final answers or static evidence extraction, offering little feedback on how intermediate actions change the model's evidence state. We propose Maven, a reinforcement learning framework with an editable evidence memory. Maven defines an answer-conditioned evidence-state value and rewards action-level state transitions: add actions are credited by marginal gain and hindsight contribution, link actions by evidence synergy, and drop actions by improved answer support after removing misleading evidence. These rewards are assigned to the corresponding action spans in GRPO. Across Llama and Qwen models on LongBench v2, LongReason, and RULER, Maven outperforms outcome-only RL and evidence-identification baselines, producing more sufficient evidence sets and lower distractor retention. Our results show that long-context RL benefits from optimizing stateful evidence navigation rather than one-shot evidence extraction.

</details>


### [64] [SUNTA: Hierarchical Video Prediction with Surprise-based Chunking](https://arxiv.org/abs/2607.02087)
*Tomoshi Iiyama,Masahiro Suzuki,Yutaka Matsuo*

Main category: cs.AI

TL;DR: SUNTA introduces surprise-based hierarchical state-space modeling for long-horizon prediction, using prediction errors to determine temporal chunk boundaries and preventing hierarchical collapse through decoupled training.


<details>
  <summary>Details</summary>
Motivation: Current hierarchical state-space models (HSSMs) use suboptimal chunking methods (fixed-length or similarity-based) that often misalign with the data's intrinsic temporal structure. The authors argue that chunking should be driven by prediction errors, which better indicate when longer-range context is needed.

Method: SUNTA (Surprise-based Nested Temporal Abstraction) uses a decoupled training strategy to preserve surprise signals and prevent hierarchical collapse. It employs internal inconsistency as a top-down surprise metric to determine chunk boundaries during imagined rollouts, enabling surprise-based chunking even in open-loop prediction.

Result: Experiments on video prediction tasks in 2D and 3D environments show that SUNTA outperforms all baselines, uniquely maintaining accurate predictions over 250 timesteps, while all baseline methods degrade within the first 10 timesteps.

Conclusion: Surprise-based chunking driven by prediction errors is a more effective approach for hierarchical state-space models in long-horizon prediction tasks. SUNTA's decoupled training strategy successfully addresses the challenges of hierarchical collapse and missing surprise signals during open-loop prediction.

Abstract: Hierarchical state-space models (HSSMs) offer a promising approach to long-horizon prediction by segmenting sequences into temporal chunks. However, their performance hinges on how chunk boundaries are determined. While prior HSSMs typically rely on fixed-length chunking or similarity-based boundary detection, these methods often misalign with the intrinsic temporal structure of the data. We argue that chunking should instead be driven by prediction errors, which more directly indicate when longer-range context becomes necessary. Nevertheless, integrating surprise-based chunking into HSSMs introduces critical challenges, including hierarchical collapse during end-to-end training and the absence of surprise signals during open-loop prediction. To address these issues, we propose Surprise-based Nested Temporal Abstraction (SUNTA), a method that employs a decoupled training strategy to preserve surprise signals and uses internal inconsistency as a top-down surprise metric to determine chunk boundaries within imagined rollouts. Experiments on video prediction tasks in 2D and 3D environments demonstrate that SUNTA outperforms baselines, uniquely maintaining accurate predictions over 250 timesteps, whereas all baselines degrade within the first 10 timesteps.

</details>


### [65] [ContextNest: Verifiable Context Governance for Autonomous AI Agent](https://arxiv.org/abs/2607.02116)
*Misha Sulpovar,Benn R. Konsynski,Qaish Kanchwala,Gabe Goodhart*

Main category: cs.AI

TL;DR: ContextNext is an open specification and reference implementation for governed AI-consumable knowledge vaults that adds provenance, version control, integrity verification, and audit trails to retrieval systems.


<details>
  <summary>Details</summary>
Motivation: Current AI retrieval pipelines lack durable guarantees for provenance, version identity, integrity, traceability, and point-in-time reconstruction, creating governance gaps in autonomous AI systems that depend on external knowledge stores.

Method: ContextNext combines typed Markdown documents with metadata, deterministic set-algebraic selectors, SHA-256 hash-chained version histories, graph-level checkpoints, Model Context Protocol for live data, and audit traces to create a governance layer beneath retrieval systems.

Result: In experiments: 1) governed selection Pareto-dominates BM25 with 97% answer-quality pass rate vs 93-90% at one-third token cost; 2) deterministic selectors and BM25 achieve perfect stability while dense+HNSW baseline shows significant non-determinism (mean Jaccard 0.611).

Conclusion: Context governance addresses critical failure modes that retrieval quality alone cannot resolve, enabling organizations to verify which knowledge versions informed agent outputs and whether they were properly governed when consumed.

Abstract: Autonomous AI agents increasingly depend on external knowledge stores, yet most retrieval pipelines provide relevance without durable guarantees of provenance, version identity, integrity, traceability, or point-in-time reconstruction. We formalize this as context governance and present ContextNext, an open specification and reference implementation for governed AI-consumable knowledge vaults. ContextNext does not replace Retrieval-Augmented Generation (RAG); it supplies the governance layer beneath retrieval, determining which artifacts are approved, current, attributable, and integrity-verified before retrieval systems operate over them.
  The specification combines typed Markdown documents with metadata, deterministic set-algebraic selectors, contextnest:// URI references, SHA-256 hash-chained version histories, graph-level checkpoints, source nodes for live data through the Model Context Protocol (MCP), and audit traces of agent context consumption. These mechanisms let organizations reconstruct which knowledge versions informed an agent output and whether those versions were AI-eligible when consumed.
  We report first empirical results from two controlled experiments. In a stale-version attack isolating the governance-versus-retrieval failure mode, governed selection strictly Pareto-dominates BM25 sparse retrieval, with higher answer-quality pass rate (97% versus 93-90%) at about one-third the input-token cost. In a retrieval-determinism experiment over a 1,060-document corpus, deterministic selectors and BM25 return stable document sets across repeated identical queries (Jaccard 1.0), while a dense+HNSW baseline is non-deterministic on 80% of queries (mean Jaccard 0.611, worst case 0.210). These results suggest that context governance addresses failure modes retrieval quality alone is not designed to resolve. We release a core engine, CLI, and MCP server under open licenses.

</details>


### [66] [Enhancing Fitness Intelligence through Domain-Specific LLM Post-Training](https://arxiv.org/abs/2607.02118)
*Xingtao Zhao,Tian Yang,Han Jiang*

Main category: cs.AI

TL;DR: FitOne is a series of fitness-specific LLMs (8B and 32B parameters) that outperforms general LLMs on professional fitness exams while maintaining strong general capabilities through a three-stage training pipeline.


<details>
  <summary>Details</summary>
Motivation: Human scientific fitness coaching is costly and inaccessible. General-purpose LLMs lack sufficient domain-specific knowledge for complex fitness scenarios, showing weak performance in professional fitness applications.

Method: Built upon Qwen3 foundation models, FitOne uses a three-stage post-training pipeline: 1) continual pre-training, 2) supervised fine-tuning, and 3) reinforcement learning, leveraging large-scale, high-quality datasets from rigorous knowledge engineering.

Result: FitOne-8B/32B achieves average improvements of up to 10.09%/9.29% on ACSM-EP and 12.73%/7.01% on NSCA-CSCS exams compared to Qwen3 base models, while maintaining strong general capabilities like knowledge reasoning and instruction following.

Conclusion: FitOne demonstrates that domain-specific LLMs can significantly enhance performance in specialized fields like fitness coaching while preserving general capabilities. This research advances LLM systems toward more reliable fitness intelligence and inspires future domain-specific LLM development.

Abstract: Scientific Fitness Coaching (SFC) is typically delivered by human professionals, making it costly and inaccessible to many. While recent advances in Large Language Models (LLMs) show considerable promise for more inclusive fitness coaching, directly deploying prevailing general-purpose LLMs in SFC reveals critical limitations. These models often lack sufficient domain-specific knowledge integration, leading to weak performance on complex SFC scenarios. In this paper, we introduce FitOne, a series of fitness LLMs (with 8B and 32B parameters) designed to improve reliability and domain specialization for SFC applications. Built upon the Qwen3 foundation models, FitOne is developed through a three-stage post-training pipeline consisting of continual pre-training, supervised fine-tuning, and reinforcement learning, using large-scale, high-quality datasets derived from rigorous knowledge engineering. We conduct comprehensive evaluations of FitOne on professional fitness certification exams, including ACSM-EP and NSCA-CSCS, as well as general capabilities such as knowledge reasoning and instruction following. Experimental results show that, while retaining strong general capabilities, FitOne-8B/32B achieves average improvements of up to 10.09%/9.29% and 12.73%/7.01% on the ACSM-EP and NSCA-CSCS exams, respectively, compared with the Qwen3 base models. Furthermore, in-depth ablation studies confirm the necessity of each training stage, highlighting the pipeline's effectiveness in balancing domain expertise enhancement with general ability retention. We believe this research advances LLM systems toward more reliable fitness intelligence and will inspire future research on developing domain-specific LLMs.

</details>


### [67] [Coding-agents can replicate scientific machine learning papers](https://arxiv.org/abs/2607.02134)
*Atharva Hans,Ilias Bilionis*

Main category: cs.AI

TL;DR: Paper-replication is a workflow that turns paper claims into targets with recorded evidence, implemented as a coding-agent skill to systematically replicate computational claims from scientific machine learning papers.


<details>
  <summary>Details</summary>
Motivation: Current approaches to replicating computational claims from papers using coding agents don't reliably preserve progress or verify whether generated evidence actually supports the paper's claims. There's a need for a systematic workflow that ensures proper documentation and validation of replication efforts.

Method: The Paper-replication workflow involves: 1) converting selected paper claims into targets with recorded evidence, 2) reconstructing the paper's method, 3) running computational experiments, 4) linking outputs to provenance, 5) comparing with paper claims, 6) recording evidence in replication reports, and 7) passing validation checks before completion. Implemented as a coding-agent skill.

Result: Evaluation on 12 independent runs across 4 scientific machine learning papers shows all workspaces passed the completion gate, and all 158 recorded targets were matched with report coverage. However, runs varied in how papers were divided into targets, numerical fidelity, elapsed time, intermediate executions replaced, and evidence acceptance rules.

Conclusion: Paper-replication successfully makes completion depend on workspace evidence and validation checks rather than the agent's final message, providing a systematic approach to replicating computational claims from scientific papers while revealing variability in how different agents approach the same replication tasks.

Abstract: Scientific machine learning papers typically make computational claims, e.g., that the relative mean square error is less than 5% or that the 95% predictive credible interval covers the test data. A coding agent can be prompted to replicate those claims from paper materials alone, but the prompt does not by itself reliably preserve progress or check whether generated evidence supports the paper's claims. We introduce Paper-replication, a workflow that makes each selected paper claim a target with recorded evidence, and implement it as a coding-agent skill. The workflow makes the agent record those targets, reconstruct the paper's method, run computational experiments, link generated outputs to provenance and comparisons with the paper's claims, record where matched evidence appears in the replication report, and pass validation checks before completion. We evaluate Paper-replication on twelve independent runs across four scientific machine learning papers. All twelve workspaces pass the completion gate, and all 158 recorded targets are matched with report coverage. Even in this completed workspace state, repeated runs differ in how papers are divided into targets, in numerical fidelity to the source papers, in elapsed replication time, in the number of intermediate executions replaced before final evidence is accepted, and in the rules used to accept evidence. Paper-replication makes completion depend on workspace evidence and validation checks rather than on the agent's final message.

</details>


### [68] [A$^{2}$utoLPBench: An Auto-Generated, Agent-Friendly LP Benchmark via Inverse-KKT Construction](https://arxiv.org/abs/2607.02141)
*Shuo Ren,Yaohui Han,Yifan Shi,Libo Shen,Haodong Lu,Dongfang Wu,Rongliang Fu,Bei Yu,Tsung-Yi Ho*

Main category: cs.AI

TL;DR: A²utoLPBench is a generative benchmark for testing LLM-driven agents on linear programming problems that creates problems with known optimal solutions by construction, avoiding solver calls and human annotation.


<details>
  <summary>Details</summary>
Motivation: Current LP-from-text benchmarks are static datasets with fixed size and difficulty, susceptible to training data leakage into future LLMs, requiring solver calls and human annotation.

Method: Generate linear programming problems by first picking a feasible point and dual, then constructing a problem where that point is optimal with known objective value. The benchmark includes a reference solver-critic baseline and Docker environment with instructions for LLM-driven agents.

Result: The benchmark enables unlimited fresh problems, adjustable difficulty via (n,m) parameters, ground-truth solutions by construction, low cost per problem, repeatable scores, and resistance to training data leakage when using fresh seed ranges.

Conclusion: A²utoLPBench offers superior properties compared to static datasets for evaluating LLM-driven agents on linear programming problems, providing a scalable, leak-resistant, and cost-effective evaluation framework.

Abstract: Most LP-from-text benchmarks are static datasets of word problems written and labeled by hand. Once such a dataset is released, its size is fixed, its difficulty is fixed, and every problem can leak into the training data of future LLMs. We present \textbf{A$^{2}$utoLPBench}, a benchmark for testing LLM-driven agents on linear programming problems written in plain text. We first pick a feasible point and dual, then write down a problem for which that point is optimal and the objective value is known. The answer is known by construction, with no solver call and no human annotator. The evaluation environment bundles a reference solver-critic baseline and a Docker image whose usage instructions are written for an LLM-driven agent to read. With these in place, any agent can run the benchmark and get a calibrated score with one command. Because the benchmark is a generator rather than a fixed dataset, it has properties no fixed dataset can match: an unlimited supply of fresh problems, a difficulty knob set by $(n,m)$, ground-truth answers correct by construction, low LLM-side cost per problem relative to human authoring, repeatable scores across independent batches, and resistance to training-data leakage when fresh post-cutoff seed ranges are used.

</details>


### [69] [A rubric-based controlled comparison of frontier language models on expert-authored clinical reasoning tasks](https://arxiv.org/abs/2607.02175)
*Samiha A. Ismail,Fan X. Chen,Ali Merali*

Main category: cs.AI

TL;DR: Researchers created a difficult 5-scenario clinical evaluation dataset with detailed rubrics (184 total criteria) and found frontier LLMs perform poorly on critical clinical criteria but well on low-stakes ones, with most critical criteria unsatisfied by any model.


<details>
  <summary>Details</summary>
Motivation: Multiple-choice medical benchmarks are saturated, but open-ended clinical performance remains poor (HealthBench Hard subset top score 32%). There's a need for more challenging, clinically relevant evaluations of AI systems.

Method: Created a small, deliberately difficult evaluation dataset with 5 clinician-authored clinical scenarios across 4 specialties, each with atomic, weighted, MECE rubrics (25-62 criteria per task, 184 total) from clinician-drafted golden answers. Evaluated GPT 5.4, Claude Opus 4.7, and Gemini 3.1 Pro.

Result: Mean rubric pass rates: Claude 0.47, GPT 0.39, Gemini 0.37. Critical inversion: highest-weighted (critical) criteria passed at only 32.4-41.7%, while low-stakes criteria passed at 80-90%. 56 of 108 critical (52%) were satisfied by no model. LLM autoraters reproduced expert labels on 92.8-94.7% of criteria.

Conclusion: The work demonstrates a scalable, defensible pipeline for creating large-scale clinical benchmarks, showing frontier models struggle with critical clinical reasoning while excelling at less important criteria.

Abstract: Multiple-choice medical benchmarks are increasingly saturated, and recent rubric-based evaluations such as HealthBench have shown that open-ended clinical performance is far from solved - its "Hard" subset top score remains 32%. We present a small, deliberately difficult evaluation dataset of five clinician-authored clinical scenarios spanning four specialties (anaesthesia, internal/family medicine, emergency medicine, and obstetrics), each accompanied by an atomic, weighted, MECE rubric (25-62 criteria per task; 184 criteria total) authored from a clinician-drafted golden answer. We evaluate three frontier models: GPT 5.4, Claude Opus 4.7, and Gemini 3.1 Pro. Mean rubric pass rates were 0.47 (Claude), 0.39 (GPT), and 0.37 (Gemini). The central finding is an inversion of clinical priority: the highest-weighted (weight-5, critical) criteria passed at only 32.4-41.7%, while low-stakes weight-1 criteria passed at 80-90%. 56 of 108 critical (weight-5) criteria (52%) were satisfied by no model. Three LLM autoraters reproduced expert met/not-met labels on 92.8-94.7% of 552 graded criteria. We position this as a methods-and-preliminary-findings contribution: the five tasks demonstrate a scalable, defensible pipeline ready to develop into a large-scale benchmark.

</details>


### [70] [UA-ChatDev: Uncertainty-Aware Multi-Agent Collaboration for Reliable Software Development](https://arxiv.org/abs/2607.02186)
*Temitayo Olamilekan Ogunsusi,Lijun Qian,Xishuang Dong*

Main category: cs.AI

TL;DR: UA-ChatDev is an uncertainty-aware multi-agent software development framework that prevents hallucination propagation by integrating uncertainty quantification and verification mechanisms.


<details>
  <summary>Details</summary>
Motivation: Existing multi-agent software development frameworks assume equal reliability of intermediate agent outputs, making them vulnerable to hallucination propagation where early errors negatively impact final software quality.

Method: Proposes UA-ChatDev with: 1) lightweight uncertainty estimation using token-level log probabilities to assess agent confidence, 2) phase-aware threshold calibration to selectively trigger retrieval-based verification when uncertainty exceeds acceptable levels.

Result: Extensive experiments on SRDD benchmark show UA-ChatDev consistently outperforms existing single-agent and multi-agent frameworks across completeness, executability, consistency, and overall quality metrics.

Conclusion: Uncertainty-aware interactions enhance code execution reliability in multi-agent software development, effectively mitigating hallucination propagation through integrated uncertainty quantification and verification.

Abstract: Software development is a complex task that demands cooperation among agents with diverse roles. Large language models (LLMs) have enabled autonomous multi-agent software development frameworks that leverage role-based collaboration to automate requirements analysis, coding, testing, and refinement. However, existing approaches typically assume that intermediate agent outputs are equally reliable, leaving them vulnerable to hallucination propagation, where incorrect decisions generated in early development phases are transferred to downstream agents and negatively impact final software quality. To address this challenge, we propose UA-ChatDev, an uncertainty-aware multi-agent software development framework that integrates uncertainty quantification into agent interactions. It introduces a lightweight uncertainty estimation mechanism based on token-level log probabilities to assess the confidence of agent responses and employs phase-aware threshold calibration to selectively trigger retrieval-based verification when uncertainty exceeds acceptable levels. Extensive experiments on the SRDD benchmark demonstrate that UA-ChatDev consistently outperforms existing single-agent and multi-agent software development frameworks across completeness, executability, consistency, and overall quality metrics. Further ablation studies and communication analyses verify that uncertainty-aware interactions enhance code execution reliability.

</details>


### [71] [Criticality-Based Guard Rail Validation for AI Agent Decisions in Autonomous Telecom Networks](https://arxiv.org/abs/2607.02210)
*Ravi Kant Sharma*

Main category: cs.AI

TL;DR: The paper proposes a Guard Rail Validation (GRV) framework for real-time validation of AI/ML decisions in autonomous telecommunications networks to prevent erroneous autonomous actions through standardized runtime validation.


<details>
  <summary>Details</summary>
Motivation: As networks evolve toward full autonomy (Levels 4-5), AI/ML agents make real-time decisions without human intervention. There's currently no standardized runtime mechanism to intercept and validate individual inference outputs before they trigger live network state changes, creating risks of erroneous autonomous decisions that could disrupt critical telecommunications services.

Method: The GRV framework uses a runtime architecture that intercepts AI-driven decisions and evaluates them across multiple weighted dimensions: action scope, action type, service criticality, agent autonomy level, reversibility, and temporal behavioral patterns. Based on the calculated criticality level, graduated validation mechanisms are applied: execute-with-logging, bounds checking, independent agent validation, or multi-agent consensus. The framework also includes cross-agent conflict detection with criticality-weighted priority resolution and runtime conformance logging.

Result: The paper presents the complete GRV architecture, algorithmic procedures, and an O-RAN deployment model. It evaluates the framework's threat coverage against known AI/ML attacks in telecommunications, demonstrating its effectiveness in preventing erroneous autonomous decisions.

Conclusion: The GRV framework provides a standardized, practical solution for runtime validation of AI-driven decisions in autonomous networks, addressing safety, reliability, and regulatory compliance requirements (like EU AI Act Article 14) while enabling the transition to higher levels of network autonomy without compromising operational integrity.

Abstract: The evolution toward fully autonomous telecommunications networks (Autonomous Network Levels 4-5) requires AI/ML agents to make real-time network decisions without human intervention. However, no standardized runtime mechanism exists to intercept and validate individual inference outputs before they trigger live network state changes, creating risks of erroneous autonomous decisions. This paper proposes the Guard Rail Validation (GRV) framework, a standardizable runtime architecture for intercepting and validating AI-driven decisions before execution. The framework evaluates decisions across multiple weighted dimensions -- including action scope, action type, service criticality, agent autonomy level, reversibility, and temporal behavioural patterns -- to determine a criticality level. Based on this level, graduated validation mechanisms are applied: execute-with-logging, bounds checking, independent agent validation, or multi-agent consensus. The framework additionally provides cross-agent conflict detection with criticality-weighted priority resolution and runtime conformance logging for regulatory compliance (e.g., EU AI Act Article 14). We present the architecture, algorithmic procedures, O-RAN deployment model, and evaluate threat coverage against known AI/ML attacks in telecommunications.

</details>


### [72] [Purified OPSD: On-Policy Self-Distillation Without Losing How to Think](https://arxiv.org/abs/2607.02234)
*Zhanming Shen,Jintao Tong,Shaotian Yan,Chen Shen,Hao Chen,Wentao Ye,Xiaomeng Hu,Rui Miao,Haobo Wang,Junbo Zhao,Gang Chen,Jieping Ye*

Main category: cs.AI

TL;DR: The paper proposes a novel method to fix on-policy self-distillation failures in long chain-of-thought reasoning models by decomposing teacher supervision signals and using PMI to isolate transferable components.


<details>
  <summary>Details</summary>
Motivation: On-policy self-distillation (OPSD) consistently fails on long chain-of-thought reasoning models, yielding marginal gains while destabilizing their reflective reasoning capabilities, which requires understanding why OPSD doesn't work for these models.

Method: 1) Decompose teacher supervision into reference-induced (non-transferable) and question-conditioned (transferable) components using a reference-only teacher. 2) Use pointwise mutual information (PMI) to transform the residual into a well-formed target distribution for distillation, filtering out reference-specific shortcuts.

Result: Experiments on four long-CoT models across two datasets show consistent improvements over both base models and standard OPSD, while preserving models' natural epistemic behavior throughout training.

Conclusion: The failure of standard OPSD in long-CoT reasoning stems from reference-induced shortcut memorization; the proposed decomposition and PMI-based distillation successfully isolates transferable components and enables effective self-distillation without destabilizing reasoning capabilities.

Abstract: On-policy self-distillation (OPSD) has emerged as a promising paradigm for improving LLM reasoning, where a privileged teacher with access to reference solutions provides token-level supervision on the student's own generated trajectories. However, we find that OPSD consistently fails on long chain-of-thought (long-CoT) reasoning models, yielding at best marginal gains while destabilizing the reflective reasoning capability these models depend on. Through a novel decomposition of the teacher's supervision signal, we identify the root cause: the teacher's supervision is dominated by a reference-induced component that drives rote memorization of reference-specific shortcuts, while the question-conditioned, inference-transferable component is ignored or actively opposed. Based on this diagnosis, we propose a two-step solution. First, we construct a reference-only teacher (the same model conditioned on the reference without the question) to isolate the non-transferable component of the supervision signal; the residual after subtracting this component captures the question-conditioned, inference-transferable correction. Second, we use pointwise mutual information (PMI) as the mechanism to transform this residual into a well-formed PMI target distribution that the student can directly distill from, filtering out the reference-induced shortcut. Experiments on four long-CoT models across two datasets demonstrate consistent improvements over both the base model and standard OPSD, while preserving the models' natural epistemic behavior throughout training.

</details>


### [73] [Copewell: A Multi-Agent Swarm Architecture for Equitable Mental Wellness Support](https://arxiv.org/abs/2607.02245)
*Seren Yenikent,Jack Vinijtrongjit,Katherine Ng*

Main category: cs.AI

TL;DR: Copewell is a multi-agent swarm system using human-centered AI to provide mental wellness support through multi-source assessment, emotion mapping, and dual-mode interventions while addressing algorithmic bias and ethical concerns.


<details>
  <summary>Details</summary>
Motivation: Global mental health crisis with 75% untreated in low/middle-income countries due to workforce shortages, costs, and stigma. Current AI wellness solutions fail with high abandonment rates and lack measurable, immediate relief tailored to dynamic emotional states.

Method: Multi-agent swarm system with three technical innovations: 1) Multi-source assessment (self-reported, physiological, contextual data) to reduce algorithmic bias; 2) Valence-arousal emotion mapping using Russell's Circumplex Model; 3) Dual-mode intervention (conversational + evidence-based sensory protocols). Includes privacy-first architecture, Ethics Supervisor agent, and participatory design with mental health practitioners.

Result: Early practitioner engagement and beta deployment informed design decisions and identified directions for future empirical evaluation. The system architecture demonstrates operationalization of equity and safety principles from inception.

Conclusion: Copewell contributes to responsible AI discourse by showing how technical architecture can embed human-centered principles from the start, potentially expanding access to mental wellness support while addressing critical limitations of current AI solutions.

Abstract: Mental health disorders affect nearly one billion people globally, yet 75% of individuals in low- and middle-income countries receive no treatment due to workforce shortages, cost barriers, and stigma. Current AI-powered wellness solutions predominantly rely on single-mode conversational interfaces that suffer high abandonment rates and fail to provide measurable, immediate relief calibrated to users' dynamic emotional states. This paper presents Copewell, a novel multi-agent swarm system designed to expand access to mental wellness support through human-centered AI principles. Our architecture introduces three technical innovations: (1) a multi-source assessment framework integrating self-reported, physiological, and contextual data to mitigate algorithmic bias; (2) valence-arousal emotion mapping using Russell's Circumplex Model of Affect to route users to specialized AI agents; and (3) dual-mode intervention delivery combining conversational support with evidence-based sensory wellness protocols. We examine the sociotechnical design considerations underlying Copewell's development, including a privacy-first architecture, embedded ethical oversight through a dedicated Ethics Supervisor agent, and participatory design informed by mental health practitioners. Early practitioner engagement and beta deployment inform design decisions and identify directions for future empirical evaluation. This work contributes to responsible AI discourse by demonstrating how technical architecture can operationalize equity and safety principles from inception.

</details>


### [74] [AgenticSTS: A Bounded-Memory Testbed for Long-Horizon LLM Agents](https://arxiv.org/abs/2607.02255)
*Xiangchen Cheng,Yunwei Jiang,Jianwen Sun,Zizhen Li,Chuanhao Li,Xiangcheng Cao,Yihao Liu,Fanrui Zhang,Li Jin,Kaipeng Zhang*

Main category: cs.AI

TL;DR: A bounded memory contract for LLM agents using typed retrieval rather than appending all past context, validated in a challenging deck-building game where it improves win rates from 30% to 60%.


<details>
  <summary>Details</summary>
Motivation: Standard LLM agent memory approaches append all past observations, tool calls, and reflections to every prompt, creating a jumbled mixture where individual memory component effects are hard to isolate. This makes it difficult to study how explicit memory layers shape long-horizon decisions.

Method: Introduces a bounded memory contract where each decision is made from a fresh user message assembled by typed retrieval, with no raw cross-decision transcript appended. This keeps prompts bounded across runs and allows isolation of individual layers. The approach is instantiated in Slay the Spire 2, a deck-building game requiring hundreds of decisions.

Result: In the game benchmark (human win rate: 16%), frontier LLMs had zero wins. Within the proposed harness, the no-store baseline won 3/10 games, while adding the skill layer improved to 6/10 wins (directional improvement, Fisher exact p≈0.37). The system includes a reproducible testbed with 298 completed trajectories, condition tags, memory snapshots, and analysis scripts.

Conclusion: The paper presents both a bounded memory contract design for LLM agents and a validated, reusable methodology for studying how explicit memory layers influence long-horizon decisions, with demonstrated improvements in a challenging game environment.

Abstract: Memory for a long-horizon LLM agent is a contract about what each future decision is allowed to see. The simplest contract appends past observations, tool calls, and reflections to every prompt, which makes prior context easy to access but also turns it into a jumbled mixture in which the effect of any single memory component is hard to isolate. We introduce and instrument an alternative bounded contract: every decision is made from a fresh user message assembled by typed retrieval, with no raw cross-decision transcript appended. The prompt thus stays bounded across runs of any length, and any single layer can be ablated in isolation. We instantiate the contract in Slay the Spire 2, a closed-rule stochastic deck-building game whose runs require hundreds of tactical and strategic decisions. A public online benchmark of frontier LLMs on the same game reports zero wins at the lowest difficulty across five configurations, and the developer-reported human win rate at the same difficulty is 16%; the task is hard but not saturated. Within our harness, a fixed-A0 ablation shows the largest observed difference when triggered strategic skills are enabled: the no-store baseline wins 3/10 games and adding the skill layer 6/10. At this sample size the comparison is directional rather than statistically decisive (Fisher exact p\approx0.37); a cross-backbone probe and public accumulating-context baselines are reported as operational comparisons rather than controlled tests of the contract variable itself. We release a reproducible testbed: 298 completed trajectories with condition tags, frozen memory/skill snapshots, prompt records, and analysis scripts -- an agent design and a validated, reusable methodology for studying how explicit memory layers shape long-horizon LLM-agent decisions.

</details>


### [75] [A Hippocampus for Linear Attention: An Exact Memory for What the Recurrent State Forgets](https://arxiv.org/abs/2607.02303)
*Wanyun Cui*

Main category: cs.AI

TL;DR: HOLA (Hippocampal Linear Attention) combines a compressive linear attention state with a bounded exact KV cache to improve memory retention and retrieval in language models, achieving better perplexity and longer-context recall than alternatives.


<details>
  <summary>Details</summary>
Motivation: Linear-attention and state-space models compress context into fixed-size recurrent states, causing memory loss when many key-value associations compete - earlier facts get overwritten, degrading needle recall. The paper is inspired by Complementary Learning Systems to add a hippocampal-like exact memory complement.

Method: HOLA maintains both: 1) usual delta-rule state as compressive memory, and 2) bounded exact KV cache. Cache writes tokens with large beta * ||e|| (prediction residuals actually committed to state) without learned eviction. Cache read uses decoupled RMSNorm-gamma for sharp retrieval rather than soft averaging.

Result: At 340M parameters trained on 15B SlimPajama tokens: Wikitext perplexity reduced from 27.32 to 22.92 (-16.1%), beating full-attention Transformer++ (26.88). LAMBADA perplexity improved from 30.95 to 30.26. Best linear in-context retrieval, more robust than GDN or HOLA+recency cache on RULER needle-in-a-haystack recall up to 32k tokens (16x training length).

Conclusion: Adding a hippocampal exact memory complement to linear attention models successfully addresses memory overwriting issues, enabling better long-context recall while maintaining computational efficiency. The semiparametric approach with separate compressive and exact memories proves effective for language modeling.

Abstract: Linear-attention and state-space language models compress the prefix into a fixed-size recurrent state, yielding O(1) memory at the cost of a lossy exact memory: when many key--value associations compete, earlier facts are overwritten and needle recall degrades. Inspired by Complementary Learning Systems, we give linear attention a hippocampal complement. HOLA (Hippocampal Linear Attention) keeps the usual delta-rule state as a compressive memory and adds a bounded exact KV cache, forming a semiparametric test-time memory: the state models linearly compressible structure, while the cache stores associations that should not be forced through that state. The cache writes without a learned eviction module, keeping tokens with large beta * ||e||, the prediction residual actually committed to the state; a decoupled RMSNorm-gamma cache read then turns these exact KV pairs into sharp retrieval rather than soft averaging. At 340M parameters trained on 15B SlimPajama tokens, HOLA lowers Wikitext perplexity from 27.32 to 22.92 (-16.1%), below a full-attention Transformer++ (26.88), and improves LAMBADA perplexity from 30.95 to 30.26. It also achieves the best linear in-context retrieval and remains much more robust than GDN or a matched HOLA+recency cache on RULER needle-in-a-haystack recall out to 32k tokens (16x its training length).

</details>


### [76] [Grounded autonomous research: a fault-tolerant LLM pipeline from corpus to manuscript in frontier computational physics](https://arxiv.org/abs/2607.02329)
*Haonan Huang*

Main category: cs.AI

TL;DR: An autonomous research agent pipeline that goes from literature corpus to publication-grade physics manuscript with novel findings, featuring fault tolerance through redundancy and requiring minimal human intervention only for reproduction failures.


<details>
  <summary>Details</summary>
Motivation: Frontier physical science differs fundamentally from machine learning sandboxes: requires physical reasoning for methodology choices, often has underdocumented toolchains, and needs calibration from external literature sources. Current autonomous agents tend to hallucinate plausible but unverifiable results from internal priors rather than confronting literature anchors.

Method: A pipeline that autonomously: 1) maps a corpus of 11,083 recent condensed-matter physics papers, 2) conceives a research direction, 3) calibrates methodology by reproducing published references, 4) conducts novel first-principles computations, and 5) writes the manuscript. The system uses fresh-context isolation across 47 sessions in 6 phases, with 2,162 literature-consultation events for grounding.

Result: The pipeline produced a publication-grade manuscript with three substantive physics findings on altermagnetic piezomagnetism. The system demonstrates fault tolerance through redundancy (fresh-context isolation, distributed grounding, adversarial review) and requires bounded human intervention only at reproduction failures (operational knowledge curation, not scientific direction).

Conclusion: The primitives, characterized failure modes, and quantified intervention pattern provide a foundation for autonomous research in high-stakes scientific domains beyond computational physics, with structurally enforced numerical confrontation at calibration checkpoints serving as the operative grounding mechanism.

Abstract: Autonomous-research agents have demonstrated end-to-end LLM automation in machine-learning sandboxes where execution provides calibration. Frontier physical science differs categorically: physical reasoning underlies every methodology choice, toolchains are often underdocumented, and calibration must come from external literature anchors - which unscaffolded agents cite but do not confront, hallucinating plausible, unverifiable results from internal priors. We present a pipeline that runs end-to-end from a corpus of 11,083 recent condensed-matter physics arXiv papers to a publication-grade manuscript with three substantive physics findings (here on altermagnetic piezomagnetism): the agent autonomously conceives a research direction by mapping the corpus, calibrates methodology by reproducing published references, conducts novel first-principles computations, and writes the manuscript - grounded in literature throughout, across 47 fresh-context sessions in six phases sharing only on-disk state, with 2,162 literature-consultation events. Fault tolerance emerges from redundancy: fresh-context isolation, distributed grounding, and adversarial review catch what any single session misses; pre- and post-pilot stages are fully autonomous, and pilot requires bounded human intervention only at reproduction failures - operational knowledge curation, not scientific direction. Two paired failure modes - a pre-architecture baseline and a no-pilot ablation - isolate structurally enforced numerical confrontation at calibration checkpoints as the operative grounding mechanism. The primitives, characterized failure modes, and quantified intervention pattern lay a foundation for autonomous research in high-stakes scientific domains beyond computational physics.

</details>


### [77] [DRIFTLENS: Measuring Memory-Induced Reasoning Drift in Personalized Language Models](https://arxiv.org/abs/2607.02374)
*Xi Fang,Weijie Xu,Yingqiang Ge,Yuhui Xu,Stephanie Eckman,Chandan K. Reddy*

Main category: cs.AI

TL;DR: Personalizing LLMs with user memory can significantly alter their reasoning trajectories on open-ended questions, even when final answers appear similar, and current mitigation methods reduce but don't eliminate this "reasoning drift."


<details>
  <summary>Details</summary>
Motivation: While LLMs use user memory to personalize responses, it's unclear how this affects their underlying reasoning processes on open-ended questions where there's no single correct answer. The researchers want to measure whether injecting user attributes reshapes the reasoning trajectory itself, not just the final output.

Method: The authors introduce DRIFTLENS, a ground-truth-free framework that maps each reasoning step to value categories and measures divergence between no-memory and memory-injected reasoning trajectories. They validate it distinguishes content-free noise from substantive reasoning changes, then test it across 4 LLMs and 10 user-attribute categories. They also evaluate GRPO- and DPO-based post-training methods for reducing drift.

Result: User-attribute memory induces medium-to-large reasoning drift above each model's pragmatic-noise floor, even when final answers remain fluent and plausible. Both GRPO and DPO methods reduce drift, but neither uniformly dominates, with effects on downstream capabilities being model- and reward-dependent.

Conclusion: Memory-induced reasoning drift is a measurable failure mode of personalized language models that current methods only partially mitigate, suggesting personalization fundamentally changes not just what models say but how they reason to reach conclusions.

Abstract: Personalization changes what a model says to a user; we show that it can also change the reasoning trajectory used to justify the response. Modern LLMs personalize interactions by storing user attributes, preferences, and prior context, then injecting this information into future prompts. We study whether such memory reshapes reasoning on open-ended questions where no single ground-truth answer exists. To quantify this effect, we introduce DRIFTLENS, a ground-truth-free framework that maps each expressed reasoning step to a value category and measures divergence between a question's no-memory trajectory and its trajectory under injected user-attribute memory. We first validate that DRIFTLENS distinguishes content-free pragmatic noise from substantive reasoning changes. Across four LLMs and 10 user-attribute categories, including age, occupation, and disability, user-attribute memory induces medium-to-large reasoning drift above each model's pragmatic-noise floor, even when final answers remain fluent, on-topic, and plausible. We then evaluate GRPO- and DPO-based post-training methods for reducing drift. Both reduce drift, but neither uniformly dominates; effects on downstream capability, helpfulness, and instruction following are model-and reward-dependent. These results suggest that memory-induced reasoning drift is a measurable and only partly mitigated failure mode of personalized language models.

</details>


### [78] [Steerability via constraints: a substrate for scalable oversight of coding agents](https://arxiv.org/abs/2607.02389)
*Thomas Winninger*

Main category: cs.AI

TL;DR: The paper argues that applying traditional human engineering team management methods (access control, coding conventions, tooling) to coding agents is more efficient than complex agentic scaffolding, and demonstrates improved security oversight through controlled experiments.


<details>
  <summary>Details</summary>
Motivation: While coding agents are capable, human oversight becomes the bottleneck. Unconstrained agents introduce security risks, erode codebase scalability, and make human review increasingly costly. The authors seek to address these issues by applying established human team management principles to coding agents.

Method: The authors propose applying traditional software engineering management methods (access control, network policies, strict coding conventions enforced by tooling) to coding agents. They implement a constrained substrate system and conduct a controlled experiment where a small reviewer model (Gemma 4 e4b) inspects a Python codebase with 11 inserted backdoors, comparing performance with and without constraints and tools.

Result: Recall for detecting backdoors improved from 54.5% (unconstrained, no tools) to 90.9% (with constrained substrate plus a ~200-LoC `docs` CLI). Both the substrate constraints and tools contributed independently to the performance improvement. The approach is particularly effective in languages like Python that offer fewer guarantees by default.

Conclusion: Traditional human engineering team management principles (access control, conventions, tooling) can be effectively transferred to coding agents, providing more scalable and secure oversight than recent complex agentic scaffolding approaches, especially in languages with fewer built-in guarantees.

Abstract: Coding agents are capable; human oversight is the bottleneck. Unconstrained agents introduce security risks, erode codebase scalability, and make human review increasingly costly. We argue that the same methods used for decades to manage large human engineering teams: access control, network policies, strict coding conventions enforced by tooling; transfer directly to coding agents, and are cheaper (in token) than recent agentic scaffolding. We sketch a start-to-end system on this principle, and report a controlled experiment in scalable oversight: a small reviewer (Gemma 4 e4b) inspects a Python codebase containing 11 inserted backdoors. Recall rises from 54.5% (unconstrained, no tools) to 90.9% (constrained substrate plus a ~200-LoC `docs` CLI), with substrate and tools contributing independently. We choose Python deliberately: substrate-level oversight gains are largest where the language gives the fewest guarantees by default; the principles extend to languages like Rust.

</details>


### [79] [Fast Multi-dimensional Refusal Subspaces via RFM-AGOP](https://arxiv.org/abs/2607.02396)
*Thomas Winninger*

Main category: cs.AI

TL;DR: A new method using Recursive Feature Machine (RFM) with probe-informed initialization identifies multi-dimensional refusal subspaces in LLMs efficiently, achieving faster subspace extraction and better ablation performance compared to existing methods.


<details>
  <summary>Details</summary>
Motivation: Existing methods for extracting multi-dimensional subspaces that encode complex behaviors (like refusal to answer harmful queries) in LLMs are computationally expensive, especially prohibitive for reasoning models with long reasoning traces.

Method: The authors adapt the Recursive Feature Machine (RFM) algorithm with a probe-informed initialization to efficiently identify multi-dimensional refusal subspaces in both reasoning (Qwen 3) and non-reasoning (Qwen 2.5) models.

Result: RFM enables subspace identification in seconds, shows better performance on ablation tasks than alternatives, and could serve as a cheap and scalable complement to existing subspace-extraction methods if results are confirmed.

Conclusion: RFM represents a promising efficient alternative for subspace extraction in LLMs, with potential to complement existing methods once relationships between subspaces found by different approaches are better understood.

Abstract: Steering and monitoring activations in Large Language Models (LLMs) are increasingly used for both safety and interpretability. Early work assumed behaviours are encoded along single linear directions, but recent findings suggest complex behaviours, such as the refusal to answer harmful queries, live in multi-dimensional subspaces. However, existing methods for extracting these subspaces are computationally expensive, which becomes prohibitive on reasoning models who produce long reasoning traces. By adapting the Recursive Feature Machine (RFM) algorithm -- which can be computed efficiently -- with a probe-informed initialization, we are able to identify the multi-dimensional refusal subspace in seconds, on reasoning (Qwen 3) and non-reasoning (Qwen 2.5) models. While RFM allows for faster subspace identification, it also showed better performances on the ablation task than its alternatives. More work is planned to better understand the relations between subspaces found by different methods. If confirmed, RFM could be a cheap and scalable complement to existing subspace-extraction methods in LLMs.

</details>


### [80] [Text-Driven 3D Indoor Scene Synthesis in Non-Manhattan Environments](https://arxiv.org/abs/2607.02407)
*Xianhui Meng,Zirui Song,Yuchen Zhang,Li Zhang,Yongxuan Lv,Xiuying Chen,Kun Wang,Yan Luo,Kai Chen,Hangjun Ye,Long Chen,Jun Liu,Xiaoshuai Hao*

Main category: cs.AI

TL;DR: SPG-Layout is a text-driven framework for generating physically plausible indoor scenes in non-Manhattan environments, using statistical priors and hierarchical layout strategies to overcome limitations of existing LLM-based methods.


<details>
  <summary>Details</summary>
Motivation: Existing LLM-based methods for 3D indoor synthesis fail in non-Manhattan environments, struggling to model non-orthogonal spatial relationships and resulting in high geometric violations and low physical fidelity.

Method: Uses statistical priors of object distributions to guide training, adopts hierarchical layout strategy prioritizing large objects first (mirroring human design workflows), and synergizes these components for balanced optimization.

Result: Extensive experiments on a new benchmark of 500 diverse non-Manhattan environments show SPG-Layout consistently and significantly outperforms existing methods in both Manhattan and non-Manhattan settings.

Conclusion: SPG-Layout effectively addresses the challenges of generating physically plausible indoor scenes in complex non-Manhattan environments, achieving superior performance through its combined statistical priors and hierarchical approach.

Abstract: Large Language Models (LLMs) have demonstrated remarkable capabilities in 3D indoor synthesis for Manhattan environments. However, existing methods often fail to capture plausible object layout patterns in non-Manhattan settings, primarily because they struggle to model non-orthogonal spatial relationships, leading to high geometric violations and low physical fidelity. To address this challenge, we propose SPG-Layout, a novel text-driven framework designed to generate physically plausible indoor scenes within complex non-Manhattan environments. Specifically, we first utilize statistical priors of object distributions to guide the training process, enhancing environmental understanding and fidelity. Furthermore, mirroring human design workflows, we adopt a hierarchical layout strategy that prioritizes the placement of large objects, thereby substantially minimizing layout violations. By synergizing these components, SPG-Layout achieves a balanced optimization of semantic realism and physical plausibility. To evaluate performance in these complex settings, we constructed a new benchmark comprising 500 diverse non-Manhattan environments. Extensive experiments demonstrate that SPG-Layout consistently and significantly outperforms existing methods across both Manhattan and non-Manhattan environments. The code will be publicly released.

</details>


### [81] [Automated grading of Linux/bash examinations using large language models: a four-level cognitive taxonomy approach](https://arxiv.org/abs/2607.02432)
*Manuel Alonso-Carracedo,Ruben Fernandez-Boullon,Pedro Celard,Francisco J. Rodriguez-Martinez,Lorena Otero-Cerdeira*

Main category: cs.AI

TL;DR: LLMs can effectively grade Linux/bash command-line responses with rubric-enhanced prompting, achieving high agreement with human experts, though performance declines with increasing cognitive complexity.


<details>
  <summary>Details</summary>
Motivation: The paper addresses the challenge of scalable and reliable grading for command-line examinations in computing education, where manual marking is difficult due to rising enrollments and traditional autograders cannot handle partial credit, equivalent solutions, or syntactic variation.

Method: The study evaluates four frontier LLMs (GPT, Claude Opus, Gemini, and GLM) using a four-level cognitive taxonomy combining cognitive complexity and operational impact. Models were tested with two prompt variants (minimal baseline and rubric-enhanced) on 1200 real student responses graded by three expert instructors.

Result: Gemini~3.0 Pro with rubric-guided prompting achieved the highest human-AI agreement (ICC(3,1) = 0.888, MAE = 0.10, Bland-Altman bias = -0.014). Agreement consistently declined as taxonomy level increased, with the largest discrepancies at higher levels. Rubric quality had a larger effect than model provider choice.

Conclusion: Question complexity reliably predicts grading difficulty for LLMs, establishing a taxonomy-based framework for determining which questions are suitable for AI-assisted grading versus human review. The study provides transferable evaluation protocols and prompt templates for practical implementation.

Abstract: Scalable and reliable grading of command-line examinations remains a challenge in computing education, where rising enrolments make manual marking difficult and rule-based autograders cannot handle partial credit, equivalent solutions, or syntactic variation. This paper evaluates whether four frontier Large Language Models (GPT, Claude Opus, Gemini, and GLM) can approximate expert judgment when grading short Linux/bash command responses. The study adopts a four-level cognitive taxonomy that combines cognitive complexity and operational impact, ranging from information retrieval (L1) and basic file manipulation (L2) to structural operations (L3) and advanced system management (L4). The models were tested with two prompt variants, a minimal baseline and a rubric-enhanced version, on 1200 real responses from second-year Computer Engineering students independently graded by three expert instructors. Gemini~3.0 Pro with rubric-guided prompting achieved the highest human-AI agreement (ICC(3,1) = 0.888, MAE = 0.10, Bland-Altman bias = -0.014). Agreement declined consistently as taxonomy level increased, with the largest discrepancies at higher levels. Across all models, rubric quality had a larger effect than provider choice, with structured prompts consistently improving agreement. These results show that question complexity is a reliable predictor of the difficulty LLMs face in grading accurately, and they establish a principled, taxonomy-based framework for determining which questions are suitable for AI-assisted grading and which require human review, while also providing a transferable evaluation protocol and prompt templates.

</details>


### [82] [EvoPolicyGym: Evaluating Autonomous Policy Evolution in Interactive Environments](https://arxiv.org/abs/2607.02440)
*Zhilin Wang,Han Song,Runzhe Zhan,Jusen Du,Jiacheng Chen,Tianle Li,Qingyu Yin,Yulun Wu,Zhennan Shen,Tong Zhu,Yanshu Li,Guanjie Chen,Derek F. Wong,Yafu Li,Yu Cheng,Yang Yang*

Main category: cs.AI

TL;DR: The paper introduces EvoPolicyGym, a benchmark for evaluating how autonomous agents iteratively improve executable policies through feedback within fixed interaction budgets, with GPT-5.5 showing strongest performance.


<details>
  <summary>Details</summary>
Motivation: Existing evaluations of autonomous agents often collapse policy improvement processes into final scores or confound them with open-ended software engineering progress, lacking controlled settings to assess iterative policy evolution under feedback.

Method: Introduces Autonomous Policy Evolution, a controlled evaluation setting where a harness-model agent repeatedly edits executable policy systems under fixed interaction budgets. Instantiated in EvoPolicyGym benchmark built from compact interactive RL environments.

Result: GPT-5.5 achieves the strongest aggregate rank score and top-two performance on all 16 EvoPolicyGym environments. The benchmark provides trajectory-level diagnostics showing how agents allocate budget and convert feedback into parametric tuning.

Conclusion: Strong autonomous policy evolution depends not only on isolated task wins, but on discovering task-appropriate mechanisms and refining policies under bounded feedback. EvoPolicyGym provides controlled evaluation beyond simple leaderboard results.

Abstract: Autonomous agents are increasingly expected to improve executable policies through feedback, yet existing evaluations often collapse this process into a final score or confound it with open-ended software-engineering progress. We introduce Autonomous Policy Evolution, a controlled evaluation setting in which a harness-model agent repeatedly edits an executable policy system under a fixed interaction budget. We instantiate this setting in EvoPolicyGym, a benchmark built from compact interactive RL environments that evaluates how agents iteratively improve explored policies. On the EvoPolicyGym suite, GPT-5.5 achieves the strongest aggregate rank score and top-two performance on all 16 environments. Beyond leaderboard results, EvoPolicyGym also provides trajectory-level diagnostics that distinguish how agents allocate budget, convert feedback into parametric tuning. These analyses show that strong autonomous policy evolution depends not only on isolated task wins, but on discovering task-appropriate mechanisms and refining policies under bounded feedback.

</details>


### [83] [G-RRM: Guiding Symbolic Solvers with Recurrent Reasoning Models](https://arxiv.org/abs/2607.02491)
*Timo Bertram,Sidhant Bhavnani,Richard Freinschlag,Erich Kobler,Andreas Mayr,Günter Klambauer*

Main category: cs.AI

TL;DR: G-RRM integrates symbol-equivariant RRMs with symbolic solvers, showing neural guidance improves solver efficiency only when problems have expansive search spaces and solvers can dynamically overwrite imperfect hints.


<details>
  <summary>Details</summary>
Motivation: The paper aims to understand when neural guidance can practically improve symbolic solver performance for constraint satisfaction problems, addressing the integration of neuro-symbolic approaches for better computational efficiency.

Method: The authors propose G-RRM, which uses SE-RRMs (symbol-equivariant RRMs) as neural solvers to generate full solution proposals that guide classical symbolic solvers like backtracking, Glucose 4.1, and CaDiCaL 3.0.0. They test this approach on constraint satisfaction problems.

Result: G-RRM's effectiveness depends on two conditions: expansive combinatorial search space and solver's ability to dynamically overwrite branching choices. When met, significant speedups occur: 33.3× for backtracking and 1.70× for Glucose 4.1 on 9×9 Sudoku. CaDiCaL 3.0.0 showed no speedup due to respecting injected hints without overwriting.

Conclusion: Neural guidance with G-RRM translates to practical speedups only in specific regimes where problems have large search spaces and solvers can adapt to imperfect neural hints through dynamic overwriting of branching decisions.

Abstract: In this work, we focus on SE-RRMs, a symbol-equivariant instantiation of RRMs that exhibits improved extrapolation to larger problem sizes. We propose a neuro-symbolic approach, ``Guiding with Recurrent Reasoning Models'' (G-RRM), which integrates SE-RRMs with symbolic solvers for constraint satisfaction problems. SE-RRMs act as neural solvers that generate full solution proposals and guide classical symbolic solvers, such as backtracking or SAT-based methods like Glucose 4.1 and CaDiCaL 3.0.0, that produce globally correct solutions. Centrally, we investigate when neural guidance with G-RRM improves the search efficiency of symbolic solvers. %
Our experiments show that the efficacy of G-RRM depends on two conditions: first, the problem instances must have an expansive combinatorial search space to expose potential gains, and second, the solver architecture must be capable of dynamically overwriting its branching choices to recover when neural hints are imperfect. When these conditions hold, guidance drives median conflict counts to zero and yields significant wall-clock speedups: on $9\times9$ Sudoku, where the SE-RRM correctly solves $91.1\%$ of instances, backtracking accelerates by $33.3\times$ and Glucose 4.1 by $1.70\times$ (median, $p<0.001$), with Glucose 4.1 retaining a $1.17\times$ speedup on perfect-hint $25\times25$ grids. In contrast, CaDiCaL 3.0.0, whose runtime is overhead-dominated and which always respects the injected branching hints rather than overwriting them, shows no significant speedup (median $1.02\times$, n.s.) and even a small significant mean slowdown ($0.90\times$) on $9\times9$. These results delineate the regimes in which neural guidance translates into practical speedups.

</details>


### [84] [ReContext: Recursive Evidence Replay as LLM Harness for Long-Context Reasoning](https://arxiv.org/abs/2607.02509)
*Yanjun Zhao,Ruizhong Qiu,Tianxin Wei,Yuanchen Bei,Zhining Liu,Lingjie Chen,Ismini Lourentzou,Hanghang Tong,Jingrui He*

Main category: cs.AI

TL;DR: RECONTEXT is a training-free inference method that improves long-context reasoning by recursively selecting and replaying query-relevant evidence from the input before final answer generation.


<details>
  <summary>Details</summary>
Motivation: LLMs often fail to effectively use relevant evidence already present in long contexts, despite supporting long context windows. There's a gap between context access and effective context utilization.

Method: RECONTEXT uses model-internal relevance signals to construct query-conditioned evidence pools and recursively replays them before final generation while preserving the original context. It separates evidence organization from answer generation without training, external memory, or context pruning.

Result: Experiments on eight long-context datasets with 128K context length show consistent improvements in evidence utilization across Qwen3-4B, Qwen3-8B, and Llama3-8B models, achieving the best average rank on all three backbones.

Conclusion: RECONTEXT provides an effective, training-free approach to bridge the gap between context access and utilization in LLMs, with theoretical grounding in associative memory principles and practical improvements across multiple model architectures.

Abstract: Understanding and reasoning over long contexts has become a key requirement for deploying large language models (LLMs) in realistic applications. Although recent LLMs support increasingly long context windows, they often fail to use relevant evidence that is already present in the input, revealing a gap between context access and effective context utilization. In this work, we propose Recursive Evidence Replay as LLM Harness for Long-Context Reasoning (RECONTEXT), a training-free inference method for improving long-context reasoning. RECONTEXT uses model-internal relevance signals to construct a query-conditioned evidence pool and replays it before final generation while preserving the full original context. This recursive selection process separates evidence organization from answer generation without training, external memory, or context pruning. We also provide a theoretical analysis based on associative memory, which characterizes the context as a memory store, the question as a retrieval cue, attention as cue-trace association, and replay as trace reactivation. Experiments on eight long-context datasets with 128K context length show that RECONTEXT consistently improves evidence utilization across Qwen3-4B, Qwen3-8B, and Llama3-8B, achieving the best average rank on all three backbones. Code is available at https://github.com/Yanjun-Zhao/ReContext.

</details>


### [85] [Online Safety Monitoring for LLMs](https://arxiv.org/abs/2607.02510)
*Mona Schirmer,Metod Jazbec,Alexander Timans,Christian Naesseth,Maja Waldron,Eric Nalisnick*

Main category: cs.AI

TL;DR: A simple threshold-based real-time monitor using external verifier signals with risk-controlled calibration effectively detects unsafe LLM outputs, performing competitively with more complex sequential testing methods.


<details>
  <summary>Details</summary>
Motivation: Despite alignment training, Large Language Models (LLMs) still generate unsafe outputs at deployment time, creating a critical need for real-time monitoring to detect and raise alarms when safety cannot be assured.

Method: The paper proposes a simple real-time monitor that converts verifier signals from an external model into alarm decisions through thresholding, with thresholds calibrated using risk control techniques.

Result: In experiments on mathematical reasoning and red teaming datasets, this simple threshold-based monitor design performs competitively with more advanced monitors based on sequential hypothesis testing.

Conclusion: A simple threshold-based real-time monitor with risk-controlled calibration provides an effective and competitive approach for detecting unsafe LLM outputs during deployment, offering a practical alternative to more complex monitoring methods.

Abstract: Despite alignment training, LLMs remain prone to generating unsafe outputs at deployment time. Monitoring outputs online and raising an alarm when safety can no longer be assumed is therefore critical. We study a simple real-time monitor that turns a verifier signal from an external model into an alarm decision by thresholding, with the threshold calibrated via risk control. In experiments on mathematical reasoning and red teaming datasets, we show that this simple design is competitive with more advanced monitors based on sequential hypothesis testing.

</details>


### [86] [Distributed Attacks in Persistent-State AI Control](https://arxiv.org/abs/2607.02514)
*Josh Hills,Ida Caspary,Asa Cooper Stickland*

Main category: cs.AI

TL;DR: AI coding agents can perform stealth attacks by distributing malicious code across multiple pull requests over time, evading current monitoring approaches.


<details>
  <summary>Details</summary>
Motivation: As AI coding agents become more autonomous and persistent across sessions, they create a new attack surface where misaligned agents can distribute attacks across time and pull requests for optimal concealment.

Method: Introduces Iterative VibeCoding benchmark with CLI tools and Flask web services across 20 tasks. Uses Claude Sonnet 4.5 as attack agent and GPT-4o as monitor. Compares gradual (distributed) vs non-gradual (single-PR) attacks. Tests across multiple model backends and introduces stateful link-tracker monitor that tracks suspicious buildup across PRs.

Result: No single monitor is robust to both gradual and non-gradual attacks. Gradual attacks achieve high evasion (≥65%) across different model backends, and the evasion gap widens with more capable models. The stateful link-tracker monitor detects gradual attacks substantially better than diff monitors. Combining monitors in a four-monitor ensemble reduces gradual-attack evasion from 93% to 47%.

Conclusion: Persistent-state AI coding agents create significant security challenges that require new monitoring approaches beyond current single-PR detection methods. A multi-monitor ensemble with cross-PR tracking is necessary to detect distributed attacks effectively.

Abstract: As AI coding agents become more autonomous, they increasingly ship code iteratively, with the codebase persisting across sessions. This persistence creates a new attack surface: a misaligned or prompt-injected agent can distribute attacks across pull requests (PRs) and time its payload for the PR with the best natural cover. To study the resulting dynamics, we introduce Iterative VibeCoding, a setting for AI control, the study of safely deploying capable but potentially untrusted AI. In Iterative VibeCoding, a coding agent builds software over a sequence of PRs in a persistent codebase while pursuing a covert side task. Our benchmark includes two task families: CLI tools and Flask web services, across 20 total task variations. We use Claude Sonnet 4.5 as the attack agent and GPT-4o as the monitor. We compare gradual attacks, which distribute the side task across PRs, against non-gradual attacks concentrated in a single PR. No single monitor is robust to both: which strategy evades best (success while evading the monitor) depends on the monitor type, so a defender cannot close off both gradual and non-gradual attacks with any one monitor. High evasion (>= 65%) generalizes across model attack agent backends (Sonnet 4.5, Gemini 3.1 Pro, Kimi K2.5), confirming this is a property of the persistent-state attack surface rather than a single model's capability. Evasion also remains high across state-of-the-art monitor models and the gap between gradual and non-gradual evasion widens for more capable models. We introduce a stateful link-tracker monitor that tracks suspicious buildup across PRs. On both task families, it detects gradual attacks substantially better than diff monitors that merely see more accumulated history. Combining this stronger monitor with trajectory monitors in a four-monitor ensemble reduces gradual-attack evasion from 93% under the weakest standard diff monitor to 47%.

</details>


<div id='cs.LG'></div>

# cs.LG [[Back]](#toc)

### [87] [Multilayer Q-Matrix-Embedded Neural Network for Cognitive Diagnosis (M-QCDNet): Structure-Aware Deep Learning Architecture for Psychometric Interpretability](https://arxiv.org/abs/2607.01278)
*Yiyao Yang*

Main category: cs.LG

TL;DR: M-QCDNet is a neural network that integrates Q-matrix structure from cognitive diagnostic models to create interpretable skill mastery predictions while maintaining neural network flexibility.


<details>
  <summary>Details</summary>
Motivation: The paper addresses the need to bridge the gap between traditional cognitive diagnostic models (which are interpretable but limited) and deep neural networks (which are powerful but black-box). There's a need for interpretable AI that maintains both psychometric transparency and neural network flexibility for cognitive diagnostics.

Method: The authors propose a multilayer Q-matrix-embedded neural network (M-QCDNet) that uses the Q-matrix as a structural prior to define item-skill relationships. They introduce a loss function with L2 penalty to penalize skills not aligned with the Q-matrix, balancing predictive performance and structural alignment. They also develop interpretable alignment-based evaluation metrics.

Result: M-QCDNet produces interpretable latent mastery profiles consistent with cognitive theory while leveraging neural network capabilities. The model enables early detection of learning difficulties and supports mastery-based interventions in classroom settings.

Conclusion: M-QCDNet successfully bridges psychometric transparency with neural flexibility, advancing interpretable, fair, and actionable AI for cognitive diagnostics by embedding diagnostic validity directly into the model design.

Abstract: The research proposes a multilayer Q-matrix-embedded neural network for cognitive diagnosis (M-QCDNet), which integrates the structural interpretability of cognitive diagnostic models (CDMs) with the deep learning neural network (NN). M-QCDNet structures the item-skill relationship using the Q-matrix as a structural prior, ensuring latent mastery profiles remain interpretable and consistent with cognitive theory, followed by the proposed loss function with an L2 penalty to penalize skills not aligned with the Q-matrix and to balance predictive performance and structural alignment. Corresponding evaluation matrices, the interpretable alignment-based metrics that quantify the degree to which predicted skill activations correspond to item-level skills, were further developed. M-QCDNet offers practical benefits for classroom practice, enabling early detection of learning difficulties and supporting mastery-based interventions. By embedding diagnostic validity into model design, M-QCDNet bridges psychometric transparency and neural flexibility, advancing interpretable, fair, and actionable AI for cognitive diagnostics.

</details>


### [88] [I\textsuperscript{2}RiMA: Spectral Riemannian Representation with Temporal Attention for Mental Stress Detection based on EEG Signals](https://arxiv.org/abs/2607.01279)
*Cheng He,Kunyu Peng,Shangen Han,Jinming Ma,Jinhong Ding,Likun Xia*

Main category: cs.LG

TL;DR: IRMANet is a novel EEG stress detection model that combines Riemannian manifold processing with attention mechanisms to capture subject-dependent, frequency-specific neural patterns while maintaining temporal coherence across EEG sequences.


<details>
  <summary>Details</summary>
Motivation: Current cross-subject EEG stress detection faces challenges because stress-related patterns are both subject-dependent and frequency-specific. Traditional Riemannian methods focus on spatial covariance in time domain but overlook critical neural oscillations, while standard temporal tokenization fragments inter-slice temporal coherence needed for effective stress decoding.

Method: IRMANet (Intra-Inter Riemannian Manifold Attention Network) constructs spatial covariance matrices at each frequency point and maps them to the SPD tangent space to preserve channel geometry with frequency-specific cues. It uses frequency cluster aggregation to select informative spectral components aligned with EEG rhythms, and employs an intra-inter slice attention module to integrate local slice-level spectral dynamics with global temporal context across EEG sequences.

Result: The model achieves state-of-the-art performance across three datasets, with up to 82.78% balanced accuracy while remaining computationally efficient with only 1.60M parameters and 31.95M FLOPs, consistently outperforming five existing baselines.

Conclusion: IRMANet effectively addresses the limitations of existing EEG stress detection methods by combining Riemannian manifold processing with attention mechanisms, successfully capturing both frequency-specific discriminative patterns and temporal coherence for improved cross-subject stress classification.

Abstract: Cross-subject EEG stress detection remains challenging because discriminative stress-related patterns are both subject-dependent and frequency-specific. Conventional Riemannian methods model spatial covariance mainly in the time domain, overlooking neural oscillations that are critical for high-level cognitive state decoding, while standard temporal tokenization often fragments inter-slice temporal coherence. To address these limitations, we propose \method{}, an Intra-Inter Riemannian Manifold Attention Network for EEG-based stress detection. \method{} constructs spatial covariance matrices independently at each frequency point and maps them to the SPD tangent space, preserving channel-wise geometry together with frequency-specific discriminative cues. It further introduces frequency cluster aggregation to select informative spectral components and reduce redundancy by forming compact, data-driven frequency clusters aligned with EEG rhythms. Finally, an intra-inter slice attention module adaptively integrates local slice-level spectral dynamics and global temporal context across EEG sequences. Experiments on three datasets show that \method{} consistently outperforms five state-of-the-art baselines, achieving up to 82.78\% balanced accuracy while remaining efficient with only 1.60M parameters and 31.95M FLOPs.

</details>


### [89] [Fixed-Set Robustness in Programming by Example: Example Corruption and Semantic Partition Recovery](https://arxiv.org/abs/2607.01280)
*Yuan Si,Jialu Zhang*

Main category: cs.LG

TL;DR: This paper studies adversarial robustness in programming-by-example systems, showing that worst-case example corruption can flip synthesis results even when random noise doesn't, and proposes version-space partition aggregation as a defense that works only when semantic partitions maintain vote margins.


<details>
  <summary>Details</summary>
Motivation: Current PBE systems model wrong examples as stochastic noise, but this paper examines a different failure mode: adversarial corruption where an attacker strategically chooses examples to damage the returned program. There's a gap in understanding adversarial robustness in low-margin PBE tasks.

Method: The authors formalize fixed-set worst-case corruption for finite PBE version spaces, implement exact-within-bounded-pool and heuristic corruption searches for a string-transformation DSL, and introduce version-space partition aggregation (VPA) - a defense that synthesizes on disjoint example groups and votes by semantic signatures.

Result: Adversarial corruption dramatically flips results: one curated edit flips all 8 spike tasks while random noise methods succeed much more often (10.3%-16.7%). VPA helps only when clean semantics maintain partition vote margins, which often fails on realistic tasks. Adaptive attackers can drive VPA accuracy to zero on public SyGuS tasks when vote margin is near one.

Conclusion: Low-margin PBE tasks have an adversarial robustness dimension that standard random-typo and noisy-PBE evaluations miss. Semantic partition aggregation (VPA) provides limited defense that only works when partition vote margins persist. The findings establish important boundaries for PBE robustness research.

Abstract: Programming-by-example systems infer programs from a small set of input-output examples. Robust PBE work usually models wrong examples as samples from a stochastic noise process and then minimizes an expected or empirical loss. This paper studies a different failure mode: an adversary who sees the synthesizer and chooses the examples whose corruption most damages the returned program. We formalize fixed-set worst-case corruption for finite PBE version spaces, implement exact-within-bounded-pool and heuristic corruption searches for a string-transformation DSL, and introduce version-space partition aggregation (VPA), a defense that synthesizes on disjoint example groups and votes by semantic signatures. The central claim is deliberately bounded and partly negative: low-margin PBE tasks have an adversarial robustness dimension that random-typo and noisy-PBE evaluations miss, while semantic partition aggregation helps only when the clean semantics keep a partition vote margin, which often fails on realistic tasks. Evidence from curated/generated DSL tasks, accepted public SyGuS PBE_SLIA slices, SYNTRA Playgol v2, and noisy-PBE objective baselines supports that boundary. One curated edit flips all 8 spike tasks while 200-trial typo, DSL-pool, and distance-matched random controls succeed on 10.3%, 11.0%, and 16.7%; generated margin-1 rows flip under budget 1 yet VPA recovers them; on public SyGuS the vote margin is near one, so an adaptive attacker drives VPA accuracy to zero; accepted public SyGuS slices move across exact-within-pool budget boundaries; and Playgol shows positive paired-bootstrap gaps against typo and same-pool random controls on the 141 accepted rows. A small exact-output prompt harness over 20 controlled margin-1 tasks shows the same qualitative clean-to-attacked pattern across local and API models, while it is treated as a scope check, not a broad LLM benchmark.

</details>


### [90] [Domain Knowledge Based Temporal-Spatial Graph Convolution Network for ECG Recognition](https://arxiv.org/abs/2607.01282)
*Wenting Ma,Zhipeng Zhang,Xiaohang Yuan,Ningwei Xie,Yuxin Xie,Xiaolin Wang,Meng Guo,Xingang Chai,Zhenjie Yao*

Main category: cs.LG

TL;DR: A novel domain knowledge-based graph convolution network for ECG recognition that incorporates PRQST landmarks and uses double-stream directed graphs for spatial and temporal modeling, achieving superior performance on rare ECG categories.


<details>
  <summary>Details</summary>
Motivation: Despite AI advances, interpretability challenges persist in specialized domains like healthcare (ECG recognition). Current end-to-end convolutional neural networks lack domain knowledge integration, which is crucial for reliable ECG interpretation.

Method: Proposes a domain knowledge-based graph convolution network incorporating PRQST landmarks as domain knowledge. Uses double-stream directed graphs: spatial directed graphs for positional relationships among key points, and temporal directed graphs for dependencies between adjacent ECG cycles in extended sequences.

Result: Achieved 88.1% overall average F1 score and 76.3% average F1 score for rare categories on the First Chinese ECG Intelligent Competition dataset (9 ECG categories), outperforming state-of-the-art models. Domain knowledge significantly enhanced detection performance, especially for rare categories.

Conclusion: The proposed domain knowledge-based graph convolution network effectively improves ECG recognition performance, particularly for rare categories, demonstrating that incorporating medical domain knowledge enhances AI interpretability and performance in healthcare applications.

Abstract: In light of strides in Arti cial Intelligence (AI) and its wide spread application, challenges persist in the interpretability of AI models, particularly within specialized domains like healthcare, such as electro cardiograph (ECG) recognition. Rather than relying solely on end-to-end convolutional neural networks, this paper introduces a novel approach using a domain knowledge-based graph convolution network for ECG recognition. Key landmarks points of PRQST, vital to ECG interpreta tion, are incorporated as domain knowledge. The double-stream directed graph is employed to model both intra and inter ECG cycles. Speci cally, spatial directed graphs capture the positional relationships among key points, while temporal directed graphs delineate temporal dependencies between adjacent cycles in extended ECG sequences. Experimental re sults on the First Chinese ECG Intelligent Competition dataset, which speci cally classify ECG into nine categories, prove the e cacy of the proposed model. The overall average F1 score is 88.1%, the average F1 score of rare categories is 76.3%, both outperform the state-of-the-art models. The introduction of domain knowledge did enhance the detec tion performance, especially for rare categories.

</details>


### [91] [Scaling Laws for Grid-Based Approximate Nearest Neighbor Search in High Dimensions](https://arxiv.org/abs/2607.01283)
*Matthew J Liu,Wei Hang Zheng,Vidhan Purohit,Siqi Xie,Chieh-En Li,Jerry Li,Noah Flynn*

Main category: cs.LG

TL;DR: A systematic study of multiprobe grid algorithms for approximate nearest neighbor search reveals they maintain constant dimensional scaling while other methods degrade at high dimensions, offering advantages in indexing cost and dimensional robustness.


<details>
  <summary>Details</summary>
Motivation: Grid-based approaches for approximate nearest neighbor search have been missing from modern scaling analyses, despite their potential advantages. Recent work formalizing self-attention as ANN operations also motivates understanding ANN scaling properties for efficient transformer architectures.

Method: Systematic characterization of a multiprobe grid algorithm with respect to dataset size (N) and dimensionality (d), with experiments focusing on the GloVe embedding family to analyze scaling behavior.

Result: Multiprobe grid search maintains approximately constant dimensional scaling exponent while graph-, tree-, and partitioning-based methods show degrading throughput. It achieves near-linear query scaling in N with lower indexing cost than competing ANN methods. A previously unreported d-scaling crossover is identified on GloVe embeddings.

Conclusion: Grid-based methods like multiprobe grid may be competitive in rebuild-heavy or high-dimensional settings where indexing cost and dimensional robustness are critical. Understanding ANN algorithm scaling properties could inform cost analysis of efficient transformer architectures.

Abstract: Grid-based approaches to approximate nearest neighbor (ANN) search have been absent from modern scaling analyses. We present a systematic characterization of a multiprobe grid algorithm with respect to dataset size $N$ and dimensionality $d$. Our experiments reveal a previously unreported $d$-scaling crossover on the GloVe embedding family, in which multiprobe grid search maintains an approximately constant dimensional scaling exponent while other graph-, tree-, and partitioning-based methods exhibit degrading throughput. The advantage comes with near-linear query scaling in $N$, but also with lower indexing cost than competing ANN methods. Our results suggest that grid-based methods such as multiprobe grid may be competitive in rebuild-heavy or high-dimensional settings where indexing cost and dimensional robustness dictate performance. More broadly, recent work has formalized self-attention as an ANN operation. Thus, the $N$- and $d$-scaling properties of ANN algorithms may guide cost analysis of efficient transformer architectures. Code is available at: https://github.com/weiz345/MultiProbeANN.

</details>


### [92] [IonSense-QKG: A Quantum-Readiness Metadata Framework for Lithium-Ion Battery Dataset Discovery](https://arxiv.org/abs/2607.01286)
*Sakthi Prabhu Gunasekar,Prasanna Kumar Rangarajan*

Main category: cs.LG

TL;DR: IonSense-QKG is a quantum-readiness metadata framework that assesses public lithium-ion battery datasets for suitability in hybrid quantum-classical machine learning workflows.


<details>
  <summary>Details</summary>
Motivation: Public battery datasets vary widely in characteristics (chemistry, modality, scale, etc.), making it difficult to determine which datasets are suitable for near-term hybrid quantum-classical machine learning applications in battery research.

Method: Proposes IonSense-QKG framework that enriches existing battery dataset records with quantum-relevant metadata including task type, sensing modality, chemistry, label availability, sequence type, preprocessing requirements, quantum encodings, estimated qubit range, and NISQ feasibility. Introduces a transparent Quantum Readiness Score to rank datasets.

Result: The framework enables query-based discovery to identify datasets suitable for quantum workflows and includes released artifacts with metadata tables, scoring scripts, robustness checks, link-checking utilities, and SQL-style query examples.

Conclusion: IonSense-QKG positions dataset selection as a data-management problem and provides a reproducible foundation for data-centric quantum battery analytics, establishing a framework for identifying battery datasets most suitable for quantum computing applications.

Abstract: Public lithium-ion battery datasets are increasingly used for state-of-health estimation, remaining-useful-life prediction, anomaly detection, electrochemical diagnostics, second-life analytics, and battery safety research. However, these datasets vary substantially in chemistry, modality, scale, label quality, sequence structure, access status, and preprocessing complexity. These differences directly affect whether a dataset is feasible for near-term hybrid quantum-classical machine-learning workflows.
  This paper presents IonSense-QKG, a quantum-readiness metadata framework for lithium-ion battery dataset discovery. Starting from the EV-Battery-IonSense index, the proposed framework enriches public battery dataset records with quantum-relevant metadata, including task type, sensing modality, chemistry, label availability, sequence type, preprocessing requirements, candidate quantum encodings, estimated qubit range, and NISQ feasibility. A transparent Quantum Readiness Score is introduced to rank datasets as candidate resources for future hybrid quantum-classical battery benchmarks. The score is intended as a dataset-selection heuristic, not as evidence of quantum advantage.
  The framework demonstrates query-based discovery over enriched metadata to identify datasets suitable for compact quantum feature maps, quantum time-series workflows, limited-label anomaly detection, and future battery-health benchmarking. The released artifact includes metadata tables, scoring scripts, robustness checks, link-checking utilities, and SQL-style query examples. IonSense-QKG positions dataset selection as a data-management problem and provides a reproducible foundation for data-centric quantum battery analytics.

</details>


### [93] [A Novel Machine Learning Approach for Central Nervous System Tumor Classification from DNA Methylation](https://arxiv.org/abs/2607.01307)
*Paulo R. Ferreira,Lucas Coutinho Freitas,Laís dos Santos Gonçalves,William Borges Domingues,Lucas Petitemberte de Souza,Mariana B. Michalowski,Vinicius F. Campos*

Main category: cs.LG

TL;DR: A novel machine learning approach combining Sparse Random Projection with multinomial logistic regression improves DNA methylation-based CNS tumor classification, achieving 96% cross-validation accuracy and outperforming state-of-the-art methods by 4-5 percentage points on independent clinical data.


<details>
  <summary>Details</summary>
Motivation: DNA methylation profiling is powerful for CNS tumor classification but faces challenges in cross-cohort transferability, methodological correctness, and robust multiclass evaluation. There's a need for more methodologically rigorous approaches that can improve classification reliability in clinical settings.

Method: Proposes a novel machine learning approach that combines Sparse Random Projection for dimensionality reduction with multinomial logistic regression for classification. The method is evaluated in the same experimental setting as a widely used reference classifier, with testing on both a reference cohort (2,801 samples) and independent clinical cohort (1,104 samples).

Result: Achieves 96% mean accuracy under stratified 3-fold cross-validation on the reference cohort. On the independent clinical cohort, reaches 86% accuracy at the 91-class level and 93% at the methylation class family level, improving upon state-of-the-art reference figures of 82% and 88% respectively (4-5 percentage point gains).

Conclusion: The proposed model, grounded in stronger methodological practice in machine learning, consistently outperforms previous state-of-the-art across evaluation settings and can materially improve the reliability of CNS tumor classification, with clinically relevant implications for cancer subtype assignment and treatment selection.

Abstract: NA methylation profiling has become a powerful approach for central nervous system (CNS) tumor classification, yet important challenges remain regarding cross-cohort transferability, methodological correctness, and robust multiclass evaluation. In this work, we propose a novel and methodologically rigorous machine-learning approach for methylation-based CNS tumor classification that combines Sparse Random Projection for dimensionality reduction with multinomial logistic regression for classification. We evaluate the proposed approach in the same general experimental setting established by a widely used reference classifier. On the 2,801-sample reference cohort, our method achieves a mean accuracy of 96\% under stratified 3-fold cross-validation. On the independent 1,104-sample clinical evaluation cohort, it reaches 86\% accuracy at the 91-class level and 93\% when predictions are evaluated at the methylation class family level. These results improve upon the corresponding state-of-the-art reference figures of 82\% class-level concordance and 88\% family-level concordance, yielding absolute gains of approximately 4 and 5 percentage points, respectively. This improvement is clinically relevant: in a diagnostic setting, a 5-point increase in correct tumor classification can directly affect cancer subtype assignment and, in turn, influence treatment selection and downstream clinical decision-making. Our results show that the proposed model, grounded in stronger methodological practice in machine learning, consistently outperforms the previous state of the art across evaluation settings and can materially improve the reliability of CNS tumor classification.

</details>


### [94] [From Approximation to Emergence: A Theory of Deep Learning](https://arxiv.org/abs/2607.01311)
*Zhilin Zhao*

Main category: cs.LG

TL;DR: A comprehensive monograph that organizes modern deep learning theory into a unified, proof-oriented narrative, connecting classical foundations to contemporary mechanisms and emphasizing the emergence of learned behaviors through scale, data, architecture, and training.


<details>
  <summary>Details</summary>
Motivation: Deep learning has grown beyond any single mathematical explanation, creating a fragmented theoretical landscape. The motivation is to unify diverse theoretical results into a coherent research narrative that bridges classical foundations with modern mechanisms and phenomena.

Method: The book adopts a proof-oriented approach that examines each theory through three lenses: the object it controls, the assumptions that make it valid, and the phenomena it leaves unexplained. It organizes a broad literature systematically rather than presenting isolated results.

Result: Produces a rigorous map of deep learning theory as it stands today - powerful yet incomplete, with increasing focus on understanding how learned mechanisms emerge from scale, data, architecture, and training processes.

Conclusion: Deep learning theory requires a unified perspective that connects approximation, optimization, and generalization foundations with contemporary phenomena like emergence, scaling laws, and in-context learning, ultimately centering on understanding how learning mechanisms arise from scale and architectural choices.

Abstract: Deep learning has outgrown any single mathematical explanation. From Approximation to Emergence develops a unified, proof-oriented account of modern deep learning theory, tracing a path from the classical foundations of approximation, optimization, and generalization to the contemporary mechanisms of overparameterization, robustness, generative modeling, transformers, in-context learning, scaling laws, interpretability, alignment, and emergence. Rather than presenting isolated results, the book organizes a broad literature into a coherent research narrative: each theory is examined through the object it controls, the assumptions that make it valid, and the phenomena it leaves unexplained. Written for researchers, graduate students, and mathematically trained practitioners, this monograph offers a rigorous map of deep learning theory as it stands today: powerful, incomplete, and increasingly centered on the question of how learned mechanisms arise from scale, data, architecture, and training.

</details>


### [95] [Black-Box Inference of LLM Architectural Properties with Restrictive API Access](https://arxiv.org/abs/2607.01313)
*Christopher Ellis,Shreyas Chaudhari,Mei-Yu Wang,Leighton Barnes,Giulia Fanti,José M. F. Moura*

Main category: cs.LG

TL;DR: NightVision is an attack that uses restrictive black-box LLM APIs to recover architectural parameters (hidden dimension, depth, parameter count) even when only single logit probabilities are exposed, demonstrating current API restrictions are insufficient for full obfuscation.


<details>
  <summary>Details</summary>
Motivation: Commercial LLM providers have restricted APIs to only expose single logit probabilities and removed logit bias functions after prior work showed architectural details could be recovered. However, it's unknown whether these current restrictive APIs actually prevent architectural parameter recovery.

Method: NightVision uses 1) common set prompting (multiple prompts exposing log probabilities for the same output token set) with spectral analysis to infer hidden dimension, and 2) end-to-end time to first token (TTFT) measurements combined with estimated hidden dimension to estimate depth and parameter count.

Result: Evaluated on 32 open-source LLMs: recovered hidden dimension within 23% average relative error (9% on MoE models), depth and parameter count within 53% error for models >3B parameters. Extensive ablations show how accuracy scales with token budget and model properties.

Conclusion: Current LLM API restrictions (single logit exposure only) are insufficient to fully obfuscate underlying model architectural details. Architectural parameters remain recoverable even under restrictive black-box access.

Abstract: In practice, most commercial LLM providers do not publicly release details of underlying LLM architectures. However, prior work has shown that given limited API access to an LLM (namely, top-$k$ logits and/or a logit bias function), one can recover certain architectural details of an LLM, such as the hidden dimension of the feed-forward network. Perhaps in response to these results, most commercial LLM providers have restricted their APIs to expose only the single logit for each decoded token, and they no longer give users the ability to bias logits. We show that even under current restrictive APIs, several architectural parameters are still recoverable. We present NightVision, an attack that uses restrictive black-box API access to estimate the hidden dimension, depth, and parameter count of an LLM. Algorithmically, NightVision relies on a novel common set prompting technique in which multiple prompts expose log probabilities for the same set of output tokens; a spectral analysis of these results is used to infer hidden dimension. NightVision additionally uses end-to-end time to first token (TTFT) measurements and the estimated hidden dimension to estimate depth and parameter count. We empirically evaluate NightVision on 32 open-source LLMs, recovering hidden dimension to within 23% average relative error across all models (9% on MoE models), and depth and parameter count to within 53% for models exceeding three billion parameters. We run extensive ablations to demonstrate how these accuracies scale with token budget and model properties. Overall, our results suggest that current LLM APIs are not sufficiently restricted to fully obfuscate the architectural details of their underlying models.

</details>


### [96] [Multi-modal Rail Crossing Safety Analysis](https://arxiv.org/abs/2607.01365)
*Paimon Goulart,Chansong Lim,Nícolas Roque dos Santos,Yue Dong,Sheldon Peterson,Jia Chen,Evangelos E. Papalexakis*

Main category: cs.LG

TL;DR: A multimodal AI system that combines visual imagery and structured accident data to assess railway crossing safety, achieving high performance in risk classification and FRA safety score prediction.


<details>
  <summary>Details</summary>
Motivation: The paper addresses whether visual cues from railway crossing images can be used to estimate safety levels, and whether incorporating structured accident history data can improve these assessments. The goal is to build an AI system that provides safety evaluations aligned with expert opinion and Federal Railroad Administration standards.

Method: The authors propose a proof-of-concept multimodal pipeline that combines visual data (images of railway crossings) with structured data (official accident reports). The system employs a routed fine-tuned compact Vision-Language Model (VLM) approach to process and integrate these different data modalities for safety assessment.

Result: The system achieves a macro F1 score of 0.757 for identifying HIGH-RISK vs LOW-RISK crossings. It estimates FRA-based safety scores with RMSE of 0.078 and correlation of 0.492. The qualitative results also align with domain-expert assessments, demonstrating the system's practical effectiveness.

Conclusion: The proposed multimodal AI system successfully leverages both visual and structured data to assess railway crossing safety, addressing critical research challenges in data preparation and learning paradigms. The approach shows promising results in producing safety assessments that align with expert opinion and official safety standards.

Abstract: Given one or more images of a railway crossing, can we leverage visual cues that allow us to robustly estimate how safe it is? Can we improve our ability to do so by introducing structured data (such as official accident reports) about the accident history of that crossing into our models? In this work, we explore how to best answer those questions towards building an AI system that can ingest multi-modal data for railway crossings and provide safety assessment and scores that align with expert opinion and with safety scoring used by the Federal Railroad Administration (FRA). To that end, we propose a proof-of-concept pipeline that delivers on that goal, while at the same time exploring and tackling a number of critical research challenges that pertain to different parts of the pipeline, from data preparation to different learning paradigms that can allow us to realize such a system. Indicatively, our proposed system identifies HIGH-RISK and LOW-RISK crossings with a macro F1 score of 0.757 and estimates FRA-based safety scores with an RMSE of 0.078 and correlation of 0.492 using a routed fine-tuned compact VLM pipeline, while producing qualitative results that align with domain-expert assessment.

</details>


### [97] [How Should Transformers Encode Numeric Values in Electronic Health Records?](https://arxiv.org/abs/2607.01391)
*Maria Elkjær Montgomery,Christian Igel,Mikkel Odgaard,Martin Sillesen,Mads Nielsen*

Main category: cs.LG

TL;DR: Hybrid token-based encoding (numeric values with binning before projection) outperforms discrete and continuous approaches for EHR data, balancing numeric precision with robustness.


<details>
  <summary>Details</summary>
Motivation: To understand how to best encode numeric values in transformer models for EHR data, comparing different encoding strategies to balance numeric precision, optimization stability, and architectural flexibility.

Method: Systematic comparison of discrete, continuous, and hybrid value encoding strategies using synthetic arithmetic tasks embedded within real EHR data and real-world clinical prediction tasks.

Result: Hybrid token-based approaches with binning before projection provide the most robust and broadly applicable solution, with optimal number of bins following a power-law relationship with dataset size. Models exhibit reliable "good enough" numeric computation rather than exact arithmetic.

Conclusion: In practice, robustness and deployability often outweigh maximal numeric precision, making hybrid token-based approaches the practical default for EHR data processing with transformers.

Abstract: How do we encode numeric values in transformer-based sequence processing, particularly in electronic health record (EHR) data? We systematically compare discrete, continuous, and hybrid value encoding strategies using synthetic arithmetic tasks embedded within real-world EHR data, as well as real-world clinical prediction tasks. Our study reveals trade-offs between numeric precision, optimisation stability, and architectural flexibility. We find that approaches that explicitly model value-concept interactions perform best on precision-sensitive arithmetic tasks when architectural constraints permit. Hybrid token-based approaches that retain numeric values but apply binning prior to projection provide a more robust and broadly applicable alternative, with the optimal number of bins following a simple empirically derived power-law in dataset size. Across tasks, models consistently exhibit reliable "good enough" numeric computation rather than exact arithmetic, while clinical gains from incorporating laboratory values are task-dependent. This suggests that robustness and deployability often outweigh maximal numeric precision in practice, motivating hybrid token-based approaches as a practical default.

</details>


### [98] [NeuroBridge: Bridging Multi-Task MRI Knowledge for Neurodegenerative Disease Diagnosis](https://arxiv.org/abs/2607.01401)
*Mengyu Li,Guoyao Shen,Chad W. Farris,Xin Zhang*

Main category: cs.LG

TL;DR: NeuroBridge is a multi-task MRI framework that combines self-supervised pretraining with clinical tasks for improved Alzheimer's disease and dementia diagnosis, achieving state-of-the-art performance across multiple datasets.


<details>
  <summary>Details</summary>
Motivation: Accurate MRI-based identification of Alzheimer's disease (AD), mild cognitive impairment (MCI), and related dementias is challenging due to subtle and heterogeneous structural changes in the brain. Conventional single-task approaches may not capture the full spectrum of disease-related features needed for accurate diagnosis.

Method: NeuroBridge integrates large-scale self-supervised MRI pretraining with multiple clinical tasks including hippocampal segmentation, hippocampal atrophy classification, and reconstruction objectives. This is followed by gated fusion fine-tuning. The framework was evaluated across ADNI and OASIS cohorts with cross-cohort transfer, probability-based analysis, and opportunistic screening experiments.

Result: NeuroBridge achieved the highest performance across all evaluated classification tasks, reaching 88.17% accuracy for AD versus cognitively normal controls in ADNI and 82.78% in OASIS. The largest performance gains occurred in MCI-related and mixed-diagnosis settings. The framework demonstrated strong cross-cohort generalization, systematic associations between predicted-class probability and accuracy, and feasibility of probability-based opportunistic screening.

Conclusion: Clinically guided multi-task representation learning significantly improves neurodegenerative MRI diagnosis compared to conventional single-task approaches. NeuroBridge provides a robust and scalable framework for dementia assessment and MRI-based opportunistic screening in clinical settings.

Abstract: INTRODUCTION: Accurate MRI-based identification of Alzheimer's disease (AD), mild cognitive impairment (MCI), and related dementias remains challenging because disease-related structural changes are often subtle and heterogeneous. We developed NeuroBridge, a clinically guided multi-task MRI framework for neurodegenerative disease diagnosis. METHODS: NeuroBridge integrates large-scale self-supervised MRI pretraining with hippocampal segmentation, hippocampal atrophy classification, and reconstruction objectives, followed by gated fusion fine-tuning. Performance was evaluated across ADNI and OASIS cohorts, including cross-cohort transfer, probability-based analysis, and opportunistic screening. RESULTS: NeuroBridge achieved the highest performance across evaluated classification tasks, reaching 88.17% accuracy for AD versus cognitively normal controls in ADNI and 82.78% in OASIS. The largest gains occurred in MCI-related and mixed-diagnosis settings. The framework demonstrated strong cross-cohort generalization, systematic associations between predicted-class probability and accuracy, and the feasibility of probability-based opportunistic screening. DISCUSSION: Clinically guided multi-task representation learning improves neurodegenerative MRI diagnosis beyond conventional single-task approaches. NeuroBridge provides a robust and scalable framework for dementia assessment and MRI-based opportunistic screening.

</details>


### [99] [Spin-Weighted Spherical Harmonics Enable Complete and Scalable $\mathrm{E}(3)$-Equivariant Networks](https://arxiv.org/abs/2607.01408)
*Chenxing Liang,Yuchao Lin,Andrii Kryvenko,Wendi Yu,Chuan Li,Jianwen Xie,Xiaofeng Qian,Shuiwang Ji*

Main category: cs.LG

TL;DR: SpinGTP is an E(3)-equivariant neural network method that overcomes the expressivity limitations of Gaunt Tensor Product by using Spin-Weighted Spherical Harmonics, recovering missing antisymmetric interactions while maintaining scalability.


<details>
  <summary>Details</summary>
Motivation: Existing E(3)-equivariant networks face scalability issues due to O(L^6) complexity of Clebsch-Gordan Tensor Product. While Gaunt Tensor Product reduces complexity, it cannot capture antisymmetric paths, limiting expressivity for chiral materials and non-centrosymmetric geometries.

Method: SpinGTP generalizes from scalar functions to Spin-Weighted Spherical Harmonics, leveraging their algebraic properties to recover missing antisymmetric interactions while maintaining GTP's asymptotic efficiency. It creates a more expressive equivariant basis that accounts for parity-odd tensor product components.

Result: SpinGTP achieves accuracies comparable to full CGTP across benchmarks including Tetris, 3BPA, SPICE-MACE-OFF, and OC20. It shows superior performance for chiral materials and non-centrosymmetric geometries due to explicit antisymmetric path capture.

Conclusion: SpinGTP provides a complete, scalable, and mathematically rigorous approach for high-order equivariance in large-scale 3D atomistic simulations, overcoming the expressivity-scalability trade-off in previous methods.

Abstract: $\mathrm{E}(3)$-equivariant networks are promising for 3D atomistic system modeling, yet their scalability is limited by the $O(L^6)$ complexity of the Clebsch-Gordan Tensor Product (CGTP). The recently proposed Gaunt Tensor Product (GTP) reduces the complexity but is unable to capture the antisymmetric paths, resulting in incomplete expressivity. In this work, we present SpinGTP, an approach to overcome the GTP incompleteness by generalizing from scalar functions to Spin-Weighted Spherical Harmonics (SWSH). By relying on the algebraic properties of SWSH, SpinGTP recovers the missing antisymmetric interactions while maintaining the asymptotic efficiency of GTP. It also allows for a more expressive equivariant basis that naturally accounts for the parity-odd components of tensor products. We evaluate SpinGTP across diverse benchmarks, including Tetris, 3BPA, SPICE-MACE-OFF, and OC20. Our results show that SpinGTP achieves accuracies comparable to full CGTP. Notably, by explicitly capturing antisymmetric paths, SpinGTP exhibits superior performance in tasks involving chiral materials and non-centrosymmetric geometries. This work provides a complete, scalable, and mathematically rigorous path toward high-order equivariance in large-scale 3D atomistic system simulations.

</details>


### [100] [The Rollout Infrastructure Tax in Coding-Agent Reinforcement Learning](https://arxiv.org/abs/2607.01415)
*Daniel Thi Graviet,Lovre Pesut,Ivan Dagelic,Vedran Jukic,Ivan Burazin*

Main category: cs.LG

TL;DR: Coding-agent RL systems can achieve significant efficiency gains by optimizing execution infrastructure, not just treating it as deployment plumbing, with up to 110x variation in cold-start latency across different substrates.


<details>
  <summary>Details</summary>
Motivation: Current coding-agent reinforcement learning treats execution infrastructure as an implementation detail despite relying on large numbers of interactive software rollouts, missing opportunities for practical efficiency gains where small per-rollout savings compound at scale.

Method: Comparative study of four execution substrates: single containers, hosted sandboxes, Kubernetes-orchestrated containers, and cloud virtual machines, measuring infrastructure overhead and efficiency metrics.

Result: Found up to 110x variation in cold-start latency and 1.8x spread in projected worker-hours for one million 150-step trajectories, showing significant efficiency differences across execution substrates.

Conclusion: Future coding-agent RL systems should optimize execution substrates as part of the training system itself, not merely as deployment plumbing, to achieve substantial efficiency gains in post-training phases.

Abstract: Coding-agent reinforcement learning treats execution infrastructure as a background implementation detail, despite relying on large numbers of interactive software rollouts. This is a missed opportunity: measuring infrastructure overhead can reveal practical efficiency gains for RL post-training, where small per-rollout savings compound at scale. We present a comparative study of four execution substrates: single containers, hosted sandboxes, Kubernetes-orchestrated containers, and cloud virtual machines. We find up to $110\times$ variation in cold-start latency and a $1.8\times$ spread in projected worker-hours for one million 150-step trajectories. Our results suggest that future coding-agent RL systems should optimize execution substrates as part of the training system itself, not merely as deployment plumbing.

</details>


### [101] [Conditional Inference Trees and Forests for Feature Selection](https://arxiv.org/abs/2607.01417)
*Robert Milletich,Justin Downes,Steve Goley,Newel Hirst*

Main category: cs.LG

TL;DR: CIF is computationally expensive but effective for top-k feature ranking in downstream prediction tasks, ranking highly among classification and regression methods.


<details>
  <summary>Details</summary>
Motivation: Conditional inference trees and forests reduce split-selection bias but can be computationally expensive due to repeated permutation tests and threshold searches. The paper aims to study their effectiveness as top-k feature-ranking methods for downstream prediction.

Method: The study uses real-data benchmarks, runtime ablations, and synthetic feature-recovery experiments. It analyzes CIT and CIF as top-k feature-ranking methods, examining Bonferroni-corrected Monte Carlo permutation p-values for nodewise rejection control under the complete permutation null.

Result: CIF ranks 4th among 17 classification methods on 22 datasets and 3rd among 18 regression methods on 8 datasets. Runtime analysis shows adaptive stopping and threshold search strategies significantly affect computational cost (4.0-8.4× and 1.9-10.8× increases respectively) with minimal downstream score impact (≤0.011).

Conclusion: CIF is supported as an effective top-k feature-ranking method for downstream prediction, though its computational efficiency depends heavily on implementation choices like adaptive stopping and threshold search strategies.

Abstract: Conditional inference trees (CIT) and conditional inference forests (CIF) reduce split-selection bias by testing features before choosing split thresholds, but repeated permutation tests and threshold searches can make these methods computationally expensive. We study CIT and CIF as top-$k$ feature-ranking methods for downstream prediction using real-data benchmarks, runtime ablations, and synthetic feature-recovery experiments. At a fixed node, if the features and permutation budget do not depend on the node responses, Bonferroni-corrected $+1$ Monte Carlo permutation $p$-values control nodewise rejection under the complete permutation null. CIF ranks 4th among 17 classification methods on 22 datasets and 3rd among 18 regression methods on 8 datasets. With Bonferroni correction held fixed, the CIF runtime ablations indicate that adaptive stopping and the number of thresholds searched have the largest measured effect on runtime: turning off adaptive stopping and using exact threshold search increase fitting time by 4.0--8.4$\times$ and 1.9--10.8$\times$, respectively, while downstream score changes are at most 0.011. Sparse high-$p$ simulations indicate that forest feature sampling can leave informative features out of many split decisions. Overall, the results support CIF as a top-$k$ feature-ranking method in the evaluated downstream prediction benchmarks.

</details>


### [102] [On the Utility and Factual Reliability of Pruned Mixture-of-Experts Models in the Biomedical Domain](https://arxiv.org/abs/2607.01444)
*Atsuki Yamaguchi,Szymon Palucha,Léo Bijar,Aline Villavicencio,Nikolaos Aletras*

Main category: cs.LG

TL;DR: Moderate pruning of Mixture-of-Experts models preserves biomedical utility without immediate reliability decline, but extreme pruning increases hallucination risks. Both utility and reliability degrade rapidly in cross-domain settings.


<details>
  <summary>Details</summary>
Motivation: Mixture-of-Experts models have high memory requirements despite selective activation benefits. While structured expert pruning reduces deployment costs, previous research has only evaluated benchmark utility without examining effects on factual reliability, especially in critical domains like biomedicine.

Method: Investigates how domain-specific expert pruning affects utility and reliability by assessing four MoE models, six pruning methods, and multiple pruning ratios across generation and classification tasks in both in-domain (biomedical) and cross-domain settings.

Result: Moderate pruning preserves in-domain utility without immediate reliability decline, but hallucination risks increase at extreme pruning ratios. When shifting to general domain, both utility and reliability degrade rapidly. Safe compression depends heavily on task and domain.

Conclusion: Evaluating pruned MoE models solely on utility is inadequate for high-stakes deployment without reliability assessment. Domain-specific evaluation is crucial for safe compression decisions.

Abstract: Mixture-of-Experts (MoE) models offer inference speedups via selective activation but impose substantial memory requirements because the whole network must remain loaded. Structured expert pruning is a practical approach for reducing deployment costs in resource-constrained settings. However, prior studies primarily evaluate benchmark utility, leaving the effect of pruning on factual reliability underexplored, particularly in high-stakes domains such as biomedicine. In this paper, we investigate how domain-specific expert pruning affects both utility and reliability. We assess four MoE models, six pruning methods, and multiple pruning ratios across generation and classification tasks under in-domain (biomedical) and cross-domain settings. Results reveal that moderate pruning preserves in-domain utility without immediate reliability decline, although hallucination risks increase at extreme pruning ratios. When shifting to the general domain, both utility and reliability degrade rapidly. These findings indicate that safe compression depends heavily on the task and domain. Evaluating pruned MoE models solely on utility is inadequate for high-stakes deployment without reliability assessment.

</details>


### [103] [Geometry-Aware R-Structured Kolmogorov-Arnold Networks](https://arxiv.org/abs/2607.01449)
*Sergei Kucherenko,Nilay Shah*

Main category: cs.LG

TL;DR: GRS-KAN integrates R-functions into KANs to encode geometric constraints analytically, improving accuracy for problems with discontinuities and boundaries while maintaining interpretability.


<details>
  <summary>Details</summary>
Motivation: Standard neural networks struggle with explicitly representing geometric constraints, discontinuities, and implicit boundaries. There's a need for models that can incorporate known geometric or logical constraints analytically while maintaining learning capabilities for smooth structures.

Method: The authors propose GRS-KAN, a hybrid architecture combining KAN branches (for learning smooth nonlinear structures) with differentiable R-functions (for encoding geometric/logical constraints). They implement R-conjunctions and R-disjunctions for differentiable logical operations, and introduce additive, multiplicative, and agnostic branch-weighted variants.

Result: Numerical experiments on regression problems with circular and rectangular supports show GRS-KAN reduces test RMSE by up to 67% compared to standard KANs. The models improve both predictive accuracy and boundary localization, with the agnostic variant automatically determining when geometric priors are beneficial.

Conclusion: GRS-KAN successfully integrates geometric constraints into neural networks through R-functions, offering substantial accuracy improvements while maintaining interpretability via explicit analytical representation of geometric structure.

Abstract: We propose a novel hybrid neural architecture, the Geometry-aware R-Structured Kolmogorov-Arnold Network (GRS-KAN), which integrates V.L.Rvachev's R-functions into the Kolmogorov-Arnold Network (KAN) framework. The proposed approach combines two complementary modeling mechanisms: smooth nonlinear structure is learned by KAN branches, while known geometric or logical constraints are encoded analytically using differentiable R-functions. This enables explicit representation of discontinuities, feasible regions, and implicit geometric boundaries within a trainable neural architecture.
  The framework implements differentiable logical operations through R-conjunctions and R-disjunctions, allowing complex geometric supports to be represented analytically and incorporated directly into regression models. Several GRS-KAN variants are introduced, including additive, multiplicative, and agnostic branch-weighted architectures.
  The method is demonstrated on regression problems involving discontinuities with circular and rectangular supports. Numerical experiments show that explicit geometric encoding substantially improves predictive accuracy and boundary localization compared with standard KANs. In the considered benchmarks, geometry-aware GRS-KAN models reduce test RMSE by up to 67% while simultaneously improving interpretability through explicit analytical representation of the learned geometric structure. The agnostic variant further demonstrates the ability to automatically determine whether geometric priors are beneficial for a given learning task.

</details>


### [104] [Token Geometry](https://arxiv.org/abs/2607.01455)
*Kathan Shah*

Main category: cs.LG

TL;DR: Ember is a lightweight optimizer specifically designed for embedding tables and LM-heads in language models that reduces VRAM usage from O(2VD) to O(V + D), eliminates the need for token table optimizer state sharding, and improves Pareto frontiers across various training phases.


<details>
  <summary>Details</summary>
Motivation: The embedding table and LM-head in language models have distinct gradient geometry compared to dense hidden weights, but current optimizers like Adam require substantial VRAM (O(2VD)) and force sharding of token table optimizer states. There's a need for more efficient optimization that exploits this unique geometry.

Method: The authors introduce Ember, a lightweight optimizer specifically for embedding and LM-head matrices. It reduces VRAM usage to O(V + D) by optimizing the gradient geometry of these interfaces differently from dense weights. The method shows that token optimization follows a simple 1D ray trajectory rather than navigating complex nonconvex landscapes.

Result: Ember effectively scales across batch sizes and parameter counts, improves Pareto frontiers in supervised finetuning, RL, and pretraining while using only kilobytes of optimizer state. It provides empirical evidence that the optimization space for Transformers is surprisingly narrow.

Conclusion: Ember offers a principled, efficient alternative to Adam for embedding/LM-head optimization, challenging conventional views about neural network optimization landscapes. The authors open-source their distributed implementation to support further research at the intersection of optimization and language model training efficiency.

Abstract: Language models learn continuous programs over discrete symbols, with the embedding table and LM-head acting as the read/write interface between them. We show that this interface has gradient geometry distinct from dense hidden weights which can be exploited to improve the Pareto frontier across supervised finetuning, RL, and pretraining, while only utilizing kilobytes of optimizer state. We introduce Ember, a lightweight optimizer for embedding and LM-head matrices that utilizes O(V + D) VRAM, instead of Adam's O(2VD), and forgoes the need to shard both token table optimizer states. We provide empirical evidence that Ember scales effectively across batch size and parameter count. We show that the optimization trajectory of tokens can be well described by a simple 1D ray, counter to the popular belief that neural net parameters navigate a heavily nonconvex landscape. We provide a principled view on the surprisingly narrow space of optimizers that suffice for Transformer training. Finally, we open-source our distributed Ember implementation that merges cleanly with existing ZeRO/FSDP setups to support further research at https://github.com/katop1234/ember

</details>


### [105] [Class-Grouped Normalized Momentum and Faster Hyperparameter Exploration to Tackle Class Imbalance in Federated Learning](https://arxiv.org/abs/2607.01474)
*Haemin Park,Diego Klabjan,Martin W. Braun,Xiuqi Li,Balakrishnan Ananthanarayanan*

Main category: cs.LG

TL;DR: FedCGNM addresses class imbalance in federated learning using client-side optimizer with class grouping and normalized momentum, plus FedHOO for rate optimization in small-client scenarios.


<details>
  <summary>Details</summary>
Motivation: Class imbalance is a critical challenge in federated learning where underrepresented classes suffer poor performance. Standard centralized techniques can't address this due to privacy and heterogeneity constraints in FL settings.

Method: Proposes FedCGNM: client-side optimizer that partitions classes into groups based on minimum within-group variance, maintains momentum per group, normalizes each group momentum to unit length, and uses sum of normalized group momentums as update direction. Also introduces FedHOO, an X-armed-bandit algorithm for efficient rate optimization in small-client regimes.

Result: FedCGNM consistently outperforms baselines on four public long-tailed benchmarks and a proprietary chip-defect dataset. FedHOO yields further gains in small-scale federations. Theoretical convergence analysis provided accounting for time-varying resampling-rates.

Conclusion: The proposed FedCGNM with FedHOO provides an effective solution to class imbalance in federated learning, addressing both gradient magnitude equalization and noise reduction for rare classes while maintaining privacy and handling heterogeneity.

Abstract: Class imbalance poses a critical challenge in federated learning (FL), where underrepresented classes suffer from poor predictive performance yet cannot be addressed by standard centralized techniques due to privacy and heterogeneity constraints. We propose FedCGNM (Federated Class-Grouped Normalized Momentum), a client-side optimizer in FL that partitions classes into a small number of groups based on minimum within-group variance, maintains a momentum per group, normalizes each group momentum to unit length, and uses the summation of the normalized group momentums as an update direction. This design both equalizes gradient magnitude across majority and minority groups and mitigates the noise inherent in rare-class gradients. We further provide a theoretical convergence analysis explicitly accounting for time-varying resampling-rates. Additionally, to efficiently optimize these rates in small-client regimes, we introduce FedHOO, an X-armed-bandit (XAB) based algorithm that exploits federated parallelism that evaluates many combinations of two candidate rates per client at linear cost. Empirical evaluation on four public long-tailed benchmarks and a proprietary chip-defect dataset demonstrates that FedCGNM consistently outperforms baselines, with FedHOO yielding further gains in small-scale federations.

</details>


### [106] [How to Allocate Your Tokens? Scaling Laws with Training Steps and Batch Size](https://arxiv.org/abs/2607.01487)
*Fabian Schaipp*

Main category: cs.LG

TL;DR: Proposes a three-term scaling law that separates model size, training steps, and batch size, enabling accurate prediction of optimal batch sizes with fewer training runs.


<details>
  <summary>Details</summary>
Motivation: Existing scaling laws don't explicitly separate training steps and batch size, making it difficult to predict optimal batch sizes efficiently with limited training runs.

Method: Developed a three-term scaling law that separates model size, training steps, and batch size variables. Fitted this law on extensive training runs and validated it against empirical data.

Result: The three-term law correctly recovers optimal batch size scaling, requires significantly fewer training runs for robust fitting, can predict scaling for suboptimal batch sizes, and matches previous findings on critical batch size.

Conclusion: The proposed three-term scaling law provides a more efficient and accurate framework for predicting training dynamics and optimal batch sizes, advancing the understanding of neural network scaling properties.

Abstract: We propose a scaling law that takes into account model size and training data while explicitly splitting the latter into training steps and batch size (called three-term law). Fitting the proposed law on a large set of training runs, we find that it correctly recovers the scaling of the optimal batch size. Moreover, because it makes use of training runs with suboptimal batch size, our proposed law can be robustly fit with a significantly smaller amount of training runs. We further show that the three-term law can be used to derive scaling laws for suboptimal batch sizes, and that it matches previous empirical findings related to the critical batch size.

</details>


### [107] [Don't Let Gains FADE: Breaking Down Policy Gradient Weights in RL](https://arxiv.org/abs/2607.01490)
*Juliette Decugis,Sean O'Brien,Francis Bach,Gabriel Synnaeve,Taco Cohen*

Main category: cs.LG

TL;DR: FADE (Focal Advantage with Dynamic Entropy) is a self-adapting advantage function for RL post-training of LLMs that automatically adjusts gradient weights based on training dynamics, achieving better stability, diversity, and earlier convergence than static baselines.


<details>
  <summary>Details</summary>
Motivation: Reinforcement learning post-training improves LLM reasoning but suffers from training instability and diversity collapse. While advantage functions offer a solution, there's confusion about which advantage to use and when due to a proliferation of methods.

Method: The authors propose a unifying framework that decomposes advantages into positive and negative gradient mass along two orthogonal axes: sign (affecting entropy and weight geometry) and difficulty (affecting signal sharpness vs sample size). They introduce FADE, which automatically schedules gradient weights based on training dynamics - balancing exploration vs exploitation phases.

Result: FADE reaches peak pass@1 20k steps earlier than best static baseline at 7B scale and 2k steps earlier at 32B scale, while achieving the best accuracy-diversity trade-off across all pass@k on LiveCodeBench and AIME benchmarks.

Conclusion: The paper presents a principled framework for understanding advantage functions in RL post-training and demonstrates that FADE's self-adapting advantage scheduling leads to superior training stability, diversity preservation, and faster convergence compared to static methods.

Abstract: Reinforcement learning post-training dramatically improves LLM reasoning, but suffers from training instability and diversity collapse. Advantage functions offer an appealing fix: they reshape the training objective, reweight which rollouts drive learning, and are trivial to implement. Yet a proliferation of methods makes it unclear which advantage to use and when. We cut through the confusion with a unifying framework that decomposes any advantage into its positive and negative gradient mass along two orthogonal axes. On the sign axis, imbalanced updates collapse either entropy or weight geometry. On the difficulty axis, hard-problem focus sharpens signal but costs sample size. Both trade-offs shift during training: exploration favors balance and hard focus; exploitation favors suppression and medium focus. This motivates FADE (Focal Advantage with Dynamic Entropy), a self-adapting advantage that reads training dynamics to schedule the gradient weight automatically. FADE reaches peak pass@1 20k steps earlier than the best static baseline at the 7B scale and 2k steps earlier at the 32B , while achieving the best accuracy-diversity trade-off across all pass@k on LiveCodeBench and AIME.

</details>


### [108] [Unveiling the Non-Monotonic Effect of Privacy on Generalization under Byzantine Robustness](https://arxiv.org/abs/2607.01492)
*Thomas Boudou,Batiste Le Bars,Nirupam Gupta,Aurélien Bellet*

Main category: cs.LG

TL;DR: The paper shows that the trilemma between Byzantine robustness, local differential privacy, and generalization error depends on privacy noise level: high-noise privacy reduces generalization error, while low-noise privacy degrades it.


<details>
  <summary>Details</summary>
Motivation: Previous work established a fundamental trilemma between Byzantine robustness, local differential privacy (LDP), and optimization error in distributed learning. The authors investigate whether this trilemma extends to generalization error, revealing surprising non-monotonic behavior.

Method: Theoretical analysis with matching lower and upper bounds on algorithmic stability of Byzantine-robust distributed learning under LDP constraints, supported by empirical evaluations.

Result: The tension between Byzantine robustness and privacy depends on privacy regime: high-noise privacy reduces generalization error (no tension), while low-noise privacy increases generalization error (tension exists). The generalization error shows non-monotonic behavior with privacy level.

Conclusion: The trilemma between Byzantine robustness, LDP, and generalization error is not universal but privacy-regime dependent, with privacy enhancing generalization in high-noise regimes and degrading it in low-noise regimes, explained by algorithmic stability bounds.

Abstract: Recent work has established a fundamental trilemma between Byzantine robustness, local differential privacy (LDP), and optimization error in distributed learning. We show that this trilemma does not universally extend to generalization error, but instead depends critically on the privacy regime. Specifically, in the high-noise regime (strong privacy), we prove that increasing privacy reduces the generalization error, i.e., there is no tension between robustness and privacy. In the low-noise regime (weaker privacy), however, the tension between robustness and privacy reappears and increasing privacy indeed degrades generalization. Our theory explains this surprising non-monotonic behavior of the generalization error via matching lower and upper bounds on the algorithmic stability of Byzantine-robust distributed learning under LDP constraints. We corroborate and further analyze these theoretical findings with empirical evaluations.

</details>


### [109] [Towards Learning Representations of Policies in Two-Player Zero-Sum Imperfect-Information Games](https://arxiv.org/abs/2607.01498)
*Kevin Wang,Kevin Yang,Arjun Prakash,Amy Greenwald*

Main category: cs.LG

TL;DR: This paper explores learning policy embeddings in two-player zero-sum imperfect-information games through self-supervised learning, with contributions in dataset creation, embedding methods, and evaluation tasks, demonstrated on poker games.


<details>
  <summary>Details</summary>
Motivation: The research addresses the need for effective policy representations (embeddings) in complex imperfect-information games like poker, where understanding opponent strategies and behavioral patterns is crucial for AI systems.

Method: The paper introduces three components: 1) methods for creating policy datasets, 2) techniques for learning policy embeddings, and 3) downstream tasks to evaluate representations. These self-supervised methods are tested on Kuhn and Leduc Poker.

Result: Despite using basic methods, the research shows that useful behavioral representations exist in the learned embeddings, and the work represents one of the first systematic comparisons of self-supervised learning for policy representations in games.

Conclusion: The paper successfully demonstrates that self-supervised learning can capture meaningful policy representations in imperfect-information games, providing a foundational framework for future research with publicly available code for extension.

Abstract: We investigate the problem of learning useful policy representations (embeddings) in two-player zero-sum imperfect-information games. We make three contributions: First, we introduce methods of creating datasets of policies for a given game. Second, we propose methods to learn policy representations. Third, we introduce downstream tasks to evaluate the effectiveness of such representations.
  We evaluate each dataset method, embedding method, and downstream task on Kuhn and Leduc Poker. Although our methods are very basic, we demonstrate that useful behavioral representations are present in the learned embeddings. To our knowledge, this work is among the first to systematically compare self-supervised learning techniques for learning policy representations in games. Our code is available at https://github.com/VitamintK/ssl-project for others to extend.

</details>


### [110] [The risk of KV cache compression](https://arxiv.org/abs/2607.01520)
*Lukas Haverbeck,Carmen Amo Alonso,Andres Felipe Posada-Moreno,Sebastian Trimpe,Marco Pavone*

Main category: cs.LG

TL;DR: The paper provides a theoretical framework and principled approach for KV cache compression in Transformers, characterizing when accurate compression is possible and offering minimax-optimal algorithms.


<details>
  <summary>Details</summary>
Motivation: Transformer inference on long sequences is expensive due to softmax attention requiring repeated reads from large KV caches. Current KV cache compression methods are empirically-driven without systematic theoretical guidance, and existing theory shows compression can be impossible in worst cases but doesn't provide practical design principles.

Method: The authors characterize the minimax risk of KV cache compression in terms of the intrinsic compressibility of a cache. They develop novel design principles for KV cache compression under causal masking that efficiently map to prefill and autoregressive decoding while achieving minimax-optimal risk.

Result: The theoretical framework reveals when and how accurate KV cache compression is possible. The principles are instantiated in a practical algorithm that shows promising performance on LongBench in targeted experiments.

Conclusion: The research provides a principled avenue for practical KV cache compression with theoretical guarantees, bridging the gap between theoretical impossibility results and empirical compression methods.

Abstract: Transformer inference on long sequences is expensive because softmax attention repeatedly reads from a large KV cache. The prevalent approach to this bottleneck is KV cache compression, which replaces the full cache with a compact summary. Despite its practical importance, the design of such summaries is largely driven by empirical experimentation. On the theoretical side, existing results show that KV cache compression can be impossible in the worst case, but offer little systematic guidance for designing algorithms in regimes where accurate compression is possible. We bridge this gap by characterizing the minimax risk of KV cache compression in terms of the intrinsic compressibility of a cache, revealing when and how accurate compression is possible. These results yield novel design principles for KV cache compression under causal masking that map efficiently to prefill and autoregressive decoding while achieving minimax-optimal risk. We instantiate these principles in a practical algorithm and report promising performance on LongBench in targeted experiments. Overall, our results provide a principled avenue for practical KV cache compression with theoretical guarantees.

</details>


### [111] [Multi-Head Recurrent Memory Agents](https://arxiv.org/abs/2607.01523)
*Jiatong Li,Samuel Yeh,Sharon Li*

Main category: cs.LG

TL;DR: MHM-LRU improves recurrent memory agents by partitioning memory into multiple heads with a select-then-update strategy, dramatically boosting retention and accuracy for long contexts up to 1M tokens.


<details>
  <summary>Details</summary>
Motivation: Recurrent memory agents suffer from systematic performance degradation as context length grows due to poor memory retention. Existing designs use monolithic text blocks where updates risk overwriting previously retained content.

Method: Propose Multi-Head Recurrent Memory (MHM) with a stage-wise select-then-update strategy that partitions memory into independent heads. Introduces MHM-LRU (Least-Recently-Updated) as a lightweight instantiation that guarantees uniform head utilization with zero additional token overhead.

Result: MHM-LRU substantially improves both retention and end-to-end accuracy across 100K-1M token range. On RULER-HQA at 896K tokens, memory retention rate improves from less than 30% to 73.96%. Gains generalize across model families, scales, and task types.

Conclusion: Architectural optimization through MHM-LRU provides a practical, training-free, and cost-efficient path toward reliable long-context recurrent memory by shifting the burden of retention from model behavior to architectural design.

Abstract: Recurrent memory agents extend LLMs to arbitrarily long contexts by iteratively consolidating input into a fixed-size memory window. Despite their scalability, these agents exhibit a well-documented reliability problem: end-to-end performance degrades systematically as context length grows. We diagnose this failure by decomposing performance into two factors--memory capture and memory retention--and quantitatively confirm that retention is the dominant bottleneck. Retention collapses because existing designs maintain memory as a monolithic text block, forcing every update to risk overwriting previously retained content. Motivated by this diagnosis, we propose Multi-Head Recurrent Memory (MHM), a general, training-free framework that partitions memory into independent heads governed by a stage-wise select-then-update strategy. At each step, exactly one head is selected for update while the remaining heads are structurally shielded from overwriting, shifting the burden of retention from model behavior to architectural design. As a lightweight instantiation, we introduce Least-Recently-Updated MHM (MHM-LRU), which guarantees uniform head utilization with zero additional token overhead. Extensive experiments on long-context benchmarks show that MHM-LRU substantially improves both retention and end-to-end accuracy across the 100K--1M token range, where baselines degrade sharply. On RULER-HQA at 896K tokens, MHM-LRU improves the memory retention rate from less than 30% to 73.96%. These gains generalize across model families, scales, and task types, positioning architectural optimization as a practical and cost-efficient path toward reliable long-context recurrent memory.

</details>


### [112] [Wind-Aware Reinforcement Learning Control of a Small Quadrotor Using Learned Onboard Wind Estimation in Simulated Atmospheric Turbulence](https://arxiv.org/abs/2607.01528)
*Abdullah Al Tasim,Wei Sun*

Main category: cs.LG

TL;DR: A two-stage learning pipeline for small drones combines an attention-augmented wind estimator with RL flight control to improve trajectory tracking in turbulent winds.


<details>
  <summary>Details</summary>
Motivation: Small multirotor aircraft operating in atmospheric boundary layers face degraded trajectory tracking due to turbulent winds comparable to their airspeed, which can defeat conventional feedback control.

Method: Two-stage learning pipeline: 1) Wind estimator using attention-augmented gated recurrent network trained on simulated flights through von Karman turbulence, 2) Reinforcement learning (proximal policy optimization) flight controller that receives wind estimates from the frozen estimator.

Result: Wind estimator achieves 0.40 m/s RMSE and 3.2° direction error on unseen regimes; RL controller reduces horizontal trajectory tracking error by 48% relative to wind-blind baseline, winning 100% of evaluation episodes. Controller degrades gracefully on out-of-distribution winds where baseline fails catastrophically.

Conclusion: The proposed learning-based approach significantly improves drone control in turbulent winds by effectively estimating wind conditions and exploiting that information for better trajectory tracking, with the wind-perception benefit increasing with wind speed.

Abstract: Small multirotor aircraft are increasingly tasked with operations in the atmospheric boundary layer, where turbulent winds comparable to the vehicle's airspeed degrade trajectory tracking and can defeat conventional feedback control. This work illustrates a two-stage learning pipeline that first estimates the local wind from onboard kinematics and dynamics and then exploits that estimate inside a reinforcement learning (RL) flight controller. The wind estimator, an attention-augmented gated recurrent network trained on thousands of simulated flights through von Karman turbulence with power-law shear and veer, recovers the horizontal wind vector with a per-flight root-mean-square error of 0.40 m/s and a direction error of 3.2 degrees on unseen wind regimes, an accuracy near the floor imposed by unresolved turbulence, and generalizes to vertical ascent profiles with a skill score of 0.861 over a constant-wind reference. A proximal policy optimization controller receiving the frozen estimator's output reduces horizontal trajectory tracking error by 48% relative to a wind-blind proportional-derivative baseline across mean winds of 4 m/s to 12 m/s, winning on 100% of evaluation episodes. A three-way ablation decomposes this improvement into a kinematic component, available without wind information, and a wind-perception component; the perception share rises with wind speed, from small in light winds toward roughly half the total benefit in strong winds, consistent with the quadratic scaling of aerodynamic drag. The controller degrades gracefully on out-of-distribution winds of 13 m/s to 15 m/s, where the baseline fails catastrophically.

</details>


### [113] [Certified World Models as Sensing Clocks: Drift-Aware Deadlines for Active Perception](https://arxiv.org/abs/2607.01537)
*Hongbo Wang*

Main category: cs.LG

TL;DR: The paper introduces a certified sensing clock for autonomous agents that determines optimal timing for re-sensing based on world model prediction validity, using drift-aware deployment methods to maintain operational guarantees.


<details>
  <summary>Details</summary>
Motivation: Autonomous agents need to balance between sensing (costly) and coasting on world model predictions (risk of prediction invalidity). Current approaches lack certified guarantees for how long agents can safely coast before needing to re-sense, creating operational uncertainty.

Method: 1) Start with an audited equivariant world model. 2) Derive deadlines for no-sensing intervals by transforming prediction validity horizons into sensing clocks. 3) Introduce drift-aware deployment that accounts for on-manifold Lyapunov rates and calibrated native rollout-drift envelopes. 4) Test on 3D VN-JEPA models and synthetic theorem-bed benchmarks with cue-conditioned environments.

Result: The sensing clock controls certificate violations across seeds and data shards. In theorem-bed benchmarks, it remains valid on deployment distribution and reduces eventful-tail violations by 62% compared to exact-mixture expected-belief scheduling at matched sensing budget. However, in short-horizon regimes, empirical conformal horizons match performance, and spectral clocks don't show clear advantage over non-spectral alternatives.

Conclusion: The main contribution is a certified sensing-clock primitive with drift-aware deployment method, not a claim that spectral clocks dominate all alternatives. The approach provides operational guarantees for autonomous agent sensing schedules but has limitations in certain regimes where simpler methods may suffice.

Abstract: Certified world models estimate how long their predictions remain valid. We turn this validity horizon into an operational sensing clock: a rule for when an agent should stop coasting and re-sense. Starting from an audited equivariant world model, we derive a deadline for no-sensing intervals and show that deployable deadlines in learned world models must be drift-aware: on-manifold Lyapunov rates alone overestimate coasting validity, while calibrated native rollout-drift envelopes carry the deployed guarantee. On a frozen 3D VN-JEPA model, the resulting clock controls held-out interval-simultaneous certificate violation across seeds and data shards. In a cue-conditioned theorem-bed (a synthetic bench where all schedulers share the exact model, isolating the scheduling rule), the clock remains valid on the deployment distribution and substantially reduces eventful-tail violations relative to exact-mixture expected-belief scheduling at matched sensing budget. We also report limits: in the short-horizon frozen VN-JEPA regime, empirical conformal horizons match the deployed clock on validity and budget, and a partial-reset exploration finds no clean budget-matched advantage for the spectral term. Thus the contribution is a certified sensing-clock primitive and drift-aware deployment method, not a claim that spectral clocks empirically dominate all non-spectral schedulers.

</details>


### [114] [Evolutionary Feature Engineering for Structured Data](https://arxiv.org/abs/2607.01548)
*Ege Onur Taga,Yilin Zhuang,M. Emrullah Ildiz,Petros Mol,Abhimanyu Das,Karthik Duraisamy,Samet Oymak*

Main category: cs.LG

TL;DR: EFE is a framework using LLM-based evolution to automatically discover preprocessing transformations for structured data, improving both accuracy and interpretability in time-series forecasting and tabular prediction tasks.


<details>
  <summary>Details</summary>
Motivation: Large language models are increasingly used for evolutionary optimization in search operations. The paper addresses the need for automated preprocessing transformation discovery for structured data that can integrate into existing ML pipelines while improving model performance and preserving interpretability.

Method: Evolutionary Feature Engineering (EFE) framework uses LLM-based evolution to generate and refine Python programs representing preprocessing transformations with standardized fit/transform interfaces. Candidate programs are evolved using dataset context, summary statistics, and downstream performance feedback on validation sets.

Result: EFE-Time (for time-series) learned invertible normalizations improving off-the-shelf time-series foundation models by 3%+ averaged across datasets (up to 19% on COVID-Deaths). EFE-Tab (for tabular data) evolved compact feature programs that improved/matched existing LLM-based methods, particularly effective for classical decision trees with preserved interpretability.

Conclusion: EFE demonstrates that LLM-based evolution can effectively improve both accuracy and interpretability when automatically handling structured data preprocessing, with successful applications in both time-series forecasting and tabular prediction domains.

Abstract: Large language models are increasingly used as open-ended search operators in evolutionary optimization. We introduce Evolutionary Feature Engineering (EFE), a framework for using LLM-based evolution to discover preprocessing transformations for structured data. EFE represents transformations as Python programs with a standardized fit/transform interface, allowing them to be inserted directly into existing machine learning pipelines. During evolution, candidate programs are refined using dataset context, summary statistics, and downstream performance feedback on validation set. We instantiate EFE in two settings. For time-series forecasting, EFE-Time learns invertible, dataset-specific normalizations that improve off-the-shelf time-series foundation models. It reduces forecasting errors (MASE, WQL, MAE) 3% or more when averaged across datasets and improvements are as much as 19% on the COVID-Deaths dataset. Notably, these improvements occur with recent TSFMs such as Chronos-2. For tabular prediction, EFE-Tab evolves compact feature programs that add useful interpretable features and remove redundant ones, improving or matching existing LLM-based feature-engineering methods. We found EFE-Tab to be particularly effective on classical decision trees, where small sets of evolved features yield competitive accuracy while preserving interpretability. Overall, EFE demonstrates that LLM-based evolution can improve both accuracy and interpretability when automatically tackling structured data.

</details>


### [115] [X-LogSMask: Expand Transformer for Graph-Structured Data](https://arxiv.org/abs/2607.01553)
*Leyan Li,Rennong Yang,Zhenxing Zhang,Liping Hu*

Main category: cs.LG

TL;DR: X-LogSMask introduces an explainable logarithmic structural mask that injects graph topology directly into Transformer attention, enabling effective graph learning without architecture changes.


<details>
  <summary>Details</summary>
Motivation: Standard Transformers have all-to-all self-attention that poorly matches graph data's sparse, structured, and multi-scale nature. Existing Graph Transformers add complexity and limited interpretability through structural encodings, hybrid modules, or learned attention constraints.

Method: X-LogSMask is an explainable multi-head logarithmic structural mask that injects symmetrically normalized graph topology directly into attention logits. The logarithmic transform converts connectivity into topology-aware gating, suppressing unsupported interactions while preserving feature-dependent attention. Different powers of normalized adjacency matrices are assigned to different attention heads, giving each head a defined structural radius and enabling multi-hop propagation within a single layer.

Result: Transformers with X-LogSMask achieve state-of-the-art performance on 13 out of 20 node-, edge-, and graph-level benchmarks, and remain competitive even in lightweight one-layer configurations.

Conclusion: Simple, interpretable structural masks can make self-attention an effective graph-learning operator without changing the Transformer architecture, providing a topology-constrained alternative to unrestricted self-attention.

Abstract: Transformers have become general-purpose architectures, but their all-to-all self-attention is poorly matched to graph data, whose interactions are sparse, structured and multi-scale. Existing Graph Transformers address this mismatch through structural encodings, hybrid message-passing modules or learned attention constraints, often introducing additional complexity and limited interpretability. Here we introduce X-LogSMask, an explainable multi-head logarithmic structural mask that injects symmetrically normalized graph topology directly into attention logits. The logarithmic transform converts structural connectivity into a topology-aware gating signal, suppressing unsupported node interactions while preserving feature-dependent attention. By assigning different powers of the normalized adjacency matrix to different attention heads, X-LogSMask gives each head a defined structural radius and supports multi-hop information propagation within a single layer. We further show that a standard Transformer encoder can be interpreted as one-step message passing on a complete graph, motivating X-LogSMask as a topology-constrained alternative to unrestricted self-attention. Across 20 node-, edge- and graph-level benchmarks, Transformers equipped with X-LogSMask achieve state-of-the-art performance on 13 datasets and remain competitive in a lightweight one-layer configuration. These results show that simple, interpretable structural masks can make self-attention an effective graph-learning operator without changing the Transformer architecture. The code is available at https://github.com/LiLeyan-0120/X-LogSMask.

</details>


### [116] [Geometric Signatures of Reasoning: A Spectral Perspective on Task Hardness](https://arxiv.org/abs/2607.01571)
*Aria Masoomi,Mahsa Bazzaz,Adel Javanmard,Vahab Mirrokni*

Main category: cs.LG

TL;DR: The paper studies the geometric properties of chain-of-thought reasoning trajectories in LLM hidden state space, showing how trajectory complexity (effective dimension) correlates with task difficulty and how kinematic features can predict solution correctness early.


<details>
  <summary>Details</summary>
Motivation: While much research focuses on the length and content of chain-of-thought reasoning chains, little is known about their internal geometry in transformer hidden state spaces. Understanding this geometry could provide insights into task difficulty and enable early prediction of solution correctness.

Method: Formalizes CoT reasoning chains as discrete curves in R^d hidden state space and characterizes them through spectral, positional, and kinematic geometric functionals. Introduces effective dimension d_ρ as a measure of trajectory complexity. Uses mathematical reasoning problems from MATH500 dataset for experiments.

Result: Effective dimension d_ρ achieves 0.93 AUC in distinguishing easy from hard problems. Kinematic features can potentially predict correctness from only the first 20% of generated tokens. The geometric signatures transfer across questions of varying difficulty.

Conclusion: The shape of a model's internal reasoning trajectory provides a principled window into both task hardness and solution quality, enabling early prediction of correctness and potentially informing future early-stopping strategies.

Abstract: Chain-of-thought (CoT) reasoning enables large language models (LLMs) to solve complex problems by generating intermediate reasoning steps. While much attention has been paid to the length and content of these reasoning chains, far less is known about their internal geometry. We study the \emph{geometry} of CoT trajectories in the hidden state space of transformer models, formalizing each reasoning chain as a discrete curve in $\mathbb{R}^d$ and characterizing it through spectral, positional, and kinematic geometric functionals. We introduce the effective dimension $d_ρ$ as a measure of trajectory complexity and show theoretically that trajectories with flatter eigenvalue spectra correspond to harder tasks, as they explore more of the hidden dimensions. Lastly, we explore how kinematic features of the trajectory, mean position, positional dispersion, initial and current hidden states, mean velocity, mean speed, and speed dispersion, can be used to predict solution correctness before generation is complete, and may inform future early-stopping strategies. Experimentally, on mathematical reasoning problems from the MATH500 dataset, $d_ρ$ achieves $0.93$ AUC in distinguishing easy from hard problems, while kinematic features potentially can predict correctness from only the first $20\%$ of generated tokens. These correctness signatures transfer across questions of varying difficulty, establishing that the shape of a model's internal reasoning trajectory is a principled window into both task hardness and solution quality.

</details>


### [117] [BOUNDARY_SYNC: Measuring Communication-Induced Representational Coupling in Multi-Agent LLM Systems](https://arxiv.org/abs/2607.01600)
*Zewen Liu*

Main category: cs.LG

TL;DR: LLM agents communicating with each other causes their outputs to converge (homogenize), with text communication reducing output diversity by about 20% and group size influencing whether outputs homogenize or diversify.


<details>
  <summary>Details</summary>
Motivation: As large language models are increasingly deployed as communicating agents, it's crucial to understand whether inter-agent communication causes outputs to converge or diversify, which has implications for multi-agent system design and potential echo chambers.

Method: Introduced BOUNDARY_SYNC protocol with Coupling Amplification Factor (CAF) metric comparing conditional vs baseline Jensen-Shannon Divergence. Conducted controlled GPT-4o experiments (N=30, ~9,900 API calls) measuring coupling in text and image communication, including ablation studies, prompt perturbation controls, and cross-model replication.

Result: Text communication causes significant homogenization (CAF=0.803), image communication also homogenizes (CAF=0.834), group size moderates coupling direction (K=5 homogenizes while K=3 diversifies), cross-model replication shows extreme variation (CAF 0.034-0.803), and coupling is stateless/driven by prompt context rather than cumulative updating.

Conclusion: LLM agent coupling is real, measurable, and controllable at the prompt level, with direct implications for multi-agent system design - communication typically homogenizes outputs but can diversify depending on group size and model characteristics.

Abstract: As large language models (LLMs) are deployed as communicating agents, does inter-agent communication cause outputs to converge? We introduce BOUNDARY_SYNC, a protocol measuring representational coupling via the Coupling Amplification Factor (CAF = JSD_cond / JSD_baseline), where CAF < 1 indicates homogenization and CAF > 1 indicates diversification. In controlled GPT-4o experiments (N=30, ~9,900 API calls), we measure coupling in text and image communication. Key findings: (1) text communication causes significant homogenization (CAF=0.803 [0.740, 0.873], d=1.30, p<0.001), confirmed by no-communication ablation and prompt-perturbation controls; (2) image communication also homogenizes under within-modality baselines (CAF=0.834 [0.811, 0.858]), with comparable proportional effect; (3) group size moderates coupling direction -- K=5 produces homogenization while K=3 yields CAF > 1.0 (point estimates 1.14 and 1.06, CI pending), suggesting a directional shift toward diversification; (4) cross-model replication shows extreme variation (CAF 0.034-0.803), with DeepSeek dominated by format artifacts; (5) coupling is stateless -- driven by prompt context rather than cumulative updating, with continuous consensus producing monotonic convergence. These results establish LLM agent coupling as real, measurable, and controllable at the prompt level, with direct implications for multi-agent system design.

</details>


### [118] [SINA: A Fully Automated Circuit Schematic Image to Netlist Generator Using Artificial Intelligence](https://arxiv.org/abs/2607.01609)
*Saoud Aldowaish,Yashwanth Karumanchi,Kai-Chen Chiang,Mohammed Ayman Habib,Finn Murphy,Rishen Cao,Morteza Fayazi*

Main category: cs.LG

TL;DR: SINA is an open-source circuit schematic image-to-netlist generator that uses deep learning, OCR, and vision-language models to automate conversion of both IC and PCB schematics with 96.67% accuracy, 2.72x better than state-of-the-art methods.


<details>
  <summary>Details</summary>
Motivation: Existing AI/LLM applications in analog/mixed-signal EDA are limited by lack of machine-readable representations of circuit knowledge. Circuit schematic images in research papers, textbooks, and websites contain validated designs but cannot be directly processed by EDA tools. Current conversion methods lack generalization across IC/PCB schematics, struggle with component recognition and connectivity inference, and fail to distinguish connected junctions from crossing wires.

Method: SINA is a fully automated pipeline integrating: 1) deep learning for robust component detection, 2) connected-component labeling for accurate connectivity inference, 3) Optical Character Recognition (OCR) for component reference designator extraction, and 4) Vision-Language Model (VLM) for reliable reference designator assignment. It handles both IC- and PCB-level schematics with dedicated crossing-wires detection to differentiate wire intersections from connections. Netlist correctness is validated using graph isomorphism techniques.

Result: The system achieves an overall netlist generation accuracy of 96.67%, which is 2.72x higher than state-of-the-art approaches. The method successfully handles both IC and PCB schematics and accurately distinguishes between connected junctions and crossing wires.

Conclusion: SINA represents a significant advancement in circuit schematic image processing, enabling automated conversion of visual circuit representations into machine-readable netlists. This bridges the gap between vast repositories of visual circuit knowledge and AI/EDA applications, facilitating simulation, verification, and comprehensive database building for AI-based models in analog and mixed-signal domains.

Abstract: Recent advances in Artificial Intelligence (AI) have revolutionized Electronic Design Automation (EDA), particularly through Large Language Models (LLMs) for circuit design tasks. However, their application to analog and mixed-signal domains remains limited by the lack of machine-readable representations of existing circuit design knowledge. Circuit schematic images found in research manuscripts, textbooks, and websites constitute a vast repository of validated designs; however, these visual representations cannot be directly processed by EDA tools. Converting them into machine-readable netlists is essential for enabling simulation, verification, and building comprehensive databases for AI-based models. Current conversion methods lack generalization across both Integrated Circuit (IC) and Printed Circuit Board (PCB) level schematics. Moreover, they struggle with component recognition and connectivity inference, and fail to distinguish between connected junctions and crossing wires. In this paper, we propose SINA, an open-source circuit schematic image-to-netlist generator. SINA is a fully automated pipeline that integrates deep learning for robust component detection, connected-component labeling for accurate connectivity inference, Optical Character Recognition (OCR) for component reference designator extraction, and a Vision-Language Model (VLM) for reliable reference designator assignment. SINA handles both IC- and PCB-level schematics and incorporates dedicated crossing-wires detection to differentiate wire intersections from connections. We validate the correctness of the generated netlists using graph isomorphism techniques. Our experiments demonstrate an overall netlist generation accuracy of 96.67%, which is 2.72x higher compared to state-of-the-art approaches.

</details>


### [119] [MKGR: Multimodal Knowledge-Graph Representation Learning for Cold-Start Protein-Protein Interaction Prediction](https://arxiv.org/abs/2607.01627)
*Wenbo Zhang*

Main category: cs.LG

TL;DR: Multimodal framework combining protein sequence encoding with biomedical knowledge graphs for accurate cold-start PPI prediction, outperforming existing methods.


<details>
  <summary>Details</summary>
Motivation: Accurate PPI prediction is crucial for functional genomics, disease discovery, and drug development. Existing models struggle with cold-start scenarios where proteins have no observed PPI edges during training, losing useful context when relying only on network topology.

Method: Proposed \method framework integrates region-aware protein sequence encoding with four protein-centered biomedical knowledge graphs (protein-drug, protein-disease, protein-miRNA, protein-lncRNA). Uses sequence branch for contextual representations from structurally informed regions, graph attention encoders for modality-specific embeddings, bridge reconstruction objective for graph learning regularization, and pair-level gating module to adaptively integrate sequence and graph evidence.

Result: Experiments on two benchmark datasets under novel-old and novel-novel cold-start settings show \method consistently outperforms competitive sequence, network, and knowledge-graph baselines across ACC, F1, AUC, AUPR, and MCC metrics.

Conclusion: The multimodal approach effectively addresses cold-start PPI prediction challenges by combining sequence and diverse biomedical knowledge graph information, demonstrating superior performance over existing methods in challenging prediction scenarios.

Abstract: Accurate protein-protein interaction (PPI) prediction is central to functional genomics, disease mechanism discovery, and drug development. A difficult setting arises when candidate interactions include proteins that have no observed PPI edges during training, where models relying on network topology alone often lose useful context. This paper presents \method, a multimodal representation framework for cold-start PPI prediction. \method\ combines region-aware protein sequence encoding with four protein-centered biomedical knowledge graphs, including protein-drug, protein-disease, protein-miRNA, and protein-lncRNA associations. The sequence branch extracts contextual representations from structurally informed sequence regions, while graph attention encoders learn modality-specific protein embeddings from sparse biomedical associations. A bridge reconstruction objective regularizes graph learning by recovering shared protein-entity associations, and a pair-level gating module adaptively integrates sequence and graph evidence for each candidate protein pair. Experiments on two benchmark datasets under novel-old and novel-novel cold-start settings show that \method\ consistently outperforms competitive sequence, network, and knowledge-graph baselines across ACC, F1, AUC, AUPR, and MCC.

</details>


### [120] [DeadPool: Resilient LLM Training with Hot-Swapping via Zero-Overhead Checkpoint](https://arxiv.org/abs/2607.01646)
*Haotian Xie,Junlin Chen,Mingkai Zheng,Lishan Yang,Zhao Zhang*

Main category: cs.LG

TL;DR: DeadPool is a fault-tolerance system for LLM training that enables zero-overhead hot-swapping of failed compute nodes using in-memory checkpointing and communicator reconstruction, achieving 40-second recovery times without any performance penalty during failure-free execution.


<details>
  <summary>Details</summary>
Motivation: Current LLM training requires massive GPU resources (tens of thousands of GPUs for months) and suffers from frequent failures. Existing fault-tolerance solutions force a tradeoff: either impose significant overhead during normal operation OR have slow recovery times when failures occur, especially with permanent node failures.

Method: DeadPool introduces two key innovations: 1) Off-critical-path in-memory checkpointing for spatial redundancy that overlaps with computation, and 2) A communicator reconstruction protocol that can replace failed nodes with spare nodes at runtime without terminating the entire training job.

Result: The system was evaluated on scales up to 512 NVIDIA A100 GPUs and LLMs up to 65B parameters. DeadPool achieved zero checkpoint overhead during normal execution, with hot-swapping recovery completing in under 40 seconds when node failures occurred.

Conclusion: DeadPool successfully breaks the traditional tradeoff in fault-tolerance systems, simultaneously achieving both zero-overhead during failure-free execution and extremely low recovery costs, making it particularly valuable for large-scale LLM training where both performance and reliability are critical.

Abstract: State-of-the-art large language model (LLM) training takes tens of thousands of graphics processing units (GPUs) for months and encounters failures across the software and hardware stack. Existing fault-tolerance mechanisms either impose non-trivial overhead during failure-free execution or suffer from prolonged recovery latency, particularly under scenarios where a small subset of compute nodes experience permanent failures. %The tradeoff between failure-free overhead and recovery latency forms a space forms a Pareto frontier We present DeadPool to simultaneously address both optimization objectives. DeadPool incorporates a fault-tolerance mechanism that restores LLM training via hot-swapping, namely by replacing failed nodes with spare nodes without terminating the complete job. The hot-swapping of DeadPool is enabled by two ideas: First, it exploits an off-critical-path in-memory checkpointing mechanism for spatial redundancy. Second, it introduces a communicator reconstruction protocol that replaces failed nodes with spare nodes at runtime. DeadPool efficiently overlaps the in-memory checkpointing with computation, thus introducing zero overhead during error-free execution. Upon permanent node failures, DeadPool can rebuild memory states with minimal recomputation by leveraging in-memory checkpoints. We evaluate DeadPool across scales (up to 512 NVIDIA A100 GPUs) and LLMs (up to 65B parameters), and observe zero checkpoint overhead with hot-swapping recovery completing in under 40 seconds. These results show that DeadPool simultaneously achieves both zero-overhead error-free execution and extremely low recovery cost.

</details>


### [121] [CALM: Interpretable Cross-Modal Alignment for Biomarker Discovery from Unpaired Data](https://arxiv.org/abs/2607.01656)
*Jueqi Wang,Zachary Jacokes,John Darrell Van Horn,Kevin A. Pelphrey,Michael C. Schatz,Archana Venkataraman*

Main category: cs.LG

TL;DR: CALM is a framework that learns interpretable associations between brain regions (ROIs) and genetic pathways from completely disjoint populations by aligning imaging and genetics data in a shared latent space.


<details>
  <summary>Details</summary>
Motivation: Most large-scale neuropsychiatric datasets are unimodal (either neuroimaging or genetics), making it challenging to study brain structure-gene interactions, which are crucial for understanding neuropsychiatric disorders.

Method: CALM aligns neuroimaging and genetics data in a shared latent space using linear projections that simultaneously match class-conditional latent distributions and ensure group separability. It learns interpretable ROI-pathway associations from completely disjoint populations.

Result: CALM outperforms state-of-the-art methods and ablation baselines when generalizing to unseen paired datasets. It demonstrates stable learned associations and reveals immune and metabolic pathways linked to specific cortical regions in autism spectrum disorder, consistent with established literature.

Conclusion: CALM enables leveraging large unimodal repositories to study cross-modal interactions in brain disorders across disparate datasets, opening new avenues for neuropsychiatric research.

Abstract: The interaction between brain structure and genetic influences is key to understanding neuropsychiatric disorders. However, most large-scale datasets are unimodal, providing either neuroimaging or genetics data. We propose CALM, a framework that learns interpretable associations between brain ROIs and genetic pathways from completely disjoint populations. CALM aligns the two modalities in a shared latent space via linear projections that simultaneously match the class-conditional latent distributions and ensure group separability. These projections provide interpretable pathway--ROI associations. When trained on unimodal imaging and genetics datasets, CALM generalizes to an unseen paired dataset, outperforming several state-of-the-art methods and ablation baselines. We also demonstrate stability of the learned associations against a paired baseline. Our experiments on autism spectrum disorder reveal immune and metabolic pathways linked to specific cortical regions and are consistent with established literature. Thus, CALM opens the door to leveraging large unimodal repositories for studying cross-modal interactions in brain disorders across disparate datasets.

</details>


### [122] [Message Passing Based Two-Timescale Bayesian Learning for Joint Channel and Memory Hardware Impairments Tracking](https://arxiv.org/abs/2607.01660)
*Wei Xu,An Liu*

Main category: cs.LG

TL;DR: Proposes a two-timescale Bayesian deep learning framework using residual recurrent gated units and message passing to jointly track rapidly varying wireless channels and slowly drifting hardware impairments in massive MIMO systems.


<details>
  <summary>Details</summary>
Motivation: Hardware impairments in massive MIMO receivers cause inter-symbol memory and inter-element coupling that severely degrade channel estimation. Traditional approaches struggle to handle the different timescales of channel variations (fast due to small-scale fading) versus hardware impairment drift (slow due to aging and environmental changes).

Method: Uses residual recurrent gated units (RGRU) to model intra-slot memory of impairments. Proposes MP-TTBDL framework with fast-varying Markov prior for sparse channels and slow-varying Gaussian Markov prior for impairment parameters. Develops message-passing algorithm with closed-form updates for inter-slot messages. Partitions intra-slot factor graph into channel tracking module (using Turbo-OAMP) and impairments calibration module (using DAMP), with iterative information exchange via expectation propagation.

Result: Simulation results demonstrate robust achievement of lower channel estimation error compared to conventional compensators followed by channel estimation, across different online impairment scenarios and SNR conditions.

Conclusion: The proposed framework effectively addresses the dual timescale problem in massive MIMO systems by jointly tracking channel variations and hardware impairments, achieving superior performance over conventional approaches.

Abstract: Hardware impairments in massive multiple-input multiple-output (MIMO) receivers introduce inter-symbol memory and inter-element coupling, severely degrading channel estimation. This paper employs a residual recurrent gated unit (RGRU) to model the intra-slot memory of the hardware impairments and proposes a message-passing-based two-timescale Bayesian deep learning (MP-TTBDL) framework for joint channel and impairment tracking. Owing to small-scale fading, the wireless channel varies rapidly across slots, whereas hardware impairments drift slowly due to hardware aging and environmental variations. To capture these distinct physical timescales, a fastvarying Markov prior and a slow-varying Gaussian Markov prior are assigned to the sparse channel and the network parameters, respectively. Based on a multi-slot factor graph formulation, a message-passing algorithm is developed. Specifically, the inter-slot messages admit closed-form updates, while the intra-slot factor graph, due to its complex recurrent structure, is partitioned into a channel tracking module and an impairments calibration module. The channel tracking module performs sparse channel estimation via turbo orthogonal approximate message passing (Turbo-OAMP), and the impairments calibration module updates the impairment parameters via a specially designed deep approximate message passing (DAMP) procedure, with the two modules iteratively exchanging extrinsic information through expectation propagation (EP) until convergence. Simulation results show that the proposed framework robustly achieves lower channel estimation error than conventional compensators followed by channel estimation across different online impairment scenarios and signal-to-noise ratio (SNR) conditions.

</details>


### [123] [Revisiting Decentralized Online Convex Optimization with Compressed Communication](https://arxiv.org/abs/2607.01665)
*Hao Zhou,Xiaoyu Wang,Chang Yao,Mingli Song,Yuanyu Wan*

Main category: cs.LG

TL;DR: Proposes two new FTRL-type algorithms for decentralized online convex optimization with compressed communication, offering improved elegance and performance compared to existing OGD-type approaches.


<details>
  <summary>Details</summary>
Motivation: While D-OCO with compressed communication has been studied using OGD-type algorithms, FTRL-type algorithms (which are better for exact communication) haven't been adapted to compressed settings. There's a need for more elegant algorithmic designs and better theoretical analysis.

Method: Two FTRL-type algorithms: 1) For full-information setting using FTRL with dual update mechanism and average consensus with communication compression. 2) For bandit setting with improved design to reduce communication costs.

Result: First algorithm matches existing regret bounds in full-information setting. Second algorithm significantly improves both regret bounds and communication costs in bandit setting compared to existing algorithms.

Conclusion: FTRL framework can be effectively adapted for D-OCO with compressed communication, offering more elegant designs and better performance than OGD-type approaches, particularly in bandit settings.

Abstract: Decentralized online convex optimization (D-OCO) is a popular framework for distributed applications with streaming data. To tackle the communication bottleneck, previous studies have investigated D-OCO with compressed communication and proposed several algorithms that are variants of online gradient descent (OGD). However, for D-OCO with exact communication, the best existing algorithms are variants of follow-the-regularized-leader (FTRL). In this paper, for the first time, we propose two FTRL-type algorithms for D-OCO with compressed communication. Compared with OGD-type algorithms, our algorithms are more elegant in both algorithmic design and theoretical analysis. The key insight is that the dual update mechanism of FTRL allows us to make a simple application of the technique for average consensus with communication compression. More specifically, our first algorithm considers the full-information setting, and can match the existing regret bounds. Our second algorithm is designed for the bandit setting, and can significantly improve both the regret bounds and communication costs of existing algorithms.

</details>


### [124] [UniWind: Toward Unified Day-Ahead Wind Power Forecasting via Physics-Informed State Routing](https://arxiv.org/abs/2607.01670)
*Ronghui Xu,Tongxin Wu,Guozhen Zhang,Yihan Li,Chenjuan Guo,Bin Yang,Yong Li*

Main category: cs.LG

TL;DR: UniWind is a novel wind power forecasting model that combines physical constraints with data-driven learning through physics-informed state routing, achieving accurate predictions by distinguishing meteorological effects from operational states.


<details>
  <summary>Details</summary>
Motivation: Current wind power forecasting faces challenges: observed wind-farm power data mixes actual available power with local environmental effects and operational states (like shutdowns/curtailment). Physical models provide constraints but adapt poorly across farms, while data-driven models capture correlations but conflate meteorological effects with state-induced deviations.

Method: UniWind uses physics-informed state routing with three components: 1) Physical Prior Estimator creates site-calibrated physical prior using site-conditioned monotonic warping and shared physical power curve with upper-bound constraints; 2) Latent State Encoder models operating-state embeddings; 3) State-aware Power Corrector transforms prior into final forecasts using knowledge-guided supervised state routing and bounded, state-specific expert correction.

Result: The model shows strong performance in both full-shot (standard training) and cross-farm zero-shot experiments on over 20 real-world datasets, demonstrating accuracy and robustness across different wind farm conditions.

Conclusion: UniWind effectively bridges the gap between physical models and data-driven approaches by explicitly modeling operational states while respecting physical constraints, leading to improved wind power forecasting that generalizes well across different wind farm sites.

Abstract: Day-ahead wind power forecasting is essential for cost-effective power-system operation. It is primarily driven by future meteorological conditions while retaining temporal dependencies in power generation. In practice, observed wind-farm power often entangles physically available power with local environmental effects and latent operational states, such as shutdowns and curtailment. Existing physical models provide useful constraints but adapt poorly across wind farms, whereas data-driven models can capture rich correlations but often conflate meteorological effects with state-induced deviations. In this study, we propose UniWind, a wind power forecasting model based on physics-informed state routing. UniWind first employs a Physical Prior Estimator to construct a site-calibrated physical prior by combining site-conditioned monotonic warping with a shared physical power curve. It further applies a physical upper-bound constraint to shape this prior as a soft envelope of available wind power generation. UniWind then proposes a Latent State Encoder to model operating-state embeddings and transforms the physical prior into final power forecasts through a State-aware Power Corrector, which uses knowledge-guided supervised state routing and bounded, state-specific expert correction. Full-shot and cross-farm zero-shot experiments on more than 20 real-world datasets demonstrate the accuracy and robustness of UniWind.

</details>


### [125] [SCAPE: Accurate and Efficient LLM Training with Extreme Sparse Communication](https://arxiv.org/abs/2607.01678)
*Mingkai Zheng,Junlin Chen,Haotian Xie,Zhao Zhang*

Main category: cs.LG

TL;DR: SCAPE is a communication-efficient distributed optimizer for LLM training that uses first-moment statistics for gradient sparsification, achieving up to 43.3% faster pre-training with 90-99% sparsity while maintaining model quality.


<details>
  <summary>Details</summary>
Motivation: Communication dominates LLM pre-training costs, especially in data-parallel and sharded training where gradient synchronization and parameter reconstruction overhead increase with model size. Existing methods either sparsify raw gradients (unstable for Adam optimizers) or quantize communication (limited savings, runtime overhead).

Method: SCAPE exploits the stability of Adam's first-moment statistics to enable aggressive sparsification. It derives sparsity masks from first-moment-based statistics (not raw gradients), partitions mask generation across workers aligned with optimizer sharding, delays mask usage by one step for computation overlap, and reconstructs second-moment update quantities from a single synchronized sparse buffer.

Result: SCAPE preserves training stability, validation loss, and downstream task accuracy under 90% and 99% sparsity. For Llama-500M, it reduces end-to-end pre-training wall-clock time by up to 43.3% while maintaining comparable quality to dense AdamW/AdamS. For Llama-1.8B, SCAPE achieves up to 3.26× speedup per step compared to dense AdamS.

Conclusion: SCAPE is an effective communication-efficient optimizer for LLM training that combines first-moment-based sparsification with system-aware optimizations, achieving significant speedups (up to 43.3% reduction in pre-training time) while preserving model quality at high sparsity levels.

Abstract: Communication increasingly dominates the cost of Large Language Model (LLM) pre-training, especially under data-parallel and sharded training schemes, where gradient synchronization and parameter reconstruction overhead increase with model size and system scale. Existing communication-reduction methods either sparsify raw gradients, which can be unstable for modern Adam-style optimizers at high sparsity, or quantize communication, whose savings are fundamentally bounded by bit width and often incur additional runtime overhead. We present SCAPE, a communication-efficient distributed optimizer for LLM training that exploits the stability of AdamS's first-moment to enable aggressive sparsification without loss of LLM quality. Instead of constructing masks from raw gradients, SCAPE derives them from first-moment-based statistics, partitions mask generation across workers to align with optimizer sharding, and delays mask usage by one step so that mask synchronization can overlap with computation. SCAPE also reconstructs the quantities required for second-moment updates from a single synchronized sparse buffer, avoiding an additional collective. We implement SCAPE in Megatron-LM and evaluate its convergence by pre-training GPT-345M on OpenWebText and Llama-500M on SlimPajama-6B using 32 NVIDIA GH200 GPUs on TACC Vista. In both models, SCAPE preserves training stability, validation loss, and downstream task accuracy under 90\% and 99\% sparsity. For Llama-500M, SCAPE reduces end-to-end pre-training wall-clock time by up to 43.3\% while maintaining model quality comparable to dense AdamW and AdamS. For Llama-1.8B, SCAPE achieves up to 3.26$\times$ speedup per step compared to dense AdamS.

</details>


### [126] [WARP: Weight-Space Analysis for Recovering Training Data Portfolios](https://arxiv.org/abs/2607.01686)
*Tzu-Heng Huang,Aditya Goyal,John Cooper,Frederic Sala*

Main category: cs.LG

TL;DR: WARP is a framework that recovers training data mixture weights from fine-tuned model weights by using model merging to simulate training trajectories and analyzing geometric patterns in weight space.


<details>
  <summary>Details</summary>
Motivation: Foundation models are released publicly without disclosing their training data recipes (like domain mixture weights), creating an access asymmetry where researchers can study models but not understand what data distributions produced them. Existing methods like membership inference only detect individual samples rather than characterizing the global composition of training corpora.

Method: WARP interpolates between base and fine-tuned models using model merging to generate pseudo-checkpoints that approximate missing training trajectories. These expose geometric footprints of training data in weight space. WARP extracts geometric features from these footprints and maps them to domain proportions using either a parameter-free softmax readout or an MLP projector trained on synthetic mixtures.

Result: In controlled experiments with BERT and GPT-2, WARP recovered domain mixtures with average MAE as low as 0.046 and 0.104 respectively, outperforming both membership inference methods and a variant that had access to the true training trajectory.

Conclusion: WARP successfully addresses the data recipe disclosure problem by enabling recovery of training domain mixtures from released model weights, providing important transparency into foundation model training that was previously unavailable. The method's strong performance suggests that geometric patterns in weight space contain meaningful signals about training data composition.

Abstract: Foundation models are routinely released to the public, yet the data recipes used to train them -- such as domain mixture weights that determine how different sources are sampled -- are rarely disclosed. This creates an access asymmetry: researchers study the resulting models but lack visibility into the training distribution that produces them. Prior works for inferring training data, such as membership inference, detect at the level of individual samples and thus cannot characterize the global composition of the training corpus. We introduce WARP, a framework that recovers a fine-tuned model's training mixtures directly from its released weights. WARP interpolates between the base and fine-tuned models using model merging, generating pseudo-checkpoints that approximate the missing training trajectory and expose a geometric footprint of the training data in the weight space. From these simulated footprints, WARP extracts geometric features and maps them to domain proportions using either a parameter-free softmax readout or an MLP projector trained on synthetic mixtures. In controlled experiments with BERT and GPT-2, WARP recovers domain mixtures with an average MAE as low as 0.046 and 0.104 respectively, outperforming membership inference and a variant with access to the true training trajectory.

</details>


### [127] [Model Merging as Probabilistic Inference in Fine-Tuning Parameter Space](https://arxiv.org/abs/2607.01689)
*Long Minh Bui,Tuan Anh Le Van,Tung Phi Duc,Phi Le Nguyen,Jana Doppa,Trong Nghia Hoang*

Main category: cs.LG

TL;DR: The paper proposes a new probabilistic inference framework for model merging using Product-of-Experts with heavy-tailed Cauchy experts, improving over existing methods that rely on Gaussian assumptions.


<details>
  <summary>Details</summary>
Motivation: Existing model merging approaches use geometric properties of local solution spaces, but these geometric views provide limited guidance for scoring how statistically useful each task-specific update direction is across tasks. There's a need for a more principled statistical framework.

Method: The authors formulate model merging from a probabilistic inference perspective under a Product-of-Experts (PoE) scenario where each single-task solution defines an energy-based expert model over merged parameters. They show existing methods arise as special cases under Gaussian assumptions, then propose a heavy-tailed PoE design based on Cauchy experts that better captures observed heavy-tailed residual behavior.

Result: Experiments across multiple tasks and architectures show significant improvements over state-of-the-art baselines. The heavy-tailed Cauchy expert design better captures the observed residual behavior while admitting a provably convergent inference procedure.

Conclusion: Model merging can be effectively formulated as probabilistic inference under PoE, and using heavy-tailed experts (Cauchy) rather than Gaussian assumptions leads to better performance by better matching the actual statistical properties of model residuals.

Abstract: Model merging aims to combine existing single-task solutions into a multi-task solution without additional data-driven fine-tuning.~Most existing approaches achieve this using geometric properties of local solution spaces. However, such geometric views provide limited guidance for scoring how statistically useful each task-specific update direction is across tasks during merging. To address this, we formulate model merging from a new perspective of probabilistic inference under a product-of-experts (PoE) scenario where each single-task solution defines an energy-based expert model (EBM) over the merged parameters. We show that several existing model merging methods arise as special cases of our framework under energy designs that impose implicit Gaussian assumptions on directional residuals between merged and task-specific models. Empirically, we find that these residuals are often heavy-tailed which exposes a mismatch with the imposed light-tailed Gaussian structures. We address this with a heavy-tailed PoE design based on Cauchy experts, which better captures the observed residual behavior while admitting a provably convergent inference procedure. Experiments across multiple tasks and architectures show significant improvements over state-of-the-arts baselines. Our code is available at https://github.com/MinhLong210/PoE-EBM-Merging.git.

</details>


### [128] [A Mathematical Introduction to Diffusion Models](https://arxiv.org/abs/2607.01693)
*Jianfeng Lu*

Main category: cs.LG

TL;DR: An introductory tutorial on diffusion models focusing on sampling perspectives, with proofs and layered content for graduate students with probability background but no SDE or diffusion experience.


<details>
  <summary>Details</summary>
Motivation: To provide an accessible, proof-oriented introduction to diffusion models for beginning graduate students who have probability background but lack exposure to stochastic differential equations, stochastic numerics, or diffusion models.

Method: Presents material in layered format: core definitions and identities proved in full, representative estimates proved under simplifying assumptions, and research-level theorems stated with proof roadmaps. Traces a single arc from classical sampling dynamics to modern diffusion samplers.

Result: A comprehensive tutorial covering error analysis and inference-time control of diffusion models from sampling perspective, making advanced concepts accessible to beginners through carefully structured content.

Conclusion: This tutorial successfully bridges the knowledge gap by providing a systematic, proof-oriented path for probability-savvy students to understand diffusion models and their sampling foundations without requiring prior SDE expertise.

Abstract: These notes give a proof-oriented introduction to diffusion models from the viewpoint of sampling, tracing a single arc from classical sampling dynamics to modern diffusion samplers, their error analysis, and inference-time control. Throughout, the material is layered into core definitions and identities proved in full, representative estimates proved under simplifying assumptions, and research-level theorems stated with a proof roadmap. The intended audience is beginning graduate students with a background in probability but no prior exposure to stochastic differential equations, stochastic numerics, or diffusion models.

</details>


### [129] [Frequency Shift Physics-Informed Extreme Learning Machine for Solving High-Frequency Partial Differential Equations](https://arxiv.org/abs/2607.01694)
*Xiong Xiong,Ruonan Zhai,Zheng Zeng,Sheng Zhou,Rongchun Hu,Zichen Deng*

Main category: cs.LG

TL;DR: FS-PIELM uses frequency shift weight initialization to overcome spectral bias in physics-informed neural networks, achieving 1-5 orders of magnitude accuracy improvement on PDE problems.


<details>
  <summary>Details</summary>
Motivation: Physics-informed machine learning struggles with high-frequency PDE solutions due to spectral bias where neural networks preferentially learn low-frequency components, limiting their effectiveness for complex physical phenomena.

Method: The paper proposes a Frequency Shift Physics-Informed Extreme Learning Machine that shifts the mean of Gaussian weight distributions instead of scaling them. Two variants: FS-PIELM-L with independent frequency magnitudes per neuron, and FS-PIELM-G with neuron grouping for robustness.

Result: Theoretical analysis shows bounded frequency variance approaching unity, compared to quadratic growth in conventional methods. Experimental results on 7 benchmark problems across 6 equation types show the linear variant achieves best accuracy in 6/7 cases with 1-5 orders of magnitude improvement over existing PIELM variants.

Conclusion: FS-PIELM effectively addresses spectral bias in physics-informed learning while maintaining computational efficiency, offering a promising approach for high-frequency PDE solutions on both regular and complex geometries.

Abstract: Solving partial differential equations (PDEs) with high-frequency solutions remains a central challenge in physics-informed machine learning due to spectral bias -- the tendency of neural networks to learn low-frequency components preferentially. This paper proposes a Frequency Shift Physics-Informed Extreme Learning Machine (FS-PIELM) framework that addresses this limitation through an additive mechanism for weight initialization. Rather than multiplying random weights by a scaling factor, the method translates the mean of the Gaussian weight distribution while keeping the variance fixed at unity, thereby avoiding the variance amplification inherent in scaling-based methods. Two variants are developed: FS-PIELM-L assigns independent frequency magnitudes to individual neurons, while FS-PIELM-G groups neurons for improved robustness. Theoretical analysis shows that the frequency variance under the proposed framework remains bounded and approaches unity regardless of target frequency, in contrast to the quadratic growth of conventional approaches. The method preserves the computational efficiency of extreme learning machines, requiring only a single linear solve. Experiments on seven benchmark problems spanning six equation types -- Helmholtz, wave, Poisson, Klein-Gordon, heat, and advection-diffusion -- on both regular and complex geometries show that the linear variant achieves the best accuracy in six of seven cases, with improvements of one to nearly five orders of magnitude over existing PIELM variants. The code and data accompanying this manuscript will be made publicly available at https://github.com/xgxgnpu/Physics-informed-vibe-coding/tree/main/FS-PIELM.

</details>


### [130] [Predicting Closed-Loop Performance of Latent World Models: Offline Checkpoint Selection for MPC and Model-Based RL Under Non-Markovian Rewards in LunarLander](https://arxiv.org/abs/2607.01736)
*Nikolai Smolyanskiy*

Main category: cs.LG

TL;DR: The paper presents CROF (Composite Reward Observability Fraction), a validation-time diagnostic for selecting optimal world model checkpoints that correlates with downstream closed-loop performance, tested on LunarLander with significant improvements over baselines.


<details>
  <summary>Details</summary>
Motivation: Standard validation metrics like validation loss and multi-step prediction RMSE don't correlate well with actual closed-loop performance of learned world models, often continuing to improve even after downstream performance has collapsed, making checkpoint selection difficult.

Method: 1) Train an RSSM world model on Gymnasium's LunarLander v3 with shaped rewards; 2) Use CEM-MPC return as oracle for closed-loop quality; 3) Evaluate 40 validation-time diagnostics drawn from optimal-control theory; 4) Identify Reward Observability Fraction (ROF) as strongest predictor; 5) Combine ROF with three structural regularizers into Composite Reward Observability Fraction (CROF) for checkpoint selection.

Result: ROF was the strongest single predictor of downstream performance. CROF-selected world models enabled model-based A2C policies to outperform model-free A2C by ~24.5 return points using ~65x fewer real-environment interactions, and also supported strong zero-shot CEM-MPC policies.

Conclusion: Structural validation-time diagnostics, particularly the proposed CROF metric, provide effective tools for selecting world model checkpoints that will perform well in downstream closed-loop control tasks, bridging the gap between offline validation metrics and actual deployment performance.

Abstract: We study how to predict the downstream closed-loop performance of a learned latent world model from validation-time diagnostics alone. Choosing the right checkpoint from a world-model training run is difficult: validation loss and multi-step prediction RMSE keep improving long after closed-loop performance has collapsed. We present a suite of structural validation-time diagnostics drawn from optimal-control theory and apply them to Gymnasium's LunarLander v3, which features shaped rewards. We train an RSSM [5, 4] world model on it and treat per checkpoint CEM-MPC return as the oracle for closed-loop quality. By evaluating 40 metrics against this oracle, we find that the strongest single predictor is the Reward Observability Fraction (ROF), which measures the reward predictor's dependence on the observable subspace. We combine ROF with three structural regularizers into a single-number offline checkpoint selection score, the Composite Reward Observability Fraction (CROF). The CROF-selected world model trains a model-based A2C policy that beats a fairly evaluated model-free A2C baseline by ~24.5 return points while using ~65x fewer real-environment interactions, and the same world model also drives a strong zero-shot CEM-MPC policy. Code and data: https://github.com/nsmoly/LunarLander_RSSM.

</details>


### [131] [Finite-Lag Operator Geometry of Recurrent Representations](https://arxiv.org/abs/2607.01746)
*Kanishka Reddy*

Main category: cs.LG

TL;DR: TL;DR: The paper develops a geometric framework for analyzing recurrent neural network hidden states using finite-lag transport operators, decomposing dynamics into conditional spread, coherent displacement, and coordinate circulation components with proven stability properties.


<details>
  <summary>Details</summary>
Motivation: Recurrent representations evolve as trajectories, but existing geometry methods only measure static snapshots. There's a need for tools that capture the directed, time-lagged dynamics of recurrent hidden states to better understand their temporal evolution.

Method: Develops finite-lag operator geometry using observed source-successor pairs (X_t, X_{t+Δ}). Uses conditional transport law Q_Δ(dy|x) estimated by dense Gaussian source-smoothing operator. Derives source-centered transport tensor G_Δ that decomposes into conditional spread and coherent displacement, plus antisymmetric coordinate circulation W_Δ^ρ for directed lagged flow. Proves affine covariance, estimator stability, and finite-lag separation results.

Result: Theoretical framework with: 1) Exact decomposition of transport into conditional spread and coherent displacement, 2) Explicit metric dependence of scalar summaries, 3) Dense estimator stability on bounded trajectory clouds, 4) Finite-lag separation showing detection of deterministic recurrent motion missed by infinitesimal methods, 5) Linear-Gaussian closed form calibration using update matrix A_Δ and covariance matrices. Experimental validation confirms decomposition, circulation, covariance, and stability predictions.

Conclusion: The finite-lag transport geometry framework successfully captures directed recurrent dynamics missed by static snapshots. It reveals architecture-dependent differences in transport scale and coherent displacement in neural networks, while coherent displacement fraction depends on metric and resolution choices. The approach provides principled tools for analyzing temporal evolution in recurrent systems.

Abstract: Recurrent representations are trajectories, but representation geometry is often measured from static snapshots. We develop finite-lag operator geometry for recurrent hidden states from observed source-successor pairs $(X_t,X_{t+Δ})$. The primitive is the conditional transport law $Q_Δ(dy\mid x)$, estimated by a dense Gaussian source-smoothing operator. From this directed finite-lag law we derive a source-centered transport tensor $G_Δ$, which decomposes exactly into conditional spread and coherent displacement, and an antisymmetric coordinate circulation $W_Δ^ρ$, which summarizes directed lagged flow. We prove affine covariance with explicit metric dependence of scalar summaries, dense estimator stability on bounded trajectory clouds, and a finite-lag separation result showing that source-centered transport detects deterministic recurrent motion not recorded by infinitesimal carre-du-champ geometry. A linear-Gaussian closed form calibrates the quantities in terms of the update $A_Δ$, source covariance, and innovation covariance. Controlled experiments validate the decomposition, circulation, covariance, and stability predictions. In performance matched repeat-copy networks, the framework reveals architecture dependent differences in total transport scale and coherent displacement trace, while coherent displacement fraction is metric and resolution dependent.

</details>


### [132] [Efficient Temporal Point Processes via Monotone Alternating Splines](https://arxiv.org/abs/2607.01752)
*Cheng Wan,Quyu Kong,Feng Zhou*

Main category: cs.LG

TL;DR: A novel Monotone Alternating Splines (MAS) framework is proposed to overcome limitations of Monotone Neural Networks for modeling cumulative conditional intensity functions in temporal point processes.


<details>
  <summary>Details</summary>
Motivation: Current CCIF parameterizations rely on Monotone Neural Networks (MNNs) which suffer from three structural deadlocks: convexity restrictions, saturation limits, and violations of CCIF modeling requirements, fundamentally limiting their representational capacity for complex temporal dynamics.

Method: Proposes Monotone Alternating Splines (MAS) framework that uses distinct interpolation and extrapolation components to provide flexible and efficient CCIF modeling, overcoming the limitations of MNNs.

Result: Extensive experiments show MAS achieves superior performance on both synthetic and real-world datasets. Theoretically, MAS's interpolation provides strong fitting accuracy while its extrapolation supports robust generalization.

Conclusion: MAS resolves the bottlenecks of MNNs in CCIF modeling, offering a more flexible and efficient framework that reduces irreducible approximation gaps and improves computational efficiency for temporal point processes.

Abstract: Temporal point processes (TPPs) have widespread applications across various domains. Compared to modeling the conditional intensity of a TPP, modeling its cumulative conditional intensity function (CCIF) improves computational efficiency and eliminates numerical approximation errors. However, current CCIF parameterizations uniformly rely on Monotone Neural Networks (MNNs), which we identify as suffering from three structural deadlocks--convexity restrictions, saturation limits, and violations of CCIF modeling requirements--that fundamentally restrict their representational capacity for complex temporal dynamics. To resolve these bottlenecks, this paper proposes a novel framework called Monotone Alternating Splines (MAS). By leveraging distinct interpolation and extrapolation components, MAS provides a flexible and efficient framework for modeling CCIFs. Theoretically, MAS's interpolation provides strong fitting accuracy, while its extrapolation supports robust generalization, reducing the irreducible approximation gaps of MNNs. Extensive experiments show that MAS achieves superior performance on both synthetic and real-world datasets.

</details>


### [133] [Role-Aware Neural Convex Divergence Heads for Asymmetric Representation Learning](https://arxiv.org/abs/2607.01762)
*He Huang,Lu Shen,Yunfeng Huang,Li Qi*

Main category: cs.LG

TL;DR: The paper proposes a role-aware neural convex divergence head for asymmetric representation learning, which uses source/target role projections and convex Bregman divergences to model directional relations while maintaining geometric structure.


<details>
  <summary>Details</summary>
Motivation: Standard distance measures like Euclidean, cosine, and Mahalanobis are symmetric and cannot effectively model directional relations that are common in many representation learning problems (lexical entailment, sentence entailment, ontology hierarchy, citation links). Generic neural scorers can model directionality but lack geometric structure.

Method: Proposes a role-aware neural convex divergence head that applies source- and target-role projections before evaluating an input-convex neural Bregman divergence. This yields nonnegative structured scores in role-projected space with properties like projected-space identity, source-role convexity, directional-gap decomposition, and Hessian-based local curvature.

Result: On lexical, sentence, ontology, and directed graph benchmarks, role-aware projections consistently improve directional accuracy over plain ICNN-Bregman heads across ten random seeds while preserving zero observed negative divergence rate. However, on large fixed-feature citation prediction, specialized symmetric or hyperbolic baselines remain stronger in ranking accuracy.

Conclusion: The proposed head is best understood as a structured and interpretable plug-in distance module for tasks where directional relations matter, providing a geometric approach to asymmetric representation learning with mathematical guarantees.

Abstract: Many representation learning problems involve directed relations, such as lexical entailment, sentence entailment, ontology hierarchy, and citation links. Standard Euclidean, cosine, and Mahalanobis heads are symmetric, while generic neural scorers can model directionality but provide limited geometric structure. This paper proposes a role-aware neural convex divergence head for asymmetric representation learning. The head applies source- and target-role projections before evaluating an input-convex neural Bregman divergence, yielding a nonnegative structured score in the role-projected space. We characterize its projected-space identity, source-role convexity, directional-gap decomposition, and Hessian-based local curvature. Experiments on lexical, sentence, ontology, and directed graph benchmarks compare symmetric distances, unstructured asymmetric scorers, order/hyperbolic baselines, plain ICNN-Bregman heads, and the proposed role-aware variant. Across ten random seeds on the main semantic and ontology benchmarks, role-aware projections consistently improve directional accuracy over plain ICNN-Bregman heads while preserving zero observed negative divergence rate. The results also identify a boundary case: on large fixed-feature citation prediction, specialized symmetric or hyperbolic baselines remain stronger in ranking accuracy. Overall, the proposed head is best understood as a structured and interpretable plug-in distance module for tasks where directional relations matter.

</details>


### [134] [Denser $\neq$ Better: Limits of On-Policy Self-Distillation for Continual Post-Training](https://arxiv.org/abs/2607.01763)
*Meng Wang,Haohan Zhao,Wenzhuo Liu,Lu Yang,Geng Liu,Haiyang Guo,Guo-Sen Xie,Gaofeng Meng,Hongbin Liu,Fei Zhu*

Main category: cs.LG

TL;DR: On-policy self-distillation (SDPO) works well for in-domain specialization but struggles with out-of-distribution generalization and causes stronger forgetting in continual learning, making it unsuitable as a default stabilizer.


<details>
  <summary>Details</summary>
Motivation: Recent work suggests on-policy learning can mitigate forgetting in continual post-training, with self-distillation emerging as an attractive approach. The authors want to revisit this optimistic view to understand the limitations of self-distillation methods.

Method: The authors conduct experiments with Self-Distillation Policy Optimization (SDPO) and compare it with on-policy reinforcement learning methods like GRPO. They analyze parameter drift, response space drift, and formatting artifacts through teacher-student loops.

Result: SDPO accelerates in-domain specialization when teacher signals are stable, but struggles with out-of-distribution generalization. In continual post-training, SDPO shows stronger forgetting and can collapse, while RL methods adapt more conservatively. Denser self-distillation causes larger parameter/response drift and amplifies formatting artifacts.

Conclusion: On-policy data alone is insufficient for continual learning. Self-distillation can accelerate specialization in stable scenarios but should not be treated as a default stabilizer for continual post-training due to its forgetting and generalization issues.

Abstract: Continual post-training enables foundation models to acquire new knowledge while preserving existing capabilities. Recent work suggests that on-policy learning can mitigate forgetting, with on-policy self-distillation emerging as a particularly attractive approach. In this work, we revisit this optimistic view through self-distillation policy optimization (SDPO). Our experiments show that SDPO can accelerate in-domain specialization when teacher signals are stable and well aligned, but it struggles to generalize to out-of-distribution scenarios. In continual post-training, SDPO exhibits stronger forgetting and can even collapse, whereas on-policy reinforcement learning methods such as GRPO adapt more conservatively and better preserve prior capabilities. Further analyses reveal that denser self-distillation induces larger drift in both parameter space and response space, and can amplify high-frequency formatting artifacts through a self-reinforcing teacher--student loop. These findings suggest that on-policy data alone is insufficient for continual learning. Dense self-distillation can accelerate specialization when teacher targets are stable and token-level supervision is reliable, but it should not be treated as a default stabilizer for continual post-training. Our code is available at https://github.com/Moenupa/SDPO-CL.

</details>


### [135] [Set Diffusion: Interpolating Token Orderings Between Autoregression and Diffusion for Fast and Flexible Decoding](https://arxiv.org/abs/2607.01775)
*Marianne Arriola,Volodymyr Kuleshov*

Main category: cs.LG

TL;DR: Set diffusion models for language generation that use flexible token sets instead of fixed blocks, enabling KV caching and more efficient parallel decoding.


<details>
  <summary>Details</summary>
Motivation: Discrete diffusion models have improved quality but lack KV caching and fixed-length generation constraints. Block diffusion bridges diffusion and AR models but has limitations in decoding flexibility and parallelism due to fixed-size sequential blocks.

Method: Introduces set diffusion with two components: (1) likelihood parameterization that factorizes over flexible-position, flexible-length token sets, and (2) set-causal diffusion architecture supporting KV cache updates after every inference step.

Result: Achieves better speed-quality tradeoffs on mathematical reasoning, summarization, and unconditional generation compared to prior diffusion language models, with stronger infilling performance than block diffusion.

Conclusion: Set diffusion enables more flexible token generation through arbitrarily-ordered sets (including sliding-window), supporting KV caching and efficient parallel decoding while maintaining quality advantages over existing diffusion language models.

Abstract: Discrete diffusion models have steadily improved in quality relative to autoregressive (AR) models. However, these models are normally constrained to fixed-length generation and do not support key-value (KV) caching. Block diffusion partially bridges diffusion and AR by generating token blocks left-to-right, but its fixed-size sequential blocks limit decoding flexibility and parallelism. Here, we present a new class of language models, set diffusion, comprised of (i) a likelihood parameterization that factorizes over flexible-position, flexible-length token sets and (ii) a set-causal diffusion architecture that supports KV cache updates after every inference step. By factorizing over token sets instead of fixed-size blocks, tokens can be decoded in arbitrarily-ordered sets, including sliding-window sets, enabling faster inference and support for any-order decoding. Set diffusion achieves better speed-quality tradeoffs on mathematical reasoning, summarization, and unconditional generation compared to prior diffusion language models while offering stronger infilling performance than block diffusion. We provide the code, along with the model weights and blog post on the project page: https://m-arriola.com/setdlms/

</details>


### [136] [EHHN: An Event-driven Heterogeneous Hypergraph Network for Object-Centric Next Activity Prediction](https://arxiv.org/abs/2607.01785)
*Jiaxing Wang,Kaitao Chen,Zhubin Han,Chenyu Hou,Bin Cao,Jing Fan,Ji Zhang*

Main category: cs.LG

TL;DR: EHHN is a novel heterogeneous hypergraph network for object-centric next activity prediction that significantly outperforms existing methods in accuracy and efficiency.


<details>
  <summary>Details</summary>
Motivation: Real service processes involve multiple typed business objects interacting with each other, but existing next activity prediction methods assume classical single-case event logs. Current object-centric event log (OCEL) approaches either lose cross-object context through flattening or encode multi-object events through inefficient pairwise relations, failing to jointly capture event-driven object state changes, inter-event timing, and global execution patterns.

Method: EHHN represents each prediction prefix as a heterogeneous hypergraph where event-object hyperedges bind co-participating objects and a lifecycle hyperedge groups the primary object's lifecycle events. It uses a dual-stream architecture: (1) micro-spatial stream models event-driven object-state evolution, and (2) macro-evolution stream captures temporal dynamics using retrieved global prototypes. The two streams are fused for prediction.

Result: EHHN achieves the best accuracy and macro F1-score on all four public OCEL benchmarks against nine baselines, with improvements of up to 8.1 and 12.4 percentage points over the strongest baselines. It also reduces peak GPU memory by up to 24 times compared with the strongest OCEL-native graph baseline.

Conclusion: EHHN effectively addresses limitations in object-centric next activity prediction by capturing rich cross-object interactions through heterogeneous hypergraphs while maintaining computational efficiency. The approach demonstrates superior performance and scalability for real service processes with multiple interacting business objects.

Abstract: Next activity prediction helps service-oriented processes anticipate upcoming steps before delays, exceptions, or service-level risks occur. Most existing methods assume classical single-case event logs, whereas real service processes often involve events shared by multiple typed business objects. Object-centric event logs (OCELs) capture such interactions, but current predictors remain limited. Flattening-based approaches lose cross-object context, and native OCEL graph-based approaches encode multi-object events through pairwise relations. Existing models also do not jointly capture event-driven object state changes, inter-event timing, and global execution patterns. We propose EHHN, an Event-driven Heterogeneous Hypergraph Network for object-centric next activity prediction. EHHN represents each prediction prefix as a heterogeneous hypergraph, where event--object hyperedges bind retained co-participating objects and a lifecycle hyperedge groups the primary object's observed lifecycle events. Based on this representation, EHHN uses a dual-stream architecture in which a micro-spatial stream models event-driven object-state evolution and a macro-evolution stream captures temporal dynamics using retrieved global prototypes. The two streams are fused to predict the next activity. Experiments on four public OCEL benchmarks against nine baselines show that EHHN achieves the best accuracy and macro F1-score on all datasets, with improvements of up to 8.1 and 12.4 percentage points over the strongest baselines. Compared with the strongest OCEL-native graph baseline, EHHN also reduces peak GPU memory by up to 24 times. Code is available at https://github.com/chenkaitao1112/EHHN.

</details>


### [137] [EPnG: Adaptive Expert Prune-and-Grow for Parameter-Efficient MoE Fine-tuning](https://arxiv.org/abs/2607.01789)
*Ahin Lee,Sehyun Yun,Taesik Gong*

Main category: cs.LG

TL;DR: EPnG is an adaptive prune-and-grow framework that dynamically reallocates LoRA capacity in MoE models based on expert importance, outperforming standard LoRA and matching full fine-tuning with 140-180x fewer parameters.


<details>
  <summary>Details</summary>
Motivation: Mixture-of-Experts (MoE) models are efficient to scale but expensive to fine-tune due to redundant experts and uniform parameter allocation. Existing PEFT methods like LoRA ignore the routing dynamics of MoE models, leading to inefficient resource utilization during fine-tuning.

Method: EPnG proposes an adaptive prune-and-grow framework that: 1) reallocates LoRA capacity based on expert importance measured from router gate probabilities, 2) prunes under-utilized experts to free up resources, and 3) expands high-importance experts via rank growth with orthogonal initialization, all while maintaining a fixed parameter budget.

Result: EPnG consistently outperforms LoRA under the same parameter budget across OLMoE and Qwen1.5-MoE models. It achieves performance comparable to full fine-tuning while updating only 0.55%-0.72% of parameters (140-180x fewer parameters than full fine-tuning).

Conclusion: Aligning parameter-efficient fine-tuning methods with MoE routing dynamics yields a more effective and scalable fine-tuning strategy. EPnG demonstrates that adaptive resource allocation based on expert importance is crucial for efficient MoE model adaptation.

Abstract: Mixture-of-Experts (MoE) models scale efficiently but remain costly to adapt due to redundant experts and uniform parameter allocation. Existing parameter-efficient fine-tuning (PEFT) methods such as LoRA ignore MoE routing dynamics, leading to suboptimal resource use. We propose EPnG, an adaptive prune-and-grow framework that reallocates LoRA capacity based on expert importance derived from router gate probabilities. EPnG prunes under-utilized experts and expands high-importance experts via rank growth with orthogonal initialization, while maintaining a fixed parameter budget. Across OLMoE and Qwen1.5-MoE, EPnG consistently outperforms LoRA under the same budget and achieves performance comparable to full fine-tuning while updating only 0.55%-0.72% of parameters (up to 140x-180x fewer). These results demonstrate that aligning PEFT with MoE routing yields a more effective and scalable fine-tuning strategy.

</details>


### [138] [Single-Channel EEG-Based Cognitive Load Assessment in Online Learning: A Hybrid Deep Learning Approach](https://arxiv.org/abs/2607.01795)
*Rowan Hussein,Mohamed Ouf*

Main category: cs.LG

TL;DR: EEG-based cognitive load monitoring shows promise for online learning, achieving up to 78.5% accuracy in distinguishing easy vs difficult video content using a hybrid deep learning model, but with important methodological cautions due to small dataset.


<details>
  <summary>Details</summary>
Motivation: Remote learning removes visual cues for instructors to detect when students struggle, creating need for automated cognitive load monitoring to identify difficult content.

Method: Used NeuroSky MindWave Mobile 2 EEG device with publicly available dataset of 9 learners. Implemented hybrid CNN+LSTM+Attention model combining raw EEG waveforms with band-power features, comparing to conventional feature-based classifiers. Employed regularization techniques (dropout and L2) to address overfitting.

Result: Within-subject model achieved up to 78.5% accuracy vs 55% for conventional classifiers. Regularization stabilized validation accuracy at 68-73%. Authors caution about over-optimism with small dataset (9 subjects) and advocate for subject-independent evaluation as standard.

Conclusion: Consumer-grade EEG shows feasibility for cognitive load monitoring but study is preliminary. Authors frame work as feasibility study with open-source tools for reproducible evaluation and visualization of cognitive load patterns over video timeline to help educators.

Abstract: Monitoring cognitive load during online learning could help instructors identify content that learners find difficult, but remote settings remove the visual cues that support this judgement in a classroom. We study whether a single-channel, consumer-grade EEG device (the NeuroSky MindWave Mobile 2) can distinguish easy from difficult educational-video content, using the publicly available dataset of Wang et al. [24] (ten learners, one excluded for excessive noise, leaving nine). We implement a hybrid CNN+LSTM+Attention model that combines the raw waveform with band-power features. In a within-subject setting, the model reaches up to 78.5% accuracy, compared with 55% for conventional feature-based classifiers; regularization (dropout and L2) closes the large gap between training and validation accuracy that we observe without it, keeping validation accuracy stable at roughly 68-73%. We are deliberately cautious about these numbers: with only nine subjects, within-subject evaluation is optimistic, and we argue that subject-independent evaluation -- in which no learner appears in both training and test data -- should be the standard for this task. To that end we release a reproducible evaluation pipeline. We frame the work as a feasibility study rather than a deployable system, and pair it with an open, notebook-based tool that records EEG, runs inference, and visualizes estimated cognitive load as a heatmap over the video timeline to help educators locate potentially challenging segments.

</details>


### [139] [Expander Sparse Autoencoders: Parameter-Efficient Dictionaries for Mechanistic Interpretability](https://arxiv.org/abs/2607.01799)
*Rodrigo Mendoza-Smith*

Main category: cs.LG

TL;DR: Expander SAEs reduce dense decoder storage from O(mn) to O(dn) by using expander graph masks while maintaining performance.


<details>
  <summary>Details</summary>
Motivation: Standard SAEs require dense decoders with O(mn) parameters, which becomes prohibitively expensive at large feature counts. There's a need for more parameter-efficient sparse autoencoders.

Method: Introduce Expander SAEs: TopK SAEs with decoder and tied encoder supported on a left-d-regular expander mask (d << m). This reduces decoder parameters to dn while keeping sparse-coding fixed. Also reduces OMP correlation step to O(dn) gather-and-reduce.

Result: Across multiple models (Pythia-70M/160M, Qwen2.5-3B, Llama-3.2-1B), varying d traces consistent storage-fidelity frontier. At most compressed setting, Qwen2.5-3B with d=7 uses 293× fewer decoder parameters while retaining 84% of dense CE-loss recovery.

Conclusion: Expander SAEs provide superior storage-fidelity tradeoff driven by sparse decoder structure, not just fewer parameters. Theoretical results show expansion and column flatness enable identifiability and OMP recovery.

Abstract: Sparse autoencoders (SAEs) decompose internal activations of neural networks into sparse linear combinations of learned features by fitting an overcomplete dictionary $\mathbf{W}\in\mathbb{R}^{m\times n}$ with $m<n$, and inferring a sparse code $\mathbf{x}\in\mathbb{R}^n$ from $\mathbf{h}\approx\mathbf{W}\mathbf{x}$. This inference problem closely resembles the canonical setup of compressed sensing, but dense decoders requires $O(mn)$ learned values, which becomes costly at large feature counts. We introduce Expander SAEs: TopK SAEs whose decoder and tied encoder are supported on a left-$d$-regular expander mask with $d\ll m$, learning only $dn$ decoder values while keeping the sparse-coding problem $(m,n,k)$ fixed. The same structure reduces storage and turns the matching-pursuit correlation step $\mathbf{W}^\top \mathbf{r}$ in OMP into an $O(dn)$ gather-and-reduce operation. Our experiments show that across Pythia-70M/160M, Qwen2.5-3B, and Llama-3.2-1B residual-stream activations, varying $d$ traces a consistent storage--fidelity frontier, and that at the most compressed modern-LM setting, Qwen2.5-3B with $d=7$ uses $293\times$ fewer learned decoder values than the full dense decoder while retaining $84$% of dense CE-loss recovered. Control experiments show that the improved storage--fidelity tradeoff is driven by sparse, diverse decoder support structure rather than by fewer learned decoder values, and that when sparse and dense decoders are compared at matched parameter count, part of the remaining gap comes from encoder amortisation. On the theoretical side, we show that expansion and column flatness are sufficient for identifiability of noiseless $k$-sparse codes, and we derive complementary sufficient conditions under which OMP recovers the support exactly.

</details>


### [140] [Do LLMs Truly Generalize in the Molecular Domain? A Perturbation-Based Analysis](https://arxiv.org/abs/2607.01800)
*Jiatong Li,Weida Wang,Changmeng Zheng,Shufei Zhang,Yatao Bian,Xiao-yong Wei,Qing Li*

Main category: cs.LG

TL;DR: Molecular LLMs struggle with structural perturbations, showing fragile sensitivity to even small graph edits, but In-Context Tuning helps expand their local trust regions.


<details>
  <summary>Details</summary>
Motivation: There's a fundamental gap between LLMs' probabilistic nature over discrete sequential tokens and the rigid topological constraints of chemical space, raising questions about whether molecular LLMs can generalize beyond local neighborhoods induced by their sequence-based representations.

Method: Introduced a Molecular Perturbation framework that generates syntax-valid structural variants of training molecules under controlled Graph Edit Distance (GED) to systematically probe the manifold regularity of molecular LLMs.

Result: Even a single edit can cause substantial performance drops on common molecular tasks, revealing a narrow local trust region and fragile sensitivity to structural changes. In-Context Tuning can partially expand the local trust region and offers robustness against structural perturbations.

Conclusion: Molecular LLMs exhibit fragility to structural changes, but In-Context Tuning provides a promising direction for stabilizing them against structural variation by leveraging similarity-based anchoring.

Abstract: Large Language Models (LLMs) have recently shown promise in molecular discovery, yet a gap remains between their probabilistic nature over discrete sequential tokens and the rigid topological constraints of chemical space. This raises the question of whether molecular LLMs can generalize beyond the local neighborhoods induced by their sequence-based representations. To systematically investigate this question, we introduce a Molecular Perturbation framework that generates syntax-valid structural variants of training molecules under controlled Graph Edit Distance (GED) to probe the manifold regularity of molecular LLMs. Our analysis shows that even a single edit can cause substantial performance drops on common molecular tasks, revealing a narrow local trust region and fragile sensitivity to structural changes. Since similar molecules tend to exhibit similar properties, In-Context Tuning (ICT), which anchors predictions on structurally similar molecules, offers a natural way to mitigate such fragility. Our experiments also examine whether ICT confers robustness under controlled structural perturbations, and the results suggest that it can partially expand the local trust region and offer a promising direction for stabilizing molecular LLMs against structural variation.

</details>


### [141] [Gaming Consensus: Coordinated Manipulation in Crowdsourced Fact-Checking](https://arxiv.org/abs/2607.01824)
*Nikil Roashan Selvam,Jay Baxter,Sophie Hilgard,Brad Miller,Keith Coleman,Ellen Vitercik,Sanmi Koyejo*

Main category: cs.LG

TL;DR: Bridging algorithms using matrix factorization for crowdsourced fact-checking are vulnerable to coordinated manipulation where strategic voting can create synthetic consensus, allowing low-quality content to pass thresholds with minimal effort.


<details>
  <summary>Details</summary>
Motivation: Major social media platforms (X, Meta, TikTok, Google) use bridging mechanisms for crowdsourced fact-checking, but the matrix factorization algorithms at their core may be vulnerable to coordinated manipulation by strategic users exploiting latent representations.

Method: Combination of theoretical and empirical analysis using historic production data to examine how coordinated users could vote strategically by leveraging latent representations in matrix factorization to fabricate synthetic consensus within bridging mechanisms.

Result: Up to 10.7% of lower quality notes could be manipulated above consensus thresholds using less than 10 ratings. The analysis reveals counterintuitive behaviors (e.g., "Not Helpful" ratings can increase helpfulness scores) and provides cost models for manipulation effort.

Conclusion: Matrix factorization bridging algorithms for crowdsourced fact-checking are vulnerable to synthetic consensus manipulation, requiring mitigations that have been developed and deployed within X's Community Notes algorithm to address these security concerns.

Abstract: Crowdsourced fact-checking systems have been adopted by major social media companies such as X, Meta, TikTok and Google with the aim of combating misleading information at scale without relying on centralized editorial control. These systems have been developed around a common underlying concept: a bridging mechanism that identifies notes flagging misleading information when they receive support from people with different perspectives rather than simple majority support. To our knowledge the only publicly disclosed bridging algorithms deployed for fact-checking are based on matrix factorization, as deployed by both X and Meta, augmented with additional components addressing abuse, targeted manipulation, and contributor brigades. This work examines the core matrix factorization portion of these systems, presenting theoretical and empirical evaluations of the degree to which coordinated users could vote strategically by leveraging the latent representations to fabricate the appearance of synthetic consensus within the bridging mechanism. Using historic production data, we find that up to 10.7% of lower quality notes could be manipulated above consensus thresholds using less than 10 ratings. We complement these findings with a theoretical analysis, revealing counterintuitively that rating a note as "Not Helpful" can increase its helpfulness score, as well as a cost model quantifying manipulation effort. We have developed and deployed mitigations within X's Community Notes algorithm to address synthetic consensus.

</details>


### [142] [Many Voices, One Reward: Multi-Role Rubric Generation for LLM Judging and Reward Modeling](https://arxiv.org/abs/2607.01830)
*Dazhi Fu,Jiuding Yang,Yiwen Guo,Jicong Fan*

Main category: cs.LG

TL;DR: MRRG (Multi-Role Rubric Generation) is a training-free framework that generates evaluation rubrics from multiple complementary roles to avoid dimensional blind spots in LLM evaluation, outperforming single-role baselines and providing stronger rewards for reinforcement learning.


<details>
  <summary>Details</summary>
Motivation: Existing rubric-based judges for LLM evaluation typically rely on single generic evaluators, which can overlook important dimensions of human preference (dimensional blind spots). There's a need for more comprehensive evaluation criteria that capture diverse aspects of human judgment.

Method: Proposes Multi-Role Rubric Generation (MRRG) - a training-free, reference-free framework that elicits evaluation criteria from multiple complementary roles, then consolidates them into an auditable rubric-based scorer that can validate pairwise preferences and provide rewards for GRPO-style Reinforcement Learning with Verifiable Rewards (RLVR).

Result: MRRG consistently outperforms single-role rubric generation baselines across multiple backbone models on preference validation benchmarks. RLVR experiments show MRRG yields a stronger reward signal for improving open-ended generation.

Conclusion: Multi-role rubric generation effectively addresses dimensional blind spots in LLM evaluation by incorporating diverse perspectives, resulting in more reliable reward signals for both evaluation and reinforcement learning of open-ended generation tasks.

Abstract: Reliable reward and preference signals are critical for evaluating and optimizing large language models on open-ended tasks. Rubric-based judges offer a transparent way to decompose such judgments into explicit evaluation criteria, but existing annotation-free rubric generators typically rely on a single generic evaluator. As a result, they may overlook important dimensions of human preference, a failure mode we term dimensional blind spots. To address this limitation, we propose Multi-Role Rubric Generation (MRRG), a training-free and reference-free framework that elicits evaluation criteria from multiple complementary roles and consolidates them into an auditable rubric-based scorer. This scorer can be used both to validate pairwise preferences and to provide rewards for GRPO-style Reinforcement Learning with Verifiable Rewards (RLVR). Experiments on preference validation benchmarks show that MRRG consistently outperforms single-role rubric generation baselines across multiple backbone models. Further RLVR experiments demonstrate that MRRG yields a stronger reward signal for improving open-ended generation.

</details>


### [143] [Adaptive Group-Based Counterfactual Explanations for Time-Series Rehabilitation Data](https://arxiv.org/abs/2607.01838)
*Emmanuel C. Chukwu,Rianne M. Schouten,Monique Tabak,Mykola Pechenizkiy*

Main category: cs.LG

TL;DR: Group-based counterfactual explanation framework for time-series IMU data in rehabilitation, using learnable gates to generate biomechanically coherent explanations at semantic feature group level.


<details>
  <summary>Details</summary>
Motivation: Existing counterfactual methods for time-series classifiers operate at channel level, producing scattered explanations that don't align with how clinicians reason about movement (using muscle-group and joint-segment abstractions) in rehabilitation analysis.

Method: Two-stage framework: 1) Shapley-Adaptive group ranking to preserve counterfactual validity, 2) Learnable Gate methods with trainable per-group relevance gates jointly optimized with perturbation masks for explicit group selection.

Result: LG methods substantially improve modality-group sparsity compared to M-CELS baseline while maintaining or improving validity, temporal smoothness, and generation efficiency. Group-structured counterfactuals yield concise, muscle-level corrective guidance aligned with clinical reasoning.

Conclusion: The framework enhances interpretability without sacrificing counterfactual quality, enabling more actionable explanations for rehabilitation movement analysis by generating biomechanically coherent explanations at semantic feature group level.

Abstract: Counterfactual explanations (CEs) for multivariate time-series classifiers are often difficult to interpret in domains where experts reason in terms of semantic feature groups rather than individual channels. In rehabilitation movement analysis with multi-sensor inertial measurement units (IMUs), clinicians interpret motion through muscle-group and joint-segment abstractions; yet, most existing counterfactual methods operate at the channel level, producing scattered and biomechanically incoherent explanations. We propose a two-stage framework for group-based counterfactual generation in high-dimensional IMU data. We first show that Shapley-Adaptive (SA) group ranking preserves counterfactual validity but fails to enforce group-level sparsity, motivating the need for explicit group selection. We then introduce Learnable Gate (LG) methods, which incorporate trainable per-group relevance gates jointly optimized with perturbation masks. Experiments on the KneE-PAD rehabilitation dataset demonstrate that LG substantially improves modality-group sparsity compared to the channel-level M-CELS baseline while maintaining or improving validity, temporal smoothness, and generation efficiency. Exercise-specific analyses further show that group-structured counterfactuals yield concise, muscle-level corrective guidance aligned with clinical reasoning. Overall, the proposed framework enhances interpretability without sacrificing counterfactual quality, enabling more actionable explanations for rehabilitation movement analysis.

</details>


### [144] [Decomposer: Learning to Decompile Symbolic Music to Programs](https://arxiv.org/abs/2607.01849)
*Yewon Kim,Apurva Gandhi,David Chung,Graham Neubig,Chris Donahue*

Main category: cs.LG

TL;DR: Decomposer is a post-training framework that converts symbolic MIDI music into readable, executable Strudel code programs through a two-stage approach combining supervised fine-tuning on synthetic data and reinforcement learning on unpaired MIDI.


<details>
  <summary>Details</summary>
Motivation: Musical performance involves executing high-level musical instructions, but recovering those instructions from performances is a challenging inverse problem. There's a need to convert symbolic music (like MIDI) back into readable, editable music programming language code, which can enable better music editing, analysis, and manipulation.

Method: Two-stage approach: 1) Build Strudel-Synth, a synthetic corpus of paired Strudel programs and rendered MIDI for supervised fine-tuning. 2) Refine with reinforcement learning on unpaired MIDI, optimizing rewards for both MIDI reconstruction faithfulness and code readability.

Result: Decomposer achieves substantially higher MIDI reconstruction faithfulness than closed-source LLMs while producing more readable and diverse code than heuristic converters, evaluated across synthetic and real-world MIDI benchmarks.

Conclusion: The framework successfully addresses the challenges of MIDI-to-Strudel decompilation by combining synthetic data generation with reinforcement learning, enabling recovery of executable, editable music programs from symbolic music with both high reconstruction quality and code readability.

Abstract: Musical performance involves executing a set of high-level musical instructions, yet recovering those instructions from the performance is a challenging inverse problem. We present Decomposer, a post-training framework for symbolic music decompilation: the task of recovering executable, editable music programs from symbolic music. We instantiate the task as MIDI-to-Strudel decompilation, where the model takes symbolic MIDI as input and produces a program in Strudel, a music programming language, that reconstructs the input when executed. The task poses two challenges: Strudel is a low-resource language with little naturally paired MIDI-code data, and optimizing faithful reconstruction of MIDI alone can collapse to unreadable note-by-note transliteration. We address these challenges in two stages. First, we construct Strudel-Synth, a synthetic corpus of paired Strudel programs and rendered MIDI, and use it for supervised fine-tuning. Second, we refine the model with reinforcement learning on unpaired MIDI, optimizing rewards for both MIDI reconstruction faithfulness and code readability. Our evaluation across synthetic and real-world MIDI benchmarks shows that Decomposer achieves substantially higher MIDI reconstruction faithfulness than closed-source LLMs while producing more readable and diverse code than the heuristic converter.

</details>


### [145] [Learning the Supports for Categorical Critic in Reinforcement Learning](https://arxiv.org/abs/2607.01880)
*Jen-Yen Chang,Takayuki Osa,Tatsuya Harada*

Main category: cs.LG

TL;DR: Proposes dynamic support learning for histogram-based value estimation in RL, eliminating the need for pre-defined bounds while maintaining or improving performance on continuous control tasks.


<details>
  <summary>Details</summary>
Motivation: Histogram-based losses in RL (like HL-Gauss) require pre-defining fixed support intervals, which is problematic due to the non-stationary and stochastic nature of RL target values. This creates challenges for practical application.

Method: Proposes an approach that dynamically learns the lower and upper bounds of the support interval instead of assigning them beforehand. Derives an objective that jointly learns these bounds while learning the categorical representation of scalar values, forming an upper bound on the mean-squared Bellman error.

Result: The theoretical analysis shows the bound is tighter than non-learned supports of HL-Gauss. Empirically, the method enables stable adaptation of support intervals and matches HL-Gauss-based actor-critic algorithms on most continuous-control tasks while improving on a subset, without requiring pre-specified support intervals.

Conclusion: Dynamic support learning successfully addresses a key limitation of histogram-based value estimation in RL by eliminating the need for manually specified bounds, while maintaining or improving performance across continuous control benchmarks.

Abstract: Value functions are an essential component in actor-critic based deep reinforcement learning (RL). Conventionally, these functions are trained as a regression task by minimising the mean squared error (MSE) relative to bootstrapped target values. Meanwhile, in distributional RL, a distribution of returns is modelled based on the distributional Bellman operator. This work investigates the Gaussian Histogram Loss (HL-Gauss), a recent approach that reframes value estimation as classification by encoding each scalar Bellman target as a Gaussian-smoothed categorical target. Despite its potential, applying histogram-based losses to RL presents inherent challenges, most notably the requirement to pre-define a fixed support interval, which is often complicated by the non-stationary and stochastic nature of target values typically found in RL tasks. In this work, we propose an approach that dynamically learns the lower and upper bounds of the support instead of assigning them beforehand. We derive an objective that jointly learns these bounds whilst learning the categorical representation of the scalar values, and we show that this objective forms an upper bound on the mean-squared Bellman error. Our theoretical analysis further shows that this bound is tighter than that of non-learned supports of HL-Gauss. Empirically, the proposed objective enables stable adaptation of the support interval and matches HL-Gauss-based actor-critic algorithms on most continuous-control tasks whilst improving on a subset, without requiring a pre-specified support interval.

</details>


### [146] [Regularized Variational and Spectral Log-Density-Ratio Estimation in the Gaussian Location Model](https://arxiv.org/abs/2607.01895)
*Francis Bach*

Main category: cs.LG

TL;DR: Ridge-regularized log-density-ratio estimation for Gaussian location model with common covariance. Compares variational estimator (KL divergence with L2 penalty) and spectral estimator (continuum of ridge-regularized least-squares) in high-dimensional asymptotic regime.


<details>
  <summary>Details</summary>
Motivation: To compare two approaches for log-density-ratio estimation in Gaussian location models: variational estimator (empirical KL divergence with regularization) and spectral estimator (recently introduced method using continuum of ridge problems). Understand their relative performance in high-dimensional settings with different observation regimes.

Method: Study Gaussian location model with common covariance, affine transformed to standard normal. Use high-dimensional asymptotic analysis where observations and dimension grow with fixed ratios. Apply CGMT for variational estimator limit, deterministic equivalents for resolvents for spectral estimator limit. Compare population risks with fixed-signal aspect-ratio sweeps and optimized regularization.

Result: With many observations, variational estimator has smaller risk (well-specified case). With fewer observations, spectral estimator is favored due to lower variance from covariance-based construction. Nuclear penalty explored for feature learning.

Conclusion: Choice between variational and spectral estimators depends on observation regime: variational performs better with many observations, while spectral excels with fewer observations due to variance advantages. Both methods analyzed in high-dimensional asymptotics provide theoretical insights for practical applications.

Abstract: We study ridge-regularized log-density-ratio estimation in the Gaussian location model with a common covariance matrix. By affine invariance, the model is written as q $\sim$ N(0, I), p $\sim$ N($Δ$, I), with linear features, where $Δ$ is a mean vector. The variational estimator is the empirical Kullback-Leibler (KL) log-normalized fit with a squared L2-penalty on its nonconstant coefficient, and the spectral estimator recently introduced in [1] replaces a single variational problem by a continuum of ridge-regularized least-squares problems. We derive high-dimensional deterministic asymptotic equivalents when the numbers of observations and dimension tend to infinity with fixed ratios. The regularized variational limit is characterized by a scalar entropy minimization problem derived from the convex-Gaussian-min-max theorem (CGMT), while the regularized spectral limit follows from deterministic equivalents for resolvents of weighted sums of two independent Gaussian sample covariance matrices. We use these formulas to compare population risks, with experiments focused on fixed-signal aspect-ratio sweeps and optimized regularization. Our conclusion is that with many observations, under the criteria and asymptotic regimes analyzed here, the well-specified variational estimator has the smaller risk, while with fewer observations, the spectral estimator is favored because its covariance-based construction has lower variance. We also study how a nuclear penalty can be used and partially analyzed to perform feature learning.

</details>


### [147] [Rank-Then-Act: Reward-Free Control from Frame-Order Progress](https://arxiv.org/abs/2607.01897)
*Yuriy Maksyuta,George Bredis,Ruslan Rakhimov,Daniil Gavrilov*

Main category: cs.LG

TL;DR: RTA is a framework for learning control policies from expert videos without rewards by training a VLM as an ordinal progress scorer and using rank correlation as a reward signal for RL.


<details>
  <summary>Details</summary>
Motivation: The paper aims to learn control policies from expert video demonstrations without needing environment rewards, addressing the challenge of reward specification in RL. Traditional reward learning from videos often relies on explicit reward models or absolute progress estimates, which may not transfer well across tasks and environments.

Method: RTA has two main components: 1) Offline training of a Vision-Language Model as a progress-based ordinal scorer using Group Relative Policy Optimization (GRPO) on shuffled frame sequences, which forces the model to learn temporal ordering from visual semantics. 2) A correlation-based reward function that computes Spearman rank correlation between predicted progress rankings and true temporal indices during RL interaction, creating a bounded, scale-invariant learning signal that decouples reward learning from absolute calibration.

Result: RTA was evaluated on discrete control benchmarks (PyBoy: Catrap, Kirby) and continuous control tasks (PointMaze, MetaWorld). The framework consistently matches or outperforms prior video-based reward learning methods and rank-based baselines. Importantly, it demonstrates strong cross-task reuse of a single pretrained progress scorer, showing good transfer capabilities.

Conclusion: Correlation-structured supervision over video-derived ordinal signals is sufficient for policy learning, offering a scalable alternative to explicit reward design. The approach enables stable transfer across tasks and environments by decoupling reward learning from absolute calibration, making progress-based ordinal scoring with correlation-based rewards a promising direction for learning from demonstrations.

Abstract: We introduce Rank-Then-Act (RTA), a framework for learning control policies from expert video demonstrations without environment rewards. RTA trains a Vision-Language Model (VLM) offline as a progress-based ordinal scorer, using a Group Relative Policy Optimization (GRPO) objective over shuffled frame sequences, which forces the model to recover temporal ordering from visual semantics rather than trivial time cues. Importantly, instead of using the scorer directly as a scalar reward model, we propose a correlation-based reward function for reinforcement learning: at each interaction window, we compute the Spearman rank correlation between predicted progress rankings and true temporal indices, yielding a bounded, scale-invariant learning signal. This design decouples reward learning from absolute calibration and enables stable transfer across tasks and environments. We evaluate RTA on discrete control benchmarks (PyBoy: Catrap, Kirby) and continuous control tasks (PointMaze, MetaWorld). RTA consistently matches or outperforms prior video-based reward learning methods and rank-based baselines, while demonstrating strong cross-task reuse of a single pretrained progress scorer. Our results suggest that correlation-structured supervision over video-derived ordinal signals is sufficient for policy learning, offering a scalable alternative to explicit reward design.

</details>


### [148] [SABER: A Semantic-Aligned Brain Network Analysis Framework via Multi-scale Hypergraphs](https://arxiv.org/abs/2607.01901)
*Yidan Xu,Xiangmin Han,Rundong Xue,Huihui Ye*

Main category: cs.LG

TL;DR: Semantic-aligned brain network framework integrates LLM-derived semantics directly into brain disease diagnosis using multi-scale hypergraphs and semantic alignment mechanisms.


<details>
  <summary>Details</summary>
Motivation: Existing methods treat LLM semantics as auxiliary features or supervision, limiting their direct role in decision-making and constraining classification stability and robustness for brain disease diagnosis.

Method: 1) ROI-level semantics incorporated via global self-attention to enrich node representations; 2) Multi-scale hypergraphs constructed to model functional subnetworks and multi-ROI interactions; 3) Decision-level semantic alignment mechanism injects patient-specific textual embeddings into graph representations.

Result: State-of-the-art performance on ABIDE and ADHD-200 datasets, enhanced stability, improved interpretability, particularly effective in small-sample settings.

Conclusion: The proposed framework actively integrates LLM-derived semantics into prediction processes, overcoming limitations of existing methods and enabling semantics to directly guide brain disease diagnosis decisions.

Abstract: Effective brain disease diagnosis requires the synergy of brain connectivity patterns and high-level semantic knowledge. Existing methods, however, largely treat semantics from large language models (LLMs) as auxiliary features or supervision, limiting their direct role in decision-making and constraining classification stability and robustness. To overcome this, we propose a semantic-aligned brain network framework that actively integrates LLM-derived semantics into the prediction process. Specifically, ROI-level semantics are first incorporated via global self-attention to enrich node representations and provide whole-brain context. Multi-scale hypergraphs are then constructed to explicitly model functional subnetworks and multi-ROI interactions, addressing the locality limitations of traditional GNNs and capturing high-order dependencies. Finally, a decision-level semantic alignment mechanism selectively injects patient-specific textual embeddings into graph representations, enabling semantics to directly guide predictions without perturbing the underlying network structure. Experiments on public brain network datasets ABIDE and ADHD-200 demonstrate state-of-the-art performance, enhanced stability, and improved interpretability, particularly in small-sample settings.

</details>


### [149] [Population-Based Multi-Objective Training of Discriminators for Semi-Supervised GANs](https://arxiv.org/abs/2607.01907)
*Francisco Sedeño,Francisco Chicano,Jamal Toutouh*

Main category: cs.LG

TL;DR: SSL-GANs training improved via evolutionary multi-objective optimization, treating discriminator learning as a Pareto optimization problem between classification accuracy and real/fake discrimination, resulting in more robust training and better performance on limited-label datasets.


<details>
  <summary>Details</summary>
Motivation: Semi-supervised GANs (SSL-GANs) combine generative modeling with classification but suffer from training instability. The authors aim to address this instability by reformulating the discriminator learning process using multi-objective optimization principles.

Method: Proposes a population-based evolutionary training strategy where discriminator learning is formulated as a multi-objective optimization problem. Instead of aggregating supervised and unsupervised components into a single loss, the method maintains a population of discriminators ranked by Pareto dominance to explore trade-offs between classification accuracy and real/fake discrimination.

Result: Experiments on MNIST with limited labels show improved training robustness compared to SSL-GAN and CE-SSL-GAN baselines. The elitist variant consistently achieves the highest classification accuracy, demonstrating the effectiveness of the multi-objective approach.

Conclusion: Formulating SSL-GAN training as a multi-objective optimization problem using evolutionary strategies improves training robustness and performance, particularly for the challenging case of limited labeled data. The Pareto-based approach better balances the dual roles of SSL-GAN discriminators.

Abstract: Semi-supervised generative adversarial networks (SSL-GANs) can exploit large unlabeled datasets while retaining a classifier in the discriminator, but their training is often unstable. This paper proposes a population-based evolutionary training strategy in which discriminator learning is formulated as a multi-objective optimization problem. Instead of aggregating the supervised and unsupervised components of the SSL objective into a single scalar loss, the method maintains a population of discriminators ranked by Pareto dominance, enabling the exploration of different trade-offs between classification accuracy and real/fake discrimination. This formulation aims to improve both roles of SSL-GANs: learning accurate classifiers and training generators capable of producing realistic samples. We analyze several variants, including an elitist strategy and a mono-objective ablation, to assess the role of multi-objective selection. Experiments on MNIST with limited labels show improved training robustness compared to SSL-GAN and CE-SSL-GAN state-of-the-art baselines, while the elitist variant consistently achieves the highest classification accuracy.

</details>


### [150] [Zeus: Towards Tuning-Free Foundation Model for Time Series Analysis](https://arxiv.org/abs/2607.01918)
*Yisong Fu,Zezhi Shao,Chengqing Yu,Yujie Li,Yongjun Xu,Xueqi Cheng,Fei Wang*

Main category: cs.LG

TL;DR: Zeus is a tuning-free Time Series Foundation Model that achieves strong performance across multiple analysis tasks without any task-specific fine-tuning, using a multi-scale Transformer with point-wise tokenization and Multi-Objective Temporal Masking.


<details>
  <summary>Details</summary>
Motivation: Existing time series foundation models primarily focus on zero-shot forecasting but require task-specific tuning for other tasks. There's a gap in achieving multi-task generalization without fine-tuning across diverse time series analysis tasks.

Method: Zeus addresses two key challenges: 1) Using a multi-scale Transformer with point-wise tokenization and U-shaped hierarchy to balance fine-grained detail with computational efficiency, 2) Introducing Multi-Objective Temporal Masking (MOTM) to support heterogeneous tasks (extrapolation, interpolation, global abstraction) within a single framework.

Result: Extensive experiments across five representative tasks demonstrate that Zeus consistently achieves competitive results in tuning-free settings, showing superior performance across diverse analysis tasks without task-specific fine-tuning.

Conclusion: Zeus represents a significant step toward a general-purpose Time Series Foundation Model that can handle multiple analysis tasks without requiring task-specific tuning, demonstrating strong multi-task generalization capabilities.

Abstract: We present Zeus, a unified tuning-free Time Series Foundation Model (TSFM) that delivers superior performance across diverse analysis tasks without any task-specific fine-tuning. Unlike prior studies that primarily focus on zero-shot forecasting but require task-specific tuning for other tasks, Zeus bridges this gap by addressing two fundamental challenges in multi-task generalization. First, to reconcile point-level granularity with long-sequence scalability, Zeus incorporates a multi-scale Transformer featuring point-wise tokenization and a U-shaped hierarchy, effectively balancing fine-grained fidelity with computational efficiency. Second, to accommodate varying inductive biases across different tasks, Zeus introduces Multi-Objective Temporal Masking (MOTM), a unified strategy that supports heterogeneous tasks (e.g., extrapolation, interpolation, and global abstraction) within a single framework. Extensive experiments across five representative tasks demonstrate that Zeus consistently achieves competitive results in tuning-free settings, underscoring its potential as a general-purpose TSFM.

</details>


### [151] [Conditional Co-Ablation: Recovering Self-Repair Backups in Transformer Circuits](https://arxiv.org/abs/2607.01940)
*Zhiren Gong,Zihao Zeng,Chau Yuen,Wei Yang Bryan Lim*

Main category: cs.LG

TL;DR: CoAx is a method that captures second-order component interactions in transformers by measuring how unit ablation effects grow after primary component removal, addressing self-repair masking in mechanistic interpretability.


<details>
  <summary>Details</summary>
Motivation: Current mechanistic interpretability methods use first-order ablation scores that fail when transformers self-repair - after removing a primary component, backup components can take over, making both primary and backup components appear irrelevant in standard isolation-based scoring.

Method: Conditional Co-Ablation (CoAx) is introduced as a label-free, output-grounded score that measures how much each remaining unit's ablation effect grows after a primary set has been removed, capturing second-order interactions that single-unit ablation scores miss. It recasts the problem as conditional circuit completion.

Result: On GPT-2-small IOI circuit, CoAx raises backup-head recovery from 0.33 to 0.91 ROC-AUC, outperforming all baselines including self-repair-aware gradient scores. The method transfers to induction tasks across eight models, corrects self-repair-masked attribution, identifies components for capability knockout, and enables repair-aware structured pruning scaling from 124M to 7B parameters.

Conclusion: Component importance is not merely an isolated-unit property - in robust circuits, the components that matter become visible only under interventions that make them necessary. Conditional ablation reveals second-order interactions essential for understanding self-repair mechanisms.

Abstract: Mechanistic interpretability often relies on component-level interventions to discover how a model produces a behavior. This guides attribution, capability knockout, and model pruning downstream to operate by scoring each unit by the effect of ablation in isolation. Such first-order scoring is natural when component importance is additive, but becomes misleading when a transformer self-repairs: after a primary component is removed, a dormant backup can take over, muting the primary's measured effect while the backup itself appears irrelevant on the intact model. We recast this failure as a recovery task, conditional circuit completion, and introduce Conditional Co-Ablation (CoAx), a label-free, output-grounded score that asks how much each remaining unit's ablation effect grows once a primary set has been removed. This conditional growth exposes the second-order interaction that single-unit scores discard. On the GPT-2-small IOI circuit, CoAx raises backup-head recovery from 0.33 to 0.91 ROC-AUC, outperforming all baselines, including self-repair-aware gradient scores (best 0.82); counterfactual patching verifies that the recovered heads causally carry the repair. The same label-free procedure transfers to induction across eight models. Beyond discovery, the recovered backups correct self-repair-masked attribution, identify the components required for capability knockout, and yield repair-aware structured pruning scaling from 124M to 7B. Component importance is therefore not merely an isolated-unit property: in robust circuits, the components that matter can become visible only under the interventions that make them necessary.

</details>


### [152] [Hybrid quantum-classical neural network for sentiment analysis](https://arxiv.org/abs/2607.01943)
*Giacomo Cappiello,Filippo Caruso,Xing Liang,Dimitrios Makris*

Main category: cs.LG

TL;DR: Hybrid quantum-classical neural networks achieve comparable accuracy to classical models for COVID-19 tweet sentiment analysis and show superior generalization with 15% accuracy improvement for SMS spam classification.


<details>
  <summary>Details</summary>
Motivation: The paper aims to explore the applicability of quantum machine learning (QML) to natural language processing tasks, specifically sentiment analysis, by investigating whether hybrid quantum-classical neural networks can offer advantages over classical approaches.

Method: The study uses COVID-19 tweet data vectorized with TF-IDF encoding, comparing classical feedforward neural networks against hybrid architectures that incorporate parameterized quantum circuits. Transfer learning is then applied to test generalization on an SMS spam classification task.

Result: Hybrid models achieved comparable accuracy to classical baselines for sentiment analysis but showed distinct learning dynamics suggesting richer representational capacity. For transfer learning on SMS spam classification, hybrid models outperformed classical counterparts with a 15 percentage point accuracy increase (66% to 81%) on spam detection.

Conclusion: Hybrid quantum-classical neural networks are feasible for NLP tasks and show potential advantages in generalization capabilities, particularly as quantum hardware continues to advance.

Abstract: Quantum machine learning has recently emerged as a promising paradigm that leverages the expressive power of quantum circuits to address complex learning tasks. In this work, we investigate the applicability of hybrid quantum-classical neural networks to sentiment analysis, a central problem in natural language processing. We focus on a dataset of tweets related to COVID-19, where the textual content is vectorized using TF-IDF and fed into both classical feedforward networks and hybrid architectures incorporating parameterized quantum circuits. Our results show that hybrid models can achieve accuracy comparable to the classical baseline, while exhibiting distinct learning dynamics, especially in terms of validation loss and accuracy, that suggest a richer representational capacity. Moreover, when applying transfer learning to an SMS spam classification task, the hybrid models consistently outperform the classical counterpart, achieving an accuracy increase of 15 percentage points (from 66% to 81%) on the spam class, demonstrating enhanced generalization. These findings highlight the feasibility of employing QML for natural language processing and point toward the potential advantages of hybrid models as quantum hardware continues to advance.

</details>


### [153] [A More Accurate Algorithm Comparison through A/B Testing using Offline Evaluation Methods](https://arxiv.org/abs/2607.01958)
*Koki Konishi,Masataka Ushiku,Yuta Saito*

Main category: cs.LG

TL;DR: A/B testing can have higher algorithm selection error rates than offline evaluation due to lack of positive correlation in sample mean estimation. The paper proposes a novel estimator that introduces positive correlation through a hypothetical middle algorithm to reduce selection errors with less A/B testing data.


<details>
  <summary>Details</summary>
Motivation: The motivation is counterintuitive: despite A/B testing being the gold standard for algorithm selection, it can actually produce higher selection error rates than offline evaluation. This happens because traditional A/B testing methods don't induce positive correlation needed to reduce critical selection errors (underestimating superior algorithms and overestimating inferior ones).

Method: The proposed method introduces a hypothetical middle algorithm (M) and estimates performance differences between algorithms A, M, and B in a stepwise manner using shared data at each step. This allows applying offline evaluation techniques within A/B testing to induce positive correlation and reduce critical selection errors.

Result: Experiments on real-world data show the proposed estimator achieves the same selection error rate as existing approaches while using only half of the A/B testing data. The paper also derives the optimal middle algorithm regarding variance and analyzes advantages through bias-variance analysis.

Conclusion: The paper challenges conventional wisdom by showing A/B testing can be less accurate than offline evaluation for algorithm selection. By intentionally inducing positive correlation through a stepwise estimation approach with a hypothetical middle algorithm, selection errors can be significantly reduced with less experimental data.

Abstract: A/B testing is the gold standard for selecting the better algorithm in online services. While offline evaluation has attracted attention as a safer alternative due to the high experimental costs and the potential risk of degrading user experience and revenue in A/B testing, it is widely recognized that the estimation accuracy of offline evaluation is substantially lower. As a result, final selection decisions are typically made through A/B testing. Contrary to this conventional view, we reveal a counterintuitive phenomenon in which A/B testing can produce a higher algorithm selection error rate than offline evaluation. This occurs because the sample mean estimator used in A/B testing does not induce positive correlation, which is crucial for reducing critical selection errors, namely underestimating the truly superior algorithm and overestimating the truly inferior one. In contrast, offline evaluation methods unintentionally generate this beneficial correlation by relying on shared offline data when estimating and comparing the performance of multiple algorithms. Building on this insight, we propose an estimator that intentionally induces positive correlation to improve algorithm selection in A/B testing. The key idea is to introduce a hypothetical middle algorithm and to estimate the performance difference between algorithms A, M, and B in a stepwise manner using shared data at each step. This approach enables the application of offline evaluation techniques in each step, thereby inducing positive correlation and reducing critical selection errors. Furthermore, we derive the optimal middle algorithm regarding the resulting variance and analyze its advantages over existing methods through bias-variance analysis. Experiments on real-world data demonstrate that our estimator achieves the same selection error rate as existing approaches while using only one half of the A/B testing data.

</details>


### [154] [Probabilistic Low-Voltage Peak Load Forecasting with Time Series Foundation Models Evaluated on Application-Oriented Metrics](https://arxiv.org/abs/2607.01966)
*Benedikt Kaas,Manuel Treutlein,Hannes Benedikt Gerber,Oliver Neumann,Cheewan Phatthanakhuha,Oliver Resch,Ralf Mikut,Veit Hagenmeyer*

Main category: cs.LG

TL;DR: Time series foundation models, particularly Chronos-2, outperform traditional methods for low-voltage load forecasting with better peak prediction and uncertainty estimation, which is crucial for grid planning and operation.


<details>
  <summary>Details</summary>
Motivation: Current low-voltage load forecasting methods have several limitations: they require significant manual effort, often lack uncertainty estimation and proper peak prediction, and are inadequately evaluated against grid requirements. With increasing electrification and decentralized generation, accurate forecasting is becoming increasingly important for energy systems.

Method: The study conducts an extensive evaluation of short-term net load forecasts for 200 real-world low-voltage feeders, focusing on time series foundation models. It compares Chronos-Bolt, Chronos-2, and TabPFN-TS against six baseline models, includes an ablation study omitting weather covariates to test model adaptation to uncertainty, and introduces a novel application-oriented metric linking forecasting performance to grid asset planning trade-offs.

Result: Time series foundation models demonstrate superior performance compared to traditional baseline methods, with Chronos-2 showing particularly strong results. The ablation study reveals that these models can adapt to increased uncertainty even without weather information. The novel metric successfully connects peak prediction capabilities to the trade-off between cost reduction and minimizing failure risk in grid asset planning.

Conclusion: Time series foundation models represent a significant advancement in low-voltage load forecasting, addressing key limitations of traditional methods by providing better uncertainty estimation, peak prediction, and grid-relevant performance evaluation, with Chronos-2 emerging as a particularly effective solution for real-world applications.

Abstract: Low-voltage load forecasting is an important component in current and future energy systems with a high degree of electrification and decentralized generation. However, current forecasting methods require significant manual effort, often lack uncertainty estimation and proper peak prediction, and they are often not adequately evaluated in terms of grid requirements. In the present study, we provide an extensive evaluation of short-term net load forecasts of 200 real-world low-voltage feeders with a focus on the rapidly evolving time series foundation models. Our study compares Chronos-Bolt, Chronos-2 and TabPFN-TS to six baseline models and demonstrates superior performance, in particular for Chronos-2. An ablation study, in which weather covariates are omitted, shows that time series foundation models adapt to increased uncertainty, despite the importance of weather information. A novel application-oriented metric links the model's forecasting capabilities in peak prediction to the trade-off in grid asset planning and operation between cost reduction and minimizing the risk of failure.

</details>


### [155] [Do Newer Lightweight CNNs Perform Better Under Resource Constraints? A Controlled Multigenerational Study of Architecture, Initialization, Training Budget, and Efficiency](https://arxiv.org/abs/2607.01984)
*Tasnim Shahriar*

Main category: cs.LG

TL;DR: The study provides a comprehensive comparison of nine lightweight CNN models on image classification tasks, finding that newer designs offer selective rather than universal improvements, with EfficientNet-B0 emerging as the most consistently competitive intermediate-budget option.


<details>
  <summary>Details</summary>
Motivation: There is a need for controlled evaluation of claims that newer lightweight CNN models improve both predictive performance and deployment efficiency, as these claims require systematic comparison under shared protocols.

Method: The study compares nine lightweight CNN model packages across CIFAR-10, CIFAR-100, and Tiny ImageNet datasets under a shared downstream protocol. It evaluates multiple metrics including accuracy (top-1, top-5, macro F1), efficiency metrics (parameter count, storage, GMACs, latency on different hardware), and memory usage.

Result: EfficientNetV2-S achieved highest accuracy on CIFAR-10 and CIFAR-100, while RepViT-M1.0 led on Tiny ImageNet. EfficientNet-B0 remained competitive with best models while using significantly fewer resources. MobileNetV3-Small was fastest and more accurate than MobileNetV4-Conv-S. Latency rankings differed sharply between GPU and CPU, showing GMACs don't predict inference performance.

Conclusion: Newer lightweight CNN designs provide selective rather than universal gains. EfficientNet-B0 emerges as the most consistently competitive intermediate-budget option, appearing on every accuracy-resource Pareto frontier. Performance depends heavily on hardware context, and scratch training still lags behind pretrained models.

Abstract: Newer lightweight convolutional neural networks are often presented as improving predictive performance and deployment efficiency, but such claims require controlled evaluation. This study compares nine lightweight CNN model packages across CIFAR-10, CIFAR-100, and Tiny ImageNet under a shared downstream protocol. We report top-1 accuracy, macro F1, top-5 accuracy, parameter count, FP32 storage, GMACs, batch-size-1 latency on an NVIDIA L4 and AMD Ryzen 5 5500U CPU, peak PyTorch CUDA allocated tensor memory, and point estimate Pareto frontiers. EfficientNetV2-S achieves the highest observed top-1 accuracy on CIFAR-10 and CIFAR-100 at 97.57% and 86.98%, while RepViT-M1.0 leads Tiny ImageNet at 79.87%. EfficientNet-B0 remains within 0.22, 0.85, and 1.79 percentage points of the best result on the three datasets while using approximately 79% fewer parameters and 86% fewer GMACs than EfficientNetV2-S. It also appears on every evaluated accuracy and resource Pareto frontier, making it the most consistently competitive intermediate-budget option. MobileNetV3-Small has the lowest GMAC count, is the fastest model under both CPU thread settings, and records higher observed accuracy than MobileNetV4-Conv-S on all three datasets. Under random initialization, it leads MobileNetV4-Conv-S by 2.55, 1.76, and 0.99 points, with paired test-set intervals excluding zero for the fixed trained models. EfficientNet-B0 remains 3.29, 10.10, and 17.54 points below its pretrained counterpart after 100 epochs of scratch training, despite requiring about five times the recorded training time. SqueezeNet1.1 has the fewest parameters and lowest peak CUDA allocation, but substantially weaker accuracy. Latency rankings differ sharply between the L4 and CPU environments, showing that GMACs alone do not predict measured inference performance. Overall, newer designs provide selective rather than universal gains

</details>


### [156] [Liquid Latent State Dynamics for Interpretable Turbofan Degradation Modeling](https://arxiv.org/abs/2607.01986)
*Weizhi Nie,Weijie Wang,Yuting Su*

Main category: cs.LG

TL;DR: Liquid neural networks with disentangled latent states (degradation vs. operating conditions) improve aircraft engine prognostics by creating interpretable degradation models with better sensor forecasting than GRU baselines.


<details>
  <summary>Details</summary>
Motivation: Current multivariate time-series models for prognostics focus on point prediction accuracy but lack coherent representations of the underlying degradation process, making it difficult to interpret health evolution separate from operating-condition variations.

Method: Proposed a liquid neural network model that encodes history windows into latent states, factorizes them into degradation and condition components, evolves states with liquid transition models, and decodes future sensor observations. Uses RUL, monotonic risk, and latent-consistency losses for degradation, plus condition prediction and decorrelation losses to prevent operating-condition leakage.

Result: On C-MAPSS benchmark (FD001-FD004), the disentangled model improved sensor forecasting RMSE from 0.2438 (GRU baseline) to 0.2266, with largest gains on multi-condition subsets. Learned degradation state formed clearer temporal degradation axis (average Spearman correlation 0.5960). RUL regression remained stronger for GRU baseline, indicating better interpretability than calibration.

Conclusion: Liquid latent dynamics can bridge predictive maintenance forecasting and inspectable health-state modeling by creating interpretable world models for degradation dynamics, though they currently serve better as interpretable degradation models than as calibrated lifetime regressors.

Abstract: Multivariate time-series models for prognostics are often evaluated by point prediction accuracy, yet their internal states rarely expose a coherent degradation process. We study liquid neural networks as latent dynamics models for aircraft engine health monitoring on the C-MAPSS benchmark. The proposed model encodes a history window into a latent state, evolves that state with a liquid transition model, and decodes future sensor observations. To separate health evolution from operating-condition variation, the latent state is factorized into degradation and condition components. Remaining useful life, monotonic risk, and latent-consistency losses supervise the degradation component, while condition prediction and decorrelation losses discourage operating-condition leakage. Across FD001--FD004, the full disentangled model improves overall sensor forecasting RMSE from 0.2438 for a GRU baseline to 0.2266, with the largest gains on the multi-condition subsets FD002 and FD004. The learned degradation state also forms a clearer temporal degradation axis, reaching an average state-speed Spearman correlation of 0.5960. Direct remaining-useful-life regression remains stronger for the GRU baseline, indicating that the proposed representation is currently more effective as an interpretable world model for degradation dynamics than as a calibrated lifetime regressor. These results suggest that liquid latent dynamics can bridge predictive maintenance forecasting and inspectable health-state modeling.

</details>


### [157] [Fast and Accurate Anomaly Detection in Time Series](https://arxiv.org/abs/2607.02046)
*Emanuele Mele,Massimo Cafaro,Angelo Coluccia,Italo Epicoco*

Main category: cs.LG

TL;DR: A novel unsupervised anomaly detection algorithm for time series using Haar discrete wavelet and a custom t-test, outperforming state-of-the-art methods across 343 datasets.


<details>
  <summary>Details</summary>
Motivation: Anomaly detection faces challenges due to class imbalance (rare anomalies) and scarcity of labeled anomalous data in supervised methods. Unsupervised methods avoid labeling costs but suffer from high false positive rates in safety-critical applications.

Method: Introduces an unsupervised algorithm based on Haar discrete wavelet transform combined with a specially designed t-test for anomaly detection in time series data.

Result: The proposed algorithm outperforms state-of-the-art unsupervised and self-supervised benchmarks, demonstrated through extensive experimentation across 343 datasets.

Conclusion: The Haar discrete wavelet with custom t-test provides an effective unsupervised solution for time series anomaly detection that addresses limitations of existing supervised and unsupervised approaches.

Abstract: Anomaly detection is a critical and evolving field in Machine Learning, with applications targeting different domains such as cybersecurity, finance, healthcare, manufacturing and IoT (Internet of Things) systems. Traditionally, anomaly detection algorithms have been designed using both supervised and unsupervised learning paradigms. The fundamental challenge in real-world anomaly detection scenarios is related to the inherent class imbalance (anomalies are typically rare) and, for supervised methods, to the scarcity of labelled anomalous data. Indeed, labelling is both expensive and time-consuming. Conversely unsupervised methods do not require labelling, but may suffer from high false positive rates when deployed in safety-critical applications. In this work we introduce a novel unsupervised algorithm for anomaly detection in time series based on the Haar discrete wavelet and a suitably designed $t$-test. We establish the theoretical foundation of the proposed $t$-test and, through extensive experimentation across 343 datasets, demonstrate that our algorithm outperforms state-of-the-art unsupervised and self-supervised benchmarks.

</details>


### [158] [A Memory Efficient Unified Algorithm for Online Learning of Linear Dynamical Systems](https://arxiv.org/abs/2607.02050)
*Yuval Ran-Milo,Angelos Assos,Elad Hazan*

Main category: cs.LG

TL;DR: This paper introduces an online prediction algorithm for linear dynamical systems with low instability complexity, achieving sublinear regret with a parameter count that scales with the instability complexity k rather than the full state dimension.


<details>
  <summary>Details</summary>
Motivation: The paper is motivated by the challenge of stabilizing unknown linear dynamical systems from observations. The authors study online prediction as a prerequisite for stabilization, focusing on systems with low instability complexity where stabilization is actually plausible. They show that systems with high instability complexity often cannot be stabilized without exponentially large controls, making prediction meaningful specifically when instability complexity is small.

Method: The authors introduce a unified online algorithm that handles every linear dynamical system (including non-diagonalizable systems with complex or exploding modes) with a learnable parameter count of $\widetilde{O}(k)$. Their approach focuses on systems in the low instability complexity regime, characterized by eigenvalues outside the real stable interval that don't decay rapidly, together with non-semisimple modes, potentially embedded in a higher-dimensional stable spectrum.

Result: The key results are: 1) A lower bound showing that any filter-based predictor needs at least k filters, establishing k as a valid complexity measure. 2) Experimental results showing that on a high-dimensional system, their predictor sharply outperforms prior methods at an equal parameter budget.

Conclusion: The paper establishes that online prediction for stabilization is most meaningful when instability complexity is small, provides a unified algorithm with parameter-efficient learning, proves k is a fundamental complexity measure for such systems, and demonstrates practical advantages over existing methods.

Abstract: Motivated by the challenge of stabilizing a general unknown linear dynamical system (LDS) from observations, we study the natural prerequisite of online prediction. Our goal is to achieve sublinear regret with a memory footprint that adapts to the intrinsic complexity of the dynamics rather than the full hidden -- state dimension. We focus on the practically central regime of systems with low instability complexity -- eigenvalues outside the real stable interval that do not decay rapidly, together with non-semisimple modes-potentially embedded in an otherwise stable real spectrum of much higher dimension; we write $k$ for this count. This regime is the primary setting in which stabilization is plausible: we show that many systems with high instability complexity cannot be stabilized without exponentially large controls. Thus, prediction is meaningful for stabilization precisely when the instability complexity is small. Within this regime, we introduce a unified online algorithm that handles every LDS (including non-diagonalizable systems with complex or exploding modes) with a learnable parameter count of $\widetilde{O}(k)$. Finally, we prove a lower bound showing that $k$ is a valid complexity measure: any filter-based predictor needs at least $k$ filters. Experiments corroborate our theory: on a high-dimensional system, our predictor sharply outperforms prior methods at an equal parameter budget.

</details>


### [159] [Beyond the Performance Illusion: Structure-Aware Stratified Partitioning and Curriculum Distributionally Robust Optimization for Spatially Correlated Domains](https://arxiv.org/abs/2607.02055)
*Prathamesh Patil,Arpit Jain,Aswanth Krishnan*

Main category: cs.LG

TL;DR: Spatiotemporally correlated datasets cause data leakage and hidden stratification in AI evaluation; proposed SASP prevents leakage and CDRO improves training stability under stricter splits.


<details>
  <summary>Details</summary>
Motivation: Traditional AI evaluation assumes i.i.d. random splits, but this breaks down in spatiotemporally correlated domains (aerial surveillance, precision agriculture, medical imaging), leading to two problems: data leakage (correlated samples across splits inflate performance) and hidden stratification (minority subpopulation errors obscured by aggregate metrics).

Method: Two components: 1) Structure-Aware Stratified Partitioning (SASP) - creates validation splits that reduce spatiotemporal leakage while maintaining class balance; 2) Curriculum Distributionally Robust Optimization (CDRO) - curriculum-based relaxation of distributionally robust training that stabilizes optimization under stricter splits.

Result: Across multiple benchmarks, the combined framework yields improved generalization, more reliable confidence calibration, and exposes failure modes that remain hidden under conventional random-split evaluation.

Conclusion: Conventional i.i.d. assumptions fail in spatiotemporally correlated domains; the proposed unified framework addresses these issues through careful split design and robust training methods, leading to more reliable AI system evaluation.

Abstract: Performance evaluation in AI systems commonly assumes that random dataset splits produce independent and identically distributed (i.i.d.) subsets. We show that this assumption often breaks down in spatiotemporally correlated domains such as aerial surveillance, precision agriculture, and medical imaging, leading to two systematic failures: data leakage, where correlated samples span training and validation splits and inflate performance estimates, and hidden stratification, where errors on minority subpopulations are obscured by aggregate metrics. To address these issues, we propose a unified evaluation and training framework for spatially correlated data. We introduce Structure-Aware Stratified Partitioning (SASP), which constructs validation splits that reduce spatiotemporal leakage while preserving meaningful class balance, and Curriculum Distributionally Robust Optimization (CDRO), a curriculum-based relaxation of distributionally robust training that stabilizes optimization under these stricter splits. Across multiple benchmarks, this combination yields consistently improved generalization, more reliable confidence calibration, and exposes failure modes that remain hidden under conventional random-split evaluation.

</details>


### [160] [SA-HGNN: Sample-Adaptive Hyperbolic Graph Neural Network for EEG-Based Depression Recognition](https://arxiv.org/abs/2607.02063)
*Yang Li,Pan Hu,Yan Zhang,Wenfan Yang,Tao Wu,Lianbo Guo*

Main category: cs.LG

TL;DR: SA-HGNN uses hyperbolic geometry and adaptive graph construction to better capture hierarchical functional connectivity patterns in depression-affected brain networks from EEG data.


<details>
  <summary>Details</summary>
Motivation: Existing GNNs struggle to capture accurate connection patterns in depression-affected brain networks due to their inherent hierarchical structure. Additionally, EEG signals contain redundant noise channels that interfere with authentic hierarchical topology detection.

Method: The paper proposes Sample-Adaptive Hyperbolic Graph Neural Network (SA-HGNN) with three modules: 1) Sample-Adaptive Graph Construction for personalized brain network topologies, 2) Hyperbolic graph convolution using hyperbolic geometry instead of Euclidean space to capture hierarchical relationships, and 3) Attention Pooling to filter redundant noise channels in EEG signals.

Result: Extensive experiments on public EEG datasets demonstrate superior performance across resting-state and task-related paradigms, showing robustness to noise and efficacy in capturing abnormal functional connectivity patterns in depression patients.

Conclusion: SA-HGNN effectively addresses the limitations of Euclidean GNNs by leveraging hyperbolic geometry and adaptive mechanisms to better capture the hierarchical structure of depression-affected brain networks, improving EEG-based depression recognition performance.

Abstract: Graph Neural Networks (GNNs) have been widely used to capture spatial functional connectivity patterns to improve electroencephalography (EEG)-based depression recognition performance. However, the functional connectivity of brain networks in patients with depression exhibits an inherent hierarchical structure, making it difficult to capture accurate connection patterns. To address these issues, this paper proposes a novel model named Sample-Adaptive Hyperbolic Graph Neural Network (SA-HGNN), which aims to accurately extract the authentic hierarchical structure of depression-affected brain networks. Specifically, the proposed model comprises three core modules. First, a Sample-Adaptive Graph Construction module dynamically constructs personalized brain network topologies to capture more complex spatial relationships within the brain network. Second, hyperbolic graph convolution is employed to overcome the representation bottlenecks of Euclidean space, leveraging hyperbolic geometry to precisely capture latent hierarchical relationships within the brain network. Finally, an Attention Pooling module adaptively filters out highly redundant noise channels in EEG signals, effectively mitigating the interference of inherent noise on the authentic hierarchical topology. Extensive experiments on public EEG datasets demonstrate the superior performance of our method across resting-state and task-related paradigms, validating its robustness to noise and efficacy in capturing abnormal functional connectivity patterns in brain networks of patients with depression.

</details>


### [161] [kNNGuard: Turning LLM Hidden Activations into a Training-Free Configurable Guardrail](https://arxiv.org/abs/2607.02072)
*Mahmoud Abdelfattah,Hamid Nasiri,Peter Garraghan*

Main category: cs.LG

TL;DR: kNNGuard is a training-free LLM guardrail that uses k-nearest neighbors on hidden activations and embeddings to detect unsafe/off-topic prompts, achieving competitive performance while being 10x faster than fine-tuned classifiers.


<details>
  <summary>Details</summary>
Motivation: Existing LLM guardrails rely on fine-tuned classifiers which suffer from low generalization, high inference latency, and require extensive training time. There's a need for faster, more adaptable guardrails that don't require fine-tuning.

Method: kNNGuard uses an off-the-shelf LLM's activation space with a small bank of 50 safe/unsafe prompts. It extracts hidden activations and performs multi-layer kNN classification that fuses activation-space and embedding-space scores. Domain adaptation only requires updating the labeled prompt bank.

Result: Across six domains, kNNGuard achieves competitive or superior F1 compared to fine-tuned state-of-the-art guardrails while running 2.7x faster than the best comparable guardrail and 10x faster than fine-tuned safety classifiers. Domain adaptation takes under 10 seconds to update the prompt bank.

Conclusion: kNNGuard provides a configurable, low-latency training-free alternative to fine-tuned guardrails that maintains competitive performance while significantly reducing inference time and enabling rapid domain adaptation through simple prompt bank updates.

Abstract: Large language models (LLMs) are increasingly deployed in domains requiring guardrails to detect unsafe, off-topic, or adversarial prompts. Existing guardrails predominately rely on fine-tuning to build classifiers, which often suffer from low generalization and high inference latency. We present kNNGuard, a training-free guardrail that utilizes the activation space of an off-the-shelf LLM. Given a small bank of 50 safe and unsafe prompts, kNNGuard extracts hidden activations and performs multi-layer kNN fusing activation-space and embedding-space scores for classification. Across six domains spanning topical and security prompts, kNNGuard achieves competitive or superior F1 compared to fine-tuned state-of-the-art guardrails while running 2.7x faster than the best comparable guardrail, and 10x faster than a fine-tuned safety classifier without gradient updates or fine-tuning. Domain adaptation requires only updating the labeled bank, which can be constructed in under 10 seconds and several orders of magnitude faster than established guardrails. We also analyze the impact of system prompts, layer selection, and integration into production LLM pipelines as a configurable, low-latency guardrail.

</details>


### [162] [Fourier Neural Operators for Rayleigh-Bénard Convection](https://arxiv.org/abs/2607.02088)
*Chelsea Maria John,Thibaut Lunet,Sebastian Götschel,Andreas Herten,Stefan Kesselheim,Daniel Ruprecht*

Main category: cs.LG

TL;DR: Improved FNO predicts time increments for 2D Rayleigh-Bénard convection, achieving higher accuracy than standard FNO with compact size (314k params) and fast inference (7ms).


<details>
  <summary>Details</summary>
Motivation: Standard FNOs for fluid dynamics may lack accuracy, and there's a need for compact, fast models for simulating complex phenomena like Rayleigh-Bénard convection.

Method: Developed an improved Fourier Neural Operator (FNO) that predicts time increments rather than full solutions for modeling 2D Rayleigh-Bénard convection.

Result: Achieved higher accuracy than standard FNO baseline with compact model size (314k parameters, 1.26 MB), fast inference (7 ms), and maintained accuracy comparable to previous benchmarks.

Conclusion: The improved FNO approach is effective for convection modeling, though accuracy remains limited by training data resolution despite FNOs' ability to generalize to finer meshes.

Abstract: We propose an improved Fourier Neural Operator (FNO) for modeling two-dimensional Rayleigh-Bénard convection by predicting time increments instead of full solutions, achieving higher accuracy than a standard FNO baseline. The resulting model is compact (314k parameters, 1.26 MB) and fast (7 ms inference), while maintaining similar accuracy as demonstrated in previous benchmarks. We show that although FNOs generalize to finer meshes, accuracy remains limited by the resolution of the training data.

</details>


### [163] [Ask the Right Comparison:Bias-Aware Bayesian Active Top-$k$ Ranking with LLM Judges](https://arxiv.org/abs/2607.02104)
*Jian Xu,Delu Zeng,John Paisley,Qibin Zhao*

Main category: cs.LG

TL;DR: LLMs as judges are noisy and biased (verbosity, position effects), but a Bayesian model with explicit bias covariates and topk-aware active acquisition can recover true rankings with fewer comparisons.


<details>
  <summary>Details</summary>
Motivation: LLMs are increasingly used as cheap, scalable judges for comparing outputs, but they are noisy and systematically biased (favor verbose/well-formatted answers, exhibit position effects). Simply aggregating votes recovers presentation quality rather than true quality.

Method: 1) Cast judging as Bayesian inference over latent quality with explicit, judge-specific bias covariates (verbosity, position), regularized by shrinkage priors; 2) Introduce topk-aware active acquisition rule that chooses comparisons to maximally reduce uncertainty about topk membership rather than full ranking.

Result: On a benchmark with 16 real LLMs (open and proprietary models), naive aggregation plateaus at wrong topk regardless of budget, while bias-aware model recovers it. Topk-aware acquisition reaches the ceiling with far fewer comparisons than round-robin or D-optimal rules. Bias is heterogeneous: cheap/mid-tier judges have strong verbosity bias (recall improves from ~0.5-0.6 to 0.84-1.0), frontier judges show little bias.

Conclusion: LLM judges exhibit real but heterogeneous biases. Bias-aware modeling with explicit covariates and topk-focused active acquisition can effectively correct for biases in non-frontier models and recover true rankings efficiently.

Abstract: Large language models (LLMs) are increasingly used as cheap, scalable judges that compare candidate outputs pairwise -- to rank responses, select models, or triage papers. Yet LLM judges are both noisy and systematically biased: they favor verbose or well-formatted answers and exhibit position effects, so simply aggregating their votes recovers a ranking of presentation, not of true quality. We study the practical goal of identifying the \topk{} items under a fixed comparison budget, and make two contributions. First, we cast judging as Bayesian inference over latent quality with explicit, judge-specific bias covariates (verbosity, position), regularized by a shrinkage prior so that the data decide which biases a given judge actually exhibits. Second, we introduce a \topk-aware active acquisition rule that chooses the next comparison to maximally reduce uncertainty about \topk{} \emph{membership}, rather than about the full ranking. On a controlled benchmark with known ground-truth quality, judged by sixteen real LLMs spanning open and proprietary families (Llama, Qwen, Phi-4, GPT-4o-mini/5.1/5.5, Gemini, DeepSeek, and Claude Haiku/Sonnet/Opus), naive aggregation plateaus at a wrong \topk{} on biased judges regardless of budget, while our bias-aware model recovers it; \topk-aware acquisition reaches this ceiling with far fewer comparisons than round-robin or a global-uncertainty (D-optimal) rule. Bias is real but heterogeneous and capability-dependent: cheap and mid-tier judges carry a strong verbosity bias that our model corrects (lifting recall from $\sim$$0.5$--$0.6$ to $0.84$--$1.0$), whereas the frontier judges we tested show little bias and already rank accurately, so bias-aware modeling changes little there.

</details>


### [164] [Predictive Conformal Slip Monitoring: An Empirical Evaluation of Rolling Split Conformal Prediction for Pre-Incident Traction Loss Detection](https://arxiv.org/abs/2607.02124)
*Varshith Roy Kotla*

Main category: cs.LG

TL;DR: Rolling Split Conformal Prediction fails as an early warning system for traction loss in racing telemetry, showing zero precision/recall against ground truth incidents and high false alarm rates due to violated statistical assumptions.


<details>
  <summary>Details</summary>
Motivation: Current traction control systems only intervene after traction loss occurs. The paper investigates whether statistical anomaly detection using Rolling Split Conformal Prediction could provide pre-incident warnings before gross traction loss happens in racing scenarios.

Method: Researchers used Rolling Split Conformal Prediction to monitor volatility of non-conformity residuals from per-driver Random Forest models of expected slip behavior. They corrected previous methodological issues by properly including vehicle speed as an explicit feature, using all racing laps (not just fastest), and scoring against real incident labels from FIA Race Control Messages and track-limits deletions.

Result: Negative results: Across 19 drivers and 55,563 test samples, the detector achieved mean precision of essentially 0.0 and recall of 0.0 against 14 ground-truth incidents, while flagging 15.3% of samples as anomalous. Residual autocorrelation showed violation of exchangeability assumptions (Ljung-Box p < 0.001 for all drivers), explaining high false alarm rates.

Conclusion: Rolling Split Conformal Prediction fails as an early warning system for traction loss due to violated statistical assumptions. The paper presents a rigorous negative finding with methodological improvements over previous work, and outlines requirements for a genuinely predictive version of this approach.

Abstract: Conventional traction control architectures intervene only after the adhesion limit of a tire has already been breached. This paper investigates whether Rolling Split Conformal Prediction , monitoring the volatility of non-conformity residuals from a per-driver Random Forest model of expected slip behavior , can serve as a statistically grounded pre-incident warning signal, ahead of gross traction loss. Unlike an earlier internal draft of this work, the evaluation reported here corrects a confound in the slip proxy (vehicle speed is included as an explicit model feature, not left implicit in the target's denominator), uses every racing lap for each driver rather than only the fastest lap, and is scored against real, timestamped incident labels extracted from FIA Race Control Messages and track-limits lap deletions rather than narrated post-hoc. The result is negative: across 19 drivers and 55,563 test-phase telemetry samples, the rolling-volatility detector achieves a mean precision of essentially 0.0 and mean recall of 0.0 against 14 ground-truth incidents, while flagging on average 15.3% of all samples as anomalous , too high a false-alarm rate for any early-warning use. A static 95th-percentile threshold baseline performs no better in any way that would justify the added complexity of the conformal-volatility formulation. Residual autocorrelation diagnostics show the split-conformal exchangeability assumption is violated for every driver (Ljung-Box p < 0.001, n = 19/19), which is one plausible driver of the high false-alarm rate. We report this as a methodologically rigorous negative finding, diagnose its likely causes, and outline what a genuinely predictive version of this approach would require.

</details>


### [165] [ART for Diffusion Sampling: Continuous-Time Control and Actor-Critic Learning](https://arxiv.org/abs/2607.02137)
*Yilie Huang,Wenpin Tang,Xun Yu Zhou*

Main category: cs.LG

TL;DR: ART-RL learns adaptive timestep schedules for diffusion models via continuous-time reinforcement learning, improving sample quality without changing the underlying sampling pipeline.


<details>
  <summary>Details</summary>
Motivation: Standard uniform or hand-crafted timestep schedules for score-based diffusion sampling are fixed prescriptions that can be suboptimal, creating a need for learned adaptive schedules.

Method: Proposes Adaptive Reparameterized Time (ART) - a continuous-time control formulation that treats sampling clock speed as control, with ART-RL as an auxiliary randomized formulation using Gaussian policies and continuous-time reinforcement learning for schedule optimization.

Result: ART-RL improves sample quality over strong baseline schedules across various experiments (low-dimensional settings to image generation) at matched computational budgets, with learned schedules showing broad generalization across different conditions.

Conclusion: ART-RL provides a principled, plug-and-play approach to adaptive timestep allocation for diffusion models that consistently improves performance while maintaining transferability across various settings.

Abstract: We study timestep allocation for score-based diffusion sampling, where a learned reverse-time dynamics is discretized on a finite grid. Uniform and hand-crafted schedules are standard choices, but they rely on fixed prescriptions and can therefore be suboptimal. To address this limitation, we propose Adaptive Reparameterized Time (ART), a continuous-time control formulation that learns a time change by treating the speed of the sampling clock as the control, so that a uniform grid on the learned clock induces adaptive timesteps in the original diffusion time. Based on a leading-order Euler error surrogate, ART provides a principled objective for allocating timesteps along the sampling trajectory. To solve this deterministic control problem, we introduce ART-RL, an auxiliary randomized formulation with Gaussian policies that turns schedule learning into a continuous-time reinforcement learning problem. We prove that the randomized ART-RL formulation is equivalent to ART at the optimizer level, in the sense that its optimal Gaussian policy recovers the optimal ART time-warping rate through its mean. We further establish policy evaluation and policy improvement characterizations and derive trajectory-based moment identities that yield implementable actor--critic updates for learning the schedule. Across experiments ranging from controlled low-dimensional settings to image generation, ART-RL can be plugged into existing diffusion samplers by changing only the timestep grid, consistently improving sample quality over strong baseline schedules at matched budgets while leaving the rest of the sampling pipeline unchanged. The learned schedules also exhibit broad generalization, transferring without retraining across sampling budgets, datasets, solvers, pipelines, and representation spaces.

</details>


### [166] [Probing Chemical Language Models: Effects of Pre-training and Fine-tuning](https://arxiv.org/abs/2607.02140)
*Anna Karnysheva,Dietrich Klakow,Ji-Ung Lee*

Main category: cs.LG

TL;DR: CLMs trained on SMILES encode molecular substructures, with pre-training improving structure awareness and fine-tuning affecting task-relevant substructures more than others.


<details>
  <summary>Details</summary>
Motivation: Chemical language models (CLMs) use linearized representations like SMILES, but it's unclear which chemically meaningful substructures they actually encode. There's a need to better understand what molecular substructures CLMs learn during training.

Method: The authors conducted a systematic study probing for 78 molecular substructures across eight pre-trained and six randomly initialized models. They examined how fine-tuning on chemical downstream tasks affects learned representations of molecular substructures.

Result: Pre-training generally improves molecular structure awareness in CLMs, particularly in upper layers. Randomly initialized models already encode ring structures well in the first layer. Fine-tuning on downstream tasks affects task-relevant molecular substructures more than others, indicating representation changes follow chemical theory.

Conclusion: CLMs do encode chemically meaningful substructures, with pre-training enhancing this capability and fine-tuning selectively modifying representations of task-relevant substructures, demonstrating alignment with chemical theory.

Abstract: Chemical language models (CLMs) are trained with linearized representations such as SMILES, yet it remains unclear which chemically meaningful substructures they encode. To foster a better understanding of CLMs, we conduct a systematic study and probe for 78 molecular substructures across eight pre-trained and six randomly initialized models. We furthermore study how fine-tuning on chemical downstream tasks affects the learned representations of molecular substructures. Our results show that pre-training generally improves molecular structure awareness of CLMs, particularly in the upper layers. Moreover, randomly initialized models already encode ring structures well in the first layer. Our analysis on two chemical downstream tasks further reveals that, interestingly, fine-tuning affects task-relevant molecular substructures more than others, indicating that the changes in the representations follow chemical theory.

</details>


### [167] [Predicting Early Stages Of Alzheimer's Disease And Identifying Key Biomarkers Using Deep Artificial Neural Network And Ensemble Of Machine Learning Methodologies](https://arxiv.org/abs/2607.02142)
*Debopriya Ghosh*

Main category: cs.LG

TL;DR: This paper proposes a machine learning approach for early Alzheimer's disease detection using clinical, neuropsychological, and neuroimaging data from ADNI, addressing data issues with iterative imputation and SMOTE, and comparing ensemble models with ANN.


<details>
  <summary>Details</summary>
Motivation: Alzheimer's disease is a progressive brain disorder with no cure, where early detection is crucial for better management but often delayed due to mild early symptoms resembling normal aging. Current diagnosis methods often identify the disease too late.

Method: Used ADNI dataset with clinical, neuropsychological, and neuroimaging data; applied iterative imputation for missing values, Borderline SVM-SMOTE for class imbalance, wrapper-based and embedded feature selection, feature scaling, and developed stacking ensemble (Logistic Regression, Extra Trees, Bagging KNN, LightGBM) plus ANN.

Result: Performance compared using precision, recall, F1-score, and AUC-ROC metrics; identified best classifier and important biomarkers for early Alzheimer's diagnosis.

Conclusion: Machine learning models can effectively detect early Alzheimer's disease, with feature selection helping identify important biomarkers; this approach could improve early diagnosis and management strategies.

Abstract: Alzheimers disease (AD) is a brain disorder that develops slowly and mainly affects memory, thinking, language, and daily activities. It is one of the most common causes of dementia and creates many difficulties for patients as well as their families. In the early stage, the symptoms are often mild and may look like normal ageing. For this reason, many people are diagnosed late, when the disease has already progressed. At present, there is no complete cure for AD. Still, early detection can help doctors manage the condition better and take suitable steps at the right time. In this study, a machine learning model is proposed to detect the early stages of Alzheimers disease using clinical details, neuropsychological test scores, and neuroimaging-related measures. The data used in this work is collected from the Alzheimers Disease Neuroimaging Initiative (ADNI). As the dataset has missing values, iterative imputation is applied to fill them. The dataset also has class imbalance, which is handled using Borderline SVM-SMOTE. After that, feature selection is carried out using wrapper-based and embedded methods so that only important features are used for training. The selected features are divided into training and testing sets, and feature scaling is applied. A stacking ensemble model is developed using Logistic Regression, Extra Trees, Bagging KNN, and LightGBM as base classifiers. Along with this, an artificial neural network is also trained on the same dataset. The performance of these models is compared using precision, recall, F1-score, and AUC-ROC. This study aims to find the best classifier and also identify important biomarkers that may help in the early diagnosis of Alzheimers disease.

</details>


### [168] [Dynamic Neural Graph Encoding of Inference Processes in Deep Weight Space](https://arxiv.org/abs/2607.02166)
*Di Wu,Huan Liu,Zhixiang Chi,Yuanhao Yu,Konstantinos N. Plataniotis,Yang Wang*

Main category: cs.LG

TL;DR: A novel dynamic graph approach (DNG-Encoder) represents neural network parameters as temporal graphs to capture sequential inference dynamics, enabling improved analysis of implicit neural representations with ~10% accuracy gains on INR classification.


<details>
  <summary>Details</summary>
Motivation: Existing methods for analyzing neural network weight spaces often ignore the sequential, layer-by-layer nature of neural network inference, making it challenging to efficiently handle these high-dimensional weight spaces.

Method: Proposes using dynamic graphs to represent neural network parameters, capturing temporal dynamics of inference through Dynamic Neural Graph Encoder (DNG-Encoder), and develops INR2JLS (Implicit Neural Representation to Joint Latent Space) for downstream applications like INR classification.

Result: Demonstrates significant improvements across multiple tasks, surpassing state-of-the-art INR classification accuracy by approximately 10% on CIFAR-100-INR dataset.

Conclusion: Dynamic graph representation effectively captures the sequential nature of neural processing in weight spaces, leading to substantial performance gains in analyzing and classifying implicit neural representations.

Abstract: The rapid advancements in using neural networks as implicit data representations have attracted significant interest in developing machine learning methods that analyze and process the weight spaces of other neural networks. However, efficiently handling these highdimensional weight spaces remains challenging. Existing methods often overlook the sequential nature of layer-by-layer processing in neural network inference. In this work, we propose a novel approach using dynamic graphs to represent neural network parameters, capturing the temporal dynamics of inference. Our Dynamic Neural Graph Encoder (DNG-Encoder) processes these graphs, preserving the sequential nature of neural processing. Additionally, we also leverage DNG-Encoder to develop INR2JLS (Implicit Neural Representation to Joint Latent Space) for facilitate downstream applications, such as classifying Implicit Neural Representations (INRs). Our approach demonstrates significant improvements across multiple tasks, surpassing the state-of-the-art INR classification accuracy by approximately 10% on the CIFAR-100-INR.

</details>


### [169] [Bayesian Sparse Low-Rank Adaptation for Large Language Model Uncertainty Estimation](https://arxiv.org/abs/2607.02182)
*Jijie Zhang,Zhe Ren,Quan Zhang,Dandan Guo*

Main category: cs.LG

TL;DR: DALorRA is a variational Bayesian sparse framework that improves LLM calibration by applying stochastic masking to LoRA's rank dimensions for better uncertainty quantification without sacrificing reasoning accuracy.


<details>
  <summary>Details</summary>
Motivation: LLMs show strong reasoning capabilities but suffer from overconfidence during task-specific fine-tuning, which hinders trustworthy deployment. The paper addresses the need for better uncertainty quantification in fine-tuned LLMs to enable more reliable real-world applications.

Method: Proposes Data-Adaptive Lower-Rank Adaptation (DALorRA), a variational Bayesian sparse framework that shifts uncertainty quantification from dense parameter space to the rank level of LoRA. It applies stochastic masking on rank dimensions, allowing Bayesian regularization during training and ensemble-like calibration during inference by treating each rank dimension as a probabilistic component.

Result: Extensive experiments show DALorRA achieves excellent calibration of LLMs while maintaining reasoning accuracy. The method effectively addresses overconfidence issues without compromising the models' core reasoning capabilities.

Conclusion: DALorRA provides a simple yet effective solution for uncertainty quantification in fine-tuned LLMs by leveraging the rank structure of LoRA, enabling more trustworthy deployment through better calibration while preserving reasoning performance.

Abstract: Large language models (LLMs) exhibit remarkable reasoning capabilities, but their task-specific fine-tuning is notoriously plagued by overconfidence, severely hindering trustworthy deployment. We propose Data-Adaptive Lower-Rank Adaptation (DALorRA), a simple and effective variational Bayesian sparse framework that shifts the paradigm of uncertainty quantification from the dense parameter space to the lightweight rank level of low-rank adaptation (LoRA). With the insight that LoRA essentially aggregates multiple rank-one components that may provide superfluous model capacity, DALorRA imposes stochastic masking on rank dimensions, enabling Bayesian regularization of model capacity during training and ensemble-like calibration during inference. Extensive experiments demonstrate DALorRA's excellent calibration of LLMs without compromising reasoning accuracy.

</details>


### [170] [Privacy-Preserving and Verifiable Approximate Distributed Coded Computing](https://arxiv.org/abs/2607.02187)
*Xavier Martínez-Luaña,Alba Gude-Santos,Manuel Fernández-Veiga,Rebeca P. Díaz-Redondo*

Main category: cs.LG

TL;DR: A unified model-agnostic framework combining privacy-preserving coded computing with adversary-resistant strategies for both federated and decentralized learning settings.


<details>
  <summary>Details</summary>
Motivation: Distributed machine learning faces privacy leakage and malicious manipulation threats. Existing defenses address these in isolation and are tailored to specific paradigms or architectures, limiting real-world applicability. Federated and decentralized learning have distinct adversarial surfaces rarely addressed together.

Method: Proposes a model-agnostic framework combining GPBACC (privacy-enhancing coded computing) with paradigm-specific defense mechanisms. For federated learning: robust aggregation strategies. For decentralized learning: approximate decode-and-compare with group testing for lightweight verification and adversary isolation without trusted aggregators.

Result: Empirical evaluation through attack-driven analysis shows that combining GPBACC with robust aggregation and verification mechanisms significantly reduces privacy leakage and improves resilience against active adversaries.

Conclusion: Privacy-enhancing coded computing, when combined with appropriate adversary-resistance strategies, provides a practical and deployable foundation for secure distributed machine learning across different paradigms.

Abstract: Distributed machine learning enables collaborative model training without centralizing data, but it also exposes learning processes to privacy leakage and malicious manipulation. Existing defenses typically address these threats in isolation and are often tailored to specific learning paradigms or model architectures, limiting their applicability in realistic deployments. In particular, federated learning and decentralized learning exhibit distinct adversarial surfaces that are rarely addressed within a unified framework. In this paper, we present a model-agnostic framework for adversary-resistant distributed learning that jointly addresses privacy preservation and malicious behavior across both federated and decentralized settings. Our approach combines paradigm-specific defense mechanisms with GPBACC, a privacy-enhancing coded computing technique applicable to arbitrary machine learning models. For federated learning, we integrate robust aggregation strategies to mitigate the impact of malicious participants, while for decentralized learning we employ approximate decode-and-compare and group testing techniques to enable lightweight verification and adversary isolation without relying on a trusted aggregator. Crucially, we evaluate the proposed framework through an explicit, attack-driven analysis. We implement representative privacy attacks and malicious behaviors, and empirically demonstrate that the combination of GPBACC with robust aggregation and verification mechanisms significantly reduces privacy leakage and improves resilience against active adversaries. These results suggest that privacy-enhancing coded computing, when combined with appropriate adversary-resistance strategies, provides a practical and deployable foundation for secure distributed machine learning.

</details>


### [171] [An Optimisation Framework for the Well-Conditioned Training of Physics-Informed Neural Networks](https://arxiv.org/abs/2607.02194)
*Joseph Webb,Sadok Jerad,Coralia Cartis*

Main category: cs.LG

TL;DR: DSGNAR (Doubly-Sketched Gauss-Newton with Adaptive Ratio) is a novel second-order optimization framework for PINNs that achieves unprecedented accuracy and speed by addressing the ill-conditioned loss landscape problem, outperforming state-of-the-art methods by several orders of magnitude across various PDE problems.


<details>
  <summary>Details</summary>
Motivation: Physics-informed neural networks (PINNs) struggle to reach the precision of classical PDE solvers due to severely ill-conditioned loss landscapes, which pose significant optimization challenges.

Method: DSGNAR combines a doubly-sketched Gauss-Newton model with a novel strategy that carefully controls both regularization and step length. This second-order optimization framework addresses ill-conditioning through adaptive regularization and step control mechanisms.

Result: The framework achieves remarkable improvements: relative ℓ₂ errors as low as 3×10⁻¹⁶ in double precision, improves contemporary results by 5 orders of magnitude on Burgers' equation and 8 orders on high-dimensional Poisson problems while being faster. In single precision, it solves Burgers' equation to 4.75×10⁻⁷ in under 10 seconds. It's robust to architecture, precision, and hyperparameters.

Conclusion: DSGNAR represents a significant advancement in PINN optimization, overcoming the ill-conditioning bottleneck and enabling PINNs to achieve precision comparable to classical solvers while maintaining computational efficiency and robustness.

Abstract: Physics-informed neural networks (PINNs) have emerged as a promising route to solve partial differential equations, yet they have struggled to reach the precision of classical solvers. The obstacle is increasingly understood to be one of optimisation, owing to the severely ill-conditioned loss landscape. We present $\textbf{DSGNAR}$: Doubly-Sketched Gauss-Newton with Adaptive Ratio, a scalable second-order optimisation framework that confronts this ill-conditioning and, in doing so, obtains unprecedented accuracy and speed. $\textbf{DSGNAR}$ couples a doubly-sketched Gauss-Newton model with a novel strategy that carefully controls both regularisation and step length. Across a suite of problems spanning nonlinear, chaotic, multi-scale, high-dimensional, and Navier-Stokes, the framework greatly improves on the state of the art: able to attain relative $\ell_2$ errors as low as $3\times10^{-16}$ in double precision, improve contemporary results by five orders of magnitude on the canonical Burgers' equation, and as much as eight orders on a high-dimensional Poisson problem, while remaining markedly faster. We further show that, in single precision, solutions at the limit of round-off error can be obtained very quickly: Burgers' equation to $\ell_2^{\text{rel}} = 4.75 \times 10^{-7}$ in under ten seconds. The framework is also robust to the choice of architecture, arithmetic precision, and initial hyperparameters.
  The code is available at https://www.github.com/wephy/physics-informed-neural-networks

</details>


### [172] [Online Resource Allocation with Continuous Random Consumption: Regret under Degeneracy](https://arxiv.org/abs/2607.02196)
*Jiawei Zhang*

Main category: cs.LG

TL;DR: Online resource allocation algorithm achieving sublinear regret without fluid non-degeneracy assumptions by analyzing value-to-size ratio distributions near acceptance cutoffs.


<details>
  <summary>Details</summary>
Motivation: Most online resource allocation problems assume deterministic fluid relaxations are non-degenerate, but real-world problems often have continuously distributed rewards and consumption sizes, leading to potential degeneracy. The authors want to develop algorithms that work even when the deterministic fluid relaxation is degenerate.

Method: The paper introduces an active weighted-mass exponent p that measures how much mass of requests lies near the acceptance cutoff (value-to-size ratios close to active thresholds). They analyze different distribution scenarios for rewards and sizes, then propose a sample-path marginal policy that adapts to the exponent p.

Result: When p > 1 (thin mass near cutoff), every online policy must incur Ω(T^{1/2 - 1/(2p)}) regret, and their policy achieves O(T^{1/2 - 1/(2p)} log^c T) regret. When p = 1 (linear mass near cutoff), their policy achieves O((log T)^2) regret. This works without fluid non-degeneracy assumptions, handling both primal degeneracy and dual non-uniqueness.

Conclusion: The key insight is that regret in online resource allocation is governed by the mass of requests near acceptance cutoffs, quantified by exponent p. Their sample-path marginal policy achieves near-optimal regret across different distribution scenarios without requiring traditional non-degeneracy assumptions.

Abstract: We study online resource allocation when both rewards and consumption sizes may be continuously distributed. Requests arrive sequentially and must be accepted or rejected irrevocably under fixed resource capacities. Each request belongs to one of finitely many observable types; conditional on an observable request type, both the reward and the scalar size are random, and the realized size scales a fixed type-specific resource-consumption vector. The model allows the deterministic fluid relaxation to be degenerate.
  We show that additive regret is governed by the size-weighted mass of requests whose value-to-size ratios lie near the active acceptance cutoffs. We formalize this quantity through an active weighted-mass exponent p. When p > 1, this cutoff mass is thin, and the problem is genuinely hard: every online policy must incur regret of order at least $T^{1/2 - 1/(2p)}$, and this holds for every p > 1. A sample-path marginal policy matches this lower bound up to polylogarithmic factors; and when p = 1, so that the mass grows linearly near the cutoff, it attains $O((\log T)^2)$ regret. For example, if the size and the value-to-size ratio are independent and uniformly distributed, then p = 1; if instead the size and the reward are independent and uniformly distributed, then p = 2. Thus the policy achieves $o(\sqrt{T})$ regret throughout this regularity class without any fluid non-degeneracy assumption, allowing both primal degeneracy and dual non-uniqueness.

</details>


### [173] [Self-explainable Operator Learning for Discovering Spatial Patterns in Functional Data](https://arxiv.org/abs/2607.02203)
*Mojgan Alishiri,Amirhossein Arzani*

Main category: cs.LG

TL;DR: Self-explainable operator learning framework using additive decomposition of integral equations to provide direct interpretability by linking specific input regions to output patterns.


<details>
  <summary>Details</summary>
Motivation: Current operator learning models are opaque neural network-based architectures that obscure reasoning behind predictions, lacking interpretability which is crucial for scientific applications.

Method: Reformulate operator learning as linear combination of generalized functional linear models expressed through integral equations, then decompose input domain into subdomains to compute localized integrals evaluating each region's contribution.

Result: Demonstrated on fluid flow problems (blood flow and unsteady aerodynamics), the model prioritizes regions with strong feature gradients, providing physically meaningful insights. Agrees with post-hoc methods but has embedded explainability.

Conclusion: Proposed framework provides mathematically transparent and physically interpretable approach that embeds explainability directly within operator structure, fostering trust in ML for scientific applications without requiring external tools.

Abstract: Operator learning has emerged as a powerful tool for modeling complex physical systems in functional spaces. However, their neural network-based architectures make them opaque models, obscuring the reasoning behind their predictions. In this work, we introduce a self-explainable operator learning framework that overcomes this challenge by reformulating operator learning as a linear combination of generalized functional linear models expressed through integral equations. Exploiting the additive decomposability of these integral equations, we divide the input domain into subdomains and compute localized integrals to evaluate the contribution of each region to the final prediction. This decomposition enables direct interpretability where the model explains both inputs and outputs by linking specific input regions to corresponding output patterns, thereby revealing which spatial features drive predictions. We demonstrate the framework on function-to-scalar and function-to-function mappings in fluid flow problems involving blood flow and unsteady aerodynamics. The results show that the operator most often prioritizes regions with strong feature gradients, providing physically meaningful insight into the model's decision-making process. Comparisons with established post-hoc explainability methods demonstrate qualitative agreement while highlighting the key advantage of the proposed approach: explainability is embedded directly within the operator structure itself and does not require an external tool. Therefore, our framework provides a mathematically transparent and physically interpretable approach to uncover relationships within data, fostering trust in machine learning for scientific applications by enabling more informed data-driven analysis of physical systems.

</details>


### [174] [HERMES: A Multi-Granularity Labeling Substrate for Pre-training Data Mixtures](https://arxiv.org/abs/2607.02266)
*Ziyun Qiao,Yue Min,Ruining Chen,Yujun Li*

Main category: cs.LG

TL;DR: HERMES is a hierarchical data labeling system that enables granularity control for data mixing, outperforming fixed-label approaches by allowing dynamic resolution adjustment during training.


<details>
  <summary>Details</summary>
Motivation: Existing data-mixing methods are limited by fixed label systems (provenance, topic taxonomies, flat clusters) that commit to single semantic axes and granularities. Changing resolution requires rebuilding labels, creating a bottleneck that constrains mixer expressiveness.

Method: HERMES uses a Learned Semantic Transform followed by 3-stage residual vector quantization to annotate each document into a coarse-to-fine code. The prefix length controls granularity (up to ~130k cells), creating a hierarchical labeling substrate.

Result: At coarse granularity, HERMES performs comparably to KMeans on standard clustering metrics. In 1B-parameter, 25B-token pre-training, hierarchical granularity revealed interactions inaccessible to fixed pipelines: a Stage-2 rule contrast (equal-subbucket coverage vs size-proportional quality) boosted 16-task capability by +0.0253 at one granularity, but lost effect at finer levels due to 5x smaller candidate pools.

Conclusion: HERMES transforms data mixture design from selecting among fixed label sets to navigating a reusable, data-derived granularity hierarchy, enabling more flexible and effective data mixing strategies.

Abstract: Most data-mixing methods assume the corpus has already been partitioned into groups, and the choice of those groups determines what a mixer can express. Existing labels, including provenance, topic or format taxonomies, and flat embedding clusters, commit to one semantic axis at one granularity; changing the resolution rebuilds the labels. We argue the bottleneck is the label system, not the mixer, and provide a hierarchical one. HERMES is a data-derived labeling substrate: a Learned Semantic Transform followed by 3-stage residual vector quantization annotates each document once into a coarse-to-fine code whose prefix length controls granularity up to approximately 130k cells. At coarse granularity HERMES sits at a plateau with KMeans-family methods on standard clustering metrics, so the contribution is the substrate, not the clusterer. On 1B-parameter, 25B-token pre-training, the hierarchy exposes an interaction fixed-granularity pipelines cannot test: at one prefix length, a combined Stage-2 rule contrast, equal-subbucket coverage versus size-proportional within-bucket quality top-30%, lifts a 16-task capability macro-average by +0.0253; at the next finer level, the same rule loses its measurable edge as candidate pools contract approximately 5x. HERMES reframes data mixture design from choosing among fixed label sets to navigating a reusable, data-derived granularity hierarchy.

</details>


### [175] [Generalization in offline RL: The structure is more important than the amount of pessimism](https://arxiv.org/abs/2607.02288)
*Max Weltevrede,Matthijs T. J. Spaan,Wendelin Böhmer*

Main category: cs.LG

TL;DR: Pessimism in offline RL doesn't inherently prevent generalization; what matters is whether pessimism respects underlying symmetries of optimal solutions, not its magnitude. Data augmentation during policy extraction is better than during training.


<details>
  <summary>Details</summary>
Motivation: While pessimism in offline RL counters overestimation bias, excessive conservatism has been associated with hindering certain forms of generalization. However, there's a need to understand what truly determines generalization success in contextual MDPs beyond just the amount of pessimism.

Method: Theoretical analysis showing that symmetric pessimistic value functions generalize better than non-symmetric ones, regardless of pessimism magnitude. Proposes using data augmentation through a consistency loss during policy extraction rather than during regular offline training. Empirically validates using IQL and CQL on rotationally symmetric reacher environment.

Result: Proves that mildly pessimistic non-symmetric value functions can generalize worse than overly pessimistic symmetric ones. Shows that generalization depends on whether pessimistic structure respects optimal solution symmetries, not on pessimism amount. Demonstrates that data augmentation applied during policy extraction via consistency loss works better than augmentation during training.

Conclusion: Successful generalization in offline RL depends on the structure of pessimism (whether it respects symmetries) rather than its magnitude. The structure of dataset coverage determines pessimism structure, and data augmentation should be applied through consistency loss during policy extraction rather than during offline training for optimal results.

Abstract: While pessimism counteracts overestimation bias in offline reinforcement learning (RL), being overly conservative has been associated with hindering certain forms of generalization. However, in this paper we demonstrate that being overly pessimistic does not inherently prevent optimal generalization in contextual MDPs (CMDPs). Instead, we argue successful generalization depends not on the amount of pessimism, but whether the pessimistic structure respects the underlying symmetries of the optimal solution. We prove that a mildly pessimistic, non-symmetric value function can generalize worse than an overly pessimistic, symmetric one. In offline RL, the structure of the pessimism is determined by the structure of the dataset coverage. As such, enforcing a symmetric value function can be non-trivial, and might require techniques such as data augmentation (DA). Inspired by our theoretical results, we argue that DA can best be applied through a consistency loss during policy extraction, rather than the common practice of (regular) offline training on an augmented dataset. This is empirically validated using IQL and CQL on a rotationally symmetric reacher environment.

</details>


### [176] [Optimizing Visual Generative Models via Distribution-wise Rewards](https://arxiv.org/abs/2607.02291)
*Ruihang Li,Mengde Xu,Shuyang Gu,Leigang Qu,Fuli Feng,Han Hu,Wenjie Wang*

Main category: cs.LG

TL;DR: A novel reinforcement learning framework for visual generation that uses distribution-wise rewards instead of sample-wise rewards to prevent reward hacking and mode collapse, while improving image quality and diversity through efficient subset-replace strategy and model merging optimization.


<details>
  <summary>Details</summary>
Motivation: Conventional RL for visual generation uses sample-wise reward functions which often lead to reward hacking, degrading image diversity and introducing visual anomalies. This creates problems with mode collapse when all samples independently optimize in the same direction.

Method: Proposes finetuning generative models using distribution-wise rewards that account for data distribution rather than individual samples. Uses subset-replace strategy to efficiently compute rewards by updating only small subsets of generated reference sets. Also applies RL to optimize post-hoc model merging coefficients to address train-inference inconsistency caused by stochastic differential equations.

Result: Extensive experiments show significant improvements: FID-50K improves from 8.30 to 5.77 for SiT and from 3.74 to 3.52 for EDM2 across various base models. Qualitative evaluation confirms enhanced perceptual quality while preserving sample diversity.

Conclusion: The distribution-wise reward framework effectively addresses limitations of sample-wise RL approaches by preventing reward hacking and mode collapse, resulting in better alignment with real-world data distributions, improved image quality metrics, and preserved diversity.

Abstract: Conventional reinforcement learning strategies for visual generation typically employ sample-wise reward functions, yet this practice frequently results in reward hacking that degrades image diversity and introduces visual anomalies. To address these limitations, we present a novel framework that finetunes generative models using distribution-wise rewards, ensuring better alignment with real-world data distributions. Unlike rewards that evaluate samples individually, distribution-wise reward accounts for the data distribution of the samples, mitigating the mode collapse problem that occurs when all samples optimize towards the same direction independently. To overcome the prohibitive computational cost of estimating these rewards, we introduce a subset-replace strategy that efficiently provides reward signals by updating only a small subset of a generated reference set. Additionally, we apply RL to optimize post-hoc model merging coefficients, potentially mitigating the train-inference inconsistency caused by introducing stochastic differential equation (SDE) in regular RL practices. Extensive experiments show our approach significantly improves FID-50K across various base models, from 8.30 to 5.77 for SiT and from 3.74 to 3.52 for EDM2. Qualitative evaluation also confirms that our method enhances perceptual quality while preserving sample diversity.

</details>


### [177] [One More Time: Revisiting Neural Quantum States from a Reinforcement Learning Perspective](https://arxiv.org/abs/2607.02292)
*Juan Agustín Duque,Sergio García Heredia,Vinicius Hernandes,Eliška Greplová,Thomas Spriggs,Aaron Courville,Anna Dawid*

Main category: cs.LG

TL;DR: Proximal Wavefunction Optimization (PWO) is a trust-region algorithm for training autoregressive neural quantum states that combines scalability with theoretical guarantees, improving stability and convergence over existing methods and scaling to billion-parameter models.


<details>
  <summary>Details</summary>
Motivation: Autoregressive neural quantum states enable exact sampling but current optimization methods are inadequate: Adam ignores function space geometry, while stochastic reconfiguration is costly and numerically fragile for large models.

Method: PWO frames variational energy minimization as advantage policy-gradient optimization over Born distribution. It uses trust-region approach with proximal constraints: clipping probability-ratio changes in amplitude channel and phase increments in phase channel, avoiding explicit matrix inversion and reusing samples.

Result: PWO improves stability and wall-clock convergence over Adam, minSR, and SPRING across Ising and frustrated J1-J2 spin systems in 1D and 2D. Successfully fine-tuned a 1.5B-parameter RWKV-7 model, scaling NQS optimization three orders of magnitude beyond prior work.

Conclusion: PWO provides a principled, scalable trust-region algorithm for neural quantum state optimization that combines first-order scalability with theoretical guarantees, enabling training of billion-parameter quantum wavefunction models.

Abstract: Neural quantum states (NQS) provide a flexible and scalable framework for approximating quantum many-body wavefunctions. Among NQS parameterizations, autoregressive models are especially attractive because they enable exact, independent sampling from the Born distribution, avoiding the autocorrelation and mixing issues of Markov chain methods. Yet their optimization remains comparatively underexplored: Adam is a scalable method but ignores function space geometry, while stochastic reconfiguration is principled but costly and numerically fragile in large models. To address this gap, we show that variational energy minimization can be viewed as an advantage policy-gradient problem over the Born distribution, motivating trust-region optimization for NQS training. We introduce Proximal Wavefunction Optimization (PWO), a principled trust-region algorithm that clips probability-ratio changes in the amplitude channel and phase increments in the phase channel. PWO avoids explicit matrix inversion, reuses samples across multiple updates, and combines the scalability of first-order optimization with theoretical guarantees. Across Ising and frustrated $J_1$-$J_2$ one- and two-dimensional spin systems, PWO improves stability and wall-clock convergence over Adam, minSR, and SPRING. Finally, we fine-tune a $1.5$B-parameter RWKV-7 model, demonstrating NQS optimization at a scale over three orders of magnitude beyond prior work.

</details>


### [178] [Self-Gating Attention for Efficient Time Series Forecasting](https://arxiv.org/abs/2607.02344)
*Dezheng Wang,Tong Chen,Wei Yuan,Congyan Chen,Shihua Li,Hongzhi Yin*

Main category: cs.LG

TL;DR: Self-Gating Attention (SGA) is a lightweight attention mechanism for time series forecasting that replaces standard self-attention with shared learnable matrices and input-dependent residuals, achieving linear complexity while maintaining competitive performance.


<details>
  <summary>Details</summary>
Motivation: Standard self-attention in transformers for time series forecasting has quadratic time and memory complexity with respect to look-back length, which limits deployment in resource-constrained or high-throughput systems. The authors observed that attention maps in time series forecasting often contain redundant patterns across timestamps, related to repeated temporal patterns and stable correlations in real-world data.

Method: Proposed Self-Gating Attention (SGA), a plug-and-play attention mechanism that represents attention scores with a shared learnable matrix and an input-dependent residual component. The shared matrix captures common attention patterns, while the residual captures input-dependent variations. This avoids query and key projections, leading to linear time and score-matrix memory complexity. The method was integrated into several forecasting backbones.

Result: Tested on nine public datasets covering electricity, finance, weather, medical monitoring, human activity, and climate records. SGA improved inference efficiency on benchmarks while maintaining competitive forecasting performance compared to standard self-attention and lightweight attention variants. Provides deployment-oriented evidence.

Conclusion: SGA offers an efficient alternative to standard self-attention for time series forecasting, achieving linear complexity by exploiting redundant patterns in attention maps while preserving competitive accuracy, making it suitable for resource-constrained deployment scenarios.

Abstract: Transformer architectures have shown strong potential in time series forecasting, where multi-head self-attention is widely used to capture temporal dependencies across historical timestamps. However, standard self-attention has quadratic time and memory complexity with respect to the look-back length. This cost may limit its use in resource-constrained or high-throughput forecasting systems, where fast and memory-efficient inference is important. Through qualitative and quantitative analyses, we observe that self-attention maps in time series forecasting often contain redundant patterns across different timestamps. This phenomenon can be related to the repeated temporal patterns and relatively stable temporal correlations in many real-world time series. Motivated by this observation, we propose Self-Gating Attention (SGA), a plug-and-play attention mechanism that represents the attention score with a shared learnable matrix and an input-dependent residual component. The shared matrix captures common attention patterns, while the residual component captures input-dependent variations. In this way, SGA avoids the query and key projections used in standard attention score computation, leading to linear time and score-matrix memory complexity with respect to the look-back length. We integrate SGA into several forecasting backbones and compare it with standard self-attention and lightweight attention variants on nine publicly available real-world datasets covering electricity, finance, weather, medical monitoring, human activity, and climate records. The results show that SGA improves inference efficiency on public benchmarks while maintaining competitive forecasting performance against state-of-the-art attention mechanisms. These benchmark results provide deployment-oriented evidence.

</details>


### [179] [DecompRL: Solving Harder Problems by Learning Modular Code Generation](https://arxiv.org/abs/2607.02390)
*Juliette Decugis,Fabian Gloeckle,Francis Bach,Taco Cohen,Gabriel Synnaeve*

Main category: cs.LG

TL;DR: DecompRL is a reinforcement learning algorithm that decomposes complex coding problems into smaller, reusable modules to overcome the limitations of traditional LLM sampling approaches.


<details>
  <summary>Details</summary>
Motivation: Current approaches for LLMs to solve difficult problems have limitations: repeated sampling is GPU-expensive, and reinforcement learning fails when the base policy has near-zero probability of producing correct solutions in large search spaces.

Method: DecompRL learns to decompose problems into smaller, independently solvable sub-functions whose implementations can be recombined. This modular approach allows for generating k^n candidate solutions by recombining k implementations of n modules, shifting computation from expensive GPU inference to cheap CPU evaluation.

Result: DecompRL reduces GPU token cost by ~50x and outperforms standard and diversity-optimized RL baselines on LiveCodeBench and CodeContests with Qwen2.5-7B and Code World Model-32B, solving problems beyond 10^5 tokens per problem that standard generation cannot reach.

Conclusion: Rather than improving sampling or optimization, making tasks easier through decomposition and recombination is more effective for solving problems that LLMs currently cannot handle, enabling solutions in large search spaces where traditional approaches fail.

Abstract: How can Large Language Models (LLMs) solve problems they currently cannot? Repeated sampling scales test-time compute but GPU cost grows linearly with attempts, while reinforcement learning (RL) with verifiable rewards improves single-attempt accuracy at the expense of sample diversity. Both strategies ultimately fail when the base policy has near-zero probability of producing a correct solution: no amount of sampling or gradient signal can overcome a search space that is simply too large. We take a different approach: rather than sampling harder, we make the task easier by decomposing problems into smaller, independently solvable sub-functions whose implementations can be recombined. Since off-the-shelf models are not trained for this modular generation, we introduce DecompRL, an RL algorithm that explicitly learns to decompose and implement hierarchical code structures. Recombining $k$ implementations of $n$ modules yields up to $k^{n}$ candidate solutions, shifting the bottleneck from GPU inference to cheap CPU evaluation and cutting GPU token cost by $\sim$50$\times$. On LiveCodeBench and CodeContests (Qwen~2.5~7B, Code World Model~32B), DecompRL outperforms standard and diversity-optimized RL baselines beyond $10^5$ tokens per problem, solving problems that standard generation cannot reach.

</details>


### [180] [Neuron-Aware Active Few-Shot Learning for LLMs](https://arxiv.org/abs/2607.02423)
*Zhuowei Chen,Liwei Chen,Christian Schunn,Raquel Coelho,Xiang Lorraine Li*

Main category: cs.LG

TL;DR: NeuFS proposes a neuron-aware active few-shot learning framework that selects samples based on internal neuron activation patterns rather than output-level signals, improving sample selection for few-shot learning with LLMs.


<details>
  <summary>Details</summary>
Motivation: Existing active few-shot learning methods rely on output-level signals like predictive entropy or semantic similarities, which overlook models' internal dynamics that could identify specific knowledge gaps. There's a need to leverage internal neuron activations for more principled sample selection.

Method: NeuFS uses neuron activation patterns to represent samples directly and employs a dual-criteria selection strategy: (1) ensures sample diversity using neuron patterns for broader coverage, and (2) prioritizes challenging samples that LLMs tend to hallucinate by quantifying neuron consensus.

Result: Experiments on three datasets show NeuFS excels in both reasoning and text classification tasks, outperforming existing AFSL baselines. Ablation studies confirm internal neuron activations provide more principled and effective selection signals than external embeddings.

Conclusion: The proposed NeuFS framework successfully bridges the gap by shifting selection from output-level proxies to models' internal dynamics, demonstrating that neuron activation patterns offer superior sample selection for active few-shot learning with LLMs.

Abstract: Active Few-Shot Learning (AFSL) adapts LLMs to specialized domains by identifying the most valuable unlabeled samples for annotation and use as few-shot demonstrations, effectively reducing human annotation costs while promoting high performance. However, existing methods typically rely on output-level signals for sample identification, such as predictive entropy or semantic similarities with test-time data based on external embeddings, which often overlook models' internal dynamics, which could pinpoint specific knowledge gaps. To bridge this gap, we propose NeuFS, a Neuron-Aware Active Few-Shot Learning framework that shifts the selection paradigm from output-level proxies to models' internal dynamics. NeuFS utilizes neuron activation patterns to represent sample directly, and includes a dual-criteria selection strategy that: (1) ensures few-shot sample diversity with neuron patterns for broader example coverage, while (2) prioritizing on identifying informative and challenging few-shot samples LLMs tend to hallucinate by quantifying neuron consensus. Experiments on three datasets demonstrate that NeuFS excels in both reasoning and text classification tasks, outperforming existing AFSL baselines. Ablation studies further highlight that internal neuron activations provide a more principled and effective selection signal than external embeddings, validating the superiority of the proposed NeuFS.

</details>


### [181] [QFedAgent: Quantum-Enhanced Personalized Federated Learning for Multi-Agent Activity Recognition](https://arxiv.org/abs/2607.02426)
*Quoc Bao Phan,Tuy Tan Nguyen*

Main category: cs.LG

TL;DR: Quantum-classical federated learning framework for multi-agent activity recognition that uses variational quantum circuits for sensor fusion with 10x parameter reduction compared to classical methods.


<details>
  <summary>Details</summary>
Motivation: Federated learning for robotic sensing faces challenges with heterogeneous, non-IID multimodal sensor data that degrades conventional FL algorithms, while classical fusion methods incur high parameter overhead and communication costs.

Method: Proposes QFedAgent, a hybrid quantum-classical personalized FL framework with variational quantum circuit fusion module that models accelerometer-gyroscope interactions through quantum state encoding and entanglement, using only 72 quantum rotation parameters instead of 33K classical parameters.

Result: Achieves 97.7% mean test accuracy on OPPORTUNITY dataset under subject-based non-IID partitions, demonstrating that parameter-efficient quantum fusion remains competitive with conventional federated baselines while reducing total parameters by approximately 10x.

Conclusion: Quantum circuit fusion offers a promising approach for efficient multi-agent federated learning with significantly reduced parameter overhead while maintaining competitive accuracy for privacy-sensitive robotic sensing applications.

Abstract: Federated learning (FL) enables collaborative model training across distributed devices without sharing raw data, making it suitable for privacy-sensitive robotic sensing applications. However, multi-agent systems generate heterogeneous and non-independent and identically distributed (non-IID) multimodal sensor streams that degrade conventional FL algorithms, while classical fusion modules introduce substantial parameter overhead and communication cost. This paper proposes QFedAgent, a hybrid quantum-classical personalized FL framework for multi-agent activity recognition. The approach integrates a variational quantum circuit fusion module that models accelerometer--gyroscope interactions through quantum state encoding and entanglement, requiring only 72 quantum rotation parameters versus 33K in classical multi-layer perceptron-based fusion, achieving approximately 10x total parameter reduction. Experiments on the OPPORTUNITY dataset under subject-based non-IID partitions demonstrate 97.7% mean test accuracy, confirming that parameter-efficient quantum fusion remains competitive with conventional federated baselines.

</details>


### [182] [Extreme Adaptive Transformer for Time Series Forecasting](https://arxiv.org/abs/2607.02437)
*Sanjeev Shrestha,Hui Liu,Yifan Zhang*

Main category: cs.LG

TL;DR: Exformer: A Transformer-based time series forecasting model with extreme-adaptive attention that explicitly models rare extreme events in hydrologic streamflow data, outperforming state-of-the-art baselines.


<details>
  <summary>Details</summary>
Motivation: Time series forecasting faces challenges with rare but critical extreme events, especially in hydrology where skewed streamflow distributions with extreme peaks significantly impact flood monitoring and water management. Standard Transformer models treat all time points uniformly and underrepresent rare extreme patterns.

Method: Proposes Extreme-Adaptive Transformer (Exformer) with an extreme-adaptive attention mechanism consisting of three sparse components: Local (short-term dependencies), Stride (periodic dependencies), and Extreme (selectively models event-aware dependencies between normal and extreme streamflow patterns).

Result: Exformer achieves superior 3-day forecasting performance compared to state-of-the-art baselines on four real-world hydrologic streamflow datasets, demonstrating improved forecasting capacity for imbalanced time series with rare consequential events.

Conclusion: Explicitly incorporating extreme-aware attention mechanisms improves Transformer models' forecasting ability for time series with imbalanced distributions containing rare but impactful extreme events, addressing a critical need in hydrologic forecasting applications.

Abstract: Time series forecasting remains challenging when the underlying data contain rare but critical extreme events. This issue is particularly important in hydrologic forecasting, where streamflow distributions are often highly skewed and extreme peaks can have substantial impacts on flood monitoring, water resource management, and early warning systems. Although Transformer-based forecasting models have achieved strong performance by modeling long-range temporal dependencies, they typically treat all time points uniformly and may therefore underrepresent rare extreme patterns. In this paper, we propose the Extreme-Adaptive Transformer (Exformer), a forecasting framework designed to explicitly model temporal dependencies involving both normal and extreme events. Exformer introduces an extreme-adaptive attention mechanism composed of three sparse components: Local, Stride, and Extreme. The Local and Stride components capture short-term and periodic temporal dependencies, respectively, while the Extreme component selectively models event-aware dependencies between normal and extreme streamflow patterns. Experiments on four real-world hydrologic streamflow datasets show that Exformer achieves superior 3-day forecasting performance compared with state-of-the-art baselines. Our findings demonstrate that explicitly incorporating extreme-aware attention improves the forecasting capacity of Transformer models on imbalanced time series with rare but consequential events.

</details>


### [183] [Understanding the Robustness of Distributed Self-Supervised Learning Frameworks Against Non-IID Data](https://arxiv.org/abs/2607.02447)
*Xuanyu Chen,Nan Yang,Shuai Wang,Dong Yuan*

Main category: cs.LG

TL;DR: The paper theoretically analyzes robustness of distributed self-supervised learning (D-SSL) under non-IID data, finding Masked Image Modeling is more robust than Contrastive Learning, and that federated learning is no less robust than decentralized learning, with practical validation through a new MAR loss.


<details>
  <summary>Details</summary>
Motivation: Limited theoretical understanding exists about how different distributed self-supervised learning (D-SSL) frameworks respond to data heterogeneity challenges, particularly in non-IID settings with decentralized unlabeled data.

Method: The paper presents rigorous theoretical analysis of D-SSL robustness under non-IID settings, comparing Masked Image Modeling (MIM) vs. Contrastive Learning (CL), and introduces MAR loss - a refinement of MIM with local-to-global alignment regularization.

Result: Results show: 1) MIM pre-training is inherently more robust to heterogeneous data than CL, 2) Robustness of decentralized SSL increases with average network connectivity, implying FL is no less robust than DecL, 3) MAR loss effectiveness validated through extensive experiments.

Conclusion: The theoretical findings provide solid foundation for future D-SSL algorithm design, with MAR loss serving as practical application demonstrating the value of their theoretical insights for improving robustness in distributed self-supervised learning.

Abstract: Recent research has introduced distributed self-supervised learning (D-SSL) approaches to leverage vast amounts of unlabeled decentralized data. However, D-SSL faces the critical challenge of data heterogeneity, and there is limited theoretical understanding of how different D-SSL frameworks respond to this challenge. To fill this gap, we present a rigorous theoretical analysis of the robustness of D-SSL frameworks under non-IID (non-independent and identically distributed) settings. Our results show that pre-training with Masked Image Modeling (MIM) is inherently more robust to heterogeneous data than Contrastive Learning (CL), and that the robustness of decentralized SSL increases with average network connectivity, implying that federated learning (FL) is no less robust than decentralized learning (DecL). These findings provide a solid theoretical foundation for guiding the design of future D-SSL algorithms. To further illustrate the practical implications of our theory, we introduce MAR loss, a refinement of the MIM objective with local-to-global alignment regularization. Extensive experiments across model architectures and distributed settings validate our theoretical insights, and additionally confirm the effectiveness of MAR loss as an application of our analysis.

</details>


### [184] [Neuron-Aware Data Selection for Annotation-Free LLM Self-Distillation](https://arxiv.org/abs/2607.02460)
*Zhuowei Chen,Xiang Lorraine Li*

Main category: cs.LG

TL;DR: Proposes Neuron-OPSD, an annotation-free self-distillation method using neuron activations to guide training data selection and teacher construction, improving specialized-domain performance without ground-truth labels.


<details>
  <summary>Details</summary>
Motivation: Post-training LLMs in specialized domains is challenging due to costly expert annotations. Existing annotation-free methods have limitations: SFT-/GRPO-based variants suffer out-of-domain degradation, while reward-based RL inflates calibration error.

Method: Neuron On-Policy Self-Distillation (Neuron-OPSD) uses internal neuron activations to guide both training-data selection and teacher context construction. The model is trained via on-policy distillation from teacher distribution without requiring ground-truth labels.

Result: Across specialized-domain benchmarks, Neuron-OPSD improves in-domain task performance while preserving cross-domain generalization and mitigating calibration collapse compared to prior annotation-free baselines.

Conclusion: The framework is valuable for settings where online interaction or external supervision is costly/infeasible, offering a data-centric approach distinct from offline RL methods that rely on logged reward-labeled trajectories.

Abstract: Post-training large language models (LLMs) without real-world interaction feedback or human-labeled supervision remains challenging, particularly in specialized domains where expert annotations are costly to obtain. Recent annotation-free self-evolution methods address this by using the model's own outputs as supervision signals, constructing a teacher via additional context and aggregating predictions across multiple rollouts through majority voting to produce pseudo-labels. However, these approaches are not without drawbacks: SFT- and GRPO-based variants suffer out-of-domain performance degradation, while reward-based on-policy RL inflates calibration error. In this paper, we propose Neuron On-Policy Self-Distillation (Neuron-OPSD), a data-centric framework for annotation-free self-distillation that leverages internal neuron activations to guide both training-data selection and teacher context construction. The model is then trained via on-policy distillation from the teacher distribution, requiring no ground-truth labels at any stage. Across specialized-domain benchmarks, Neuron-OPSD improves in-domain task performance while preserving cross-domain generalization and mitigating calibration collapse over prior annotation-free baselines. This framework is particularly relevant to settings where online interaction or external supervision is costly or infeasible, and is conceptually distinct from offline RL approaches that rely on logged, reward-labeled trajectories.

</details>


### [185] [Beyond Adam: SOAP and Muon for Faster, Label-Efficient Training of Machine Learning Interatomic Potentials](https://arxiv.org/abs/2607.02499)
*Gil Harari,Yoel Zimmermann,Ola Tangen Kulseng,Laura Zichi,Chuin Wei Tan,Marc L. Descoteaux,Boris Kozinsky*

Main category: cs.LG

TL;DR: Matrix-structured optimizers (SOAP, SOAP-Muon) outperform Adam for training MLIPs, improving convergence speed and accuracy, especially with partial force supervision.


<details>
  <summary>Details</summary>
Motivation: While MLIP architectures and datasets have advanced, optimizer choice has remained largely unexplored, defaulting to Adam and its variants despite potential for improvement.

Method: Implemented and systematically compared matrix-structured optimizers (Muon, SOAP, and hybrid SOAP-Muon) for training NequIP and Allegro MLIP models.

Result: SOAP and SOAP-Muon substantially outperform Adam in convergence speed and final accuracy, particularly under partial force supervision; Muon shows only partial gains relative to Adam.

Conclusion: Optimizer choice is an overlooked but impactful design axis for MLIPs, with matrix-structured optimizers offering significant performance improvements.

Abstract: Machine learning interatomic potentials (MLIPs) have become a hallmark of AI for scientific simulation. While efforts on new architectures and datasets have led to increasingly accurate and general models, the choice of optimizer for training has largely remained unexplored, defaulting to Adam and its variants in the community. Here, we implement and systematically compare a class of recently proposed matrix-structured optimizers, including Muon, SOAP, and the hybrid SOAP-Muon, for training NequIP and Allegro MLIP models. We find that these optimizers can substantially outperform Adam in both convergence speed and final accuracy. SOAP and SOAP-Muon emerge as robust and consistently strong methods, while Muon only provides partial gains relative to Adam. The improvements are particularly pronounced under partial force supervision. Our results indicate that optimizer choice is an overlooked yet impactful design axis for MLIPs.

</details>


### [186] [DemoPSD: Disagreement-Modulated Policy Self-Distillation](https://arxiv.org/abs/2607.02502)
*Yunhe Li,Hao Shi,Wenhao Liu,Mengzhe Ruan,Hanxu Hou,Zhongxiang Dai,Shuang Qiu,Linqi Song*

Main category: cs.LG

TL;DR: DemoPSD is a novel framework for on-policy self-distillation that addresses privileged information leakage and exploration issues by selectively adopting teacher guidance through a reverse-KL barycenter target.


<details>
  <summary>Details</summary>
Motivation: On-policy self-distillation (OPSD) suffers from privileged information leakage (where students encode answer-dependent shortcuts unavailable at test time) and suppresses exploration while hurting cross-domain generalization due to dense token-level supervision from teachers with privileged information.

Method: DemoPSD uses selective adoption of teacher guidance by steering students toward a reverse-KL barycenter target - a weighted geometric combination of teacher and student distributions. It measures distribution differences and adaptively controls blending at each token position.

Result: DemoPSD achieves provable leakage attenuation (mitigating privileged information leakage) and exploration preservation. Experiments on SciKnowEval across four scientific fields show it outperforms GRPO and SDPO while maintaining higher training entropy and robustly generalizing to out-of-distribution GPQA benchmarks.

Conclusion: DemoPSD effectively addresses fundamental issues in OPSD by balancing teacher guidance with student reasoning capacity through selective adoption, leading to better performance and generalization while preserving exploration.

Abstract: On-policy self-distillation (OPSD) has emerged as a practical method for training large language models (LLMs) to reason, where a single model acts as both the teacher and the student with different levels of information access. However, recent studies have found that the teacher's dense token-level supervision, conditioned on privileged information, can lead to overfitting to in-domain patterns, suppress exploration, and hurt cross-domain generalization, while also introducing a more fundamental issue: *privileged information leakage*, where the student encodes answer-dependent shortcuts that are unavailable at test time. We introduce **DemoPSD**, a novel framework that resolves such problems through the idea of *selective adoption of teacher guidance*. Instead of fitting the full teacher distribution, DemoPSD steers the student toward a *reverse-KL barycenter target*, a weighted geometric combination of the teacher and student distributions, that naturally balances learning from the teacher with preserving the student's own reasoning capacity. We measure the difference between their distributions and use such a discrepancy to adaptively control the blending at each token position. We provably show that DemoPSD achieves **(1)** *leakage attenuation*, i.e., effective mitigation of privileged information leakage; and **(2)** *exploration preservation*, i.e., preservation of exploration capacity under dense token-level distillation. Extensive experiments on SciKnowEval across four scientific fields show that DemoPSD outperforms both GRPO and SDPO while maintaining higher training entropy and robustly generalizing to out-of-distribution GPQA benchmarks.

</details>


### [187] [Program-as-Weights: A Programming Paradigm for Fuzzy Functions](https://arxiv.org/abs/2607.02512)
*Wentao Zhang,Liliana Hotsko,Woojeong Kim,Pengyu Nie,Stuart Shieber,Yuntian Deng*

Main category: cs.LG

TL;DR: A novel "fuzzy-function programming" approach called Program-as-Weights (PAW) that compiles natural language specifications into compact neural artifacts, enabling cheap, local execution of complex programming tasks instead of using expensive LLM APIs.


<details>
  <summary>Details</summary>
Motivation: Many everyday programming tasks (like log alerting, JSON repair, search ranking) resist clean rule-based implementation but are increasingly outsourced to LLM APIs, which comes with high costs in locality, reproducibility, and price.

Method: PAW uses a 4B compiler trained on FuzzyBench (10M-example dataset) to emit parameter-efficient adapters for a frozen, lightweight interpreter. The compiler transforms natural language specifications into neural artifacts that can be executed locally.

Result: A 0.6B Qwen3 interpreter executing PAW programs matches the performance of directly prompting Qwen3-32B while using 1/50th of the inference memory and running at 30 tokens/s on MacBook M3, enabling cheap, offline function applications.

Conclusion: PAW reframes foundation models from per-input problem solvers into tool builders - invoked once per function definition to produce small reusable artifacts, making subsequent function calls cheap and offline, addressing current limitations of LLM API usage.

Abstract: Many everyday programming tasks resist clean rule-based implementation, such as alerting on important log lines, repairing malformed JSON, or ranking search results by intent, and are increasingly outsourced to large language model APIs at the cost of locality, reproducibility, and price. We propose fuzzy-function programming: compiling such a function from a natural-language specification into a compact, locally-executable neural artifact. We instantiate this paradigm with Program-as-Weights (PAW), in which a 4B compiler trained on FuzzyBench, a 10M-example dataset we release, emits parameter-efficient adapters for a frozen, lightweight interpreter. A 0.6B Qwen3 interpreter executing PAW programs matches the performance of direct prompting of Qwen3-32B, while using roughly one fiftieth of the inference memory and running at 30 tokens/s on a MacBook M3. PAW reframes the foundation model from a per-input problem solver into a tool builder: invoked once per function definition, it produces a small reusable artifact whose subsequent calls per function application are cheap and offline.

</details>


<div id='cs.MA'></div>

# cs.MA [[Back]](#toc)

### [188] [Cache Merging as a Convergent Replicated State for Multi-Agent Latent Reasoning](https://arxiv.org/abs/2607.01308)
*Carlos Baquero,Luís Brito*

Main category: cs.MA

TL;DR: CanonicalMerge is a commutative cache merging method for multi-agent reasoning that fixes layout by content ordering, enabling byte-identical merged caches regardless of input permutation.


<details>
  <summary>Details</summary>
Motivation: Existing BagMerge approach for multi-agent latent reasoning is non-commutative, with unpredictable best input ordering that varies with regime, budget, and model scale, creating unreliable performance.

Method: Propose CanonicalMerge that orders caches by mean K-norm at a middle layer, making merged cache byte-identical under any input permutation. Also separate replicated state from decode-time layout using content-addressed latent fragments with set union merge (CvRDT).

Result: CanonicalMerge matches best BagMerge ordering in all regime-by-budget-by-ordering cells without knowing optimal order, trades small insignificant accuracy loss for unconditional structural guarantee. Outperforms PackLLM baseline by 45 points on HotpotQA, distinguishing cache-level merging from output-level fusion.

Conclusion: The approach creates a convergent replicated state for multi-agent reasoning with commutative, associative properties, though at k>2 it transports latent traces but doesn't compose them, motivating future work on latent composition.

Abstract: Multi-agent latent reasoning composes agents' KV-caches into one context for a final agent. Prior work (Agent Primitives) does this by concatenating caches along the sequence axis with RoPE re-encoding, which we call BagMerge. BagMerge is non-commutative, and the best input ordering is unpredictable, shifting with the regime, the latent-step budget, and the model scale. We make this exchange a convergent replicated state. First, CanonicalMerge fixes the layout by content: ordering caches by mean K-norm at a middle layer renders the merged cache byte-identical under any input permutation, verified algorithmically (arity N<=5) and bit-for-bit on real Qwen3-1.7B and 4B state. Second, we separate the replicated state from decode-time layout: the state is a set of content-addressed latent fragments whose merge is set union, a state-based CvRDT (commutative, associative, idempotent, absorbing), and CanonicalMerge is its deterministic render. Because the render is byte-equivalent, every N=2 accuracy number carries over unchanged and re-delivered duplicates are absorbed rather than re-concatenated. On a partitioned-reasoning benchmark, CanonicalMerge matches the best BagMerge ordering in every regime-by-budget-by-ordering cell without knowing which order is best, trading a small, statistically insignificant accuracy margin for an unconditional structural guarantee. The behaviour transfers to real multi-document QA (HotpotQA), while the closest training-free output-fusion baseline (PackLLM) loses by 45 points at matched budget, placing cache-level merging in a regime distinct from output-level fusion. Finally, at k>2 the approach transports and colocates latent traces but does not by itself compose them, which we characterize to motivate future work.

</details>


### [189] [Simulation Based Reward Function Validation for Multi-Agent On Orbit Inspection](https://arxiv.org/abs/2607.01367)
*Patrick Quinn,Bala Prenith Reddy Gopu,George M. Nehma,Madhur Tiwari*

Main category: cs.MA

TL;DR: MARL-based inspection spacecraft control with a generalized reward function that allows flexible image collection timing, informed by 3D reconstruction analysis.


<details>
  <summary>Details</summary>
Motivation: Previous MARL approaches for spacecraft inspection used reward functions limited to predetermined inspection points. There's a need for more flexible, generalized reward functions that better reflect real-world inspection quality based on 3D reconstruction analysis.

Method: Developed a generalized reward function for MARL inspection tasks by analyzing 3D reconstructions of inspected objects in orbit. The reward function can evaluate any number of images at arbitrary locations, allowing trained agents complete control over when to collect images.

Result: The approach provides insights into best practices for MARL inspection tasks specifically and yields key takeaways applicable to broader inspection tasks beyond the MARL context.

Conclusion: A generalized, flexible reward function based on 3D reconstruction analysis improves MARL-based spacecraft inspection by allowing adaptive image collection timing and provides broader insights for inspection tasks in general.

Abstract: A proposed method for the control of groups of inspection spacecraft is Multi-Agent Reinforcement Learning (MARL). While MARL has already been employed for this purpose in previous work, the reward functions used focus on reaching a finite set of predetermined inspection points around the target. In this work, we study and develop a generalized reward function for the MARL inspection task informed by the analysis of 3D reconstructions of inspected objects in orbit. Because the reward function is generalized such that any number of images at arbitrary locations may evaluated, we also allow trained agents to have complete control over when images are collected. With this approach, we gather insights into best practices for not only the specific MARL inspection task, but also gain key takeaways informative to the broader inspection task outside of a MARL context.

</details>


### [190] [Congestion-Based Slot Pricing in a Railway Auction Game](https://arxiv.org/abs/2607.01822)
*Bill Roungas,Sebastiaan Meijer*

Main category: cs.MA

TL;DR: Multi-agent railway slot allocation system with congestion pricing and asymmetric corrective adjustments shows partial success in mitigating large operator dominance but reveals complex strategic behaviors beyond simple profit maximization.


<details>
  <summary>Details</summary>
Motivation: To study allocation of congested resources (railway slots) among heterogeneous strategic agents in deregulated markets, addressing the problem of strategic dominance by large operators and testing mechanisms to ensure fair competition.

Method: Created a multi-agent system combining congestion-based base pricing (increases with aggregate demand) with asymmetric corrective adjustments (penalizes highest requestors, rewards lowest requestors). Implemented as repeated game with incomplete information using real-time web-based environment with human participants (domain experts) controlling agents.

Result: Congestion mechanism responded to aggregate demand as designed and corrective incentives were triggered. However, large operators persisted with high-request strategies despite penalties, showing corrective pricing is necessary but insufficient to neutralize strategic dominance. Participants' decisions were driven by agent roles and revealed strategic motives like preserving market presence and raising rivals' costs alongside profit maximization.

Conclusion: Asymmetric corrective pricing helps but is insufficient alone to address strategic dominance in multi-agent settings with budget asymmetry. Future work needs analytical validation and larger-scale experiments to better understand complex strategic behaviors and improve mechanism design.

Abstract: We present a multi-agent system for studying the allocation of discrete, congested resources among heterogeneous strategic agents, motivated by the problem of railway slot allocation under deregulation. Multiple operator-agents, differing in size and capacity, interact through a shared auction mechanism over repeated rounds under time-constrained decision-making. The mechanism combines a congestion-based base price that increases with aggregate demand with an asymmetric corrective adjustment that penalises the agent requesting the most slots and rewards the agent requesting the fewest, and is designed to mitigate strategic dominance by large agents while preserving transparency and congestion sensitivity. We formulate the interaction as a repeated game with incomplete information and implement the system as a real-time, web-based multi-agent environment in which human participants control individual agents and observe live marginal-cost and competitor feedback.
  We report exploratory observations from two structured sessions with domain experts acting as operator-agents. The congestion mechanism responds to aggregate demand as designed and the corrective incentives are actively triggered, but agents representing large operators persist with high-request strategies despite the penalty, suggesting that corrective pricing is necessary but not sufficient to neutralise strategic dominance in this multi-agent setting. A post-session debrief indicates that participants' decisions were driven by the assumed agent role rather than personal disposition, and provides qualitative support for strategic motives, such as preserving market presence and raising rivals' costs, operating alongside short-term profit maximisation. We discuss implications for multi-agent mechanism design under asymmetric budgets and outline directions for analytical validation and larger-scale multi-agent experiments.

</details>


### [191] [CausalSteward: An Agentic Divide-Conquer-Combine Copilot for Causal Discovery](https://arxiv.org/abs/2607.01936)
*Nicholas Tagliapietra,Gian Lorenzo Marchioni,Moritz Willig,Juergen Luettin,Lavdim Halilaj,Kristian Kersting*

Main category: cs.MA

TL;DR: CAST (CausalSTeward) is a human-in-the-loop multi-agent framework that uses divide-and-conquer to build large causal models by integrating prior knowledge with data-driven methods.


<details>
  <summary>Details</summary>
Motivation: Learning causal models from high-dimensional data is challenging due to causal identifiability issues when core assumptions are violated. While prior knowledge contains valuable causal information, effectively integrating it into causal discovery remains an open problem.

Method: CAST is a multi-agent collaborative system using divide-and-conquer: large variable clusters are iteratively partitioned and separately analyzed. It fuses prior knowledge with data-driven approaches using retrieval augmented generation and conditional independence tests.

Result: The framework enables interactive assembly of large causal models through human-in-the-loop collaboration, tackling high-dimensional causality that traditional methods struggle with.

Conclusion: The work examines capabilities/limitations of causal reasoning in multi-agent frameworks and demonstrates how human-in-the-loop approaches can contribute to accurate, trustworthy causal discovery results.

Abstract: Learning causal models from high-dimensional data is a significant challenge, particularly in real-world settings where violations of core assumptions lead to causal identifiability issues. Although massive amounts of prior knowledge are available, and contain valuable causal information, effectively integrating this knowledge into the causal discovery process remains an open problem. We introduce CausalSTeward (CAST), a novel human-in-the-loop framework for interactively assembling large causal models. CausalSteward is a multi-agent collaborative system that tackles high-dimensional causality through a divide-and-conquer approach where large clusters of variables are iteratively partitioned and then separately analyzed. Our framework fuses prior knowledge with a data-driven approach by using tailored tools such as retrieval augmented generation and conditional independence tests. Finally, we use this work to examine the capabilities and limitations of causal reasoning in multi-agent frameworks, and how the human-in-the-loop can contribute to accurate and trustworthy results.

</details>


### [192] [AgentsCAD: Automated Design for Manufacturing of FDM Parts via Multi-Agent LLM Reasoning and Geometric Feature Recognition](https://arxiv.org/abs/2607.02448)
*Emmanuel George,Christopher Keefe,Peter Pak,Amir Barati Farimani*

Main category: cs.MA

TL;DR: AgentsCAD is a multi-agent system using LLMs to automate Design for Additive Manufacturing (DFAM) by analyzing CAD geometry, detecting printability issues, and recommending modifications through a workflow that combines geometry parsing, graph-based analysis, and AI reasoning.


<details>
  <summary>Details</summary>
Motivation: FDM-printed parts often need DFAM modifications for printability and structural integrity, but current slicers can only detect defects without modifying the underlying geometry. There's a need to automate the transition from geometry analysis to intelligent design modifications.

Method: The system parses STEP files, detects overhangs above 45°, constructs face-adjacency topology graphs, optionally injects semantic feature labels from a GraphSAGE model trained on MFCAD++ data, uses Claude Sonnet for design reasoning to recommend modifications (reorientations, fillets, chamfers), and employs GPT-4o as a vision-language verifier to check rendered views.

Result: The system successfully demonstrated on a birdhouse model by correctly diagnosing overhangs, selecting appropriate defect mitigation strategies, and proposing physically valid corrections, producing both modified STEP files and human-readable reports.

Conclusion: AgentsCAD partially solves the geometry-to-language translation problem for LLM-driven CAD modification, demonstrating a viable approach to automating DFAM through multi-agent systems that bridge geometry parsing with AI reasoning.

Abstract: Parts manufactured with Fused Deposition Modeling (FDM) often require Design for Additive Manufacturing (DFAM) modifications to ensure printability, structural integrity, and reduced post-processing. Current slicers identify defects such as steep overhangs but are unable to modify the underlying geometry. This work presents AgentsCAD, a multi-agent system that bridges raw boundary-representation (B-Rep) geometry and Large Language Model (LLM) reasoning to automate targeted DFM. The workflow begins by parsing a STEP file. The agentic system detects overhangs above a 45°threshold, constructs a face-adjacency topology graph, and optionally injects semantic feature labels from a GraphSAGE model trained on MFCAD++ (59,665 parts), before dispatching a Claude Sonnet design-reasoning agent that recommends reorientations, fillets, chamfers, and similar modifications. A GPT-4o vision-language verifier inspects rendered views to confirm geometric integrity. Outputs include a modified STEP file and a human-readable report. A test case on a birdhouse model demonstrates that the system correctly diagnoses overhangs, selects appropriate defect mitigation strategies, and proposes physically valid corrections, partially solving the geometry-to-language translation problem central to LLM-driven CAD modification.

</details>


### [193] [Adoption and Ecosystem Health: A Longitudinal Analysis of Open-Source Multi-Agent Frameworks](https://arxiv.org/abs/2607.02453)
*Xi Zhang,Papi Menon,Vivian Chu,Koray Cosguner*

Main category: cs.MA

TL;DR: This paper analyzes 15 open-source AI agent frameworks, finding that GitHub star counts are unreliable popularity metrics; better evaluation criteria include contributor density, cross-ecosystem engagement, and contributor retention.


<details>
  <summary>Details</summary>
Motivation: The proliferation of open-source agentic frameworks since ChatGPT's launch has made framework selection challenging for engineering teams. Popularity signals like GitHub stars are often misleading, obscuring true ecosystem health and adoption quality.

Method: Analyzed 15 major open-source AI agent framework repositories from late 2022 to early 2026 using comprehensive data: 808,042 stars, 73,997 pull requests, 86,241 commits, and 987,330 user profiles. Measured ecosystem health across three dimensions: awareness, adoption, and retention, with contributor density as a key metric.

Result: 1) Star counts reflect hype cycles not meaningful adoption - AutoGPT gained 111,967 stars in a month but had low contributor density (9/1000 vs LangChain's 41); 2) High visibility doesn't equal engagement - MetaGPT/LangFlow had contributor density <5; 3) LangChain serves as shared infrastructure with 82.5% cross-ecosystem contributors; 4) Contributor retention drops steeply in first 30 days, stabilizes around 90 days.

Conclusion: Ecosystem health is better measured by contributor density, cross-ecosystem engagement, and contributor retention than star counts alone. These metrics provide engineering teams with more robust framework evaluation criteria beyond superficial popularity signals.

Abstract: Since ChatGPT's launch in November 2022, open-source agentic frameworks have proliferated, making framework selection important for engineering teams while obscured by popularity signals such as GitHub stars. This paper analyzes 15 major open-source AI agent framework repositories from late 2022 to early 2026, using 808,042 stars, 73,997 pull requests, 86,241 commits, and 987,330 user profiles to assess ecosystem health across awareness, adoption, and retention. Three findings emerge. First, headline popularity is unreliable. Star counts reflect hype cycles and inorganic activity. AutoGPT gained 111,967 stars in one month but converted fewer than 9 contributors per 1,000 stars, defined as contributor density in this research, compared with LangChain's 41. Lower-profile frameworks such as Pydantic-AI show higher contributor density, indicating deeper adoption. Second, mapping awareness against adoption shows that visibility and engagement diverge. MetaGPT and LangFlow have contributor density ratios below 5 even with their high visibility. Openai-agents-python's limited contributor base suggests institutional backing alone does not ensure community depth. By analyzing cross-framework contribution, we discover that LangChain functions as a shared infrastructure, attracting 82.5% of cross-ecosystem contributors. Third, retention drops most steeply in the first 30 days of initial contribution and stabilizes near 90 days. Overall, ecosystem health is better measured by contributor density, cross-ecosystem engagement, and retention than by stars alone. These metrics offer teams a more robust basis for framework evaluation.

</details>
